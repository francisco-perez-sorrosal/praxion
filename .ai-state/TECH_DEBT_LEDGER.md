# Technical Debt Ledger

<!-- Living, append-only ledger of grounded debt findings.
     Producers: verifier (per-change Phase 5/5.5), sentinel (repo-wide TD dimension).
     Consumers: systems-architect, implementation-planner, implementer, test-engineer, doc-engineer.
     Schema, owner-role heuristic, worktree-merge dedupe semantics, and lifecycle conventions
     are defined canonically in skills/software-planning/references/tech-debt-ledger.md
     (rules/swe/agent-intermediate-documents.md retains a 5-line summary + pointer).
     Do not duplicate the schema here — the skill reference is the single source of truth. -->

**Schema**: 14 row fields + 1 structural `dedup_key`. See [`skills/software-planning/references/tech-debt-ledger.md`](../skills/software-planning/references/tech-debt-ledger.md) § `TECH_DEBT_LEDGER.md` for field definitions, enum values, the owner-role heuristic, and the post-merge dedupe contract.

**Append new rows at the end of this active ledger.** Consumers update `status`, `resolved-by`, and `last-seen` in place; when status transitions to `resolved` or `wontfix`, the row migrates to the sibling [`TECH_DEBT_RESOLVED.md`](TECH_DEBT_RESOLVED.md) (cut + paste, in-commit or via `scripts/finalize_tech_debt_ledger.py`). The pair forms one logical namespace — see the rule for re-open semantics.

| id | severity | class | direction | location | goal-ref-type | goal-ref-value | source | first-seen | last-seen | owner-role | status | resolved-by | notes | dedup_key |
|----|----------|-------|-----------|----------|---------------|----------------|--------|------------|-----------|-----------|--------|-------------|-------|-----------|
| td-012 | important | complexity | code-to-goals | scripts/project_metrics/report.py | code-quality |  | sentinel | 2026-05-01 | 2026-05-17 | implementation-planner | open |  | Rank-2 hotspot (score=21,399): complexity=21, churn=1,019 over 90d; central reporting aggregator in active development; complexity trend warranted monitoring before it reached the 800-line ceiling. // 2026-05-13: re-severitied suggested→important and owner-role implementer→implementation-planner — `report.py` is now 929 lines, OVER the 800-line ceiling in `rules/swe/coding-style.md` it was being monitored toward (still rank-3 hotspot, score=21,399, in METRICS_REPORT_2026-05-13). Module split warranted — extract per-collector rendering subsections to a `report_sections/` package, or split aggregate/trend/hotspot rendering from the orchestration shell. Owner-role flipped per the "module restructuring → implementation-planner" heuristic, matching td-003 / td-004; revert to implementer if a modest extraction (vs. a full decomposition) turns out to suffice. | 62a324775090 |
| td-037 | suggested | complexity | code-to-goals | eval/src/praxion_evals/harness/families/family1_pipeline_fidelity.py | code-quality |  | verifier | 2026-05-26 | 2026-05-26 | implementer | open |  | [F-04 from praxion-self-eval-v1 verification] `_check_supersession_reciprocity` is 78 lines, exceeding the 50-line ceiling in `rules/swe/coding-style.md`. Refactor opportunity — extract helper functions for the symmetric-pair detection and back-link checking phases. Not blocking; v1 ships as-is. | 550d3cf99cc9 |
| td-054 | important | complexity | code-to-goals | scripts/finalize_adrs.py | code-quality |  | verifier | 2026-06-19 | 2026-06-19 | implementation-planner | open |  | Originally filed as td-039; renumbered to td-054 to resolve id collision, 2026-07-15. [W-01 from finalize-consumer-bugs verification] `scripts/finalize_adrs.py` is 996 lines, over the 800-line hard ceiling in `rules/swe/coding-style.md` (same class as td-012 for report.py). Pre-existing and NOT introduced by the consumer-bug fixes — those added ~140 lines but the file was already ~895 before. The shared repo-root resolver was already extracted to `scripts/_repo_root.py` this pass (retiring 5x duplication, −~45 lines), which did not cross the ceiling. Next cohesive extraction: the fragment-filename parser (`parse_fragment_filename` + `_split_user_branch_slug` + `_discover_branch_from_siblings` + `_parse_via_siblings` + `_collect_peer_*` + `_dash_aligned_common_prefix` + `_read_draft_branch` + `_sanitize`, ~200 lines) into a `finalize_adrs_parsing.py` sibling — would bring the file under ~800. Deferred deliberately: that parser has prior-bug history (td-017 hyphenated-branch ambiguity) and couples to the module `_git`/`REPO_ROOT` globals + frontmatter patterns + logger, so a clean extraction needs care (pass git-runner/patterns as params or move them too) and its own focused pass rather than a tail-of-session rush. Owner-role implementation-planner per the "module restructuring → implementation-planner" heuristic (matches td-012). | b12531a53419 |
| td-056 | suggested | duplication | code-to-goals | scripts/refresh_claude_blocks.py | code-quality |  | verifier | 2026-07-16 | 2026-07-16 | implementer | open |  | [verifier, block-refresh-mechanism] `refresh_claude_blocks.py::_find_block_span` (lines 244-259) re-implements `canonical_block_identity.extract_live_body`'s heading->next-`## `-or-EOF boundary logic verbatim rather than calling a shared primitive. Traced byte-identical today; the duplication is a disclosed, scoped trade-off (the apply-mode step's file scope was `refresh_claude_blocks.py` only, and the shared module returns text not line-indices). Residual risk: nothing enforces the two boundary functions stay in sync, so a future edit to one (e.g. an H3-nesting or fenced-block rule) could silently diverge classify-time extraction from apply-time span computation. Fix: expose a shared boundary-index primitive from `canonical_block_identity.py` that both `extract_live_body` and `_find_block_span` call. Secondary (same fix direction, lower priority): `run_apply` re-reads the target `CLAUDE.md` independently of the read `_classify_all` already did -- a TOCTOU double-read with zero realistic trigger in the single-threaded CLI path; thread the already-read text through. Not blocking; refuse-to-clobber safety is unaffected (failure direction stays safe). | 6d6738daf6b6 |
| td-057 | suggested | maintenance | code-to-goals | .github/workflows/reusable-ci-autofix.yml | code-quality |  | verifier | 2026-07-26 | 2026-07-26 | implementer | open |  | [verifier, pr-dependabot-autofix] The autofix workflows (reusable-ci-autofix.yml, ci-autofix.yml, issue-autofix.yml) pin claude-code-action@51ea8ea (v1); the v1 tag has since moved ~60 releases to v1.0.183 (be7b93b). P3a deliberately kept the audited 51ea8ea for consistency + surgical scope (a fleet-wide bump is out of P3a's scope; light-review + verifier both confirmed keeping the audited pin is correct). Owed: a deliberate, audited lockstep bump of claude-code-action across all autofix workflows to a current pinned SHA, verifying no breaking changes. Not urgent — the audited pin works. | 52d4569e37a7 |
| td-058 | important | complexity | code-to-goals | codex/config/export-codex-rules-bridge.py | code-quality |  | sentinel | 2026-07-29 | 2026-07-29 | implementation-planner | open |  | Rank-3 hotspot (score=39,842): complexity=22, churn=1,811 over 90d; file is 1,435 lines, nearly double the 800-line ceiling in `rules/swe/coding-style.md`. High, sustained churn (codex-bridge export logic under active iteration) combined with size well past the ceiling signals a module-split candidate rather than routine growth — same class as td-012/td-054. No existing ledger row references this file. Owner-role implementation-planner per the "module restructuring required" heuristic (matches td-012/td-054 precedent). | 0eeb47c32516 |
| td-059 | important | coverage-gap | code-to-goals | scripts/aac_fence_validator.py, scripts/spec_drift.py | code-quality |  | sentinel | 2026-07-29 | 2026-07-29 | test-engineer | open |  | Both files sit well under the project coverage floor (70%): `spec_drift.py` 16% (per METRICS_REPORT_2026-07-29_22-11-31), `aac_fence_validator.py` 31%. Elevated to `important` (not `suggested`) because both are gate-liveness-critical — `spec_drift.py` backs sentinel SH07 (spec-drift detection) and `aac_fence_validator.py` backs sentinel AC10 (fence integrity); per gate-liveness.md a thinly-tested detector risks a silent false-PASS on the very defects it exists to catch. Four additional scripts sit modestly under floor (`check_paths_syntax.py` 56%, `prune_reports.py` 57%, `project_metrics/cli.py` 60%, `self_healing_metrics.py` 66%) — not filed this pass to avoid ledger noise; re-evaluate if they drift further or gain gate-consuming callers. | 56b1260c734b |
| td-064 | suggested | other | goals-to-code | skills/multi-perspective-analysis/references/discipline-registry.md | architecture | .ai-state/DESIGN.md §3 | verifier | 2026-07-30 | 2026-07-30 | systems-architect | open |  | [verifier, multidisciplinary-identities] The discipline registry ships inside a Praxion-distributed skill reference file, so a managed-project user cannot add their own discipline without editing content Praxion's sync tooling may overwrite on update. TASK_BRIEF sub-goal 5 ('optionally proposed by the praxion users') therefore silently fails for anyone who is not a Praxion maintainer. Recorded path, not built: a project-local overlay (e.g. .ai-state/discipline_registry_overlay.md) with consultant Phase-1 resolution order overlay -> shipped registry -> [BLOCKED]. Deliberately deferred by the implementation-planner as outside its remit (none of the six ADRs address it, and resolving it would be architecting a new resolution algorithm rather than decomposing one); filed by the verifier so the gap is tracked rather than rediscovered at first external use. | 3245e959a915 |
| td-073 | important | drift | code-to-goals | docs/multidisciplinary-identities-evidence.md, scripts/check_release_staleness.py | code-quality |  | orchestrator | 2026-07-30 | 2026-07-30 | systems-architect | open |  | [Wave 2 intake, verified three ways] `evidence-appraiser` is merged on `main` but absent from the `v0.18.0` release: the tag tree carries skills/applied-statistics and not skills/evidence-appraisal, `git show v0.18.0:...discipline-registry.md` returns zero evidence-appraiser rows, and the installed plugin cache at ~/.claude/plugins/cache/bit-agora/i-am/0.18.0/skills/ agrees. Dossier 12.2 claimed "SHIPPED 2026-07-30"; corrected this wave to "MERGED ... NOT YET RELEASED". Consequence for managed projects on v0.18.0: a `Discipline: evidence-appraiser` directive resolves to [BLOCKED] (fail-loud as designed, but not the advertised behaviour), and Wave 1's AC13 two-disciplines-concurrently proof ran against the working tree rather than the shipped plugin. The structural defect is NOT a missing gate: scripts/check_release_staleness.py detects this correctly, is wired into .github/workflows/test.yml, and fires today. It is advisory by deliberate and well-argued design (its docstring: firing per-feat-commit "would contradict the pinned-stable version model"), and nothing consumes its output. Second of two independent instances that motivated the new gate-liveness clause 'a named consumer for every gate output'. Resolution needs a release cut (user-owned) plus a named consumer for the advisory. | 712a7da1b5c1 |
| td-074 | important | drift | code-to-goals | skills/context-security-review/references/permission-baseline.md | code-quality |  | systems-architect | 2026-07-30 | 2026-07-30 | context-engineer | open |  | [surfaced by the td-072 architect as an adjacent finding; deliberately not fixed there to stay surgical] The permission baseline tables 12 agents; the fleet is 17. Missing: agentic-transactions-architect, architect-validator, discipline-consultant, interface-designer, roadmap-cartographer. Its summary claims "8 of 12 agents use acceptEdits" and "No web access (11 of 12)" are consequently wrong as stated. Independent of the td-072 strip, which removed an inert field; this is a stale enumeration that was already wrong beforehand. | 6e3a8cd0815c |
| td-075 | minor | other | goals-to-code | commands/onboard-project.md, claude/canonical-blocks | code-quality |  | systems-architect | 2026-07-30 | 2026-07-30 | systems-architect | open |  | [surfaced by the td-072 architect; explicitly excluded from that decision as a separate blast radius] Praxion's own .claude/settings.json pre-allows Write(.ai-work/**); managed projects receive no permissions.allow from onboarding at all. td-035 (subagent Write denials to .ai-work/) is the standing argument that they should. This is a fleet-wide config decision about what onboarding ships, NOT compensation for the stripped permissionMode -- that ADR ruled explicitly that no compensating grant is warranted, since stripping an inert field removes no capability. | 592c016f280d |
| td-076 | suggested | coverage-gap | code-to-goals | docs/multidisciplinary-identities-evidence.md | code-quality |  | systems-architect | 2026-07-30 | 2026-07-30 | systems-architect | open |  | [named by the td-071 architect as an honest limit of its own design, filed rather than bundled] The CONSULT_COSTS.md series instruments AC15's numerator only (per-consult cost). The denominator -- the cost of the pipeline the consult attaches to -- is not knowable at the Round-2 disposition seam where the convener writes, so the ratio the AC15 envelope is expressed in still cannot be computed from recorded data. statistician CH-04 becomes revisitable once the series accumulates, but the envelope's ratio form stays uncomputable until a denominator producer exists. Do not close td-071 as though it discharged AC15 entirely. | d9f930598ba0 |
| td-077 | suggested | drift | code-to-goals | skills/software-planning/references/tech-debt-ledger.md, scripts/finalize_tech_debt_ledger.py | code-quality |  | orchestrator | 2026-07-30 | 2026-07-30 | context-engineer | open |  | [found while filing td-073..td-076] The documented dedup_key derivation -- sha1 over class, normalize(location), direction, goal-ref-type, goal-ref-value, truncated to 12 -- does not reproduce the keys on existing rows. Checked against td-071 (expected 52702faa9ec4) and td-072 (expected 08a11c91797c) with both ',' and ', ' joiners; no variant matches. Either the shipped rows are non-conforming or the documented formula is wrong, and nothing distinguishes the two because no code validates the derivation: finalize_tech_debt_ledger.py consumes dedup_key for grouping and re-open detection but never recomputes it. A wrong key silently merges unrelated debt or silently fails to re-open a recurrence. Rows td-073..td-078 were written using the documented formula with collision-checked uniqueness across both ledger files. Same pattern class as td-073: a documented contract with no gate. | 32e691da0699 |
| td-078 | important | other | goals-to-code | rules/swe/vcs/git-conventions.md | code-quality |  | orchestrator | 2026-07-30 | 2026-07-30 | context-engineer | open |  | [cost real work during Wave 2; the hazard is known but unencoded] Bare `git stash` / `git stash pop` is forbidden when concurrent agents share a worktree -- it sweeps up every other writer's uncommitted work regardless of pathspec discipline. A Wave-2 implementer ran one mid-step and destroyed the orchestrator's uncommitted ledger rows and dossier edits; recovery was partial (files edited before the stash were restorable per-file, files written during it were not). Grep of rules/, agents/, and skills/software-planning/ for 'git stash' returns ZERO hits: the constraint lives only in per-session environment guidance and a personal memory note, neither of which a subagent loads. So the agent was not disobeying Praxion -- Praxion never told it. Existing Staging Discipline guidance covers pathspec staging, which prevents over-staging but does nothing against stash/checkout/reset. Fix: a short bullet under Staging Discipline in git-conventions.md (always-loaded; ~1,555 tokens of headroom available) plus the safe-recovery idiom (push with a unique tag, restore with `git stash apply <sha>`, never pop). | 56c0ceca6368 |
| td-079 | important | drift | code-to-goals | .ai-state/CONSULT_LEDGER.md, .ai-state/CONSULT_COSTS.md | code-quality |  | orchestrator | 2026-07-30 | 2026-07-30 | context-engineer | open |  | [found by triggering it] Both CONSULT_LEDGER.md and CONSULT_COSTS.md instructed their single writer to append new rows at the end of the file/ledger, which the orchestrator followed literally and landed a row after the trailing prose sections (Column Definitions / Falsifier / Single Writer), outside the parsed data table -- invisible to every counting recipe and to the cost-coverage gate while still rendering as a plausible stray table to a human reader. Verified by direct comparison: an EOF-appended row produced zero gate failures; the identical row inserted inside the table produced the expected failure. Both instructions were reworded this pass to name the table and the section that follows it. `fitness/tests/test_discipline_registry_invariants.py` gained `check_no_data_row_outside_table` plus a canary per file and a real-file test per file, closing the gap -- the real-file check was observed RED before the fix (a stray row temporarily appended at EOF) and green after restoration. dedup_key formula note: confirmed it reproduces td-078's and td-077's own keys exactly (class, sorted comma-joined location with no space separator, direction, goal-ref-type, goal-ref-value, sha1 truncated to 12); per td-077 it does NOT reproduce the earliest rows (td-071, td-072), and nothing in `finalize_tech_debt_ledger.py` validates the derivation, so this confirmation applies only to rows written under the documented formula from td-073 onward. | 0a0567d66a83 |
| td-080 | important | coverage-gap | goals-to-code | docs/multidisciplinary-identities-evidence.md, .ai-state/CONSULT_LEDGER.md | code-quality |  | discipline-consultant | 2026-07-31 | 2026-07-31 | systems-architect | open |  | [statistician consult CH-03, accepted switch-now, dec-309] No Type-II instrument exists anywhere in the multidisciplinary-identities design. Every measurement surface -- the ledger, the k/n estimate and Wilson interval, section 14 falsifier 1, section 17.4's Leave-One-Out audit, dec-304's standing conditions, the fitness counting helpers -- detects only Type-I error (a shipped discipline that turns out decorative). Nothing fires when a warranted discipline is wrongly excluded. The consult offered a falsifiable test -- name any artifact that would fire -- and the convener could name none. The asymmetry is created by dec-306 itself: a criterion selecting for disciplines whose errors do not surface selects, by construction, for a population whose wrongful exclusion is undetectable. Proposed instrument: a reverse-LOO row in section 17.4 -- sample decisions made WITHOUT a given discipline and check post-hoc whether it would have changed one. Until it exists, every ship-zero verdict accumulates exclusively the error the project cannot see. | e1c6479cfd68 |
| td-082 | suggested | coverage-gap | goals-to-code | skills/multi-perspective-analysis/references/discipline-registry.md | code-quality |  | discipline-consultant | 2026-07-31 | 2026-07-31 | systems-architect | open |  | [statistician consult CH-02, accepted switch-now, dec-309] The Wave-2 roster produced three rejections and zero acceptances, which leaves the silence criterion's sensitivity unestimable -- three independent rejection routes raise P(reject) whether or not the criterion discriminates. dec-306 names its own positive control: `security` is both a Lens-Catalog lens AND a domain whose errors are archetypally silent, and dec-306's Option D discussion states a flat no-consultant-where-a-lens-exists rule would wrongly exclude it. Owed test: run the three rejection routes (absorbed-by-lens / not-silent / no-decision-surface) against `security` and against the two shipped disciplines. If the criterion rejects `security`, it rejects a domain dec-306 itself defends, which falsifies the criterion rather than the candidate -- this is reversal trigger (c) of the roster ADR. | 9cd77e5221bb |
| td-083 | important | coverage-gap | code-to-goals | fitness/tests/test_gate_canary_coverage.py | code-quality |  | orchestrator | 2026-07-31 | 2026-07-31 | context-engineer | open |  | [found by the orchestrator while verifying td-081's gate, by neutering each check in turn] The canary-coverage meta-test asks whether a *file* contains a canary, not whether each *check* has one: `_has_canary(file)` returns a single bool per file and `gates_without_canary` lists files. So a new check added to a file that already carries canaries is invisible to it -- which is the normal way checks get added. Concretely: td-081's gate specification listed six checks and eleven canaries, omitting one for `check_classification_row_has_eight_columns`. The implementer followed the plan faithfully, the meta-test stayed green, and the uncanaried check was found only because the orchestrator stubbed each check in turn and observed which tests went red -- one produced zero. The missing canary was written and proven to bite in the same pass. This is the Scope Fidelity clause (gate-liveness.md, added dec-307's wave) applied to the meta-gate itself: its computed scope (files) is narrower than its documented purpose (gates bite). Remedy sketch, not built: discover `check_*` / `validate_*` function definitions within a gate file and require a canary naming or exercising each, rather than one canary per file. | 29eec55f9b3f |
| td-084 | suggested | coverage-gap | goals-to-code | fitness/tests/test_discipline_registry_invariants.py, .ai-state/CONSULT_PRIORS.md | code-quality |  | discipline-consultant | 2026-07-31 | 2026-07-31 | systems-architect | open |  | [statistician consult CH-05 on td-081, dispositioned defer-with-rationale, dec-310] The unit of noise in the novelty rate is the SPAWN, not the consult. The same discipline, same draft, same prompt and same HEAD spawned again yields a different challenge set and therefore a different rate, and that variability sits INSIDE what the design counts as one observation. Nothing in this repository estimates it. Worse, the schema cannot record a replication: G4 asserts that seal-witness agrees across a triple's classification rows, which forecloses two runs of one consult sharing a triple. Deferred rather than built because the remedy is a gate change (relax G4, or add a run ordinal) and not a migration of accrued rows, so unlike CH-06's column it does not get more expensive as the series grows. TRIGGER, and this is the reason the row exists: before any ADR cites the novelty rate, either run the replication -- two further spawns of the same discipline against the same draft at the same HEAD -- or state its absence in that ADR. Citing a rate whose run-to-run spread has never been observed is the defect dec-304 corrected on the sibling statistic, one level down. | d49361cba54a |
| td-085 | suggested | drift | code-to-goals | .ai-state/TECH_DEBT_LEDGER.md, .ai-state/TECH_DEBT_RESOLVED.md | code-quality |  | verifier | 2026-07-31 | 2026-07-31 | context-engineer | open |  | [verifier, multidisciplinary-identities Waves 1-2 + td-081 verification] Re-derivation of td-077's open question, now resolved in the formula's favor: the documented dedup_key formula (sha1 over class/location/direction/goal-ref-type/goal-ref-value, sorted comma-joined paths with no space separator, truncated to 12) reproduces only 43 of 84 rows across both ledger files (51.2% conforming). 56 formula variants were brute-forced against the full row set; the documented one scores highest of all 56 -- so the 41 non-conforming rows are bad DATA (rows written before the formula was codified at td-073, or drifted since), not a wrong formula. Nothing validates the derivation at write time or on migration, so the gap keeps growing as new rows land under the (correct) formula while old non-conforming rows are never backfilled. Resolution: a validator in finalize_tech_debt_ledger.py that recomputes and flags mismatched dedup_key values on every run, plus a one-time backfill pass for the 41 non-conforming rows. Owner-role context-engineer, matching the established local pattern for this ledger-mechanism-integrity finding family (td-077/078/079/083 all use this owner for defects in the ledger's own bookkeeping machinery, distinct from the general drift default of doc-engineer). | a249956d9903 |
| td-086 | important | drift | code-to-goals | fitness/tests/conftest.py | code-quality |  | verifier | 2026-07-31 | 2026-07-31 | implementer | open |  | [verifier, multidisciplinary-identities Waves 1-2 + td-081 verification] `fitness/tests/conftest.py`'s `project_root` fixture resolves via `Path(__file__).resolve().parent.parent.parent`, correct only when the fitness suite ships as source inside the checkout it audits. Run from the read-only version-nested plugin cache -- the ONLY path by which a managed project receives the fitness suite today -- that resolution lands on the plugin's own root, not the consuming project's, so a managed project running the shipped fitness suite audits Praxion's registry/files instead of its own and reports green about files it does not own. Same defect family as the well-known __file__-vs-git-root gotcha from consumer git hooks: plugin-cache self-hosting masks the bug, because on Praxion's own machine the plugin's root and the audited root happen to coincide. Fix: resolve project_root via git (e.g. `git rev-parse --show-toplevel` from cwd, with an explicit override env var for non-git contexts) rather than __file__ position. Owner-role implementer (a code-logic fix in a test fixture) rather than the drift-class default of doc-engineer, since the remedy is a path-resolution correction, not a documentation update. | f42eba3d8423 |
| td-087 | suggested | other | goals-to-code | commands/onboard-project.md, commands/new-project.md, new_project.sh, claude/canonical-blocks/ | architecture | .ai-state/DESIGN.md §3 | verifier | 2026-07-31 | 2026-07-31 | systems-architect | open |  | [verifier, multidisciplinary-identities Waves 1-2 + td-081 verification] No onboarding surface scaffolds anything for the consult mechanism: `git grep -c CONSULT` over `commands/onboard-project.md`, `commands/new-project.md`, `new_project.sh`, and `claude/canonical-blocks/` returns zero across all four. A managed project that installs the plugin today receives no `CONSULT_LEDGER.md`/`CONSULT_COSTS.md`/`CONSULT_PRIORS.md` skeleton, no discipline-registry pointer, and no fitness-suite wiring for the mechanism -- the entire multidisciplinary-consult subsystem is Praxion-only in practice, not merely the registry (td-064's narrower finding). Same root as td-064 but broader scope: td-064 is about the registry being unextendable; this is about NOTHING in the consult surface reaching a managed project via onboarding at all. Resolution: extend /onboard-project (or a dedicated opt-in phase) to scaffold the three consult-ledger skeletons plus a project-local registry-overlay stub, once the accepted overlay decision (filed alongside this row) actually ships an implementation. Owner-role systems-architect, matching td-064's precedent for this registry/consult-extensibility finding family. | 9ff37e5b05e8 |
| td-088 | suggested | other | goals-to-code | skills/multi-perspective-analysis/references/discipline-registry.md, agents/discipline-consultant.md, commands/consult.md | adr | dec-305 | verifier | 2026-07-31 | 2026-07-31 | implementation-planner | open |  | [verifier, multidisciplinary-identities Waves 1-2 + td-081 verification] dec-305 (project-local discipline registry overlay, resolution order: project overlay, then shipped registry, then [BLOCKED]) is `status: accepted` with zero implementation: grepping for 'project-local' or 'overlay' (case-insensitive) across `skills/multi-perspective-analysis/`, `agents/discipline-consultant.md`, and `commands/consult.md` returns zero files. This is the concrete mechanism td-064 recorded as a recorded-but-unbuilt path, now formally accepted as an ADR and still unbuilt -- the accepted decision has not yet been decomposed into implementation steps. Consequence: moderate now, severe the moment dec-305 lands without its companion fix -- ship the overlay and a correctly-rooted gate (see the conftest.py project_root row filed alongside this one) together, never the overlay alone, or a managed project's overlay registry entries would be silently ignored by a gate still resolving to the plugin's own root. Owner-role implementation-planner: the design decision is already made (accepted ADR); what remains is step decomposition and execution, not further architecture. | cde98c605843 |
| td-089 | important | drift | code-to-goals | .ai-state/decisions/032-roadmap-md-location-and-lifecycle.md, .ai-state/decisions/046-staleness-detection-system.md, .ai-state/decisions/057-concurrency-collab-research-unified-worktree-home.md, .ai-state/decisions/077-agent-model-routing-routing-frontmatter-floor-semantic.md, .ai-state/decisions/111-req-arch-element-traceability.md, .ai-state/decisions/129-pipeline-dashboard-dashboard-supersede-metrics-viewer.md, .ai-state/decisions/152-interface-designer-opus-tier.md, .ai-state/decisions/153-review-interface-skill-command-pattern.md, .ai-state/decisions/168-core-frontmatter-single-source-of-truth.md, .ai-state/decisions/169-git-conventions-blacklistable.md, .ai-state/decisions/190-hackathon-mode-plan-decomposition.md, .ai-state/decisions/224-nebius-neocloud-nebius-direct-adapter.md, .ai-state/decisions/294-label-taxonomy-manifest.md, scripts/build_doc_manifest.py | code-quality |  | verifier | 2026-07-31 | 2026-07-31 | doc-engineer | open |  | [verifier, multidisciplinary-identities Waves 1-2 + td-081 verification] 13 pre-existing ADR records fail yaml.safe_load on the same defect class already fixed on dec-306/dec-309 by this pipeline: an unquoted colon-space inside a frontmatter scalar breaks the YAML scanner. Affected: 032, 046, 057, 077, 111, 129, 152, 153, 168, 169, 190, 224, 294. `adr-conventions.md` calls this class of failure 'harmless only because the ADR tooling parses frontmatter by regex' -- true of the ADR tooling itself, irrelevant to `scripts/build_doc_manifest.py:132`, which does `except yaml.YAMLError: fm = {}` silently on these same files, so each renders its raw filename as `title:` in `.ai-state/doc_manifest.yaml` instead of the real title -- confirmed by grepping the manifest directly. Consequence: `dissent:`/`summary:` frontmatter fields cannot honestly be called 'machine-queryable' (as `adr-conventions.md` states) while 13 records fail the parser silently. Fix: quote the offending scalar in each of the 13 files, regenerate the manifest from the canonical checkout (a worktree regen silently drops all docs -- `.claude` paths are excluded from the manifest build by an absolute-path check), and add a fitness check that fails loudly on a frontmatter parse error instead of swallowing it. | 293577e56b98 |
| td-090 | suggested | drift | code-to-goals | .ai-state/CONSULT_PRIORS.md, .ai-state/CONSULT_LEDGER.md | code-quality |  | verifier | 2026-07-31 | 2026-07-31 | doc-engineer | open |  | [verifier, multidisciplinary-identities Waves 1-2 + td-081 verification] `.ai-state/CONSULT_PRIORS.md`'s `## Column Definitions` documents the Challenge Classification table's `timestamp` column as matching 'this consult's CONSULT_LEDGER.md rows'. Live data on the td-081-sealed-prior CH-01 row: the classification row (`CONSULT_PRIORS.md`) carries `2026-07-31T06:40:00Z`; the corresponding ledger row (`CONSULT_LEDGER.md`) carries `2026-07-31T06:10:00Z` -- a 30-minute gap, presumably the write-order latency between the two files (the ledger disposition is written first, the classification row after). No check reads that cell to enforce the documented equality; the existing gates validate format and seal-witness agreement, not this specific cross-file timestamp claim. Fix: either loosen the doc's claim to describe the two timestamps as independently-written and order-only (ledger row precedes classification row), or add the cross-file check if exact equality is actually intended. | 7c60645b9ae6 |
| td-091 | suggested | drift | code-to-goals | .ai-state/DESIGN.md, docs/architecture.md | code-quality |  | verifier | 2026-07-31 | 2026-07-31 | doc-engineer | open |  | [verifier, multidisciplinary-identities Waves 1-2 + td-081 verification] `.ai-state/DESIGN.md` and `docs/architecture.md` both still describe a one-discipline roster; `evidence-appraiser` (shipped since Wave 1) appears in NEITHER. The two documents are stale asymmetrically -- `DESIGN.md` knows about the consult state files (CONSULT_LEDGER.md/COSTS.md/PRIORS.md) but not the second discipline; `docs/architecture.md` knows a second discipline mechanism exists structurally but not the state files -- so neither is a superset of the other, and a reader consulting only one gets a materially different (and wrong in a different way) picture of the same subsystem. Same staleness-asymmetry shape as td-068's three-CLAUDE.md-basis finding, one level down in a different doc pair. Fix: a single coordinated pass over both documents' consult/discipline sections, bringing each to parity with the other before either is trusted as complete. | 0bdde8be34c2 |
| td-092 | suggested | drift | code-to-goals | docs/diagrams/architecture/src/architecture.c4 | code-quality |  | verifier | 2026-07-31 | 2026-07-31 | doc-engineer | open |  | [verifier, multidisciplinary-identities Waves 1-2 + td-081 verification] `docs/diagrams/architecture/src/architecture.c4` models `challenge_loop` (9 references across the model) -- the CONSEQUENCE of the discipline-consultant mechanism -- but has no `discipline-consultant` element of its own, even though the sibling peer sub-architects (interface-designer, agentic-transactions-architect) each have their own modeled element. The model shows the loop's effect without the agent that runs it. Fix: add a `discipline-consultant` element (matching the shape of the interface-designer/agentic-transactions-architect elements already in the model) plus its relationships into `challenge_loop`, then run the diagram-regen hook so the committed `.d2`/`.svg` renders stay in sync. | 5ae622c67354 |
| td-093 | suggested | drift | code-to-goals | docs/architecture.md, docs/diagrams/architecture/src/architecture.c4 | code-quality |  | verifier | 2026-07-31 | 2026-07-31 | doc-engineer | open |  | [verifier, multidisciplinary-identities Waves 1-2 + td-081 verification] All four `aac:generated` fences in `docs/architecture.md` claim `last-regen=2026-05-14`, while the most recent commit touching any `.c4` source is from 2026-07-29 -- over two months of drift between the claimed and actual regen dates. The fence metadata exists precisely so a reader can trust a generated region without re-deriving it; a stale claim defeats that purpose silently, since the fence still renders as 'generated,' producing false confidence. Likely the same root cause as the sibling `discipline-consultant`-element gap filed alongside this row (the model changed without the regen hook running). Fix: run the diagram-regen hook and commit the refreshed fences with a corrected `last-regen` stamp; consider a fitness check that fails when a `.c4` source's commit date postdates its fence's `last-regen` claim. | 8b2133f511e5 |
| td-094 | important | coverage-gap | code-to-goals | fitness/tests/test_discipline_registry_invariants.py | code-quality |  | verifier | 2026-07-31 | 2026-07-31 | context-engineer | open |  | [verifier, multidisciplinary-identities Waves 1-2 + td-081 verification] Extends td-083 rather than duplicating it: td-083 found the canary-coverage meta-test counts canaries per FILE, so a check added to an already-canaried file is invisible to it. This finding is upstream of that one -- five gates in `fitness/tests/test_discipline_registry_invariants.py` assert inline (directly in a test function body) with no extractable `check_*`/`validate_*` function at all, so they are invisible not just to canary coverage but to gate ENUMERATION itself: nothing can list them as named checks, and td-083's own proposed remedy ('discover check_*/validate_* function definitions... require a canary naming or exercising each') cannot see them either, since there is no function definition to discover. Same root defect one level upstream: extraction discipline (one check = one named function) is not itself enforced. Fix: extract the five inline asserts into named `check_*` functions matching the file's existing convention, so td-083's remedy becomes applicable to all of them. Owner-role context-engineer, matching td-083's precedent for this gate-liveness-mechanism finding family. | 6dcf83049c9a |
| td-095 | important | coverage-gap | code-to-goals | scripts/check_gate_liveness.py | code-quality |  | verifier | 2026-07-31 | 2026-07-31 | context-engineer | open |  | [verifier, multidisciplinary-identities Waves 1-2 + td-081 verification] GL02 (`scripts/check_gate_liveness.py`'s clause-5 scope-fidelity detector) documents a golden bad-case meant to prove the detector actually fires on the defect class it targets. Fed verbatim to GL02 itself, that golden bad-case returns ZERO findings -- the detector's own liveness proof does not bite. This is the meta-level instance of the entire gate-liveness discipline's own thesis (a check with no proof it fires is worse than no check, because it manufactures false confidence) turned on the tool that enforces that thesis for every other gate in the repo. Fix: trace why the documented golden bad-case doesn't reproduce (possibly the same class of exclusion/pattern-matching gap as the sibling absolute-path-exclusion row filed alongside this one) and add a regression test pinning the golden bad-case to a real, currently-failing assertion. Owner-role context-engineer, matching the td-077/078/079/083 precedent for gate-liveness-mechanism-self-defects. | 5c36129c198e |
| td-096 | important | other | code-to-goals | scripts/check_gate_liveness.py | code-quality |  | verifier | 2026-07-31 | 2026-07-31 | context-engineer | open |  | [verifier, multidisciplinary-identities Waves 1-2 + td-081 verification] GL02's exclusion list is implemented as an absolute-path substring test, so identical bytes at identical relative positions yield 1 finding or 0 findings purely depending on the checkout's absolute path on disk (e.g. a home-directory checkout vs a worktree path vs a CI runner's checkout root). A detector whose result depends on filesystem location rather than file content cannot be trusted to reproduce between a local run and CI, or between two contributors' machines. Fix: match the exclusion list against paths relative to the repo root (as resolved by git), never an absolute-path substring. Owner-role context-engineer, matching the td-077/078/079/083 precedent for gate-liveness-mechanism-self-defects (same file and finding family as the sibling golden-bad-case row filed alongside this one). | 56e256df04d9 |
| td-097 | important | other | code-to-goals | fitness/tests/test_discipline_registry_invariants.py | code-quality |  | verifier | 2026-07-31 | 2026-07-31 | test-engineer | open |  | [verifier, multidisciplinary-identities Waves 1-2 + td-081 verification] The real-file tests in `fitness/tests/test_discipline_registry_invariants.py` iterate over multiple (task-slug, discipline, stage) triples inside a single test function and call `pytest.skip` from inside that loop when one triple's precondition isn't met (e.g. an unreachable seal-witness commit on a shallow clone). `pytest.skip` raises immediately and unwinds the whole test function, so ONE skippable triple silences every OTHER triple's checks in the same run -- live on the second consult (td-081-sealed-prior), where a skip firing partway through the loop meant later triples' checks never executed. This is the coverage-loss sibling of the shallow-clone finding filed against the same real-file arms: not just 'this precondition-dependent arm cannot run in CI,' but 'when it cannot run, checks for triples that COULD have run silently do not either.' Fix: catch the skip condition per-triple and continue the loop (accumulating skipped triples for one summary skip/xfail at the end), or parametrize the test over triples so pytest treats each as an independent case with its own pass/fail/skip outcome. Class `other` rather than `coverage-gap` to avoid an artificial dedup collision with the sibling five-gates-no-check-function row filed against the same file; owner-role test-engineer (override) since this is a test-structure defect, squarely testing-infrastructure work. | e35a15331abe |
| td-098 | important | drift | goals-to-code | .ai-state/CONSULT_PRIORS.md, fitness/tests/test_discipline_registry_invariants.py | code-quality |  | verifier | 2026-07-31 | 2026-07-31 | context-engineer | open |  | [verifier, multidisciplinary-identities Waves 1-2 + td-081 verification] `.ai-state/CONSULT_PRIORS.md`'s own 'one concern' rule requires each `source: lens` sealed-prior row to name the `challenge-obligations` clause it derives from. Live data: all 8 sealed rows (P-01 through P-08) are `source: lens` and NONE names a clause -- zero compliant instances -- and no gate in `fitness/tests/test_discipline_registry_invariants.py` checks for clause-anchoring; the existing checks validate non-placeholder text, the `source` enum, and `prior-id` format only. The rule describes an obligation it does not constrain. This means CH-07 -- the challenge this exact rule was written to close -- is NOT fully discharged: its accepted disposition produced descriptive prose, not a compliant instance or an enforcing gate, so wherever CH-07 is recorded as closed, that record overstates what actually shipped. Fix: add a check requiring a `challenge-obligations` clause reference in the `concern` column for every `source: lens` row, then backfill the 8 existing rows or grandfather them explicitly. Owner-role context-engineer, matching the td-077/078/079/083 precedent for consult/gate-liveness-mechanism-artifact-integrity findings. | f55fcc3c848b |
| td-099 | important | coverage-gap | code-to-goals | .ai-state/CONSULT_COSTS.md, docs/multidisciplinary-identities-evidence.md | code-quality |  | verifier | 2026-07-31 | 2026-07-31 | systems-architect | open |  | [verifier, multidisciplinary-identities Waves 1-2 + td-081 verification] Extends the td-071/td-076/td-080 cost-instrumentation-gap family with a numerator AND denominator defect in AC15's currently-recorded figure. Numerator: `.ai-state/CONSULT_COSTS.md`'s series includes a blocked probe that the series is documented to exclude -- correcting for it changes the headline multiplier from 1.565x to 1.344x. Denominator: 48% of the pipeline-stage-cost denominator is `sentinel` + `skill-genesis` spend, but `rules/swe/swe-agent-coordination-protocol.md` states these two are NOT pipeline stages (sentinel is independent of the pipeline; skill-genesis is on-demand only, never pipeline-spawned) -- so nearly half the denominator is spend the series' own governing protocol says should not be counted as pipeline cost. Only 1 of the 3 cost figures currently cited traces to a durable artifact; the other 2 are as-yet-unrecorded derivations. Owner-role systems-architect, matching the td-071/td-076/td-080 precedent for this same AC15 measurement-methodology family -- fixing the numerator/denominator definitions is a measurement-design decision, not a test-coverage percentage to raise. | 4dd72cdeffee |
| td-100 | suggested | drift | code-to-goals | CLAUDE.md, .gitignore | claude-md | CLAUDE.md#critical-conventions | verifier | 2026-07-31 | 2026-07-31 | doc-engineer | open |  | [verifier, multidisciplinary-identities Waves 1-2 + td-081 verification] `CLAUDE.md:60` states 'temp files in `tmp/` (gitignored)' as part of the always-loaded Critical Conventions. Verified directly: `tmp/` has no entry in `.gitignore`, and `git check-ignore tmp/x` exits 1 (not ignored). An always-loaded instruction is asserting a gitignore protection that does not exist -- a temp file written to `tmp/` per this exact instruction is one `git add -A` away from being committed, which this repo's own git-conventions rules explicitly steer contributors away from. Fix: either add `tmp/` to `.gitignore` (matching the claim) or correct the CLAUDE.md wording to drop the parenthetical, if `tmp/` is meant to be manually excluded per commit rather than gitignore-protected. | d2cbead414e5 |
| td-101 | important | other | goals-to-code | rules/swe/id-citation-discipline.md, scripts/check_id_citation_discipline.py | code-quality |  | verifier | 2026-07-31 | 2026-07-31 | systems-architect | open |  | [verifier, multidisciplinary-identities Waves 1-2 + td-081 verification] `scripts/check_id_citation_discipline.py` enforces REQ-/AC-/EC-/Step-N/test-name citation patterns but has NO pattern for `dec-draft-<hash>`, even though `rules/swe/id-citation-discipline.md`'s own lifecycle table lists `dec-draft-<hash>` as 'Never' allowed in code -- a documented prohibition with zero enforcement. Same gap class as a sibling live finding this pipeline hit directly (two dangling `dec-draft-` ids surviving in `tests/` and `fitness/tests/` files, invisible to both `finalize_adrs.py`'s walk scope and this citation gate). Two naive fixes were considered and both are wrong: (1) widening `finalize_adrs.py`'s allowlist to include `tests/`/`fitness/` contradicts that script's own explicit NOTE, which deliberately excludes code from its rewrite scope; (2) a blanket `dec-draft-[0-9a-f]{8}` regex pattern in the citation gate would flag 21 currently-legitimate files -- ADR-parsing scripts and their synthetic test fixtures that construct draft-id-shaped strings on purpose. A sound detector needs to distinguish 'a real draft citation surviving in shipped code' from 'a parser or fixture that legitimately manipulates draft-id-shaped strings,' which is a design decision, not a regex tweak -- why this is filed as a row rather than fixed inline. Owner-role systems-architect: the citation-gate's detection strategy (e.g. an explicit fixture/parser allowlist alongside the pattern, mirroring finalize_adrs.py's own excluded-synthetic-ids list) needs to be decided once, not improvised per gate. | 33e989aa5ad9 |
| td-102 | important | coverage-gap | goals-to-code | .ai-state/CONSULT_PRIORS.md, docs/multidisciplinary-identities-evidence.md | code-quality |  | verifier | 2026-07-31 | 2026-07-31 | systems-architect | open |  | [verifier, multidisciplinary-identities Waves 1-2 + td-081 verification] The novel/matched classification in `.ai-state/CONSULT_PRIORS.md § Challenge Classification` has no inter-rater or second-reader check anywhere in the design -- the convener alone judges each challenge against its own sealed prior list, at Round 2, with full knowledge of the challenge text. Live consequence: two independent readers reached a different call on one of three `matched` classifications in the first sealed consult (td-081-sealed-prior), moving the recorded novelty rate from 4/7 = 0.57 to 5/7 = 0.71 -- a material shift from a single reclassification, at n=1. A sealed prior recorded before the spawn predicted exactly this failure mode (an unsealed, interested-party, full-knowledge classification with no inter-rater check) and no challenge from the live consult matched it -- the register worked exactly as designed, preserving a concern the consultant never independently raised, which then proved live. Record for future ADR citations: the bias direction is the one the threat model did NOT anticipate. Over-matching -- classifying a challenge as 'matched' against a sealed prior when a stranger would call it 'novel' -- flatters the convener's OWN prior lens pass, not the consultant, and pushes the recorded novelty rate DOWN, the opposite of what a self-interested convener would be suspected of doing. Every stated defense in the sealed-prior design (the pre-spawn seal, round-0 isolation, the seal-witness commit check) constrains only WHEN priors are written; none constrains HOW GENEROUSLY a challenge is matched against them after the fact. Owner-role systems-architect, matching the td-071/td-076/td-080 precedent for this same sealed-prior measurement-design family. | 973e95cae5ee |
