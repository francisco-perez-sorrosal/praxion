# Technical Debt Ledger

<!-- Living, append-only ledger of grounded debt findings.
     Producers: verifier (per-change Phase 5/5.5), sentinel (repo-wide TD dimension).
     Consumers: systems-architect, implementation-planner, implementer, test-engineer, doc-engineer.
     Schema, owner-role heuristic, worktree-merge dedupe semantics, and lifecycle conventions
     are defined canonically in skills/software-planning/references/tech-debt-ledger.md
     (rules/swe/agent-intermediate-documents.md retains a 5-line summary + pointer).
     Do not duplicate the schema here — the skill reference is the single source of truth. -->

**Schema**: 14 row fields + 1 structural `dedup_key`. See [`skills/software-planning/references/tech-debt-ledger.md`](../skills/software-planning/references/tech-debt-ledger.md) § `TECH_DEBT_LEDGER.md` for field definitions, enum values, the owner-role heuristic, and the post-merge dedupe contract.

**Append new rows at the end of this active ledger.** Consumers update `status`, `resolved-by`, and `last-seen` in place; when status transitions to `resolved` or `wontfix`, the row migrates to the sibling [`TECH_DEBT_RESOLVED.md`](TECH_DEBT_RESOLVED.md) (cut + paste, in-commit or via `scripts/finalize_tech_debt_ledger.py`). The pair forms one logical namespace — see the rule for re-open semantics.

| id | severity | class | direction | location | goal-ref-type | goal-ref-value | source | first-seen | last-seen | owner-role | status | resolved-by | notes | dedup_key |
|----|----------|-------|-----------|----------|---------------|----------------|--------|------------|-----------|-----------|--------|-------------|-------|-----------|
| td-037 | suggested | complexity | code-to-goals | eval/src/praxion_evals/harness/families/family1_pipeline_fidelity.py | code-quality |  | verifier | 2026-05-26 | 2026-05-26 | implementer | open |  | [F-04 from praxion-self-eval-v1 verification] `_check_supersession_reciprocity` is 78 lines, exceeding the 50-line ceiling in `rules/swe/coding-style.md`. Refactor opportunity — extract helper functions for the symmetric-pair detection and back-link checking phases. Not blocking; v1 ships as-is. | dae581a60fde |
| td-057 | suggested | maintenance | code-to-goals | .github/workflows/reusable-ci-autofix.yml | code-quality |  | verifier | 2026-07-26 | 2026-07-26 | implementer | open |  | [verifier, pr-dependabot-autofix] The autofix workflows (reusable-ci-autofix.yml, ci-autofix.yml, issue-autofix.yml) pin claude-code-action@51ea8ea (v1); the v1 tag has since moved ~60 releases to v1.0.183 (be7b93b). P3a deliberately kept the audited 51ea8ea for consistency + surgical scope (a fleet-wide bump is out of P3a's scope; light-review + verifier both confirmed keeping the audited pin is correct). Owed: a deliberate, audited lockstep bump of claude-code-action across all autofix workflows to a current pinned SHA, verifying no breaking changes. Not urgent — the audited pin works. | 80564a5a3e02 |
| td-064 | suggested | other | goals-to-code | skills/multi-perspective-analysis/references/discipline-registry.md | architecture | .ai-state/DESIGN.md §3 | verifier | 2026-07-30 | 2026-07-31 | systems-architect | open |  | **[PREMISE CORRECTED 2026-07-31 -- READ BEFORE THE ORIGINAL TEXT BELOW.]** This row's phrase 'resolution order overlay -> shipped registry -> [BLOCKED]' describes a PRECEDENCE model that dec-305 explicitly REJECTED (its Options A and B). dec-305 decided UNION + collision-as-hard-error: a key present in both sources is a named [BLOCKED] failure, never an override or a shadow. dec-305 governs; this row's wording is stale and is not a second design. DEFERRED as a unit with td-086/td-088 (never split); reversal trigger recorded in the deferring ADR. ORIGINAL TEXT FOLLOWS: [verifier, multidisciplinary-identities] The discipline registry ships inside a Praxion-distributed skill reference file, so a managed-project user cannot add their own discipline without editing content Praxion's sync tooling may overwrite on update. TASK_BRIEF sub-goal 5 ('optionally proposed by the praxion users') therefore silently fails for anyone who is not a Praxion maintainer. Recorded path, not built: a project-local overlay (e.g. .ai-state/discipline_registry_overlay.md) with consultant Phase-1 resolution order overlay -> shipped registry -> [BLOCKED]. Deliberately deferred by the implementation-planner as outside its remit (none of the six ADRs address it, and resolving it would be architecting a new resolution algorithm rather than decomposing one); filed by the verifier so the gap is tracked rather than rediscovered at first external use. // 2026-07-31: re-confirmed, still open, by the Waves-1-2 + td-081 independent verification, which reached this gap from the other direction (what does a managed project actually receive?) and decomposed it into three separately-actionable rows: td-088 (dec-305 accepted, unbuilt), td-086 (the shipped fitness suite resolves project_root to the plugin's own tree, so it would audit Praxion's files rather than the project's), td-087 (no onboarding surface scaffolds any of it). Ship the overlay and the correctly-rooted gate together; the overlay alone would have a managed project's registry entries silently ignored. // 2026-07-31 (systems-architect, gate-integrity-and-overlay): WORDING CORRECTION -- this row's "resolution order overlay -> shipped registry -> [BLOCKED]" describes a PRECEDENCE model, which dec-305 explicitly rejected in both directions (Options A and B). dec-305 decided UNION with collision-as-hard-error. The row predates the ADR; dec-305 governs, and an implementer must read dec-305's Decision section rather than this cell as the spec. Also: the coupling rationale in this row ("the overlay alone would have a managed project's registry entries silently ignored by a gate still resolving to the plugin's own root") is VOID -- a managed project never receives that gate at all (the /onboard-project 8b.2 and /new-project 5f.2 template set copies conftest.py, import-linter.cfg, test_meta_citation.py, test_starter.py and README.md; it does NOT include test_discipline_registry_invariants.py). Unit DEFERRED with td-086/td-088 per dec-draft-c33dd9a3, which re-affirms dec-305 and names two revisit triggers. | 3245e959a915 |
| td-076 | suggested | coverage-gap | code-to-goals | docs/multidisciplinary-identities-evidence.md | code-quality |  | systems-architect | 2026-07-30 | 2026-07-30 | systems-architect | open |  | [named by the td-071 architect as an honest limit of its own design, filed rather than bundled] The CONSULT_COSTS.md series instruments AC15's numerator only (per-consult cost). The denominator -- the cost of the pipeline the consult attaches to -- is not knowable at the Round-2 disposition seam where the convener writes, so the ratio the AC15 envelope is expressed in still cannot be computed from recorded data. statistician CH-04 becomes revisitable once the series accumulates, but the envelope's ratio form stays uncomputable until a denominator producer exists. Do not close td-071 as though it discharged AC15 entirely. | d9f930598ba0 |
| td-080 | important | coverage-gap | goals-to-code | docs/multidisciplinary-identities-evidence.md, .ai-state/CONSULT_LEDGER.md | code-quality |  | discipline-consultant | 2026-07-31 | 2026-07-31 | systems-architect | open |  | [statistician consult CH-03, accepted switch-now, dec-309] No Type-II instrument exists anywhere in the multidisciplinary-identities design. Every measurement surface -- the ledger, the k/n estimate and Wilson interval, section 14 falsifier 1, section 17.4's Leave-One-Out audit, dec-304's standing conditions, the fitness counting helpers -- detects only Type-I error (a shipped discipline that turns out decorative). Nothing fires when a warranted discipline is wrongly excluded. The consult offered a falsifiable test -- name any artifact that would fire -- and the convener could name none. The asymmetry is created by dec-306 itself: a criterion selecting for disciplines whose errors do not surface selects, by construction, for a population whose wrongful exclusion is undetectable. Proposed instrument: a reverse-LOO row in section 17.4 -- sample decisions made WITHOUT a given discipline and check post-hoc whether it would have changed one. Until it exists, every ship-zero verdict accumulates exclusively the error the project cannot see. | e1c6479cfd68 |
| td-082 | suggested | coverage-gap | goals-to-code | skills/multi-perspective-analysis/references/discipline-registry.md | code-quality |  | discipline-consultant | 2026-07-31 | 2026-07-31 | systems-architect | open |  | [statistician consult CH-02, accepted switch-now, dec-309] The Wave-2 roster produced three rejections and zero acceptances, which leaves the silence criterion's sensitivity unestimable -- three independent rejection routes raise P(reject) whether or not the criterion discriminates. dec-306 names its own positive control: `security` is both a Lens-Catalog lens AND a domain whose errors are archetypally silent, and dec-306's Option D discussion states a flat no-consultant-where-a-lens-exists rule would wrongly exclude it. Owed test: run the three rejection routes (absorbed-by-lens / not-silent / no-decision-surface) against `security` and against the two shipped disciplines. If the criterion rejects `security`, it rejects a domain dec-306 itself defends, which falsifies the criterion rather than the candidate -- this is reversal trigger (c) of the roster ADR. | 9cd77e5221bb |
| td-084 | suggested | coverage-gap | goals-to-code | fitness/tests/test_discipline_registry_invariants.py, .ai-state/CONSULT_PRIORS.md | code-quality |  | discipline-consultant | 2026-07-31 | 2026-07-31 | systems-architect | open |  | [statistician consult CH-05 on td-081, dispositioned defer-with-rationale, dec-310] The unit of noise in the novelty rate is the SPAWN, not the consult. The same discipline, same draft, same prompt and same HEAD spawned again yields a different challenge set and therefore a different rate, and that variability sits INSIDE what the design counts as one observation. Nothing in this repository estimates it. Worse, the schema cannot record a replication: G4 asserts that seal-witness agrees across a triple's classification rows, which forecloses two runs of one consult sharing a triple. Deferred rather than built because the remedy is a gate change (relax G4, or add a run ordinal) and not a migration of accrued rows, so unlike CH-06's column it does not get more expensive as the series grows. TRIGGER, and this is the reason the row exists: before any ADR cites the novelty rate, either run the replication -- two further spawns of the same discipline against the same draft at the same HEAD -- or state its absence in that ADR. Citing a rate whose run-to-run spread has never been observed is the defect dec-304 corrected on the sibling statistic, one level down. | d49361cba54a |
| td-086 | important | drift | code-to-goals | fitness/tests/conftest.py | code-quality |  | verifier | 2026-07-31 | 2026-07-31 | implementer | open |  | **[PREMISE FALSIFIED 2026-07-31 -- READ BEFORE THE ORIGINAL TEXT BELOW.]** The claim below that the read-only plugin cache is 'the ONLY path by which a managed project receives the fitness suite today' is FALSE, and the live consequence it asserts has NO producing invocation path. Verified: `/onboard-project` Phase 8b.2 COPIES `claude/aac-templates/fitness-conftest.py.tmpl` into the managed project's OWN tree (commands/onboard-project.md:571), where `Path(__file__).parent.parent.parent` resolves correctly; and the scaffold never ships the registry-invariants suite at all (`grep discipline_registry commands/onboard-project.md` -> 0 hits). The `__file__` rooting defect is LATENT, not live. The REAL exposure here is filed as td-106: `fitness/tests/conftest.py` and its `.tmpl` are byte-identical duplicates with NO sync check, while the analogous `claude/canonical-blocks/` surface has one -- so the two can silently diverge. DEFERRED as a unit with td-064/td-088 (never split). ORIGINAL TEXT FOLLOWS: [verifier, multidisciplinary-identities Waves 1-2 + td-081 verification] `fitness/tests/conftest.py`'s `project_root` fixture resolves via `Path(__file__).resolve().parent.parent.parent`, correct only when the fitness suite ships as source inside the checkout it audits. Run from the read-only version-nested plugin cache -- the ONLY path by which a managed project receives the fitness suite today -- that resolution lands on the plugin's own root, not the consuming project's, so a managed project running the shipped fitness suite audits Praxion's registry/files instead of its own and reports green about files it does not own. Same defect family as the well-known __file__-vs-git-root gotcha from consumer git hooks: plugin-cache self-hosting masks the bug, because on Praxion's own machine the plugin's root and the audited root happen to coincide. Fix: resolve project_root via git (e.g. `git rev-parse --show-toplevel` from cwd, with an explicit override env var for non-git contexts) rather than __file__ position. Owner-role implementer (a code-logic fix in a test fixture) rather than the drift-class default of doc-engineer, since the remedy is a path-resolution correction, not a documentation update. // 2026-07-31 (systems-architect, gate-integrity-and-overlay): PREMISE CORRECTED. "the read-only version-nested plugin cache -- the ONLY path by which a managed project receives the fitness suite today" is FALSE. /onboard-project Phase 8b.2 and /new-project sub-step 5f.2 scaffold fitness/ into a managed project by COPYING claude/aac-templates/fitness-conftest.py.tmpl to <project>/fitness/tests/conftest.py, where Path(__file__).resolve().parent.parent.parent resolves to the project's OWN root -- correct. Nothing instructs a managed project to run the plugin-cache copy (fitness/README.md documents a repo-relative `uv run pytest fitness/tests/`; install_claude.sh contains zero references to fitness). The defect is therefore LATENT -- an execution path no instruction produces -- so fixing it now would wire a fix for a consumer with no producer (gate-liveness clause 2). Live sub-finding worth more than this row as written: fitness/tests/conftest.py and claude/aac-templates/fitness-conftest.py.tmpl are BYTE-IDENTICAL duplicates with NO sync check, while claude/canonical-blocks/ has one (scripts/sync_canonical_blocks.py --check) -- hardening one would silently not harden the other; recommend a separate row for the aac-templates sync gap (architect is not a ledger writer). Unit DEFERRED with td-064/td-088 per dec-draft-c33dd9a3. | f42eba3d8423 |
| td-088 | suggested | other | goals-to-code | skills/multi-perspective-analysis/references/discipline-registry.md, agents/discipline-consultant.md, commands/consult.md | adr | dec-305 | verifier | 2026-07-31 | 2026-07-31 | systems-architect | open |  | **[PREMISE FALSIFIED 2026-07-31 -- READ BEFORE THE ORIGINAL TEXT BELOW.]** This row states the design decision is already made and 'what remains is step decomposition and execution, not further architecture'. FALSE: dec-305's `## Decision` carries two mechanism claims now known wrong -- (1) neither runtime read is mechanical, so union/collision semantics cannot be enforced by the shared parser it names and would ship as prose at multiple sites; (2) its 'three additive fitness assertions' never reach a managed project, so the overlay would deliver resolution with ZERO validation to the deployment it targets. dec-305 now carries an in-place Correction block recording both. The decision's SHAPE stands; its enforcement design does not. What remains is architecture, not decomposition -- hence `owner-role` corrected implementation-planner -> systems-architect. DEFERRED as a unit with td-064/td-086 (never split); the deferring ADR names the three conditions a future supersession must supply. ORIGINAL TEXT FOLLOWS: [verifier, multidisciplinary-identities Waves 1-2 + td-081 verification] dec-305 (project-local discipline registry overlay, resolution order: project overlay, then shipped registry, then [BLOCKED]) is `status: accepted` with zero implementation: grepping for 'project-local' or 'overlay' (case-insensitive) across `skills/multi-perspective-analysis/`, `agents/discipline-consultant.md`, and `commands/consult.md` returns zero files. This is the concrete mechanism td-064 recorded as a recorded-but-unbuilt path, now formally accepted as an ADR and still unbuilt -- the accepted decision has not yet been decomposed into implementation steps. Consequence: moderate now, severe the moment dec-305 lands without its companion fix -- ship the overlay and a correctly-rooted gate (see the conftest.py project_root row filed alongside this one) together, never the overlay alone, or a managed project's overlay registry entries would be silently ignored by a gate still resolving to the plugin's own root. Owner-role implementation-planner: the design decision is already made (accepted ADR); what remains is step decomposition and execution, not further architecture. // 2026-07-31 (systems-architect, gate-integrity-and-overlay): THAT PREMISE IS FALSIFIED -- this is NOT decomposition-ready. (1) dec-305's Enforcement paragraph claims "Both files are parsed by the one existing table parser, so 'a valid row' cannot drift between them"; true of the fitness-test ENFORCEMENT layer, false of RESOLUTION -- both real readers (convener pre-spawn, consultant post-spawn) resolve Discipline: by an LLM reading the markdown table, and parse_registry_table_rows is referenced by no file outside fitness/tests/test_discipline_registry_invariants.py. Implementing union+collision therefore means authoring the semantics as prose at FOUR independent textual sites (agents/discipline-consultant.md, commands/consult.md, agents/CLAUDE.md, and the registry's own new section) -- the gate-liveness "convention at two+ textual sites" anti-pattern. (2) DECISIVE: the enforcement layer never reaches the deployment it targets -- the onboarding template set excludes test_discipline_registry_invariants.py, so a managed project would get the overlay's RESOLUTION with none of its VALIDATION, while dec-305's fail-loud contract leans explicitly on "row-shape validity of overlay rows (reusing the existing shape checker)". That checker never ships: a producer-less consumer inside dec-305's own enforcement design, not repairable by a step plan. DEFERRED as a unit with td-064/td-086 per dec-draft-c33dd9a3 (re-affirms dec-305; triggers: a concrete request for a project-local discipline, OR a decision on deterministic-resolver-vs-prose-at-N-sites, which is a SUPERSESSION of dec-305 and enlarges the unit). Recommend owner-role be corrected implementation-planner -> systems-architect by a sanctioned ledger writer (owner-role is not one of the three consumer-mutable fields, so the architect does not edit it in place). // 2026-07-31 (orchestrator, gate-integrity-and-overlay): owner-role corrected implementation-planner -> systems-architect. The row was filed on the premise that dec-305's design was settled and only step decomposition remained; the architect's deferral analysis falsified two of dec-305's own premises, so what remains is architecture (re-establishing the prerequisites the ADR assumed), not decomposition. Corrected by the orchestrator rather than the architect because owner-role is not one of the three consumer-mutable fields. | cde98c605843 |
| td-098 | important | drift | goals-to-code | .ai-state/CONSULT_PRIORS.md, fitness/tests/test_discipline_registry_invariants.py | code-quality |  | verifier | 2026-07-31 | 2026-07-31 | context-engineer | open |  | [verifier, multidisciplinary-identities Waves 1-2 + td-081 verification] `.ai-state/CONSULT_PRIORS.md`'s own 'one concern' rule requires each `source: lens` sealed-prior row to name the `challenge-obligations` clause it derives from. Live data: all 8 sealed rows (P-01 through P-08) are `source: lens` and NONE names a clause -- zero compliant instances -- and no gate in `fitness/tests/test_discipline_registry_invariants.py` checks for clause-anchoring; the existing checks validate non-placeholder text, the `source` enum, and `prior-id` format only. The rule describes an obligation it does not constrain. This means CH-07 -- the challenge this exact rule was written to close -- is NOT fully discharged: its accepted disposition produced descriptive prose, not a compliant instance or an enforcing gate, so wherever CH-07 is recorded as closed, that record overstates what actually shipped. Fix: add a check requiring a `challenge-obligations` clause reference in the `concern` column for every `source: lens` row, then backfill the 8 existing rows or grandfather them explicitly. Owner-role context-engineer, matching the td-077/078/079/083 precedent for consult/gate-liveness-mechanism-artifact-integrity findings. | f55fcc3c848b |
| td-099 | important | coverage-gap | code-to-goals | .ai-state/CONSULT_COSTS.md, docs/multidisciplinary-identities-evidence.md | code-quality |  | verifier | 2026-07-31 | 2026-07-31 | systems-architect | open |  | [verifier, multidisciplinary-identities Waves 1-2 + td-081 verification] Extends the td-071/td-076/td-080 cost-instrumentation-gap family with a numerator AND denominator defect in AC15's currently-recorded figure. Numerator: `.ai-state/CONSULT_COSTS.md`'s series includes a blocked probe that the series is documented to exclude -- correcting for it changes the headline multiplier from 1.565x to 1.344x. Denominator: 48% of the pipeline-stage-cost denominator is `sentinel` + `skill-genesis` spend, but `rules/swe/swe-agent-coordination-protocol.md` states these two are NOT pipeline stages (sentinel is independent of the pipeline; skill-genesis is on-demand only, never pipeline-spawned) -- so nearly half the denominator is spend the series' own governing protocol says should not be counted as pipeline cost. Only 1 of the 3 cost figures currently cited traces to a durable artifact; the other 2 are as-yet-unrecorded derivations. Owner-role systems-architect, matching the td-071/td-076/td-080 precedent for this same AC15 measurement-methodology family -- fixing the numerator/denominator definitions is a measurement-design decision, not a test-coverage percentage to raise. | 4dd72cdeffee |
| td-102 | important | coverage-gap | goals-to-code | .ai-state/CONSULT_PRIORS.md, docs/multidisciplinary-identities-evidence.md | code-quality |  | verifier | 2026-07-31 | 2026-07-31 | systems-architect | open |  | [verifier, multidisciplinary-identities Waves 1-2 + td-081 verification] The novel/matched classification in `.ai-state/CONSULT_PRIORS.md § Challenge Classification` has no inter-rater or second-reader check anywhere in the design -- the convener alone judges each challenge against its own sealed prior list, at Round 2, with full knowledge of the challenge text. Live consequence: two independent readers reached a different call on one of three `matched` classifications in the first sealed consult (td-081-sealed-prior), moving the recorded novelty rate from 4/7 = 0.57 to 5/7 = 0.71 -- a material shift from a single reclassification, at n=1. A sealed prior recorded before the spawn predicted exactly this failure mode (an unsealed, interested-party, full-knowledge classification with no inter-rater check) and no challenge from the live consult matched it -- the register worked exactly as designed, preserving a concern the consultant never independently raised, which then proved live. Record for future ADR citations: the bias direction is the one the threat model did NOT anticipate. Over-matching -- classifying a challenge as 'matched' against a sealed prior when a stranger would call it 'novel' -- flatters the convener's OWN prior lens pass, not the consultant, and pushes the recorded novelty rate DOWN, the opposite of what a self-interested convener would be suspected of doing. Every stated defense in the sealed-prior design (the pre-spawn seal, round-0 isolation, the seal-witness commit check) constrains only WHEN priors are written; none constrains HOW GENEROUSLY a challenge is matched against them after the fact. Owner-role systems-architect, matching the td-071/td-076/td-080 precedent for this same sealed-prior measurement-design family. | 973e95cae5ee |
| td-105 | suggested | complexity | code-to-goals | fitness/tests/test_discipline_registry_invariants.py | code-quality |  | orchestrator | 2026-07-31 | 2026-07-31 | implementation-planner | open |  | [referred to the orchestrator by the gate-integrity-and-overlay architect, which is not one of the four sanctioned ledger writers] `fitness/tests/test_discipline_registry_invariants.py` is 3014 lines -- 3.8x the 800-line hard ceiling in `rules/swe/coding-style.md` -- with 17 named `check_*` functions and 88 test functions. No existing row covers this file (td-012/td-054/td-058 name other files). The gate-integrity-and-overlay work deliberately ADDS ~140 lines to it (extracting inline-asserting gates into named `check_*` functions per td-094), and deliberately places the new CONSULT append-only gate in its own file rather than here, precisely to avoid growing it further. Splitting it was excluded from that task by explicit user instruction, so the debt is filed rather than absorbed. Same class as td-012/td-054/td-058. Owner-role implementation-planner per the `complexity` + module-restructuring-required override in the canonical owner-role table. | 6479a9dcc319 |
| td-116 | nice-to-have | other | goals-to-code | rules/swe/staleness-policy.md, skills/skill-crafting/references/schema.md, skills/skill-crafting/scripts/validate.py, agents/sentinel.md | code-quality |  | orchestrator | 2026-08-05 | 2026-08-06 | context-engineer | open |  | [orchestrator, sentinel-remediation-i1] No marker state expresses "checked, and it is WRONG, fix pending". The staleness vocabulary has exactly three states: a date (verified-correct at that date), `permanent` (never stale), and absent (never verified, F07 WARN). Known-false content is therefore indistinguishable from merely-neglected content to the next agent that loads the skill -- which is the state td-114 sat in for ~10 weeks while `communicating-agents` served two non-running code snippets. The extended marker form has a `note:` field, but the policy is explicit that `by:`/`note:` are advisory and the sentinel ignores them for staleness calculation, so a note would be written and never surfaced. A real `verified-incorrect` state is a schema change across four surfaces: the policy rule, the skill-crafting schema doc, `validate.py`, and sentinel F07/F08/F09 (it would need to FAIL, not WARN -- known-false is worse than unverified, which inverts the current severity ordering). Deliberately NOT bundled into the td-114 content correction: that was a content fix, and this changes the measurement instrument. Value is highest for a skill that is verified-wrong but cannot be fixed in the same pass. // 2026-08-06 DEFERRED with reasoning, not dropped: this row's payoff is highest when a skill is verified-wrong but cannot be fixed in the same pass -- precisely the situation that no longer exists now that all six affected skills are corrected. Building the instrument with no live case to validate it against is weaker than waiting for the next one, and the design question it raises is real: a genuine `verified-incorrect` state must FAIL rather than WARN (known-false is worse than never-verified), which INVERTS the current severity ordering and is not a wording change. Deferred also because `rules/swe/staleness-policy.md` is always-loaded and the batch that would carry it had already spent 59 of 1,611 tokens of headroom. Re-open when the next verified-wrong-but-unfixable skill appears -- that instance is the validation case. | f00effefec5c |
| td-137 | suggested | coverage-gap | code-to-goals | skills/testing-strategy/references/test-topology.md, .ai-state/TEST_TOPOLOGY.md | architecture |  | orchestrator | 2026-08-07 | 2026-08-07 | systems-architect | open |  | [orchestrator, test-topology-adoption 2026-08-07] Three Built 3a components have no test group and cannot be given a sound one today: `Chronograph MCP`, `Eval framework` and `Pipeline Dashboard`. Objection raised by the systems-architect during `/refresh-topology --init` and recorded in `.ai-state/TEST_TOPOLOGY.md` section Objection. Cause is a genuine gap in Registry 1, not an authoring omission: every registered selector strategy (`pytest-globs`, `pytest-markers`, `pytest-keywords`, `manual`) materializes in the ROOT environment, but both `task-chronograph-mcp/` and `eval/` are self-contained uv projects requiring Python >=3.13 and their own dependency sets, and the dashboard runs vitest -- whose `vitest-projects` identifier the trunk lists explicitly as an UNREGISTERED future identifier. Inventing ids would yield selectors that cannot materialize, i.e. a dead seam wearing a group's clothes. The trunk's own Additive Leaf Escalation Clause prescribes the path: propose an `external-runner`-shaped strategy (name, argument shape, registered-by, concrete meaning) to the architect, record it as an ADR, append the row to Registry 1, then author the three groups. Until then the three components are recorded in the Subsystems table as `- (separate runner)` naming their concrete invocation, so they are visible rather than silently absent. | 5214105c942b |
| td-144 | suggested | drift | code-to-goals | .ai-state/DESIGN.md, docs/architecture.md | architecture |  | orchestrator | 2026-08-07 | 2026-08-07 | systems-architect | open |  | [orchestrator, important-findings-remediation 2026-08-07] Section 3b is unguarded in both directions, which is why it is where the two architecture docs drift. AC13 binds 3a; AC09's rule text binds 3a in both its subset and coverage clauses and explicitly states that 3b capabilities do not demand they appear. Sentinel finding I-15 was filed against AC09 but lies outside AC09's actual scope -- the divergence was real (an Existing-project onboarding row present in the guide, absent from the design doc; repaired this session) but no check covers it. A naive extension binding 3b on NAME EQUALITY would misfire: 8 further 3b pairs carry different labels that are each correct for their own document (the design doc names the language-open target, the guide names the Built set), reproducing the AC08 fire-on-correct-content failure this same batch fixed. Binding 3b correctly means binding on capability PRESENCE, not name -- a design question, not a widening. | 49a61323a031 |
| td-146 | suggested | coverage-gap | code-to-goals | scripts/check_agent_shared_blocks.py, scripts/check_paths_syntax.py, scripts/check_ruff_pin_drift.py, scripts/project_metrics/cli.py, scripts/prune_reports.py, scripts/render_claude_md.py, scripts/self_healing_metrics.py | code-quality |  | orchestrator | 2026-08-13 | 2026-08-13 | test-engineer | open |  | [orchestrator, deep-fix-round] ACTIVE CARRIER for the coverage-deferral register, succeeding td-135's carrier duty at its closure -- a terminal row migrates to TECH_DEBT_RESOLVED.md, and a register living only in terminal rows is invisible to active-ledger scans (the defect td-135 itself documented; this row exists so the register always has an active carrier). DEFERRAL REGISTER (all deliberately deferred: modest sub-70% coverage gaps in scripts/ gates with no gate-consuming caller; re-evaluate any entry under the two-clause trigger "drifts further OR gains a gate-consuming caller"): check_paths_syntax.py 56.3%, check_ruff_pin_drift.py 56.3%, prune_reports.py 58.3%, project_metrics/cli.py 60.4%, self_healing_metrics.py 65.7%, check_state_ledgers.py 67.0% (carried by td-133), render_claude_md.py 69.1%, check_agent_shared_blocks.py 65.7% (routed by sentinel S-10, 2026-08-07). Closes only by migrating carrier duty to a named successor row or by the register emptying. | 8dfbcde156ae |
| td-148 | minor | coverage-gap | code-to-goals | skills/agent-evals/, skills/observability/, skills/deployment/ | code-quality |  | verifier | 2026-08-30 | 2026-08-30 | doc-engineer | open |  | [verifier, rust-first-class pipeline, LEARNINGS harvest] The polyglot language-leaf pattern is unevenly applied across three skills that predate it: `agent-evals`, `observability`, and `deployment` each ship a TypeScript satellite with no Python counterpart. Surfaced by the rust-first-class language-surface research (34-row integration-point inventory) and explicitly declined as out of scope for that pipeline, which was scoped to Rust parity only -- so the finding is grounded and unaddressed rather than dispositioned. This is the inverse of the gap rust-first-class closed: there, Rust lacked leaves Python and TypeScript had; here, Python lacks leaves TypeScript has. Re-evaluation trigger: the next time any of the three skills is edited for a language-specific reason, or the next polyglot parity pass. Cheap to close -- each is an additive `references/` leaf under the same already-formalized extension protocol rust-first-class used, with no SKILL.md body restructuring required. | 754979499098 |
| td-149 | important | other | code-to-goals | .ai-state/CONSULT_LEDGER.md, .ai-state/CONSULT_COSTS.md, .ai-state/CONSULT_PRIORS.md | code-quality |  | orchestrator | 2026-08-30 | 2026-08-30 | systems-architect | open |  | [orchestrator, state-remediation session] The rust-first-class pipeline appended a post-boundary CONSULT_LEDGER row (evidence-appraiser, architecture stage) without its companion CONSULT_COSTS.md cost row, CONSULT_PRIORS.md sealed prior, or challenge classification -- three fitness gates now fail on main (test_discipline_registry_invariants: cost-row / sealed-prior / challenge-classified). The source data lived in that pipeline's ephemeral .ai-work/ artifacts and cannot be reconstructed honestly; the convener of the next consult (or a human with the original run records) must either backfill the three files or record an explicit tombstone the gates accept. | ce5dc201c2b7 |
| td-150 | suggested | complexity | code-to-goals | scripts/finalize_adrs_crossrefs.py, rules/swe/adr-conventions.md, agents/sentinel.md, dashboard_app/src/server/view-models/adr-graph.ts | code-quality |  | orchestrator | 2026-08-31 | 2026-08-31 | implementer | open |  | [orchestrator, adr-living-view consult CH-08 defer-with-rationale] The ADR edge-field vocabulary (supersedes/superseded_by, re_affirms/re_affirmed_by, retired_by, and the incoming supersedes_in_part pair) is hand-enumerated independently in four sites — the adr-conventions frontmatter table, sentinel DL04, sentinel DL06, and the dashboard AdrGraphNode reader (the implementation-planner verified finalize_adrs_crossrefs.py rewrites draft ids field-agnostically; the consult's fifth site, its 'rewrite field list', does not exist) — and adr-living-view adds two more (the index narrowing-marker renderer and the query-tool caveat renderer). retired_by already demonstrates the failure mode (absent from the dashboard reader). Consolidate into a single named registry (one list, consumed by the rewrite, asserted against by a drift test, in the artifact_registry.py pattern) the next time the field set changes; deferred from adr-living-view because a cross-cutting refactor mid-pipeline violates Stay Surgical and the set is small and slow-moving. | 156a4e4f5c49 |
| td-151 | important | coverage-gap | code-to-goals | agents/implementation-planner.md, fitness/tests/test_discipline_registry_invariants.py | code-quality |  | verifier | 2026-08-31 | 2026-08-31 | implementation-planner | open |  | [verifier, adr-living-view Phase 10] TEST_BASELINE.md is captured with the canonical `pytest tests scripts hooks` invocation, which does not cover `fitness/`. Any fitness-tier regression is therefore invisible to the baseline protocol and lands in the verifier's lap as an undispositionable failure: the adr-living-view run found 3 red fitness tests, 1 a genuine regression from this pipeline and 2 pre-existing (already td-149), with no baseline row to separate them. The planner's baseline-capture step should either run the fitness tier as a second baselined invocation or record explicitly that fitness/ is out of baseline scope so the verifier knows to treat its failures as unbaselined rather than pre-existing. Cheap to close: one extra invocation in the capture step plus a second fenced block in the TEST_BASELINE.md template. | 48a26683b561 |
| td-038 | important | other | code-to-goals | scripts/check_id_citation_discipline.py | code-quality |  | verifier | 2026-05-15 | 2026-09-03 | implementer | resolved | 2fcc0b35 | The inbound id-citation discipline check (`scripts/check_id_citation_discipline.py`, invoked via `hooks/commit_gate.sh` on every `git commit`) skips extensionless bash scripts — it scans only files matching a configured extension whitelist (likely `.py`, `.ts`, `.tsx`, `.js`, `.md`, etc.). The Phase 2 dispatch-reworks pipeline shipped `scripts/dispatch-reworks` (extensionless, executable bash following Praxion's `scripts/ccwt` precedent). The verifier pass discovered two id-citation violations in that file (line 300: "Step 1 smoke-check"; line 377: "...in Step 6") that the inbound gate would have allowed through to merge. The companion test-file violation at `tests/test_dispatch_reworks_bg.py:505` WAS caught by the gate (Python extension). So: every future extensionless Praxion script that ships from a pipeline has the same coverage hole. Resolution path: extend `check_id_citation_discipline.py`'s file selection to detect bash scripts by shebang inspection (`head -n1 == #!/usr/bin/env bash` or `#!/bin/bash`) in addition to extension matching; also include the `scripts/` directory's extensionless executables in the scan corpus. Test coverage: add a regression test that creates an extensionless bash file with a forbidden id-citation pattern, runs the checker, and asserts a violation is reported. Severity `important` because every Phase-N pipeline using bash scripts inherits this gap. // recurrence: re-opened 2026-09-03 [orchestrator, sidecar-placement Step 8 review] the bash-shebang fix left extensionless PYTHON executables unscanned: check_id_citation_discipline.py --files scripts/praxion-sidecar reports 0 files scanned (also scripts/praxion-parallel-ui); extend the shebang detection to python and add an extensionless-python canary | b5c427341a99 |
| td-153 | suggested | drift | goals-to-code | .ai-state/specs/SPEC_agent-model-routing_2026-04-25.md, .ai-state/DESIGN_CHANGELOG.md, .ai-state/sentinel_reports/, .ai-state/calibration_log.md, .ai-state/TECH_DEBT_LEDGER.md | adr |  | orchestrator | 2026-09-03 | 2026-09-03 | systems-architect | open |  | [orchestrator, sidecar-placement integration pass, scenario A-05] A finalize dry run over a clean extraction promoted this branch's 13 drafts cleanly, but 7 pre-existing files still carry dangling dec-draft-<hash> citations outside the bounded rewrite allowlist (rules/swe/adr-conventions.md § Finalize Protocol): an archived SPEC, DESIGN_CHANGELOG.md and three sentinel snapshots are not allowlisted at all, while calibration_log.md and TECH_DEBT_LEDGER.md are allowlisted and drifted anyway (their originating feature's finalize never rewrote them). Widen the allowlist for the first group per the rule's own instruction, or declare snapshots frozen; re-run finalize's detect_unrewritten_ids over the second group. Not introduced by this branch. | a5bcc36180b9 |
| td-154 | suggested | coverage-gap | code-to-goals | scripts/_sidecar_inputs.py | code-quality |  | orchestrator | 2026-09-03 | 2026-09-03 | test-engineer | open |  | [orchestrator, sidecar-placement integration pass, fixer F3] No test exercises _sidecar_inputs.gather() end to end -- the wiring layer that reads the world once into CheckInputs is covered only indirectly through the CLI tests, so a mis-threaded field (the retired guards_unreadable_by was one) survives until a doctor row reads wrong. Add scripts/test_sidecar_inputs.py driving gather() over a mounted fixture and asserting every CheckInputs field. | f168286087fd |
| td-155 | suggested | other | code-to-goals | hooks/test_check_code_quality.py, hooks/test_lang_tools.py, scripts/check_design_checkpoint.py, scripts/test_adr_health.py, scripts/test_check_adr_frontmatter_promotion.py, scripts/test_check_design_checkpoint.py, scripts/test_query_adrs.py, tests/commands/test_onboard_capability_defaults.py, tests/commands/test_onboard_ci_autofix_install.py, tests/commands/test_onboard_gate_consolidation.py, tests/commands/test_onboard_hackathon_teardown.py, tests/commands/test_onboard_labels_install.py, tests/onboard_project_test.sh, tests/test_spec_drift.py | claude-md | id-citation-discipline | orchestrator | 2026-09-03 | 2026-09-03 | implementer | open |  | [orchestrator, sidecar-placement integration pass, closing checkpoint] A repo-wide run of check_id_citation_discipline.py over a clean extraction exits 1 on these 14 files, none touched by the sidecar-placement branch; the commit gate scopes itself to the files a commit stages, so a backlog that predates the gate's hook-mode hardening stays invisible until someone edits one of them. Sweep them once with the id-decontamination skill (/decontaminate-ids), or add a CI job running the checker repo-wide so the backlog cannot regrow. td-038's python-shebang fix widened the corpus but did not cause these. | 590539158ba2 |
| td-156 | suggested | other | goals-to-code | scripts/_sidecar_mount.py | architecture | DESIGN.md#3b9-self-healing-loop-canonical-blocks-sidecar-and-issue-autofix | orchestrator | 2026-09-03 | 2026-09-03 | systems-architect | open |  | [orchestrator, sidecar-placement integration pass, scenario B-07, user decision 2026-09-03] Under sidecar placement the main checkout's state mount is a git worktree of the sidecar on branch main, one per project per machine. A second clone of the same project on the same machine cannot link: git worktree add refuses the branch and praxion-sidecar link raises MountBranchInUse naming the holding checkout and the sanctioned alternative (a linked worktree created from the holding checkout). Accepted as the contract rather than per-root branch naming (main, main@2 ...), which would add a new convergence shape for extra mains. Reversal trigger: a second operator sharing one machine, or CI-style disposable clones of a sidecar-placed project. Documented in the state-mount ADR's Consequences and in docs/onboarding.md. | 15ebfb3b1a98 |
| td-157 | important | drift | code-to-goals | scripts/check_id_citation_discipline.py, scripts/_sidecar_manifest.py, scripts/install_git_hooks.py, scripts/_sidecar_checks.py, scripts/_sidecar_inputs.py | claude-md | id-citation-discipline | verifier | 2026-09-04 | 2026-09-04 | implementer | open |  | [verifier, sidecar-placement Phase 5] Two identifier families violate rules/swe/id-citation-discipline.md in shapes the mechanical checker cannot see. (1) DS-N -- SYSTEMS_PLAN.md sec Data Structures ids -- appears in 27 files, every one in this branch's file set (zero repo precedent outside it); several cite the ephemeral document by name, e.g. _sidecar_manifest.py:2 and install_git_hooks.py:14. The archived SPEC carries no DS matrix, so they resolve to nothing once .ai-work/ is deleted. (2) P1-14, a literal acceptance criterion, at _sidecar_checks.py:303 and _sidecar_inputs.py:55; the rule's table says AC-NN never, and P1-14 is an AC-NN in this project's local numbering. check_id_citation_discipline.py:149-204 has no DS pattern and matches criteria only as AC-\d+, so the gate stays green. IF-15's disposition (rephrase every site behaviourally) was executed only over the checker-visible shapes -- those are confirmed clean at HEAD, including the test_upgrade_project_pins.py straggler. Fix: add a DS-\d+ pattern plus a criterion-shape pattern to the checker, then sweep. Without the pattern the class regrows silently. Behavioral-contract tag: UNSURFACED-ASSUMPTION (green gate assumed to equal satisfied rule). | 5a5c8a9b2835 |
| td-158 | important | drift | goals-to-code | .ai-state/DESIGN.md, docs/architecture.md | architecture | DESIGN.md#state-placement-in-repo-sidecar | verifier | 2026-09-04 | 2026-09-04 | doc-engineer | resolved | 42462e1c | [verifier, sidecar-placement Phases 8-9] Four doc-vs-code mismatches in the two architecture documents after the 9be5f858 mount rename and the delivered module split. (a) .ai-state/DESIGN.md:141 still names the mount .praxion/ in three places -- the state-mount sentence, the dashboard allowlist constant (.praxion/.ai-state), and the _sidecar_mount.py parenthetical -- while _sidecar_mount.py:49 sets MOUNT_DIRNAME to .praxion-state and project-root.ts:15 allowlists .praxion-state/.ai-state. docs/architecture.md was updated by the c848da39 / 9be5f858 pass, DESIGN.md was not. This matters beyond cosmetics: .praxion/ is the documented team-committed recipes directory (dec-113), the collision IF-11 filed and the rename exists to resolve, so DESIGN.md tells an architect the mount occupies exactly the directory the feature was changed to vacate. (b) Both docs say fourteen _sidecar_*.py sibling modules; there are 13 on disk (the fourteenth is _state_repo.py, listed separately in the same sentence). (c) Both say a 545-line entry point; scripts/praxion-sidecar is 613. (d) Both assert every module under the 800-line ceiling, which is now false -- see td-159. The third and fourth turn a code-verified guide into a reason not to check. Also outstanding from AUDIT_PHILOSOPHY sec 1.11: DS-3's claim that no-full-parse-on-the-hot-path is a property of the type is false as built (ManifestView/MinimalView were correctly never constructed), and the correction is recorded in the archived SPEC but not in DESIGN.md. One edit pass closes all five. | b01019c6b571 |
| td-159 | suggested | complexity | code-to-goals | scripts/_sidecar_mount.py | code-quality |  | verifier | 2026-09-04 | 2026-09-04 | implementer | open |  | [verifier, sidecar-placement Phase 5] scripts/_sidecar_mount.py is 835 lines, over the 800-line hard ceiling in rules/swe/coding-style.md sec File Size. It is the only breach in the family; the other 14 modules run 204-751. The module carries two cohesive-but-separable concerns -- the mount lifecycle (classify_mount / create_mount / repair_mount / prune_mount, roughly 390 lines) and state-branch classification plus convergence (classify_branch / converge / merge_back / drop_branch, roughly 400 lines) -- and _sidecar_mergeback.py (319 lines) already exists as the natural home for the second. The module-split ADR's own recursive-split rationale applies. Split by cohesion at that seam; the split also makes the docs' every-module-under-the-ceiling claim true again (td-158d). | 2b963873fd5e |
| td-160 | important | coverage-gap | code-to-goals | scripts/refresh_claude_blocks.py, scripts/test_refresh_claude_blocks.py | code-quality |  | verifier | 2026-09-04 | 2026-09-04 | implementer | resolved | 42462e1c | [verifier, sidecar-placement Phase 5] refresh_claude_blocks._claude_md_target (:167-174) resolves placement and then returns repo_root/CLAUDE.md for every non-SidecarOwned variant -- so Dangling, Foreign and NotYetLinked all take the in-repo fallback. Concrete failure: a sidecar-placed project whose mount is missing (removed sidecar, a worktree prune in the sidecar, a moved sidecar root -- every state doctor already reports as placement FAIL) then has /refresh-claude-blocks --apply, or onboarding Phase 6 which delegates to this script in apply mode, append the Praxion blocks to the project's TRACKED CLAUDE.md. That is exactly the write P1-10 and REQ-30 promise never happens, and it lands as a tracked-file modification in git status, breaking the invisibility promise in the same stroke. This is the failure _state_repo.require_writable_placement() exists to make unreachable -- its docstring says there is no way to reach a Dangling or Foreign placement through this entry point and write into it anyway -- and this writer does not call it. Coverage: test_refresh_claude_blocks.py:965 covers only the healthy sidecar arm; no test drives a broken placement. Fix: route through require_writable_placement (or an explicit refusal naming the resolved target) plus one test per error variant. Behavioral-contract tag: UNSURFACED-ASSUMPTION (unresolvable placement silently equated with no sidecar). | 0dae13a4e0e5 |
| td-161 | suggested | other | code-to-goals | scripts/_sidecar_git.py, scripts/_sidecar_mount.py | code-quality |  | verifier | 2026-09-04 | 2026-09-04 | implementer | open |  | [verifier, sidecar-placement Phase 3, P1-19 qualification] _sidecar_git.patch_already_applied (:260-281) writes one unreachable commit object into the PROJECT repository (git commit-tree, before the git cherry probe) each time it runs. _merge_evidence calls it only after the cheap ancestor test fails -- which is precisely the set of branches classified UnmergedIneligible, and REQ-40 guarantees those survive every automatic channel untouched forever. So a project carrying one long-lived ineligible wt/* branch writes a fresh loose object into its own object store on every converge run, i.e. every SessionStart heal and every on-main post-merge. P1-19's zero-writes-at-the-fixed-point guarantee is scoped to the sidecar repository and the mounts, so this is outside the criterion's letter but inside its intent, and the project repo is the one the whole feature exists to keep untouched. Mitigations already in place: the objects are unreachable (invisible to git status and git log, so P1-1 is not broken) and reclaimed by routine gc, and the docstring states the cost honestly. Cheap fix if wanted: memoise the probe result per (head, ref) for the life of a converge run, or cache the negative verdict in the branch config alongside the project-branch mapping so a branch proven ineligible against a given head is not re-probed. | 9543d35cd533 |
| td-162 | suggested | coverage-gap | code-to-goals | skills/onboard-project/references/phases-optional.md, scripts/test_resolve_test_scope.py, tests/test_plugin_namespace_invariants.py | code-quality |  | verifier | 2026-09-04 | 2026-09-04 | test-engineer | open |  | [verifier, sidecar-placement Phase 10] Two independent evidence gaps, both about tests that were never run rather than tests that failed. (a) P1-15 / REQ-33-34: the plan-level half is proven (F-01 shows --check --json --with ci giving rc=8 and unavailable:[ci]; F-02 shows no .github/workflows/architecture.yml written), but the enforcement half is NOTRUN in both scenarios -- the one-line refusal text, the actual .github write block (phases-optional.md sec 8e.8-8e.9), the architecture/ and fitness/ shadow rows, and the Block-D-into-local-chain install (sec 8b.3) all live session-side and the stubbed claude never ran them; J-05's real pipeline did not exercise a --with ci or --with aac onboarding. The criterion's observable is asserted by design docs and not demonstrated. Close with one real (non-stubbed) onboarding session per capability. (b) Three pre-existing failures flip verdict with working-tree dirtiness and carry no ledger row: test_resolve_test_scope.py::TestGlobSemantics::test_single_star_does_not_cross_a_separator_end_to_end and ::test_bare_directory_pattern_does_not_expand_to_its_subtree fail in a clean git archive extraction and pass in the working tree, while test_plugin_namespace_invariants.py::test_historical_records_are_left_intact fails in any clean tree and its sibling test_no_live_surface_carries_the_retired_namespace fails only in the dirty tree -- the pair swaps on whether the uncommitted observations WAL is present. All three fail identically at the branch base (216dadcf), so they are not this pipeline's responsibility, but pre-existing is not a disposition. A suite whose verdict depends on untracked working-tree state is green locally and red in CI or the reverse, which is the failure mode TEST_BASELINE.md's own verifier note exists to guard against. Make the three read their inputs from the committed tree, or mark them as requiring a clean extraction. | 35c984241911 |
