# Technical Debt Ledger

<!-- Living, append-only ledger of grounded debt findings.
     Producers: verifier (per-change Phase 5/5.5), sentinel (repo-wide TD dimension).
     Consumers: systems-architect, implementation-planner, implementer, test-engineer, doc-engineer.
     Schema, owner-role heuristic, worktree-merge dedupe semantics, and lifecycle conventions
     are defined canonically in skills/software-planning/references/tech-debt-ledger.md
     (rules/swe/agent-intermediate-documents.md retains a 5-line summary + pointer).
     Do not duplicate the schema here — the skill reference is the single source of truth. -->

**Schema**: 14 row fields + 1 structural `dedup_key`. See [`skills/software-planning/references/tech-debt-ledger.md`](../skills/software-planning/references/tech-debt-ledger.md) § `TECH_DEBT_LEDGER.md` for field definitions, enum values, the owner-role heuristic, and the post-merge dedupe contract.

**Append new rows at the end of this active ledger.** Consumers update `status`, `resolved-by`, and `last-seen` in place; when status transitions to `resolved` or `wontfix`, the row migrates to the sibling [`TECH_DEBT_RESOLVED.md`](TECH_DEBT_RESOLVED.md) (cut + paste, in-commit or via `scripts/finalize_tech_debt_ledger.py`). The pair forms one logical namespace — see the rule for re-open semantics.

| id | severity | class | direction | location | goal-ref-type | goal-ref-value | source | first-seen | last-seen | owner-role | status | resolved-by | notes | dedup_key |
|----|----------|-------|-----------|----------|---------------|----------------|--------|------------|-----------|-----------|--------|-------------|-------|-----------|
| td-012 | important | complexity | code-to-goals | scripts/project_metrics/report.py | code-quality |  | sentinel | 2026-05-01 | 2026-05-17 | implementation-planner | open |  | Rank-2 hotspot (score=21,399): complexity=21, churn=1,019 over 90d; central reporting aggregator in active development; complexity trend warranted monitoring before it reached the 800-line ceiling. // 2026-05-13: re-severitied suggested→important and owner-role implementer→implementation-planner — `report.py` is now 929 lines, OVER the 800-line ceiling in `rules/swe/coding-style.md` it was being monitored toward (still rank-3 hotspot, score=21,399, in METRICS_REPORT_2026-05-13). Module split warranted — extract per-collector rendering subsections to a `report_sections/` package, or split aggregate/trend/hotspot rendering from the orchestration shell. Owner-role flipped per the "module restructuring → implementation-planner" heuristic, matching td-003 / td-004; revert to implementer if a modest extraction (vs. a full decomposition) turns out to suffice. | 62a324775090 |
| td-037 | suggested | complexity | code-to-goals | eval/src/praxion_evals/harness/families/family1_pipeline_fidelity.py | code-quality |  | verifier | 2026-05-26 | 2026-05-26 | implementer | open |  | [F-04 from praxion-self-eval-v1 verification] `_check_supersession_reciprocity` is 78 lines, exceeding the 50-line ceiling in `rules/swe/coding-style.md`. Refactor opportunity — extract helper functions for the symmetric-pair detection and back-link checking phases. Not blocking; v1 ships as-is. | 550d3cf99cc9 |
| td-054 | important | complexity | code-to-goals | scripts/finalize_adrs.py | code-quality |  | verifier | 2026-06-19 | 2026-06-19 | implementation-planner | open |  | Originally filed as td-039; renumbered to td-054 to resolve id collision, 2026-07-15. [W-01 from finalize-consumer-bugs verification] `scripts/finalize_adrs.py` is 996 lines, over the 800-line hard ceiling in `rules/swe/coding-style.md` (same class as td-012 for report.py). Pre-existing and NOT introduced by the consumer-bug fixes — those added ~140 lines but the file was already ~895 before. The shared repo-root resolver was already extracted to `scripts/_repo_root.py` this pass (retiring 5x duplication, −~45 lines), which did not cross the ceiling. Next cohesive extraction: the fragment-filename parser (`parse_fragment_filename` + `_split_user_branch_slug` + `_discover_branch_from_siblings` + `_parse_via_siblings` + `_collect_peer_*` + `_dash_aligned_common_prefix` + `_read_draft_branch` + `_sanitize`, ~200 lines) into a `finalize_adrs_parsing.py` sibling — would bring the file under ~800. Deferred deliberately: that parser has prior-bug history (td-017 hyphenated-branch ambiguity) and couples to the module `_git`/`REPO_ROOT` globals + frontmatter patterns + logger, so a clean extraction needs care (pass git-runner/patterns as params or move them too) and its own focused pass rather than a tail-of-session rush. Owner-role implementation-planner per the "module restructuring → implementation-planner" heuristic (matches td-012). | b12531a53419 |
| td-056 | suggested | duplication | code-to-goals | scripts/refresh_claude_blocks.py | code-quality |  | verifier | 2026-07-16 | 2026-07-16 | implementer | open |  | [verifier, block-refresh-mechanism] `refresh_claude_blocks.py::_find_block_span` (lines 244-259) re-implements `canonical_block_identity.extract_live_body`'s heading->next-`## `-or-EOF boundary logic verbatim rather than calling a shared primitive. Traced byte-identical today; the duplication is a disclosed, scoped trade-off (the apply-mode step's file scope was `refresh_claude_blocks.py` only, and the shared module returns text not line-indices). Residual risk: nothing enforces the two boundary functions stay in sync, so a future edit to one (e.g. an H3-nesting or fenced-block rule) could silently diverge classify-time extraction from apply-time span computation. Fix: expose a shared boundary-index primitive from `canonical_block_identity.py` that both `extract_live_body` and `_find_block_span` call. Secondary (same fix direction, lower priority): `run_apply` re-reads the target `CLAUDE.md` independently of the read `_classify_all` already did -- a TOCTOU double-read with zero realistic trigger in the single-threaded CLI path; thread the already-read text through. Not blocking; refuse-to-clobber safety is unaffected (failure direction stays safe). | 6d6738daf6b6 |
| td-057 | suggested | maintenance | code-to-goals | .github/workflows/reusable-ci-autofix.yml | code-quality |  | verifier | 2026-07-26 | 2026-07-26 | implementer | open |  | [verifier, pr-dependabot-autofix] The autofix workflows (reusable-ci-autofix.yml, ci-autofix.yml, issue-autofix.yml) pin claude-code-action@51ea8ea (v1); the v1 tag has since moved ~60 releases to v1.0.183 (be7b93b). P3a deliberately kept the audited 51ea8ea for consistency + surgical scope (a fleet-wide bump is out of P3a's scope; light-review + verifier both confirmed keeping the audited pin is correct). Owed: a deliberate, audited lockstep bump of claude-code-action across all autofix workflows to a current pinned SHA, verifying no breaking changes. Not urgent — the audited pin works. | 52d4569e37a7 |
| td-058 | important | complexity | code-to-goals | codex/config/export-codex-rules-bridge.py | code-quality |  | sentinel | 2026-07-29 | 2026-07-29 | implementation-planner | open |  | Rank-3 hotspot (score=39,842): complexity=22, churn=1,811 over 90d; file is 1,435 lines, nearly double the 800-line ceiling in `rules/swe/coding-style.md`. High, sustained churn (codex-bridge export logic under active iteration) combined with size well past the ceiling signals a module-split candidate rather than routine growth — same class as td-012/td-054. No existing ledger row references this file. Owner-role implementation-planner per the "module restructuring required" heuristic (matches td-012/td-054 precedent). | 0eeb47c32516 |
| td-059 | important | coverage-gap | code-to-goals | scripts/aac_fence_validator.py, scripts/spec_drift.py | code-quality |  | sentinel | 2026-07-29 | 2026-07-29 | test-engineer | open |  | Both files sit well under the project coverage floor (70%): `spec_drift.py` 16% (per METRICS_REPORT_2026-07-29_22-11-31), `aac_fence_validator.py` 31%. Elevated to `important` (not `suggested`) because both are gate-liveness-critical — `spec_drift.py` backs sentinel SH07 (spec-drift detection) and `aac_fence_validator.py` backs sentinel AC10 (fence integrity); per gate-liveness.md a thinly-tested detector risks a silent false-PASS on the very defects it exists to catch. Four additional scripts sit modestly under floor (`check_paths_syntax.py` 56%, `prune_reports.py` 57%, `project_metrics/cli.py` 60%, `self_healing_metrics.py` 66%) — not filed this pass to avoid ledger noise; re-evaluate if they drift further or gain gate-consuming callers. | 56b1260c734b |
