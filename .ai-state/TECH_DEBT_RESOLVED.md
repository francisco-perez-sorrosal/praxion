# Resolved Tech Debt

<!-- Sibling of TECH_DEBT_LEDGER.md holding rows with terminal status (resolved / wontfix).
     Rows arrive via scripts/finalize_tech_debt_ledger.py migration when status transitions
     to a terminal value. Schema, lifecycle, and re-open semantics are defined canonically
     in skills/software-planning/references/tech-debt-ledger.md § Schema. Do not duplicate
     the schema here — the skill reference is the single source of truth (F-12). -->

**Schema**: 14 row fields + 1 structural `dedup_key`. See [`skills/software-planning/references/tech-debt-ledger.md`](../skills/software-planning/references/tech-debt-ledger.md) § Schema for field definitions.

**Rows arrive here automatically** when status transitions to `resolved` or `wontfix`. The pair forms one logical namespace with `TECH_DEBT_LEDGER.md`: `id` and `dedup_key` are unique across both files. Cross-file `dedup_key` matches trigger re-open (the historical resolved row moves back to LEDGER).

| id | severity | class | direction | location | goal-ref-type | goal-ref-value | source | first-seen | last-seen | owner-role | status | resolved-by | notes | dedup_key |
|----|----------|-------|-----------|----------|---------------|----------------|--------|------------|-----------|-----------|--------|-------------|-------|-----------|
| td-055 | suggested | other | goals-to-code | commands/onboard-project.md | claude-md |  | orchestrator | 2026-07-16 | 2026-07-16 | implementation-planner | resolved | orchestrator (4a40d01) | Onboarded CLAUDE.md blocks never refresh: Phase 6's idempotency predicate is per-heading (heading present → skip append), so canonical-block updates (e.g. the td-002 agent-pipeline trim, dec-270) never propagate to already-onboarded projects, and appended blocks carry no version marker to distinguish stale-canonical from locally-modified content. Fix sketch: (1) append blocks with an inner marker comment carrying the canonical content hash; (2) add a refresh mode to /onboard-project (or a --refresh-blocks flag) that on heading-present compares the body — replace when it matches any historical canonical version, surface a diff when locally modified; (3) pre-marker projects are matched against the canonical file's git history (the one-off estate refresh of 2026-07-16 used exactly this). // 2026-07-16 (pipeline block-refresh-mechanism): implementation complete in worktree — manifest generator + --check-history pre-commit gate, shared identity module, refresh_claude_blocks.py three-way classify/apply with refuse-to-clobber canary, /refresh-claude-blocks command, Phase 6 delegation, docs. Flips to resolved with the merge sha at merge-to-main. // 2026-07-16: in-flight via block-refresh-mechanism pipeline. Design chose MANIFEST-ONLY (dec-271) over the sketch's in-file marker (step 1 dropped) — a shipped git-derived historical-hash manifest classifies all estates marker-free, meeting the sketch's intent (steps 2+3) at zero always-loaded token cost. Resolve to resolved-by <merge-sha> at implementation. | 96681362dd98 |
| td-002 | important | other | code-to-goals | claude/canonical-blocks/agent-pipeline.md | code-quality |  | orchestrator | 2026-04-27 | 2026-07-16 | implementation-planner | resolved | orchestrator (bac3ba3) | Agent-pipeline canonical block is ~1818 bytes (~505 tokens), exceeding per-block ~250-token budget by ~2x; pre-existing condition surfaced during td-001 refactor, not introduced by it. Trimming changes downstream injection contract — needs content review and separate ADR. Currently xfailed in `hooks/test_onboard_praxion_block.py::test_block_stays_within_token_budget[agent-pipeline]`. Filed by orchestrator mid-pipeline (slight protocol stretch — canonical sources are verifier and sentinel); verifier may re-source. Class `other` because no enum slot covers content-size-budget violations. // 2026-05-17: re-verified by sentinel audit + orchestrator sweep — the block has grown to 1,921 bytes / ~549 tokens (was ~1,818 B / ~505 tokens at filing); still ~2x the per-block ~250-token budget, and the debt is widening rather than holding. The xfail reason string in `hooks/test_onboard_praxion_block.py` still cites the stale ~1,818 B figure — worth refreshing when the block is next touched. // 2026-07-16: RESOLVED (dec-270) — block restructured to a ~738 B / ~205-token pointer (universal framing + prose pointer to the coordination rule and software-planning skill + absolute docs URL); the PoC-journey paragraph relocated to software-planning SKILL.md § End of Feature (only non-derivable content); xfail removed, token-budget test green for all four blocks. Context-engineer audit found ~70-80% of the old block redundant with always-loaded rules on Praxion machines and inert without them. Refresh of already-onboarded projects tracked as td-055. | deee2e5a8b62 |
| td-049 | suggested | coverage-gap | code-to-goals | hooks/inject_worktree_paths.py, hooks/inject_subagent_context.py, hooks/hooks.json | code-quality |  | orchestrator | 2026-07-01 | 2026-07-02 | verifier | resolved | orchestrator (dec-266, fe6a1db) | [debt-batch-2] Two PreToolUse(Agent) hooks now co-register and can BOTH emit updatedInput (inject_subagent_context for host-native agents; inject_worktree_paths for any agent when the session is in a linked worktree). Whether the harness CHAINS updatedInput across multiple matching hooks (vs first- or last-writer-wins, silently dropping one injection) is an unverified hook-harness contract assumption — the exact class that produced the td-021 envelope breakage. Filed by orchestrator from a standalone session under the dec-261-widened exception. Resolution: live probe from a fresh worktree session with the updated plugin installed — spawn a host-native agent and assert BOTH the Praxion preamble and the worktree-root briefing line arrive in its prompt; if not chained, merge the two injections into one hook. // 2026-07-02: RESOLVED by elimination per dec-266: both injections consolidated into inject_subagent_context.py as the single PreToolUse(Agent) updatedInput emitter — the multi-hook chaining question is structurally moot (inject_worktree_paths.py deleted; hooks.json row removed, 29→28). Live end-to-end proof via dev-linked cache: a host-native Explore probe quoted the composed injection verbatim. | 7c1e2a90d034 |
| td-050 | important | other | code-to-goals | dashboard_app/src/components/viz/diagram-modal.tsx | code-quality |  | verifier | 2026-07-02 | 2026-07-02 | implementer | resolved | implementer (7099578) | [dashboard-debt-trio verification] Pre-existing WCAG 2.2 AA defect, out of the debt-trio's scope. `DiagramModal`'s outer backdrop `<div className="diagram-modal__backdrop" ... aria-hidden="true">` (diagram-modal.tsx:126-130) wraps the `<div role="dialog" aria-modal="true" aria-labelledby=...>` (lines 132-138) as its child. `aria-hidden="true"` on an ancestor removes the entire subtree from the accessibility tree, so the dialog — despite correct `role="dialog"`/`aria-modal`/`aria-labelledby`/focus-trap — is invisible to assistive technology while open. Surfaced during the td-024 viz render-tests: `screen.getByRole("dialog")` finds nothing after the Expand click; only `getByRole("dialog", { hidden: true })` or a raw `querySelector` locates it, and the new `diagram-frame.test.tsx` uses `{ hidden: true }` with an inline note to route around the bug rather than mask it. The pre-existing node-env test `tests/server/diagram-frame.test.ts` never caught it because it string-matches raw HTML without evaluating ARIA visibility. Resolution: drop `aria-hidden="true"` from the backdrop (the dialog's own `aria-modal="true"` already conveys modal semantics), or restructure so the backdrop is a sibling of the dialog rather than its ancestor; then tighten the render assertion to `getByRole("dialog")` without `{ hidden: true }`. Class `other`: no enum slot covers accessibility-conformance defects. // 2026-07-02: RESOLVED: DiagramModal restructured so backdrop and dialog are siblings (aria-hidden no longer an ancestor of role=dialog); regression test proven red on unfixed code (getByRole dialog threw) then green + aria-modal asserted; dashboard suite 457/457, stacking/centering preserved via CSS split. | c1783e020878 |
| td-051 | suggested | coverage-gap | code-to-goals | hooks/inject_worktree_paths.py, agents/*.md briefing discipline | code-quality |  | orchestrator | 2026-07-02 | 2026-07-02 | implementation-planner | resolved | implementer (fe6a1db, dec-266) | [dashboard-debt-trio verification WARN-2] The cwd-bifurcation direction NOT covered by inject_worktree_paths.py: a CANONICAL-checkout session briefing an agent INTO a worktree gets no deterministic path injection (the hook fires only when the spawning session itself is in a linked worktree) — live incident: a doc-engineer edited+committed in canonical despite an absolute-worktree-path briefing; caught by a sibling step's cross-check, commit relocated via cherry-pick + soft-reset. Verifier recommended extending td-034; filed as a NEW row instead because td-034 is terminal and the ledger is append-only (deviation registered). Resolution: extend the hook (or a sibling) to fire when the spawned prompt names a worktree root unequal to the session root; interim mitigation — route cross-tree-briefed steps to sonnet+, never haiku. Cross-refs: td-034 (resolved), td-049 (chaining probe). // 2026-07-02: RESOLVED: the consolidated hook adds briefed-root detection — when the outgoing prompt names a .claude/worktrees/<name> path unequal to the session root, a [Worktree briefing] absolute-path mandate is injected for ALL agent types. Canary proves gate independence (canonical fixture + named worktree → briefing fires, session-line absent); LIVE-verified same-session via dev-link + Explore probe quoting the line verbatim. | 9f21c3e5a801 |
| td-052 | suggested | other | code-to-goals | scripts/finalize_adrs.py | code-quality |  | orchestrator | 2026-07-02 | 2026-07-02 | implementer | resolved | implementer (01c826e) | [dashboard-debt-trio merge] finalize_adrs fragment-filename parser mis-split a fragment whose USER slug is itself hyphenated (francisco-perez-sorrosal vs the expected fperezsorrosal) EVEN THOUGH the branch: frontmatter disambiguator was present (branch: dashboard-debt-trio) — promoted slug came out as sorrosal-dashboard-debt-trio-renderer-field-resolver-key; repaired by manual rename to 265-renderer-field-resolver-key.md + index regen. The branch-frontmatter path apparently is not consulted (or not preferred) for the user/slug split. Same ambiguity class as the td-017 history. Resolution: make the parser strip a matched branch: value from the filename FIRST, then split user/slug; add a hyphenated-user-slug regression test; consider validating fragment author slugs at creation. Class other: filename-parsing defect. // 2026-07-02: RESOLVED: root cause — the branch: frontmatter check was nested inside the user-hint-match branch, so nonconforming hyphenated user slugs bypassed it; fixed as an unconditional Tier-0 strip of the branch token run before the user/slug split. Red-then-green on the exact historical filename; conforming-path behavior characterized unchanged; 46/46 parser tests. | 4b7d61c2ee90 |
| td-024 | suggested | coverage-gap | code-to-goals | dashboard_app/tests/ | code-quality |  | orchestrator | 2026-05-11 | 2026-07-02 | test-engineer | resolved | test-engineer/implementer (dashboard-debt-trio) | React component render tests (jsdom + `@testing-library/react`) deferred — no jsdom dep in v1. v1 covers server view-models + pure parsers/helpers + `usePanZoom` math via synthetic events. Resolution: `pnpm add -D jsdom @testing-library/react @testing-library/user-event` + vitest jsdom env config + tests for DiagramViewer, DecisionGraph, TrendChart, shells. User-confirmed defer 2026-05-11. // 2026-07-02: RESOLVED by the dashboard-debt-trio pipeline: jsdom + @testing-library infra via per-file @vitest-environment docblocks (zero config bifurcation, zero server-test exposure); render/interaction tests for DiagramFrame/DecisionGraph/TrendChart + shells + renderers; suite 404 -> 447. Known jsdom limits documented (recharts hover untestable -> legend-click interaction substituted). | 70176a5a9742 |
| td-025 | suggested | other | code-to-goals | dashboard_app/src/components/shells/ | code-quality |  | orchestrator | 2026-05-11 | 2026-07-02 | implementation-planner | resolved | implementer (dashboard-debt-trio) | Dedicated shell chrome for `tutorial`, `how-to`, and `concepts` Diátaxis types deferred. Currently these three types alias `DefaultShell`. Resolution: build dedicated layout components (tutorial: step-numbered layout; how-to: goal + steps layout; concepts: narrative with key-takeaway callout) and register them in `RENDERER_REGISTRY`. Class `other`: no enum slot covers deferred-feature gaps. // 2026-07-02: RESOLVED: TutorialShell (ordered step-rail), HowToShell (goal banner), ConceptsShell (key-takeaway aside) are real components with distinguishing-element render tests; shells/index.ts aliases flipped; registry untouched by design (keys already resolved via diataxis lookup). | 69bd97ba8d95 |
| td-027 | suggested | other | code-to-goals | dashboard_app/src/components/registry.ts | code-quality |  | orchestrator | 2026-05-11 | 2026-07-02 | implementation-planner | resolved | implementer (dashboard-debt-trio) | The Streamlit `metrics_view` / `plan_view` / `verification_report` / `idea_grid` / `architecture_explorer` per-artifact renderers have no Next.js equivalents yet. v1 ships `reference` + `explanation` + `default` Diátaxis shells only. Resolution: build dedicated renderer components and register them in `RENDERER_REGISTRY`. Class `other`: no enum slot covers deferred-feature gaps. // 2026-07-02: RESOLVED per dec-265: five per-artifact renderers in src/components/renderers/ wired via the manifest renderer: field as highest-priority resolveRenderer key; metrics_view and architecture_explorer are thin previews linking to the existing /metrics and /architecture routes (rows' reimplementation premise was stale); malformed-input -> DefaultShell fallback tests bite. | 4ce09473665c |
| td-029 | suggested | other | code-to-goals | dashboard_app/src/server/view-models/documentation.ts | code-quality |  | orchestrator | 2026-05-11 | 2026-07-01 | systems-architect | resolved | implementer (29add49) | `assertContainedProjectPath` is intentionally wide for documentation surfaces (any file in project root). A compromised `doc_manifest.yaml` could expose `src/` or `.env` files. Resolution: add a secondary allowlist for doc surfaces restricting paths to known-safe subdirectories (e.g., `docs/`, `.ai-state/`). Class `other`: no enum slot covers security-hardening deferrals. // 2026-07-01: RESOLVED: assertAllowedDocSurface secondary gate after containment (docs/, .ai-state/, .ai-work/, root *.md, manifest API-spec dirs mirrored from build_doc_manifest); reject canaries for src/, .env, path-escape; 413-test package suite green. | 70e11df29f5c |
| td-034 | important | other | code-to-goals | hooks/worktree_guard.py, hooks/inject_worktree_banner.py, agents/implementer.md, agents/doc-engineer.md, agents/test-engineer.md | code-quality |  | orchestrator | 2026-05-13 | 2026-07-01 | implementation-planner | resolved | implementer (c55d044) | Standard/Full-tier pipelines that run in a worktree (per `swe-agent-coordination-protocol.md` § Pipeline Isolation) can have spawned subagents (implementer, test-engineer, doc-engineer, etc.) emit `Write`/`Edit` calls that land in the canonical checkout instead of the briefed worktree. Reproduced 2026-05-13 in the `rules-reorg-blacklist` pipeline: Phase A implementer received an explicit worktree-cwd brief with a `pwd && git rev-parse --abbrev-ref HEAD` pre-flight, reported success, but Phase A's 21 rule-file edits landed in `/Users/fperez/dev/praxion/rules/` instead of `/Users/fperez/dev/praxion/.claude/worktrees/rules-reorg-blacklist/rules/`. The orchestrator session DID stay correctly in the worktree and could observe the divergence via `git -C <main> status` — but only after the user signaled the mismatch. The PreToolUse `worktree_guard.py` hook is documented (CLAUDE.md § Pipeline Isolation) as blocking `Write`/`Edit` resolving outside the session worktree, yet did not intercept the subagent's calls. Three competing hypotheses for the root cause (none ruled in/out by this observation): (a) subagent sessions do not inherit the orchestrator's PreToolUse hooks (hook-scope gap), (b) the subagent's path-resolution layer reports the worktree path while the actual filesystem write resolves to the canonical checkout (path-resolution bifurcation between `pwd` and Edit-tool target), (c) the hook's path-containment check has a gap (e.g., relative-path normalization). Recovery is mechanical (sync main → worktree, then `git -C <main> checkout -- rules/`) but expensive in pipeline-cycle terms (one wasted phase of work + manual reconciliation + lost cache warmth on the orchestrator side). Recurring failure mode: saved auto-memory `feedback_implementer_cwd_in_worktrees` already records this with a verify-pwd-and-git-status mitigation, but the mitigation is REACTIVE (catches after damage) not PREVENTIVE. Resolution path: (1) write a controlled-test hook script that spawns a minimal subagent in a worktree session, has it attempt an Edit outside the worktree, and asserts `worktree_guard.py` fired; if the hook doesn't fire, that confirms hypothesis (a) or (c). (2) If subagents bypass the orchestrator's hooks: either register `worktree_guard.py` in subagent-inheritable settings OR add a SessionStart hook for subagents that asserts cwd matches the briefed worktree-root, abort on mismatch. (3) Add a per-tool absolute-path-required preflight to subagent system prompts when launched into a worktree pipeline. (4) Until structural fix lands: orchestrators should brief subagents with absolute paths only (worktree-rooted), not relative paths — relative paths silently resolve against whatever cwd the subagent's tool layer chooses. Class `other` because no enum slot covers cross-session-hook-scope or path-resolution-bifurcation gaps (same precedent as td-021, td-033). Owner-role `implementation-planner` because resolution requires a hook-scope architecture decision + a new SessionStart hook + a settings-inheritance change. // 2026-05-23: ASSESSED — substantial defensive work already shipped: ADR `dec-180` (accepted) mandates a controlled-test harness as a ship gate; `hooks/test_worktree_guard_subagent.py` exists but its automated Path A is SKIPPED (the `Agent` tool is not callable from a pytest context), falling back to `hooks/MANUAL_VERIFICATION.md`. `hooks/worktree_guard.py` enforces only on ABSOLUTE paths *by design* — the relative-path skip is intentional and tested (`test_ignores_relative_path`), so hardening it contradicts dec-180's tested contract and would need its own ADR (not a free win). Defense-in-depth absolute-path briefing in `/resume-rework` is in place and tested. The CORE open question — whether subagents inherit the orchestrator's PreToolUse hooks (hypothesis a) vs a path-resolution/relative-path gap (b/c) — remains empirically unverified: confirming it needs a live subagent spawn in a worktree, which is BLOCKED this session by the td-021 spawn crash (a `general-purpose` probe crashed with the `K.length` signature). Same subagent-runtime family as td-021/td-033/td-035. No productive, non-speculative code change is available without live verification; next step is to run the harness's Path A in a fresh session where spawning works (or an upstream determination of subagent hook inheritance). Held OPEN. // 2026-05-23: BLOCKER DOWNGRADED — the td-021 spawn crash is builtin-agent-specific, not a hard wall: a spawned `i-am:researcher` ran cleanly this session (where `general-purpose` crashed 2/2), so Path A (spawn a subagent in a worktree, have it Edit outside the worktree, assert `worktree_guard.py` fires) is runnable via an i-am-type agent — no td-021 fix needed. Still OPEN: Path A has not actually been executed (needs a worktree session + an i-am-subagent Edit-outside-worktree probe); the hook-inheritance-vs-path-resolution question (hypothesis a/b/c) is unresolved until that test runs. Confidence that it's tractable is now high. // 2026-06-16: PATH A EXECUTED LIVE — SHIP GATE PASSES. Spawned an `i-am:implementer` from a linked worktree session and had it `Write` a NEW file at an absolute path in the MAIN git tree; `hooks/worktree_guard.py` PreToolUse:Write hook FIRED on the SUBAGENT's call — `[worktree-guard] BLOCKED: cross-worktree write ... resolves in different git tree: /Users/fperez/dev/praxion`, exit 2, target never created. This REJECTS hypothesis (a): subagents DO inherit the orchestrator's PreToolUse hooks. Defense-in-depth also observed — the permission layer denied subagent Read/Edit of outside-worktree paths BEFORE the guard ran (an Edit probe to /tmp and a Read of a main-tree file were denied generically; /tmp is non-git so the guard intentionally skips it per its tested contract). Guard logic independently unit-verified (direct payload → exit 2 + correct message). Residual exposure NARROWED to the original incident's actual mechanism: RELATIVE paths (guard skips by design, dec-180) + subagent cwd-bifurcation (cwd resolves to canonical checkout) — mitigated by absolute-path-only briefing, already standard practice. Hook-inheritance question RESOLVED; only the relative-path+cwd-bifurcation preventive-vs-documented follow-up remains. // 2026-07-01: RESOLVED: residual relative-path/cwd-bifurcation exposure now has a deterministic preventive layer — new inject_worktree_paths.py PreToolUse(Agent) hook (Option A; worktree_guard's dec-180 contract untouched) prepends the absolute session-worktree root + absolute-path mandate to every subagent prompt when the spawning session is in a linked worktree; canary + suppression + fail-open tests (16). One follow-up assumption extracted to td-049 (multi-hook updatedInput chaining). | d1528ee8da7e |
| td-036 | suggested | other | code-to-goals | install.sh | code-quality |  | verifier | 2026-05-15 | 2026-07-01 | implementation-planner | resolved | implementer (dcf14d5) | `bash install.sh` performs a marketplace fetch via `claude plugin install` rather than copying local files into the installed plugin cache (`~/.claude/plugins/cache/bit-agora/i-am/<version>/`). Praxion contributors editing hooks, scripts, or commands locally cannot test their changes against `claude --bg`-launched sessions or other plugin-cache-resolved code paths without manually `cp`-ing files into the cache between iterations. Step 10 and Step 11 of the dispatch-reworks Phase 2 pipeline both used manual `cp .../hooks/notify_bg_session_state.py ~/.claude/plugins/cache/bit-agora/i-am/0.2.0/hooks/` workarounds during live integration testing, which is fragile (forgettable, easy to mis-target by version) and bypasses the install-time validation chain. Resolution path: add a `bash install.sh --dev-link` (or `--editable` / `--symlink`) mode that creates symlinks from the installed plugin cache back to the worktree's source files, so local edits take effect immediately. Alternative: a `praxion-claude-dev` extension that pre-pends the worktree's hooks/commands/scripts dirs to whichever lookup paths Claude Code consults (similar to the existing `--plugin-dir` flag but for the hook subsystem). Class `other`: no enum slot covers tooling-developer-experience gaps. // 2026-07-01: RESOLVED: install.sh --dev-link / --dev-link=off symlinks the pinned cache's hooks/, scripts/, commands/ to the working tree (17 hermetic tests incl. refusal canaries); README_DEV manual-cp workaround replaced; motivated same-day by the td-021 live-fix requiring a manual cp. | caefc92cc30b |
| td-021 | important | other | code-to-goals | CLAUDE.md, rules/swe/swe-agent-coordination-protocol.md | code-quality |  | orchestrator | 2026-05-08 | 2026-07-01 | doc-engineer | resolved | orchestrator (1c9183c) | Claude Code shipped `Explore` subagent (Claude Code 2.1.136) crashes during initialization with `undefined is not an object (evaluating 'K.length')` when spawned from a Praxion session. Reproducible: a 50-word minimal prompt (`Find /tmp/x, count lines, return first three`) fails identically — NOT prompt-size dependent. Failure mode: orphaned-tool-start in the chronograph observability panel — input context is packaged and billed (~55K tokens/call observed by operator on 2026-05-08), output never returns. Distinct from upstream issue #38868 (CLOSED, 2.1.83) which reported a graceful "Prompt is too long" / AxiosError surface for the same trigger condition (many plugins/skills/MCPs injecting context); our failure is a silent JS crash with no recoverable error message. Praxion `i-am:researcher` (3rd attempt in the same session) handled the same workload cleanly. Token cost (operator-reported, not directly visible in `.ai-state/observations.jsonl` schema). Resolution path: (a) document workaround in CLAUDE.md `Known Claude Code Limitations` (this commit) — prefer `i-am:researcher` for substantive inventory; (b) add note in `rules/swe/swe-agent-coordination-protocol.md` to scope when Explore is reasonable vs when to skip; (c) upstream bug filed at [anthropics/claude-code#57493](https://github.com/anthropics/claude-code/issues/57493) on 2026-05-08, referencing #38868, #43983, #30620. Distinct from td-018 (token-budget) — this is a hard JS crash, not a budget overflow, even though both surface in many-skills environments. // 2026-06-16: ROOT CAUSE FOUND (Praxion-side contributor) + SOURCE FIX. A live `Explore` probe from a Praxion session failed with `PreToolUse hook for Agent returned updatedInput that failed schema validation: required parameter description is missing` — NOT the K.length JS crash, a different (more concrete) signature. Traced to `hooks/inject_subagent_context.py` `_emit_updated_input`, which rebuilt `tool_input` from scratch with only `{subagent_type, prompt}`, DROPPING the Agent tool's required `description` field (and `model`/`run_in_background`). Fires only on host-native agents (Explore/Plan/general-purpose); `i-am:*` agents are skipped by gate (d), which is why every `i-am:*` spawn worked all along. Fix: preserve the full `tool_input`, override only `prompt`; canary test `test_output_preserves_description_and_other_tool_input_fields` added (36/36 pass, ruff clean) — fails on the old reconstruct-from-scratch code, passes on the fix. NOT yet live-verified: the running session executes the INSTALLED hook copy under `~/.claude/plugins/cache/bit-agora/i-am/<ver>/hooks/`, not the repo source; live verification needs `bash install.sh` + session restart, then an `Explore` re-probe. Held OPEN pending that post-reinstall check; whether this fully subsumes the upstream K.length crash or is a separate Praxion-side bug is unverifiable until the reinstalled hook is exercised. // 2026-07-01: RESOLVED: true root cause one level deeper than the 2026-06-16 fix — the emit wrapped updatedInput in a {"tool_input": ...} envelope while the harness validates updatedInput directly as the tool params object, so ALL required Agent params read as missing (live Explore repro 2026-07-01: description+prompt both flagged). Fixed by unwrapping (1c9183c) + harness-contract comment + tests inverted to the direct shape; installed 0.12.0 cache synced (documented cp workaround); live Explore re-probe returned cleanly with the injected preamble evident in its answer. Codex-bridge test aligned in 6d67024. | 2df31ac7a5ba |
| td-053 | suggested | other | code-to-goals | pyproject.toml ([tool.coverage.run]), tests/ | code-quality |  | orchestrator | 2026-06-05 | 2026-07-01 | test-engineer | resolved | test-engineer (b605b51) | Originally filed as td-038; renumbered to td-053 to resolve id collision, 2026-07-15. Pre-existing: `python3 -m pytest tests/ -q` passes all tests (185/185 incl. Wave 0 additions) but the post-run pytest-cov COMBINE step crashes with `INTERNALERROR ... coverage.exceptions.DataError: Can't combine branch coverage data with statement data`. Root cause: `pyproject.toml [tool.coverage.run] branch = true` while at least one test spawns a subprocess that writes statement-only coverage data; the parallel-data combine cannot merge branch with statement. Confirmed PRE-EXISTING, not a Wave 0 (sia-praxion-fit) regression: reproduced via `pytest tests/ -q --ignore=tests/test_run_store_backend.py` (181 passed, same INTERNALERROR); the new Wave 0 test file alone runs clean. Surfaced at the Wave 0 Step 5 integration checkpoint. Impact: tests pass but the coverage report / `coverage.xml` is not produced, and a CI coverage gate would fail; `.coverage.<host>.<pid>.<rand>` parallel files also accumulated untracked (mitigated same-commit by adding `.coverage.*` to `.gitignore`). Resolution path: identify the subprocess-spawning test and make its coverage config inherit `branch=true` (consistent `.coveragerc`/`COVERAGE_PROCESS_START`, or `[tool.coverage.run] parallel=true` with one branch setting), OR run that test with coverage disabled. owner-role test-engineer (test-infra config). Class `other`: no enum slot covers coverage-tooling config defects (precedent td-021/td-034). Orchestrator-filed mid-pipeline — canonical sources are verifier/sentinel; a slight protocol stretch, verifier may re-source. // 2026-07-01: RECURRED in direct-capture-contract pipeline — the pytest-cov branch/statement COMBINE DataError reproduced on unmodified subprocess-spawning test files (hooks/test_worktree_guard.py alone); `--no-cov` runs clean. Still open; unchanged root cause. // 2026-07-01: RESOLVED: root cause — pytest-cov's subprocess embedding resolves its default `.coveragerc` config path relative to EACH CHILD's cwd; tests spawning children with tmp_path cwd silently fell back to statement-only data, crashing the branch-mode combine. Fix: pin `--cov-config=pyproject.toml` in addopts so children inherit the absolute config. Verified twice, coverage.xml produced, no stray parallel files. Un-crashing the suite also unmasked a stale codex-bridge assertion (fixed separately, 6d67024). | 45bdea2de547 |
| td-044 | suggested | other | code-to-goals | commands/onboard-project.md | code-quality |  | orchestrator | 2026-06-27 | 2026-07-01 | systems-architect | resolved | systems-architect (dec-263) | [E1/R15 from the process-flow analysis] The `future-designed` artifacts `project_profile.yaml` and `eval_ledger/EVAL_LOG.md` have no producer: the archetype-detection + agentic-eval scaffold that would create them is designed (schema in `skills/agent-evals/references/run-ledger-schema.md`; lifecycle/absent-fallback in `skills/software-planning/references/artifact-inventory.md`) but not wired into `/onboard-project`, so both are absent on every project. Downstream agents already treat them as optional (absent = OK), so this is a latent dead seam, not active breakage — tracked here so it stays visible rather than silently forgotten. Class `other` (no enum slot for "designed-but-unwired producer"). Close by either wiring the producers into onboarding or formally retiring the designed-but-unbuilt scaffold via ADR. // 2026-07-01: RESOLVED by dec-263 (retire, asymmetric): project_profile.yaml's never-built Phase 8f producer clause partially supersedes dec-219 (schema stands, reactivates if the agentic-eval archetype feature ships); EVAL_LOG.md untouched — lazily run-created by design with absence-tolerant readers. | 50f945d358ea |
| td-045 | suggested | coverage-gap | code-to-goals | fitness/tests/test_gate_canary_coverage.py | code-quality |  | verifier | 2026-07-01 | 2026-07-01 | test-engineer | resolved | test-engineer (d43d11b) | [direct-capture-contract verification] `test_gate_canary_coverage.py::_hook_gates` globs only `hooks/*_gate.py` / `*_guard.py` / `*_gate.sh`, so the `remind_*.py` advisory-hook family (`remind_adr.py`, `remind_calibration.py`) is invisible to the canary-coverage meta-test. `remind_calibration.py` ships a live-biting canary (`test_flags_lagging_calibration_log`, verifier-confirmed) but a future accidental deletion of it would NOT be caught by `test_every_gate_has_canary`. Flagged as architect Structural Issue 2 (SYSTEMS_PLAN.md § Codebase Readiness) and confirmed by test-engineer. Resolution: widen the glob to include `remind_*.py`; doing so also requires backfilling `remind_adr.py`'s currently-absent canary (its inclusion is the reason the glob was left narrow — out of this task's charter). Class coverage-gap: a live gate whose liveness is not mechanically enforced. // 2026-07-01: RESOLVED: canary-coverage glob widened to hooks/remind_*.py; hooks/test_remind_adr.py backfilled (canary + contrast + fail-open). Meta-test green across both remind hooks. | 2aec2676eccd |
| td-046 | suggested | other | code-to-goals | rules/swe/swe-agent-coordination-protocol.md, skills/spec-driven-development/references/calibration-procedure.md, claude/canonical-blocks/praxion-process.md, rules/swe/agent-behavioral-contract.md | code-quality |  | verifier | 2026-07-01 | 2026-07-01 | systems-architect | resolved | systems-architect (dec-264) | [direct-capture-contract verification] D8's optional mechanical tier-threshold-drift check was deliberately deferred (architect Decision 3). No mechanical check asserts tier-threshold agreement (file-count bands, score bands) across the four defining surfaces (coordination-protocol tier table, calibration-procedure matrix, praxion-process canonical block, behavioral-contract). Deferred because a substantive cross-surface check over heterogeneous prose ('4+ files' vs 'Multiple files' vs score bands) risks gate-liveness anti-patterns (brittle grep, existence-not-substance), and D8's consolidation itself already shrinks the restatement surface. Resolution: either build a narrow sentinel EC-series check with careful canonical-threshold extraction, or formally accept periodic-detection-only via ADR. owner-role systems-architect (needs a canonical-threshold model) or sentinel (EC-series check). Class other: no enum slot for a deliberately-deferred consistency gate. // 2026-07-01: RESOLVED by dec-264 (accept periodic-detection-only): post-dec-261 consolidation, only the coordination protocol fully states the bands; a mechanized cross-surface agreement check fails every-check-earns-its-place. Reversal trigger: one real band-boundary drift reaching main, or >=2 review-caught near-misses, then build a narrow EC(L) check over file-count boundaries only. | 6a14d6010df2 |
| td-047 | suggested | coverage-gap | code-to-goals | scripts/check_readiness_feedback.py, scripts/test_check_readiness_feedback.py | code-quality |  | verifier | 2026-07-01 | 2026-07-01 | test-engineer | resolved | test-engineer (85e8ea2) | [direct-capture-contract verification, pre-existing baseline failure] `fitness/tests/test_gate_canary_coverage.py::test_every_gate_has_canary` fails naming `check_readiness_feedback.py`: its test file `test_check_readiness_feedback.py` has no canary-named test function (no `reject`/`flag`/`fail`/`block`/... substring per gate-canaries.md), so this gate's liveness is unproven. Confirmed pre-existing (TEST_BASELINE.md) and unrelated to this pipeline's scope; this pipeline fixed the sibling `check_calibration_coverage.py` canary-name sub-finding, leaving this the sole remaining sub-finding of that meta-test. Resolution: add a canary-named test feeding a known-bad readiness-feedback input and asserting the flag. Class coverage-gap. // 2026-07-01: RESOLVED: test_flags_below_floor_readiness added, driving compute_readiness_verdict with an adjusted_level=2 fixture; proven by mutation (gutting the floor check goes red). test_every_gate_has_canary fully green for the first time. | c6b4678a37b6 |
| td-003 | important | complexity | code-to-goals | memory-mcp/src/memory_mcp/store.py:1-947 | code-quality |  | sentinel | 2026-04-28 | 2026-05-17 | implementation-planner | resolved | dec-225 | `store.py` is 947 lines, exceeding the 800-line ceiling in `rules/swe/coding-style.md`. Suggested module split (per prior planning): extract auto-link logic, helper functions, and incoming-link scanning into separate modules. Module restructuring → owner-role = implementation-planner per heuristic. Migrated from retired ROADMAP §5.6 on 2026-04-28; depends on `dec-041` type-checking foundation already shipped. | a04e05874650 |
| td-013 | suggested | complexity | code-to-goals | memory-mcp/src/memory_mcp/metrics.py | code-quality |  | sentinel | 2026-05-01 | 2026-05-17 | implementer | resolved | dec-225 | Rank-4 hotspot (score=15,540): complexity=28 (second-highest in codebase after otel_relay.py); metrics module in active development; warrants restructuring consideration if complexity grows further. // 2026-05-13: de-escalated — `metrics.py` is no longer in the top-10 hotspots (churn dropped out of the 90d window per METRICS_REPORT_2026-05-13); now just a high-complexity-density file: 503 lines (well under the 800 ceiling), p95 CCN ~27 (still 2nd-highest in the codebase after otel_relay.py). Holding at `suggested`; close if the team accepts the current density, or re-escalate if complexity grows. // 2026-05-17: re-verified by sentinel audit — 503 lines (unchanged, under the 800 ceiling); dropped out of the top-10 hotspot list per METRICS_REPORT_2026-05-15 (churn fell out of the 90d window). wontfix-eligible — no ceiling breach, no active hotspot pressure — held open pending an explicit team decision to accept the standing CCN density (~27 p95, 2nd-highest in the codebase). | 8fe359ca6f92 |
| td-043 | suggested | other | code-to-goals | eval/src/praxion_evals/judges/anthropic.py, eval/src/praxion_evals/judges/openai.py, eval/pyproject.toml | code-quality |  | verifier | 2026-05-26 | 2026-05-26 | implementation-planner | resolved | dec-204 (Step 11 — F-05 resolved in same pipeline) | [F-05 from praxion-self-eval-v1 verification] Legacy `judges/anthropic.py` + `judges/openai.py` stubs retained alongside the new `harness/judge_client.py` adapter; `pandas` + `openai` deps in `eval/pyproject.toml` survive solely for these stubs. Originally filed as td-038 (ID collision with existing td-038 in TECH_DEBT_RESOLVED.md — renumbered to td-043 at resolution). // 2026-05-26: RESOLVED in Step 11 of praxion-self-eval-v1. Legacy `judges/anthropic.py` + `judges/openai.py` stubs removed; `judges/` directory deleted; `pandas` + `openai` deps pruned from `eval/pyproject.toml` + `uv.lock`. `cli.py` `judge` subcommand removed; `tiers.py` `judge-anthropic` stub entry removed; `test_judges.py` updated to drop tests covering deleted modules. The `harness/judge_client.py` adapter is now the sole judge surface. 104/104 tests GREEN post-removal. | ecc41a49e379 |
| td-041 | important | other | code-to-goals | scripts/project_metrics/collectors/git_collector.py | code-quality |  | verifier | 2026-05-24 | 2026-05-24 | implementer | resolved | orchestrator | Latent determinism defect surfaced as 7 test failures in `scripts/project_metrics/tests/test_git_collector.py` (multi-author churn/ownership/age/truck-factor + determinism). Root cause: `_run_git_log` bounds the window with `git log --since='{window_days} days ago'`, which git resolves against the REAL wall clock — it ignores the `PROJECT_METRICS_REFERENCE_TIME` env var the tests pin to 2026-04-23 (that injection point flows only into `_compute_age_days`, never into the `--since` boundary). The `minimal_repo` fixture's 10 commits span 2026-02-15→2026-03-30; once wall-clock-now passed first-commit+90d (≈2026-05-16) the window silently dropped the first two commits (init README 2026-02-15, add core 2026-02-20). Confirmed deltas: `core.py` churn 22 vs 32 (lost commit-2's +10), `churn_total` 49 vs 62, `core.py` top-author 0.667 vs 0.80 (Alice 10/15 not 20/25), `README.md` age+ownership absent (its only commit fell out of window). Fixture git history independently verified CORRECT against FIXTURE_SPEC.md — this is a collector defect, NOT fixture drift. Production reports against the live repo are unaffected (a rolling wall-clock 90d window is correct for a live repo); only the deterministic fixture tests break. Time-bomb class: re-fails on every machine until fixed, regardless of when run. Resolution: thread the reference clock into the window boundary — pass an absolute `--since=<reference_time - window_days>` ISO date computed from `_resolve_reference_time()` instead of the relative `'N days ago'` string, so the env-pinned clock governs BOTH age and the window. Class `other`: no enum slot covers test-determinism / wall-clock-window defects. // 2026-05-24: RESOLVED (orchestrator) — threaded the reference clock into the git-log window boundary: `_run_git_log` now takes `reference_time` and computes an absolute `--since=(reference_time - window_days).isoformat()` cutoff instead of the relative `'N days ago'` string, so `PROJECT_METRICS_REFERENCE_TIME` governs BOTH age and the window. All 34 `test_git_collector.py` tests green (the 7 that failed + 27 prior); full `scripts/project_metrics/` suite (421) green. Production reports unaffected — a live wall-clock reference yields the same rolling 90d window as the old relative string. | da1f110c472a |
| td-042 | suggested | drift | code-to-goals | AGENTS.md.tmpl | code-quality |  | verifier | 2026-05-24 | 2026-05-24 | doc-engineer | resolved | orchestrator | Generated artifact `AGENTS.md.tmpl` is stale relative to its source `CLAUDE.md`; surfaced as 1 failing test `scripts/test_claude_to_agents.py::test_praxion_root_agents_template_matches_generator`. The test regenerates the template from current `CLAUDE.md` via `skills/adapt-claude-to-agents/scripts/claude_to_agents.py::render_project_template` and asserts equality with the committed template. `CLAUDE.md` opening line was reworded in commit ac9d394 (2026-05-23, "docs: trim always-loaded CLAUDE.md + rules prose") to "Operational infrastructure for the development philosophy in `~/.claude/CLAUDE.md` — …", but `AGENTS.md.tmpl` was last regenerated 2026-05-15 (commit 1a4e06c) and still carries the old "The operational infrastructure for this project's shared development philosophy…" wording. Mechanical fix: re-run the generator and commit the regenerated `AGENTS.md.tmpl`. Class `drift`: generated artifact diverged from source-of-truth. // 2026-05-24: RESOLVED (orchestrator) — re-ran `claude_to_agents.py` against current `CLAUDE.md`; `AGENTS.md.tmpl` regenerated (9 ins / 7 del — the reworded intro). All 3 `scripts/test_claude_to_agents.py` tests green, including `test_praxion_root_agents_template_matches_generator`. | 0f2f99125f8f |
| td-035 | important | other | code-to-goals | CLAUDE.md | code-quality |  | orchestrator | 2026-05-14 | 2026-05-23 | implementer | resolved | 1a247c5 + i-am-subagent probe 2026-05-23 | Subagent `Write` to `.ai-work/<task-slug>/` can be denied by Claude Code's sandbox while the main agent's `Write` to the same path succeeds. Observed 2026-05-14: `i-am:systems-architect` dispatched by `/resume-rework` reported "The Write tool is denied. This is a sandbox policy decision, not a path issue" when attempting to write `SYSTEMS_PLAN.md` into `.ai-work/fix-manifest-fixture-target-agent/`. The architect's `tools:` frontmatter explicitly includes `Write` (`agents/systems-architect.md:tools: Read, Glob, Grep, Bash, Write, Edit`); the target resolved inside the session worktree so `hooks/worktree_guard.py` allowed it (it exits early when the target is `_is_within(session_root)`); no PostToolUse hook would surface as a "denied" message. Most likely root cause: Claude Code's per-tool permission prompt — the main agent had accumulated a session-scoped auto-allow for `.ai-work/` writes that did not propagate to the subagent, and subagents have no UI to answer the prompt so the call is silently denied. Distinct from `td-034` (subagent writes resolve to the wrong tree); same root-cause family in that subagent settings/permission inheritance differs from the orchestrator session. Workaround (interim): when a subagent reports a Write denial for `.ai-work/`, the main agent reads the subagent's returned content and places the file from the orchestrator session — the pipeline contract is honored, just routed through the orchestrator. Permanent resolution requires either (a) adding a project-scope `Write` permission rule for `.ai-work/**` (Praxion has no `Write(<glob>)` precedent in `.claude/settings*.json` and the exact syntax is unverified — needs upstream-docs check), (b) Claude Code subagents inheriting the orchestrator's session-scoped allows, or (c) subagents prompting an inbox the orchestrator can answer. Class `other`: no enum slot covers subagent-permission-inheritance gaps. Owner-role `unassigned`: gated on the upstream stance like `td-033`. CLAUDE.md `Known Claude Code Limitations` bullet added 2026-05-14. // 2026-05-17: re-verified by sentinel audit — finding still grounded. Sentinel recommended wontfix alongside td-033; orchestrator holds OPEN. Unlike td-033 (upstream #23478/#38487 explicitly CLOSED `NOT_PLANNED` — zero action possible), td-035 has no filed/declined upstream issue, and resolution path (a) is a concrete LOCAL fix: a project-scope `Write` permission for `.ai-work/**` in `.claude/settings.json`. Actionable next step — verify the `Write(<glob>)` settings syntax against current Claude Code docs, then add the rule; not upstream-gated. Re-tier owner-role unassigned→implementer once the syntax is confirmed. // 2026-05-23: `Write(<glob>)` syntax confirmed — `.claude/settings.local.json` already carries a working `Read(//…/**)` path-glob allow rule, proving the `Tool(<path-glob>)` form is valid for file tools, so a `Write(.ai-work/**)` allow rule follows the same shape. Per the re-tier rule above, owner-role unassigned→implementer. Actionable local fix: add a project-scope `Write` allow rule for `.ai-work/**` to `.claude/settings.json`, then confirm it propagates to spawned subagents (the remaining open sub-question — subagent permission inheritance). Re-tiered to clear the TD05(c) >7d-unassigned discipline finding. // 2026-05-23: PARTIAL — local fix (a) applied: added `"allow": ["Write(.ai-work/**)"]` to the `permissions` block of `.claude/settings.json` (mirrors the proven `Read(//…/**)` path-glob already in `settings.local.json`; JSON re-validated). Remaining acceptance criterion — *subagent permission propagation* — NOT verified: a probe subagent spawned to Write into `.ai-work/td-035-probe/` crashed on init with the td-021 `undefined is not an object (evaluating 'K.length')` failure, so propagation cannot be confirmed from this many-skill session. Held OPEN pending fresh-session confirmation (project-scope settings may only fully load at session start regardless of a mid-session edit). // 2026-05-23: RESOLVED — verified. A spawned i-am subagent (`i-am:researcher`) wrote into `.ai-work/td-035-probe/` SUCCESSFULLY under the committed `Write(.ai-work/**)` rule (settings.json, commit 1a247c5); the original symptom (subagent `.ai-work/` Write denied) does NOT reproduce — so the rule DOES propagate to spawned subagents, answering the open sub-question. Verification-path note: two `general-purpose` builtin probes crashed at init with the td-021 `K.length` bug, but the `i-am:researcher` spawn ran cleanly — confirming td-021 is builtin-agent-specific and i-am plugin agents bypass it (consistent with the Shipped-Explore fallback rule). The mid-session-settings-load worry was unfounded: the rule took effect without a session restart. | 35b531a6cc86 |
| td-040 | suggested | complexity | code-to-goals | agents/verifier.md, agents/sentinel.md | code-quality |  | orchestrator | 2026-05-23 | 2026-05-23 | context-engineer | resolved | dec-203 + verifier Phase 3a de-dup | `agents/verifier.md` (588L) and `agents/sentinel.md` (583L) exceed the sentinel T03 500-line FAIL threshold (warn=300, fail=500); `implementation-planner.md` (477L) and `systems-architect.md` (404L) exceed the 400-line WARN. Sentinel finding S6 (2026-05-23 audit) recommends a progressive-disclosure split (core prompt + reference files). Deferred from the 2026-05-23 sentinel-cleanup pass by user decision — it is the one finding that is a genuine refactor, not a mechanical fix. Non-trivial because there is NO existing agent-local reference-file pattern in Praxion (agents cite shared skill references, not their own split-out files; no `agents/references/` dir exists). Establishing one needs a design decision (likely an ADR): where agent-local references live and how the agent reliably loads them — an agent prompt loads wholesale, so moving the verifier's `## Process` (~363L) or the sentinel's `## Check Catalog` (~258L) into a file the agent does not reliably re-read would silently disable the gate (a Gate-Liveness 'dead gate'). Resolution: load the agent-crafting skill, design the agent progressive-disclosure pattern, split both agents (largest extractable section each), then re-run sentinel (confirm T03 clears) + full pytest to prove no gate regressed. owner-role context-engineer per sentinel; may hand to systems-architect if an ADR is warranted. Class `complexity`: prompt-file size/structure, same family as td-002/003/004. // 2026-05-23: ASSESSED (orchestrator, "assess then trim-or-report" path) — read both files in full. VERDICT: oversize is largely INTRINSIC, not bloat; a line-count trim would game the gate (gate-liveness "substance over structure"). sentinel.md (581L): the `## Check Catalog` (~258L) is ~80 distinct check rows across 16 dimensions — it IS the gate, near-zero redundancy. Trimming = deleting checks (gutting coverage); extracting the catalog = dead-gate risk (the sentinel executes it inline; the genuinely-conditional SH/TT detail is ALREADY externalized). Principled fix: RECALIBRATE T03 for the ecosystem's most comprehensive gate-agent (carve-out or higher fail threshold + justification) — an ADR-level call, NOT a gut/risk-extract. verifier.md (588L): has a clean ~100L NON-gaming path — (a) ~62L of MISPLACED main-agent content (the `VERIFIER_FINDINGS.md` 7-section template ~395-440 + `### Rework Worktree Spawn Behavior` ~489-505 document what the MAIN AGENT does, not the verifier; relocate to a main-agent-facing home after de-dup check vs `commands/resume-rework.md` + coordination-details), and (b) the ~38L conditional `#### Phase 3a — ML Metric Threshold Evaluation` (~75-112) externalized to a verifier reference loaded only on ML signal (matches the existing on-demand `test-coverage`-skill pattern; low dead-gate risk — conditional + verifier already loads refs on demand). That clears verifier <500 without gaming. RECOMMENDATION: focused effort = verifier clean-path (relocate + conditional-externalize) + sentinel T03-recalibration ADR; both are decisions, not mechanical trims. Per the "don't game the gate" protocol, STOPPED at report rather than improvising a T03 change or risky catalog extraction. Held OPEN. // 2026-05-23: RESOLVED (user chose the T03-exception path for both gate agents). Both FAILs cleared by recalibrating T03 (ADR dec-203): the two gate-encoding agents (`sentinel.md`, `verifier.md`) now assessed at warn 550 / fail 700, so sentinel (580) and verifier (556) resolve to WARN, not FAIL. verifier also got a genuine lean-up — its conditional Phase 3a ML-metric procedure was de-duplicated against the schema reference it already cited (`skills/llm-training-eval/references/training-results-schema.md` § Verifier Consumption (Phase 3a)), 588→556. The verifier's rework-spawn contract was found to be deliberately tested-in-place (5 ship-gate tests in `tests/orchestration/` + dec-173..182), so it was NOT relocated — relocating would have overridden a tested design for a line count. T03 exception rationale inlined in `agents/sentinel.md` (no dec-NNN in the shipped artifact). Verified: 181 Praxion tests pass, shipped-artifact-isolation clean on both agents, DECISIONS_INDEX regenerated. The two WARN-level agents (`implementation-planner.md` 476, `systems-architect.md` 403) were never part of the FAIL and remain normal monitored WARNs — out of scope for this row. | 9d955e75c7e7 |
| td-004 | important | complexity | code-to-goals | task-chronograph-mcp/src/task_chronograph_mcp/otel_relay.py | code-quality |  | sentinel | 2026-04-28 | 2026-05-23 | implementation-planner | resolved | otel_relay inheritance-chain split | `otel_relay.py` is 1,468 lines (grew from 921 in earlier audit), nearly 2x the 800-line ceiling. Suggested module split: extract span creation methods to `span_factory.py`, agent context tracking to `context_tracker.py`. Worsening trend across recent telemetry work (`dec-052` duration-aware spans) compounds the excess. Module restructuring → owner-role = implementation-planner. Migrated from retired ROADMAP §5.6 on 2026-04-28. // 2026-05-23: RESOLVED — split into a 4-file linear-inheritance chain (one `__init__`/`self`; behavior-identical): `relay_helpers.py` (241L — constants + stateless helpers, no internal deps), `context_tracker.py` (335L — `_RelayContextTracking`: shared state + agent-context tracking + reaper), `span_factory.py` (515L — `_RelaySpanFactory(_RelayContextTracking)`: span construction + lazy `_ensure_agent_context`), `otel_relay.py` (509L — `OTelRelay(_RelaySpanFactory)`: public event API). Every file now under the 800-line ceiling. Acyclic layering helpers←context←span←public, no upward calls. Behavior preserved: 292/292 chronograph tests pass (unchanged count), pyright `src` clean, ruff clean. Test monkeypatch targets for the five dynamically-read module symbols (`_git_user_id`/`_git_head_sha`/`_read_pipeline_tier`/`FORK_CLUSTER_WINDOW_S`/`AGENT_SPAN_TIMEOUT_S`) repointed to their new `relay_helpers` home via aliased imports — assertion lines byte-unchanged; readers access them `rh.`-qualified so patches still take effect. Backward compat: `otel_relay` re-exports moved public symbols via `__all__`; `server.py` + diagnostic-test imports unchanged. | f8ceab2e7268 |
| td-005 | important | other | code-to-goals | eval/pyproject.toml, eval/src/praxion_evals/cli.py, eval/src/praxion_evals/regression/ | code-quality |  | verifier | 2026-04-28 | 2026-05-26 | implementation-planner | resolved | dec-204 + eval/EVAL_PLAN.md | Eval framework `regression` mode was shipped-but-broken: keyed baselines by `task_slug` (one-shot slugs, no comparison corpus). Compounding bug: missing `arize-phoenix` dep silently swallowed `phoenix.Client()` errors. // 2026-05-26: RESOLVED by `/eval-praxion` v1 — `dec-204` narrowly supersedes dec-040 clause 3 to allow LLM-as-judge calls over completed artifacts; fills the `judges/anthropic.py` NotImplementedError stub via the new `harness/judge_client.py` adapter; retires the broken-by-design `regression/` package (448 LOC) entirely. Families shipped: #1 (Pipeline-outcome fidelity over SPECs + ADRs + DECISIONS_INDEX.md) + #2 (Behavioral-contract adherence over VERIFICATION_REPORT.md). The broader full regression-mode redesign (tier/shape-keyed envelope baselines over a Phoenix corpus) remains deferred — requires both a shape-definition ADR and a multi-run Phoenix corpus that does not yet exist. Deferred-scope tracked in `eval/EVAL_PLAN.md` (families #3/#4/#5/#6 + LEARNINGS-distillation prerequisite + `--project` flag open design question). | 51fe8bd48f7a |
| td-039 | important | topology-drift | code-to-goals | skills/testing-strategy/references/python-testing.md, skills/testing-strategy/references/test-topology.md | adr | dec-193 | orchestrator | 2026-05-23 | 2026-05-23 | systems-architect | resolved | dec-draft-fc34719e | Python topology selector identifiers are unregistered, so test-topology M2 adoption is effectively blocked for Python consumer projects. The trunk (`test-topology.md`) lists `pytest-globs`/`pytest-markers`/`pytest-keywords` (Registry 1) and `pytest-xdist-loadfile`/`pytest-xdist-load` (Registry 2) only as "indicative future" identifiers explicitly "not registered at this milestone"; the Python leaf (`python-testing.md`) never registered them. Any Python project adopting the protocol hits the trunk's Additive Leaf Escalation Clause and must escalate to the architect on every scoped-test step. Surfaced 2026-05-23 by the pipeline-efficiency research (RESEARCH_BDD F9/R1) and authorized for fix as Tier-B item B5, but DEFERRED by the orchestrator rather than implemented as a mechanical leaf edit: the trunk's own governance routes registry additions through the systems-architect with an ADR, and registry rows are append-only (trunk: "rows are not removed") — a wrong contract can never be removed, only superseded. Resolution path: systems-architect promotes the five identifiers indicative→registered (define each selector's concrete arg contract + invocation + default parallel runner in `python-testing.md`, mirror in the trunk registries, bump staleness markers), records the M1→M2 promotion as an ADR, then the leaf references the registered identifiers. Owner-role systems-architect per the escalation clause. // 2026-05-23: RESOLVED by systems-architect (ADR dec-draft-fc34719e). All five identifiers promoted indicative→registered in the trunk's Registry 1 (`pytest-globs`/`pytest-markers`/`pytest-keywords`) and Registry 2 (`pytest-xdist-loadfile`/`pytest-xdist-load`); trunk `<!-- last-verified -->` markers bumped to 2026-05-23; leaf (`python-testing.md`) marked the rows live-not-indicative and gained explicit edge-case semantics (empty-arg invalid, list-vs-string contract, OR-join for markers) plus recommended-default rationale for `pytest-xdist-loadfile`. The leaf already documented the contracts; deficiency was the trunk registry asymmetry + missing edge-case semantics. Sentinel TT02 now passes for Python `selectors[].strategy` values instead of FAILing; the Additive Leaf Escalation Clause no longer fires for these primitives. All 181 Praxion tests pass. ADR re-affirms dec-091. Orchestrator to migrate this row to TECH_DEBT_RESOLVED.md. | 05d24fcd6e09 |
| td-038 | important | other | code-to-goals | scripts/check_id_citation_discipline.py | code-quality |  | verifier | 2026-05-15 | 2026-05-15 | implementer | resolved | 2026-05-15: added `is_bash_shebang()` shebang-detection helper + extended `iter_code_files()` (full-scan, exec-bit-gated) and `filter_files()` (explicit-pass, exec-bit-override) to include extensionless bash; 7 regression tests in `tests/test_check_id_citation_discipline.py` PASS; `scripts/dispatch-reworks` now scanned cleanly. Will migrate to TECH_DEBT_RESOLVED.md at post-merge. | The inbound id-citation discipline check (`scripts/check_id_citation_discipline.py`, invoked via `hooks/commit_gate.sh` on every `git commit`) skips extensionless bash scripts — it scans only files matching a configured extension whitelist (likely `.py`, `.ts`, `.tsx`, `.js`, `.md`, etc.). The Phase 2 dispatch-reworks pipeline shipped `scripts/dispatch-reworks` (extensionless, executable bash following Praxion's `scripts/ccwt` precedent). The verifier pass discovered two id-citation violations in that file (line 300: "Step 1 smoke-check"; line 377: "...in Step 6") that the inbound gate would have allowed through to merge. The companion test-file violation at `tests/test_dispatch_reworks_bg.py:505` WAS caught by the gate (Python extension). So: every future extensionless Praxion script that ships from a pipeline has the same coverage hole. Resolution path: extend `check_id_citation_discipline.py`'s file selection to detect bash scripts by shebang inspection (`head -n1 == #!/usr/bin/env bash` or `#!/bin/bash`) in addition to extension matching; also include the `scripts/` directory's extensionless executables in the scan corpus. Test coverage: add a regression test that creates an extensionless bash file with a forbidden id-citation pattern, runs the checker, and asserts a violation is reported. Severity `important` because every Phase-N pipeline using bash scripts inherits this gap. | 8b756308dcf1 |
| td-019 | suggested | dead-code | code-to-goals | .ai-state/metrics_reports/index.html, claude/aac-templates/metrics-viewer.html.tmpl | code-quality |  | orchestrator | 2026-05-08 | 2026-05-11 | implementer | resolved | dashboard-redesign Step 8 | Legacy static HTML metrics viewer is superseded by the Streamlit Metrics page (`streamlit_app/pages/metrics.py`). `claude/aac-templates/metrics-viewer.html.tmpl` is no longer referenced by any code under `scripts/project_metrics/` (verified via grep) and `.ai-state/metrics_reports/index.html` is a stale snapshot — `/project-metrics` no longer regenerates it. Both files are committed but unmaintained. Resolution options: (a) delete both and update CLAUDE.md / aac-templates README to remove references; (b) restore HTML generation in `scripts/project_metrics/report.py` if a static-HTML offline view is still wanted alongside the dashboard. Filed by orchestrator under explicit user direction during the 2026-05-08 install + dashboard polish session. Resolved: template replaced with ~43-line static redirect stub pointing to praxion-dashboard; Praxion's own .ai-state/metrics_reports/index.html + SENTRUX_HISTORY.md + SENTRUX_REPORT_2026-05-05_03-43-48.{json,md} deleted. | 14f3c9778784 |
| td-022 | important | coverage-gap | code-to-goals | pyproject.toml testpaths, streamlit_app/tests/ | code-quality |  | sentinel | 2026-05-09 | 2026-05-09 | test-engineer | resolved |  | Coverage dropped from 83.3% to 54.1% when streamlit_app/ was added to coverage source scope (source = ["."]) after the dashboard build (commits 0629cbe–e0bacce), but testpaths = ["tests", "scripts"] was not updated to include streamlit_app/tests/. 16 test files (including 13 renderer smoke tests from e0bacce) are never discovered by pytest. 2,164 streamlit lines tracked at 0%. Configuration inconsistency: code in coverage scope, tests out of discovery scope. Resolution: add "streamlit_app" to testpaths in pyproject.toml. Filed by sentinel TD03 (coverage-gap class; test-engineer owner per heuristic). RESOLVED 2026-05-09: added "streamlit_app" to testpaths in pyproject.toml; 216 streamlit_app tests now discovered (839 passed, 3 skipped); line-rate recovered to 82.4% (combined line+branch 79.1%). | 8b3a19c7d205 |
| td-018 | suggested | token-budget | code-to-goals | rules/swe/agent-intermediate-documents.md, skills/software-planning/references/tech-debt-ledger.md | claude-md | rules/CLAUDE.md#token-budget | orchestrator | 2026-05-08 | 2026-05-09 | implementer | resolved | docs-strategy/p1-foundation P2.1 commit | LEDGER schema migration EXECUTED 2026-05-09: moved the 14-field schema + owner-role heuristic + worktree-concurrency dedupe semantics from `rules/swe/agent-intermediate-documents.md` to `skills/software-planning/references/tech-debt-ledger.md`. Recovery: ~7,950 always-loaded bytes / ~2,210 tokens conservative (more than the originally-estimated 3,300 because owner-role heuristic + lifecycle conventions also migrated). Total surface 22,530 → 20,320 tokens (4,680 token margin under 25k guardrail). Citation sites updated: `.ai-state/DESIGN.md`, `.ai-state/TECH_DEBT_LEDGER.md` header, `docs/architecture.md`, `agents/verifier.md`. Rule retains a 5-line summary + pointer to the skill reference. Migrates to TECH_DEBT_RESOLVED.md at next post-merge run via `scripts/finalize_tech_debt_ledger.py`. // resolved 2026-05-09 by P2.1 of the docs-strategy plan. | 4be4d6a5abd1 |
| td-016 | suggested | other | code-to-goals | agents/implementer.md, agents/sentinel.md, agents/verifier.md, claude/aac-templates/sentrux-rules.toml.tmpl, commands/onboard-project.md, hooks/, install.sh, skills/sentrux/SKILL.md | code-quality |  | orchestrator | 2026-05-07 | 2026-05-08 | systems-architect | wontfix | context-quick-wins-pr | Sentrux structural-quality sensor integration paused after Phase 1 (binary install + per-project rules + verifier Phase 9b advisory `sentrux check .`). // 2026-05-08: marked wontfix — sentrux integration removed wholesale per user direction (quality did not meet expectations; advisory-only signal value did not justify the maintenance + always-loaded-skill-metadata cost). Removed surfaces: `skills/sentrux/`, `scripts/sentrux_history.py`, `claude/aac-templates/sentrux-rules.toml.tmpl`, verifier Phase 9b, onboard-project Phase 8b.6 sub-step, install.sh sentrux block, scripts/CLAUDE.md entry, skills/README.md catalog row, docs/metrics/README.md mentions. Open Q1–Q5 from the original integration and the deferred work items (B)–(F) become moot at wontfix. | 38af28d65c23 |
| td-011 | suggested | other | code-to-goals | scripts/finalize_adrs.py | code-quality |  | orchestrator | 2026-04-30 | 2026-05-08 | implementer | resolved | 299c9be | Two `finalize_adrs.py` defects surfaced during AaC+DaC v1 FF-merge: (1) `--merged` mode uses `HEAD~1..HEAD` detection on FF-merges (linear history), missing drafts in non-latest commits when the worktree merge spans multiple draft-adding commits — 6/7 drafts auto-promoted, 7th required manual finalize via `--all`. (2) `parse_fragment_filename` cannot disambiguate the branch fragment from the slug when only 1 draft remains in `drafts/` (no siblings to share a common prefix). // 2026-05-08 (commit 299c9be): defect (1) resolved — `_drafts_added_in_last_merge` now uses reflog `HEAD@{1}` as the primary detection point so the diff range captures every commit landed by the most recent HEAD update (FF or merge-commit); first-parent detection retained as fallback for shallow clones / no-reflog edge cases. 3 new regression tests in `scripts/test_finalize_adrs.py::TestDraftsAddedDetection`. Scope of this row covers defect (1) only; defect (2) — single-fragment branch-parsing ambiguity — re-filed cleanly as `td-017` with `class: complexity` (avoids dedup_key collision with this row's `other` classification and is the more accurate categorization for brittle parser logic). | a9f59b941c3f |
| td-017 | suggested | complexity | code-to-goals | scripts/finalize_adrs.py | code-quality |  | orchestrator | 2026-05-08 | 2026-05-08 | implementer | resolved | f55bf40 | Single-fragment branch-parsing ambiguity in `parse_fragment_filename`: when only one draft fragment remains in `drafts/`, the sibling-prefix-discovery heuristic cannot disambiguate a hyphenated branch name from the slug (e.g., `feat-x-step-1` could be `feat / x-step-1` or `feat-x / step-1`). Originally filed as defect (2) of td-011; defect (1) was the FF-merge multi-commit detection bug, resolved separately by 299c9be (reflog `HEAD@{1}` primary detection). // 2026-05-08 (commit f55bf40): resolved via fix candidate (b) — record the authoring branch in fragment frontmatter at write time. New optional `branch:` field in the ADR frontmatter schema; when present, `parse_fragment_filename` uses it as the authoritative branch name and splits the filename around it cleanly, eliminating the heuristic ambiguity entirely. Backward compatible: pre-td-017 fragments without the field continue to parse via the existing filename heuristics. Schema docs updated in `rules/swe/adr-conventions.md` (Frontmatter table, "Recommended on drafts") and `skills/software-planning/references/adr-authoring-protocols.md` (creation protocol step 4 + identity-derivation pseudocode comment). 5 new regression tests in `TestParseFragmentBranchFromFrontmatter`. | f540526ae620 |
| td-014 | important | complexity | code-to-goals | hooks/send_event.py | code-quality |  | sentinel | 2026-05-01 | 2026-05-01 | implementer | resolved | 2c4a199 | Rank-8 hotspot (score=11,118): complexity=34 — highest in codebase; always-executed pre-commit hook where high complexity has outsized reliability impact. Deep conditional logic warrants refactor to reduce blast radius per commit. // resolved by extracting _handle_session_start, _handle_stop, _handle_subagent_start, _handle_subagent_stop, _handle_pre_tool_use, _handle_post_tool_use, _handle_post_tool_use_failure, _enrich_mcp_meta, _extract_progress_content into a dispatch table; _build_events CC now 3, max per-function CC now 7 | 028c4857d3e1 |
| td-015 | important | coverage-gap | code-to-goals | scripts/reconcile_ai_state.py | code-quality |  | sentinel | 2026-05-01 | 2026-05-01 | test-engineer | resolved | 9a073da | Coverage 60.1%, below 70% project floor; handles .ai-state/ reconciliation — logic errors in untested paths could corrupt the permanent project-intelligence store. // coverage now 98.2%; tests added for: ours-wins-on-older-timestamp, empty-category-excluded, blank-line-between-valid-jsonl-lines, has_drafts git-error-safe-fallback, has_drafts CLAUDE.md-exclusion, has_drafts positive-detection, has_drafts non-draft-md, reconcile_adr_numbers no-decisions-dir, reconcile_adr_numbers defers-to-finalize-when-drafts-present, reconcile_file missing-file, reconcile_file clean-json, reconcile_file invalid-json-warns, reconcile_file conflicted-json-reconciled-and-written, reconcile_file conflicted-text-with-write_fn, reconcile_file conflicted-missing-git-stages-warns, write_text_file creates-and-overwrites, _check_merge_drivers warns-and-silent, _reconcile_adr_and_index absent-dir / duplicates / no-regen-script / regen-success / regen-failure, main nothing-to-reconcile / post-merge / clean-memory / clean-observations / reconciliation-complete-memory / reconciliation-complete-observations | 764de6379a8d |
| td-006 | suggested | duplication | code-to-goals | commands/co.md, commands/cop.md | code-quality |  | verifier | 2026-04-28 | 2026-05-01 | implementer | resolved | dec-114 | `/cop` (commit-and-push) duplicates `/co` (commit) instead of layering on top. Refactor: extract shared commit logic; `/cop` adds the push step on completion. Low-priority quality-of-life cleanup; behavior is correct, just structurally redundant. Migrated from retired ROADMAP Deprecation table on 2026-04-28. // 2026-05-01: resolved as part of dec-114 commit 3. Extracted Process steps 1-6 to claude/canonical-blocks/commit-process.md; both /co and /cop embed via HTML-comment-fenced canonical-source marker; sync_canonical_blocks.py extended with per-block fence config (BLOCKS dict + BlockSpec) + per-block consumer mapping + REPO_ROOT-vs-tmp_path filter for test compatibility; redundant "Do not push after each commit" sub-bullet dropped from /cop (Push once step 7 makes semantics clear). | de5d0128654b |
| td-007 | suggested | token-budget | code-to-goals | rules/swe/swe-agent-coordination-protocol.md | claude-md | rules/CLAUDE.md#token-budget | orchestrator | 2026-04-30 | 2026-05-01 | implementer | resolved | dec-114 | ~1,125 tokens potential always-loaded headroom: L2 extraction of `### Cross-Agent Skill Conventions` (~700) to `skills/software-planning/references/cross-agent-skill-conventions.md` + L5 tightening of Process Calibration Lightweight bullets (~225) and Coordination Pipeline rules table (~200). Identified by P0a recon during AaC+DaC v1, deferred per user direction. // 2026-05-01: resolved as part of dec-114 token-budget pass; achieved ~1,078 tokens (file 18552→14670 bytes) via L2 extraction + Lightweight bullet compression + Pipeline-rules table; reclassified `other`→`token-budget`. | 66ec3c5e5526 |
| td-008 | suggested | token-budget | code-to-goals | rules/swe/agent-intermediate-documents.md | claude-md | rules/CLAUDE.md#token-budget | orchestrator | 2026-04-30 | 2026-05-01 | implementation-planner | resolved | dec-114 | ~211 tokens immediate (L4 mermaid removal + L5 ARCHITECTURE.md description tightening) plus ~1,700 RESERVED (L2 TECH_DEBT_LEDGER schema extraction requires adding `software-planning` to verifier+sentinel skill frontmatter — coordinated refactor outside compression scope). Identified by P0a recon, deferred per user direction. // 2026-05-01: resolved — L4 mermaid removed in dec-114 commit 1 (was structurally wrong post-pair-split); L5 ARCHITECTURE.md description tightened in commit 2; combined achieved ~189 tokens. RESERVED L2 cut still pending (separate coordination concern); reclassified `other`→`token-budget`. | 5335ed851980 |
| td-009 | suggested | token-budget | code-to-goals | rules/swe/adr-conventions.md | claude-md | rules/CLAUDE.md#token-budget | orchestrator | 2026-04-30 | 2026-05-01 | implementer | resolved | dec-114 | ~150 tokens recovery: L5 tightening of Finalize Protocol step 4 cross-reference list to a table form (same content, tabular). Identified by P0a recon, deferred per user direction. // 2026-05-01: resolved — converted bullet list to table; bytes nearly neutral (table markdown overhead negated savings) but structure improved; reclassified `other`→`token-budget`. Original ~150-token estimate was optimistic. | 4d7083638fad |
| td-010 | suggested | token-budget | code-to-goals | rules/swe/memory-protocol.md | claude-md | rules/CLAUDE.md#token-budget | orchestrator | 2026-04-30 | 2026-05-01 | implementer | resolved | dec-114 | ~80 tokens recovery: L5 removal of three `remember()` call examples from `### When to Remember`; function signature in `### How to Remember` section remains canonical. Identified by P0a recon, deferred per user direction. // 2026-05-01: resolved — examples removed plus the surrounding scaffolding; achieved ~135 tokens (file 4895→4409 bytes), exceeded target; reclassified `other`→`token-budget`. | 891cac861d28 |
| td-001 | important | duplication | code-to-goals | commands/onboard-project.md, commands/new-project.md | code-quality |  | verifier | 2026-04-27 | 2026-04-27 | implementation-planner | resolved | 72d6db7 | Four canonical blocks (Agent Pipeline, Compaction Guidance, Behavioral Contract, Praxion Process) duplicated byte-identically across both onboarding commands; mirror discipline enforced by author + regex test, not by extraction. Proper fix: extract canonical blocks to a single source-of-truth (e.g., `claude/canonical-blocks/<name>.md`) consumed by both commands. User flagged during Step 11; refactor must cover all four blocks together to avoid mixed-state. Resolved by extracting to `claude/canonical-blocks/<slug>.md` with `scripts/sync_canonical_blocks.py` enforcing byte-identicality via pre-commit hook (build-time compilation per dec-082). | 83fa92c1f787 |
| td-020 | suggested | other | code-to-goals | install_claude.sh:382-396 | code-quality |  | orchestrator | 2026-05-08 | 2026-05-13 | implementer | resolved | install_claude.sh:install_praxion_pre_commit | `install_git_merge_infra` pre-commit handling: first install with a pre-existing user pre-commit hook that lacks `check_shipped_artifact_isolation` correctly *appends* the Praxion block, preserving the user's content. Second install detected the Praxion marker and fell through to `cp` — silently *overwriting* the entire file, losing any user customizations made between installs. Edge case, but the asymmetry between first-install (preserve) and second-install (overwrite) was unprincipled. Resolution: detect the Praxion-managed block by sentinel comments on both ends, then in-place replace just that block on subsequent installs. Filed by orchestrator under explicit user direction during the 2026-05-08 install audit. // 2026-05-13: verified RESOLVED — `install_praxion_pre_commit` in `install_claude.sh` implements the prescribed 4-path sentinel-comment approach (`PRAXION_PRECOMMIT_SENTINEL_START`/`_END` markers; no-hook→write-fresh-wrapped / sentinels-present→in-place-replace / legacy-content→warn+append / user-hook→append-preserve). The inline comment block originally mislabeled this as the "td-021 fix"; corrected to "td-020 fix" in the same housekeeping pass. | 3d0f02e0b7d4 |
| td-023 | suggested | other | code-to-goals | dashboard_app/src/app/globals.css | code-quality |  | orchestrator | 2026-05-11 | 2026-05-13 | systems-architect | resolved | dashboard-ux-overhaul (REQ-16, dec-draft-16cd58ca) | Single light theme only for v1 (Decision 9). Dark mode deferred — multiplied CSS surface cost for unclear demand in a local read-only portal. Resolution: add `prefers-color-scheme: dark` media-query block to `globals.css` + test with dark system theme. User-confirmed defer 2026-05-11. // 2026-05-13: verified RESOLVED — `globals.css` carries the `@media (prefers-color-scheme: dark)` :root override block (line ~169); shipped by the `dashboard-ux-overhaul` pipeline (SYSTEMS_PLAN REQ-16, ADR dec-draft-16cd58ca) alongside the de-hardcode token-layer sweep. (The same sweep grew `globals.css` to 2,368 lines — that's td-030, a distinct file-size-ceiling row, still open.) | 40e438f5d799 |
| td-026 | suggested | dead-code | code-to-goals | streamlit_app/ | code-quality |  | orchestrator | 2026-05-11 | 2026-05-13 | systems-architect | resolved | 313a50e | dec-134 says retire Streamlit after parity. Parity reached with the dashboard-redesign. Hard retire (delete `streamlit_app/`) was a separate cleanup task requiring explicit user approval. // 2026-05-13: RESOLVED — `streamlit_app/` deleted in commit 313a50e (53 tracked files: app, components, pages, widgets, data layer, tests) + entry removed from `[tool.pytest.ini_options].testpaths` in `pyproject.toml`; pytest collection clean afterward (770→667 tests, 0 collection errors). Doc + codex-bridge references to `streamlit_app` (CLAUDE.md / AGENTS.md(.tmpl) repo-layout rows, docs/architecture.md, dashboard_app/README.md, dashboard-conventions.md `paths:` globs + §7, html-output-conventions.md `paths:` glob, two skill mentions, export-codex-rules-bridge.py keyword alias) intentionally left for a follow-up cleanup pass. | d6c181e7be92 |
| td-028 | suggested | other | code-to-goals | agents/sentinel.md | code-quality |  | orchestrator | 2026-05-11 | 2026-05-13 | unassigned | wontfix |  | `html-output-conventions.md` referenced a phantom `EC07-doc-manifest-fresh` check (since removed). If a manifest-freshness check is wanted, it needs a new ID (EC08+) to avoid collision with the removed EC07 entry. Class `other`: no enum slot covers missing-sentinel-check gaps; owner-role `unassigned` per heuristic (sentinel implementation is not owned by a standard consumer role). // 2026-05-13: WONTFIX — not actionable debt. The dangling reference was already removed before this row was filed (verified: grep for `EC07` / `doc-manifest-fresh` in `rules/writing/html-output-conventions.md` returns nothing on 2026-05-13). This row only records the forward-looking ID-allocation note: any future manifest-freshness sentinel check must allocate EC08+ to avoid namespace collision with the retired EC07. No code or doc change required. | 3504f9c45d1e |
| td-031 | suggested | dead-code | code-to-goals | dashboard_app/src/lib/health-tone.ts | code-quality |  | verifier | 2026-05-12 | 2026-05-13 | implementer | resolved | dashboard dead-code cleanup (td-031/td-032) | `metricTone()` + `ToneResult` (the `arrow`/`word` per-metric tone deriver) were exported and unit-tested (8 `it()` blocks) but never consumed by any production component: `metrics-dashboard.tsx` uses `getMetricTone()` from `@/lib/metrics` plus a local arrow ternary instead. `healthSummary()` from the same module IS used. Resolution options were: route the metrics components through `metricTone()` (DRY) or delete `metricTone`/`ToneResult` and the tests. // 2026-05-13: RESOLVED via delete — removed `metricTone()` + `ToneResult` from `src/lib/health-tone.ts` and the `describe("metricTone")` block (8 tests) from `tests/server/health-tone.test.ts`; `healthSummary` / `HealthLabel` / `HealthSummary` retained. Chose delete over "route through it": the latter would refactor working code in the #1 hotspot (`metrics-dashboard.tsx`) to consume a function nothing asked for. Verified: `vitest run` 301 passed; `next build` clean. | 2a16b6ca023b |
| td-032 | suggested | dead-code | code-to-goals | dashboard_app/src/server/view-models/sidebar-signals.ts | code-quality |  | verifier | 2026-05-12 | 2026-05-13 | implementer | resolved | dashboard dead-code cleanup (td-031/td-032) | `SidebarSignals` exported both `activeWorkshops` and `activeWorkshopCount` carrying the identical value; only `activeWorkshops` is read (`sidebar-nav.tsx`). The `activeWorkshopCount` alias was a test-vs-impl naming wrinkle that survived into the merged module. Resolution: drop `activeWorkshopCount` (and update any test referencing it). // 2026-05-13: RESOLVED — removed the `activeWorkshopCount` field from the `SidebarSignals` type and the `getSidebarSignals` return value in `src/server/view-models/sidebar-signals.ts`; tightened the `activeWorkshops` doc-comment; fixed the stale `activeWorkshopCount` reference in the `.nav-badge` comment in `globals.css`. No test change needed — `tests/server/sidebar-signals.test.ts` already used `activeWorkshops` exclusively. Verified: `vitest run` 301 passed; `next build` clean. | 0f91418de311 |
| td-030 | important | complexity | code-to-goals | dashboard_app/src/app/globals.css | code-quality |  | verifier | 2026-05-12 | 2026-05-13 | implementation-planner | resolved | dashboard CSS layer split (td-030) | `globals.css` was 2,368 lines after the `dashboard-ux-overhaul` token-layer rewrite + de-hardcode sweep, ~3x the 800-line ceiling in `rules/swe/coding-style.md`. The file's own `/* TODO */` proposed a 3-file split (tokens + base + components), but `components.css` would still have been ~2,100 lines — so the actual split is six layer files, each ≤ 800 lines: `tokens.css` (210 — `:root` + dark `:root`), `base.css` (27 — element resets), `app-chrome.css` (349 — app shell / sidebar / brand / nav / header / page wrappers / lede), `surfaces.css` (390 — page grids / cards / markdown surface / lists / metric-summary + metric-trend cards / trend chart), `widgets.css` (712 — chips / tabs / error-state / educational popover / collapsible cards / metadata chips / collector status / metrics-raw / KPI tiles / health strip / hotspot bar / collector chips), `pages-and-viz.css` (675 — responsive @media breakpoints / recharts wrappers / AaC badges / per-page layouts for architecture+ADR+sentinel / component index / markdown ToC / DiagramFrame / DiagramModal). `globals.css` is now a ~16-line `@import` manifest in cascade order (`pages-and-viz.css` imported last so its `@media (max-width:…)` overrides keep winning by source order). // 2026-05-13: RESOLVED — split executed cut-don't-reorder; verified `diff` of `cat(tokens base app-chrome surfaces widgets pages-and-viz)` against the original-minus-TODO-header → byte-identical, so the cascade is provably unchanged. `cd dashboard_app && ./node_modules/.bin/next build` → clean (the `@import` chain resolves and bundles; the built css chunk contains classes from all six layers); `./node_modules/.bin/vitest run` → 301 passed. `layout.tsx` still imports `./globals.css` unchanged. Note: minor stale `globals.css` mentions remain in `skills/web-ui-design/*` (tokens now live in `tokens.css`) — left for a future doc-freshness pass. | dc936cc43878 |
| td-033 | important | other | code-to-goals | agents/doc-engineer.md, agents/implementer.md, agents/test-engineer.md, skills/rule-crafting/SKILL.md | code-quality |  | orchestrator | 2026-05-12 | 2026-05-17 | unassigned | wontfix |  | Path-scoped rules (`paths:` frontmatter) inject only when Claude reads a matching file — not on Write/Edit/MultiEdit (verified 2026-05-12 against `code.claude.com/docs/en/memory` + GitHub issues anthropics/claude-code#23478, #38487, #16853). An agent that creates a new file via Write without first reading a matching sibling misses that file type's conventions: new `.py`/`.ts` misses `coding-style.md`; new doc misses `readme-style.md`/`diagram-conventions.md`/`html-output-conventions.md`/`aac-dac-conventions.md`; new `.github/*.md` misses `pr-conventions.md`; new files miss `id-citation-discipline.md`, `staleness-policy.md`. Touches Praxion's rules-as-guardrails guarantee surface directly; moderate untreated severity (agents usually read a directory before working in it, which incidentally loads the rule). Mitigated 2026-05-12 (ROADMAP P1): added a "read an existing sibling before creating a new file" instruction to the Constraints sections of `agents/implementer.md`, `agents/doc-engineer.md`, `agents/test-engineer.md`; documented symptom + full mitigation in `skills/rule-crafting/SKILL.md` (new "Path-Scoped Rules: Read-Only Loading Trigger" section); added a `Known Claude Code Limitations` bullet to CLAUDE.md; added a path-scoped caveat next to the `paths:` convention in `rules/CLAUDE.md`. // **2026-05-12 upstream re-check** — #23478 (core bug) CLOSED `NOT_PLANNED` 2026-03-14, #38487 (feature request) CLOSED `NOT_PLANNED` 2026-04-30: upstream has **explicitly declined** to load path-scoped rules on Write/Edit. The agent-prompt mitigation is therefore **permanent** (not provisional) and should be treated as a design constraint, not awaited tech debt. #16853 remains open with a relevant workaround signal: per user comments, multi-entry YAML-list `paths:` may fail under the `_9A()` CSV parser, while single-entry brace-expansion (`paths: ["**/*.{py,pyi}"]`) and inline-array form work consistently — Praxion's rules predominantly use YAML-block-list syntax, so `scripts/check_paths_syntax.py` was added 2026-05-12 to flag at-risk rule files. Windows variant #21858 CLOSED `COMPLETED` 2026-03-24 — no further Praxion action (won't-do); the existing Windows caveat in `docs/existing-project-onboarding.md` stays in place as historical guidance. Optional non-blocking follow-up: sandbox an `InstructionsLoaded` hook to log which path-scoped rules actually load in a pipeline run; lean against unless the prompt mitigation proves insufficient. Class `other`: no enum slot covers upstream-tool-behavior gaps (same as td-021). Owner-role `unassigned`: resolution is gated on an external upstream stance that won't change, so this row should be promoted to a "documented design constraint" (move to RESOLVED as `wontfix`) in a subsequent ledger sweep once the syntax-checker script confirms no at-risk rules remain. // 2026-05-17: migrated to `wontfix` — exit condition met. `scripts/check_paths_syntax.py` exits clean (all 13 path-scoped rules use a reliable `paths:` form, 0 at-risk); upstream #23478 + #38487 both permanently CLOSED `NOT_PLANNED`; the "read a sibling first" agent-prompt mitigation in implementer/doc-engineer/test-engineer is in place and is a permanent design constraint, not awaited debt. Confirmed grounded-as-constraint by the 2026-05-17 sentinel audit + orchestrator sweep. | b8fb1a83b357 |
| td-048 | suggested | drift | code-to-goals | CLAUDE.md, scripts/test_claude_to_agents.py | code-quality |  | verifier | 2026-07-01 | 2026-07-15 | doc-engineer | resolved | orchestrator (0ad86df) | [direct-capture-contract verification, pre-existing baseline failure] `scripts/test_claude_to_agents.py::test_praxion_root_agents_template_matches_generator` fails: the project-root `CLAUDE.md` has drifted from the generated `AGENTS.md.tmpl` (a `Runtime: Python >=3.11 required` line and other content not mirrored into the generator's expected output). Confirmed pre-existing (TEST_BASELINE.md) and fully unrelated to this pipeline — this task never touches project-root `CLAUDE.md` (only `claude/config/CLAUDE.md.tmpl`, a different file). Resolution: regenerate the AGENTS template from project-root `CLAUDE.md`, or reconcile the drifted lines. Class drift; owner-role doc-engineer. // 2026-07-15: RESOLVED: regenerated AGENTS.md.tmpl from project-root CLAUDE.md via the canonical claude_to_agents.py generator (commit 0ad86df); test_praxion_root_agents_template_matches_generator now green (3/3). The drifted "Runtime: Python >=3.11" bullet is mirrored into the template. | 3427ba281d32 |
