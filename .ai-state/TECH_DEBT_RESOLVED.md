# Resolved Tech Debt

<!-- Sibling of TECH_DEBT_LEDGER.md holding rows with terminal status (resolved / wontfix).
     Rows arrive via scripts/finalize_tech_debt_ledger.py migration when status transitions
     to a terminal value. Schema, lifecycle, and re-open semantics are defined canonically
     in skills/software-planning/references/tech-debt-ledger.md § Schema. Do not duplicate
     the schema here — the skill reference is the single source of truth (F-12). -->

**Schema**: 14 row fields + 1 structural `dedup_key`. See [`skills/software-planning/references/tech-debt-ledger.md`](../skills/software-planning/references/tech-debt-ledger.md) § Schema for field definitions.

**Rows arrive here automatically** when status transitions to `resolved` or `wontfix`. The pair forms one logical namespace with `TECH_DEBT_LEDGER.md`: `id` and `dedup_key` are unique across both files. Cross-file `dedup_key` matches trigger re-open (the historical resolved row moves back to LEDGER).

| id | severity | class | direction | location | goal-ref-type | goal-ref-value | source | first-seen | last-seen | owner-role | status | resolved-by | notes | dedup_key |
|----|----------|-------|-----------|----------|---------------|----------------|--------|------------|-----------|-----------|--------|-------------|-------|-----------|
| td-056 | suggested | duplication | code-to-goals | scripts/refresh_claude_blocks.py | code-quality |  | verifier | 2026-07-16 | 2026-08-31 | implementer | resolved | d5024424 (onboarding-unification) | [verifier, block-refresh-mechanism] `refresh_claude_blocks.py::_find_block_span` (lines 244-259) re-implements `canonical_block_identity.extract_live_body`'s heading->next-`## `-or-EOF boundary logic verbatim rather than calling a shared primitive. Traced byte-identical today; the duplication is a disclosed, scoped trade-off (the apply-mode step's file scope was `refresh_claude_blocks.py` only, and the shared module returns text not line-indices). Residual risk: nothing enforces the two boundary functions stay in sync, so a future edit to one (e.g. an H3-nesting or fenced-block rule) could silently diverge classify-time extraction from apply-time span computation. Fix: expose a shared boundary-index primitive from `canonical_block_identity.py` that both `extract_live_body` and `_find_block_span` call. Secondary (same fix direction, lower priority): `run_apply` re-reads the target `CLAUDE.md` independently of the read `_classify_all` already did -- a TOCTOU double-read with zero realistic trigger in the single-threaded CLI path; thread the already-read text through. Not blocking; refuse-to-clobber safety is unaffected (failure direction stays safe). // 2026-08-31 [implementer R3, onboarding-unification] RESOLVED: `canonical_block_identity.py` now exposes `find_heading_span(lines, heading)`, the single boundary-index primitive; `extract_live_body` and `refresh_claude_blocks.py`'s `_apply_stale_replacements` both call it (the private `_find_block_span` wrapper is deleted). Secondary TOCTOU fix: `main()` reads CLAUDE.md once and threads the text through both `_classify_all` and `run_apply`. Divergence canary (H3-nested fixture) + `test_module_no_longer_defines_its_own_block_span_scanner` + single-read regression test added; `pytest scripts/test_canonical_block_identity.py scripts/test_refresh_claude_blocks.py -q` -> 45 passed. | ed830cb22d60 |
| td-130 | suggested | other | goals-to-code | scripts/onboard-project, skills/onboard-project/SKILL.md, skills/onboard-project/references/detection.md, skills/onboard-project/references/phases-core.md, tests/onboard_project_test.sh | code-quality |  | orchestrator | 2026-08-06 | 2026-08-31 | context-engineer | resolved | 109be8bb (onboarding-unification) | [surfaced while closing td-075] `new_project.sh` leaves `.claude/` empty by contract -- both its own guard check and the greenfield shell test assert this -- so the seed pipeline spawns its subagents before `/onboard-project` installs the permissions.allow baseline. The subagent-Write exposure td-075 closed for existing projects therefore stays open for the greenfield path's own seed run, which is the one place a brand-new user meets it first. Closing it means changing the empty-`.claude/` contract and the two assertions pinning it, so it is a decision rather than a fix. Proposed owner-role systems-architect. // 2026-08-30 [systems-architect, onboarding-unification] DECIDED (dec-344), not yet fixed. Old location fields (`commands/new-project.md`, `new_project.sh`) are deleted by the onboarding unification; re-pointed to the live surfaces. Decision: the bash scaffold (`scripts/onboard-project::scaffold_project`) seeds a minimal `.claude/settings.json` carrying ONLY the `permissions.allow` baseline — the empty-`.claude/` contract becomes a seeded-`.claude/` contract — AND Phase 5's 5b sub-step is hoisted ahead of Phase 0s in `new` mode to cover direct skill invocation, which the bash layer cannot reach. Phase 5 stays the single idempotent authority; the baseline value stays canonical in `phases-core.md § Phase 5` with an agreement test pinning the bash copy to it. Full core-before-seed reorder REJECTED: phases 0.5/6/8c/8d/8e are detection-dependent and would run against an empty tree. Amends one bullet of dec-055 (`create empty .claude/`) while re-affirming its layering principle; dec-053 untouched. NOTE the two assertions this row cites as pinning the contract did not survive the rewrite as tests — `tests/onboard_project_test.sh` asserts only that `.claude/` exists; the contract is now pinned by `guard_greenfield_shape` (script) + `detection.md § Guard — greenfield shape`, i.e. a routing predicate, so changing it silently changes routing rather than failing a test. Mechanism spec: `.ai-work/onboarding-unification/TD130_MECHANISM_SPEC.md` (6 edits). Stays `open` until those edits land. // 2026-08-31 [implementer R1, onboarding-unification] RESOLVED: all 6 `TD130_MECHANISM_SPEC.md` edits landed in commit `109be8bb`. `scripts/onboard-project::scaffold_project` seeds `.claude/settings.json` with `{"permissions":{"allow":["Write(.ai-work/**)"]}}` at scaffold time (guarded, non-destructive); `guard_greenfield_shape` + `detection.md § Guard — greenfield shape` widened to tolerate the seeded file (still matches a pre-seed empty `.claude/`); `SKILL.md § Mode × Phase Matrix` documents the `new`-mode hoist as ordering marker `5b′` (absent from `§Capability IDs`/`§Idempotency Predicates` by design); `phases-core.md § Phase 5` sub-step 5b carries the canonical value other sites cite. `tests/onboard_project_test.sh` T13 pins the scaffold-to-canonical-value agreement, T14 pins the guard's post-seed regression + pre-seed backward-tolerance. `bash tests/onboard_project_test.sh` → 15 passed; `python3 -m pytest tests/commands/ tests/test_retired_onboarding_surfaces.py -q` → 87 passed. | 4efc8051657d |
| td-133 | important | drift | code-to-goals | skills/software-planning/references/tech-debt-ledger.md, .ai-state/TECH_DEBT_RESOLVED.md | code-quality |  | orchestrator | 2026-08-06 | 2026-08-31 | context-engineer | resolved | 26cdbc86 (onboarding-unification) | [orchestrator, scope gap surfaced while building `scripts/check_state_ledgers.py` for td-085] The `dedup_key` 5-tuple cannot express the identity of five live rows. Four pairs of genuinely distinct findings share a (class, location, direction, goal-ref-type, goal-ref-value) tuple: td-054 with td-017 and td-052 with td-011 (both `scripts/finalize_adrs.py`), td-045 with td-083 (`fitness/tests/test_gate_canary_coverage.py`), td-028 with td-118 (`agents/sentinel.md`). Recomputing their keys would make the next `finalize_tech_debt_ledger.py` run collapse each pair into a single row -- erasing a `td-NNN` outright and, for td-028, destroying a `wontfix` tombstone the schema states is preserved and never removed. So the one-time backfill deliberately skipped all five, and the checker reports them as ADVISORY `dedup-collision-blocked` findings rather than blocking ones: a gate nobody can legally satisfy is a gate that gets disabled. THIS ROW IS THE NAMED READER for that advisory count (gate-liveness clause 6) -- the count is 5 today, and any change to it belongs here. Consequence while open: those five rows are unreachable by re-open-on-recurrence, so a genuine recurrence at one of those tuples files a fresh row instead of re-opening the historical one. Fix direction: extend the tuple with a discriminator the producers already write (a short `notes` digest, or an explicit sequence suffix minted on collision), NOT a change to the existing five terms -- that would invalidate all 127 conforming keys at once and re-open td-085 wholesale. td-085 recorded that the documented formula scores highest of 56 brute-forced variants, so the defect is granularity, not correctness; the two are separate questions and only the first is open. // 2026-08-31 [implementer R3, onboarding-unification] RESOLVED: `state_ledger_schema.py` mints a `notes`-digest discriminator only on base-5-tuple collision (`resolve_dedup_keys`); the 127+ non-colliding keys are byte-identical to before. Backfilled the five blocked rows (td-054, td-052, td-045, td-028, td-118) with discriminated keys in `TECH_DEBT_RESOLVED.md` via `check_state_ledgers.py --backfill` -- only the `dedup_key` cell of each row changed (the td-028 `wontfix` tombstone's other fields are untouched). `python3 scripts/check_state_ledgers.py --check` -> 148/148 conformance, 0 blocking, 0 advisory (advisory count 5 -> 0). Added regression tests: discriminator-on-collision, discriminator-does-not-touch-existing-holder-keys, `finalize_tech_debt_ledger.py` no longer collapses the historical td-017/td-054 shape. | 3bba4a2f8920 |
| td-142 | important | drift | goals-to-code | scripts/spec_drift.py | code-quality |  | orchestrator | 2026-08-07 | 2026-08-31 | implementer | resolved | d72f8b08 (onboarding-unification) | [orchestrator, important-findings-remediation 2026-08-07] Sentinel check SH07 is structurally blind to 48% of the REQ corpus. scripts/spec_drift.py:60 defines REQ_PATTERN = re.compile(r'\bREQ-\d+\b'), which matches only the bare REQ-NN form. Measured across .ai-state/specs/: 130 REQ-NN (matched) vs 50 REQ-PM-NN, 41 REQ-DDL-NN and 30 REQ-DIAGRAM-NN (all unmatched) -- 121 of 251 identifiers invisible to untracked-req detection. Separately _detect_archived is an acknowledged stub returning []; its own test module says 'When _detect_archived is implemented, replace the injection with a real drifting...'. This is the scope-fidelity clause of rules/swe/gate-liveness.md: the gate flags correctly inside a narrower-than-documented scope and returns a false all-clear outside it. Widening the regex is NOT a one-line change -- it would make 121 previously-invisible REQ IDs eligible for findings, so it needs its own test work and a look at the fallout. [test-engineer R4, 2026-08-31] Widened to \bREQ-(?:[A-Z]+-)?\d+\b, derived from and matching the exact 130/41/30/50 corpus counts above; added corpus-form regression tests + a canary parsing .ai-state/specs/ directly. Fallout: ZERO live findings -- REQ_PATTERN is reachable only from _extract_reqs_from_spec_delta (the in-flight SPEC_DELTA.md untracked-req path), never from _detect_archived (still the acknowledged stub, unchanged, returning [] before any REQ parsing) which is the code path that actually walks .ai-state/specs/. Confirmed via `scripts/check_spec_drift.py --repo-root . --json` -> 0 findings both before and after. The 48%-blindness the row describes therefore persists in latent form until _detect_archived is implemented (separate, out-of-scope stub) -- this fix is correct and complete for its own code path but does not by itself change SH07's live behavior against the archived corpus. | d394c7c6b253 |
| td-147 | important | coverage-gap | code-to-goals | hooks/remind_task_brief.py | code-quality |  | sentinel | 2026-08-24 | 2026-08-31 | test-engineer | resolved | 469e94b1 (onboarding-unification) | [sentinel TD03, sweep 2026-08-24; re-scoped by orchestrator 2026-08-25] Originally filed as FIVE hooks/ modules below the 70% floor (worst 38.07%) -- but the filing read the 17-day-stale METRICS_REPORT_2026-08-07 artifact, the exact TD06 staleness defect the same sweep flagged. Fresh measurement (METRICS_REPORT_2026-08-25_04-25-13, full-suite coverage refresh, 3266 passed): four of the five now measure above the floor (the td-139/td-140 in-process runpy remediation had already landed); the sole remaining gap is hooks/remind_task_brief.py at 33.3% (18/54 lines). Remediate with in-process tests per skills/testing-strategy/references/python-testing.md section Subprocess-Driven Tests Measure Zero Coverage -- do not file or judge hook coverage from subprocess-driven suites. [test-engineer R4, 2026-08-31] Rewrote hooks/test_remind_task_brief.py to drive `_process()`/`main()` in-process (module loaded via importlib, stdin/stderr monkeypatched) mirroring the td-139/td-140 pattern, kept runpy-driven tests for the `__main__` fail-open wrapper, retained 2 subprocess tests as an end-to-end contract proof only. Measured: `pytest hooks/test_remind_task_brief.py --cov=hooks.remind_task_brief` -> 100.0% (54/54 lines, 18/18 branches). | 71c06fbb41fc |
| td-140 | important | other | code-to-goals | pyproject.toml, hooks/test_inject_worktree_banner.py | code-quality |  | orchestrator | 2026-08-07 | 2026-08-24 | test-engineer | resolved | orchestrator | [orchestrator, important-findings-remediation 2026-08-07] Coverage is structurally unmeasurable for subprocess-driven tests. Under pytest-cov 7.1.0 no COV_CORE_* or COVERAGE_PROCESS_START variable is exported during an active --cov run (verified by an env probe), so the installed a1_coverage.pth never activates in spawned children and every subprocess-driven test measures ZERO. Demonstrated: inject_worktree_banner.py carried 15 real behavioural tests and measured 37.0%. `parallel = true` does not fix it. This directly contradicts the comment at pyproject.toml:76-84, which asserts subprocess coverage embedding works and pins --cov-config to serve it. Consequence for this ledger specifically: any subprocess-tested module will keep being filed as coverage debt however well tested, so TD03 findings against such modules are false positives until this is resolved. Surfaced while remediating td-139, whose five modules were remediated with in-process tests instead. // RESOLVED 2026-08-24: the false pyproject.toml comment (lines ~76-84) was rewritten to state the measured truth, after a wired experiment re-confirmed the defect one level deeper — with COVERAGE_PROCESS_START hand-exported the a1_coverage.pth DOES fire and the child collects, but writes its data file into its own cwd (tmp_path, destroyed at teardown) and pytest-cov 7 has no combine step for foreign child data, so no cheap mechanical wiring closes the loop. Disposition: subprocess auto-embedding is documented as dead; the sanctioned measurable pattern (in-process runpy.run_path(run_name="__main__") tests, proven by the td-139 remediation) is codified in skills/testing-strategy/references/python-testing.md § Subprocess-Driven Tests Measure Zero Coverage, which also instructs readers of coverage numbers not to file debt rows against subprocess-only-tested modules. | e7bcef08638b |
| td-012 | important | complexity | code-to-goals | scripts/project_metrics/report.py | code-quality |  | sentinel | 2026-05-01 | 2026-08-13 | implementation-planner | resolved | deep-fix-round | Rank-2 hotspot (score=21,399): complexity=21, churn=1,019 over 90d; central reporting aggregator in active development; complexity trend warranted monitoring before it reached the 800-line ceiling. // 2026-05-13: re-severitied suggested→important and owner-role implementer→implementation-planner — `report.py` is now 929 lines, OVER the 800-line ceiling in `rules/swe/coding-style.md` it was being monitored toward (still rank-3 hotspot, score=21,399, in METRICS_REPORT_2026-05-13). Module split warranted — extract per-collector rendering subsections to a `report_sections/` package, or split aggregate/trend/hotspot rendering from the orchestration shell. Owner-role flipped per the "module restructuring → implementation-planner" heuristic, matching td-003 / td-004; revert to implementer if a modest extraction (vs. a full decomposition) turns out to suffice. // 2026-08-06 last-seen refreshed (was 2026-05-17, stalled 81 days). The subject has WORSENED, not held: the row was re-severitied at 929 lines against the 800-line ceiling in rules/swe/coding-style.md; the file measured 1087 at the 2026-08-06 sentinel audit (+17%) and is 1117 now, +30 from the same day's pydeps scope-fidelity work (which widened the collector to every import root and published an analyzed-vs-repository coverage denominator -- a net-positive change that nonetheless lands in this file). No new row: consumers update status in place and never add rows. The module-split this row recommends is now 40% larger than when proposed, and each correct improvement to the metrics report grows it further -- which is the argument FOR splitting rather than against, since the file is absorbing changes that belong to distinct concerns (rendering, aggregation, per-collector summarisation). // 2026-08-13 resolved (deep-fix-round): "a modest extraction... suffices" — reverted to implementer scope per the row's own escape clause. Split into 4 flat modules matching the package's existing leading-underscore private-helper idiom (`_stdlib_sloc.py`, `_path_filter.py`): `_report_format.py` (122 lines, shared fmt_*/marker primitives), `_report_deep_dive.py` (560 lines, the 7 per-collector summarizers + deep-dive rendering), `_report_sections.py` (305 lines, aggregate/top-n/trends/per-language sections); `report.py` shell now 227 lines (orchestration + header/tool-availability/install-to-improve/run-metadata + the two public entry points). All four well under the 800-line ceiling. Public surface (`render_markdown`, `render_json`) unchanged. Proof: `pytest scripts/project_metrics/tests/` 669/669 passed before and after (git-checkout-restored baseline vs. split), `--collect-only` node-id diff clean, `python3 -m scripts.project_metrics --help` byte-identical. | 62a324775090 |
| td-058 | important | complexity | code-to-goals | codex/config/export-codex-rules-bridge.py | code-quality |  | sentinel | 2026-07-29 | 2026-08-13 | implementation-planner | resolved | deep-fix-round | Rank-3 hotspot (score=39,842): complexity=22, churn=1,811 over 90d; file is 1,435 lines, nearly double the 800-line ceiling in `rules/swe/coding-style.md`. High, sustained churn (codex-bridge export logic under active iteration) combined with size well past the ceiling signals a module-split candidate rather than routine growth — same class as td-012/td-054. No existing ledger row references this file. Owner-role implementation-planner per the "module restructuring required" heuristic (matches td-012/td-054 precedent). // 2026-08-13 resolved (deep-fix-round): split into 5 rules_bridge_* modules (parsing 307 / routing-hooks 162 / static-modules 258 / delegate-hooks 416 / registrations 256); entry point now a 117-line CLI shell; suite 13/13 green post-split, CLI --help surface intact. | 0eeb47c32516 |
| td-090 | suggested | drift | code-to-goals | .ai-state/CONSULT_PRIORS.md, .ai-state/CONSULT_LEDGER.md | code-quality |  | verifier | 2026-07-31 | 2026-08-13 | doc-engineer | resolved | deep-fix-round | [verifier, multidisciplinary-identities Waves 1-2 + td-081 verification] `.ai-state/CONSULT_PRIORS.md`'s `## Column Definitions` documents the Challenge Classification table's `timestamp` column as matching 'this consult's CONSULT_LEDGER.md rows'. Live data on the td-081-sealed-prior CH-01 row: the classification row (`CONSULT_PRIORS.md`) carries `2026-07-31T06:40:00Z`; the corresponding ledger row (`CONSULT_LEDGER.md`) carries `2026-07-31T06:10:00Z` -- a 30-minute gap, presumably the write-order latency between the two files (the ledger disposition is written first, the classification row after). No check reads that cell to enforce the documented equality; the existing gates validate format and seal-witness agreement, not this specific cross-file timestamp claim. Fix: either loosen the doc's claim to describe the two timestamps as independently-written and order-only (ledger row precedes classification row), or add the cross-file check if exact equality is actually intended. // 2026-08-13 resolved (deep-fix-round): Column Definitions loosened to order-only independent timestamps (fix direction (a) of the row); no cross-file check added. | 7c60645b9ae6 |
| td-091 | suggested | drift | code-to-goals | .ai-state/DESIGN.md, docs/architecture.md | code-quality |  | verifier | 2026-07-31 | 2026-08-13 | doc-engineer | resolved | deep-fix-round | [verifier, multidisciplinary-identities Waves 1-2 + td-081 verification] `.ai-state/DESIGN.md` and `docs/architecture.md` both still describe a one-discipline roster; `evidence-appraiser` (shipped since Wave 1) appears in NEITHER. The two documents are stale asymmetrically -- `DESIGN.md` knows about the consult state files (CONSULT_LEDGER.md/COSTS.md/PRIORS.md) but not the second discipline; `docs/architecture.md` knows a second discipline mechanism exists structurally but not the state files -- so neither is a superset of the other, and a reader consulting only one gets a materially different (and wrong in a different way) picture of the same subsystem. Same staleness-asymmetry shape as td-068's three-CLAUDE.md-basis finding, one level down in a different doc pair. Fix: a single coordinated pass over both documents' consult/discipline sections, bringing each to parity with the other before either is trusted as complete. // 2026-08-13 resolved (deep-fix-round): coordinated parity pass done -- DESIGN.md row retitled Waves 1-2 (statistician, evidence-appraiser) with the wave sentence updated and skills/evidence-appraisal/ added to affected files; docs/architecture.md binding sentence names both shipped disciplines; both deliberate-label parentheticals preserved. | 0bdde8be34c2 |
| td-093 | suggested | drift | code-to-goals | docs/architecture.md, docs/diagrams/architecture/src/architecture.c4 | code-quality |  | verifier | 2026-07-31 | 2026-08-13 | doc-engineer | resolved | deep-fix-round | [verifier, multidisciplinary-identities Waves 1-2 + td-081 verification] All four `aac:generated` fences in `docs/architecture.md` claim `last-regen=2026-05-14`, while the most recent commit touching any `.c4` source is from 2026-07-29 -- over two months of drift between the claimed and actual regen dates. The fence metadata exists precisely so a reader can trust a generated region without re-deriving it; a stale claim defeats that purpose silently, since the fence still renders as 'generated,' producing false confidence. Likely the same root cause as the sibling `discipline-consultant`-element gap filed alongside this row (the model changed without the regen hook running). Fix: run the diagram-regen hook and commit the refreshed fences with a corrected `last-regen` stamp; consider a fitness check that fails when a `.c4` source's commit date postdates its fence's `last-regen` claim. // 2026-08-13 resolved (deep-fix-round): regen re-run from the current .c4 (likec4 gen produced zero drift -- committed renders were already current; only the stamps lied) and all four fences stamped last-regen=2026-08-13. The considered fitness check (fence date vs source commit date) deliberately NOT built: no tooling owns the stamp, so the check would mint a manual chore on every .c4 edit without hook support -- if drift recurs, the remedy is auto-stamping inside scripts/diagram-regen-hook.sh, not a bare check. | 8b2133f511e5 |
| td-115 | important | complexity | code-to-goals | dashboard_app/src/components/readiness-section.tsx | code-quality |  | sentinel | 2026-08-05 | 2026-08-13 | implementer | resolved | deep-fix-round | [sentinel, sentinel-remediation] TD01 hotspot: rank 2 by hotspot_score (47840 = churn 736 x complexity 65) in METRICS_REPORT_2026-07-29_22-11-31.json, and the highest raw complexity of any top-5 entry. The file is 684 lines with 2 commits in the last 90d -- churn independently confirmed at 736 via `git log --since="90 days ago" --numstat`, matching the report exactly, and the file is confirmed unremediated on disk. Filed in preference to rank-1 `metrics-dashboard.tsx` (score 111203), which was REJECTED because commit 56d5e50 (2026-07-30) decomposed it to 133 lines -- that debt is already paid and the metrics hotspots block appears frozen across three consecutive reports (see report I4). Also rejected: `export-codex-rules-bridge.py` (already td-058), `hooks/inject_rules.py` (complexity 31, ~half this candidate), `build_doc_manifest.py` (complexity 22). Remediation is component decomposition along the same lines as 56d5e50. // 2026-08-13 resolved (deep-fix-round): decomposed per the 56d5e50 pattern -- container 684 -> 77 lines + 7 extracted components (hero / criteria / pillar-views / heatmap / level-track / shared + lib/readiness-section-data.ts); public export and prop contract unchanged; vitest 473/473 identical to baseline, tsc clean, next build green. | cb4bf858b8c7 |
| td-135 | important | coverage-gap | code-to-goals | scripts/check_metrics_freshness.py | code-quality |  | orchestrator | 2026-08-06 | 2026-08-13 | test-engineer | resolved | deep-fix-round | [orchestrator, sentinel-suggested-fixes] S7/TD03 disposition of td-059's deferral trigger -- FILED, not re-affirmed. td-059's notes deferred four sub-floor scripts with an explicit re-evaluation trigger: "re-evaluate if they drift further OR gain gate-consuming callers". Clause 1 is NOT met -- check_metrics_freshness.py is 65.5%, unchanged since the deferral. Clause 2 IS met, and unambiguously: sentinel check TD06 (agents/sentinel.md:179) runs `python3 scripts/check_metrics_freshness.py --json`, and its `hotspots_touched` output BARS candidate paths from TD01 filing -- it did so twice in the 2026-08-06 audit. This module's verdict therefore gates another gate's filing decision; a silent false-PASS here un-bars stale filings, which is precisely the gate-liveness-critical shape that justified `important` severity in td-059 and td-122 rather than `suggested`. SECOND, LARGER FINDING -- the deferral had no active carrier. The four-then-eight module deferral register lived ONLY in the notes of td-059 (status resolved) and td-122 (status wontfix). Both are terminal and migrate to TECH_DEBT_RESOLVED.md, so nobody scanning the ACTIVE ledger could see the register or its trigger: 7 of the 8 deferred modules carry no open row (only check_state_ledgers.py, via td-133). Per gate-liveness.md's "a named consumer for every gate output" clause, a deferral that computes a trigger no document is named to read is inert -- which is exactly how it rolled forward twice unexamined. This row is that carrier. DEFERRAL REGISTER (still deferred, deliberately, all modest gaps with no gate-consuming caller; re-evaluate under the same two-clause trigger): check_paths_syntax.py 56.3%, check_ruff_pin_drift.py 56.3%, prune_reports.py 58.3%, project_metrics/cli.py 60.4%, self_healing_metrics.py 65.7%, check_state_ledgers.py 67.0% (carried by td-133), render_claude_md.py 69.1%, check_agent_shared_blocks.py 65.7% (routed here by sentinel S-10, 2026-08-07 — same profile: a scripts/ gate, modest gap, no gate-consuming caller; an 8th near-identical row would be noise). Closes when check_metrics_freshness.py clears the 70% floor with tests covering its stale/withheld/absent classification branches -- the paths whose false-PASS would un-bar a TD01 filing. // 2026-08-13 resolved (deep-fix-round): closure condition met -- check_metrics_freshness.py 61.0% -> 91.2% with the stale/withheld/absent classification branches covered (9 new tests, 16/16 green). DEFERRAL REGISTER carrier duty migrates to td-146 (letting this row go terminal without a successor would re-orphan the register -- the exact defect this row documented). | 1bbabfca4026 |
| td-136 | important | drift | goals-to-code | eval/src/praxion_evals/harness | code-quality |  | orchestrator | 2026-08-07 | 2026-08-13 | test-engineer | resolved | deep-fix-round | [orchestrator, eval-run 2026-08-07] The eval's `supersession_reciprocity` and `re_affirmation_reciprocity` checks report FAIL on a corpus that is verifiably clean, via two distinct defects. FIRST, they treat a NULL-VALUED key as a reference to resolve: 14 of 14 supersession cases and 11 of 11 re-affirmation cases in the failing run are ADRs carrying a bare `supersedes:` / `re_affirms:` with no value, reported as "supersedes 'None' but target ADR not found in corpus". SECOND, list-form is mishandled: `dec-225` carries `supersedes: [dec-009, dec-025, dec-039]` and all three target files exist, yet the check reports the target as not found. That is the same `string`-or-`list` gap `rules/swe/adr-conventions.md` types explicitly and that sentinel DL04/DL06 were hardened against on 2026-08-06 -- the eval's equivalent checks never received the fix, so two implementations of one contract have diverged. Ground truth: the corpus was verified twice on 2026-08-07 with a real YAML parser, bidirectionally (every `supersedes` -> `superseded_by`, every `re_affirms` -> `re_affirmed_by`, and the reverse direction including DL06's partial-supersession carve-out) -- 330 ADRs, zero broken links. Fix: iterate list elements, and skip a key whose value is None rather than resolving it. Add canaries for both shapes, since a check that reports two false FAILs on a clean corpus trains its reader to ignore the family. // 2026-08-13 resolved (deep-fix-round): shared _normalize_reference_ids() helper (None -> [], list -> per-element) now feeds both reciprocity checks; premise reproduced on dec-186 (bare-key None) and dec-225 (list-form) before fixing; real corpus 2 FAIL -> 2 PASS (334 ADRs); 4 canaries added; eval suite 119 green. | 1976327d12de |
| td-141 | important | other | code-to-goals | hooks/capture_memory.py, hooks/inject_decisions.py, hooks/inject_worktree_banner.py | code-quality |  | orchestrator | 2026-08-07 | 2026-08-13 | implementer | resolved | deep-fix-round | [orchestrator, important-findings-remediation 2026-08-07] main() is not total against well-formed non-object JSON. A payload of [], null, "str" or 123 parses successfully and then reaches payload.get(...), raising AttributeError. Each module guards json.loads with except (json.JSONDecodeError, TypeError), which catches malformed input but not well-formed-non-dict. Nothing raises into the harness TODAY because every hook wraps main() at the __main__ boundary -- but the fail-open property lives in the wrapper, not the function, so any in-process caller inherits an unguarded raise. Found by the test-engineer covering these modules; deliberately not enshrined in a test asserting the current behaviour. // 2026-08-13 resolved (deep-fix-round): isinstance-dict guards now inside main() in all three hooks (fail-open in-function, matching each file's style); parametrized [] / null / str / int tests driven through main() in-process; 172 hook tests green. | af9c91b62b86 |
| td-143 | suggested | drift | code-to-goals | .ai-state/TEST_TOPOLOGY.md | architecture |  | orchestrator | 2026-08-07 | 2026-08-13 | test-engineer | resolved | deep-fix-round | [orchestrator, important-findings-remediation 2026-08-07] No gate validates that TEST_TOPOLOGY selector args resolve. Hit live this session: splitting tests/test_ci_autofix_hub_invariants.py (td-138) left the ci-workflows group's pytest-globs selector naming a deleted path, so that group's invocation would exit 4 -- and nothing in the suite checks selectors, so the group stays green by not running. A test group that silently stops selecting anything is indistinguishable from one that passes. The instance was repaired by hand (five new module paths substituted, group verified at 335 passed); the missing gate is what this row tracks. Remedy: a check over every literal (non-glob) selector arg asserting the path exists, run in the same suite that guards the topology schema. // 2026-08-13 resolved (deep-fix-round): selector-resolution guard added to scripts/test_resolve_test_scope.py, including a non-empty-match assertion for globs (ablation-verified load-bearing); fixture canary deleted_selector_path_topology.md proves the gate fires; live topology verified clean; 95/95 green. | 892d8e5bd58d |
| td-145 | important | drift | goals-to-code | claude/aac-templates/architecture.yml.tmpl, claude/aac-templates/precommit-block-d.sh.frag | architecture |  | orchestrator | 2026-08-07 | 2026-08-13 | cicd-engineer | resolved | deep-fix-round | [orchestrator, plugin-namespace-rename 2026-08-07] The plugin rename to `praxion` cannot reach files already instantiated into managed projects, and both affected shipped templates FAIL OPEN. (1) `.github/workflows/architecture.yml` carries `Load the <old-ns>:architect-validator agent` inside a free-text claude-code-action prompt -- nothing rejects an unresolvable agent name, so the job reports GREEN while the architecture sweep never runs. (2) The Block D pre-commit fragment carries an explicit "Skip-gracefully guard: ... exit 0 (non-blocking)", so the AaC golden-rule gate silently stops enforcing behind one `info:` line. Verified that NEITHER `/upgrade-project` NOR `/refresh-claude-blocks` re-renders either file -- refresh-claude-blocks only touches canonical blocks, which carry zero occurrences -- so no shipped refresh path repairs this. The marketplace `renames` map migrates a user's enabledPlugins key; it does not rewrite another repo's committed files. In-repo canaries (tests/test_plugin_namespace_invariants.py) cannot see downstream instances by construction. Remedy: extend `/upgrade-project` to detect and re-render both instantiated files, or ship a one-shot migration check; until then the release note must carry a manual step. Surfaced by the systems-architect while writing the rename ADR, not by any gate -- itself evidence that no gate covers this seam. // 2026-08-13 resolved (deep-fix-round): /upgrade-project gained a detect-and-reconcile surface: token-level namespace repair against the LIVE template (regex anchors, no hardcoded namespace literal -- survives future renames; preserves project-specific substitutions and hand edits), idempotency proven functionally (check -> dry-run -> apply -> apply -> check); canary pinning both target paths and both anchors added to tests/test_plugin_namespace_invariants.py. | ece267d1d785 |
| td-138 | important | complexity | code-to-goals | tests/test_ci_autofix_hub_invariants.py | code-quality |  | sentinel | 2026-08-07 | 2026-08-07 | implementation-planner | resolved | important-findings-remediation | [sentinel, sentinel-full-sweep Lane D] TD01 hotspot: rank 7 by hotspot_score (18,198 = churn 2,022 x complexity 9) in METRICS_REPORT_2026-08-07_07-41-41. The file is 1,712 lines -- 2.1x the 800-line hard ceiling in `rules/swe/coding-style.md` -- and is confirmed unremediated on disk (last touched 03405da 2026-08-02, a repo-wide ruff pass, so the size is current and not mid-refactor). NO existing row covers it: `grep -c test_ci_autofix_hub_invariants` returns 0 in BOTH TECH_DEBT_LEDGER.md and TECH_DEBT_RESOLVED.md. Direct precedent is td-105, which filed `fitness/tests/test_discipline_registry_invariants.py` on identical grounds (a test module far past the ceiling, filed rather than absorbed) -- so "it is a test file" is not a disposition this ledger has accepted. Complexity is low (9), which is why this is a size/decomposition row and not a complexity-reduction row: the remedy is splitting by invariant family (autofix routing, PM detection, green-decline, bot-actor guard) into sibling modules, not simplifying logic. Owner-role implementation-planner per the `complexity` + module-restructuring-required override in the canonical owner-role table, matching td-012/td-058/td-105. // 2026-08-07 closure: split into tests/test_ci_autofix_hub_contract.py (531), _surfaces.py (396), _finalize.py (275), _js_runner.py (248), _pm_install.py (552) -- all under the 800-line ceiling; original file deleted. Behaviour-preserving: 87 tests before and after, --collect-only node-id sets diff-clean, AST compare shows 83 test functions + 20 helpers with byte-identical bodies. The ledger's `location` cell deliberately retains the historical path -- it records where the debt was, and recomputing it would change the structural dedup_key of a resolved row. | cb4ffb4d3e92 |
| td-139 | important | coverage-gap | code-to-goals | hooks/auto_complete_install.py, hooks/inject_worktree_banner.py, hooks/surface_praxion_feedback.py, hooks/capture_memory.py, hooks/inject_decisions.py | code-quality |  | sentinel | 2026-08-07 | 2026-08-07 | test-engineer | resolved | important-findings-remediation | [sentinel, sentinel-full-sweep Lane D] TD03: five hook modules sit below the 70% project coverage floor in METRICS_REPORT_2026-08-07_07-41-41 -- auto_complete_install.py 38.07% (75/197, the LOWEST-covered file in the repository), inject_worktree_banner.py 44.44% (28/63), surface_praxion_feedback.py 48.39% (30/62), capture_memory.py 56.80% (71/125), inject_decisions.py 63.52% (101/159). Read from `coverage.per_file` in the JSON sibling, NOT the markdown list: that list shows 10 of 110 and states its bound as "all at or above 65.66%", which is BELOW the 70% floor, so it is not exhaustive for this floor -- the JSON yields 14 sub-floor modules, 4 more than the list could show. The aggregate is 84.47%, comfortably clear of the floor, which is exactly how these stayed invisible. CAUSE, and the reason this is a first disposition rather than a backlog item: `hooks` joined `testpaths` on 2026-08-06 (pyproject.toml:86-101) -- before that the canonical coverage measurement was blind to all 41 hook modules, so these percentages had never been measured, let alone deferred. capture_memory.py is compound: it is ALSO top-5 by cognitive complexity repo-wide (p95 29.2, complexipy) -- complex code with weak tests -- and was deliberately not double-filed under TD04. Filed as ONE carrier row rather than five near-identical rows, following td-135's own precedent for a cohort sharing one cause, one remedy direction and one owner; split into five if the cohort is worked incrementally. Deliberately EXCLUDED from this row: scripts/check_agent_shared_blocks.py (65.69%), which matches td-135's DEFERRAL REGISTER profile and is recommended for that register instead; and the 8 modules already tracked or deferred by td-135/td-133, whose two-clause re-evaluation trigger was checked and has NOT fired. | c89d970d624b |
| td-132 | important | coverage-gap | code-to-goals | .github/workflows/test.yml, hooks/ | code-quality |  | orchestrator | 2026-08-06 | 2026-08-06 | test-engineer | resolved | orchestrator (sentinel-important-fixes) | [surfaced by closing td-123; the concession is tracked here rather than absorbed silently] Bringing hooks into the canonical coverage measurement widened the measured set by 1367 statements of never-measured code at 53.69%, moving the aggregate below the 80% floor, so the floor was re-derived to 77. Nothing regressed -- the previously measured slice is unchanged at 85.52% over the same 9739 statements, confirmed by independent recomputation from coverage.xml -- and the floor's own comment derives it as just under the observed baseline, so re-deriving from the new baseline applies that rule to a new number. **But it is still a reduction**, and it loosens the bar for the code that was already covered: a regression of the prior slice from 85.5% to 78% would now pass. Two obligations. First, eight genuinely under-tested hook modules are now visible and unaddressed -- worktree_guard.py at 21%, inject_subagent_context.py 25%, send_event.py 33%, auto_complete_install.py 38%, plus four more; these are real gates whose own failure modes are untested. Second, RE-RAISE TRIGGER: when hooks coverage reaches roughly 70%, the aggregate clears 80 again and the floor must go back. Closing this row means both -- writing the hook tests and restoring the floor -- not one. // RESOLVED 2026-08-06: both obligations met. Hook suites written -- worktree_guard.py 21.28% -> 98.9%, inject_subagent_context.py 24.55% -> 100%, send_event.py 33.04% -> 96.3%, capture_session.py 98.5% (602 hook tests pass). Aggregate floor ratcheted 77 -> 80 in .github/workflows/test.yml, exercising the RATCHET note the concession itself carried; measured full-suite baseline 82.08% over 11,677 statements (2,878 passed), so 80 is the same "couple of points under baseline" rule applied to the new number. Residual named, not hidden: auto_complete_install.py remains 38.1% and is why the floor went to 80 rather than 82. | 4180ebe02027 |
| td-134 | important | duplication | code-to-goals | scripts/finalize_adrs.py, scripts/check_squash_safety.py, scripts/check_calibration_coverage.py, scripts/check_release_staleness.py, scripts/check_adr_frontmatter_promotion.py, scripts/check_aac_golden_rule.py | code-quality |  | sentinel | 2026-08-06 | 2026-08-06 | implementer | resolved | orchestrator (sentinel-important-fixes) | [sentinel, sentinel-fanout-audit] CH01 duplication scan. Twelve production `_git`/`_run_git` helpers are independently defined across `scripts/` and `hooks/`; no two share a contract. Some take `repo_root: Path`, some close over a module-level constant; some catch `(OSError, subprocess.SubprocessError)`, others only `FileNotFoundError`. AST-verified split: 6 pass `timeout=`, 6 do not -- and the split is populational, not random: all four `hooks/` helpers (`inject_subagent_context`, `inject_worktree_banner`, `remind_calibration`, `worktree_guard`) carry timeouts, while every timeout-less definition is in `scripts/`. Hooks run in a blocking harness where a hang is visible and acquired timeouts; advisory scripts did not. `scripts/finalize_adrs.py::_git` (line 136) is the dangerous crossover -- it follows the `scripts/` convention (no timeout) but executes at least six times on the BLOCKING post-merge git-hook path (`config --get user.email`, `rev-parse --abbrev-ref HEAD`, `rev-parse HEAD@{1}`, `rev-list --parents`, `merge-base`), so a hung or credential-prompting `git` blocks ADR finalize with no bound -- while `scripts/adr_health.py::_git`, same directory and same name, does pass a timeout. Body-hash duplication detection is structurally blind to this: the helpers are not copies, they are a behavioural fork. The tree already establishes the shared-primitive convention this violates -- `scripts/_repo_root.py` and `scripts/_script_cli.py` are imported as siblings by exactly these modules. Fix: add a `_git` primitive beside them with a mandatory timeout and one error contract; migrate the call sites, `finalize_adrs.py` first. Same class and fix-direction as td-056, different subject (td-056 is one byte-identical pair whose risk is future divergence; here the divergence has already occurred and reaches the post-merge hook). // RESOLVED 2026-08-06: extracted scripts/_git_runner.py::run_git with a keyword-only, undisableable timeout (None or non-positive raises ValueError) plus a canary. All six modules migrated, finalize_adrs.py first. AST re-verified: no timeout-less _git remains anywhere under scripts/. All six still run under ambient python3 (3.14.0a2), so no GL05 ambient-import regression; check_gate_liveness.py still 0 across all four classes; 1,591 script tests pass. hooks/ helpers deliberately untouched -- all four already carried timeouts. | 1b6de73a2b88 |
| td-079 | important | drift | code-to-goals | .ai-state/CONSULT_LEDGER.md, .ai-state/CONSULT_COSTS.md | code-quality |  | orchestrator | 2026-07-30 | 2026-08-06 | context-engineer | resolved | orchestrator (bc82c81) | [found by triggering it] Both CONSULT_LEDGER.md and CONSULT_COSTS.md instructed their single writer to append new rows at the end of the file/ledger, which the orchestrator followed literally and landed a row after the trailing prose sections (Column Definitions / Falsifier / Single Writer), outside the parsed data table -- invisible to every counting recipe and to the cost-coverage gate while still rendering as a plausible stray table to a human reader. Verified by direct comparison: an EOF-appended row produced zero gate failures; the identical row inserted inside the table produced the expected failure. Both instructions were reworded this pass to name the table and the section that follows it. `fitness/tests/test_discipline_registry_invariants.py` gained `check_no_data_row_outside_table` plus a canary per file and a real-file test per file, closing the gap -- the real-file check was observed RED before the fix (a stray row temporarily appended at EOF) and green after restoration. dedup_key formula note: confirmed it reproduces td-078's and td-077's own keys exactly (class, sorted comma-joined location with no space separator, direction, goal-ref-type, goal-ref-value, sha1 truncated to 12); per td-077 it does NOT reproduce the earliest rows (td-071, td-072), and nothing in `finalize_tech_debt_ledger.py` validates the derivation, so this confirmation applies only to rows written under the documented formula from td-073 onward. RESOLVED 2026-08-06. The class is now parsed rather than warned about: check_state_ledgers.py's `stray-row` check fails on a row appended after a ledger's trailing prose sections, and `unterminated-row` catches the adjacent shape nobody had named -- a line opening with a table delimiter and not closing with one, which every grep recipe AND finalize_tech_debt_ledger.py skip in silence. Demonstrated caught on copies of both CONSULT_LEDGER.md and CONSULT_COSTS.md, and through the pre-commit harness. The detector handles the structural asymmetry that made this hard: tech-debt tables run to end-of-file while CONSULT tables are followed by prose, so row identity is a per-ledger first-cell signature rather than a position. Both live CONSULT files were already clean; the gate is prospective for them and retrospective for the ledger pair. Residual, filed as a recommendation not implemented: /onboard-project scaffolds the three CONSULT skeletons into managed projects with a warning sentence and no gate, so the fleet half wants the CONSULT-scoped check installed into the managed pre-commit hook -- an onboarding-contract change needing its own ADR. | 0a0567d66a83 |
| td-085 | suggested | drift | code-to-goals | .ai-state/TECH_DEBT_LEDGER.md, .ai-state/TECH_DEBT_RESOLVED.md | code-quality |  | verifier | 2026-07-31 | 2026-08-06 | context-engineer | resolved | orchestrator (scripts/check_state_ledgers.py + one-time --backfill) | [verifier, multidisciplinary-identities Waves 1-2 + td-081 verification] Re-derivation of td-077's open question, now resolved in the formula's favor: the documented dedup_key formula (sha1 over class/location/direction/goal-ref-type/goal-ref-value, sorted comma-joined paths with no space separator, truncated to 12) reproduces only 43 of 84 rows across both ledger files (51.2% conforming). 56 formula variants were brute-forced against the full row set; the documented one scores highest of all 56 -- so the 41 non-conforming rows are bad DATA (rows written before the formula was codified at td-073, or drifted since), not a wrong formula. Nothing validates the derivation at write time or on migration, so the gap keeps growing as new rows land under the (correct) formula while old non-conforming rows are never backfilled. Resolution: a validator in finalize_tech_debt_ledger.py that recomputes and flags mismatched dedup_key values on every run, plus a one-time backfill pass for the 41 non-conforming rows. Owner-role context-engineer, matching the established local pattern for this ledger-mechanism-integrity finding family (td-077/078/079/083 all use this owner for defects in the ledger's own bookkeeping machinery, distinct from the general drift default of doc-engineer). // 2026-08-06 resolved: `scripts/check_state_ledgers.py` recomputes and validates `dedup_key` (format, value, and uniqueness across the pair) on every commit that stages a ledger and on every CI run, and its `--backfill` mode repaired 44 rows in one pass. Two departures from the resolution proposed above, both deliberate. The validator is a standalone `check_*` script rather than a mode of `finalize_tech_debt_ledger.py`: finalize runs only post-merge, which is far too late to stop a bad key from being written, and the `check_*` prefix is what places the gate inside `check_gate_liveness.py`'s uninvoked-gate detector and the fitness canary-coverage meta-test. And the backfill is 44 of 49, not 49 of 49 -- five rows are unrepairable by construction and are tracked as td-133. Conformance measured at this commit: 83/132 before, 127/132 after. | a249956d9903 |
| td-097 | important | other | code-to-goals | fitness/tests/test_discipline_registry_invariants.py | code-quality |  | verifier | 2026-07-31 | 2026-08-06 | test-engineer | resolved | orchestrator (8017a01) | [verifier, multidisciplinary-identities Waves 1-2 + td-081 verification] The real-file tests in `fitness/tests/test_discipline_registry_invariants.py` iterate over multiple (task-slug, discipline, stage) triples inside a single test function and call `pytest.skip` from inside that loop when one triple's precondition isn't met (e.g. an unreachable seal-witness commit on a shallow clone). `pytest.skip` raises immediately and unwinds the whole test function, so ONE skippable triple silences every OTHER triple's checks in the same run -- live on the second consult (td-081-sealed-prior), where a skip firing partway through the loop meant later triples' checks never executed. This is the coverage-loss sibling of the shallow-clone finding filed against the same real-file arms: not just 'this precondition-dependent arm cannot run in CI,' but 'when it cannot run, checks for triples that COULD have run silently do not either.' Fix: catch the skip condition per-triple and continue the loop (accumulating skipped triples for one summary skip/xfail at the end), or parametrize the test over triples so pytest treats each as an independent case with its own pass/fail/skip outcome. Class `other` rather than `coverage-gap` to avoid an artificial dedup collision with the sibling five-gates-no-check-function row filed against the same file; owner-role test-engineer (override) since this is a test-structure defect, squarely testing-infrastructure work. RESOLVED 2026-08-06 by parametrizing per triple. Reproduced two ways before fixing: a tampered second triple went unchecked when the first triple's fragment was absent (whole test reported SKIPPED), and the identical tamper FAILED once the first was supplied; the same shape was proven for the witness-commit test in a synthetic git repo. An AST scan found exactly three in-loop skip calls repo-wide, all in the two named functions. Parametrize was chosen over collect-and-report (a green pass still hides which triples ran) and over `continue` (which converts a loud skip into a silent pass -- the same defect recostumed); the tiebreak is that this repo's own testing conventions already mandate parametrize over assert-in-loop. Two silent `continue`s became named per-triple skips. Suite skip count rises by one, which is an information gain: one skip line previously named one fragment while silently covering both triples. | e35a15331abe |
| td-109 | important | other | code-to-goals | scripts/test_reconcile_ai_state.py | code-quality |  | orchestrator | 2026-07-31 | 2026-08-06 | test-engineer | resolved | orchestrator (7c34243) | [surfaced by the gate-integrity-and-overlay Step-12 neutering sweep, which ran the full suite ~26 times and so hit a defect a single run hides] `TestMain::test_main_post_merge_skips_observations` and `TestMain::test_main_processes_clean_observations_file` regenerate the **live repository's** `.ai-state/decisions/DECISIONS_INDEX.md` rather than a `tmp_path` copy. Consequence, observed live: while a stray ADR-shaped file existed under `.ai-state/decisions/` (a throwaway `999-my-repro.md` left by an earlier step), the suite indexed it and appended a bogus `dec-999` row to a COMMITTED file -- and did so again on every subsequent full-suite run. Confirmed quiescent after the stray was deleted (36 passed, `git status .ai-state/decisions/` clean of index damage), which is exactly what makes it dangerous: the defect is invisible whenever the decisions directory happens to be tidy, and arms itself silently the moment any ADR-shaped scratch file appears. Violates three clauses of `rules/swe/testing-conventions.md` at once -- Isolation (no state left for other tests), No Hardcoded Paths (use the framework temp dir), and Cleanup (no trace after the test). PRE-EXISTING and NOT introduced by this task; filed rather than fixed to stay surgical. Fix: point both tests at a `tmp_path` repo fixture, as the sibling tests in the same file already do. Owner-role test-engineer (override on the `other` default) since this is squarely test-infrastructure work, matching the td-097 precedent for a test-structure defect. RESOLVED 2026-08-06, and a second lie in the same file was found and fixed alongside. Root cause: main() calls apply_repo_root(), which OVERWRITES the monkeypatched path constants, so patching them was never going to hold -- the file's other main()-calling tests patch around it and these two did not. Reproduced: with a throwaway ADR-shaped file planted, the two tests passed while appending a bogus row to the committed index, and again on every run. The second lie: one test asserted against the live observations.jsonl while its temp fixture sat inert. Fixed by passing --repo-root so main()'s own resolution lands in a tmp tree -- hermetic by construction rather than by stubbing the resolver, which the existing precedent does while leaving REPO_ROOT live as a latent trap. An autouse fixture now fails any test in the module that mutates the live decisions tree; it fingerprints mtime as well as content, because the rewrite that hid this was byte-identical. Independently verified: live index sha unchanged across three consecutive runs with a throwaway planted. | 25ea3b771df6 |
| td-129 | suggested | coverage-gap | code-to-goals | fitness/tests/test_gate_canary_coverage.py, scripts/sync_canonical_blocks.py | code-quality |  | orchestrator | 2026-08-06 | 2026-08-06 | test-engineer | resolved | orchestrator (72d2fd8) | [surfaced while adding the template-mirror gate] The canary-coverage meta-test and the uninvoked-gate detector both identify a gate by a `check_*` / `validate_*` filename. `sync_canonical_blocks.py` is a real pre-commit gate whose `--check` mode blocks a commit, but its `sync_*` name places it outside both, so neither its invocation nor its canary coverage is ever asserted. A naming convention became a scope boundary by accident, and nothing announces the omission. The new mirror gate was named `check_*` deliberately to stay inside both, which is what surfaced this. Two fixes: rename (touching every invocation site and the hook config), or identify gates by invocation shape rather than filename -- the second also serves the widening tracked as the sibling row filed alongside this one. RESOLVED 2026-08-06 by widening gate identity rather than renaming. Discovery is now a union of three routes -- filename shape, sentinel dispatch, and any Python script a pre-commit entry runs -- so sync_canonical_blocks.py and regenerate_rules_manifest.py are both inside the boundary; both already satisfied canary coverage and nothing had asserted it. Renaming was rejected: it touches every invocation site and moves the boundary rather than removing the accident. Verified after: the meta-test's gates_without_canary and checks_without_canary both return empty. Residual named in code rather than left silent: diagram-regen-hook.sh stays out because its canary lives in shell functions the def test_* contract cannot grade. | 4a6e5a325a33 |
| td-131 | important | coverage-gap | goals-to-code | rules/swe/gate-liveness.md, scripts/check_gate_liveness.py | code-quality |  | orchestrator | 2026-08-06 | 2026-08-06 | test-engineer | resolved | orchestrator (72d2fd8, b6a2ea8) | [surfaced while fixing td-095/td-096] The gate-liveness rule states seven clauses; the detector mechanises three. The session that produced this pass found four separate instances of one shape -- a correct signal that never reaches a decision: a gate whose input was wrong, a gate nothing invoked, a gate whose verdict was discarded by an exit code, and a gate whose output was overwritten before anyone read it. The detector caught none of them, because the uninvoked-gate check keys on `check_*` / `validate_*` FILENAMES and the ambient-import check excludes hooks wholesale. Proposal, not yet implemented and wanting an ADR: a fourth check, `discarded-verdict`, asking the missing question in the existence-is-not-operation family -- the first two ask whether a gate is called and whether it can load; this asks whether its verdict is read. Three mechanically decidable shapes: an invocation whose exit code is swallowed (a shell or-true suffix, `subprocess.run` without `check=`, a hook body whose last statement is not the gate); a gate writing to a path another gate later overwrites unconditionally in the same chain; a gate whose declared consumer names a different module than the one it writes. Plus a scope change: identify gates by invocation shape rather than filename prefix, and bring hooks into the gate inventory while keeping them out of the ambient-import scope -- two separate scopes collapsed into one exclusion today. The first shape alone would have caught one of the four. RESOLVED 2026-08-06 at the scope the evidence supported, which is narrower than the row proposed and deliberately so. Six candidate formulations were measured against this repo BEFORE any were written; five were dropped rather than tuned -- two fired twice each with zero true positives, one was unmeasurable, two had no grounded instance and a wide future false-positive surface. The kept check is scoped to hook registrations, where a findings exit of 1 cannot block because only exit 2 does, and it is false-positive-free by construction: a hook with no exit-1 path has no verdict to discard, so advisory reminders are silent for what they are rather than by exemption. The hooks-scope premise held with a correction -- ambient-import excludes hooks as call sites, correctly, while uninvoked-gate simply never had them in its candidate set; two distinct scopes that coincided rather than one collapsed exclusion. Fixing that exposed a latent bug where the own-test exclusion computed a .sh test name for shell gates, excusing them via their own .py test. The new check has a named consumer of its own and a catalog row, so it is not a seventh instance of the finding it reports. Recorded as dec-327. | bee613ec35d3 |
| td-107 | suggested | other | goals-to-code | .github/workflows/test.yml | code-quality |  | orchestrator | 2026-07-31 | 2026-08-06 | systems-architect | wontfix | orchestrator (premise disproved, 0ba61d4) | [referred to the orchestrator by the gate-integrity-and-overlay architect; flagged deliberately rather than fixed, to stay surgical] `.github/workflows/test.yml:208`'s `calibration-coverage` advisory job describes itself in its own comment as mirroring the release-staleness advisory pattern -- push-only, `continue-on-error: true`, never blocking. If that self-description is accurate it carries the identical `rules/swe/gate-liveness.md` clause-6 defect being closed for `check_release_staleness.py` under td-073: a correctly-computed, deliberately-advisory value with no document, check, or decision point named as its required reader. NOT verified to the same depth as td-073's instance -- the mirror claim comes from the workflow comment, and whether a consumer exists elsewhere was not established. Verify before acting; if confirmed, the remedy is the same shape as td-073's (name the reader and the decision point, do not make the advisory blocking). Owner-role systems-architect, matching td-073's precedent for this named-consumer finding family. WONTFIX 2026-08-06 -- premise disproved, verified independently. The row inferred an absent-reader defect from the job's own comment claiming it 'mirrors the release-staleness advisory pattern'. The inference was wrong: calibration-coverage has TWO named readers and always did. The sentinel's calibration-coverage check runs the same script with --json and flags an Important finding when coverage is false, and hooks/remind_calibration.py imports compute_coverage in-process and is registered in hooks.json through the commit gate. Both confirmed by direct inspection. The two jobs share a SHAPE, not a defect, and the comment sentence is what generated the finding -- it has been rewritten to name both readers and record why the inference failed. Residual, not worth a row: scripts/check_calibration_coverage.py lacks the Named consumer docstring block its sibling carries. Documentation parity, not a liveness gap. | b68d53913ca1 |
| td-108 | important | other | code-to-goals | hooks/commit_gate.sh, scripts/check_id_citation_discipline.py | code-quality |  | orchestrator | 2026-07-31 | 2026-08-06 | systems-architect | resolved | orchestrator (04a339d, 0ba61d4) | [measured by the gate-integrity-and-overlay architect at 832c47e; independently re-confirmed by the orchestrator] `scripts/check_id_citation_discipline.py`'s full-repo scan is RED at 65 violations (28 `req-id`, 20 `step-ref`, 17 `ac-id`) across 22 files, while its commit-time invocation is GREEN -- because `hooks/commit_gate.sh` passes `--files` over staged paths only. The gate's computed scope at the enforcing seam is therefore materially narrower than the repo-wide scope its own rule (`rules/swe/id-citation-discipline.md`) documents it as policing, which is exactly the `gate-liveness.md` clause-5 scope-fidelity defect -- on the inbound citation gate itself. Consequence: the repo can accumulate citation debt indefinitely as long as no single commit stages a violating file, and a reader of the rule would reasonably believe the tree is clean. This is why the gate-integrity-and-overlay task scoped its own acceptance criterion to zero `[draft-adr-id]` findings rather than a clean whole-repo exit -- demanding the latter would have silently imported a full decontamination pass. Resolution needs a decision, not a tweak: either accept staged-only enforcement and correct the rule's claim, or add a periodic/CI full-repo run with an agreed remediation path for the 65. Owner-role systems-architect, matching td-101's precedent that this gate's detection strategy is decided once, not improvised per invocation. UPDATE 2026-08-06: the 65-violation BACKLOG is cleared -- the full-repo scan now exits 0 over 812 files, by real remediation (each site rewritten to describe the behaviour) rather than by annotation; ten sites keep a per-site marker where the literal is the input grammar of the code under test, and no path exemption was added. **This row stays OPEN**, at the narrowed scope of its actual subject: the scope-fidelity defect is untouched. The commit-time seam still passes `--files` over staged paths only, so the tree can drift red again exactly as before and a reader of the rule would still believe otherwise. What changed is the cost of the decision: with the tree green, the second of the two resolutions this row names -- a repo-wide CI invocation -- is now a non-disruptive addition rather than one that would import a full decontamination pass, which is why the gate-integrity task scoped around it. Recommended disposition: add the repo-wide run to CI and keep the staged-only hook as the fast local seam; failing that, correct the rule's claim to say staged-only. Owner-role unchanged. RESOLVED 2026-08-06 at its actual subject, having been deliberately kept open earlier the same day when only the backlog was cleared. The scope-fidelity defect is now closed: a blocking repo-wide CI job runs the checker in its default mode, so the enforcing seam's computed scope matches the scope the rule documents. Blocking rather than advisory because the tree is clean (816 files, exit 0) so it costs nothing today, and the objection that a legitimate fixture literal would be stopped is answered by the per-site ignore marker -- having to write one deliberately is the mechanism, not a workaround. Advisory would have reproduced the defect being closed. The rule's claim now matches reality with no rule edit. | e7cb4f2520fd |
| td-123 | important | coverage-gap | code-to-goals | .github/workflows/test.yml, pyproject.toml | code-quality |  | orchestrator | 2026-08-06 | 2026-08-06 | cicd-engineer | resolved | orchestrator (0ba61d4) | [orchestrator, sentinel-remediation] The canonical coverage measurement excludes every hook module, so TD03 has a live false-positive path into this ledger -- td-122 is that false positive, realized and now tombstoned. `pyproject.toml` sets `testpaths = ["tests", "scripts"]`, and `.github/workflows/test.yml` runs `pytest hooks/ -q --no-cov` as a separate job, so all 41 hooks under `hooks/` are measured only by whatever `tests/`+`scripts/` incidentally import. Evidence 2026-08-06: `_hook_utils.py` reports 0.429 but is 1.000 under its own suite; `inject_process_framing.py` reports 0.658 vs 0.848. Both hook files appearing in `coverage.data.per_file` are understated, and the other 39 are absent from it entirely. Consequence compounds with td-120 (TD03 reads the MD aggregate, which renders no per-module data): TD03 is specified to file a row per module under a 70% floor and its input is wrong in two independent ways. Fix direction: drop `--no-cov` from the hooks job and merge its data (coverage `combine`), or add `hooks` to `testpaths` -- the second changes default local runs, so the first is likely correct. Do NOT raise the floor or add tests to satisfy the number; the modules are already tested. Proposed owner cicd-engineer (the defect is in test/CI wiring, not in any hook). RESOLVED 2026-08-06 by adding hooks to testpaths and retiring the separate no-coverage step. Fixing CI alone would not have worked: the coverage artifact the metrics report reads is produced by the target discovered from pyproject, which usually runs on a developer's machine, so the measurement has to be correct in the default invocation or it is not correct where it is read. The two false positives are gone, confirmed: _hook_utils.py 42.9 to 100%, inject_process_framing.py 65.8 to 89.9%. Eight genuinely under-tested hooks now enter the sub-floor list as true findings -- filed separately, along with the floor concession this required. | 91b9878bd824 |
| td-125 | important | coverage-gap | code-to-goals | .github/workflows/test.yml, tests/test_diagram_regen_hook.sh, tests/test_install_filter.sh | code-quality |  | orchestrator | 2026-08-06 | 2026-08-06 | cicd-engineer | resolved | orchestrator (194a089, 0ba61d4) | [orchestrator, Stay-Surgical redirect while fixing td-124] Praxion's entire bash test tier is unrun: `tests/*.sh` holds four suites and NO runner references any of them -- grep for `tests/.*\.sh` across `.github/workflows/`, `pyproject.toml` and `.pre-commit-config.yaml` returns zero. `pyproject.toml` `testpaths = ["tests", "scripts"]` collects only pytest files, so the `.sh` suites are invisible to it too. Measured 2026-08-06: 2 of the 4 are RED right now -- `tests/test_install_filter.sh` and `tests/test_diagram_regen_hook.sh` -- and have been failing with nothing reporting it. This is gate-liveness clause 7 (existence is not operation) at the tier level, and it is the direct cause of td-124: `tests/new_project_test.sh` T7 asserts the canonical .gitignore block byte-for-byte and was correctly RED on the three-way drift, but no runner ever executed it, so the drift shipped. The gate worked; nothing listened. Note GL04 (`uninvoked-gate`) cannot catch this -- its scope is `check_*` / `validate_*` scripts, not `tests/*.sh`. Fix direction: add a shell-tests job to `test.yml` (the two green suites can gate immediately), diagnose the two red ones, and consider widening GL04's scope so an unrun test tier is mechanically detectable. Deliberately NOT wired during the td-124 fix: turning on a job with two known-red suites would have shipped a red CI, and diagnosing them is unrelated to the .gitignore block. Proposed owner cicd-engineer. // 2026-08-06 PARTIALLY CLOSED, scope narrowed to the two red suites. A `Shell test suites` job now runs `tests/new_project_test.sh` and `tests/test_finalize_chain.sh` in `.github/workflows/test.yml`, so the tier is no longer wholly invisible and new drift in the canonical `.gitignore` block is gated. The suites are ENUMERATED, not globbed, deliberately: a glob would either ship a red CI or invite someone to delete the job, whereas an explicit list keeps the remaining gap visible. Add each suite back as it goes green. Independently reproduced this run by the fan-out's lens 5. Remaining, each its own unrelated investigation: (1) `tests/test_install_filter.sh` -- 19 pass / 4 fail, all in `test_sweep_stale_rule_symlinks_prunes_stale_and_preserves_live`: `sweep_stale_rule_symlinks` removes neither the stale rule symlinks (`swe/memory-protocol.md`, `swe/obsolete.md`, `swe/vcs/git-conventions.md`) nor the emptied `swe/vcs` subdirectory, so stale rule links survive an upgrade in a real install. (2) `tests/test_diagram_regen_hook.sh` -- T5 expects the regen hook to emit at least one `.d2` and one `.svg` and gets zero of each. NOT an environment gap: `d2`, `likec4` and `mmdc` are all on PATH here and the suite already carries explicit skip guards for their absence (T2/T3), so the hook genuinely produced nothing. Both predate this audit and are unrelated to it and to each other. RESOLVED 2026-08-06. Both red suites fixed at the defect, not the assertion, then all four gated. test_install_filter: the sweep and its sibling linker both parsed the manifest with Python plus PyYAML, and on a python3 without PyYAML each fell back silently -- one to 'link every rule', the other to 'remove nothing'. The two defaults compose into a ratchet: one created contamination, the other declined to remove it. Replaced with a shared awk reader and an empty-whitelist bail. test_diagram_regen_hook T5: the hook was correct and the TEST was stale -- ef8259d moved renders to a per-diagram rendered/ directory in May and the test was last touched in April, so it had asserted a layout that exists nowhere in the repo for two and a half months. CI now globs the suites rather than enumerating them: the enumeration's premise was the two red suites and that premise is gone, while a list still drops the fifth suite someone adds. The glob asserts a non-zero count so it cannot pass vacuously. | 9d2daf2c0ae9 |
| td-126 | important | other | code-to-goals | scripts/finalize_adrs_backlinks.py | code-quality |  | orchestrator | 2026-08-06 | 2026-08-06 | unassigned | resolved | orchestrator (a2df5a6) | [surfaced during the td-054 split; the code was moved verbatim, so both defects are pre-existing rather than introduced] `_ensure_re_affirmed_by_link` crashes on two shapes a real ADR corpus can hold. A scalar `re_affirmed_by: dec-NNN` where a list is expected leaves the inline-pattern match as None and dereferences it -> AttributeError. An ADR whose frontmatter has no closing `---` raises an uncaught StopIteration from the `next(...)` that computes the end index, and `main()` catches only FileExistsError / ValueError / CalledProcessError / OSError. Both were reproduced directly. Both crash AFTER promotion and cross-reference rewriting have already written to disk, so the run dies before index regeneration and leaves DECISIONS_INDEX.md stale -- silently, because the finalize hook is non-blocking by design. This code is symlinked into every managed project's git-hook chain. Deliberately not fixed inside the pure refactor that found them. Proposed owner-role implementer. RESOLVED 2026-08-06, and the row understated it: a THIRD shape was found while building the guard and is the worst of the three. An ADR with no OPENING delimiter did not crash -- the boundary scan took the first body horizontal rule for the frontmatter terminator and wrote re_affirmed_by into the prose, then returned zero. The two filed crashes were the lucky failure modes; a traceback is a signal and this one succeeded at the wrong thing. Scalar re_affirmed_by is coerced to the list the convention specifies, since adding the new id needs a list either way and skipping would leave the field malformed AND drop the back-link. Malformed frontmatter is skipped untouched, because with no delimiter there is no principled place to write and guessing is exactly what produced the corruption. Both paths log a WARNING naming the file and the shape. All four new tests fail against the prior implementation. | 2f1aacb2636f |
| td-127 | important | coverage-gap | code-to-goals | agents/sentinel.md, scripts/project_metrics/report.py | code-quality |  | orchestrator | 2026-08-06 | 2026-08-06 | test-engineer | resolved | orchestrator (3187029) | [surfaced while fixing td-120] The sentinel's coverage dimension is specified per-module, but the metrics `.md` renders only the aggregate: the deep-dive coverage layout emits line_pct / artifact_format / artifact_path, and there is no coverage entry among the deep-dive summarizers, so the per-file block that IS present in the JSON never reaches the surface the check reads -- grepping the newest report for it returns zero hits. The check therefore cannot do what it says regardless of the render bug now closed; it can only ever evaluate the aggregate against the floor. This is a scope-fidelity defect in the gate-liveness sense: documented scope exceeds computed input. Two honest fixes -- render per-module coverage into the `.md`, or narrow the documented clause to the aggregate. The second is cheaper and stops the check claiming a reach it does not have. Owner-role test-engineer per the coverage-gap default, though the change itself is a renderer edit. RESOLVED 2026-08-06 by rendering per-module coverage rather than narrowing the clause. Narrowing would have kept the false all-clear and merely relabelled it as intended: this repo reads 85.02% aggregate while hiding nine files under the floor, worst at 42.86%, which is precisely the failure an aggregate is structurally blind to. The cap is disclosed twice in the rendered output -- shown-of-total in the header, plus the coverage every omitted file clears -- so on current data the list is provably exhaustive for a 70% floor rather than probably so. The floor value stays the consumer's parameter. Secondary finding recorded: the golden byte fixture needed no change because its coverage collector is not_applicable and short-circuits before any summarizer runs, which is why both this omission and the earlier fraction-vs-percent bug survived a green golden test. | 533f696303f7 |
| td-128 | important | other | goals-to-code | .github/workflows/architecture.yml, agents/architect-validator.md | code-quality |  | orchestrator | 2026-08-06 | 2026-08-06 | unassigned | resolved | orchestrator (dfc9f51, 503ad69) | [surfaced while giving the architecture-validation report a named reader] The pre-merge CI job invokes the validator with a tool allowlist granting no Write, so that mode cannot produce the report its own phase mandates -- a gate whose output cannot exist in the environment that runs it. The agent definition was made honest about this in the same change (in pre-merge mode the findings are structured output; the CI job and the PR are their named readers), but the workflow itself was not touched: changing a CI allowlist is a cicd-engineer call and the choice is genuinely open -- grant Write, or keep the mode write-free and drop the mandate. Proposed owner-role cicd-engineer, which the owner-role enum does not carry. RESOLVED 2026-08-06, and the filed claim was the smaller half. The missing Write grant was real, but nothing consumed the output in ANY form: no step id, no --json-schema, no reader step, pull-requests scoped to read, and a prompt instructing an exit code the agent has no mechanism to set. A clean run reporting FAIL produced a green check, so the job's only signal was whether the agent crashed. Resolved by keeping the mode write-free -- a file on a runner has no task slug, no uploader and no lifetime -- and building the two missing halves: --json-schema as the transport, and a render-and-gate step that publishes to the job summary and exits non-zero on FAIL. Empty output stays advisory but loud; unparseable output blocks, because a gate fails closed on garbage. The canary owed at commit time was paid: it executes the shipped run: block parsed from the workflow and structurally cannot hold a copy of it. Recorded as dec-326. | 2a2016061673 |
| td-054 | important | complexity | code-to-goals | scripts/finalize_adrs.py | code-quality |  | verifier | 2026-06-19 | 2026-08-06 | implementation-planner | resolved | orchestrator (f8e0c8b) | Originally filed as td-039; renumbered to td-054 to resolve id collision, 2026-07-15. [W-01 from finalize-consumer-bugs verification] `scripts/finalize_adrs.py` is 996 lines, over the 800-line hard ceiling in `rules/swe/coding-style.md` (same class as td-012 for report.py). Pre-existing and NOT introduced by the consumer-bug fixes — those added ~140 lines but the file was already ~895 before. The shared repo-root resolver was already extracted to `scripts/_repo_root.py` this pass (retiring 5x duplication, −~45 lines), which did not cross the ceiling. Next cohesive extraction: the fragment-filename parser (`parse_fragment_filename` + `_split_user_branch_slug` + `_discover_branch_from_siblings` + `_parse_via_siblings` + `_collect_peer_*` + `_dash_aligned_common_prefix` + `_read_draft_branch` + `_sanitize`, ~200 lines) into a `finalize_adrs_parsing.py` sibling — would bring the file under ~800. Deferred deliberately: that parser has prior-bug history (td-017 hyphenated-branch ambiguity) and couples to the module `_git`/`REPO_ROOT` globals + frontmatter patterns + logger, so a clean extraction needs care (pass git-runner/patterns as params or move them too) and its own focused pass rather than a tail-of-session rush. Owner-role implementation-planner per the "module restructuring → implementation-planner" heuristic (matches td-012). RESOLVED 2026-08-06: 1229 -> 715 lines by cohesive extraction into three siblings (fragment identity parsing 336, bounded cross-reference propagation 180, re_affirmed_by reciprocity 99), all under the ceiling. The seam test was whether a module answers one question and needs none of the entry module's mutable repo-root state; draft detection, NNN assignment, promotion and locking stay put because they fail it. Behaviour unchanged -- same 58 tests, no pinned signature altered. The consumer-layout fixture's copy list gained all three and caught the first extraction before anything else did; it remains the only site enumerating the shipped file set. Two pre-existing crashes in the backlink writer were found and deliberately not fixed here: a scalar `re_affirmed_by` and a truncated frontmatter each kill the run after promotion has written to disk, leaving the index silently stale. Filed separately. | 68a1ae78beed |
| td-074 | important | drift | code-to-goals | skills/context-security-review/references/permission-baseline.md | code-quality |  | systems-architect | 2026-07-30 | 2026-08-06 | context-engineer | resolved | orchestrator (7f1aebf) | [surfaced by the td-072 architect as an adjacent finding; deliberately not fixed there to stay surgical] The permission baseline tables 12 agents; the fleet is 17. Missing: agentic-transactions-architect, architect-validator, discipline-consultant, interface-designer, roadmap-cartographer. Its summary claims "8 of 12 agents use acceptEdits" and "No web access (11 of 12)" are consequently wrong as stated. Independent of the td-072 strip, which removed an inert field; this is a stale enumeration that was already wrong beforehand. RESOLVED 2026-08-06: all 17 agents tabled, every tool set read from the agent's own frontmatter. Ground truth also refuted three rows that were already present -- `researcher` was missing Edit, `promethean` has WebFetch (falsifying both the sole-web-access claim and the escalation rule keyed to it), and `skill-genesis` carried a phantom AskUserQuestion with its disallowedTools unrecorded. The derived summaries lose their agent name-lists and the web-access summary is deleted outright: those restatements are the mechanism by which five agents went missing and three went stale unseen. A count is one drift surface; a roster restated four times is four. | 6e3a8cd0815c |
| td-075 | minor | other | goals-to-code | commands/onboard-project.md, claude/canonical-blocks | code-quality |  | systems-architect | 2026-07-30 | 2026-08-06 | systems-architect | resolved | orchestrator (3230f8c) | [surfaced by the td-072 architect; explicitly excluded from that decision as a separate blast radius] Praxion's own .claude/settings.json pre-allows Write(.ai-work/**); managed projects receive no permissions.allow from onboarding at all. td-035 (subagent Write denials to .ai-work/) is the standing argument that they should. This is a fleet-wide config decision about what onboarding ships, NOT compensation for the stripped permissionMode -- that ADR ruled explicitly that no compensating grant is warranted, since stripping an inert field removes no capability. RESOLVED 2026-08-06: `/onboard-project` Phase 5 splits into two independently-predicated sub-steps so a project onboarded by an older version gains the allow baseline on re-run rather than being stranded by a phase-level skip. The baseline is one entry, `Write(.ai-work/**)`, merged non-destructively and never clobbering a user's array. The bar applied was a denial both observed and unanswerable by a subagent; only that entry cleared it, and the sub-step records why nothing else does so the list cannot be padded later in good faith. Verified against four settings.json fixtures. | 592c016f280d |
| td-087 | suggested | other | goals-to-code | commands/onboard-project.md, commands/new-project.md, new_project.sh, claude/canonical-blocks/ | architecture | .ai-state/DESIGN.md §3 | verifier | 2026-07-31 | 2026-08-06 | systems-architect | resolved | orchestrator (3230f8c) | **[HALF VOID 2026-07-31 -- READ BEFORE THE ORIGINAL TEXT BELOW.]** This row's proposal to scaffold 'a project-local registry-overlay stub' directly CONTRADICTS dec-305 § Considered Options F, which rejects scaffolding an empty overlay because 'a header-only overlay is a populated-looking absence' and 'absence is the common case and should cost nothing'. dec-305 governs: the overlay-stub half is VOID and must not be built. The row's OTHER half -- the three `.ai-state/CONSULT_*.md` skeletons plus a registry pointer -- is untouched by that objection and remains independently warranted, but is gated on the overlay unit shipping (scaffolding a consumer for a producer that does not exist is the gate-liveness clause-2 defect). ORIGINAL TEXT FOLLOWS: [verifier, multidisciplinary-identities Waves 1-2 + td-081 verification] No onboarding surface scaffolds anything for the consult mechanism: `git grep -c CONSULT` over `commands/onboard-project.md`, `commands/new-project.md`, `new_project.sh`, and `claude/canonical-blocks/` returns zero across all four. A managed project that installs the plugin today receives no `CONSULT_LEDGER.md`/`CONSULT_COSTS.md`/`CONSULT_PRIORS.md` skeleton, no discipline-registry pointer, and no fitness-suite wiring for the mechanism -- the entire multidisciplinary-consult subsystem is Praxion-only in practice, not merely the registry (td-064's narrower finding). Same root as td-064 but broader scope: td-064 is about the registry being unextendable; this is about NOTHING in the consult surface reaching a managed project via onboarding at all. Resolution: extend /onboard-project (or a dedicated opt-in phase) to scaffold the three consult-ledger skeletons plus a project-local registry-overlay stub, once the accepted overlay decision (filed alongside this row) actually ships an implementation. Owner-role systems-architect, matching td-064's precedent for this registry/consult-extensibility finding family. // 2026-07-31 (systems-architect, gate-integrity-and-overlay): RULING on the direct conflict between this row and dec-305 Option F. dec-305 GOVERNS: its Option F rejects scaffolding an empty overlay ("a header-only overlay is a populated-looking absence... absence is the common case and should cost nothing"), on grounds nothing found this pass challenges. The "project-local registry-overlay stub" half of this row is therefore VOID. The SURVIVING half -- the three CONSULT_LEDGER/CONSULT_COSTS/CONSULT_PRIORS skeletons plus a registry pointer -- is independently warranted and is NOT blocked by the td-064/086/088 deferral: the consult mechanism's PRODUCER ships globally (agents/discipline-consultant.md, commands/consult.md, agents/CLAUDE.md all install), so a managed project can convene a consult today and the convener is instructed to append to ledger files that do not exist -- producer-present/consumer-absent, a genuine live gap. Not taken in the gate-integrity-and-overlay pipeline (suggested priority, disjoint, and that pipeline already closes eight rows); re-scope this row to the three skeletons only. // 2026-08-06 RE-SCOPED by ledger triage, per the 2026-07-31 architect ruling this row already records but never applied. The VOID half is struck: dec-305 § Considered Options F governs and rejects scaffolding a project-local registry-overlay stub ('a header-only overlay is a populated-looking absence... absence is the common case and should cost nothing'). Do not build it. The row's REMAINING and sole scope is the three `.ai-state/CONSULT_*.md` skeletons (CONSULT_LEDGER / CONSULT_COSTS / CONSULT_PRIORS) plus a registry pointer in onboarding. That half is independently warranted and is NOT gated on the td-064/086/088 deferral unit: the consult mechanism's PRODUCER ships globally (agents/discipline-consultant.md, commands/consult.md, agents/CLAUDE.md all install), so a managed project can convene a consult today and its convener is instructed to append to ledger files that do not exist -- producer-present / consumer-absent, a live gap. Kept OPEN at that narrowed scope. RESOLVED 2026-08-06 at the narrowed scope this row was kept open for. `/onboard-project` Phase 2 now scaffolds the three `.ai-state/CONSULT_*.md` skeletons header-only, with a per-file predicate that never overwrites. Column shapes are derived from the live writers rather than invented, and each skeleton carries its own Column Definitions section because the convener is instructed to read that section as the schema. `/new-project` and `new_project.sh` deliberately do not mirror the scaffolding: a committed guard pins the single install path through `/onboard-project`, and copying would break it -- the mirror is a pointer, and that guard is now mirrored for CONSULT_ too. The registry-overlay half stays void per the governing ADR. | 9ff37e5b05e8 |
| td-095 | important | coverage-gap | code-to-goals | scripts/check_gate_liveness.py | code-quality |  | verifier | 2026-07-31 | 2026-08-06 | context-engineer | resolved | orchestrator (3761c04) | [verifier, multidisciplinary-identities Waves 1-2 + td-081 verification] GL02 (`scripts/check_gate_liveness.py`'s clause-5 scope-fidelity detector) documents a golden bad-case meant to prove the detector actually fires on the defect class it targets. Fed verbatim to GL02 itself, that golden bad-case returns ZERO findings -- the detector's own liveness proof does not bite. This is the meta-level instance of the entire gate-liveness discipline's own thesis (a check with no proof it fires is worse than no check, because it manufactures false confidence) turned on the tool that enforces that thesis for every other gate in the repo. Fix: trace why the documented golden bad-case doesn't reproduce (possibly the same class of exclusion/pattern-matching gap as the sibling absolute-path-exclusion row filed alongside this one) and add a regression test pinning the golden bad-case to a real, currently-failing assertion. Owner-role context-engineer, matching the td-077/078/079/083 precedent for gate-liveness-mechanism-self-defects. RESOLVED 2026-08-06: exclusions now match `path.relative_to(root).as_posix()` instead of the absolute path. Demonstrated before and after: identical bytes at repo-relative `rules/bad.md` yielded 1 finding under a neutral parent directory and 0 under one named `gate-liveness`; post-fix it is 1 and 1. A canary pins the relocation invariant and fails against the prior implementation. | 5c36129c198e |
| td-096 | important | other | code-to-goals | scripts/check_gate_liveness.py | code-quality |  | verifier | 2026-07-31 | 2026-08-06 | context-engineer | resolved | orchestrator (3761c04, 57b8d54) | [verifier, multidisciplinary-identities Waves 1-2 + td-081 verification] GL02's exclusion list is implemented as an absolute-path substring test, so identical bytes at identical relative positions yield 1 finding or 0 findings purely depending on the checkout's absolute path on disk (e.g. a home-directory checkout vs a worktree path vs a CI runner's checkout root). A detector whose result depends on filesystem location rather than file content cannot be trusted to reproduce between a local run and CI, or between two contributors' machines. Fix: match the exclusion list against paths relative to the repo root (as resolved by git), never an absolute-path substring. Owner-role context-engineer, matching the td-077/078/079/083 precedent for gate-liveness-mechanism-self-defects (same file and finding family as the sibling golden-bad-case row filed alongside this one). RESOLVED 2026-08-06: two independent causes, both closed. The scan-verb pattern matched only bare stems, so every inflection (scans/greps/searches/searching/matched) slipped past; widening it costs zero new findings on this repo, measured. The documented golden bad-case also named `a REQ-id prefix`, which carries no digit and so could never match the forbidden-literal pattern -- and relaxing that pattern to bare `REQ-`/`AC-` was measured at four findings, all false positives, so the pattern is right and the prose was wrong. The catalog cell now states the banned placeholder literal concretely and carries `gate-liveness:ignore`, since the cell is itself an instance of what the check forbids; strip the marker and it fires, which is what makes it a golden bad-case rather than a paraphrase. | 56e256df04d9 |
| td-106 | important | duplication | code-to-goals | claude/aac-templates/fitness-conftest.py.tmpl, fitness/tests/conftest.py | code-quality |  | orchestrator | 2026-07-31 | 2026-08-06 | implementer | resolved | orchestrator (5596d70) | [referred to the orchestrator by the gate-integrity-and-overlay architect] `fitness/tests/conftest.py` and `claude/aac-templates/fitness-conftest.py.tmpl` are byte-identical duplicates with NO sync check, while the structurally analogous `claude/canonical-blocks/` surface has one (`scripts/sync_canonical_blocks.py --check`, wired into pre-commit). This is the LIVE finding sitting underneath td-086's latent one: td-086 asserts the shipped fitness suite mis-resolves `project_root` from the plugin cache, but `/onboard-project` Phase 8b.2 actually COPIES the .tmpl into the managed project's own tree (commands/onboard-project.md:571), where `Path(__file__).parent.parent.parent` is correct -- so td-086's stated consequence has no producing invocation path, and the real exposure is that the template and the dogfooded original can silently diverge with nothing to catch it. A fix to one would not propagate to the other. Resolution: add the .tmpl/original pair to a sync check mirroring the canonical-blocks pattern. Owner-role implementer per the `duplication` default. RESOLVED 2026-08-06: `scripts/check_template_mirrors.py` gates byte-identity between the shipped template and its in-repo source, wired as the `template-mirror-sync` pre-commit hook beside the canonical-block gates. Membership is an explicit registry rather than a directory scan -- the other fitness starters have deliberately diverged here -- and the registry is the datum the hook's `files:` pattern is tested against, so documented scope cannot drift from computed scope. `--write` is one-directional (source to template). Named `check_*` rather than `sync_*` deliberately: the prefix is what places the new gate inside the uninvoked-gate detector and the canary-coverage meta-test. Divergence proven to exit 1 on either half, in sync to exit 0. | 5dcf42b12187 |
| td-120 | important | other | code-to-goals | scripts/project_metrics/report.py | code-quality |  | orchestrator | 2026-08-05 | 2026-08-06 | implementer | resolved | orchestrator (0a8386d) | [surfaced during a `/project-metrics` run; deliberately not fixed there to stay surgical] The MD report understates line coverage by 100x. `report.py:606` renders `f"Line coverage is {_fmt_float_2(coverage)}%."` where `coverage` is the cobertura `line-rate` **fraction**, so a real 85.02% prints as "0.85%". Same class at the trends delta column: `delta_pct` 0.0219 renders "0.02%" for a real +2.19%. Evidence: `coverage.xml` carries `line-rate="0.8502"`; METRICS_REPORT_2026-08-05_22-03-40.md line 25 reads "Line coverage is 0.85%." and line 77 reads "0.02%". This is important rather than cosmetic because the sentinel's **TD03** is specified to read `METRICS_REPORT_*.md` -- the markdown, not the JSON -- for the coverage namespace and to file a ledger row per module under a 70% floor; a report whose own prose asserts 0.85% coverage is a live false-positive path into this ledger. The JSON payload is correct, so machine consumers reading JSON are unaffected. Fix direction: multiply by 100 at the two `%`-suffixed render sites (preferred -- a human should read "85.02%" without converting), or drop the `%` and label the value a fraction. Leave the JSON field alone: `coverage_line_pct` is misnamed (holds a fraction) but renaming it is a breaking change for existing consumers and is a separate decision. Adjacent gap found while grounding this, NOT fixed and NOT separately filed: TD03 is specified per-module but the MD's `### coverage` subsection renders only the aggregate (`- Line coverage: 0.85`), so no per-module data reaches the surface TD03 reads -- TD03 cannot do what it says it does regardless of this bug. Proposed named class `correctness`, as td-119. RESOLVED 2026-08-06: scaling moved into a single `_fmt_pct` formatter applied at all three render sites -- narrative, deep-dive bullet, and the trends delta cell. The deep-dive bullet carried no `%` suffix and so read as correct, but it is the site the sentinel's coverage dimension parses, so fixing only the suffixed sites would have left the false-positive path open. The trends fixture, which had carried pre-scaled values no producer emits and thereby endorsed the bug through the golden comparison, is corrected to true ratios. Four new tests fail against the prior renderer. JSON payload untouched. The per-module gap noted above is confirmed and remains open. | e6e1c052a945 |
| td-119 | important | other | code-to-goals | scripts/prune_reports.py | code-quality |  | orchestrator | 2026-08-05 | 2026-08-06 | implementer | resolved | orchestrator (per-family prefix) | [surfaced during a `/project-metrics` run; deliberately not fixed there to stay surgical] `_report_runs` (scripts/prune_reports.py:69-83) groups a family's files by the **generic** `_REPORT_` marker plus a timestamp, so *any* `*_REPORT_<ts>.*` file sitting in a family directory consumes a retention slot for that family. `.ai-state/metrics_reports/` holds two deliberately-distinct triples: the code-health `METRICS_REPORT_*` triple (`scripts.project_metrics`) and the self-healing `SELF_HEALING_REPORT_*` triple, whose namespace `self_healing_metrics.py` and `scripts/CLAUDE.md` both declare "deliberately distinct ... to avoid schema collision". They collide anyway here. Live evidence 2026-08-05: `_report_runs` discovered 10 runs in the metrics family, one of them `SELF_HEALING_REPORT_2026-07-28_05-58-29`; with `keep=10` and 10 genuine metrics runs present the correct prune was a no-op, but `METRICS_REPORT_2026-06-11_08-38-15.{json,md}` was evicted instead (restored from HEAD by hand). Consequence is silent and compounding: every self-healing report costs one metrics-retention slot and vice versa, interleaved by timestamp, and the summary line reports `kept 10` while 9 metrics runs remain -- the count is right for the mixed family and wrong for either real one. No test catches it because the fixtures put one prefix per directory. Fix direction: carry a per-family report prefix in `_REPORT_FAMILIES` (currently directory-only) and match on `<PREFIX>_REPORT_`, or move self-healing reports to their own directory; add a canary with two prefixes in one directory. Proposed owner implementer (localized, no interface change). Proposed named class `correctness` -- `other` is now 12 rows, well past the schema's "propose a new enum value when other rows exceed 5". // 2026-08-06 RESOLVED, after the defect fired a SECOND time and destroyed the same two files. During this session's fan-out audit the sentinel's Phase 7 `prune_reports.py` run evicted `METRICS_REPORT_2026-06-11_08-38-15.{json,md}` again — the identical pair named in this row's original 2026-08-05 evidence — restored from HEAD both times. Fix as proposed here: `_REPORT_FAMILIES` now carries a per-family prefix, so it is a (directory, prefix) pair rather than a directory alone, and `_report_runs` matches `<PREFIX>_REPORT_` by `startswith` instead of a bare `_REPORT_` substring. `.ai-state/metrics_reports/` is registered twice, once per series, so METRICS and SELF_HEALING now hold independent retention budgets in one directory. Verified: a dry run that previously overflowed by one now reports metrics_reports:METRICS kept=10 pruned=0, metrics_reports:SELF_HEALING kept=1 pruned=0, sentinel_reports:SENTINEL kept=10 pruned=0. Two tests added, including the two-prefixes-in-one-directory canary this row asked for; every prior fixture put one prefix per directory, which is precisely why no test could ever fail on it. Note the prose was never wrong: `agents/sentinel.md` and `commands/project-metrics.md` both already specified per-series retention ("the last 10 `METRICS_REPORT_*` runs") — the implementation had drifted from its own documented contract. | 16bb3b983efd |
| td-124 | suggested | drift | goals-to-code | commands/new-project.md, commands/onboard-project.md, new_project.sh | claude-md |  | orchestrator | 2026-08-06 | 2026-08-06 | context-engineer | resolved | orchestrator (ae12b35 follow-up) | [orchestrator, Stay-Surgical redirect while fixing td-100] td-100's defect exists identically in every managed project, where it is worse: the instruction ships and the protection does not. `claude/config/CLAUDE.md:118` -- installed to `~/.claude/CLAUDE.md`, so loaded in every session on every project -- states 'Temporary files in `tmp/`'. The `.gitignore` block that `/onboard-project` Phase 1 installs (`commands/onboard-project.md:185-196`) lists `.ai-work/`, the `.ai-state/` lock/backup set, `.claude/settings.local.json`, `.claude/worktrees/` and the `.env` family -- and no `tmp/`. So a managed project is globally instructed to write scratch to a path onboarding never protects, which is exactly the `git add -A` exposure td-100 named, multiplied by the fleet. Praxion itself was the same shape until td-100 closed, which is why this went unseen: dogfooding masked it only until someone checked the block rather than the repo. Fix direction: add `tmp/` plus a `Why each line` table row to the Phase 1 block, then mirror byte-identically into `commands/new-project.md` and `new_project.sh` per the source-of-truth chain in CLAUDE.md (onboard-project -> new-project -> new_project.sh); the block is not in `claude/canonical-blocks/`, so `sync_canonical_blocks.py --check` will NOT catch a mirror miss -- the mirror is manual and must be verified by hand. Deliberately not fixed inside the td-100 commit: that was a one-line repo fix, this changes what ships to N repositories. Proposed owner context-engineer (the onboarding command set is a context artifact and the change spans its mirror chain), not the drift-class default doc-engineer. // 2026-08-06 RESOLVED. `tmp/` added to the shipped block and all FOUR mirror sites resynced byte-identically (commands/onboard-project.md, new_project.sh, tests/new_project_test.sh, docs/existing-project-onboarding.md) -- md5 2c7fbcd6f513330cb761ef8915d29b80 at each. The row predicted an uncaught mirror miss and one had ALREADY happened: three distinct variants across the four sites, disagreeing on `.ai-state/observations.jsonl.1` and `.ai-state/*.pre-forget.json`. Resolved by DROPPING pre-forget rather than propagating it -- nothing produces that file and no `forget` operation survives dec-225's removal of the in-house memory subsystem, so adding it to a second shipped surface would have exported dead cruft to N repositories. Also dropped the 'canonical 10-line block' labels (a count in prose that must track a block is its own drift surface, and it was already wrong at both sites) and backfilled two missing `Why each line` rows. `tests/new_project_test.sh` T7 was already RED on this drift; now 10/10. The reason that red test went unseen is filed as td-125. | 845f7379070b |
| td-100 | suggested | drift | code-to-goals | CLAUDE.md, .gitignore | claude-md | CLAUDE.md#critical-conventions | verifier | 2026-07-31 | 2026-08-06 | doc-engineer | resolved | orchestrator (.gitignore `tmp/`) | [verifier, multidisciplinary-identities Waves 1-2 + td-081 verification] `CLAUDE.md:60` states 'temp files in `tmp/` (gitignored)' as part of the always-loaded Critical Conventions. Verified directly: `tmp/` has no entry in `.gitignore`, and `git check-ignore tmp/x` exits 1 (not ignored). An always-loaded instruction is asserting a gitignore protection that does not exist -- a temp file written to `tmp/` per this exact instruction is one `git add -A` away from being committed, which this repo's own git-conventions rules explicitly steer contributors away from. Fix: either add `tmp/` to `.gitignore` (matching the claim) or correct the CLAUDE.md wording to drop the parenthetical, if `tmp/` is meant to be manually excluded per commit rather than gitignore-protected. // 2026-08-06 RESOLVED. Took the first of the row's two options -- add `tmp/` to `.gitignore` -- rather than dropping the parenthetical, because the always-loaded convention actively directs writers to that path, so the protection is what should exist. Safe as a pure addition: `git ls-files tmp/` was empty, so nothing already tracked was orphaned by the new rule. Verified `git check-ignore -v tmp/x` now resolves to `.gitignore:55`, and `tmp/` no longer appears as untracked in `git status`. The same gap in the SHIPPED onboarding block is filed separately as td-124 -- fixing it here does not fix it for managed projects. | d2cbead414e5 |
| td-059 | important | coverage-gap | code-to-goals | scripts/aac_fence_validator.py, scripts/spec_drift.py | code-quality |  | sentinel | 2026-07-29 | 2026-08-06 | test-engineer | resolved | orchestrator (ledger triage 2026-08-06) | Both files sit well under the project coverage floor (70%): `spec_drift.py` 16% (per METRICS_REPORT_2026-07-29_22-11-31), `aac_fence_validator.py` 31%. Elevated to `important` (not `suggested`) because both are gate-liveness-critical — `spec_drift.py` backs sentinel SH07 (spec-drift detection) and `aac_fence_validator.py` backs sentinel AC10 (fence integrity); per gate-liveness.md a thinly-tested detector risks a silent false-PASS on the very defects it exists to catch. Four additional scripts sit modestly under floor (`check_paths_syntax.py` 56%, `prune_reports.py` 57%, `project_metrics/cli.py` 60%, `self_healing_metrics.py` 66%) — not filed this pass to avoid ledger noise; re-evaluate if they drift further or gain gate-consuming callers. // 2026-08-06 RESOLVED by ledger triage -- the coverage gap is closed and the row is outdated. Both named files now sit well ABOVE the 70% floor, not under it: `scripts/spec_drift.py` 89.4% (was 16%) and `scripts/aac_fence_validator.py` 89.5% (was 31%), per METRICS_REPORT_2026-08-05_22-03-40 `coverage.data.per_file`; the adjacent `scripts/check_spec_drift.py` is 92.3%. The gate-liveness-critical rationale that justified the `important` severity is satisfied. No action remains. | 56b1260c734b |
| td-077 | suggested | drift | code-to-goals | skills/software-planning/references/tech-debt-ledger.md, scripts/finalize_tech_debt_ledger.py | code-quality |  | orchestrator | 2026-07-30 | 2026-08-06 | context-engineer | resolved | td-085 (question answered, action carried forward) | [found while filing td-073..td-076] The documented dedup_key derivation -- sha1 over class, normalize(location), direction, goal-ref-type, goal-ref-value, truncated to 12 -- does not reproduce the keys on existing rows. Checked against td-071 (expected 52702faa9ec4) and td-072 (expected 08a11c91797c) with both ',' and ', ' joiners; no variant matches. Either the shipped rows are non-conforming or the documented formula is wrong, and nothing distinguishes the two because no code validates the derivation: finalize_tech_debt_ledger.py consumes dedup_key for grouping and re-open detection but never recomputes it. A wrong key silently merges unrelated debt or silently fails to re-open a recurrence. Rows td-073..td-078 were written using the documented formula with collision-checked uniqueness across both ledger files. Same pattern class as td-073: a documented contract with no gate. // 2026-08-06 RESOLVED by ledger triage -- superseded, not abandoned. This row posed a disjunction ('either the shipped rows are non-conforming or the documented formula is wrong') that td-085 answered decisively in the formula's favour by brute-forcing 56 variants against the full row set: the documented derivation scores highest of all 56, so the non-conforming rows are bad DATA, not a wrong formula. Re-verified 2026-08-06 over the active ledger: 37 of 43 rows reproduce, and the 6 that do not predate the formula's codification at td-073. The actionable remainder -- a write-time validator in finalize_tech_debt_ledger.py plus a one-time backfill -- is carried in full by td-085, which stays open. Closing this row removes a duplicate question, not an open task. | 32e691da0699 |
| td-078 | important | other | goals-to-code | rules/swe/vcs/git-conventions.md | code-quality |  | orchestrator | 2026-07-30 | 2026-08-06 | context-engineer | resolved | rules/swe/vcs/git-conventions.md § Staging Discipline | [cost real work during Wave 2; the hazard is known but unencoded] Bare `git stash` / `git stash pop` is forbidden when concurrent agents share a worktree -- it sweeps up every other writer's uncommitted work regardless of pathspec discipline. A Wave-2 implementer ran one mid-step and destroyed the orchestrator's uncommitted ledger rows and dossier edits; recovery was partial (files edited before the stash were restorable per-file, files written during it were not). Grep of rules/, agents/, and skills/software-planning/ for 'git stash' returns ZERO hits: the constraint lives only in per-session environment guidance and a personal memory note, neither of which a subagent loads. So the agent was not disobeying Praxion -- Praxion never told it. Existing Staging Discipline guidance covers pathspec staging, which prevents over-staging but does nothing against stash/checkout/reset. Fix: a short bullet under Staging Discipline in git-conventions.md (always-loaded; ~1,555 tokens of headroom available) plus the safe-recovery idiom (push with a unique tag, restore with `git stash apply <sha>`, never pop). // 2026-08-06 RESOLVED by ledger triage -- the row asked for the hazard to be encoded, and it now is. `rules/swe/vcs/git-conventions.md` § Staging Discipline carries it as a named prohibition ('Never a bare `git stash` / `git stash pop` in a worktree concurrent agents may share'), with the rationale (pathspec discipline does not protect other writers), the sanctioned alternative (a temporary WIP commit), and the full escape procedure when a stash is unavoidable (`push -u -m <tag>`, capture the SHA, `apply` never `pop`, drop by tag). The rule is always-loaded, so the guidance reaches every session. Nothing further to encode. | 56c0ceca6368 |
| td-089 | important | drift | code-to-goals | .ai-state/decisions/032-roadmap-md-location-and-lifecycle.md, .ai-state/decisions/046-staleness-detection-system.md, .ai-state/decisions/057-concurrency-collab-research-unified-worktree-home.md, .ai-state/decisions/077-agent-model-routing-routing-frontmatter-floor-semantic.md, .ai-state/decisions/111-req-arch-element-traceability.md, .ai-state/decisions/129-pipeline-dashboard-dashboard-supersede-metrics-viewer.md, .ai-state/decisions/152-interface-designer-opus-tier.md, .ai-state/decisions/153-review-interface-skill-command-pattern.md, .ai-state/decisions/168-core-frontmatter-single-source-of-truth.md, .ai-state/decisions/169-git-conventions-blacklistable.md, .ai-state/decisions/190-hackathon-mode-plan-decomposition.md, .ai-state/decisions/224-nebius-neocloud-nebius-direct-adapter.md, .ai-state/decisions/294-label-taxonomy-manifest.md, scripts/build_doc_manifest.py | code-quality |  | verifier | 2026-07-31 | 2026-08-06 | doc-engineer | resolved | orchestrator (ledger triage 2026-08-06) | [verifier, multidisciplinary-identities Waves 1-2 + td-081 verification] 13 pre-existing ADR records fail yaml.safe_load on the same defect class already fixed on dec-306/dec-309 by this pipeline: an unquoted colon-space inside a frontmatter scalar breaks the YAML scanner. Affected: 032, 046, 057, 077, 111, 129, 152, 153, 168, 169, 190, 224, 294. `adr-conventions.md` calls this class of failure 'harmless only because the ADR tooling parses frontmatter by regex' -- true of the ADR tooling itself, irrelevant to `scripts/build_doc_manifest.py:132`, which does `except yaml.YAMLError: fm = {}` silently on these same files, so each renders its raw filename as `title:` in `.ai-state/doc_manifest.yaml` instead of the real title -- confirmed by grepping the manifest directly. Consequence: `dissent:`/`summary:` frontmatter fields cannot honestly be called 'machine-queryable' (as `adr-conventions.md` states) while 13 records fail the parser silently. Fix: quote the offending scalar in each of the 13 files, regenerate the manifest from the canonical checkout (a worktree regen silently drops all docs -- `.claude` paths are excluded from the manifest build by an absolute-path check), and add a fitness check that fails loudly on a frontmatter parse error instead of swallowing it. // 2026-08-06 RESOLVED by ledger triage -- outdated; the defect class is gone. The row named 13 pre-existing ADR records failing `yaml.safe_load` on an unquoted colon-space inside a frontmatter scalar. Re-verified 2026-08-06 by parsing the frontmatter of every finalized ADR under `.ai-state/decisions/`: 0 of 324 fail. The corpus is fully machine-parseable; `check_adr_frontmatter_promotion.py` and `adr_health.py` both read it clean. | 293577e56b98 |
| td-092 | suggested | drift | code-to-goals | docs/diagrams/architecture/src/architecture.c4 | code-quality |  | verifier | 2026-07-31 | 2026-08-06 | doc-engineer | resolved | orchestrator (ledger triage 2026-08-06) | [verifier, multidisciplinary-identities Waves 1-2 + td-081 verification] `docs/diagrams/architecture/src/architecture.c4` models `challenge_loop` (9 references across the model) -- the CONSEQUENCE of the discipline-consultant mechanism -- but has no `discipline-consultant` element of its own, even though the sibling peer sub-architects (interface-designer, agentic-transactions-architect) each have their own modeled element. The model shows the loop's effect without the agent that runs it. Fix: add a `discipline-consultant` element (matching the shape of the interface-designer/agentic-transactions-architect elements already in the model) plus its relationships into `challenge_loop`, then run the diagram-regen hook so the committed `.d2`/`.svg` renders stay in sync. // 2026-08-06 RESOLVED by ledger triage -- outdated; the model now carries the element. The row's claim was that `architecture.c4` models `challenge_loop` (the consequence) but has no `discipline-consultant` element of its own. Re-verified 2026-08-06: the element is declared at `docs/diagrams/architecture/src/architecture.c4:102` (`discipline_consultant = agent "Discipline Consultant"`) and carries a dispatch relationship at line 165. `check_architecture_projection.py` reconciles 16 rows against 16 elements with zero findings. | 5ae622c67354 |
| td-122 | important | coverage-gap | code-to-goals | hooks/_hook_utils.py | code-quality |  | sentinel | 2026-08-05 | 2026-08-06 | test-engineer | wontfix | orchestrator | [sentinel, full-ecosystem-audit] TD03: `hooks/_hook_utils.py` sits at 42.9% line coverage (12/28 lines, per METRICS_REPORT_2026-08-05_22-03-40.json `coverage.data.per_file`) -- the lowest of any file under the 70% project floor this run, and by a wide margin (next-lowest is `check_paths_syntax.py` at 56.3%). Elevated to `important` rather than `suggested` per the td-059 precedent (gate-liveness-critical code gets elevated severity): this module is imported by all 18 capture/telemetry hooks (`grep -rl _hook_utils hooks/*.py` = 18) and owns the shared `append_observation` helper, including its `fcntl`-based best-effort rotation at `OBSERVATIONS_MAX_BYTES` -- a silent-failure-prone code path (lock contention, rotation-boundary off-by-one) whose bugs would corrupt `.ai-state/observations.jsonl` across every hook invocation rather than one call site. Four other scripts sit modestly under floor this run (`check_ruff_pin_drift.py` 56.3%, `scripts/check_metrics_freshness.py` 65.5%, `hooks/inject_process_framing.py` 65.8%, `scripts/render_claude_md.py` 69.1%) plus the four already named and explicitly deferred in td-059's own notes (`check_paths_syntax.py`, `prune_reports.py`, `project_metrics/cli.py`, `self_healing_metrics.py`) -- not filed this pass, consistent with td-059's precedent of not flooding the ledger with modest gaps. Read via JSON `per_file` rather than the `.md` report per td-120's own finding that the MD's `### coverage` subsection renders only the aggregate, not per-module data -- TD03 cannot execute as literally specified (read the `.md`) until td-120 lands. // 2026-08-06 WONTFIX -- FALSE FINDING, premise refuted. The 42.9% is a measurement-scope artifact, not a coverage gap: `hooks/test_hook_utils.py` already covers the module 28/28 statements (line-rate 1.0). It never reaches the canonical coverage run because `pyproject.toml` `testpaths = ["tests", "scripts"]` omits `hooks/`, and `.github/workflows/test.yml` runs the hook suite as a separate job with `--no-cov` -- so 377 hook tests pass in CI while contributing nothing to `coverage.xml`. Verified by re-running `pytest hooks/ --cov=hooks`: `_hook_utils.py` 0.429 reported vs 1.000 actual, and `inject_process_framing.py` 0.658 vs 0.848 -- both hook files present in the metrics `per_file` are understated. Writing tests here would raise no real coverage. Root cause filed as td-123. | 340a9f40d3cc |
| td-121 | suggested | drift | goals-to-code | scripts/artifact_registry.py | code-quality |  | orchestrator | 2026-08-06 | 2026-08-06 | implementer | resolved | artifact_registry CONSULT entry | [orchestrator, sentinel-remediation-i2; surfaced while grounding P07's consult clause, deliberately not fixed there to stay surgical] `CONSULT_<discipline>.md` is listed in the canonical `.ai-work/<task-slug>/` tree in `rules/swe/agent-intermediate-documents.md` but is absent from `scripts/artifact_registry.py`, which describes itself as the single source of truth for that set (23 ai-work artifacts registered; CONSULT is not one). Its two P07-gated siblings both have entries carrying `detection_gate="sentinel:P07"`; the consult fragment has none, so P07's coverage of it is invisible to the registry -- the same named-consumer gap the gate-liveness rule names, one level up. Fix is one `Artifact(...)` entry, but the `snapshot` flag is a real judgment call rather than a fill-in: a consult fragment carries in-flight adjudication state, so snapshotting it on PreCompact is arguably right, and setting it true changes a projection and therefore requires updating `precompact_state.PIPELINE_DOCS` in the same pass or the registry drift test fails. Setting it false is safe and changes no projection. Decide the semantics, do not default. // 2026-08-06 RESOLVED. Registered as `CONSULT_<discipline>.md` with `production_gate=producer:discipline-consultant`, `detection_gate=sentinel:P07`, `cleanup_policy=delete`; ai-work set 23 -> 24, and all three P07-gated artifacts are now visible to the registry. The `snapshot`/`dashboard` question this row flagged as a judgment call turned out to be FORCED rather than chosen, which is the more useful finding: both consumers match exact filenames -- the dashboard's `CANONICAL_WORKSHOP_ARTIFACTS` is a TS string list and precompact does `task_dir / doc_name` -- and this is the registry's first and only variable-named artifact (one fragment per convened discipline, so one slug can hold several). Setting either flag true would demand a consumer entry that cannot be written as a literal filename. Both are false with that reason recorded inline at the entry, so it does not read as a judgment that the fragment is uninteresting. Wiring it in needs glob support on the consumer side -- a change to them, not to this registry -- and is deliberately NOT filed as a follow-up row: nobody has asked for consult fragments on the dashboard, and a row for a speculative feature is backlog, not debt. `cleanup_policy=delete` is the accurate record rather than the aspirational one: `clean_work_safety.py` does not special-case CONSULT, so its real class is SAFE. Recording `block-if-active` would have asserted behaviour no code implements. Drift gate green: both projections unchanged (dashboard 19, snapshot 15), all 18 registry tests pass. | c2f07f188deb |
| td-117 | nice-to-have | drift | code-to-goals | skills/agentic-transactions/references/robinhood.md | code-quality |  | orchestrator | 2026-08-05 | 2026-08-06 | context-engineer | resolved | robinhood.md inventory -> classification rule | [orchestrator, sentinel-remediation-i1] The Robinhood agentic-trading surface moved 10 -> ~50 MCP tools AND gained a whole asset class (options) in ~64 days, inside a 60-day staleness threshold. The threshold cannot catch a surface moving that fast: the content was wrong for weeks before the marker was even eligible to fire, and the errors were load-bearing for HITL spend-gating (`place_option_order` was a second capital-moving tool outside every documented gate). Two options, not exclusive: (a) drop `skills/agentic-transactions` to a 30-day threshold, accepting more frequent re-verification on a fast-moving vendor surface; (b) lean harder on the skill's own instruction to resolve the surface operationally via live `tools/list` and stop documenting a tool inventory at all, keeping only the classification (capital-moving vs state-mutating-non-capital) which is what the gating design actually needs and which does not rot when tools are added. The 2026-08-05 correction already added the velocity warning and the operational instruction; this row is about whether the threshold instrument should change. Note the asymmetry the surface demonstrated: every NEGATIVE claim ("rate limits not published", "schemas not published") held across 64 days, while both POSITIVE claims ("all 10 tools", "options on roadmap") rotted. // 2026-08-06 RESOLVED via option (b), and option (a) DECLINED with reason. (a) was rejected on two grounds. First, `staleness_threshold_days` is skill-level and `staleness_sensitive_sections` lists two sections, so halving it to 30 doubles re-verification on `Provider Transport Strategies`, which is not fast-moving -- paying a cadence cost across the skill to chase one volatile subsection. Second and more important, 30 is not derived from anything: the observed drift was 64 days against a 60-day threshold, so 30 is the last miss halved. That is the same instrument recalibrated by anecdote, and nothing bounds the next drift at 30 days either. dec-322 settled this exact pattern in a different unit -- when a time-based staleness measure misses, change what is measured rather than retuning the threshold. (b) applied surgically: the `### Categories` table (50 tool names) and every present-tense count are gone from `references/robinhood.md` and `SKILL.md`; what remains is a name-pattern classification (`place_*_order` / `cancel_*_order` capital-moving, `review_*_order` the HITL primitive, `get_*` read, other mutating verbs state-mutating-non-capital), the negative claims, and the `tools/list` procedure. Historical narration ("grew 10 -> ~50 in 64 days") is kept deliberately: a past fact is durable, a present count is not. The deeper fix, which neither option named: the harm was never a stale list, it was a new capital-moving tool arriving outside every documented gate. The HITL gate is now stated against the `place_*_order` PATTERN rather than an enumerated equities+options pair, so the next asset class is covered on the day it ships. That converts a time-dependent correctness problem into a time-independent one, which no threshold value can do. Threshold left at 60 deliberately -- after the enumeration is gone the remaining content is the durable kind, and tightening it would impose cost for no gain. | fffc2b1587cd |
| td-118 | nice-to-have | other | code-to-goals | agents/sentinel.md | code-quality |  | orchestrator | 2026-08-05 | 2026-08-06 | context-engineer | resolved | sentinel F09 template-placeholder exclusion | [orchestrator, sentinel-remediation-i1] Sentinel staleness accounting counts a template placeholder as a real marker. `skills/agentic-transactions/references/_provider-template.md:100` contains the literal string `<!-- last-verified: [YYYY-MM-DD] -->` inside a blank-slate template that provider authors copy and fill. It has no parseable date and cannot age, but it inflated the reported marker count for `agentic-transactions` from 6 to 7 in the 2026-08-05 report. Low severity -- it produces a small over-count, not a missed finding -- but it is scaffolding entering a measurement, and the `_`-prefixed filename convention already marks such files as templates. Fix is a scanner-side skip for files matching the template convention, or for markers whose date does not parse as ISO 8601 (note F09 currently FAILs on invalid format, so a bare skip must not silently swallow genuinely malformed markers -- distinguish `[YYYY-MM-DD]`-shaped placeholders from typos). // 2026-08-06 RESOLVED: F09 now carries a deliberately narrow exclusion -- a marker whose date is the literal `[YYYY-MM-DD]` token AND which sits in an `_`-prefixed blank-slate template under `references/` is a placeholder, excluded from F07, F08 and the marker count. Keyed on the token and the filename convention TOGETHER, so a typo'd date is never mistaken for a template and F09's FAIL-on-invalid-format behaviour is unchanged for every other case -- which was the trap this row named. Exactly one such file exists today (`skills/agentic-transactions/references/_provider-template.md`). | f86ff79a8f4e |
| td-114 | important | drift | code-to-goals | skills/communicating-agents/, skills/mcp-crafting/, skills/hook-crafting/, skills/cicd/, skills/likec4-querying/, skills/agentic-transactions/ | code-quality |  | sentinel | 2026-08-05 | 2026-08-05 | context-engineer | resolved | sentinel-remediation-i1 | [orchestrator, sentinel-remediation-i1] RESOLVED 2026-08-05. Content corrected across all 6 skills against primary sources (SDK samples, protos, registries, raw upstream docs, live tools/list), then re-marked -- 34 markers moved, 20 files, all 6 validators pass. Scope grew beyond the marked sections: the satellites those sections point at carried the same dead APIs, so refreshing SKILL.md markers alone would have silenced the sentinel with the harm intact. The finding was also mis-classed -- part of it was FABRICATION rather than staleness: 5 identifiers that exist nowhere upstream (PostgreSQLTaskStore, PushNotifier, A2APlugin, fast_a2a, ClientFactory.create()), an invented likec4 maxNodes param, and two Framework Integrations rows (LangGraph-via-LangSmith, AutoGen connector) with no first-party code or docs anywhere -- kept as sourced negatives rather than deleted. Robinhood settled at 50 ENUMERATED tools (two independent no-arithmetic transcriptions agree; the article's own prose counts sum to 45 -- upstream self-inconsistency), options trading live, and 'place_equity_order is the only write-side placement tool' false. Evidence preserved at tmp/sentinel-remediation-research/ (byte-identical, SHA-256 verified) since .ai-work/ is gitignored and /clean-work classifies that dir SAFE for unconditional rm -rf. Follow-ups filed: td-116 (verified-incorrect marker state), td-117 (Robinhood threshold vs surface velocity), td-118 (template placeholder counted as a marker). | 063a9fd135ed |
| td-112 | suggested | drift | goals-to-code | skills/software-planning/assets/ARCHITECTURE_TEMPLATE.md, rules/writing/diagram-conventions.md | design |  | orchestrator | 2026-08-04 | 2026-08-04 | doc-engineer | resolved | arch-ground-truth gate-class | [orchestrator, arch-ground-truth B5] ARCHITECTURE_TEMPLATE.md caps the component table and L1 diagram at 10-12 nodes, a budget rules/writing/diagram-conventions.md explicitly scopes to Mermaid only and exempts LikeC4 and D2 from, since those manage complexity by nesting and per-view scoping rather than a count. The same convention therefore reads two ways at two sites -- the anti-pattern gate-liveness names. Praxion's own DESIGN.md comment was corrected to cite the exemption; the shipped template still carries the Mermaid-era cap and would impose it on every managed project that adopts the AaC tier. Fix is to scope the template's cap to its toolchain, but it edits a shipped artifact and so wants its own change. // RESOLVED 2026-08-05: both cap statements in ARCHITECTURE_TEMPLATE.md now scope the 10-12 budget to Mermaid rendering and cite the LikeC4/D2 exemption, so a managed project adopting the AaC tier is told to add structure when a model outgrows twelve elements rather than to drop elements. The section-3a comment additionally states that row count follows the model rather than a node budget, which is what the projection checker enforces. | d0749808c37c |
| td-113 | important | other | code-to-goals | scripts/check_spec_drift.py | code-quality |  | orchestrator | 2026-08-05 | 2026-08-05 | sentinel | resolved | arch-ground-truth gate-class | [orchestrator, arch-ground-truth gate-class] Found by the new GL04 uninvoked-gate check on its first run. check_spec_drift.py is a sentinel-formatting wrapper whose own docstring says it is invoked by the SH07 dimension; SH07 instead imports detect_drift from scripts/spec_drift directly, so the wrapper has never executed. Because nothing ever ran it, nobody discovered it is also broken: invoked as a script it dies on ModuleNotFoundError for its own import fallback. Two honest dispositions -- wire SH07 to the wrapper (making both artifacts' claims true, after fixing the import), or delete the wrapper since SH07's inline import already works. Deliberately left open rather than fixed in the same change, because choosing between them is a judgment about the sentinel's own structure. // CORRECTION 2026-08-05: the wrapper is NOT broken. It runs clean under the project interpreter (0 findings, exit 0) and its three tests pass; it fails only under the ambient python3, because spec_drift.py imports PyYAML which the pyenv shim lacks -- the interpreter -resolution issue finalize_chain.sh already documents, not a code defect. // RESOLVED 2026-08-05: root cause was two-layered. SH07 described an inline import of detect_drift instead of invoking the wrapper, AND SH07 was absent from the Pass-1 auto dispatch list, so it never ran at all. Both fixed: SH07 now runs `python3 scripts/check_spec_drift.py --json` and is dispatched alongside SH01-SH02/SH08. Fixing only the first layer would have left the gate still uninvoked. GL04 now reports zero findings. | 4cff9341a505 |
| td-111 | suggested | drift | goals-to-code | .ai-state/DESIGN.md, docs/architecture.md, docs/diagrams/architecture/src/architecture.c4 | design | .ai-state/DESIGN.md#3a-structural-components | orchestrator | 2026-08-04 | 2026-08-04 | systems-architect | resolved | arch-ground-truth B5 | [orchestrator, arch-ground-truth B0-A] Splitting DESIGN.md §3 into 3a structural / 3b capabilities surfaced four disagreements between §3a and the LikeC4 model, left unfixed so the split stays reviewable: (1) `versioning` ("Versioning", commitizen) is a model element with no §3a row; (2) `adrs` ("ADR Fragments") is a model element subsumed by the `.ai-state/` row -- model over-granularity, likely remove from the model rather than add a row; (3) Pipeline Dashboard is a §3a row with no model element, though `dashboard_app/` is a separate Next.js runtime with its own process and port -- belongs under `tooling`; (4) Eval framework is a §3a row with no model element, though `eval/` is a separate package with its own pyproject and interpreter floor -- also `tooling`. Fix direction: add `versioning`-peer elements for Dashboard and Eval, drop `adrs`, add a `versioning` row. Editing the .c4 model triggers diagram regeneration and its own CI leg, so it is deliberately a separate change. These four are exactly the reverse-drift class the projection reconciler is designed to catch continuously. // RESOLVED 2026-08-04 (B5): scripts/check_architecture_projection.py binds each 3a row to a model element by id in a new Element column and fails the commit on either-side drift. Three of the four disagreements were fixed by adding what was missing rather than deleting: Pipeline Dashboard and Eval Framework became tooling elements (they are real runtimes, so modelling them widens enforcement), and Versioning gained a row. The fourth reversed td-111's own guess -- ADR Fragments was kept in the model and given a row, because its promotion lifecycle is carried by four enforced edges that folding it into .ai-state/ would erase. | a34594d85c11 |
| td-110 | important | other | goals-to-code | .pre-commit-config.yaml, rules/swe/vcs/git-conventions.md | code-quality |  | orchestrator | 2026-07-31 | 2026-07-31 | context-engineer | resolved | dec-316 | [cost real work during the gate-integrity-and-overlay pipeline; recovered, but only because the loss was noticed] THE HAZARD CLASS, stated once: a git operation that is routine on a clean tree and silently destructive on a dirty one. Three distinct instances hit this repository, two of them inside a single task: (1) bare `git stash` (already documented in td-078, and now in git-conventions.md); (2) `git checkout -- <path>` prescribed as the neuter-restore primitive in a gate-liveness proof protocol -- correct on a clean tree, and it would have discarded an entire pipeline's uncommitted work here (caught by a test-engineer's Register Objection before it ran, and replaced fleet-wide with bytes-in-memory + SHA-256 assert); and (3) NEW AND UNDOCUMENTED -- **pre-commit's own stash/restore is not a no-op when a hook modifies the same file**. pre-commit stashes unstaged changes, runs hooks, then restores the stash ON TOP of whatever the hooks wrote. With the ruff version skew active (pinned 0.8.6 vs a newer local ruff, which format `assert X, msg` differently -- td-078's sibling symptom), every failed commit attempt reformats the working tree and the restore then reverts it, so `git add` can never capture a stable state and the loop never converges. OBSERVED CONSEQUENCE: two completed implementation steps (an extraction taking a fitness file 17 -> 23 named checks, and the per-check canary-coverage rewrite) were silently reverted to their HEAD state mid-session. `git status` reported the directory CLEAN. The blobs were NOT in the object store (the `git add` had staged an already-reverted file) and NOT in the recent pre-commit patches. Recovered only from an older ~914KB pre-commit patch under ~/.cache/pre-commit/, applied selectively with `git apply --include`. Nothing warned; the failure presented as 'this file was never modified'. ESCAPE THAT WORKED, for the record: run the PINNED ruff binary directly (found under ~/.cache/pre-commit/repo*/py_env-*/bin/ruff) to reach its fixed point, then `git add` and `git commit` in ONE atomic invocation so no restore can intervene. `git update-index --assume-unchanged` on the churning files (observations.jsonl, uv.lock) reduces but does not eliminate the stash surface. RESOLUTION: (a) document the pre-commit stash/restore hazard alongside the bare-stash rule in `rules/swe/vcs/git-conventions.md`, naming the recovery path (~/.cache/pre-commit/patch* + `git apply --include`); (b) resolve the ruff skew at its root by aligning the `.pre-commit-config.yaml` rev with the toolchain contributors actually run -- the skew is the engine driving the loop, and every mitigation above is a workaround for it. Owner-role context-engineer, matching the td-077/078/079/083 precedent for defects in the repository's own bookkeeping and tooling machinery. // RESOLVED 2026-08-02. Root cause was NOT a stale pin -- a stale pin drifts once. It was an UNPINNED local side: the hook pinned v0.8.6 while nothing declared what a developer should run, so it fought a different version on every machine (0.15.4 locally, 0.15.11 in eval's resolution, anything at all elsewhere). Fix couples the two into one decision: pyproject declares `ruff==0.15.22` EXACTLY (`==`, not `>=` -- a floating constraint leaves the same defect in a looser mask), both pre-commit configs pin the same rev, and `scripts/check_ruff_pin_drift.py` BLOCKS a commit when they disagree, running ahead of the ruff hooks so the version mismatch is named rather than presenting as a formatter that cannot make up its mind. Chose v0.15.22 over the newer v0.16.1 on SCOPE, not caution: 0.16 scans a different file set entirely (1255 files vs 310 here), so adopting it would smuggle a silent scope change in under a formatting bump -- that evaluation is owed separately. Gate resolves the installed ruff project-environment-first and only then from PATH, because in a uv-managed project the ruff a developer runs is the one the project provides; comparing against an unrelated global binary would fail someone doing everything right, and a gate that punishes correct behaviour gets bypassed. Absent inputs are never drift (no ruff hook, or no adopted dep pin = out of scope, not broken) since this ships fleet-wide. MANAGED PROJECTS: the shipped baseline carries the same pin plus the coupling instruction at the exact site someone would bump it, and /onboard-project's commit gate runs the drift check ahead of the citation check, failing open when the plugin is absent. Hazard class documented in git-conventions.md alongside the bare-stash rule it belongs with, including pre-commit's stash/restore instance, the recovery path through ~/.cache/pre-commit/patch*, and the practice this cost taught: verify what a commit CONTAINS, not that it succeeded. Adoption also surfaced 129 lint findings that had accumulated invisibly -- the gate lints STAGED files only, so a violation in an untouched file never surfaced; same shape as td-108. All resolved, none configured away: 88 auto-fixed, 29 PT019 converted to usefixtures, 13 PT018 split, 3 PT017 to pytest.raises, 2 E402 (one hoisted, one suppressed with its sys.path reason), 1 N818 renamed, and 3 N802 suppressed WITH reasons rather than renamed because the capitals carry meaning (README/CLAUDE/%2F hex). Canaries: neutering each of the gate's three arms reddens 3/2/1 tests. Suite 2095 passed, 1 skipped. | 1cc405ec3027 |
| td-073 | important | drift | code-to-goals | docs/multidisciplinary-identities-evidence.md, scripts/check_release_staleness.py | code-quality |  | orchestrator | 2026-07-30 | 2026-07-30 | systems-architect | resolved | dec-315 | [Wave 2 intake, verified three ways] `evidence-appraiser` is merged on `main` but absent from the `v0.18.0` release: the tag tree carries skills/applied-statistics and not skills/evidence-appraisal, `git show v0.18.0:...discipline-registry.md` returns zero evidence-appraiser rows, and the installed plugin cache at ~/.claude/plugins/cache/bit-agora/i-am/0.18.0/skills/ agrees. Dossier 12.2 claimed "SHIPPED 2026-07-30"; corrected this wave to "MERGED ... NOT YET RELEASED". Consequence for managed projects on v0.18.0: a `Discipline: evidence-appraiser` directive resolves to [BLOCKED] (fail-loud as designed, but not the advertised behaviour), and Wave 1's AC13 two-disciplines-concurrently proof ran against the working tree rather than the shipped plugin. The structural defect is NOT a missing gate: scripts/check_release_staleness.py detects this correctly, is wired into .github/workflows/test.yml, and fires today. It is advisory by deliberate and well-argued design (its docstring: firing per-feat-commit "would contradict the pinned-stable version model"), and nothing consumes its output. Second of two independent instances that motivated the new gate-liveness clause 'a named consumer for every gate output'. Resolution needs a release cut (user-owned) plus a named consumer for the advisory. // Closed by the gate-integrity-and-overlay pipeline, both halves. HALF 1 (release) discharged by evidence, not by a fix: `v0.18.0` ships 0 evidence-appraisal skill files and 0 registry rows, while `v0.19.0` and `v0.20.0` each ship 5 skill files and a complete 7-column roster row -- so the row's original claim was correct when filed and is no longer true. The workstation's newest cached plugin being 0.18.0 is an INSTALL fact, not a RELEASE fact; the two were deliberately not conflated. HALF 2 (the gate-liveness clause-6 named-consumer gap) closed by naming a reader at a decision point that actually fires on a cadence: `commands/release.md` now runs the advisory and records its verdict as part of the cut, and `check_release_staleness.py` carries a `## Named consumer` docstring block plus a golden bad-case. The advisory was deliberately NOT made blocking -- its fail-open design is correct per its own docstring, and clause 6's remedy is to name the reader, not to escalate the gate. The stale dossier sentence ('MERGED ... NOT YET RELEASED') was corrected with its drift history preserved rather than erased. Adjacent `calibration-coverage` advisory flagged as td-107, deliberately not fixed. | 712a7da1b5c1 |
| td-083 | important | coverage-gap | code-to-goals | fitness/tests/test_gate_canary_coverage.py | code-quality |  | orchestrator | 2026-07-31 | 2026-07-31 | context-engineer | resolved | dec-314 | [found by the orchestrator while verifying td-081's gate, by neutering each check in turn] The canary-coverage meta-test asks whether a *file* contains a canary, not whether each *check* has one: `_has_canary(file)` returns a single bool per file and `gates_without_canary` lists files. So a new check added to a file that already carries canaries is invisible to it -- which is the normal way checks get added. Concretely: td-081's gate specification listed six checks and eleven canaries, omitting one for `check_classification_row_has_eight_columns`. The implementer followed the plan faithfully, the meta-test stayed green, and the uncanaried check was found only because the orchestrator stubbed each check in turn and observed which tests went red -- one produced zero. The missing canary was written and proven to bite in the same pass. This is the Scope Fidelity clause (gate-liveness.md, added dec-307's wave) applied to the meta-gate itself: its computed scope (files) is narrower than its documented purpose (gates bite). Remedy sketch, not built: discover `check_*` / `validate_*` function definitions within a gate file and require a canary naming or exercising each, rather than one canary per file. // Closed by the gate-integrity-and-overlay pipeline, together with td-094 which is upstream of it. `fitness/tests/test_gate_canary_coverage.py`'s computed unit is now the CHECK, not the file: it discovers `check_*`/`validate_*` definitions and requires a canary-named test that calls each. The matcher resolves `ast.Attribute` as well as `ast.Name` -- a Name-only version was measured producing a false violation against REAL code (`check_gate_liveness`), not merely a fixture. The policy skip sets were DROPPED from the check-level pass (retained for the file-level rule): every entry in `_SKIP_GATE_STEMS` was excused because 'the file already has a canary', which is precisely the reasoning this row falsifies, so carrying it forward would have re-imported the defect inside its own fix. Measured coverage after the fix: 27/27 checks canaried, ZERO uncanaried (23 registry-invariants + 2 consult-append-only picked up dynamically via the glob + 1 meta-citation + 1 check_gate_liveness). The row's prediction that 4-5 gates had never been observed to fail did not survive measurement once td-094's extraction landed. | 29eec55f9b3f |
| td-094 | important | coverage-gap | code-to-goals | fitness/tests/test_discipline_registry_invariants.py | code-quality |  | verifier | 2026-07-31 | 2026-07-31 | context-engineer | resolved | dec-314 | [verifier, multidisciplinary-identities Waves 1-2 + td-081 verification] Extends td-083 rather than duplicating it: td-083 found the canary-coverage meta-test counts canaries per FILE, so a check added to an already-canaried file is invisible to it. This finding is upstream of that one -- five gates in `fitness/tests/test_discipline_registry_invariants.py` assert inline (directly in a test function body) with no extractable `check_*`/`validate_*` function at all, so they are invisible not just to canary coverage but to gate ENUMERATION itself: nothing can list them as named checks, and td-083's own proposed remedy ('discover check_*/validate_* function definitions... require a canary naming or exercising each') cannot see them either, since there is no function definition to discover. Same root defect one level upstream: extraction discipline (one check = one named function) is not itself enforced. Fix: extract the five inline asserts into named `check_*` functions matching the file's existing convention, so td-083's remedy becomes applicable to all of them. Owner-role context-engineer, matching td-083's precedent for this gate-liveness-mechanism finding family. // Closed by the gate-integrity-and-overlay pipeline, as the upstream half of td-083. Six inline-asserting gates extracted into named module-level `check_*` functions in `fitness/tests/test_discipline_registry_invariants.py` (17 -> 23 named checks, +150 lines), making them visible to gate ENUMERATION and therefore to td-083's per-check canary remedy. NOTE the row's count was off by one in a way that mattered: it is five TESTS but SIX CHECKS -- `test_exactly_one_agent_declares_the_consultant_role` bundled two distinct defect classes (consultant-file-missing, per-discipline-agent-file-leaked) and was split into `check_consultant_agent_file_exists` + `check_no_per_discipline_agent_files`. Extracting it behind a single name would have reproduced td-083's own defect one level down: a name hiding two checks is the same granularity error as a file hiding many. Behaviour preserved (extraction refactor only); 93 passed, 1 skipped. A reciprocal cross-reference between this file's `check_no_data_row_outside_table` and the new append-only gate was added at BOTH sites per the two-textual-sites anti-pattern. | 6dcf83049c9a |
| td-101 | important | other | goals-to-code | rules/swe/id-citation-discipline.md, scripts/check_id_citation_discipline.py | code-quality |  | verifier | 2026-07-31 | 2026-07-31 | systems-architect | resolved | dec-312 | [verifier, multidisciplinary-identities Waves 1-2 + td-081 verification] `scripts/check_id_citation_discipline.py` enforces REQ-/AC-/EC-/Step-N/test-name citation patterns but has NO pattern for `dec-draft-<hash>`, even though `rules/swe/id-citation-discipline.md`'s own lifecycle table lists `dec-draft-<hash>` as 'Never' allowed in code -- a documented prohibition with zero enforcement. Same gap class as a sibling live finding this pipeline hit directly (two dangling `dec-draft-` ids surviving in `tests/` and `fitness/tests/` files, invisible to both `finalize_adrs.py`'s walk scope and this citation gate). Two naive fixes were considered and both are wrong: (1) widening `finalize_adrs.py`'s allowlist to include `tests/`/`fitness/` contradicts that script's own explicit NOTE, which deliberately excludes code from its rewrite scope; (2) a blanket `dec-draft-[0-9a-f]{8}` regex pattern in the citation gate would flag 21 currently-legitimate files -- ADR-parsing scripts and their synthetic test fixtures that construct draft-id-shaped strings on purpose. A sound detector needs to distinguish 'a real draft citation surviving in shipped code' from 'a parser or fixture that legitimately manipulates draft-id-shaped strings,' which is a design decision, not a regex tweak -- why this is filed as a row rather than fixed inline. Owner-role systems-architect: the citation-gate's detection strategy (e.g. an explicit fixture/parser allowlist alongside the pattern, mirroring finalize_adrs.py's own excluded-synthetic-ids list) needs to be decided once, not improvised per gate. // Closed by the gate-integrity-and-overlay pipeline. `scripts/check_id_citation_discipline.py` gained a `draft-adr-id` PATTERNS entry (`\bdec-draft-[0-9a-f]{8}\b`); the designed discriminator is the exact pattern plus the rule's own sanctioned per-line `id-citation-discipline:ignore` marker -- NOT a lexical 'looks synthetic' heuristic, which was rejected for having no statable computed scope and therefore being unscope-checkable under gate-liveness clause 5. Remediation shipped alongside the gate, because a gate the tree fails is not a shipped gate: 12 real dangling citations rewritten to their finalized dec-NNN (each mapping independently verified twice -- semantically against the target ADR's title, and by git-history co-location of the originating fragment), and 11 fixture-literal sites marked with the escape hatch. Two prose sites narrating the historical bug were REWORDED to drop the literal rather than marked, per the rule's 'describe behavior' guidance. Final state: ZERO `[draft-adr-id]` findings repo-wide. Neutering the pattern reddens 10/10 tests. finalize_adrs.py's rewrite allowlist deliberately NOT widened. | 33e989aa5ad9 |
| td-103 | important | coverage-gap | goals-to-code | scripts/finalize_adrs.py, scripts/validate_adr_references.py | adr | dec-310 | orchestrator | 2026-07-31 | 2026-07-31 | cicd-engineer | resolved | dec-313 | Release v0.20.0 shipped .ai-state/decisions/310-sealed-consult-prior.md carrying `id: dec-310` and `status: proposed` -- the finalize hook renamed the fragment to its NNN filename and staged the rename, but left its OWN frontmatter rewrite unstaged, so the promoted ADR self-identified as an unpromoted draft. Sibling files' cross-reference rewrites were staged by name and are correct; only the renamed file's own frontmatter was missed, because `git status` reports it as `RM` -- renamed-and-staged AND content-modified-and-unstaged -- which reads as done. Nothing checks that a finalized ADR's frontmatter id equals the NNN in its filename, nor that no file outside drafts/ carries `status: proposed`; validate_adr_references.py only resolves affected_files. Fixed in the tree; the tag retains it. Proposed gate: assert for every .ai-state/decisions/<NNN>-*.md that frontmatter id == dec-<NNN> and status != proposed, with a canary feeding a renamed-but-unrewritten fixture. // Closed by the gate-integrity-and-overlay pipeline, as gate AND root-cause fix -- the row proposed only a gate, but the cause was reproduced empirically and fixed. Cause: `git mv` stages a rename from the INDEX blob, so `promote_draft`'s write-then-`git mv` order left its own frontmatter rewrite unstaged behind the staged rename (the `RM` shape that reads as done). Fix: `_rename` now calls `_stage_path(dst)` after a successful `git mv`; deliberately best-effort (logs a warning, never raises) because it runs inside the post-merge/post-commit hook chain where raising would abort promotion mid-flight with no recovery path -- so the failure mode degrades to the pre-fix behaviour plus a visible warning. Gate: new `scripts/check_adr_frontmatter_promotion.py` asserts frontmatter `id` == `dec-<NNN>` and `status` != proposed, with a `--staged` mode reading index blobs (wired into pre-commit) because the defect ships a correct working tree over a stale staged blob. Placed in `scripts/` not `fitness/` for scope fidelity: `test.yml`'s `test-root` job has no `paths:` filter, so it fires on a finalize_adrs.py-only PR that architecture.yml would miss. 310/310 ADRs conform. Independently light-reviewed: verdict accept, AC-2 reproduced by the reviewer's own harness. | 982269f7ae61 |
| td-104 | important | coverage-gap | goals-to-code | fitness/tests/test_discipline_registry_invariants.py | adr | dec-299 | orchestrator | 2026-07-31 | 2026-07-31 | test-engineer | resolved | dec-313 | The three .ai-state/CONSULT_*.md files each declare themselves append-only -- CONSULT_LEDGER.md line 14: 'no row is ever edited or deleted after being written. If a disposition is revisited later, append a new row rather than mutating the old one'. NOTHING ENFORCES IT. The phrase appears only in prose and in fitness docstrings; no check compares a row set against its own history. Demonstrated live: during this verification the orchestrator corrected a rationale-ref by MUTATING the row in place, which destroyed the record of what the original disposition pointed at, and every gate stayed green. Restored by reverting the row and appending a superseding one. A gate is feasible and cheap because git holds the history: for each CONSULT_*.md, assert that every data row present in the previous commit is still present verbatim in the working file (deletions and edits both fail; appends pass). Canary: a fixture whose prior revision holds a row the current one has altered. // Closed by the gate-integrity-and-overlay pipeline. New `fitness/tests/test_consult_append_only.py` asserts every data row present at `merge-base(origin/main, HEAD)` is still present verbatim in the working file -- edits and deletions fail, appends pass. Placed in `fitness/` not `scripts/` because only `architecture.yml`'s `fitness-functions` job guarantees `fetch-depth: 0`; in `scripts/` every `git show <baseline>:` call would fail under the shallow default and the gate would degrade to a permanent skip. Given its own file rather than added to the 3014-line registry-invariants file (see td-105). AC-4 exclusion is MECHANICAL, not documentary: `consult_files()` globs `CONSULT_*.md` and structurally cannot match `TECH_DEBT_*.md`, whose contract is the opposite (in-place status/resolved-by/last-seen updates are sanctioned there). REQ-A8: an unreachable baseline returns `None`, structurally distinct from a clean `[]`, so it can never read as a silent pass. Disclosed residual: whole-file extraction (deliberate, to close td-079's outside-the-table blind spot) puts 11 of CONSULT_PRIORS.md's 42 rows in scope, so rewording a column definition reddens the gate. 9 distinct failure shapes canaried, not one shape re-tested. Independently light-reviewed: verdict accept, 2 of 6 neutering counts reproduced exactly. | 3ec1d8a80c79 |
| td-071 | suggested | coverage-gap | code-to-goals | .ai-state/CONSULT_LEDGER.md, .ai-state/observations.jsonl | code-quality |  | orchestrator | 2026-07-30 | 2026-07-31 | systems-architect | resolved | dec-308 | Nothing accumulates a per-consult cost series. observations.jsonl carries no token or cost fields and the CONSULT_LEDGER schema has 11 columns with no cost column, so the AC15 measurement recorded in evidence-dossier section 17.12 rests on figures that existed only because agent completions surfaced them in one session. This is the decay the statistician consult named in CH-04: per-consult cost c is unrecoverable after the fact, unlike k which stays derivable by counting rows. Deliberately NOT resolved unilaterally -- adding a 12th column to a documented append-only ledger whose existing rows carry 11 is a schema decision with a compatibility question (the anchored grep survives ragged rows, markdown renders them, but the documented schema and its consumers would both need updating), and the alternative of a side-record avoids that at the cost of a second artifact. Needs a systems-architect call. Consequence if ignored: AC15 stays a one-observation measurement forever and the cost envelope can never be characterised as a distribution. | 1eaa5307ee4f |
| td-072 | important | drift | code-to-goals | agents/*.md, .claude-plugin/plugin.json | code-quality |  | discipline-consultant | 2026-07-30 | 2026-07-31 | systems-architect | resolved | dec-307 | [evidence-appraiser consult at the research stage, verified independently against the vendor page] All 17 agents declare permissionMode and 13 also declare hooks, and all 17 are registered plugin subagents in plugin.json -- so every one of those declarations is silently ignored. The Claude Code sub-agents page states verbatim: 'For security reasons, plugin subagents don't support the hooks, mcpServers, or permissionMode frontmatter fields. These fields are ignored when loading agents from a plugin.' This affects Praxion and every managed project that installs the plugin, since the plugin path is the only shipping path. Two consequences: any agent whose intended behaviour depends on acceptEdits is not getting it and has never been getting it; and the declarations mislead every reader and every future agent author who copies the pattern. Resolution needs a decision, not just a delete: either strip the inert fields fleet-wide and record why the capability is unavailable, or copy specific agents into .claude/agents/ where the fields are honoured -- the vendor page names that as the supported escape hatch, at the cost of leaving the plugin distribution path. | 9a25540e5d39 |
| td-081 | suggested | coverage-gap | goals-to-code | docs/multidisciplinary-identities-evidence.md, agents/discipline-consultant.md | code-quality |  | discipline-consultant | 2026-07-31 | 2026-07-31 | systems-architect | resolved | dec-310 | [statistician consult CH-06, dispositioned defer-with-rationale, dec-309] dec-306's Falsifier -- compare evidence-appraiser's accepted-challenge rate against what a lens-style always-on reference would have produced -- is **unidentified, not underpowered**: no sample size makes it estimable, because the ledger's eleven columns encode nothing distinguishing a challenge the architect would have caught anyway from one it would not. Remedy named by the consult: a sealed round-0 'already-caught' list -- the convening agent records, before the consultant reports, which concerns it had already identified; the comparison then becomes a matter of set difference rather than counterfactual judgment. Deferred because it changes the consult protocol itself (a new round-0 obligation on the convener), which was outside Wave 2's scope. Until built, dec-306's falsifier must not be cited as live. // RESOLVED 2026-07-31 by dec-310. Shipped: a lens-run prior list sealed in a commit before the spawn, plus a per-challenge classification, so the falsifier's estimand is now a set difference rather than a counterfactual. Two honest limits, both recorded in the ADR rather than smoothed. First, the remedy is TAMPER-EVIDENT, not tamper-proof: every artifact passes through the convener's hands before becoming a commit, which is a property of the trust topology and not of the budget. Second, and this is the wording that matters: dec-306's Falsifier is now INSTRUMENTED FOR THE POST-BOUNDARY SERIES, NOT LIVE. Computable is not decidable -- no rate, n or interval is pre-registered at which anyone concludes anything, and that absence is deliberate, since dec-304 demoted exactly such a threshold on the sibling statistic. Do not cite the falsifier as live. First observation: one consult, novel 4/7, rate 0.57, which at n=1 licenses no verdict in either direction. | fea929fbad6f |
| td-060 | important | complexity | code-to-goals | agents/implementation-planner.md | code-quality |  | orchestrator | 2026-07-30 | 2026-07-30 | context-engineer | resolved | orchestrator (pending-commit) | RESOLVED 2026-07-30: 526 -> 459 lines (41 under the threshold) by progressive disclosure, not deletion -- the agent body was carrying a lossy fork of content already canonical in the software-planning skill. Verified line-by-line: all 70 removed lines accounted for, 8 adjudicated by hand, 4 differing only cosmetically. Two things came out better than they went in: the WIP template gained fidelity (one status value -> five) and parallel-mode fields are now enumerated. 526 lines, past sentinel T03's FAIL threshold of 500, and unfiled until now. Pre-existing: the file was already over before the multidisciplinary-identities pipeline touched it. That pipeline's step 11 deliberately held it at net +0 lines by generalizing an existing intake item rather than appending one, so the debt is neither created nor worsened here. Flagged twice in-flight (by the systems-architect and again by the implementer) but never filed, because both are ledger consumers rather than writers -- filed by the orchestrator at the pre-merge promotion gate. Remediation is decomposition, not trimming: the file carries several distinct concerns (intake, decomposition, supervision, resumption) that could split along those seams. | 2d5eb8a2ca65 |
| td-070 | suggested | coverage-gap | goals-to-code | .ai-state/specs/SPEC_multidisciplinary_identities_2026-07-30.md, skills/multi-perspective-analysis/references/discipline-registry.md | spec-req | REQ-08, REQ-13 | verifier | 2026-07-30 | 2026-07-30 | systems-architect | resolved | orchestrator (pending-commit) | RESOLVED 2026-07-30: AC13 exercised and PASSED once evidence-appraiser shipped as discipline #2. Two different disciplines ran concurrently against different targets; separate fragments, neither read the sibling or the draft, and both appended to one PROGRESS.md with the four entries interleaving and no corruption. Stronger result than the AC required: the statistician/evidence-appraiser boundary held with ZERO overlap -- not one challenge from either would have belonged in the other's fragment. Recorded in the evidence dossier section 17.14. [W-02 from multidisciplinary-identities verification] AC13 (two DIFFERENT disciplines running concurrently, each writing its own CONSULT_<discipline>.md with no shared-file write and no cross-read during round 0) was never exercised and is untestable by construction at a one-discipline roster. Deferred, NOT failed -- the multi-instance design is asserted by the fragment-per-discipline file convention and the single-owner reconciliation rule, but no run has ever demonstrated it. Filed because the verification report that recorded it is ephemeral and was deleted at pipeline cleanup, leaving the finding with no durable home. Resolution: exercise AC13 as the FIRST act of Wave 2, before the second discipline is trusted in a real pipeline -- a concurrency contract assumed for a full wave and then found broken is materially more expensive than one probed on day one. | 71c284e76474 |
| td-062 | suggested | coverage-gap | code-to-goals | agents/verifier.md | code-quality |  | orchestrator | 2026-07-30 | 2026-07-30 | verifier | resolved | orchestrator (pending-commit) | RESOLVED 2026-07-30, both halves. The original objection -- wiring a gate with no checklist behind it -- was honoured by fixing its cause rather than routing around it: skills/agentic-transactions/references/design-review-checklist.md now exists (133 lines, authored by the transactions domain owner, matching the four sibling checklists' shape), and agents/verifier.md reads it on demand for TRANSACTIONS_DESIGN.md. The checklist makes two failure modes explicit that a generic review misses: an irreversible operation with no human gate and no written exemption, and a guardrail whose sole enforcement is prompt text (the boundary check must re-derive the amount from the outgoing request, not trust a model-supplied field). It also declines to checkbox three things that resist objective evaluation -- whether a ceiling is the right number, whether the operation should be agentic at all, and counterparty trust -- routing them to narrative findings instead. Verified the design template asserts supports_sandbox and approval_mode, so the items are evaluable; absent fields are WARN(unevaluable), never PASS. PARTIALLY ADDRESSED 2026-07-30: the disposition half is wired -- agents/verifier.md's specialist-review section now names TRANSACTIONS_DESIGN.md alongside INTERFACE_DESIGN.md and checks switch-now challenges from a specialist doc's Architecture Challenges section, a mechanical shape already proven to bite via the CONSULT path and the sentinel P07 fixtures. The original objection is narrowed, not overridden: the checklist half stays unwired and the agent is now explicitly instructed NOT to invent one, because a gate with no proof it bites is worse than a declared gap. REMAINING: author skills/agentic-transactions/references/design-review-checklist.md (the other four interface skills each have one), then wire it. The specialist-review section covers INTERFACE_DESIGN.md and CONSULT_<discipline>.md but not TRANSACTIONS_DESIGN.md, so the transactions architect's output reaches the planner (fixed during this pipeline) but is still never verified. Deliberately not patched: the section's procedure is to run the artifact's design-review-checklist.md, and no such checklist exists for the transactions domain, so wiring it now would create a gate with no proof it bites. Resolution requires authoring the checklist first; filing the row so the gap is tracked rather than rediscovered. | ea7dfe5bcafb |
| td-061 | suggested | staleness | goals-to-code | rules/README.md | code-quality |  | orchestrator | 2026-07-30 | 2026-07-30 | doc-engineer | resolved | orchestrator (pending-commit) | RESOLVED 2026-07-30: replaced the hardcoded count with count-independent phrasing ('one row per agent') rather than bumping 13 to 17 -- the original finding named recurring count-drift beside a growing table as the actual shape, so a corrected number would just drift again at the next agent. The model-tier table prose states it covers 13 agents; the table has 16 rows, and 17 once the discipline-consultant row lands. Pre-existing drift, widened rather than caused by this pipeline. A hardcoded count in prose beside a table that grows is a recurring shape in this repo -- see also the two '16 agents' strings in docs/architecture.md -- and is worth a mechanical check rather than repeated manual correction. | b427680e6411 |
| td-063 | important | correctness | code-to-goals | scripts/finalize_adrs.py | code-quality |  | orchestrator | 2026-07-30 | 2026-07-30 | implementer | resolved | orchestrator (1b136c7) | RESOLVED 2026-07-30 by commit 1b136c7, which implemented exactly the fix prescribed here: separator-normalized slug matching before the SPEC_* comparison, plus .ai-state/CONSULT_LEDGER.md added to the named persistent-file allowlist. Two regression tests added, each confirmed to fail against the previous implementation. NOT implemented: the optional third clause -- warning when a task-slug-shaped spec exists but matches nothing. PROCESS NOTE: this row already existed and prescribed the fix; the same two bugs were independently rediscovered and re-derived hours later because the ledger was not consulted before fixing. The ledger was right and unread. The bounded rewrite scope silently missed two files at a live merge. First, the SPEC_* entry matches on the task slug, but slugs are hyphenated (multidisciplinary-identities) while spec filenames are underscored (SPEC_multidisciplinary_identities_YYYY-MM-DD.md), so the match never fires -- 26 draft ids were left dangling in a committed spec. Second, .ai-state/CONSULT_LEDGER.md postdates the allowlist and is not in it. Both were repaired by hand this pass. The failure mode is the one finalize exists to prevent, and it is silent: no warning is emitted for a file the walk never considered, so a dangling id surfaces later as an unresolvable reference. Fix is slug normalization before the SPEC_* match, plus adding the ledger; consider warning when a task-slug-shaped spec exists but matches nothing. | 47ccc3df6488 |
| td-069 | important | complexity | code-to-goals | dashboard_app/src/components/metrics-dashboard.tsx | code-quality |  | sentinel | 2026-07-30 | 2026-07-30 | implementation-planner | resolved | orchestrator (pending-commit) | RESOLVED 2026-07-30: decomposed 459 lines into a 133-line composing container plus 4 presentational siblings (health-strip, hotspots-table, collector-chips, raw-data) and a pure lib module (metrics-dashboard-data.ts) holding the derivations. Aggregate complexity 61 -> avg CCN 3.3 across 24 functions; the container itself 26 -> 17. Pure structural change, zero behaviour edits: 472/472 dashboard tests unchanged from baseline, tsc clean, next build green across all 12 routes. RESIDUAL, stated rather than hidden: the container still trips lizard's CCN>15 warning at 17. The remainder is inherent layout branching (render-section-if-data guards plus two empty-state early returns); extracting section wrappers purely to move those across a file boundary would lower the number without improving readability, so it was declined. Note also that the hotspot score is churn-dominated -- the split redistributes churn across 6 files rather than eliminating it, so the wins are review surface, test isolation, and merge-conflict reduction, not a metric going to zero. ORIGINAL FINDING: Rank-1 hotspot (score=111,203) by a wide margin over rank-2 (47,840): complexity=61 (lizard), churn=1,823 over 90d, 459 lines. This file has been the #1 hotspot since at least May 2026 (named in td-031's resolution rationale as "the #1 hotspot" three months ago) but has never received its own ledger row -- prior sentinel passes filed sibling dead-code cleanups (td-031/td-032) against it without addressing the complexity itself. Sustained top-rank churn x complexity over 3+ months signals a module-split/decomposition candidate (same class as td-058/td-012/td-054), not routine dashboard iteration. Elevated to important because it is the single largest hotspot on record, not a borderline p95 crossing. | 003f3b731260 |
| td-067 | important | coverage-gap | goals-to-code | agents/skill-genesis.md, .ai-state/specs/SPEC_multidisciplinary_identities_2026-07-30.md | spec-req | REQ-18 | verifier | 2026-07-30 | 2026-07-30 | systems-architect | resolved | orchestrator (pending-commit) | RESOLVED 2026-07-30: shipped the recording half only -- a fourth triage leaf in agents/skill-genesis.md records discipline-gap signals into a Discipline-Gap Signals report section for human disposition, explicitly forbidden from proposing a discipline or drafting a registry row. Tier-3 identity genesis stays gated per dec-300; the deferral was of genesis, and REQ-18 clause 2 asks for the anti-genesis behaviour, so the two do not conflict. Resolves the circularity noted at filing: the Tier-3 gate is denominated in evidence nothing was collecting. [W-03 from multidisciplinary-identities verification] REQ-18 clause 2 is written in the present tense as shipped behaviour -- 'when a recurring we-needed-discipline-X signal appears, the system records it for human disposition' -- but nothing implements it: grep for a discipline path in agents/skill-genesis.md returns nothing, and dec-300 deliberately deferred Tier-3 identity genesis behind a >=20-ledger-row gate. The deferral is coherent; the present-tense REQ is not. Two honest resolutions: implement the recording leaf, or amend the REQ to state the clause as deferred. The SPEC text has been annotated to name the deferral so the REQ list and the shipped tree no longer silently disagree, but the underlying choice is a systems-architect call at Wave 2. Consequence if ignored: the Tier-3 gate has no data-collection path feeding it, so the >=20-row threshold can never be reached by the mechanism it is meant to gate. | 51e403e88dfe |
| td-065 | suggested | drift | code-to-goals | agents/sentinel.md | code-quality |  | verifier | 2026-07-30 | 2026-07-30 | context-engineer | resolved | orchestrator (pending-commit) | [F-02 from multidisciplinary-identities verification] P07's canonical check-table row (line 159) was correctly extended to cover CONSULT_<discipline>.md with two golden bad-cases and two no-false-positive controls, but the Pass-1 dispatch paragraph (line 413) still scopes P07 to INTERFACE_DESIGN.md/TRANSACTIONS_DESIGN.md only. Not inert -- the row is authoritative -- but it is a narrower duplicate of the same scope one screen away, and P07's failure mode is silence, which is why that step carried review: force. Same shape as the Available-Agents table row asymmetry: a paired contract updated at one of two sites. Fix is one line at 413. | e9fa73702d3e |
| td-068 | important | other | code-to-goals | rules/CLAUDE.md, agents/sentinel.md, CLAUDE.md | code-quality |  | verifier | 2026-07-30 | 2026-07-30 | context-engineer | resolved | orchestrator (pending-commit) | RESOLVED 2026-07-30: basis settled by direct observation of a live session's loaded context -- rules/README.md is a catalog, is NOT injected, and must be excluded; authoritative basis is now stated in rules/CLAUDE.md and sentinel T02's command was rewritten to compute exactly that set (it previously swept in paths:-scoped rules, dropped the global CLAUDE.md, and missed rules/ml, rules/eval, rules/writing). Correct total ~23,060 tok @3.6 -- under the 25,000 ceiling. [W-04 from multidisciplinary-identities verification, pre-existing] The always-loaded token budget has three coexisting measurement bases that disagree materially, and they straddle the 25,000-token ceiling: the pipeline basis (project + global CLAUDE.md + paths:-less rules) gives ~23,061 tok at 3.6 chars/tok and passes; including rules/README.md -- which has no frontmatter and is therefore always-loaded by rules/CLAUDE.md's own stated rule -- gives ~27,631 and fails; sentinel T02's literal command (cat CLAUDE.md rules/swe/*.md rules/swe/vcs/*.md rules/CLAUDE.md, /3.5) sweeps in paths:-scoped rules while dropping the global CLAUDE.md and rules/README.md, giving ~33,013. Every budget PASS in this repo's history is therefore basis-dependent, and no single authority defines the basis. Consequence if ignored: the guardrail cannot be trusted in either direction -- a real overrun reads as a pass under one basis, and a healthy budget reads as a failure under another. | dca774790643 |
| td-066 | important | coverage-gap | goals-to-code | .ai-state/specs/SPEC_multidisciplinary_identities_2026-07-30.md | spec-req | REQ-01..REQ-18 | verifier | 2026-07-30 | 2026-07-30 | implementation-planner | resolved | orchestrator (pending-commit) | [F-01/F-03 from multidisciplinary-identities verification] RESOLVED 2026-07-30: the reconstructed coverage table was folded into the archived SPEC (§ Test coverage), and the plan-local I2/I4 ids were stripped from the ledger header and all 13 fitness-test sites. traceability.yml shipped with 18 of 19 tests: arrays and 18 of 19 implementation: arrays empty; the implementer fragment was never merged and no test-engineer fragment was ever written despite 22 test functions landing. The archived SPEC's matrix has no test column, so the REQ->test mapping exists nowhere in the repo and /sdd-coverage reads emptiness -- the gate-liveness 'consumer with no producer' anti-pattern, discharged silently at archival. The mapping was reconstructed by hand in VERIFICATION_REPORT.md section 2 (3 REQs test-covered, 5 partial, 6 live-demonstrated only, 4 code-only, 1 prose-only, 1 unimplemented); remediation is to fold that table into the archived SPEC. Also carries the id-citation hygiene fix: strip the plan-local I2/I4 SYSTEMS_PLAN interface ids from .ai-state/CONSULT_LEDGER.md line 12 and eight fitness-test docstring sites -- a permanent file citing an ephemeral one, invisible to check_id_citation_discipline.py which only detects REQ-/AC-/Step N/dec-draft-. | e9579c3f40e4 |
| td-055 | suggested | other | goals-to-code | commands/onboard-project.md | claude-md |  | orchestrator | 2026-07-16 | 2026-07-16 | implementation-planner | resolved | orchestrator (4a40d01) | Onboarded CLAUDE.md blocks never refresh: Phase 6's idempotency predicate is per-heading (heading present → skip append), so canonical-block updates (e.g. the td-002 agent-pipeline trim, dec-270) never propagate to already-onboarded projects, and appended blocks carry no version marker to distinguish stale-canonical from locally-modified content. Fix sketch: (1) append blocks with an inner marker comment carrying the canonical content hash; (2) add a refresh mode to /onboard-project (or a --refresh-blocks flag) that on heading-present compares the body — replace when it matches any historical canonical version, surface a diff when locally modified; (3) pre-marker projects are matched against the canonical file's git history (the one-off estate refresh of 2026-07-16 used exactly this). // 2026-07-16 (pipeline block-refresh-mechanism): implementation complete in worktree — manifest generator + --check-history pre-commit gate, shared identity module, refresh_claude_blocks.py three-way classify/apply with refuse-to-clobber canary, /refresh-claude-blocks command, Phase 6 delegation, docs. Flips to resolved with the merge sha at merge-to-main. // 2026-07-16: in-flight via block-refresh-mechanism pipeline. Design chose MANIFEST-ONLY (dec-271) over the sketch's in-file marker (step 1 dropped) — a shipped git-derived historical-hash manifest classifies all estates marker-free, meeting the sketch's intent (steps 2+3) at zero always-loaded token cost. Resolve to resolved-by <merge-sha> at implementation. | 96681362dd98 |
| td-002 | important | other | code-to-goals | claude/canonical-blocks/agent-pipeline.md | code-quality |  | orchestrator | 2026-04-27 | 2026-07-16 | implementation-planner | resolved | orchestrator (bac3ba3) | Agent-pipeline canonical block is ~1818 bytes (~505 tokens), exceeding per-block ~250-token budget by ~2x; pre-existing condition surfaced during td-001 refactor, not introduced by it. Trimming changes downstream injection contract — needs content review and separate ADR. Currently xfailed in `hooks/test_onboard_praxion_block.py::test_block_stays_within_token_budget[agent-pipeline]`. Filed by orchestrator mid-pipeline (slight protocol stretch — canonical sources are verifier and sentinel); verifier may re-source. Class `other` because no enum slot covers content-size-budget violations. // 2026-05-17: re-verified by sentinel audit + orchestrator sweep — the block has grown to 1,921 bytes / ~549 tokens (was ~1,818 B / ~505 tokens at filing); still ~2x the per-block ~250-token budget, and the debt is widening rather than holding. The xfail reason string in `hooks/test_onboard_praxion_block.py` still cites the stale ~1,818 B figure — worth refreshing when the block is next touched. // 2026-07-16: RESOLVED (dec-270) — block restructured to a ~738 B / ~205-token pointer (universal framing + prose pointer to the coordination rule and software-planning skill + absolute docs URL); the PoC-journey paragraph relocated to software-planning SKILL.md § End of Feature (only non-derivable content); xfail removed, token-budget test green for all four blocks. Context-engineer audit found ~70-80% of the old block redundant with always-loaded rules on Praxion machines and inert without them. Refresh of already-onboarded projects tracked as td-055. | deee2e5a8b62 |
| td-049 | suggested | coverage-gap | code-to-goals | hooks/inject_worktree_paths.py, hooks/inject_subagent_context.py, hooks/hooks.json | code-quality |  | orchestrator | 2026-07-01 | 2026-07-02 | verifier | resolved | orchestrator (dec-266, fe6a1db) | [debt-batch-2] Two PreToolUse(Agent) hooks now co-register and can BOTH emit updatedInput (inject_subagent_context for host-native agents; inject_worktree_paths for any agent when the session is in a linked worktree). Whether the harness CHAINS updatedInput across multiple matching hooks (vs first- or last-writer-wins, silently dropping one injection) is an unverified hook-harness contract assumption — the exact class that produced the td-021 envelope breakage. Filed by orchestrator from a standalone session under the dec-261-widened exception. Resolution: live probe from a fresh worktree session with the updated plugin installed — spawn a host-native agent and assert BOTH the Praxion preamble and the worktree-root briefing line arrive in its prompt; if not chained, merge the two injections into one hook. // 2026-07-02: RESOLVED by elimination per dec-266: both injections consolidated into inject_subagent_context.py as the single PreToolUse(Agent) updatedInput emitter — the multi-hook chaining question is structurally moot (inject_worktree_paths.py deleted; hooks.json row removed, 29→28). Live end-to-end proof via dev-linked cache: a host-native Explore probe quoted the composed injection verbatim. | f7b1e401aa85 |
| td-050 | important | other | code-to-goals | dashboard_app/src/components/viz/diagram-modal.tsx | code-quality |  | verifier | 2026-07-02 | 2026-07-02 | implementer | resolved | implementer (7099578) | [dashboard-debt-trio verification] Pre-existing WCAG 2.2 AA defect, out of the debt-trio's scope. `DiagramModal`'s outer backdrop `<div className="diagram-modal__backdrop" ... aria-hidden="true">` (diagram-modal.tsx:126-130) wraps the `<div role="dialog" aria-modal="true" aria-labelledby=...>` (lines 132-138) as its child. `aria-hidden="true"` on an ancestor removes the entire subtree from the accessibility tree, so the dialog — despite correct `role="dialog"`/`aria-modal`/`aria-labelledby`/focus-trap — is invisible to assistive technology while open. Surfaced during the td-024 viz render-tests: `screen.getByRole("dialog")` finds nothing after the Expand click; only `getByRole("dialog", { hidden: true })` or a raw `querySelector` locates it, and the new `diagram-frame.test.tsx` uses `{ hidden: true }` with an inline note to route around the bug rather than mask it. The pre-existing node-env test `tests/server/diagram-frame.test.ts` never caught it because it string-matches raw HTML without evaluating ARIA visibility. Resolution: drop `aria-hidden="true"` from the backdrop (the dialog's own `aria-modal="true"` already conveys modal semantics), or restructure so the backdrop is a sibling of the dialog rather than its ancestor; then tighten the render assertion to `getByRole("dialog")` without `{ hidden: true }`. Class `other`: no enum slot covers accessibility-conformance defects. // 2026-07-02: RESOLVED: DiagramModal restructured so backdrop and dialog are siblings (aria-hidden no longer an ancestor of role=dialog); regression test proven red on unfixed code (getByRole dialog threw) then green + aria-modal asserted; dashboard suite 457/457, stacking/centering preserved via CSS split. | 5c100a6a0a16 |
| td-051 | suggested | coverage-gap | code-to-goals | hooks/inject_worktree_paths.py, agents/*.md briefing discipline | code-quality |  | orchestrator | 2026-07-02 | 2026-07-02 | implementation-planner | resolved | implementer (fe6a1db, dec-266) | [dashboard-debt-trio verification WARN-2] The cwd-bifurcation direction NOT covered by inject_worktree_paths.py: a CANONICAL-checkout session briefing an agent INTO a worktree gets no deterministic path injection (the hook fires only when the spawning session itself is in a linked worktree) — live incident: a doc-engineer edited+committed in canonical despite an absolute-worktree-path briefing; caught by a sibling step's cross-check, commit relocated via cherry-pick + soft-reset. Verifier recommended extending td-034; filed as a NEW row instead because td-034 is terminal and the ledger is append-only (deviation registered). Resolution: extend the hook (or a sibling) to fire when the spawned prompt names a worktree root unequal to the session root; interim mitigation — route cross-tree-briefed steps to sonnet+, never haiku. Cross-refs: td-034 (resolved), td-049 (chaining probe). // 2026-07-02: RESOLVED: the consolidated hook adds briefed-root detection — when the outgoing prompt names a .claude/worktrees/<name> path unequal to the session root, a [Worktree briefing] absolute-path mandate is injected for ALL agent types. Canary proves gate independence (canonical fixture + named worktree → briefing fires, session-line absent); LIVE-verified same-session via dev-link + Explore probe quoting the line verbatim. | 222fe6b1a941 |
| td-052 | suggested | other | code-to-goals | scripts/finalize_adrs.py | code-quality |  | orchestrator | 2026-07-02 | 2026-07-02 | implementer | resolved | implementer (01c826e) | [dashboard-debt-trio merge] finalize_adrs fragment-filename parser mis-split a fragment whose USER slug is itself hyphenated (francisco-perez-sorrosal vs the expected fperezsorrosal) EVEN THOUGH the branch: frontmatter disambiguator was present (branch: dashboard-debt-trio) — promoted slug came out as sorrosal-dashboard-debt-trio-renderer-field-resolver-key; repaired by manual rename to 265-renderer-field-resolver-key.md + index regen. The branch-frontmatter path apparently is not consulted (or not preferred) for the user/slug split. Same ambiguity class as the td-017 history. Resolution: make the parser strip a matched branch: value from the filename FIRST, then split user/slug; add a hyphenated-user-slug regression test; consider validating fragment author slugs at creation. Class other: filename-parsing defect. // 2026-07-02: RESOLVED: root cause — the branch: frontmatter check was nested inside the user-hint-match branch, so nonconforming hyphenated user slugs bypassed it; fixed as an unconditional Tier-0 strip of the branch token run before the user/slug split. Red-then-green on the exact historical filename; conforming-path behavior characterized unchanged; 46/46 parser tests. | 2a3f1902df1b |
| td-024 | suggested | coverage-gap | code-to-goals | dashboard_app/tests/ | code-quality |  | orchestrator | 2026-05-11 | 2026-07-02 | test-engineer | resolved | test-engineer/implementer (dashboard-debt-trio) | React component render tests (jsdom + `@testing-library/react`) deferred — no jsdom dep in v1. v1 covers server view-models + pure parsers/helpers + `usePanZoom` math via synthetic events. Resolution: `pnpm add -D jsdom @testing-library/react @testing-library/user-event` + vitest jsdom env config + tests for DiagramViewer, DecisionGraph, TrendChart, shells. User-confirmed defer 2026-05-11. // 2026-07-02: RESOLVED by the dashboard-debt-trio pipeline: jsdom + @testing-library infra via per-file @vitest-environment docblocks (zero config bifurcation, zero server-test exposure); render/interaction tests for DiagramFrame/DecisionGraph/TrendChart + shells + renderers; suite 404 -> 447. Known jsdom limits documented (recharts hover untestable -> legend-click interaction substituted). | 19de3aa2bd74 |
| td-025 | suggested | other | code-to-goals | dashboard_app/src/components/shells/ | code-quality |  | orchestrator | 2026-05-11 | 2026-07-02 | implementation-planner | resolved | implementer (dashboard-debt-trio) | Dedicated shell chrome for `tutorial`, `how-to`, and `concepts` Diátaxis types deferred. Currently these three types alias `DefaultShell`. Resolution: build dedicated layout components (tutorial: step-numbered layout; how-to: goal + steps layout; concepts: narrative with key-takeaway callout) and register them in `RENDERER_REGISTRY`. Class `other`: no enum slot covers deferred-feature gaps. // 2026-07-02: RESOLVED: TutorialShell (ordered step-rail), HowToShell (goal banner), ConceptsShell (key-takeaway aside) are real components with distinguishing-element render tests; shells/index.ts aliases flipped; registry untouched by design (keys already resolved via diataxis lookup). | bfd32a2c3308 |
| td-027 | suggested | other | code-to-goals | dashboard_app/src/components/registry.ts | code-quality |  | orchestrator | 2026-05-11 | 2026-07-02 | implementation-planner | resolved | implementer (dashboard-debt-trio) | The Streamlit `metrics_view` / `plan_view` / `verification_report` / `idea_grid` / `architecture_explorer` per-artifact renderers have no Next.js equivalents yet. v1 ships `reference` + `explanation` + `default` Diátaxis shells only. Resolution: build dedicated renderer components and register them in `RENDERER_REGISTRY`. Class `other`: no enum slot covers deferred-feature gaps. // 2026-07-02: RESOLVED per dec-265: five per-artifact renderers in src/components/renderers/ wired via the manifest renderer: field as highest-priority resolveRenderer key; metrics_view and architecture_explorer are thin previews linking to the existing /metrics and /architecture routes (rows' reimplementation premise was stale); malformed-input -> DefaultShell fallback tests bite. | 7c081da912fd |
| td-029 | suggested | other | code-to-goals | dashboard_app/src/server/view-models/documentation.ts | code-quality |  | orchestrator | 2026-05-11 | 2026-07-01 | systems-architect | resolved | implementer (29add49) | `assertContainedProjectPath` is intentionally wide for documentation surfaces (any file in project root). A compromised `doc_manifest.yaml` could expose `src/` or `.env` files. Resolution: add a secondary allowlist for doc surfaces restricting paths to known-safe subdirectories (e.g., `docs/`, `.ai-state/`). Class `other`: no enum slot covers security-hardening deferrals. // 2026-07-01: RESOLVED: assertAllowedDocSurface secondary gate after containment (docs/, .ai-state/, .ai-work/, root *.md, manifest API-spec dirs mirrored from build_doc_manifest); reject canaries for src/, .env, path-escape; 413-test package suite green. | 6ef9aa858393 |
| td-034 | important | other | code-to-goals | hooks/worktree_guard.py, hooks/inject_worktree_banner.py, agents/implementer.md, agents/doc-engineer.md, agents/test-engineer.md | code-quality |  | orchestrator | 2026-05-13 | 2026-07-01 | implementation-planner | resolved | implementer (c55d044) | Standard/Full-tier pipelines that run in a worktree (per `swe-agent-coordination-protocol.md` § Pipeline Isolation) can have spawned subagents (implementer, test-engineer, doc-engineer, etc.) emit `Write`/`Edit` calls that land in the canonical checkout instead of the briefed worktree. Reproduced 2026-05-13 in the `rules-reorg-blacklist` pipeline: Phase A implementer received an explicit worktree-cwd brief with a `pwd && git rev-parse --abbrev-ref HEAD` pre-flight, reported success, but Phase A's 21 rule-file edits landed in `/Users/fperez/dev/praxion/rules/` instead of `/Users/fperez/dev/praxion/.claude/worktrees/rules-reorg-blacklist/rules/`. The orchestrator session DID stay correctly in the worktree and could observe the divergence via `git -C <main> status` — but only after the user signaled the mismatch. The PreToolUse `worktree_guard.py` hook is documented (CLAUDE.md § Pipeline Isolation) as blocking `Write`/`Edit` resolving outside the session worktree, yet did not intercept the subagent's calls. Three competing hypotheses for the root cause (none ruled in/out by this observation): (a) subagent sessions do not inherit the orchestrator's PreToolUse hooks (hook-scope gap), (b) the subagent's path-resolution layer reports the worktree path while the actual filesystem write resolves to the canonical checkout (path-resolution bifurcation between `pwd` and Edit-tool target), (c) the hook's path-containment check has a gap (e.g., relative-path normalization). Recovery is mechanical (sync main → worktree, then `git -C <main> checkout -- rules/`) but expensive in pipeline-cycle terms (one wasted phase of work + manual reconciliation + lost cache warmth on the orchestrator side). Recurring failure mode: saved auto-memory `feedback_implementer_cwd_in_worktrees` already records this with a verify-pwd-and-git-status mitigation, but the mitigation is REACTIVE (catches after damage) not PREVENTIVE. Resolution path: (1) write a controlled-test hook script that spawns a minimal subagent in a worktree session, has it attempt an Edit outside the worktree, and asserts `worktree_guard.py` fired; if the hook doesn't fire, that confirms hypothesis (a) or (c). (2) If subagents bypass the orchestrator's hooks: either register `worktree_guard.py` in subagent-inheritable settings OR add a SessionStart hook for subagents that asserts cwd matches the briefed worktree-root, abort on mismatch. (3) Add a per-tool absolute-path-required preflight to subagent system prompts when launched into a worktree pipeline. (4) Until structural fix lands: orchestrators should brief subagents with absolute paths only (worktree-rooted), not relative paths — relative paths silently resolve against whatever cwd the subagent's tool layer chooses. Class `other` because no enum slot covers cross-session-hook-scope or path-resolution-bifurcation gaps (same precedent as td-021, td-033). Owner-role `implementation-planner` because resolution requires a hook-scope architecture decision + a new SessionStart hook + a settings-inheritance change. // 2026-05-23: ASSESSED — substantial defensive work already shipped: ADR `dec-180` (accepted) mandates a controlled-test harness as a ship gate; `hooks/test_worktree_guard_subagent.py` exists but its automated Path A is SKIPPED (the `Agent` tool is not callable from a pytest context), falling back to `hooks/MANUAL_VERIFICATION.md`. `hooks/worktree_guard.py` enforces only on ABSOLUTE paths *by design* — the relative-path skip is intentional and tested (`test_ignores_relative_path`), so hardening it contradicts dec-180's tested contract and would need its own ADR (not a free win). Defense-in-depth absolute-path briefing in `/resume-rework` is in place and tested. The CORE open question — whether subagents inherit the orchestrator's PreToolUse hooks (hypothesis a) vs a path-resolution/relative-path gap (b/c) — remains empirically unverified: confirming it needs a live subagent spawn in a worktree, which is BLOCKED this session by the td-021 spawn crash (a `general-purpose` probe crashed with the `K.length` signature). Same subagent-runtime family as td-021/td-033/td-035. No productive, non-speculative code change is available without live verification; next step is to run the harness's Path A in a fresh session where spawning works (or an upstream determination of subagent hook inheritance). Held OPEN. // 2026-05-23: BLOCKER DOWNGRADED — the td-021 spawn crash is builtin-agent-specific, not a hard wall: a spawned `i-am:researcher` ran cleanly this session (where `general-purpose` crashed 2/2), so Path A (spawn a subagent in a worktree, have it Edit outside the worktree, assert `worktree_guard.py` fires) is runnable via an i-am-type agent — no td-021 fix needed. Still OPEN: Path A has not actually been executed (needs a worktree session + an i-am-subagent Edit-outside-worktree probe); the hook-inheritance-vs-path-resolution question (hypothesis a/b/c) is unresolved until that test runs. Confidence that it's tractable is now high. // 2026-06-16: PATH A EXECUTED LIVE — SHIP GATE PASSES. Spawned an `i-am:implementer` from a linked worktree session and had it `Write` a NEW file at an absolute path in the MAIN git tree; `hooks/worktree_guard.py` PreToolUse:Write hook FIRED on the SUBAGENT's call — `[worktree-guard] BLOCKED: cross-worktree write ... resolves in different git tree: /Users/fperez/dev/praxion`, exit 2, target never created. This REJECTS hypothesis (a): subagents DO inherit the orchestrator's PreToolUse hooks. Defense-in-depth also observed — the permission layer denied subagent Read/Edit of outside-worktree paths BEFORE the guard ran (an Edit probe to /tmp and a Read of a main-tree file were denied generically; /tmp is non-git so the guard intentionally skips it per its tested contract). Guard logic independently unit-verified (direct payload → exit 2 + correct message). Residual exposure NARROWED to the original incident's actual mechanism: RELATIVE paths (guard skips by design, dec-180) + subagent cwd-bifurcation (cwd resolves to canonical checkout) — mitigated by absolute-path-only briefing, already standard practice. Hook-inheritance question RESOLVED; only the relative-path+cwd-bifurcation preventive-vs-documented follow-up remains. // 2026-07-01: RESOLVED: residual relative-path/cwd-bifurcation exposure now has a deterministic preventive layer — new inject_worktree_paths.py PreToolUse(Agent) hook (Option A; worktree_guard's dec-180 contract untouched) prepends the absolute session-worktree root + absolute-path mandate to every subagent prompt when the spawning session is in a linked worktree; canary + suppression + fail-open tests (16). One follow-up assumption extracted to td-049 (multi-hook updatedInput chaining). | dea9786fa0dd |
| td-036 | suggested | other | code-to-goals | install.sh | code-quality |  | verifier | 2026-05-15 | 2026-07-01 | implementation-planner | resolved | implementer (dcf14d5) | `bash install.sh` performs a marketplace fetch via `claude plugin install` rather than copying local files into the installed plugin cache (`~/.claude/plugins/cache/bit-agora/i-am/<version>/`). Praxion contributors editing hooks, scripts, or commands locally cannot test their changes against `claude --bg`-launched sessions or other plugin-cache-resolved code paths without manually `cp`-ing files into the cache between iterations. Step 10 and Step 11 of the dispatch-reworks Phase 2 pipeline both used manual `cp .../hooks/notify_bg_session_state.py ~/.claude/plugins/cache/bit-agora/i-am/0.2.0/hooks/` workarounds during live integration testing, which is fragile (forgettable, easy to mis-target by version) and bypasses the install-time validation chain. Resolution path: add a `bash install.sh --dev-link` (or `--editable` / `--symlink`) mode that creates symlinks from the installed plugin cache back to the worktree's source files, so local edits take effect immediately. Alternative: a `praxion-claude-dev` extension that pre-pends the worktree's hooks/commands/scripts dirs to whichever lookup paths Claude Code consults (similar to the existing `--plugin-dir` flag but for the hook subsystem). Class `other`: no enum slot covers tooling-developer-experience gaps. // 2026-07-01: RESOLVED: install.sh --dev-link / --dev-link=off symlinks the pinned cache's hooks/, scripts/, commands/ to the working tree (17 hermetic tests incl. refusal canaries); README_DEV manual-cp workaround replaced; motivated same-day by the td-021 live-fix requiring a manual cp. | 064319b4dfb8 |
| td-021 | important | other | code-to-goals | CLAUDE.md, rules/swe/swe-agent-coordination-protocol.md | code-quality |  | orchestrator | 2026-05-08 | 2026-07-01 | doc-engineer | resolved | orchestrator (1c9183c) | Claude Code shipped `Explore` subagent (Claude Code 2.1.136) crashes during initialization with `undefined is not an object (evaluating 'K.length')` when spawned from a Praxion session. Reproducible: a 50-word minimal prompt (`Find /tmp/x, count lines, return first three`) fails identically — NOT prompt-size dependent. Failure mode: orphaned-tool-start in the chronograph observability panel — input context is packaged and billed (~55K tokens/call observed by operator on 2026-05-08), output never returns. Distinct from upstream issue #38868 (CLOSED, 2.1.83) which reported a graceful "Prompt is too long" / AxiosError surface for the same trigger condition (many plugins/skills/MCPs injecting context); our failure is a silent JS crash with no recoverable error message. Praxion `i-am:researcher` (3rd attempt in the same session) handled the same workload cleanly. Token cost (operator-reported, not directly visible in `.ai-state/observations.jsonl` schema). Resolution path: (a) document workaround in CLAUDE.md `Known Claude Code Limitations` (this commit) — prefer `i-am:researcher` for substantive inventory; (b) add note in `rules/swe/swe-agent-coordination-protocol.md` to scope when Explore is reasonable vs when to skip; (c) upstream bug filed at [anthropics/claude-code#57493](https://github.com/anthropics/claude-code/issues/57493) on 2026-05-08, referencing #38868, #43983, #30620. Distinct from td-018 (token-budget) — this is a hard JS crash, not a budget overflow, even though both surface in many-skills environments. // 2026-06-16: ROOT CAUSE FOUND (Praxion-side contributor) + SOURCE FIX. A live `Explore` probe from a Praxion session failed with `PreToolUse hook for Agent returned updatedInput that failed schema validation: required parameter description is missing` — NOT the K.length JS crash, a different (more concrete) signature. Traced to `hooks/inject_subagent_context.py` `_emit_updated_input`, which rebuilt `tool_input` from scratch with only `{subagent_type, prompt}`, DROPPING the Agent tool's required `description` field (and `model`/`run_in_background`). Fires only on host-native agents (Explore/Plan/general-purpose); `i-am:*` agents are skipped by gate (d), which is why every `i-am:*` spawn worked all along. Fix: preserve the full `tool_input`, override only `prompt`; canary test `test_output_preserves_description_and_other_tool_input_fields` added (36/36 pass, ruff clean) — fails on the old reconstruct-from-scratch code, passes on the fix. NOT yet live-verified: the running session executes the INSTALLED hook copy under `~/.claude/plugins/cache/bit-agora/i-am/<ver>/hooks/`, not the repo source; live verification needs `bash install.sh` + session restart, then an `Explore` re-probe. Held OPEN pending that post-reinstall check; whether this fully subsumes the upstream K.length crash or is a separate Praxion-side bug is unverifiable until the reinstalled hook is exercised. // 2026-07-01: RESOLVED: true root cause one level deeper than the 2026-06-16 fix — the emit wrapped updatedInput in a {"tool_input": ...} envelope while the harness validates updatedInput directly as the tool params object, so ALL required Agent params read as missing (live Explore repro 2026-07-01: description+prompt both flagged). Fixed by unwrapping (1c9183c) + harness-contract comment + tests inverted to the direct shape; installed 0.12.0 cache synced (documented cp workaround); live Explore re-probe returned cleanly with the injected preamble evident in its answer. Codex-bridge test aligned in 6d67024. | d93688e91dd1 |
| td-053 | suggested | other | code-to-goals | pyproject.toml ([tool.coverage.run]), tests/ | code-quality |  | orchestrator | 2026-06-05 | 2026-07-01 | test-engineer | resolved | test-engineer (b605b51) | Originally filed as td-038; renumbered to td-053 to resolve id collision, 2026-07-15. Pre-existing: `python3 -m pytest tests/ -q` passes all tests (185/185 incl. Wave 0 additions) but the post-run pytest-cov COMBINE step crashes with `INTERNALERROR ... coverage.exceptions.DataError: Can't combine branch coverage data with statement data`. Root cause: `pyproject.toml [tool.coverage.run] branch = true` while at least one test spawns a subprocess that writes statement-only coverage data; the parallel-data combine cannot merge branch with statement. Confirmed PRE-EXISTING, not a Wave 0 (sia-praxion-fit) regression: reproduced via `pytest tests/ -q --ignore=tests/test_run_store_backend.py` (181 passed, same INTERNALERROR); the new Wave 0 test file alone runs clean. Surfaced at the Wave 0 Step 5 integration checkpoint. Impact: tests pass but the coverage report / `coverage.xml` is not produced, and a CI coverage gate would fail; `.coverage.<host>.<pid>.<rand>` parallel files also accumulated untracked (mitigated same-commit by adding `.coverage.*` to `.gitignore`). Resolution path: identify the subprocess-spawning test and make its coverage config inherit `branch=true` (consistent `.coveragerc`/`COVERAGE_PROCESS_START`, or `[tool.coverage.run] parallel=true` with one branch setting), OR run that test with coverage disabled. owner-role test-engineer (test-infra config). Class `other`: no enum slot covers coverage-tooling config defects (precedent td-021/td-034). Orchestrator-filed mid-pipeline — canonical sources are verifier/sentinel; a slight protocol stretch, verifier may re-source. // 2026-07-01: RECURRED in direct-capture-contract pipeline — the pytest-cov branch/statement COMBINE DataError reproduced on unmodified subprocess-spawning test files (hooks/test_worktree_guard.py alone); `--no-cov` runs clean. Still open; unchanged root cause. // 2026-07-01: RESOLVED: root cause — pytest-cov's subprocess embedding resolves its default `.coveragerc` config path relative to EACH CHILD's cwd; tests spawning children with tmp_path cwd silently fell back to statement-only data, crashing the branch-mode combine. Fix: pin `--cov-config=pyproject.toml` in addopts so children inherit the absolute config. Verified twice, coverage.xml produced, no stray parallel files. Un-crashing the suite also unmasked a stale codex-bridge assertion (fixed separately, 6d67024). | a0b80874490a |
| td-044 | suggested | other | code-to-goals | commands/onboard-project.md | code-quality |  | orchestrator | 2026-06-27 | 2026-07-01 | systems-architect | resolved | systems-architect (dec-263) | [E1/R15 from the process-flow analysis] The `future-designed` artifacts `project_profile.yaml` and `eval_ledger/EVAL_LOG.md` have no producer: the archetype-detection + agentic-eval scaffold that would create them is designed (schema in `skills/agent-evals/references/run-ledger-schema.md`; lifecycle/absent-fallback in `skills/software-planning/references/artifact-inventory.md`) but not wired into `/onboard-project`, so both are absent on every project. Downstream agents already treat them as optional (absent = OK), so this is a latent dead seam, not active breakage — tracked here so it stays visible rather than silently forgotten. Class `other` (no enum slot for "designed-but-unwired producer"). Close by either wiring the producers into onboarding or formally retiring the designed-but-unbuilt scaffold via ADR. // 2026-07-01: RESOLVED by dec-263 (retire, asymmetric): project_profile.yaml's never-built Phase 8f producer clause partially supersedes dec-219 (schema stands, reactivates if the agentic-eval archetype feature ships); EVAL_LOG.md untouched — lazily run-created by design with absence-tolerant readers. | 841f30d1c8a5 |
| td-045 | suggested | coverage-gap | code-to-goals | fitness/tests/test_gate_canary_coverage.py | code-quality |  | verifier | 2026-07-01 | 2026-07-01 | test-engineer | resolved | test-engineer (d43d11b) | [direct-capture-contract verification] `test_gate_canary_coverage.py::_hook_gates` globs only `hooks/*_gate.py` / `*_guard.py` / `*_gate.sh`, so the `remind_*.py` advisory-hook family (`remind_adr.py`, `remind_calibration.py`) is invisible to the canary-coverage meta-test. `remind_calibration.py` ships a live-biting canary (`test_flags_lagging_calibration_log`, verifier-confirmed) but a future accidental deletion of it would NOT be caught by `test_every_gate_has_canary`. Flagged as architect Structural Issue 2 (SYSTEMS_PLAN.md § Codebase Readiness) and confirmed by test-engineer. Resolution: widen the glob to include `remind_*.py`; doing so also requires backfilling `remind_adr.py`'s currently-absent canary (its inclusion is the reason the glob was left narrow — out of this task's charter). Class coverage-gap: a live gate whose liveness is not mechanically enforced. // 2026-07-01: RESOLVED: canary-coverage glob widened to hooks/remind_*.py; hooks/test_remind_adr.py backfilled (canary + contrast + fail-open). Meta-test green across both remind hooks. | 9f484292b67e |
| td-046 | suggested | other | code-to-goals | rules/swe/swe-agent-coordination-protocol.md, skills/spec-driven-development/references/calibration-procedure.md, claude/canonical-blocks/praxion-process.md, rules/swe/agent-behavioral-contract.md | code-quality |  | verifier | 2026-07-01 | 2026-07-01 | systems-architect | resolved | systems-architect (dec-264) | [direct-capture-contract verification] D8's optional mechanical tier-threshold-drift check was deliberately deferred (architect Decision 3). No mechanical check asserts tier-threshold agreement (file-count bands, score bands) across the four defining surfaces (coordination-protocol tier table, calibration-procedure matrix, praxion-process canonical block, behavioral-contract). Deferred because a substantive cross-surface check over heterogeneous prose ('4+ files' vs 'Multiple files' vs score bands) risks gate-liveness anti-patterns (brittle grep, existence-not-substance), and D8's consolidation itself already shrinks the restatement surface. Resolution: either build a narrow sentinel EC-series check with careful canonical-threshold extraction, or formally accept periodic-detection-only via ADR. owner-role systems-architect (needs a canonical-threshold model) or sentinel (EC-series check). Class other: no enum slot for a deliberately-deferred consistency gate. // 2026-07-01: RESOLVED by dec-264 (accept periodic-detection-only): post-dec-261 consolidation, only the coordination protocol fully states the bands; a mechanized cross-surface agreement check fails every-check-earns-its-place. Reversal trigger: one real band-boundary drift reaching main, or >=2 review-caught near-misses, then build a narrow EC(L) check over file-count boundaries only. | 27e49a514e3f |
| td-047 | suggested | coverage-gap | code-to-goals | scripts/check_readiness_feedback.py, scripts/test_check_readiness_feedback.py | code-quality |  | verifier | 2026-07-01 | 2026-07-01 | test-engineer | resolved | test-engineer (85e8ea2) | [direct-capture-contract verification, pre-existing baseline failure] `fitness/tests/test_gate_canary_coverage.py::test_every_gate_has_canary` fails naming `check_readiness_feedback.py`: its test file `test_check_readiness_feedback.py` has no canary-named test function (no `reject`/`flag`/`fail`/`block`/... substring per gate-canaries.md), so this gate's liveness is unproven. Confirmed pre-existing (TEST_BASELINE.md) and unrelated to this pipeline's scope; this pipeline fixed the sibling `check_calibration_coverage.py` canary-name sub-finding, leaving this the sole remaining sub-finding of that meta-test. Resolution: add a canary-named test feeding a known-bad readiness-feedback input and asserting the flag. Class coverage-gap. // 2026-07-01: RESOLVED: test_flags_below_floor_readiness added, driving compute_readiness_verdict with an adjusted_level=2 fixture; proven by mutation (gutting the floor check goes red). test_every_gate_has_canary fully green for the first time. | be86dbba9e30 |
| td-003 | important | complexity | code-to-goals | memory-mcp/src/memory_mcp/store.py:1-947 | code-quality |  | sentinel | 2026-04-28 | 2026-05-17 | implementation-planner | resolved | dec-225 | `store.py` is 947 lines, exceeding the 800-line ceiling in `rules/swe/coding-style.md`. Suggested module split (per prior planning): extract auto-link logic, helper functions, and incoming-link scanning into separate modules. Module restructuring → owner-role = implementation-planner per heuristic. Migrated from retired ROADMAP §5.6 on 2026-04-28; depends on `dec-041` type-checking foundation already shipped. | a04e05874650 |
| td-013 | suggested | complexity | code-to-goals | memory-mcp/src/memory_mcp/metrics.py | code-quality |  | sentinel | 2026-05-01 | 2026-05-17 | implementer | resolved | dec-225 | Rank-4 hotspot (score=15,540): complexity=28 (second-highest in codebase after otel_relay.py); metrics module in active development; warrants restructuring consideration if complexity grows further. // 2026-05-13: de-escalated — `metrics.py` is no longer in the top-10 hotspots (churn dropped out of the 90d window per METRICS_REPORT_2026-05-13); now just a high-complexity-density file: 503 lines (well under the 800 ceiling), p95 CCN ~27 (still 2nd-highest in the codebase after otel_relay.py). Holding at `suggested`; close if the team accepts the current density, or re-escalate if complexity grows. // 2026-05-17: re-verified by sentinel audit — 503 lines (unchanged, under the 800 ceiling); dropped out of the top-10 hotspot list per METRICS_REPORT_2026-05-15 (churn fell out of the 90d window). wontfix-eligible — no ceiling breach, no active hotspot pressure — held open pending an explicit team decision to accept the standing CCN density (~27 p95, 2nd-highest in the codebase). | 8fe359ca6f92 |
| td-043 | suggested | other | code-to-goals | eval/src/praxion_evals/judges/anthropic.py, eval/src/praxion_evals/judges/openai.py, eval/pyproject.toml | code-quality |  | verifier | 2026-05-26 | 2026-05-26 | implementation-planner | resolved | dec-204 (Step 11 — F-05 resolved in same pipeline) | [F-05 from praxion-self-eval-v1 verification] Legacy `judges/anthropic.py` + `judges/openai.py` stubs retained alongside the new `harness/judge_client.py` adapter; `pandas` + `openai` deps in `eval/pyproject.toml` survive solely for these stubs. Originally filed as td-038 (ID collision with existing td-038 in TECH_DEBT_RESOLVED.md — renumbered to td-043 at resolution). // 2026-05-26: RESOLVED in Step 11 of praxion-self-eval-v1. Legacy `judges/anthropic.py` + `judges/openai.py` stubs removed; `judges/` directory deleted; `pandas` + `openai` deps pruned from `eval/pyproject.toml` + `uv.lock`. `cli.py` `judge` subcommand removed; `tiers.py` `judge-anthropic` stub entry removed; `test_judges.py` updated to drop tests covering deleted modules. The `harness/judge_client.py` adapter is now the sole judge surface. 104/104 tests GREEN post-removal. | 3b7b3b8b6265 |
| td-041 | important | other | code-to-goals | scripts/project_metrics/collectors/git_collector.py | code-quality |  | verifier | 2026-05-24 | 2026-05-24 | implementer | resolved | orchestrator | Latent determinism defect surfaced as 7 test failures in `scripts/project_metrics/tests/test_git_collector.py` (multi-author churn/ownership/age/truck-factor + determinism). Root cause: `_run_git_log` bounds the window with `git log --since='{window_days} days ago'`, which git resolves against the REAL wall clock — it ignores the `PROJECT_METRICS_REFERENCE_TIME` env var the tests pin to 2026-04-23 (that injection point flows only into `_compute_age_days`, never into the `--since` boundary). The `minimal_repo` fixture's 10 commits span 2026-02-15→2026-03-30; once wall-clock-now passed first-commit+90d (≈2026-05-16) the window silently dropped the first two commits (init README 2026-02-15, add core 2026-02-20). Confirmed deltas: `core.py` churn 22 vs 32 (lost commit-2's +10), `churn_total` 49 vs 62, `core.py` top-author 0.667 vs 0.80 (Alice 10/15 not 20/25), `README.md` age+ownership absent (its only commit fell out of window). Fixture git history independently verified CORRECT against FIXTURE_SPEC.md — this is a collector defect, NOT fixture drift. Production reports against the live repo are unaffected (a rolling wall-clock 90d window is correct for a live repo); only the deterministic fixture tests break. Time-bomb class: re-fails on every machine until fixed, regardless of when run. Resolution: thread the reference clock into the window boundary — pass an absolute `--since=<reference_time - window_days>` ISO date computed from `_resolve_reference_time()` instead of the relative `'N days ago'` string, so the env-pinned clock governs BOTH age and the window. Class `other`: no enum slot covers test-determinism / wall-clock-window defects. // 2026-05-24: RESOLVED (orchestrator) — threaded the reference clock into the git-log window boundary: `_run_git_log` now takes `reference_time` and computes an absolute `--since=(reference_time - window_days).isoformat()` cutoff instead of the relative `'N days ago'` string, so `PROJECT_METRICS_REFERENCE_TIME` governs BOTH age and the window. All 34 `test_git_collector.py` tests green (the 7 that failed + 27 prior); full `scripts/project_metrics/` suite (421) green. Production reports unaffected — a live wall-clock reference yields the same rolling 90d window as the old relative string. | da1f110c472a |
| td-042 | suggested | drift | code-to-goals | AGENTS.md.tmpl | code-quality |  | verifier | 2026-05-24 | 2026-05-24 | doc-engineer | resolved | orchestrator | Generated artifact `AGENTS.md.tmpl` is stale relative to its source `CLAUDE.md`; surfaced as 1 failing test `scripts/test_claude_to_agents.py::test_praxion_root_agents_template_matches_generator`. The test regenerates the template from current `CLAUDE.md` via `skills/adapt-claude-to-agents/scripts/claude_to_agents.py::render_project_template` and asserts equality with the committed template. `CLAUDE.md` opening line was reworded in commit ac9d394 (2026-05-23, "docs: trim always-loaded CLAUDE.md + rules prose") to "Operational infrastructure for the development philosophy in `~/.claude/CLAUDE.md` — …", but `AGENTS.md.tmpl` was last regenerated 2026-05-15 (commit 1a4e06c) and still carries the old "The operational infrastructure for this project's shared development philosophy…" wording. Mechanical fix: re-run the generator and commit the regenerated `AGENTS.md.tmpl`. Class `drift`: generated artifact diverged from source-of-truth. // 2026-05-24: RESOLVED (orchestrator) — re-ran `claude_to_agents.py` against current `CLAUDE.md`; `AGENTS.md.tmpl` regenerated (9 ins / 7 del — the reworded intro). All 3 `scripts/test_claude_to_agents.py` tests green, including `test_praxion_root_agents_template_matches_generator`. | 0f2f99125f8f |
| td-035 | important | other | code-to-goals | CLAUDE.md | code-quality |  | orchestrator | 2026-05-14 | 2026-05-23 | implementer | resolved | 1a247c5 + i-am-subagent probe 2026-05-23 | Subagent `Write` to `.ai-work/<task-slug>/` can be denied by Claude Code's sandbox while the main agent's `Write` to the same path succeeds. Observed 2026-05-14: `i-am:systems-architect` dispatched by `/resume-rework` reported "The Write tool is denied. This is a sandbox policy decision, not a path issue" when attempting to write `SYSTEMS_PLAN.md` into `.ai-work/fix-manifest-fixture-target-agent/`. The architect's `tools:` frontmatter explicitly includes `Write` (`agents/systems-architect.md:tools: Read, Glob, Grep, Bash, Write, Edit`); the target resolved inside the session worktree so `hooks/worktree_guard.py` allowed it (it exits early when the target is `_is_within(session_root)`); no PostToolUse hook would surface as a "denied" message. Most likely root cause: Claude Code's per-tool permission prompt — the main agent had accumulated a session-scoped auto-allow for `.ai-work/` writes that did not propagate to the subagent, and subagents have no UI to answer the prompt so the call is silently denied. Distinct from `td-034` (subagent writes resolve to the wrong tree); same root-cause family in that subagent settings/permission inheritance differs from the orchestrator session. Workaround (interim): when a subagent reports a Write denial for `.ai-work/`, the main agent reads the subagent's returned content and places the file from the orchestrator session — the pipeline contract is honored, just routed through the orchestrator. Permanent resolution requires either (a) adding a project-scope `Write` permission rule for `.ai-work/**` (Praxion has no `Write(<glob>)` precedent in `.claude/settings*.json` and the exact syntax is unverified — needs upstream-docs check), (b) Claude Code subagents inheriting the orchestrator's session-scoped allows, or (c) subagents prompting an inbox the orchestrator can answer. Class `other`: no enum slot covers subagent-permission-inheritance gaps. Owner-role `unassigned`: gated on the upstream stance like `td-033`. CLAUDE.md `Known Claude Code Limitations` bullet added 2026-05-14. // 2026-05-17: re-verified by sentinel audit — finding still grounded. Sentinel recommended wontfix alongside td-033; orchestrator holds OPEN. Unlike td-033 (upstream #23478/#38487 explicitly CLOSED `NOT_PLANNED` — zero action possible), td-035 has no filed/declined upstream issue, and resolution path (a) is a concrete LOCAL fix: a project-scope `Write` permission for `.ai-work/**` in `.claude/settings.json`. Actionable next step — verify the `Write(<glob>)` settings syntax against current Claude Code docs, then add the rule; not upstream-gated. Re-tier owner-role unassigned→implementer once the syntax is confirmed. // 2026-05-23: `Write(<glob>)` syntax confirmed — `.claude/settings.local.json` already carries a working `Read(//…/**)` path-glob allow rule, proving the `Tool(<path-glob>)` form is valid for file tools, so a `Write(.ai-work/**)` allow rule follows the same shape. Per the re-tier rule above, owner-role unassigned→implementer. Actionable local fix: add a project-scope `Write` allow rule for `.ai-work/**` to `.claude/settings.json`, then confirm it propagates to spawned subagents (the remaining open sub-question — subagent permission inheritance). Re-tiered to clear the TD05(c) >7d-unassigned discipline finding. // 2026-05-23: PARTIAL — local fix (a) applied: added `"allow": ["Write(.ai-work/**)"]` to the `permissions` block of `.claude/settings.json` (mirrors the proven `Read(//…/**)` path-glob already in `settings.local.json`; JSON re-validated). Remaining acceptance criterion — *subagent permission propagation* — NOT verified: a probe subagent spawned to Write into `.ai-work/td-035-probe/` crashed on init with the td-021 `undefined is not an object (evaluating 'K.length')` failure, so propagation cannot be confirmed from this many-skill session. Held OPEN pending fresh-session confirmation (project-scope settings may only fully load at session start regardless of a mid-session edit). // 2026-05-23: RESOLVED — verified. A spawned i-am subagent (`i-am:researcher`) wrote into `.ai-work/td-035-probe/` SUCCESSFULLY under the committed `Write(.ai-work/**)` rule (settings.json, commit 1a247c5); the original symptom (subagent `.ai-work/` Write denied) does NOT reproduce — so the rule DOES propagate to spawned subagents, answering the open sub-question. Verification-path note: two `general-purpose` builtin probes crashed at init with the td-021 `K.length` bug, but the `i-am:researcher` spawn ran cleanly — confirming td-021 is builtin-agent-specific and i-am plugin agents bypass it (consistent with the Shipped-Explore fallback rule). The mid-session-settings-load worry was unfounded: the rule took effect without a session restart. | bf428c0287f9 |
| td-040 | suggested | complexity | code-to-goals | agents/verifier.md, agents/sentinel.md | code-quality |  | orchestrator | 2026-05-23 | 2026-05-23 | context-engineer | resolved | dec-203 + verifier Phase 3a de-dup | `agents/verifier.md` (588L) and `agents/sentinel.md` (583L) exceed the sentinel T03 500-line FAIL threshold (warn=300, fail=500); `implementation-planner.md` (477L) and `systems-architect.md` (404L) exceed the 400-line WARN. Sentinel finding S6 (2026-05-23 audit) recommends a progressive-disclosure split (core prompt + reference files). Deferred from the 2026-05-23 sentinel-cleanup pass by user decision — it is the one finding that is a genuine refactor, not a mechanical fix. Non-trivial because there is NO existing agent-local reference-file pattern in Praxion (agents cite shared skill references, not their own split-out files; no `agents/references/` dir exists). Establishing one needs a design decision (likely an ADR): where agent-local references live and how the agent reliably loads them — an agent prompt loads wholesale, so moving the verifier's `## Process` (~363L) or the sentinel's `## Check Catalog` (~258L) into a file the agent does not reliably re-read would silently disable the gate (a Gate-Liveness 'dead gate'). Resolution: load the agent-crafting skill, design the agent progressive-disclosure pattern, split both agents (largest extractable section each), then re-run sentinel (confirm T03 clears) + full pytest to prove no gate regressed. owner-role context-engineer per sentinel; may hand to systems-architect if an ADR is warranted. Class `complexity`: prompt-file size/structure, same family as td-002/003/004. // 2026-05-23: ASSESSED (orchestrator, "assess then trim-or-report" path) — read both files in full. VERDICT: oversize is largely INTRINSIC, not bloat; a line-count trim would game the gate (gate-liveness "substance over structure"). sentinel.md (581L): the `## Check Catalog` (~258L) is ~80 distinct check rows across 16 dimensions — it IS the gate, near-zero redundancy. Trimming = deleting checks (gutting coverage); extracting the catalog = dead-gate risk (the sentinel executes it inline; the genuinely-conditional SH/TT detail is ALREADY externalized). Principled fix: RECALIBRATE T03 for the ecosystem's most comprehensive gate-agent (carve-out or higher fail threshold + justification) — an ADR-level call, NOT a gut/risk-extract. verifier.md (588L): has a clean ~100L NON-gaming path — (a) ~62L of MISPLACED main-agent content (the `VERIFIER_FINDINGS.md` 7-section template ~395-440 + `### Rework Worktree Spawn Behavior` ~489-505 document what the MAIN AGENT does, not the verifier; relocate to a main-agent-facing home after de-dup check vs `commands/resume-rework.md` + coordination-details), and (b) the ~38L conditional `#### Phase 3a — ML Metric Threshold Evaluation` (~75-112) externalized to a verifier reference loaded only on ML signal (matches the existing on-demand `test-coverage`-skill pattern; low dead-gate risk — conditional + verifier already loads refs on demand). That clears verifier <500 without gaming. RECOMMENDATION: focused effort = verifier clean-path (relocate + conditional-externalize) + sentinel T03-recalibration ADR; both are decisions, not mechanical trims. Per the "don't game the gate" protocol, STOPPED at report rather than improvising a T03 change or risky catalog extraction. Held OPEN. // 2026-05-23: RESOLVED (user chose the T03-exception path for both gate agents). Both FAILs cleared by recalibrating T03 (ADR dec-203): the two gate-encoding agents (`sentinel.md`, `verifier.md`) now assessed at warn 550 / fail 700, so sentinel (580) and verifier (556) resolve to WARN, not FAIL. verifier also got a genuine lean-up — its conditional Phase 3a ML-metric procedure was de-duplicated against the schema reference it already cited (`skills/llm-training-eval/references/training-results-schema.md` § Verifier Consumption (Phase 3a)), 588→556. The verifier's rework-spawn contract was found to be deliberately tested-in-place (5 ship-gate tests in `tests/orchestration/` + dec-173..182), so it was NOT relocated — relocating would have overridden a tested design for a line count. T03 exception rationale inlined in `agents/sentinel.md` (no dec-NNN in the shipped artifact). Verified: 181 Praxion tests pass, shipped-artifact-isolation clean on both agents, DECISIONS_INDEX regenerated. The two WARN-level agents (`implementation-planner.md` 476, `systems-architect.md` 403) were never part of the FAIL and remain normal monitored WARNs — out of scope for this row. | 13ccf4380738 |
| td-004 | important | complexity | code-to-goals | task-chronograph-mcp/src/task_chronograph_mcp/otel_relay.py | code-quality |  | sentinel | 2026-04-28 | 2026-05-23 | implementation-planner | resolved | otel_relay inheritance-chain split | `otel_relay.py` is 1,468 lines (grew from 921 in earlier audit), nearly 2x the 800-line ceiling. Suggested module split: extract span creation methods to `span_factory.py`, agent context tracking to `context_tracker.py`. Worsening trend across recent telemetry work (`dec-052` duration-aware spans) compounds the excess. Module restructuring → owner-role = implementation-planner. Migrated from retired ROADMAP §5.6 on 2026-04-28. // 2026-05-23: RESOLVED — split into a 4-file linear-inheritance chain (one `__init__`/`self`; behavior-identical): `relay_helpers.py` (241L — constants + stateless helpers, no internal deps), `context_tracker.py` (335L — `_RelayContextTracking`: shared state + agent-context tracking + reaper), `span_factory.py` (515L — `_RelaySpanFactory(_RelayContextTracking)`: span construction + lazy `_ensure_agent_context`), `otel_relay.py` (509L — `OTelRelay(_RelaySpanFactory)`: public event API). Every file now under the 800-line ceiling. Acyclic layering helpers←context←span←public, no upward calls. Behavior preserved: 292/292 chronograph tests pass (unchanged count), pyright `src` clean, ruff clean. Test monkeypatch targets for the five dynamically-read module symbols (`_git_user_id`/`_git_head_sha`/`_read_pipeline_tier`/`FORK_CLUSTER_WINDOW_S`/`AGENT_SPAN_TIMEOUT_S`) repointed to their new `relay_helpers` home via aliased imports — assertion lines byte-unchanged; readers access them `rh.`-qualified so patches still take effect. Backward compat: `otel_relay` re-exports moved public symbols via `__all__`; `server.py` + diagnostic-test imports unchanged. | f8ceab2e7268 |
| td-005 | important | other | code-to-goals | eval/pyproject.toml, eval/src/praxion_evals/cli.py, eval/src/praxion_evals/regression/ | code-quality |  | verifier | 2026-04-28 | 2026-05-26 | implementation-planner | resolved | dec-204 + eval/EVAL_PLAN.md | Eval framework `regression` mode was shipped-but-broken: keyed baselines by `task_slug` (one-shot slugs, no comparison corpus). Compounding bug: missing `arize-phoenix` dep silently swallowed `phoenix.Client()` errors. // 2026-05-26: RESOLVED by `/eval-praxion` v1 — `dec-204` narrowly supersedes dec-040 clause 3 to allow LLM-as-judge calls over completed artifacts; fills the `judges/anthropic.py` NotImplementedError stub via the new `harness/judge_client.py` adapter; retires the broken-by-design `regression/` package (448 LOC) entirely. Families shipped: #1 (Pipeline-outcome fidelity over SPECs + ADRs + DECISIONS_INDEX.md) + #2 (Behavioral-contract adherence over VERIFICATION_REPORT.md). The broader full regression-mode redesign (tier/shape-keyed envelope baselines over a Phoenix corpus) remains deferred — requires both a shape-definition ADR and a multi-run Phoenix corpus that does not yet exist. Deferred-scope tracked in `eval/EVAL_PLAN.md` (families #3/#4/#5/#6 + LEARNINGS-distillation prerequisite + `--project` flag open design question). | 51fe8bd48f7a |
| td-039 | important | topology-drift | code-to-goals | skills/testing-strategy/references/python-testing.md, skills/testing-strategy/references/test-topology.md | adr | dec-193 | orchestrator | 2026-05-23 | 2026-05-23 | systems-architect | resolved | dec-202 | Python topology selector identifiers are unregistered, so test-topology M2 adoption is effectively blocked for Python consumer projects. The trunk (`test-topology.md`) lists `pytest-globs`/`pytest-markers`/`pytest-keywords` (Registry 1) and `pytest-xdist-loadfile`/`pytest-xdist-load` (Registry 2) only as "indicative future" identifiers explicitly "not registered at this milestone"; the Python leaf (`python-testing.md`) never registered them. Any Python project adopting the protocol hits the trunk's Additive Leaf Escalation Clause and must escalate to the architect on every scoped-test step. Surfaced 2026-05-23 by the pipeline-efficiency research (RESEARCH_BDD F9/R1) and authorized for fix as Tier-B item B5, but DEFERRED by the orchestrator rather than implemented as a mechanical leaf edit: the trunk's own governance routes registry additions through the systems-architect with an ADR, and registry rows are append-only (trunk: "rows are not removed") — a wrong contract can never be removed, only superseded. Resolution path: systems-architect promotes the five identifiers indicative→registered (define each selector's concrete arg contract + invocation + default parallel runner in `python-testing.md`, mirror in the trunk registries, bump staleness markers), records the M1→M2 promotion as an ADR, then the leaf references the registered identifiers. Owner-role systems-architect per the escalation clause. // 2026-05-23: RESOLVED by systems-architect (ADR dec-202). All five identifiers promoted indicative→registered in the trunk's Registry 1 (`pytest-globs`/`pytest-markers`/`pytest-keywords`) and Registry 2 (`pytest-xdist-loadfile`/`pytest-xdist-load`); trunk `<!-- last-verified -->` markers bumped to 2026-05-23; leaf (`python-testing.md`) marked the rows live-not-indicative and gained explicit edge-case semantics (empty-arg invalid, list-vs-string contract, OR-join for markers) plus recommended-default rationale for `pytest-xdist-loadfile`. The leaf already documented the contracts; deficiency was the trunk registry asymmetry + missing edge-case semantics. Sentinel TT02 now passes for Python `selectors[].strategy` values instead of FAILing; the Additive Leaf Escalation Clause no longer fires for these primitives. All 181 Praxion tests pass. ADR re-affirms dec-091. Orchestrator to migrate this row to TECH_DEBT_RESOLVED.md. | 05d24fcd6e09 |
| td-038 | important | other | code-to-goals | scripts/check_id_citation_discipline.py | code-quality |  | verifier | 2026-05-15 | 2026-05-15 | implementer | resolved | 2026-05-15: added `is_bash_shebang()` shebang-detection helper + extended `iter_code_files()` (full-scan, exec-bit-gated) and `filter_files()` (explicit-pass, exec-bit-override) to include extensionless bash; 7 regression tests in `tests/test_check_id_citation_discipline.py` PASS; `scripts/dispatch-reworks` now scanned cleanly. Will migrate to TECH_DEBT_RESOLVED.md at post-merge. | The inbound id-citation discipline check (`scripts/check_id_citation_discipline.py`, invoked via `hooks/commit_gate.sh` on every `git commit`) skips extensionless bash scripts — it scans only files matching a configured extension whitelist (likely `.py`, `.ts`, `.tsx`, `.js`, `.md`, etc.). The Phase 2 dispatch-reworks pipeline shipped `scripts/dispatch-reworks` (extensionless, executable bash following Praxion's `scripts/ccwt` precedent). The verifier pass discovered two id-citation violations in that file (line 300: "Step 1 smoke-check"; line 377: "...in Step 6") that the inbound gate would have allowed through to merge. The companion test-file violation at `tests/test_dispatch_reworks_bg.py:505` WAS caught by the gate (Python extension). So: every future extensionless Praxion script that ships from a pipeline has the same coverage hole. Resolution path: extend `check_id_citation_discipline.py`'s file selection to detect bash scripts by shebang inspection (`head -n1 == #!/usr/bin/env bash` or `#!/bin/bash`) in addition to extension matching; also include the `scripts/` directory's extensionless executables in the scan corpus. Test coverage: add a regression test that creates an extensionless bash file with a forbidden id-citation pattern, runs the checker, and asserts a violation is reported. Severity `important` because every Phase-N pipeline using bash scripts inherits this gap. | b5c427341a99 |
| td-019 | suggested | dead-code | code-to-goals | .ai-state/metrics_reports/index.html, claude/aac-templates/metrics-viewer.html.tmpl | code-quality |  | orchestrator | 2026-05-08 | 2026-05-11 | implementer | resolved | dashboard-redesign Step 8 | Legacy static HTML metrics viewer is superseded by the Streamlit Metrics page (`streamlit_app/pages/metrics.py`). `claude/aac-templates/metrics-viewer.html.tmpl` is no longer referenced by any code under `scripts/project_metrics/` (verified via grep) and `.ai-state/metrics_reports/index.html` is a stale snapshot — `/project-metrics` no longer regenerates it. Both files are committed but unmaintained. Resolution options: (a) delete both and update CLAUDE.md / aac-templates README to remove references; (b) restore HTML generation in `scripts/project_metrics/report.py` if a static-HTML offline view is still wanted alongside the dashboard. Filed by orchestrator under explicit user direction during the 2026-05-08 install + dashboard polish session. Resolved: template replaced with ~43-line static redirect stub pointing to praxion-dashboard; Praxion's own .ai-state/metrics_reports/index.html + SENTRUX_HISTORY.md + SENTRUX_REPORT_2026-05-05_03-43-48.{json,md} deleted. | 14f3c9778784 |
| td-022 | important | coverage-gap | code-to-goals | pyproject.toml testpaths, streamlit_app/tests/ | code-quality |  | sentinel | 2026-05-09 | 2026-05-09 | test-engineer | resolved | 064bf1e | Coverage dropped from 83.3% to 54.1% when streamlit_app/ was added to coverage source scope (source = ["."]) after the dashboard build (commits 0629cbe–e0bacce), but testpaths = ["tests", "scripts"] was not updated to include streamlit_app/tests/. 16 test files (including 13 renderer smoke tests from e0bacce) are never discovered by pytest. 2,164 streamlit lines tracked at 0%. Configuration inconsistency: code in coverage scope, tests out of discovery scope. Resolution: add "streamlit_app" to testpaths in pyproject.toml. Filed by sentinel TD03 (coverage-gap class; test-engineer owner per heuristic). RESOLVED 2026-05-09: added "streamlit_app" to testpaths in pyproject.toml; 216 streamlit_app tests now discovered (839 passed, 3 skipped); line-rate recovered to 82.4% (combined line+branch 79.1%). | a685831f83b5 |
| td-018 | suggested | token-budget | code-to-goals | rules/swe/agent-intermediate-documents.md, skills/software-planning/references/tech-debt-ledger.md | claude-md | rules/CLAUDE.md#token-budget | orchestrator | 2026-05-08 | 2026-05-09 | implementer | resolved | docs-strategy/p1-foundation P2.1 commit | LEDGER schema migration EXECUTED 2026-05-09: moved the 14-field schema + owner-role heuristic + worktree-concurrency dedupe semantics from `rules/swe/agent-intermediate-documents.md` to `skills/software-planning/references/tech-debt-ledger.md`. Recovery: ~7,950 always-loaded bytes / ~2,210 tokens conservative (more than the originally-estimated 3,300 because owner-role heuristic + lifecycle conventions also migrated). Total surface 22,530 → 20,320 tokens (4,680 token margin under 25k guardrail). Citation sites updated: `.ai-state/DESIGN.md`, `.ai-state/TECH_DEBT_LEDGER.md` header, `docs/architecture.md`, `agents/verifier.md`. Rule retains a 5-line summary + pointer to the skill reference. Migrates to TECH_DEBT_RESOLVED.md at next post-merge run via `scripts/finalize_tech_debt_ledger.py`. // resolved 2026-05-09 by P2.1 of the docs-strategy plan. | 2cb946a676dc |
| td-016 | suggested | other | code-to-goals | agents/implementer.md, agents/sentinel.md, agents/verifier.md, claude/aac-templates/sentrux-rules.toml.tmpl, commands/onboard-project.md, hooks/, install.sh, skills/sentrux/SKILL.md | code-quality |  | orchestrator | 2026-05-07 | 2026-05-08 | systems-architect | wontfix | context-quick-wins-pr | Sentrux structural-quality sensor integration paused after Phase 1 (binary install + per-project rules + verifier Phase 9b advisory `sentrux check .`). // 2026-05-08: marked wontfix — sentrux integration removed wholesale per user direction (quality did not meet expectations; advisory-only signal value did not justify the maintenance + always-loaded-skill-metadata cost). Removed surfaces: `skills/sentrux/`, `scripts/sentrux_history.py`, `claude/aac-templates/sentrux-rules.toml.tmpl`, verifier Phase 9b, onboard-project Phase 8b.6 sub-step, install.sh sentrux block, scripts/CLAUDE.md entry, skills/README.md catalog row, docs/metrics/README.md mentions. Open Q1–Q5 from the original integration and the deferred work items (B)–(F) become moot at wontfix. | 38af28d65c23 |
| td-011 | suggested | other | code-to-goals | scripts/finalize_adrs.py | code-quality |  | orchestrator | 2026-04-30 | 2026-05-08 | implementer | resolved | 299c9be | Two `finalize_adrs.py` defects surfaced during AaC+DaC v1 FF-merge: (1) `--merged` mode uses `HEAD~1..HEAD` detection on FF-merges (linear history), missing drafts in non-latest commits when the worktree merge spans multiple draft-adding commits — 6/7 drafts auto-promoted, 7th required manual finalize via `--all`. (2) `parse_fragment_filename` cannot disambiguate the branch fragment from the slug when only 1 draft remains in `drafts/` (no siblings to share a common prefix). // 2026-05-08 (commit 299c9be): defect (1) resolved — `_drafts_added_in_last_merge` now uses reflog `HEAD@{1}` as the primary detection point so the diff range captures every commit landed by the most recent HEAD update (FF or merge-commit); first-parent detection retained as fallback for shallow clones / no-reflog edge cases. 3 new regression tests in `scripts/test_finalize_adrs.py::TestDraftsAddedDetection`. Scope of this row covers defect (1) only; defect (2) — single-fragment branch-parsing ambiguity — re-filed cleanly as `td-017` with `class: complexity` (avoids dedup_key collision with this row's `other` classification and is the more accurate categorization for brittle parser logic). | a9f59b941c3f |
| td-017 | suggested | complexity | code-to-goals | scripts/finalize_adrs.py | code-quality |  | orchestrator | 2026-05-08 | 2026-05-08 | implementer | resolved | f55bf40 | Single-fragment branch-parsing ambiguity in `parse_fragment_filename`: when only one draft fragment remains in `drafts/`, the sibling-prefix-discovery heuristic cannot disambiguate a hyphenated branch name from the slug (e.g., `feat-x-step-1` could be `feat / x-step-1` or `feat-x / step-1`). Originally filed as defect (2) of td-011; defect (1) was the FF-merge multi-commit detection bug, resolved separately by 299c9be (reflog `HEAD@{1}` primary detection). // 2026-05-08 (commit f55bf40): resolved via fix candidate (b) — record the authoring branch in fragment frontmatter at write time. New optional `branch:` field in the ADR frontmatter schema; when present, `parse_fragment_filename` uses it as the authoritative branch name and splits the filename around it cleanly, eliminating the heuristic ambiguity entirely. Backward compatible: pre-td-017 fragments without the field continue to parse via the existing filename heuristics. Schema docs updated in `rules/swe/adr-conventions.md` (Frontmatter table, "Recommended on drafts") and `skills/software-planning/references/adr-authoring-protocols.md` (creation protocol step 4 + identity-derivation pseudocode comment). 5 new regression tests in `TestParseFragmentBranchFromFrontmatter`. | f540526ae620 |
| td-014 | important | complexity | code-to-goals | hooks/send_event.py | code-quality |  | sentinel | 2026-05-01 | 2026-05-01 | implementer | resolved | 2c4a199 | Rank-8 hotspot (score=11,118): complexity=34 — highest in codebase; always-executed pre-commit hook where high complexity has outsized reliability impact. Deep conditional logic warrants refactor to reduce blast radius per commit. // resolved by extracting _handle_session_start, _handle_stop, _handle_subagent_start, _handle_subagent_stop, _handle_pre_tool_use, _handle_post_tool_use, _handle_post_tool_use_failure, _enrich_mcp_meta, _extract_progress_content into a dispatch table; _build_events CC now 3, max per-function CC now 7 | 028c4857d3e1 |
| td-015 | important | coverage-gap | code-to-goals | scripts/reconcile_ai_state.py | code-quality |  | sentinel | 2026-05-01 | 2026-05-01 | test-engineer | resolved | 9a073da | Coverage 60.1%, below 70% project floor; handles .ai-state/ reconciliation — logic errors in untested paths could corrupt the permanent project-intelligence store. // coverage now 98.2%; tests added for: ours-wins-on-older-timestamp, empty-category-excluded, blank-line-between-valid-jsonl-lines, has_drafts git-error-safe-fallback, has_drafts CLAUDE.md-exclusion, has_drafts positive-detection, has_drafts non-draft-md, reconcile_adr_numbers no-decisions-dir, reconcile_adr_numbers defers-to-finalize-when-drafts-present, reconcile_file missing-file, reconcile_file clean-json, reconcile_file invalid-json-warns, reconcile_file conflicted-json-reconciled-and-written, reconcile_file conflicted-text-with-write_fn, reconcile_file conflicted-missing-git-stages-warns, write_text_file creates-and-overwrites, _check_merge_drivers warns-and-silent, _reconcile_adr_and_index absent-dir / duplicates / no-regen-script / regen-success / regen-failure, main nothing-to-reconcile / post-merge / clean-memory / clean-observations / reconciliation-complete-memory / reconciliation-complete-observations | 764de6379a8d |
| td-006 | suggested | duplication | code-to-goals | commands/co.md, commands/cop.md | code-quality |  | verifier | 2026-04-28 | 2026-05-01 | implementer | resolved | dec-114 | `/cop` (commit-and-push) duplicates `/co` (commit) instead of layering on top. Refactor: extract shared commit logic; `/cop` adds the push step on completion. Low-priority quality-of-life cleanup; behavior is correct, just structurally redundant. Migrated from retired ROADMAP Deprecation table on 2026-04-28. // 2026-05-01: resolved as part of dec-114 commit 3. Extracted Process steps 1-6 to claude/canonical-blocks/commit-process.md; both /co and /cop embed via HTML-comment-fenced canonical-source marker; sync_canonical_blocks.py extended with per-block fence config (BLOCKS dict + BlockSpec) + per-block consumer mapping + REPO_ROOT-vs-tmp_path filter for test compatibility; redundant "Do not push after each commit" sub-bullet dropped from /cop (Push once step 7 makes semantics clear). | de5d0128654b |
| td-007 | suggested | token-budget | code-to-goals | rules/swe/swe-agent-coordination-protocol.md | claude-md | rules/CLAUDE.md#token-budget | orchestrator | 2026-04-30 | 2026-05-01 | implementer | resolved | dec-114 | ~1,125 tokens potential always-loaded headroom: L2 extraction of `### Cross-Agent Skill Conventions` (~700) to `skills/software-planning/references/cross-agent-skill-conventions.md` + L5 tightening of Process Calibration Lightweight bullets (~225) and Coordination Pipeline rules table (~200). Identified by P0a recon during AaC+DaC v1, deferred per user direction. // 2026-05-01: resolved as part of dec-114 token-budget pass; achieved ~1,078 tokens (file 18552→14670 bytes) via L2 extraction + Lightweight bullet compression + Pipeline-rules table; reclassified `other`→`token-budget`. | 66ec3c5e5526 |
| td-008 | suggested | token-budget | code-to-goals | rules/swe/agent-intermediate-documents.md | claude-md | rules/CLAUDE.md#token-budget | orchestrator | 2026-04-30 | 2026-05-01 | implementation-planner | resolved | dec-114 | ~211 tokens immediate (L4 mermaid removal + L5 ARCHITECTURE.md description tightening) plus ~1,700 RESERVED (L2 TECH_DEBT_LEDGER schema extraction requires adding `software-planning` to verifier+sentinel skill frontmatter — coordinated refactor outside compression scope). Identified by P0a recon, deferred per user direction. // 2026-05-01: resolved — L4 mermaid removed in dec-114 commit 1 (was structurally wrong post-pair-split); L5 ARCHITECTURE.md description tightened in commit 2; combined achieved ~189 tokens. RESERVED L2 cut still pending (separate coordination concern); reclassified `other`→`token-budget`. | 5335ed851980 |
| td-009 | suggested | token-budget | code-to-goals | rules/swe/adr-conventions.md | claude-md | rules/CLAUDE.md#token-budget | orchestrator | 2026-04-30 | 2026-05-01 | implementer | resolved | dec-114 | ~150 tokens recovery: L5 tightening of Finalize Protocol step 4 cross-reference list to a table form (same content, tabular). Identified by P0a recon, deferred per user direction. // 2026-05-01: resolved — converted bullet list to table; bytes nearly neutral (table markdown overhead negated savings) but structure improved; reclassified `other`→`token-budget`. Original ~150-token estimate was optimistic. | 4d7083638fad |
| td-010 | suggested | token-budget | code-to-goals | rules/swe/memory-protocol.md | claude-md | rules/CLAUDE.md#token-budget | orchestrator | 2026-04-30 | 2026-05-01 | implementer | resolved | dec-114 | ~80 tokens recovery: L5 removal of three `remember()` call examples from `### When to Remember`; function signature in `### How to Remember` section remains canonical. Identified by P0a recon, deferred per user direction. // 2026-05-01: resolved — examples removed plus the surrounding scaffolding; achieved ~135 tokens (file 4895→4409 bytes), exceeded target; reclassified `other`→`token-budget`. | 891cac861d28 |
| td-001 | important | duplication | code-to-goals | commands/onboard-project.md, commands/new-project.md | code-quality |  | verifier | 2026-04-27 | 2026-04-27 | implementation-planner | resolved | 72d6db7 | Four canonical blocks (Agent Pipeline, Compaction Guidance, Behavioral Contract, Praxion Process) duplicated byte-identically across both onboarding commands; mirror discipline enforced by author + regex test, not by extraction. Proper fix: extract canonical blocks to a single source-of-truth (e.g., `claude/canonical-blocks/<name>.md`) consumed by both commands. User flagged during Step 11; refactor must cover all four blocks together to avoid mixed-state. Resolved by extracting to `claude/canonical-blocks/<slug>.md` with `scripts/sync_canonical_blocks.py` enforcing byte-identicality via pre-commit hook (build-time compilation per dec-082). | 83fa92c1f787 |
| td-020 | suggested | other | code-to-goals | install_claude.sh:382-396 | code-quality |  | orchestrator | 2026-05-08 | 2026-05-13 | implementer | resolved | install_claude.sh:install_praxion_pre_commit | `install_git_merge_infra` pre-commit handling: first install with a pre-existing user pre-commit hook that lacks `check_shipped_artifact_isolation` correctly *appends* the Praxion block, preserving the user's content. Second install detected the Praxion marker and fell through to `cp` — silently *overwriting* the entire file, losing any user customizations made between installs. Edge case, but the asymmetry between first-install (preserve) and second-install (overwrite) was unprincipled. Resolution: detect the Praxion-managed block by sentinel comments on both ends, then in-place replace just that block on subsequent installs. Filed by orchestrator under explicit user direction during the 2026-05-08 install audit. // 2026-05-13: verified RESOLVED — `install_praxion_pre_commit` in `install_claude.sh` implements the prescribed 4-path sentinel-comment approach (`PRAXION_PRECOMMIT_SENTINEL_START`/`_END` markers; no-hook→write-fresh-wrapped / sentinels-present→in-place-replace / legacy-content→warn+append / user-hook→append-preserve). The inline comment block originally mislabeled this as the "td-021 fix"; corrected to "td-020 fix" in the same housekeeping pass. | 3d0f02e0b7d4 |
| td-023 | suggested | other | code-to-goals | dashboard_app/src/app/globals.css | code-quality |  | orchestrator | 2026-05-11 | 2026-05-13 | systems-architect | resolved | dashboard-ux-overhaul (REQ-16, dec-143) | Single light theme only for v1 (Decision 9). Dark mode deferred — multiplied CSS surface cost for unclear demand in a local read-only portal. Resolution: add `prefers-color-scheme: dark` media-query block to `globals.css` + test with dark system theme. User-confirmed defer 2026-05-11. // 2026-05-13: verified RESOLVED — `globals.css` carries the `@media (prefers-color-scheme: dark)` :root override block (line ~169); shipped by the `dashboard-ux-overhaul` pipeline (SYSTEMS_PLAN REQ-16, ADR dec-143) alongside the de-hardcode token-layer sweep. (The same sweep grew `globals.css` to 2,368 lines — that's td-030, a distinct file-size-ceiling row, still open.) | add7a2c6e703 |
| td-026 | suggested | dead-code | code-to-goals | streamlit_app/ | code-quality |  | orchestrator | 2026-05-11 | 2026-05-13 | systems-architect | resolved | 313a50e | dec-134 says retire Streamlit after parity. Parity reached with the dashboard-redesign. Hard retire (delete `streamlit_app/`) was a separate cleanup task requiring explicit user approval. // 2026-05-13: RESOLVED — `streamlit_app/` deleted in commit 313a50e (53 tracked files: app, components, pages, widgets, data layer, tests) + entry removed from `[tool.pytest.ini_options].testpaths` in `pyproject.toml`; pytest collection clean afterward (770→667 tests, 0 collection errors). Doc + codex-bridge references to `streamlit_app` (CLAUDE.md / AGENTS.md(.tmpl) repo-layout rows, docs/architecture.md, dashboard_app/README.md, dashboard-conventions.md `paths:` globs + §7, html-output-conventions.md `paths:` glob, two skill mentions, export-codex-rules-bridge.py keyword alias) intentionally left for a follow-up cleanup pass. | afb37b5044d8 |
| td-028 | suggested | other | code-to-goals | agents/sentinel.md | code-quality |  | orchestrator | 2026-05-11 | 2026-05-13 | unassigned | wontfix |  | `html-output-conventions.md` referenced a phantom `EC07-doc-manifest-fresh` check (since removed). If a manifest-freshness check is wanted, it needs a new ID (EC08+) to avoid collision with the removed EC07 entry. Class `other`: no enum slot covers missing-sentinel-check gaps; owner-role `unassigned` per heuristic (sentinel implementation is not owned by a standard consumer role). // 2026-05-13: WONTFIX — not actionable debt. The dangling reference was already removed before this row was filed (verified: grep for `EC07` / `doc-manifest-fresh` in `rules/writing/html-output-conventions.md` returns nothing on 2026-05-13). This row only records the forward-looking ID-allocation note: any future manifest-freshness sentinel check must allocate EC08+ to avoid namespace collision with the retired EC07. No code or doc change required. | c810f2fc70cf |
| td-031 | suggested | dead-code | code-to-goals | dashboard_app/src/lib/health-tone.ts | code-quality |  | verifier | 2026-05-12 | 2026-05-13 | implementer | resolved | dashboard dead-code cleanup (td-031/td-032) | `metricTone()` + `ToneResult` (the `arrow`/`word` per-metric tone deriver) were exported and unit-tested (8 `it()` blocks) but never consumed by any production component: `metrics-dashboard.tsx` uses `getMetricTone()` from `@/lib/metrics` plus a local arrow ternary instead. `healthSummary()` from the same module IS used. Resolution options were: route the metrics components through `metricTone()` (DRY) or delete `metricTone`/`ToneResult` and the tests. // 2026-05-13: RESOLVED via delete — removed `metricTone()` + `ToneResult` from `src/lib/health-tone.ts` and the `describe("metricTone")` block (8 tests) from `tests/server/health-tone.test.ts`; `healthSummary` / `HealthLabel` / `HealthSummary` retained. Chose delete over "route through it": the latter would refactor working code in the #1 hotspot (`metrics-dashboard.tsx`) to consume a function nothing asked for. Verified: `vitest run` 301 passed; `next build` clean. | 2a16b6ca023b |
| td-032 | suggested | dead-code | code-to-goals | dashboard_app/src/server/view-models/sidebar-signals.ts | code-quality |  | verifier | 2026-05-12 | 2026-05-13 | implementer | resolved | dashboard dead-code cleanup (td-031/td-032) | `SidebarSignals` exported both `activeWorkshops` and `activeWorkshopCount` carrying the identical value; only `activeWorkshops` is read (`sidebar-nav.tsx`). The `activeWorkshopCount` alias was a test-vs-impl naming wrinkle that survived into the merged module. Resolution: drop `activeWorkshopCount` (and update any test referencing it). // 2026-05-13: RESOLVED — removed the `activeWorkshopCount` field from the `SidebarSignals` type and the `getSidebarSignals` return value in `src/server/view-models/sidebar-signals.ts`; tightened the `activeWorkshops` doc-comment; fixed the stale `activeWorkshopCount` reference in the `.nav-badge` comment in `globals.css`. No test change needed — `tests/server/sidebar-signals.test.ts` already used `activeWorkshops` exclusively. Verified: `vitest run` 301 passed; `next build` clean. | 0f91418de311 |
| td-030 | important | complexity | code-to-goals | dashboard_app/src/app/globals.css | code-quality |  | verifier | 2026-05-12 | 2026-05-13 | implementation-planner | resolved | dashboard CSS layer split (td-030) | `globals.css` was 2,368 lines after the `dashboard-ux-overhaul` token-layer rewrite + de-hardcode sweep, ~3x the 800-line ceiling in `rules/swe/coding-style.md`. The file's own `/* TODO */` proposed a 3-file split (tokens + base + components), but `components.css` would still have been ~2,100 lines — so the actual split is six layer files, each ≤ 800 lines: `tokens.css` (210 — `:root` + dark `:root`), `base.css` (27 — element resets), `app-chrome.css` (349 — app shell / sidebar / brand / nav / header / page wrappers / lede), `surfaces.css` (390 — page grids / cards / markdown surface / lists / metric-summary + metric-trend cards / trend chart), `widgets.css` (712 — chips / tabs / error-state / educational popover / collapsible cards / metadata chips / collector status / metrics-raw / KPI tiles / health strip / hotspot bar / collector chips), `pages-and-viz.css` (675 — responsive @media breakpoints / recharts wrappers / AaC badges / per-page layouts for architecture+ADR+sentinel / component index / markdown ToC / DiagramFrame / DiagramModal). `globals.css` is now a ~16-line `@import` manifest in cascade order (`pages-and-viz.css` imported last so its `@media (max-width:…)` overrides keep winning by source order). // 2026-05-13: RESOLVED — split executed cut-don't-reorder; verified `diff` of `cat(tokens base app-chrome surfaces widgets pages-and-viz)` against the original-minus-TODO-header → byte-identical, so the cascade is provably unchanged. `cd dashboard_app && ./node_modules/.bin/next build` → clean (the `@import` chain resolves and bundles; the built css chunk contains classes from all six layers); `./node_modules/.bin/vitest run` → 301 passed. `layout.tsx` still imports `./globals.css` unchanged. Note: minor stale `globals.css` mentions remain in `skills/web-ui-design/*` (tokens now live in `tokens.css`) — left for a future doc-freshness pass. | dc936cc43878 |
| td-033 | important | other | code-to-goals | agents/doc-engineer.md, agents/implementer.md, agents/test-engineer.md, skills/rule-crafting/SKILL.md | code-quality |  | orchestrator | 2026-05-12 | 2026-05-17 | unassigned | wontfix |  | Path-scoped rules (`paths:` frontmatter) inject only when Claude reads a matching file — not on Write/Edit/MultiEdit (verified 2026-05-12 against `code.claude.com/docs/en/memory` + GitHub issues anthropics/claude-code#23478, #38487, #16853). An agent that creates a new file via Write without first reading a matching sibling misses that file type's conventions: new `.py`/`.ts` misses `coding-style.md`; new doc misses `readme-style.md`/`diagram-conventions.md`/`html-output-conventions.md`/`aac-dac-conventions.md`; new `.github/*.md` misses `pr-conventions.md`; new files miss `id-citation-discipline.md`, `staleness-policy.md`. Touches Praxion's rules-as-guardrails guarantee surface directly; moderate untreated severity (agents usually read a directory before working in it, which incidentally loads the rule). Mitigated 2026-05-12 (ROADMAP P1): added a "read an existing sibling before creating a new file" instruction to the Constraints sections of `agents/implementer.md`, `agents/doc-engineer.md`, `agents/test-engineer.md`; documented symptom + full mitigation in `skills/rule-crafting/SKILL.md` (new "Path-Scoped Rules: Read-Only Loading Trigger" section); added a `Known Claude Code Limitations` bullet to CLAUDE.md; added a path-scoped caveat next to the `paths:` convention in `rules/CLAUDE.md`. // **2026-05-12 upstream re-check** — #23478 (core bug) CLOSED `NOT_PLANNED` 2026-03-14, #38487 (feature request) CLOSED `NOT_PLANNED` 2026-04-30: upstream has **explicitly declined** to load path-scoped rules on Write/Edit. The agent-prompt mitigation is therefore **permanent** (not provisional) and should be treated as a design constraint, not awaited tech debt. #16853 remains open with a relevant workaround signal: per user comments, multi-entry YAML-list `paths:` may fail under the `_9A()` CSV parser, while single-entry brace-expansion (`paths: ["**/*.{py,pyi}"]`) and inline-array form work consistently — Praxion's rules predominantly use YAML-block-list syntax, so `scripts/check_paths_syntax.py` was added 2026-05-12 to flag at-risk rule files. Windows variant #21858 CLOSED `COMPLETED` 2026-03-24 — no further Praxion action (won't-do); the existing Windows caveat in `docs/existing-project-onboarding.md` stays in place as historical guidance. Optional non-blocking follow-up: sandbox an `InstructionsLoaded` hook to log which path-scoped rules actually load in a pipeline run; lean against unless the prompt mitigation proves insufficient. Class `other`: no enum slot covers upstream-tool-behavior gaps (same as td-021). Owner-role `unassigned`: resolution is gated on an external upstream stance that won't change, so this row should be promoted to a "documented design constraint" (move to RESOLVED as `wontfix`) in a subsequent ledger sweep once the syntax-checker script confirms no at-risk rules remain. // 2026-05-17: migrated to `wontfix` — exit condition met. `scripts/check_paths_syntax.py` exits clean (all 13 path-scoped rules use a reliable `paths:` form, 0 at-risk); upstream #23478 + #38487 both permanently CLOSED `NOT_PLANNED`; the "read a sibling first" agent-prompt mitigation in implementer/doc-engineer/test-engineer is in place and is a permanent design constraint, not awaited debt. Confirmed grounded-as-constraint by the 2026-05-17 sentinel audit + orchestrator sweep. | f9b9c7817e99 |
| td-048 | suggested | drift | code-to-goals | CLAUDE.md, scripts/test_claude_to_agents.py | code-quality |  | verifier | 2026-07-01 | 2026-07-15 | doc-engineer | resolved | orchestrator (0ad86df) | [direct-capture-contract verification, pre-existing baseline failure] `scripts/test_claude_to_agents.py::test_praxion_root_agents_template_matches_generator` fails: the project-root `CLAUDE.md` has drifted from the generated `AGENTS.md.tmpl` (a `Runtime: Python >=3.11 required` line and other content not mirrored into the generator's expected output). Confirmed pre-existing (TEST_BASELINE.md) and fully unrelated to this pipeline — this task never touches project-root `CLAUDE.md` (only `claude/config/CLAUDE.md.tmpl`, a different file). Resolution: regenerate the AGENTS template from project-root `CLAUDE.md`, or reconcile the drifted lines. Class drift; owner-role doc-engineer. // 2026-07-15: RESOLVED: regenerated AGENTS.md.tmpl from project-root CLAUDE.md via the canonical claude_to_agents.py generator (commit 0ad86df); test_praxion_root_agents_template_matches_generator now green (3/3). The drifted "Runtime: Python >=3.11" bullet is mirrored into the template. | 9efcaf328f4f |
