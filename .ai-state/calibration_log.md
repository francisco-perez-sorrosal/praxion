# Calibration Log

Append-only tier-selection log. Each Standard/Full pipeline (and every tier on completion) appends one row so the sentinel can analyze recommendation-vs-actual match rate (CA01/CA02) over time. See `rules/swe/swe-agent-coordination-protocol.md` and `rules/swe/agent-intermediate-documents.md`.

| Timestamp | Task | Signals | Recommended Tier | Actual Tier | Source | Retrospective |
|-----------|------|---------|------------------|-------------|--------|----------------|
| 2026-04-30 | structurizr-d2-diagrams — Introduce LikeC4 + D2 dual-toolchain for C4-architectural diagrams (coexistence with Mermaid) | 13 ACs, 9 implementation steps + post-verifier iteration, ~26 files touched, 4 ADR drafts, multi-behavior (hook + templates + live migrations + AI tooling), architectural decision (toolchain swap), cross-cutting (rules + skills + agents + commands + templates + live docs) | Standard | Standard | pipeline-explicit (user requested dedicated worktree + signal matrix matches Standard tier: 4-8 behaviors, architectural decision, 9+ files) | On target: Standard-tier ran cleanly end-to-end; one post-verifier fix cycle (T4 call-site typo + ADR re_affirms cleanup); behavioral-contract adherence improved across steps (silent-fix incidence dropped, surfaced objections increased); AC10 numerical-band FAIL was a planning-inventory gap (missed 6 Mermaid blocks in .ai-state/DESIGN.md), not a system defect; verifier verdict PASS WITH FINDINGS confirms tier selection correct — a Full-tier pipeline would have added parallel overhead not warranted for this scope. |
| 2026-04-30 | landscape-watchlist — Per-project curated index of external sources (peer projects, blogs, standards bodies, reference repos) for ideation agents (promethean, roadmap-cartographer); structure follows llms.txt convention with Praxion-specific `last-checked` extension | 5 files (1 shipped template, 1 Praxion seed in .ai-state/, 2 surgical agent edits, 1 slash command), single behavior (curated-landscape feed for ideation grounding), no architectural decision (fits within existing roadmap-synthesis skill home), no SDD spec required, optional ADR consciously skipped (rationale inline in template + command per Stay Surgical) | Lightweight | Lightweight | handoff-anchored (CONTEXT_HANDOFF.md proposed Lightweight; signal matrix matches: ~5 files, single behavior, clear scope, no architect/planner needed, optional researcher invoked) | On target: stayed within Lightweight throughout; one researcher invocation for llms.txt convention details + 48-candidate scan; user pruned the candidate set, added AgentHub via direct URL, narrowed `## Optional` to 2 entries (Every, Sourcegraph blog) per reputation-not-commercial filter. Behavioral contract: surfaced Phase 2.5 placement assumption (held to handoff vs mid-task instinct to use Phase 3.5), registered objection to adopting `llms-full.txt` (rejected as overreach for an inbound-consumed index), asked clarifying questions on starter list + refresh mechanism before spawning researcher. Schema decision worth noting for future agents: the `last-checked YYYY-MM-DD` per-entry field is a Praxion-specific extension to the llms.txt spec — fits inside the colon-notes free-form area but is non-standard; rationale recorded inline in `LANDSCAPE_WATCHLIST_TEMPLATE.md` and `commands/landscape-refresh.md`, not in a separate ADR. |
| 2026-05-12 | dashboard-ux-overhaul — UI/UX overhaul of `dashboard_app/`: design-token layer rewrite, diagram rendering defect fix, Architecture+Metrics page restructures, heading-anchor plumbing, global chrome; 18 first-cut REQs + 3 second-cut REQs, ~25 components touched, 10 new/split files, 6 ADR drafts, parallel execution planned (6 parallel groups) | 9+ files, 5+ behaviors, cross-cutting CSS+components+server modules, architectural decisions (SVG normalization, heading anchoring, page restructures), Full-tier signals | Full | Full | pipeline-explicit (interface-designer + systems-architect + implementation-planner pipeline; user confirmed Full tier via scope) | On target: Full-tier warranted — 18+ REQs, 25+ components, 6 parallel implementation+test pairs, two priority-page structural restructures, a cross-cutting token-layer rewrite with backward-compat aliases, new pure-function modules with unit-test requirements. Behavioral contract applied: surfaced user "with character" palette steer as ADR amendment (dec-draft-1124a9f4); registered and resolved `/api/diagram` route normalization objection (architect's amendment to dec-draft-895ba185); flagged `globals.css` split as optional sub-step with time-box heuristic. Critical path: token layer → chrome adoption → Architecture+Metrics restructures. |
| 2026-05-08 | drop-pep440-dev-prereleases — Remove `.devN` pre-release scheme; align all version files to last released stable (`0.2.0`); simplify versioning skill, release workflow, `/release` command, and README_DEV releases section | 9 changes (4 sub-pyproject + root pyproject.toml version bumps; .claude-plugin/plugin.json was already aligned; 1 workflow drops the post-release dev-cycle step + marketplace reset; 1 command drops `dev` arg+intent row; 1 SKILL.md drops `dev` flag-row + 30-line marketplace policy + cache-staleness recovery; 1 commitizen.md drops PEP 440 pre-release section + `--prerelease` flag row; 1 README_DEV rewrites Releases section); behavioral decision (versioning policy change, shipped-skill simplification); 1 ADR draft authored (`dec-draft-a724a737` cross-referenced to ADR-006 without superseding) | Standard | Standard | direct-execution by main agent (scope fully specified upfront via in-conversation review; no agent fanout warranted — work is mostly deletion + version-string updates, no architectural ambiguity remained after planning conversation) | On target: Standard signal matrix matched (multiple files, behavioral decision, shipped-skill changes), but no architect/planner needed since user co-authored the scope and approved before any file edits. Behavioral contract: surfaced version-target trade-off (0.2.0 vs 0.3.0 vs dry-run answer), surfaced full-vs-minimal scope choice, asked before implementing. Stay-Surgical: deleted dev-cycle ceremony but preserved orthogonal settings (`major_version_zero=true`, `update_changelog_on_bump=false`, alpha/beta/rc remain re-introducible by reversing the ADR). Simplicity-First: replaced 30-line marketplace policy with 4-line marketplace guidance — net ~−60 lines in shipped skill alone. Deviation-from-stated-target: picked 0.2.0 instead of dry-run's 0.3.0 because setting files to 0.3.0 would force a manual `git tag v0.3.0` to avoid double-bumping at next workflow run; 0.2.0 leaves the next `cz bump` atomic. Explained in surfacing so user could redirect. |
