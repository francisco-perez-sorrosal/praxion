# Calibration Log

Append-only tier-selection log. Each Standard/Full pipeline (and every tier on completion) appends one row so the sentinel can analyze recommendation-vs-actual match rate (CA01/CA02) over time. See `rules/swe/swe-agent-coordination-protocol.md` and `rules/swe/agent-intermediate-documents.md`. Row format and the `Retrospective` convention (enum value `correct`/`under-calibrated`/`over-calibrated`/`[pending]`, optionally followed by an em-dash micro-capture note — the Direct-tier learning slot) are specified in `skills/spec-driven-development/references/calibration-procedure.md` § Calibration Log. Rows are append-only; historical rows predating the convention keep their original phrasing.

| Timestamp | Task | Signals | Recommended Tier | Actual Tier | Source | Retrospective |
|-----------|------|---------|------------------|-------------|--------|----------------|
| 2026-04-30 | structurizr-d2-diagrams — Introduce LikeC4 + D2 dual-toolchain for C4-architectural diagrams (coexistence with Mermaid) | 13 ACs, 9 implementation steps + post-verifier iteration, ~26 files touched, 4 ADR drafts, multi-behavior (hook + templates + live migrations + AI tooling), architectural decision (toolchain swap), cross-cutting (rules + skills + agents + commands + templates + live docs) | Standard | Standard | pipeline-explicit (user requested dedicated worktree + signal matrix matches Standard tier: 4-8 behaviors, architectural decision, 9+ files) | On target: Standard-tier ran cleanly end-to-end; one post-verifier fix cycle (T4 call-site typo + ADR re_affirms cleanup); behavioral-contract adherence improved across steps (silent-fix incidence dropped, surfaced objections increased); AC10 numerical-band FAIL was a planning-inventory gap (missed 6 Mermaid blocks in .ai-state/DESIGN.md), not a system defect; verifier verdict PASS WITH FINDINGS confirms tier selection correct — a Full-tier pipeline would have added parallel overhead not warranted for this scope. |
| 2026-04-30 | landscape-watchlist — Per-project curated index of external sources (peer projects, blogs, standards bodies, reference repos) for ideation agents (promethean, roadmap-cartographer); structure follows llms.txt convention with Praxion-specific `last-checked` extension | 5 files (1 shipped template, 1 Praxion seed in .ai-state/, 2 surgical agent edits, 1 slash command), single behavior (curated-landscape feed for ideation grounding), no architectural decision (fits within existing roadmap-synthesis skill home), no SDD spec required, optional ADR consciously skipped (rationale inline in template + command per Stay Surgical) | Lightweight | Lightweight | handoff-anchored (CONTEXT_HANDOFF.md proposed Lightweight; signal matrix matches: ~5 files, single behavior, clear scope, no architect/planner needed, optional researcher invoked) | On target: stayed within Lightweight throughout; one researcher invocation for llms.txt convention details + 48-candidate scan; user pruned the candidate set, added AgentHub via direct URL, narrowed `## Optional` to 2 entries (Every, Sourcegraph blog) per reputation-not-commercial filter. Behavioral contract: surfaced Phase 2.5 placement assumption (held to handoff vs mid-task instinct to use Phase 3.5), registered objection to adopting `llms-full.txt` (rejected as overreach for an inbound-consumed index), asked clarifying questions on starter list + refresh mechanism before spawning researcher. Schema decision worth noting for future agents: the `last-checked YYYY-MM-DD` per-entry field is a Praxion-specific extension to the llms.txt spec — fits inside the colon-notes free-form area but is non-standard; rationale recorded inline in `LANDSCAPE_WATCHLIST_TEMPLATE.md` and `commands/landscape-refresh.md`, not in a separate ADR. |
| 2026-05-12 | dashboard-ux-overhaul — UI/UX overhaul of `dashboard_app/`: design-token layer rewrite, diagram rendering defect fix, Architecture+Metrics page restructures, heading-anchor plumbing, global chrome; 18 first-cut REQs + 3 second-cut REQs, ~25 components touched, 10 new/split files, 6 ADR drafts, parallel execution planned (6 parallel groups) | 9+ files, 5+ behaviors, cross-cutting CSS+components+server modules, architectural decisions (SVG normalization, heading anchoring, page restructures), Full-tier signals | Full | Full | pipeline-explicit (interface-designer + systems-architect + implementation-planner pipeline; user confirmed Full tier via scope) | On target: Full-tier warranted — 18+ REQs, 25+ components, 6 parallel implementation+test pairs, two priority-page structural restructures, a cross-cutting token-layer rewrite with backward-compat aliases, new pure-function modules with unit-test requirements. Behavioral contract applied: surfaced user "with character" palette steer as ADR amendment (dec-draft-1124a9f4); registered and resolved `/api/diagram` route normalization objection (architect's amendment to dec-draft-895ba185); flagged `globals.css` split as optional sub-step with time-box heuristic. Critical path: token layer → chrome adoption → Architecture+Metrics restructures. |
| 2026-05-08 | drop-pep440-dev-prereleases — Remove `.devN` pre-release scheme; align all version files to last released stable (`0.2.0`); simplify versioning skill, release workflow, `/release` command, and README_DEV releases section | 9 changes (4 sub-pyproject + root pyproject.toml version bumps; .claude-plugin/plugin.json was already aligned; 1 workflow drops the post-release dev-cycle step + marketplace reset; 1 command drops `dev` arg+intent row; 1 SKILL.md drops `dev` flag-row + 30-line marketplace policy + cache-staleness recovery; 1 commitizen.md drops PEP 440 pre-release section + `--prerelease` flag row; 1 README_DEV rewrites Releases section); behavioral decision (versioning policy change, shipped-skill simplification); 1 ADR draft authored (`dec-draft-a724a737` cross-referenced to ADR-006 without superseding) | Standard | Standard | direct-execution by main agent (scope fully specified upfront via in-conversation review; no agent fanout warranted — work is mostly deletion + version-string updates, no architectural ambiguity remained after planning conversation) | On target: Standard signal matrix matched (multiple files, behavioral decision, shipped-skill changes), but no architect/planner needed since user co-authored the scope and approved before any file edits. Behavioral contract: surfaced version-target trade-off (0.2.0 vs 0.3.0 vs dry-run answer), surfaced full-vs-minimal scope choice, asked before implementing. Stay-Surgical: deleted dev-cycle ceremony but preserved orthogonal settings (`major_version_zero=true`, `update_changelog_on_bump=false`, alpha/beta/rc remain re-introducible by reversing the ADR). Simplicity-First: replaced 30-line marketplace policy with 4-line marketplace guidance — net ~−60 lines in shipped skill alone. Deviation-from-stated-target: picked 0.2.0 instead of dry-run's 0.3.0 because setting files to 0.3.0 would force a manual `git tag v0.3.0` to avoid double-bumping at next workflow run; 0.2.0 leaves the next `cz bump` atomic. Explained in surfacing so user could redirect. |
| 2026-05-19 | test-topology-activation — Activate the test-topology protocol (M2): advisory growth trigger (sentinel TT06 + systems-architect Phase 2) + behavioral wiring of six pipeline agents to author/honor TEST_TOPOLOGY.md and the per-step Tests field + new /refresh-topology command, language-agnostic via the trunk/leaf split | 12 shipped/state artifacts (6 agent defs, 2 skill files, 1 new command, 1 always-loaded rule, orchestrator config template, 2 architecture docs), 18-REQ behavioral spec, 4 ADR drafts, cross-cutting (agents + skills + command + rule + orchestrator), no production code (context-artifact wiring) | Full | Full | pipeline-explicit (user approved "Full M2 in one pass" via AskUserQuestion at intake; signal matrix matches Full — 9+ artifacts, 5+ behaviors, cross-cutting) | On target: Full-tier warranted. Research-first per user request — researcher agent skipped because the orchestrator verified both gaps (GAP A no growth trigger, GAP B agents not wired) directly against source, so codebase context sufficed. Decomposed into 3 sequential parallel groups: trunk foundation → 8 agent/skill/command edits in 3 concurrent context-engineer batches → orchestrator config + doc catalog. Verifier verdict PASS WITH FINDINGS (2 FAIL + 4 WARN — all ADR/artifact hygiene; behavioral work all 18 REQs + 10 ACs PASS). Rework applied inline by the orchestrator (Direct-tier fixes — rework worktrees not warranted for ~6 tiny edits, per match-weight-to-scale). Orchestrator re-validation caught 2 YAML-validity bugs the verifier missed: an unquoted `Tests:` token in two ADR-draft `summary:` fields would have broken finalize_adrs.py at merge. Behavioral contract: both gaps surfaced with file/line evidence before acting; phase-transition surfacing at architecture and pre-verification boundaries; AskUserQuestion at intake for the 2 forking design calls (scope, trigger home). |
| 2026-06-04 | factory-agent-readiness — Embed a Factory-style Agent Readiness scorer in `/project-metrics`: stdlib-urllib LLM judge + mechanical collector, dashboard section, agent-readiness skill; ships to every managed project via `claude plugin install` with zero install.sh dependency edits | ~21 files (Python engine package + collector + cli enrichment + 5 test modules, 5 dashboard files, skill + rubric, 4 docs/config, 3 ADRs), 10 ACs, 7 REQs, 3 ADRs (dec-213/214/215), cross-cutting (Python + TypeScript + skill + docs + installer), architectural decisions (judge transport, determinism boundary, embed-vs-sibling) | Standard | Standard | pipeline-explicit (user requested dedicated worktree + standard-tier pipeline; signal matrix matches Standard: 4-8 files-class scope across two languages, 3-4 behaviors, architectural decisions) | On target: Standard ran end-to-end (researcher → systems-architect → implementation-planner → two parallel implementer tracks + Group H → verifier). Verifier verdict SHIP-WITH-FOLLOWUPS (10/10 AC, 7/7 REQ, 0 FAIL, 3 WARN); two WARNs fixed inline (enrich_readiness 53→30 lines via `_mark_llm_scored` extraction; dec-206 `re_affirmed_by` back-ref), third (`next build`) was a worktree node_modules-symlink/Turbopack limitation, confirmed green in the canonical checkout post-merge. Notable failure-recovery pattern: all four implementer/architect spawns hit turn-limit or socket cutoffs near their finish (architect socket-died at Phase 2.5 then resumed; 3 implementers cut off before writing deliverables; 1 misreported dashboard count 304 vs actual 337) — orchestrator re-derived state from the filesystem at every handoff, wrote the missing TEST_RESULTS/traceability fragments, and caught a real TS `noUncheckedIndexedAccess` type error that would have broken `next build`. Behavioral contract: surfaced the default-on-LLM contract shift (network/auth entering a previously pure-offline package) with a graceful-degrade reconciliation; user overrode opt-in→default-on; judge-transport ADR (dec-214) re-affirms dec-206 (no subprocess-`claude` under CLAUDECODE=1). |
| 2026-06-16 | solid-ai-era-embedding — Embed SOLID for the AI era into Praxion's philosophy/process: new "Balanced Coupling" principle + canonical software-design-principles skill (SOLID-as-heuristics, balanced-coupling model, agent-as-consumer), composed by software-planning (up-front) and refactoring (reactive), enforced via fitness functions, with systems-architect/verifier awareness | 9 artifacts (1 philosophy tmpl, new skill = SKILL.md + 2 references + README, 2 composing-skill cross-links, 1 fitness-functions citation example, 2 agent defs), 1 ADR draft, cross-cutting (philosophy + skills + agents), architectural/philosophy decision | Standard | Standard | user-explicit (user requested exhaustive research + flexible embedding in a dedicated worktree; signal matrix matches Standard — context-artifact work across philosophy + skills + agents, architectural decisions) | On target: research-then-design task. Sonnet rate-limit blocked the researcher subagent → main agent (Opus) mapped the Praxion surface directly via grep/Read (Shipped-Explore-style fallback applied to researcher). Two user-decided forks via AskUserQuestion (deep-tier home, philosophy aggressiveness); user chose dedicated principle + canon orthogonal to both pipeline skills. Behavioral contract: surfaced the worktree-base + gitignored-rendered-CLAUDE.md + fitness-citation-regex constraints as load-bearing assumptions before editing; worktree guard correctly blocked an initial cross-tree write (redirected to worktree paths); Simplicity-First held the philosophy delta to one paragraph and the skill to SKILL.md + 2 references. Heuristics-not-commandments guard threaded through principle, skill, and both agent edits to prevent dogma. |
| 2026-06-22 | agent-truncation-recovery — Make pipeline step-completion a *verified fact from ground truth* (not the agent-authored WIP checkbox) so a context-ceiling-truncated subagent can't advance the pipeline on a false `[COMPLETE]`: new `reconcile_pipeline_state.py` (6-verdict state machine; Tier-1 git+tests is arbiter, `observations.jsonl` WAL is localization-only; ground truth — not the checkbox — drives verified-complete, catching the died-before-checkbox case) + `/resume-pipeline` (auto-mark verified / auto-resume partials scoped to remainder / surface unknown; 5-surface audit trail) + completion-handshake contract (always-loaded rule + deep-dive) + implementer write-ahead `[IN-PROGRESS]` prevention | 13 files: 2 new scripts (reconciler + 20-test suite incl. mutation-proven gate-liveness canary), 1 new command, 1 ADR draft, 4 contract/doc edits (rule + skill ref + implementer + detector exemption), DESIGN.md recovery-loop component (4th feedback loop), docs/architecture.md + 2 catalogs + onboarding observability note | Standard | Standard (Opus-bracketed) | pipeline-explicit (user: "best way possible … quality and reliability as northstars … take into account the praxion process") | On target. Opus-bracketed because Sonnet quota blocked implementer/planner/test-engineer: architect (Opus) → orchestrator-implement (Opus) → verifier (Opus); user picked this fork via AskUserQuestion. Architect **Register-Objection deleted a planned component** (a redundant `AGENT_TRACE` journal — the `observations.jsonl` WAL already existed; only its consumer was missing) — Simplicity First in action. Real-data dry-run against a live sandbook pipeline caught **4 bugs clean fixtures hid** (substring `"error"` matching `ModuleNotFoundError`; workstream step-number collision; checkbox-vs-ground-truth inversion; unexpanded `Files:` globs) — the empirical "pair every claim with a verification path" discipline applied to the tool itself. Verifier PASS (0 FAIL, 3 WARN — all fixed inline; rework worktree not warranted for 3 doc/convention fixups, per match-weight-to-scale). Post-verify the **user caught a real ship-vs-wire gap** (reconciler not `chmod +x` → not PATH-linked → `/resume-pipeline` would silently fail in managed projects, though it worked in tests + self-host); fixed shebang + chmod + command invocation path. Behavioral contract: reliability-hierarchy (Tier-1 arbiter, WAL hint-only, ambiguity→`unknown`) made recovery safe-by-construction; load-bearing assumptions surfaced at every checkpoint. |
| 2026-06-26 | wave1-honesty-sweep — Stop `.ai-state/DESIGN.md` + `dec-248` asserting capabilities that don't exist; restore the DESIGN↔DESIGN_CHANGELOG split (§1 1,830→193 chars); correct the `TECH_DEBT_RESOLVED` schema pointer; remove the dead `evals/baselines/` orphan (B1 doc-half, B2, B3, A6, PF-06; partials R17/R19) | ~5 doc/state files, no production code, single behavior (documentation honesty), no architectural decision | Lightweight | Lightweight | handoff-anchored (process-flow analysis §0.1 Wave 1 scope) | **Post-hoc entry appended 2026-06-26 during the Wave-1–3 audit-remediation pass** (row was owed but not logged at completion — flagged here so CA01/CA02 match-rate analysis stays honest). On target — doc-truth corrections with no code and no architecture warranted Lightweight. External audit verdict: PASS-WITH-FINDINGS (every Wave-1 claim re-verified on disk; no regressions). |
| 2026-06-26 | wave2-criticals — WAL bounding (best-effort rotation in `hooks/_hook_utils.py` + windowed `_read_wal` in `reconcile_pipeline_state.py`) and the TASK_BRIEF floor (mandatory at Standard/Full + sentinel P06 + verifier WARN); R1 + R2; ADRs dec-249 + dec-250 | rotation + reconciler code + always-loaded rule + goal-disambiguation skill + sentinel/verifier wiring; 2 ADRs; 2 behaviors (WAL bound, criteria-thread floor); architectural decisions (rotation shape, floor placement) | Standard | Standard | pipeline-explicit (branch `wave2-criticals`; architect → implement → verifier) | **Post-hoc entry appended 2026-06-26 during the audit-remediation pass.** On target — Standard warranted (2 ADRs, architectural rotation/floor decisions, cross-file but cohesive). Verifier PASS (1545 tests at the time); the dogfood TASK_BRIEF carried its Key-Signal check live. External audit: PASS-WITH-FINDINGS — the one real finding (EA-01) was a durability-prose overclaim in dec-250, not a behavior defect, corrected in this same remediation pass. |
| 2026-06-26 | wave3-production-gates — Registry declarative spine (`production_gate`/`cleanup_policy` on all 24 artifacts; dec-251) + four production gates (spec-archival SH08, calibration CA03, challenge P07, stale-slug P08; dec-252) + R5 LEARNINGS→td harvest + A6 DESIGN_CHANGELOG producer; archived its own 13-REQ spec | 9+ files (registry + 2 detector scripts + tests + sentinel + verifier + rule + spec + 2 ADRs), 5 R-items (R3/R4/R5/R9/R13), cross-cutting, architectural (declarative-spine shape, gate-liveness proofs) | Standard | Standard | pipeline-explicit (branch `wave3-production-gates`; verifier with one rework loop) | **Post-hoc entry appended 2026-06-26 during the audit-remediation pass.** Brushed Full signals (9+ files, 5 R-items, cross-cutting) but stayed Standard because the work was one cohesive theme (declarative spine + sibling detectors), not 5 independent behaviors — defensible, though a Full split into parallel gate-implementer pairs was the arguable alternative. Verifier PASS after one rework loop (id-citation + plugin-cache fixed in place); 1561 tests green. External audit: PASS-WITH-FINDINGS — the registry production-vs-detection-gate nuance (EA-02, accepted by design) and the §0.1 count drift (EA-04, this row) surfaced here. |
| 2026-06-27 | wave4a-waste-pruning — Periphery hygiene from the process-flow analysis §5/§6: doc_manifest summary+frontmatter strip 484KB→163KB (R12), token_budgeting retirement + historical-retained state reversal (R17), retain-last-N report pruning (`prune_reports.py` + 70-report dogfood; R11), single living idea-ledger (R7) | 5 R-items, ~12 files (1 new script + test, build_doc_manifest, promethean, sentinel, project-metrics, 2 lifecycle docs, 7 ledger file-ops), 3 ADR drafts, mixed mechanism+policy+content, no deep architecture | Standard | Standard (orchestrator-driven batch) | pipeline-explicit (user chose "sequenced, 4a waste first" via AskUserQuestion) | On target — Standard batch warranted (3 ADRs, 5 R-items, cross-file) but executed orchestrator-as-implementer rather than full agent fanout: each item was well-specified by the analysis and mostly mechanical, so no architect/planner added value. Behavioral contract: **Register-Objection moved R18 to 4b** (it's an EA-11 registry-consumer concern, not waste-pruning) and split R12 into strip (4a) vs regen-wiring (4b); **Surface-Assumptions widened R12** from the analysis's literal "strip summaries" to also drop the larger redundant `frontmatter` embed (the 47%-vs-25% half) after confirming no consumer reads it; grounding caught two `build_doc_manifest` globs that would have hidden the renamed living idea-ledger (a regression no test would flag). Each item = one logical commit; full suite 1568 green throughout; R11 dogfood pruned 70 backlog reports. |
| 2026-06-27 | wave4b-registry-seams — Registry & dead seams: split `detection_gate` from `production_gate` + canary (EA-02, dec-256), `build_doc_manifest` reads the registry as the first reading consumer (R18/EA-11), correct the false doc_manifest auto-regen claim (R12b honesty half; hook deferred), dead-seam ledger row td-044 (R15) | ~7 files (registry + 2 tests + build_doc_manifest + document-api + ledger + manifest regen), 1 ADR draft, one architectural decision (registry schema), bounded | Standard | Standard (orchestrator-driven batch) | pipeline-explicit (user chose 4b after 4a; approved the detection_gate fork via AskUserQuestion) | On target — Standard batch with one genuine architectural decision (the EA-02 `detection_gate` schema), surfaced to the user as a fork before implementing. Behavioral contract: **Register-Objection deferred R12b's auto-regen *hook*** (build_doc_manifest has no content-aware write mode → a naive finalize regen would churn `generated_at` on every on-main commit; sentinel F11 already *detects* staleness, so a hook is disproportionate now) while delivering R12b's honesty half; **EA-11 closed only partially** — R18 reads the membership projection, but per-artifact `cleanup_policy` still has no reader (the per-dir/per-file granularity gap, recorded in dec-256's Disconfirmation and surfaced to the user). Hit the ruff-version-skew commit loop (pre-commit 0.8.6 vs local 0.15.4 disagree on `assert` formatting) — resolved by staging the pinned-formatted tree via Bash, not re-editing. Manual doc_manifest regen dogfooded the R12b workflow (the manifest had silently missed dec-253/254/255 since the 4a merge). Full suite 1572 green. |
| 2026-06-28 | wave4c-r8-feedback — R8/D1 close the readiness write-only loop (A1: sentinel RD01 routes below-floor readiness into the existing sentinel→promethean edge) + document eval human-gating as deliberate (B2) | ~9 files (new detector + test, 3 fixtures, 4 markdown edits sentinel/skill/eval/DESIGN), 1 ADR draft (finalized to dec-257), 2 behaviors + one architectural fork (feed-back vs document), bounded | Standard | Standard (full agent pipeline: architect → planner → implementer → verifier) | pipeline-explicit (user: "let the architect decide the fork and implement what he decides") | On target — Standard warranted by a genuine architectural fork needing an ADR; the architect's deciding role was load-bearing, so full agent fanout was justified (unlike 4a/4b orchestrator-as-implementer). Notable: (1) **implementer return truncated** with no terminal marker → orchestrator re-derived completion from ground truth per the completion-handshake protocol (re-ran the gate-liveness canary + full suite, confirmed done, completed the truncated traceability.yml REQ→test bookkeeping) rather than advancing blindly or re-spawning a finished agent; (2) caught the architect prematurely tagging DESIGN.md §5 "Built" — the exact Theme-B capability-claim drift this Wave-4c work fights — and corrected it to "Designed" until verifier PASS, flipping to "Built" only at gated Step 7; (3) verifier PASS-WITH-FINDINGS, sole WARN W-01 (dangling `dec-89c07cfc` ref that finalize_adrs.py's literal `dec-draft-` replace would skip) folded into Step 7. RD01 ships armed-and-red on Praxion's own level-2 mechanical-only readiness — the intended true finding per D1. Full suite 1288 green (tests/ scripts/). |
| 2026-06-28 | wave4c-tail — Wave 4c hygiene batch (3 file-disjoint items run in parallel): EA-10 (datetime.UTC → timezone.utc portability sweep across hooks + scripts), R19b (content-gate the program.md ML-detection signal in onboard-project), EA-09 (document the id-citation dogfood asymmetry in README_DEV) | 3 disjoint items, ~12 files (10 EA-10 + 1 each R19b/EA-09), no architectural decision, one user decision (EA-09 document-vs-dogfood) | Lightweight | Lightweight (EA-10 via implementer; R19b/EA-09 orchestrator-direct; each item one commit) | parallel-batch (user: "move to the next 4c items that don't collide") | On target — mechanism/content/policy items on disjoint files parallelize cleanly. Behavioral contract: **Register-Objection scoped EA-10 down** — dropped 3 docstring-only files (runner/git_collector/test_integration; no executable UTC) to keep the commit surgical, while keeping the implementer's UP017 pyproject suppression (required, or ruff reverts the fix). Grounding flipped EA-09 to "document" (180 mostly-legitimate meta-test fixture violations make dogfooding disproportionate) and found R19a already shipped in Wave 1. Recovered cleanly from a self-inflicted `git stash --keep-index` mishap (no work lost; lesson: never stash against a populated index mid-commit). scripts/ suite 1077 green. |
| 2026-06-29 | wave4c-tail-2 — Wave 4c parallel batch (3 disjoint-domain items): EA-06 (mechanical CODE-kind P06 gate-liveness checker + tmp_path canary, replacing a PROMPT-only gate), R9-dash (dashboard Active/Done completion-state split), D2 (first `/skill-genesis` run validating the dormant harvest loop) | 3 items, ~13 files, no architectural decision; agent-fanout (2 implementers + skill-genesis, all background, disjoint domains) | Lightweight | Lightweight (EA-06 + R9-dash via parallel implementers; D2 via skill-genesis) | parallel-batch (user: "do all that can be done in parallel if it makes sense and we don't screw anything") | On target — disjoint *toolchains* (Python/ruff vs TS/Biome) made parallelism safe; each verified from ground truth before commit. **Two defects caught at the ground-truth gate that "tests pass" hid:** R9-dash's build failure was a pre-existing missing-`@scalar` install (not the change); EA-06's golden-bad-case fixture sat under a gitignored `.ai-work/` path → would fail in CI → one rework loop to a `tmp_path` canary. **Register-Objection deferred 3 items** as too risky/large for a parallel batch: EA-11-reader (file-deletion logic), R10 (Wave-5 capstone), R12b-hook (churns `generated_at`). |
| 2026-06-29 | criteria-spec-eval — R10/Wave-5 capstone: integration eval driving the criteria→spec chain (`TASK_BRIEF → SYSTEMS_PLAN → traceability.yml → SPEC archival`) end-to-end through four real validators, proving dec-252 liveness (faithful PASSES, per-link BITES) | 2 committed code/decision files (1 new test module + 1 ADR draft) + 2 status-doc edits; 1 architectural decision (form a pytest-suite vs b /eval-praxion vs c eval/-harness — the always-green-vs-out-of-band boundary); 12 ACs / 10 REQs; full agent pipeline | Standard | Standard (full agent pipeline: researcher→systems-architect→implementation-planner→test-engineer→verifier) | pipeline-explicit (user requested Standard tier + dedicated worktree + full pipeline, "maximum care") | On target — Standard warranted by a genuine architectural form decision (needs an ADR) plus a load-bearing gate-liveness guarantee. The deliverable is **entirely test code** (the eval imports existing validators, writes no production code), so the orchestrator ran the **test-engineer as the sole execution agent** — Register-Objection against spawning an implementer with no disjoint production code to write. Notable: (1) the **architect's Register-Objection** corrected a wrong id-citation rationale inherited from research — committed `tests/*.py` IS scanned (only `tests/fixtures/` exempt), so the `f"REQ-{n:02d}"` builder idiom is the real compliance mechanism, not "runtime strings unscanned"; (2) the orchestrator **verified from ground truth, not "tests pass"** — an independent bite-proof (7/7 incl. a control case isolating link-4's load-bearing stale-date break) confirmed the four per-link canaries are live, and a REQ-flow "assumed-not-proven" gap was closed inline (the faithful test now asserts the same REQ IDs across all four artifacts, not just the SPEC — the core G1 "live evidence, not prose" claim made literal); (3) verifier **PASS WITH FINDINGS** caught **W-01** — an out-of-scope `SPECS_INDEX.md` index-staleness edit from a dev-time `regenerate_specs_index.main()` — reverted to stay surgical. SPEC archival of this feature deliberately deferred (a faithful regen would bundle the unrelated index-drift fix; not in stated acceptance). Form-decision ADR dec-258 re-affirms dec-040 (eval out-of-band). Full suite **216** (`tests/`) + **1086** (`scripts/`) green. |
| 2026-06-29 | cleanup-policy-reader — EA-11: wire `clean_work_safety.py` to READ `artifact_registry.cleanup_policy` instead of hardcoded deletion classes (the highest-risk backlog item — governs what `/clean-work` DELETES, a one-way door) | 3 code files (clean_work_safety refactor + a registry read-helper + 14 new tests) + 1 ADR draft + 2 status-doc edits; 1 architectural granularity decision (per-file-policy ↔ per-dir-verdict, Option a, re-affirms dec-256); 14 ACs / 9 REQs; behavior-preserving refactor of safety-critical logic | Standard | Standard (characterization-tests-first refactor; full agent pipeline architect→planner→test-engineer→implementer→verifier + intra-step light-review at the RISKY seam) | pipeline-explicit (user: "highest-risk change in the backlog… deletion is a ONE-WAY DOOR. Treat accordingly", characterization-tests-FIRST) | On target — Standard warranted by a genuine architectural granularity decision (ADR) plus the safety-criticality of a one-way-door deletion change. Characterization-tests-first was the spine: 12 new pins of the EXACT pre-refactor BLOCK/WARN/SAFE verdicts were proven GREEN on UNCHANGED code (SHA 426b3f1) BEFORE any refactor, so they characterize *current*, not *desired*, behavior. Behavior-preservation verified from ground truth at three independent levels: (1) 41 unit pins green; (2) the old-vs-new **dry-run diff over 22 LIVE `.ai-work/` dirs is EMPTY** — the integration-level safety gate, because fixtures alone could miss a real-world shape; (3) a **genuine-consumption** test proves the registry read is load-bearing (verifier mutation-confirmed: hardcoding the severity makes it FAIL). Deletion-monotonic floor (unknown/`archive`→WARN, never SAFE) + a REQ-09 set-equality **drift test** with a `FAKE_ARTIFACT` canary make the new code↔registry coupling self-guarding. Granularity ADR **re-affirms dec-256, not supersedes** — the reader materializing discharges dec-256's own reversal-trigger. The RISKY one-way-door step got an intra-step verifier **light-review** (PASS WITH NITS) at the implementer→planner seam + a full verifier at the end (**PASS WITH FINDINGS**, "no path found by which the change could delete a file it should not"). KS7 deployment-symlink import de-risked empirically up front (same `_repo_root` sibling-on-`sys.path[0]` mechanism). One cosmetic nit (double `_severity_for` call per site) consciously deferred — re-touching the verified file would invalidate the empty-dry-run-diff evidence for nil behavioral gain. SPEC archival deferred (a faithful regen would bundle the unrelated `SPECS_INDEX.md` staleness). Full suite **1100** (`scripts/`) + **216** (`tests/`) green. Granularity ADR dec-259 (re-affirms dec-256). |
| 2026-06-29 | manifest-volatile-split — R12b done *properly*: stop the committed `doc_manifest.yaml` from indexing volatile gitignored `.ai-work/` (Option B), add a content-aware write + an existence-gated finalize-time regen (the last deferred Wave-4 item) | 9 files (build_doc_manifest producer + finalize_chain.sh hook + 3 test files + artifact_registry dead-function removal + 3 prose-coherence sites) + 1 ADR draft + 2 status edits; 1 architectural decision (committed-static vs live-dynamic manifest split; dec-256/R18 coherence); 11 ACs / 8 REQs; cross-cutting (producer + finalize chain + registry + docs) | Standard | Standard (full agent pipeline researcher→architect→planner→implementer×2→verifier + intra-step light-review at the RISKY finalize-hook seam) | pipeline-explicit (user: "address R12b properly if it's small" → orchestrator scoped it, found it NOT small, surfaced the real blocker + 3 options, user chose B) | On target — **scoping-before-committing earned its keep**: "if it's small" → the orchestrator read the producer and found the content-aware write IS tiny but the auto-regen hook is blocked because the committed manifest indexes **volatile `.ai-work/`** (a fresh regen yielded **2** entries vs **61** committed — the same `main` commit produces different manifests, a category error). Register-Objection: NOT small; the §0.1 deferral (`generated_at` churn) understated the real blocker. Surfaced 3 options; user chose B (the real fix). Research nailed the blast radius: the dashboard's workshops view already reads `.ai-work/` LIVE, so removing the manifest's entries dropped only a permanently-stale docs-nav group (no functional breakage). The **dec-256/R18** interaction (removing build_doc_manifest's registry read undoes R18's "first reader") was framed as a coherence improvement — the read served the removed `.ai-work/` walk; the genuine-reader property is preserved by dec-259's `clean_work_safety` reader; new ADR, no supersession. Existence-gated finalize regen (never creates a manifest in a non-opt-in project) — light-review ACCEPT. **The verifier's standout catch (W-01): a close-out-LOCATION hazard the orchestrator's OWN checks missed** — build_doc_manifest's pre-existing `.claude` exclusion means a regen *from this worktree* (under `.claude/worktrees/`) silently drops ALL `docs/`; the orchestrator's "310 durable == baseline" check *passed* because it compared two identically-contaminated in-worktree numbers (both missing docs). The verifier broke the symmetry by running the pre-change generator (true = **349**). Decorrelation in action: an author's verification shares the author's blind spot — here, literally the cwd. Fix: regen from the canonical checkout; the new finalize hook does exactly that at merge (dogfooded). Orchestrator coherence catch: removed the now-dead `dashboard_artifacts_ordered()` (plan kept it; it became dead when its sole caller was removed) + a 2nd "four consumers" prose site the plan missed. Verifier **PASS WITH FINDINGS** (no FAIL); full suite **1103** (`scripts/`) + **216** (`tests/`) green. Residuals deferred (not regressions): the pre-existing `.claude`-exclusion in-worktree-regen bug; a stale `files.ts:11` comment (no-touch-dashboard decision). ADR dec-260. |
| 2026-06-20 | healthcheck-baseline — Extend _detect_healthcheck to bounded subtree scan for monorepo-aware service detection (subdir Dockerfile HEALTHCHECK, package.json signals, route handler in health-named dirs) | 1 architectural decision (detector subtree scanning), 2 files (checks.py + tests), bounded scope, observability/monorepo | Standard | Standard | post-hoc backfill (direct-capture-contract data-hygiene pass) | [pending] — Post-hoc entry appended 2026-07-01 during the direct-capture-contract data-hygiene pass; row was owed but not logged at completion. |
| 2026-06-20 | agentic-reliability — Blend five frontier practices (runtime guardrails, eval loop, observability, simulation testing, cost/latency budgets) into skills, not a new agent | ~19 files (2 new skills + deepened references in 2 existing skills, no new rules), 5 behaviors, cross-cutting, architectural decision (skills-not-agent) | Full | Full | post-hoc backfill (direct-capture-contract data-hygiene pass) | [pending] — Post-hoc entry appended 2026-07-01 during the direct-capture-contract data-hygiene pass; row was owed but not logged at completion. |
| 2026-07-01 | direct-capture-contract — Close the Direct-tier durable-capture leaks: commit-time calibration nudge hook (fail-open, lag-gated K=2, worktree/bump-suppressed), CA03 widened tier-blind, one-line-row + Retrospective micro-journal contract, orchestrator ADR authorship + tier-vs-execution-mode taxonomy, skill-genesis sixth source, dec-133 correction + 2-row backfill | ~20 files (1 new hook + canary suite, 1 detector widening + test inversion, 2 always-loaded rules, 2 skill refs, 1 agent def, 1 tmpl trim, .ai-state hygiene), 9 ACs / 9 REQs, 2 ADR drafts, cross-cutting, architectural decision (capture-contract shape) | Standard | Standard | pipeline-explicit (3-lens internal research → architect + context-engineer shadow → planner → parallel implementers + TDD hook pair → verifier) | correct — on target; Standard warranted (cross-cutting, ADR-worthy capture-contract decision). Shadow pattern paid twice: context-engineer caught a schema-vs-prose conflict (Retrospective closed enum vs micro-journal) and the generated-vs-.tmpl config target the architect missed. Verifier PASS WITH FINDINGS (0 FAIL, 4 WARN; W-02 fixed inline, W-04 → td-045). Two parallel-execution incidents recovered from ground truth (git index race; Step 3 truncation + shared-stash) — pathspec-scoped commits should be in every parallel implementer prompt from the start. Budget gate honored via amended cap: +585B ≤ +600 after a same-topic tightening pass. Full suite 1636 green vs baseline. |
| 2026-07-01 | debt-batch-1 — Resolve 6 ledger rows in one parallel wave: readiness canary (td-047), remind-family canary coverage (td-045), pytest-cov subprocess combine (td-053), Explore-spawn envelope bug (td-021, root-caused deeper than recorded), retire dead producers (td-044/dec-263), accept drift-detection posture (td-046/dec-264) | 6 disjoint items, ~9 files, 2 ADRs, no architectural coupling between items | Lightweight | Lightweight | parallel-batch (3 test-engineers + 2 architects + orchestrator probe/fix; ledger flips and fragment commits centralized in the orchestrator to avoid shared-file races) | correct — batch parallelism paid off; two live catches: the td-021 probe reproduced a NEW envelope root cause the prior fix+canary had validated against its own wrong shape (lesson: canaries must assert the CONSUMER contract, not the producer assumption), and un-crashing pytest-cov unmasked a stale codex-bridge assertion. One orchestrator index-race near-miss recovered (pathspec discipline). |
| 2026-07-01 | debt-batch-2 — Resolve td-029 (doc-surface allowlist), td-034 residual (deterministic worktree-path briefing hook), td-036 (install.sh --dev-link); plus same-wave orchestrator fixups (codex-bridge test aligned to unwrapped updatedInput; dec-219 partial-supersession cross-ref) | 3 disjoint items, ~11 files, no ADR (dec-263/264 were batch-1), one new hook + one new install mode + one security gate | Lightweight | Lightweight | parallel-batch (3 implementers; one truncated return recovered from ground truth per completion handshake) | correct — parallel lanes stayed collision-free with pathspec discipline; td-036 return truncated at final verification, work verified complete from ground truth (syntax + 17/17 tests) and committed by orchestrator; td-034 surfaced an unverified multi-hook updatedInput chaining assumption, extracted to td-049 rather than left implicit. |
| 2026-07-02 | dashboard-debt-trio — Close td-024/025/027: jsdom render-test infra (404→447 tests), three real Diátaxis shells, five per-artifact renderers behind the manifest renderer: key (dec-265); interface-designer led, 10-step plan, TDD renderer pair, forced light-review on the registry seam | ~30 files, 9 commits, 1 ADR, 2 parallel streams after a blocking infra step, Standard pipeline in dedicated worktree | Standard | Standard | pipeline-explicit (interface-designer → planner → parallel implementers/test-engineers → light-review → verifier PASS WITH FINDINGS) | correct — on target; the designer earned its stage twice (renderer: as the only sound resolver key; two rows had stale premises vs the now-existing /metrics + /architecture routes). Two live incidents: a haiku doc-engineer committed in the canonical tree despite absolute-path briefing (relocated surgically; direction gap filed as td-051; routing lesson: cross-tree briefings warrant sonnet+) and finalize_adrs mis-split a hyphenated user slug despite branch: frontmatter (repaired; td-052). Verifier re-derived all signals; a11y defect found en route filed as td-050. |
| 2026-07-02 | debt-findings-fix — Close the four program-discovered rows: consolidate subagent prompt injections into one emitter (dec-266; td-049 eliminated + td-051 covered, both live-proven via dev-link probe), DiagramModal a11y sibling restructure (td-050), finalize_adrs Tier-0 branch-token strip (td-052); plus calibration-log coherence pass (2 same-session rows normalized to the enum schema, header format pointer added, coverage check current) | 4 disjoint fixes, ~11 files, 1 ADR, 3 parallel implementers + orchestrator (ADR author, log curation, live probe) | Lightweight | Lightweight | parallel-batch (orchestrator-authored ADR under the dec-262 sanction; fixes verified red-then-green) | correct — key learning: for unverifiable cross-layer contract assumptions, elimination beats verification (dec-266 dissolved the chaining question instead of probing it); dev-link (shipped hours earlier) enabled same-session live proof of both injection directions — the program consumed its own outputs twice in one wave. |
| 2026-07-02 | railway-integration — Integrate Railway into Praxion's provider surface: external research pass, new deployment-skill reference consuming Railway's first-party plugin/MCP/CLI (consume-not-vendor, dec-267), cicd Railway Deploy workflow example, staleness catalogs extended, cross-links + catalogs updated | 6 files (1 new reference + 5 edits), 1 ADR draft, single behavior, docs/skills only, no code | Lightweight | Lightweight | researcher-assisted direct edits (1 background researcher; user set the deployment-lane constraint mid-intake; shape/scope question timed out AFK → recommended defaults applied, onboarding detection deferred as named follow-up) | correct — Railway ships a complete standard Claude Code plugin (MIT) + CLI-embedded MCP, so integration collapsed to a recipe + auth matrix; the load-bearing external fact (remote MCP rejects project tokens → CLI is the only headless path) came from research, not training data. Follow-up pending user: /onboard-project Railway detection. |
| 2026-07-15 | sentinel-report-fixes — Resolve the 2026-07-15 sentinel report's non-length Important+Recommended findings: catalog drift in rules/commands READMEs (I1/S1), td-038/td-039 ledger id collisions + uniqueness guard (I2), DL06 re_affirmed_by backfill across 36 links + finalize self-heal (I3), stale readiness-metrics refresh (I4) | 4 disjoint findings, ~40 files (31 mechanical ADR frontmatter backfills), 2 script guards + 6 regression tests, no architectural decision | Lightweight | Lightweight | parallel-batch (doc-engineer + 2 implementers concurrent, metrics CLI in background; orchestrator verified all claims from ground truth and centralized pathspec-scoped commits) | correct — batch parallelism fit the disjoint-fix shape; bonus catch: rules/README.md's Always-Loaded table carried 3 pre-existing misclassified path-scoped rules (coding-style, eval-driven-verification, gpu-budget-conventions) that sentinel C05 misses because it counts files without diffing frontmatter against table placement; I4 dissolved rather than escalated — a full (LLM-tier) metrics run scored readiness level 3, meeting the Practiced floor, exposing the stale level-2 as a mechanical-only floor artifact, no user action needed. |
| 2026-07-15 | readiness-judge-evidence — Fix the c.testing.test_quality readiness evaluation: replace the directory-listing judge artifact with a per-criterion gatherer registry and a bounded multi-section test-quality evidence bundle (framework config across monorepo apps, coverage-collector reuse, test inventory, deterministic code samples, policy docs) | 4 files (1 new module + 1 new test file + cli delegation + doc), single behavior, extensibility explicitly requested, no architectural decision | Lightweight | Lightweight | direct (orchestrator implemented after reading the collector contract; no agent fan-out — design integrity favored a single context) | correct — live-verified: judge flipped test_quality to pass on an evidence-grounded rationale and readiness rose level 3→4 (97.8% adjusted pass); one live catch: Runner.run's collectors map is heterogeneous (CollectorResult or skip-marker dict), surfaced only in full-suite order where an earlier test's env pollution let enrichment reach the new accessor — fixed with duck-typing + regression test; lesson: judge-input quality bounds judge-verdict quality — same context-engineering law as everywhere else. |
| 2026-07-16 | judge-test-hermeticity — Close the env-pollution hazard the readiness-judge-evidence pass surfaced: test_dotenv.py leaked fake Anthropic credentials process-wide (monkeypatch restores only test-touched keys, not keys the code-under-test writes), letting integration tests pass the readiness auth gate and POST real judge requests mid-suite | 3 test files, single behavior (test hermeticity), no production code touched | Direct | Direct | orchestrator (root-cause fix in test_dotenv.py + package-wide autouse credential scrub in conftest + liveness canary) | correct — proven three ways: in-process post-suite env check (0 leaked keys), full package green under ambient fake credentials (643 passed, canary bites if the guard is removed), bare repo-wide run green (1377) since testpaths collects tests+scripts into one process where the leak crossed packages; lesson: monkeypatch teardown restores what the TEST touched — a var written by code-under-test into a previously-absent slot survives teardown. |
| 2026-07-16 | ci-gate-root-suites — ROADMAP 1.1+1.2: gate the root pytest corpus (1,377 tests) and dashboard vitest suite in CI, flip the 77-day-overdue validate-context-artifacts strict, set an 80% coverage floor (CI-flag, not pyproject — a global fail_under breaks scoped inner-loop runs) | 3 files (workflow, one test stub, pnpm-workspace.yaml), single behavior, staged rollout explicitly requested ("carefully") | Lightweight | Lightweight | orchestrator direct with report-only soft-launch per job: 3 push-observe-fix CI rounds, each job flipped strict only after a proven green run | correct — the soft-launch pattern earned its keep twice: round 1 caught a $RANDOM-under-dash portability bug (stub emitted constant '00000000' on Ubuntu; fixed with PID-derived IDs, strictly stronger distinctness) and a pnpm/action-setup root-manifest miss; round 2 caught pnpm 11's silent schema change (allowBuilds per-package map replaces the v10 onlyBuiltDependencies list — diagnosed via exact CI repro in a scratch dir, verified exit-0 before pushing); also corrected the roadmap's 71.7% coverage baseline to the real 82.1% (stale coverage.xml artifact_sha). Lesson: report-only-then-flip converts every latent env difference into a visible, unblocking finding. |
| 2026-07-16 | efficacy-measurement — ROADMAP 2.2 (W7/O6): first-class skill_activation observations event (capture_memory build_observation extraction) + eval-ledger producer (append_eval_log_row, schema-truth tests), lifecycle future-designed→optional-lazy, ADRs dec-268/dec-269 | 2 behaviors, 15 files, observability-contract decision (dec-002 rails), 11 new tests | Standard | Standard | pipeline-explicit (architect → planner → 2× paired TDD groups with RED-first + orchestrator-executed mechanical checkpoints → forced light-review on the tier-H WAL step → verifier) | correct — Standard earned its weight three times: the architect registered two brief-overriding objections (availability≠activation; optional-lazy not active), the forced light-review independently verified the reclassification against the one event_type-branching consumer, and the verifier's independent pass caught 7 id-citation violations in test comments that both authoring agents and the orchestrator missed (fixed inline pre-merge, detector-verified). Pre-mortem finding shipped as code: hooks/ suite was CI-invisible (testpaths exclusion) — explicit CI step added in the same merge. Full-circle: the DL06 self-heal built two days ago auto-backfilled dec-263.re_affirmed_by during this pipeline's own finalize. |
| 2026-07-16 | td-002-block-pointer — Resolve td-002: restructure the agent-pipeline canonical block (~658 tokens, 2.6x the ~250-token budget, xfailed since filing) into a ~205-token pointer; relocate the PoC-journey paragraph into software-planning § End of Feature; remove the xfail; file td-055 for the block-refresh gap | 5 fix files + state records, single behavior (block injection contract), content decision requiring ADR per the ledger row | Lightweight | Lightweight | context-engineer standalone single-artifact audit → user dialog gated the option choice (pointer over trim-in-place/wontfix) → orchestrator direct fix | correct — the audit quantified ~70-80% redundancy against always-loaded rules before any edit, turning a content-taste question into an evidence question; budget test green for all four blocks (91 passed). Retrospective: pinned-vs-local ruff skew bit again (PT006 + two format ping-pongs) — escape is stage-the-pinned-tree via Bash, never re-Edit; and the 250-token "budget" was one block's measured size generalized by a test, not a decided ceiling — worth remembering when a uniform gate inherits a number from a single instance. |
| 2026-07-16 | block-refresh-mechanism — td-055: make onboarded CLAUDE.md canonical blocks safely refreshable; manifest-only version identity (dec-271), three-way Phase 6 predicate, /refresh-claude-blocks command, refuse-to-clobber guarantees | 17 files, 3 behaviors (manifest producer / classify+apply consumer / command+predicate UX), architectural decision on version identity, fleet-wide blast radius | Standard | Standard | pipeline-explicit in worktree: researcher ∥ context-engineer shadow → opus architect → planner → orchestrator pre-mortem (5 modes, 2 became tests, 1 verified at checkpoint) → sequential TDD groups A-D with 2 forced light-reviews (both ADVANCE) + WARN-closure micro-step → concurrent group E ×3 with fragment reconciliation → doc-engineer → orchestrator checkpoints 5/10/15 → opus verifier | correct — Standard earned it: the shadows disagreed productively (in-file marker vs sidecar) and the architect's manifest-only synthesis beat the brief's assumed marker design (zero token cost, zero test-contract perturbation, re-affirms dec-270); verifier's only FAIL was one id-citation comment, whose detector run then exposed 3 sibling sites in a file the verifier had scanned single-file — run gate detectors repo-scoped, not finding-scoped. Retrospective: observations.jsonl is perpetually mid-commit-modified by capture hooks (MM state) → pre-commit stash-restore conflicts roll back ruff fixes; escape = stage observations.jsonl into the same commit so the stash is empty. Pre-extraction estates measured 2/8 vs R2's "low likelihood" — estate was zeroed by the morning refresh, so YAGNI held empirically, not by assumption. |
| 2026-07-22 | self-healing-p0-verify-harden — P0 gate for the self-healing loop: raw-source verification of claude-code-action's event switch at the pinned SHA 51ea8ea (workflow_run + issues[labeled] + workflow_dispatch confirmed handlers; push absent → no repository_dispatch bridge needed), manual curl of cursor.com/docs/bugbot (fabrication check), daily run-budget cap added to ci-autofix.yml, backfilled ADR dec-draft-85e6d0dc | live verification + 2 surgical workflow edits + 1 ADR backfill, no new behavior, hard gate for P1/P2/P5 | Direct | Direct | orchestrator direct, no pipeline (curl pinned-SHA tarball + grep compiled event switch; raw-HTML grep of Bugbot docs) | correct — gate PASSED, P1 unblocked with no bridge. Two research reversals recorded honestly: (a) workflow_dispatch is a real supported case, not "coming soon" (brief §10 Q1 resolved); (b) the Bugbot REST API is real (POST api.cursor.com/bugbot/review literally in raw HTML), not fetch-tool fabrication as RESEARCH_CURSOR row 18 suspected — CLI-only gate still correct by the popularity-trap evidence (Bugbot autofix would make Cursor a second fixer). Lesson: verify against the pinned SHA's compiled event switch, never docs summaries that self-contradict; a "suspected fabrication" flag is a hypothesis to test with curl, not a conclusion. |
| 2026-07-24 | self-healing-p5-issue-autofix — Subsystem C: label-gated triage-first issue-autofix workflow (`on: issues.labeled`, `ecosystem-feedback` arming gate), `issue_triage` validation module reusing `render.SECTION_HEADINGS`, 3 architectural ADRs (praxion-only / label-arming-gate / triage-first deny-by-default), gate-reviewed by P2's `agent-prs` scope, structurally-main-safe fixer | ~6 files (workflow + module + 2 test suites + DESIGN.md + architecture.md), 18 REQs, 3 architectural ADRs, security-critical privileged auto-fix surface, stacked on feat/self-healing-p4 | Standard | Standard | pipeline-explicit (systems-architect → planner → orchestrator pre-mortem [6 FMs, 4 became invariant assertions] → parallel TDD groups [issue_triage ∥ workflow] RED-first → 2 forced light-reviews on tier-H steps [Step 5 ADVANCE; Step 9 REVISE] → opus verifier PASS WITH FINDINGS, 0 FAIL / 6 WARN) | correct — Standard earned it decisively: the Step 9 `review: force` caught a structural reach-`main` hole that the 40 invariant tests AND the orchestrator read both certified safe — unprotected `main` + `Bash(git checkout:*)` + `gh pr create` pushing the current branch left `main` reachable behind only a prompt instruction; fixed structurally (non-agent pre-created fix branch + dropped `git checkout`/`git branch` from the allowlist → reaching `main` impossible, locked by 2 tests). Composition-over-novelty held: P5 reused P1/P2/P4 audited parts and P2's `agent-prs` scope already pre-listed `issue-autofix/*` (loop co-designed from one brief). Live-only gotcha logged: `--allowedTools` must be a single physical line (runtime comma-split narrows a wrapped allowlist; YAML folding hides it from structural tests). Owed: live labeled-issue dogfood, P2 merge (O2) for gate coverage, `gh label create` prereq, `main` branch protection (orthogonal). P1/P2/P4 calibration rows still owed (merge-time backfill). |
| 2026-07-24 | finalize-hook-ci — issue #45: server-side GitHub Actions finalize workflow (push:main) backstopping web-UI merges that client-side git hooks cannot reach; shared finalize_chain_run_on_main entry + FINALIZE_CHAIN_STRICT strict-mode gate; ADR dec-284 | ~4 core files (new .github/workflows/finalize-adrs.yml + finalize_chain.sh ~15-line refactor + 2 test files + 3 additive docs), 12 ACs / 10 REQs, 1 architectural decision (privileged push-to-main governance workflow — one-way-door), 1 review:force step, governance surface (.github/workflows + finalize mechanism) | Standard | Standard | user-directed (mission brief mandated Standard for governance surfaces); architectural-decision + one-way-door signals drive the tier independent of the modest file count | correct — Standard justified by the privileged one-way-door + architectural ADR, not file count (which was Lightweight-sized). Full pipeline + review:force earned its cost: research reframed the branch-protection risk to a documented reversal-trigger (main unprotected → GITHUB_TOKEN valid + free anti-recursion), the DRY shared-entry-point avoided composition drift, and the light-review source-traced actions/checkout to clear the git-push concern. 1726 passed, 0 FAIL; AC-05/AC-12 dogfood-pending by nature. A Lightweight tier would have skipped the ADR + adversarial review the privileged surface required. |
| 2026-07-26 | pr-dependabot-autofix (P3a) — extend the main-only reusable-ci-autofix hub to pr_checks + dependabot fix-commit + fork suggest-only surfaces; N-jobs-one-file seam (main autofix job byte-unchanged), P5 containment, PR-branch-scoped attempt cap, Dependabot probe-main gate, fork inverted privilege; ADRs dec-draft-ef47bb76/160fc228 | 2 workflow/policy files + 1 test file + 2 docs, 14 ACs / 14 REQs, 2 architectural ADRs, 1 review:force step, privileged CI surface (writes to contributor + Dependabot PR branches — widest blast radius in the loop), full pipeline (researcher→architect→planner→test-engineer∥cicd-engineer→verifier) | Standard | Standard | user-directed (split the larger P3 into P3a autofix-scope + P3b fleet-install); architectural decisions + privileged one-way-door surface → Standard | correct — Standard fit the privileged, wide-blast-radius CI surface; the full pipeline earned its cost: research corrected two premises (P5-not-P1 containment; base-ref-edge unverifiable → fail-closed), the light-review caught F1 (a cross-PR attempt-counter accumulation bug no structural test could see), and the final verifier caught V-01 (fork surface missing the budget gate) — both fixed pre-merge. 44 invariant + 1747 full-suite green, 0 FAIL. AC-14 + runtime halves of AC-4/AC-5 dogfood-pending (#35-38; #38=TS7↔Next16 unfixable-escalation validator). Splitting P3→P3a/P3b kept blast radius contained; Lightweight would have skipped the ADRs + adversarial reviews the privileged surface required. |
| 2026-07-27 | p3b-fleet-install — roll the P2 cross-model review gate + P3a autofix surfaces into the managed fleet for BOTH new onboards (onboard §8e.8 cross-model caller install + CURSOR_API_KEY print) and already-onboarded projects (/upgrade-project + upgrade_project_pins.sh --hub-sha caller-reconcile: in-place SHA re-point + add-caller-if-absent); closes dec-278 | ~7 files, 2 behaviors (new-onboard wiring + existing-project upgrade), 2 architectural ADRs, 1 review:force step, governance/fleet surface, full pipeline (architect→planner→impl∥test→verifier; researcher skipped — recon sufficed) | Standard | Standard | user-directed (P3b split from P3; user chose BOTH scopes → leaning Full) | correct — Standard fit the governance/fleet surface; the review:force light-review caught a REPRODUCED fleet-crash (a caller with two hub `uses:` lines made the SHA-extraction return a multi-line value → `sed: unterminated substitute pattern` mid-fleet-run) + the implementer's own Register Objection caught a test-bug (naive `{{` check tripping on legitimate `${{ }}`), both fixed pre-merge; the command-layer/script determinism split (--hub-sha) kept the reconciler network-free/testable. 1774 full-suite green, 0 FAIL. new-project.md correctly untouched (regression-guarded). A Lightweight tier would have skipped the ADR + adversarial review the fleet-touching SHA-rewrite required. |
| 2026-07-28 | p6-metrics-baseline — self-healing-loop metrics collector (read-only gh-run-history → SELF_HEALING_* report triple in .ai-state/metrics_reports/, namespace kept distinct from the code-health METRICS_REPORT_* triple); non-skipped autofix *attempts* metric + fetch-limit saturation flag; credit-burn / gate-verdict / in-place-fix (Autofix-Attempt trailer) / override-rate / cost-per-fix as explicit deferred nulls; first snapshot; self-healing-metrics-baseline ADR | 4 files (collector + 16 tests + scripts/CLAUDE.md + ADR) + first snapshot triple, single read-only on-demand behavior (no workflow), 1 implementation ADR | Lightweight | Lightweight | orchestrator direct, no pipeline (user-approved tier via intake checkpoint); live-dogfood-driven metric refinement | correct — Lightweight fit the read-only on-demand collector; running it live immediately earned its keep (quantified a 12/17 ≈70% autofix-attempt failure rate) and the verify step caught two measurement weaknesses pre-commit: raw run count was skip-dominated (170/200) → count non-skipped attempts; fix_prs_* missed in-place P3a Dependabot fixes (pushed to the existing PR branch, not a ci-autofix/ branch) → scope-noted + deferred to Autofix-Attempt trailer-scan. Register Objection dropped a planned shipped /loop-metrics command (script is Praxion-internal → a fleet-shipped command would dangle). Same live run (Task 2 dogfood of #47/#48) surfaced two autofix bugs — max-turns→limbo defeats idempotency; Python-only fixer allowlist thrashes on vitest failures — escalated to a separate Standard fix. |
| 2026-07-29 | prompting-ux-touchpoints — Apply modern coding-agent prompting research (external: Anthropic/OpenAI/Willison/Osmani convergence on explicit test-criteria + discussion-first; internal: audit of Praxion's own handoff-prompt and human-facing-report gaps) to 3 concrete touchpoints: `## Handoff Prompt Structure` template (document-templates.md), `AskUserQuestion` `preview`-field upgrade at the pre-verification and verifier-vs-loopback checkpoints (coordination-details.md), scoped exception to the HTML authorship boundary for ephemeral user-requested output (html-output-conventions.md) | 3 doc/rule files, no production code, one behavioral/architectural decision (HTML authorship scope), 1 ADR draft (dec-draft-0f1596ea), no architect/planner spawned | Lightweight | Lightweight | user-explicit (2 parallel `researcher` agents — external + internal, structurally decorrelated per Balanced Coupling — then orchestrator synthesized and the user picked touchpoints via AskUserQuestion) | correct — on target: pure context-artifact edits, no architect/planner warranted. The HTML-boundary relaxation was scoped narrowly (ephemeral/no-MD-backing/tmp-by-default) rather than a blanket removal, specifically to avoid reintroducing the exact MD/HTML drift `dec-145` already flagged — that drift instance became the ADR's falsifier (superseded below: the metrics_reports/index.html file turned out to already be deleted, not a live instance — a stale-report correction, not a fabrication). Two candidate touchpoints deferred to the same-day follow-up batch below, not silently dropped. |
| 2026-07-29 | prompting-ux-followups — Follow-through on the batch above: mechanical pre-commit guard for the HTML authorship boundary (scripts/check_html_authorship.py), dashboard "Copy as prompt" Handoff Prompt feature (composer + button in the workshops view), refined ephemeral-HTML location (.ai-work/<task-slug>/ preferred over bare tmp/) + durable-learnings-promotion guidance, verified the metrics_reports/index.html drift claim against ground truth (already resolved, no action needed) | 2 disjoint implementer items (guard hook: 4 files; dashboard feature: 6 files) + 3 direct orchestrator edits (rule refinement, ADR amendment, drift verification), ~14 files total, no architect/planner spawned, 1 ADR amended in place (dec-draft-0f1596ea, flipped proposed→accepted) | Lightweight | Lightweight (2 parallel background implementers + orchestrator-direct for the small items) | parallel-batch (user: "let's address the follow ups... ultrathink and use the best praxion resources") | correct — disjoint file sets parallelized cleanly (Python/hooks vs TypeScript/dashboard), matching this repo's own debt-batch-1/debt-batch-2 precedent for small independent items over architect/planner ceremony. **Ground-truth verification caught two real gaps a prior report had missed**: (1) the "metrics_reports/index.html drift" cited in the prior batch's synthesis was already fixed (git ls-files showed no tracked file) — the earlier ADR's Context section was corrected in place rather than left asserting a stale claim; (2) the dashboard implementer's return was truncated mid-sentence with no completion marker, right as it began tracing the composer against a real `.ai-work/` directory — ground-truth check (git diff + running the actual test suite + build + finishing the trace myself) confirmed the work was substantively complete, and the manual trace against the real `label-taxonomy-manifest` task directory found one genuine bug the implementer's own test fixtures had masked: `extractTaskIntent` only matched a `## Task Intent` heading, but the real TASK_BRIEF.md convention in practice uses the shorter `## Intent` — fixed with a broadened heading match plus a regression test proving the pre-fix behavior would have failed. Full suite green: `scripts/` 1356, `tests/` 559, `dashboard_app` 472 (vitest) + clean `next build`. **User-requested pre-commit verification pass** (3 parallel independent reviewers — `verifier`, `architect-validator`, `context-engineer`, decorrelated per Balanced Coupling) found real, fixable issues none of the prior ground-truth checks caught: a dangling reference to a `dataviz` skill that doesn't exist in Praxion's own skill set (found independently by 2 of 3 reviewers — strong cross-validation), an ADR `affected_files` list mixing in files from the unrelated dashboard task while omitting the guard's own canary test, a weak ADR Falsifier that tested the guard's mechanics instead of the underlying decision, a doc/code contradiction ("mandatory" vs. actually-omittable field), an under-specified rule sentence missing the allowlist mechanism, and an unscoped default-mode `rglob` in the guard script that would flood on `node_modules/`/`.next/` if ever run by hand. All fixed; re-verified full suite green (`scripts/` 1357, `tests/` 559, `dashboard_app` 472, clean build) after fixes. |
| 2026-07-30 | multidisciplinary-identities (Steps 15-16, Wave 1 tail) — End-to-end live proof of the discipline-consultant mechanism (`statistician` against the real discipline-expansion gate, plus a `nonexistent-xyz` fail-loud probe), convener disposition of 6 challenges, the CONSULT_LEDGER's first rows, then documentation catch-up flipping the component `Designed` -> `Built` | 2 plan steps executed on an already-approved Standard-tier plan; 2 consultant spawns + 1 doc-engineer; 6 files touched (CONSULT_LEDGER, DESIGN.md, architecture.md, agents/README.md, .mmd + .svg) plus 2 ephemeral artifacts; no new architectural decisions, no new ADRs; gated on a hard Step 0 install-parity check | Standard (inherited) | Standard (inherited) | Tail of an in-flight Standard pipeline; tier not re-selected. Steps 15-16 delegated per the plan's own assignees (consultant spawns at `opus` per the `standard` difficulty-hint row; doc-engineer at `haiku` per the routing table). Handoff-driven execution from `STEP_15_16_HANDOFF.md` | correct — the Step 0 gate earned its cost: the previous session halted here for a real reason (plugin pinned 0.16.0 vs consultant landed post-snapshot), and re-running it confirmed 0.17.0 parity so the proof measured the shipped path rather than self-hosting. Three learnings. (1) **A live dogfood found what structural tests cannot**: 595 tests stayed green throughout, yet the smoke test surfaced 6 substantive statistical defects in the project's own expansion gate — at n=10 it passes a true 70% dismiss rate 35% of the time. Mechanism-works and mechanism-is-well-calibrated are different claims. (2) **The `haiku` doc-engineer route produced structurally-correct but factually-drifting prose** — invented a "two-column" registry (actually 7), a 5-column ledger (actually 11), and cited a nonexistent `skills/economics`, all inside a document carrying a Built-only contract; every claim needed orchestrator verification against disk. The routing table's L tier is defensible for mechanical doc work but not self-verifying where a doc asserts file-level facts. (3) **The plan's Step 16 `Files:` list omitted `.ai-state/DESIGN.md`** even though that is the only place the "seventh Built pipeline loop" is enumerated and where the status cell read `Designed`; flipped it and flagged the scope note rather than leaving the two architecture docs contradicting each other. Also noted: the ledger's first content is 6 defer / 0 dismiss — a degenerate distribution that the one-sided gate passes, instantiating challenge CH-05 on day one; recorded rather than corrected, since manufacturing a dismissal would corrupt the counter the gate reads. |
| 2026-07-30 | consult-expansion-criterion-revision — Act on the six defects the first live discipline-consult found in Praxion's own Wave-2 expansion gate: demote it from a binary "dismiss rate not >60% over >=10 challenges" threshold to a reported estimate with a Wilson interval, denominate it in distinct consults rather than clustered challenge rows, add a two-sided standing condition, and state the Tier-3 criterion per-discipline | 7 files (evidence dossier §14/§17.4 + new §17.11, CONSULT_LEDGER counting recipe + 6 appended rows, DESIGN.md, archived spec, 3 finalized-ADR amendment notes) + 1 new draft ADR; one architectural decision revising a pre-registered falsifier that 3 finalized ADRs cite in their reversal triggers | Standard | Standard | User directive ("address the real defects before they propagate"); 3 forks resolved by explicit user choice via AskUserQuestion — gate shape (demote to indicator), denominator (consults), ADR scope (new ADR + retarget triggers) | correct — Standard was right and the coupling is why: the numbers were mirrored across the dossier, DESIGN.md, an archived spec, and three finalized ADRs' Disconfirmation blocks, so a Direct-tier text edit would have left reversal triggers pointing at a threshold that no longer exists — a falsifier quietly ceasing to be falsifiable. Three learnings. (1) **The consultant mechanism's first act was to break the document that authorized it**, and the repair was cheap only because instrumentation shipped before the roster: with zero ledger rows at consult time, no collected evidence was contaminated. Deciding the measurement before generating the data is what made all six defects free to fix — a property worth protecting deliberately, not by luck. (2) **Demoting a gate to an indicator is better statistics and weaker governance**, and the honest framing is that this trade was made, not that it does not exist; the chosen option's own `dissent:` field records that a pre-registered bar's value was never its calibration but that it bound the future decider, who is typically the party wanting expansion. Reversal trigger recorded on both signals. (3) **A permanent ledger row pointed its `rationale-ref` at an ephemeral `.ai-work/` file** that disappears at pipeline cleanup; append-only semantics meant the fix was six replacement rows with a durable `.ai-state/` target, not an edit. Worth a schema note: `rationale-ref` should prefer durable targets. Also noted: applied to the current ledger the revised criterion reads *not yet informative* (n=1 consult, 2 of 3 disposition values) where the old gate read "0% dismiss rate, passes" — the repair is load-bearing on day one. |
| 2026-07-30 | finalize-rewrite-scope — Two dangling-draft-id bugs in `scripts/finalize_adrs.py`, surfaced by finalizing a real consult ADR: `.ai-state/CONSULT_LEDGER.md` was absent from the named persistent-file list despite its documented `rationale-ref` schema, and spec matching compared a kebab-case task slug to an underscored filename | 6 files (script, tests, scripts/CLAUDE.md, adr-conventions rule + regenerated rules manifest, authoring-protocols reference); no behavior change beyond two scope additions | Direct | Direct | Found mid-task while verifying the previous commit's post-commit hook output; fixed in place rather than filed | correct — Direct was right (two localized scope fixes, no design question), but the *finding* only happened because the finalize output was verified against ground truth rather than trusted: the hook logged "rewrote 4 file(s)" and exited 0 while silently skipping two files that carried the id. **A bounded-allowlist rewriter fails silently by construction** — a file outside the allowlist is indistinguishable from a file with no matches, so the only detection is grepping for the draft id afterward. Both regression tests were confirmed to fail against the previous implementation before being kept. Second-order lesson: the ledger's schema *documented* that it holds `dec-draft-<hash>` pre-finalize, so the allowlist and the schema disagreed in writing — a consistency check between "files documented to hold draft ids" and "files finalize rewrites" would have caught this without a live run. |
| 2026-07-30 | multidisciplinary-identities closeout — Retrospective verifier on a Full-tier pipeline that shipped 16 steps without ever running its quality gate, then sentinel, budget-basis resolution, REQ-18 clause 2 implementation, learning harvest, `.ai-work/` cleanup, and the v0.18.0 release | 5 agents (verifier/opus, sentinel/sonnet, skill-genesis/sonnet + the 2 earlier consults); ~15 files; 6 tech-debt rows opened and 5 closed; 1 new ADR already finalized; 1 release cut | Standard | Standard | User directive ("what else is missing?" then "do your recommendations first"); closeout ordering chosen by consequence — reconcile WIP, verify, audit, close findings, harvest, clean, release | correct — but the finding is that **the gate itself had been skipped**, which no tier selection prevents. Four learnings. (1) **A Full-tier pipeline reached `main` with no `VERIFICATION_REPORT.md` and an entirely empty `traceability.yml`** (18/19 arrays), and nothing surfaced it: the implementer fragment was never merged and no test-engineer fragment ever existed. Measured coverage turned out to be 3 REQs test-covered, 5 partial, 6 live-demonstrated-once, 4 code-only, 1 prose-only, 1 unimplemented — defensible for LLM-interpreted contract behaviour, but far weaker than the empty file communicated, which was nothing. Consumer-with-no-producer, discharged silently at archival. (2) **Four independent bugs shared one mechanism** — claimed scope > computed scope, green while not looking (`finalize_adrs.py`, the id-citation checker, release-staleness, sentinel `T02`). Judged independently by skill-genesis to be one generalizable defect class, now proposed as a gate-liveness Scope Fidelity clause. (3) **The always-loaded budget had three coexisting bases spanning ~22.9k-33k against a 25k ceiling**, so every historical PASS was basis-dependent; settled by observing a live session's loaded context — `rules/README.md` is a catalog, is not injected, and is the single largest candidate file, worth 4.5k tokens of the disagreement. Evidence the subagents structurally could not obtain. (4) **`git push --follow-tags` is not atomic across refs**: the first release dispatch lost a race to Praxion's own finalize backstop, publishing tag `v0.18.0` while its branch push was rejected — an orphaned tag on a commit reachable from no branch, with no release object. Repaired by deleting the tag and re-dispatching. A release workflow that pushes both refs needs orphaned-tag cleanup on failure. |
| 2026-07-31 | multidisciplinary-identities-wave2 — Wave 2: five workstreams (td-071 cost series, td-072 fleet-wide frontmatter strip, gate integrity + skill-genesis dispositions, the §12.2 roster decision, td-064 spike redesign), 4 parallel research/architecture agents, 5 implementers, 1 adversarial consult | 9+ files, 5+ behaviours, cross-cutting; three always-loaded/shipped surfaces touched; a charter-level architectural decision; concurrent writers in one worktree | Full | Full | orchestrator (tier held; no escalation or de-escalation) | Ordering by *evidence decay* rather than severity was the highest-leverage call: td-071 first meant the roster consult became the first cost-instrumented one. Ten findings, seven sharing one shape — a correct constraint with no enforcing consumer (release-staleness advisory unread; consult counter unconsumed; dedup_key unvalidated; `git stash` unencoded; the ledgers' own append instruction self-defeating; consult identity keyed differently in two places; no Type-II instrument at all). Two cost real work: a bare `git stash` by a parallel implementer destroyed uncommitted orchestrator state — the hazard lived only in session guidance and personal memory, in no Praxion rule (now td-078, fixed). Three of my own published claims were wrong and self-corrected: a wrongly-scoped `tests/` grep, swapped hash arguments, and a pooled consult count. The consult changed the verdict's grounds rather than its conclusion, which is the outcome an honest adversarial gate should be able to produce. |
| 2026-07-31 | td-081-sealed-prior — the sealed round-0 prior list: make dec-306's lens-versus-consultant falsifier computable by sealing a lens-run prior list before the spawn and classifying every challenge against it at round 2 | 9 files, one new .ai-state artifact, four paired contract sites, a new gate section, an architectural decision about what "sealed" can mean | Standard | Standard | orchestrator (tier held) | The estimand was mis-specified twice before any code ran, and both catches came from reading the *comparison group* rather than the metric: first that dec-306's arm is the convener-with-the-skill and not the convener as-is, then — from the user — that the delta measures framing AND standing jointly, since dec-306 dropped framing despite its own §4.1 naming it. Delegation split cleanly by whether the value came from independence: architect and consultant produced things I could not have (the trust-topology framing of the seal; two fail-open defects in a gate committed an hour earlier). Implementers were faithful, which is what you want and also means spec gaps pass straight through — the G6 substring bug was born in the gap between a plan saying "contains a row" and an implementer writing `in blob`. Four subagent returns truncated mid-sentence; every time the work was done and only the report was missing. The strongest single finding: a gate can be correct, canaried, and structurally unreachable — dec-308's cost gate had run in CI exactly once, on the PR that introduced it. Three defects escaped canaries for one shared reason: a canary supplies both sides of a comparison from one source, and reality supplies them from two. |
| 2026-07-31 | verify-multidisciplinary — independent verification of the multidisciplinary-identities initiative (Wave 1 dec-298..306, Wave 2 dec-307..309, and the unmerged td-081 sealed-prior register), executed from a written verification plan | 6 lens-independent verifier spawns + architect-validator + sentinel, ~17 fitness checks and ~12 ADRs under review, 3 bodies of work, a shipped-vs-repo extensibility question, and an adversarial pass on the verification's own report | Full | Full | orchestrator (plan-driven; no re-tiering needed — the plan pre-specified the fan-out and it held) | 8 FAIL / 19 WARN; 10 fixed on-branch. Three lessons worth carrying. (1) The mandated positive control was load-bearing: the neutering recipe I supplied silently no-oped under PEP-420 namespace packages, and only the control caught it — without it the run would have reported all 17 gates uncanaried. (2) Lens independence paid twice, correcting a 2-vs-15 count and a cache-listing-as-release-evidence inference; but deterministic re-execution is NOT independent corroboration, and I presented it as such until the consult said otherwise. (3) Convening a consultant against my OWN report was the highest-yield hour: 9 challenges, 9 switch-now, two of them landing on the sentences I was most confident in. Zero dismissals now stands at 47 challenges / 7 consults, which is itself the open question. |
| 2026-07-31 | gate-integrity-and-overlay — close the gate-integrity and extensibility debt the multidisciplinary-identities verification filed: three unguarded contracts (td-101 draft-id citations, td-103 ADR frontmatter promotion, td-104 CONSULT append-only), a canary-coverage unit that counted files instead of checks (td-083 + td-094), td-073's named-consumer half, and a ship-or-defer ruling on the project-local registry overlay (td-064/086/088) | 8 tech-debt rows in 3 groups, one architectural; ~20 files across scripts/ + fitness/ + tests/ + hooks/ + commands/ + docs/ + rules/; 5 ADR drafts; 14 planned steps in 5 file-disjoint concurrent tracks; 2 steps flagged RISKY with forced light-review; a self-referential acceptance bar (every new gate proven to bite per check, with positive and no-op controls) | Full | Full | pipeline-explicit (user specified Full tier, the task slug, the artifact set, and the parallelism plan; explicitly forbade convening a discipline consultant on the grounds that no registry fires-when predicate matched — no load-bearing quantitative claim, no imported external claim) | On target. Full warranted: 2 researchers → architect → planner → 8 implementer/test-engineer spawns → 2 light-reviews → neutering sweep. **Group C deferred as a unit on FALSIFIED PREMISES, not on cost** — the single most consequential outcome. td-086 asserts the plugin cache is 'the ONLY path by which a managed project receives the fitness suite'; verified false — `/onboard-project` Phase 8b.2 COPIES `claude/aac-templates/fitness-conftest.py.tmpl` into the project's own tree, where `__file__` resolution is correct, and the scaffold never ships the registry gate at all (`grep discipline_registry commands/onboard-project.md` → 0). So the brief's coupling rationale ('the overlay without the rooting fix means a managed project's entries are silently ignored by a gate resolving to the plugin's own root') is void — there is no such gate in a managed project. The architect registered that objection, honored the never-split instruction anyway, and deferred all three together; `dec-305` re-affirmed rather than superseded, reversal trigger named. The live finding underneath was filed instead (td-106: conftest.py and its .tmpl are byte-identical with no sync check, while the analogous canonical-blocks surface has one). **The task proved self-similar at every level it touched** — 'a check that cannot fail' recurred four times independently: td-083's file-vs-check granularity; a bundled test hiding two defect classes (extracting it behind one name would have reproduced td-083 one level down); negative-control canaries that pass vacuously before their pattern exists (dual-asserted against positive siblings to fix); and the sweep harness itself, where the PEP-420 double-import trap makes a broken harness report zero reds indistinguishably from 'every gate already works'. **The plan's own §7 protocol contained a work-destroying instruction** — `git checkout -- <path>` as the neuter-restore primitive, safe on a clean tree and silently destructive on this one; caught by a test-engineer's Register Objection, overridden fleet-wide to bytes-in-memory + SHA-256 assert. Second instance in one task of a git command that fails by succeeding at the wrong thing (bare `git stash` was the first, pre-known). **Orchestrator-as-backstop twice**: independently verified all 12 citation rewrites on two axes (semantic title match + git-history co-location of the originating fragment) because pre-mortem item 4 named it the one residual with no automated catch; and re-derived Step 3's completion from ground truth when its return truncated mid-sentence with no terminal marker, avoiding both a false advance and a wasteful re-run. Result: 6 rows resolved, 5 filed (td-105..109), 4 deferred with documented rationale. Acceptance bar met — 26 neuters, 23/23 non-control checks reddened ≥1, ZERO reddened zero, 27/27 per-check canary coverage, zero disagreements with any step's self-reported count (all 17 reproduced at wider scope), all 6 files byte-identical by SHA-256. Suite 2027→2077 passed, +50 reconciled test-by-test. The sweep's ~26 full-suite runs also exposed a pre-existing test-isolation defect a single run hides (td-109: two tests regenerate the LIVE repo's DECISIONS_INDEX.md rather than a tmp_path copy, appending a bogus row to a committed file whenever an ADR-shaped scratch file exists). No discipline consultant convened, per instruction and per dec-306's criterion. |
| 2026-08-05 | arch-ground-truth — make Praxion's architectural state a living system: decisions that evolve (decay classification, retirement vocabulary, batch disposition), are reconciled against the model (§3a↔LikeC4 and §4↔shipped-block projections), and are delivered by relevance (relevance-ranked ADR injection); plus a falsifiable `architectural` test to bound production at the source | 68 files, 32 commits (**corrected 2026-08-05** from "70 files, 31 commits" — neither figure reproduces under any base; measured `git diff --shortstat bb61d68..e6fa0e6`), 3 ADRs (dec-317/318/319), 3 new detectors + 1 command + 4 test modules, 2 always-loaded rule changes, 2 new sentinel dimensions (DH, AC13 — **renumbered 2026-08-05** from AC11, which collided with the pre-existing model↔markdown check dec-112 named) + 3 new checks (DH04/DH05, GL04), 1 pre-commit gate, 1 fitness contract; cross-cutting (rules + agents + skills + commands + scripts + fitness + CI + model + docs); multiple architectural decisions | Full | Full (sequenced orchestrator-driven batches, no agent fanout except one sentinel audit) | pipeline-explicit (research + architecture pre-existed as RESEARCH_FINDINGS.md + SYSTEMS_PLAN.md; the user drove phase selection turn by turn) | **On target on tier, wrong about the plan four times — and the plan being wrong was the main yield.** Full-scale signals throughout; executed as sequenced batches rather than agent fanout because each phase was already specified by the systems plan and the decisive work was measurement, which does not parallelise. **Four planned items were refuted by measuring before building.** B6's model-binding test: only 2 of 227 architectural ADRs touch the model, and sampling showed the 192 it would demote are genuine composition decisions the model does not represent — a granularity mismatch, replaced with an inventory/boundary test. B2's archive directory: `finalize_adrs.py` derives the next NNN by iterating the flat directory, so moving terminal records would reissue a live id — shipped as status + `retired_by` instead, and 3 of B2's 4 planned parts were dropped as harmful, jobless, or negative-value. B4's premise that `removed-by-later` adds supersession edges: zero of 28 warranted one, and supersession is *unrepresentable* for the many-to-one case since `supersedes` is typed string. B8's 'grow the fitness config': the config had not grown for lack of stated invariants, not lack of will — exactly one existed. **The recurring failure class hit seven times, twice in my own code one commit apart** (an index lookup blind to directory paths, then its mirror in the ownership predicate). It also opened the effort: the DH dimension had never executed, because every cached copy of the installed plugin predated it. Named as dec-319 with a mechanical half (GL04), which found a live instance on its first run. **Errors I made and corrected:** predicted the attribution fix would take removed-by-later 14→11, it went to 28 (I counted corrections and missed that fixing upward containment also *recovers* unattributed references); mis-stated a corrected count as 47 when it was 49; claimed an orphaned gate was 'also broken' after running it once under the wrong interpreter — it runs clean under the project one, corrected in place in dec-319 rather than edited away; and filed dec-124 as a novel 'contradicted' decay class when its real cause was subject-deletion, already covered — withdrawn in B4_STUDY with the reasoning kept. **Behavioral contract:** every phase opened by verifying the substrate against the repo rather than the plan (which is what produced all four refutations); objections registered with evidence before deviating, each landing as an ADR the user could overrule; scope held — three planned parts dropped rather than built, and no ADR minted for the gray-matter reversal because dec-318's own test forbids it two commits after shipping. **Outcome:** decision-health findings 127→85 and `removed-by-later` 13→28→0 (fully dispositioned); model and §3a reconciled at 16↔16 with td-111 closed; the published contract named as architecture and reconciled; suite 1994→2018 root + 135 fitness, all green; always-loaded budget 23,958→24,609 of 25,000. **Not dogfooded:** `/decisions` cannot be invoked until a fresh session, so the command body is unverified — flagged rather than implied. |
| 2026-08-05 | verify-arch-ground-truth-fixes — independent verification of the arch-ground-truth effort, then repair of every finding: the withheld-residual contract and its fleet-unreachable oracle, a duplicate sentinel check id, the interpreter half of the gate-liveness clause (detector + the live instance it found), registry scoping under `--repo-root`, a mis-stated calibration row, and an unlinked shipped detector | ~14 files across scripts + tests + one agent + one shipped command + catalog + state; 7 distinct behaviours; 2 ADR drafts; a new sentinel check (GL05) and a renumbered one (AC11→AC13); cross-cutting | Full | Standard (sequenced batches, no agent fan-out) | orchestrator (de-escalated from Full with reason: the verification pass immediately preceding had established ground truth on every affected file, so a researcher or architect would have re-derived context already held — the fan-out would have cost context quality, not added it) | **On target after de-escalation; the two most valuable findings both came from refusing to trust a passing test.** (1) **dec-319's falsifier fired on dec-319's own closing commit.** The ADR predicted that if the next instances of its class were *wrong interpreters*, its mechanical half had addressed the least important one. The next instance was a wrong interpreter, introduced by the commit that closed it, and invisible to the check it shipped — GL04 asks whether anything *calls* the gate, and after the rewiring something did. Response was the one the ADR itself prescribed, recorded as a re-affirmation rather than a supersession because the clause was right and its own test proved it. (2) **A canary asserted the negation of its own docstring.** `test_canary_unparseable_inventory_withholds_the_lazy_class` was named for retirement safety and asserted only that the class *changed* — which passes the instant the finding becomes `vanished`/`retire-candidate`, the defect it names. Third instance in this repo's recent history of 'a check that cannot fail'; the fix asserts the disposition, plus a report-wide invariant, because the leak was in a shared residual and a per-path assertion would miss the next one. **Two design calls that measurement decided.** The new check went *inside* `check_gate_liveness.py` rather than into a new script — same rule, same clause, same `--json` the sentinel already routes, so zero new dispatch and zero new installer surface. And its transitivity was load-bearing rather than thorough: the live instance imported only stdlib plus one sibling, and the dependency lived in the sibling, so a direct-imports scan reports it clean — the same scope-fidelity trap the rule names one clause earlier. Built against the live known-bad state per B5's precedent, it flagged exactly one script before any fix, then went green. **Errors made and corrected:** stated the installer had left an `.orphaned_at` marker after a half-applied interactive run — the markers predate it by days (zero created in the two-hour window); corrected immediately rather than left standing. **Behavioral contract:** worktree isolation objected to with cause and skipped (`EnterWorktree` branches from `origin/main`, and local `main` carried 33 unpushed commits — the entire effort under repair); one blocking question asked, on the only fork whose answer changed the work; scope held — three catalog entries added for scripts materially changed, none for the unrelated pre-existing gaps beside them. **Outcome:** 4 FAIL + 3 WARN closed; suite 2018→2032 root (+14: 3 decision-health, 7 gate-liveness, 2 spec-drift, 2 projection) and 135 fitness, all green; all 13 sentinel-prescribed scripts load under bare `python3` (was 12); decision-health 85 findings / `withheld: []` unchanged; model↔doc 16↔16; gate-liveness 0; budget 24,609 of 25,000. |
| 2026-08-05 | sentinel-remediation-i1 — the staleness *content* correction (td-114): six skills whose `last-verified` markers certified factually wrong content, corrected against primary sources rather than refreshed | 6 skill directories, 20 files, ~35 marked sections plus every satellite reachable from them; 34 markers; two always-loaded-adjacent shipped surfaces; new reference file; frontmatter schema change in one skill | Standard/Full | Full (sequenced, no agent fan-out — session forbade subagents; research done inline against primaries) | brief-directed (a handoff brief scoped the item; the user directed satellite depth and the MCP v1/v2 disclosure structure mid-run) | **The finding was mis-classed as staleness. A material fraction was fabrication.** Three researchers had verified ~35 marked sections and found 22 factually wrong; the full-depth pass found the category was wider. `communicating-agents` alone carried **five identifiers that exist nowhere upstream** — `PostgreSQLTaskStore`, `PushNotifier`, `A2APlugin`, `fast_a2a`, `ClientFactory.create()` — plus a `maxNodes` parameter invented for a likec4 tool that has `includeIndirect`. And two `## Framework Integrations` rows (LangGraph "A2A endpoint via LangSmith", AutoGen "A2A connector") describe integrations with **no first-party code or documentation anywhere**: `langchain-ai/langgraph` has no `a2a` module and its platform docs expose agents over MCP; `microsoft/autogen` returns three "a2a" hits — a README, a PNG avatar, a graphrag test fixture. Staleness is "was true, drifted"; this was "never verified." Both were kept as *sourced negatives* rather than deleted, since a reader asking "does LangGraph do A2A?" is better served by an evidenced no. **The marker-honesty argument decided scope.** Markers sit only on SKILL.md sections, but the satellites they point at carried the same dead APIs — so refreshing the markers while leaving `A2AStarletteApplication` in `contexts/a2a-python.md` would have silenced the sentinel with the harm intact: precisely the brief's forbidden pattern. Surfaced as a decision; the user chose full depth, which turned a marked-section fix into a 1,262-line rewrite for one skill. **My own measuring instrument failed twice, in opposite directions.** WebFetch summarization over the 251 KB hooks doc returned false negatives for `stop_hook_active` — a field the doc plainly contains and the skill itself asserts — so "not found" proved nothing; `curl` + `grep` on the raw file was the only reliable read, and confirmed the 8-consecutive-blocks cap verbatim at line 2320. In the other direction, three fetches of one Robinhood article produced three different tool totals (45/50/44, one silently dropping a whole category). Forbidding arithmetic in the prompt made the *enumeration* reproducible — two independent transcriptions agreed exactly at 5+12+9+8+10+6 = 50 — and the 45 turned out to be the page's own prose counts disagreeing with its own lists. Encoding either number alone would have been asserting something unreproducible. **The staleness machinery had a blind spot the work exposed.** `staleness_sensitive_sections` is a flat list of bare heading texts resolved SKILL.md → references → contexts, first match wins; both MCP context files had a heading literally named `SDK Landscape`, so python.md always won and **the TypeScript marker was invisible to F07/F08 — it could never age out**. Both happened to be stale so nothing was missed, but the check could not have caught it. Renamed to `(Python)`/`(TypeScript)`; all 8 cataloged titles now resolve 1:1. **Self-correction:** wrote `GetTaskRequest(name=task_id)` from pattern-matching typical proto resource APIs, checked the proto, found the field is `id`, fixed it — the same plausible-default failure mode that produced the original finding, caught only because every identifier was checked rather than trusted. **Highest-consequence single correction:** `agentic-transactions` claimed `place_equity_order` was "the **only** write-side placement tool" — false since options shipped, so `place_option_order` was a second capital-moving tool outside every documented HITL gate. Restated the permission policy as a rule (`every place_* and cancel_* goes in ask`) rather than an enumeration, and introduced a capital-moving vs state-mutating-non-capital distinction the old read/write model had no room for — watchlist and scanner mutators are neither reads nor capital moves, so "allow everything that isn't a place_*" silently authorizes them. **Verification:** 2042 root + 134 fitness green (baseline-matching), gate-liveness 0, projection 16↔16, isolation 464/0, budget 23,389/25,000 unchanged, lint-imports 2 kept 0 broken; all 6 skill validators pass; 34 markers moved, the template placeholder correctly left alone. |
| 2026-08-05 | sentinel-remediation-i4 — the "frozen metrics substrate" finding: verify it, and fix what is actually broken | 8 files across scripts/project_metrics + scripts/ + agents/; a schema field addition, a new gate script, a new sentinel check, 11 new tests; one architectural ADR | Standard | Standard (sequenced, no agent fan-out) | brief-directed (a handoff brief scoped the item and prescribed the remedy; the prescription was refuted by measurement before any code was written) | **The finding was false and the harm was real — they had different causes, and conflating them would have produced a fix to correct code.** The brief asserted `hotspots.top_n` was frozen because "a sliding 90-day window advanced 13 days cannot leave every entry unchanged". That premise is wrong: `churn_90d` moves only when commits *enter or leave* the window, so a file written in a burst then left alone is legitimately constant. Recomputed every entry from git at each report's own timestamp — **10 of 10 reproduce exactly**, and the flagged `metrics-dashboard.tsx` is genuinely 1823 on three dates and 2191 today because a commit moved it. Had I accepted the diagnosis, I would have "fixed" a correct sliding window and left the actual gap untouched. **The real defect sat between producer and consumer.** A report recorded how the run was *configured* (`window_days`, `top_n`, `wall_clock_seconds`) but never *what it was a report of* — no timestamp, no commit — so the consuming staleness policy fell back on report **age**, and age is the wrong unit: the near-miss report was **6 days old** (fresh under the 14-day rule, so silent) sitting behind **~180 commits**, one of which had decomposed the very file TD01 was about to file against. Fix is provenance (`generated_at`/`commit`/`dirty`) plus per-path liveness — *has this ranked hotspot been touched since the report was taken?* — which is threshold-free by construction. **I deliberately rejected a commit threshold as the gate** and kept `commits_since` as context only: any N would have reproduced the original defect in a new unit, and the relationship is not monotonic (one commit to the right file invalidates what a thousand elsewhere do not). An inverse guard pins that distance alone is not staleness. **Three measurement errors of my own, all caught by checking rather than reasoning:** probed `churn` when the field is `churn_90d` and read nine `None`s as a signal; asserted the freeze was total when the full `top_n` hashes actually differ (only the top three are constant, and entries 4-5 moved 25270→26847 in 39 minutes — the partial freeze was itself the clue); and estimated "~30 commits" where `git rev-list` says 177. **The patch-vs-minor schema bump was load-bearing, not pedantry**: trends compare major.minor, so 1.3.0 would have marked all nine prior reports `schema_mismatch` and severed trend continuity for a purely additive change — pinned now by a test asserting the *property* (major.minor stays 1.2) rather than the number. **Gate proven to bite, not merely to pass**: two independent neuters (detection disabled, withholding disabled) drove 2 and 1 canary red respectively, restored bytes-in-memory with a SHA-256 equality assert — never `git checkout --`, which is silently destructive on a dirty tree. Also confirmed the canary-coverage meta-test *discovers* the new gate (22 script gates, not in the skip list) rather than passing because it never saw it — the fourth instance in this session of checking whether a check can actually fail. **Withholding over guessing**: nine existing reports have unrecoverable commit distance, so the checker returns a distinct `withheld` with a named reason rather than a silent `fresh`; collapsing unanswerable into clean is exactly how the original gap survived. **Behavioral contract:** objection registered with evidence and the prescribed remedy declined rather than performed; scope held to the producer/consumer seam; the ADR's steelmanned runner-up concedes that on a slower repository the one-number age fix would be the better engineering. Suite 2042→2053 root, 134+1 fitness, gate-liveness 0, isolation 464/0, budget unchanged. **Grade effect:** I4 is not a defect, so the report's 4 Important reduce to 2 (I3, I2) with I1 already closed. |
| 2026-08-06 | sentinel-remediation-2 — drive the report to zero Important findings: retire AC11 (I3), adjudicate P07's consult clause (I2), the sentinel.md substance/log/marker batch (S9, S10, td-118), the Robinhood staleness instrument (td-117), plus a metrics refresh that brought the TD06 gate live for the first time | ~18 files across agents + rules + skills + scripts + docs + the LikeC4 model + test fixtures + .ai-state; 6 behaviors (retire a check, repair a producer tier, widen a check, replace an inventory with a rule, register an artifact, refresh the metrics substrate); 2 ADRs; 2 forks requiring a user decision; cross-cutting | Full | Full (sequenced items, no agent fan-out) | brief-directed with per-item user gating (a handoff brief scoped five items and named two forks as decision-required; the user chose on both, and chose scope on two further findings surfaced mid-run) | correct — Full was right on signals and right in practice, and the two highest-value outcomes both came from refusing to take the brief at its word. (1) I2's central premise was false. The brief asserted that no instruction tells the convener to write a disposition back into the consult fragment, making sentinel P07 a consumer with no producer. Three documents require exactly that, and the two clauses landed ~14 hours before the consults that violated them. The brief had checked the two always-loaded surfaces and stopped; the clause lives in the on-demand reference those two point to — so the brief reproduced the precise failure it was documenting, and so had the convener. The fixable thing was never the check: both summary-tier surfaces state one of Round 2's two obligations, and the stated half is gated by a fitness test while the omitted half was gated only by an audit nobody had run. The gated half was met and the ungated half was not, which is the ordinary outcome and not a lapse of care. Asking why a competent agent skipped an obligation stated three times produced a better fix than acting on what the finding literally said. (2) The brief's ADR category for I3 was wrong. It called both options architectural; the canonical falsifier — name the component added, removed, or whose responsibility moved — returns nothing for retiring a check inside one agent, and the superseded decision is itself behavioral. The rejected option would have been architectural, which is what made the error plausible. A third refutation held from the prior session: I4's frozen-substrate claim stayed refuted, and the fresh metrics run corroborated it independently (top-hotspot score fell 37% in six days — the moving-window mechanism that makes a constant per-file churn legitimate). Three generalizable lessons. A gate needs proof in BOTH directions: P07's fixture directory had two bad cases and one control, and a P07 that flagged every fragment carrying a challenges section would have satisfied every file in it — the missing control was the one distinguishing 'detects undispositioned challenges' from 'detects challenges'. A symmetric diff is blind to anything absent from both sides, which is why AC11 could never have found the real model drift (three agents in neither the model nor the doc) that grounding its retirement uncovered. And a staleness threshold is the right instrument only for content that decays predictably: halving Robinhood's 60-day threshold was the comfortable move and would have changed nothing, so the inventory that provably rots was deleted and the gate rewritten against the place_*_order pattern, converting a time-dependent correctness problem into a time-independent one. Errors made and corrected: read the wrong JSON nesting level and briefly treated a healthy readiness run as a hard-fail, corrected by reading the structure rather than guessing; named a fixture discipline absent from the registry, which would have modelled a BLOCKED consult instead of an undispositioned one, caught by checking the roster before writing; let the 40-char slug cap truncate an ADR filename mid-word and renamed it before it accreted references. Behavioral contract: objections registered with evidence twice before complying, both landing as ADRs the user could overrule; both decision-required forks surfaced with a recommendation rather than picked silently; scope held — five findings filed and deliberately not fixed (td-119 prune-family collision, td-120 coverage rendered 100x low into the surface TD03 reads, td-116 deferred with its reasoning recorded, the doc_consult model gap, and consumer glob support left unfiled because a speculative feature is backlog rather than debt). Outcome: both remaining Important findings closed — one by fixing, one by refuting and fixing the real cause; td-117, td-118 and td-121 resolved; 2 ADRs finalized. Suite 2053 root and 134+1 fitness green throughout; gate-liveness 0; model↔doc 16↔16; shipped-artifact isolation 464/0; always-loaded budget 23,389 → 23,448 of 25,000; TD06 returned a real verdict for the first time since shipping. Not done: the closing sentinel re-run, deferred as a separate expense rather than implied. |
| 2026-08-06 | sentinel-fanout-audit — re-run the ecosystem audit as a six-agent fan-out after a solo run died at its turn cap, then remediate the Critical, all four Important, and seven Suggested findings | 6 agents (5 collectors + synthesis), 221 collection turns, 516 artifacts, ~126 checks over 22 dimension groups; remediation touched 15 files across agents, rules, commands, skills, scripts, hooks and CI; 8 commits | Full | Full (agent fan-out for collection, orchestrator-driven remediation) | user-directed (audit requested, then "follow the recommended fixes in the order you prefer") | **The fan-out was worth more for independence than for survival.** It was designed to beat a `maxTurns: 100` cap — every lens finished near half its budget, so the cap was never the binding constraint once scope was right. The real yield was lens independence: two lenses disagreed on DL04/DL06 (one read the written contract, the other executed it) and the reconciliation — that the check passes by executor judgment, not by specification — was reachable from neither alone. A collector also caught a `supersedes` typing gap introduced *that same day by the orchestrator commissioning the audit*, which a solo run scoped by that orchestrator would likely have inherited. **Grade moved A→D and the audit got better**, because the sole Critical was in the instrument itself and a solo run structurally cannot report the defect that terminates it. Two things to carry forward: (1) `prune_reports.py` destroyed the same two committed files for the *second* time mid-session, so a known-but-unfixed data-loss bug outranks any Suggested finding — fix bleeding before polish; (2) three findings this session were the same meta-defect wearing different clothes — a gate whose input is wrong (td-123), a gate nothing invokes (td-125), a gate whose verdict is discarded by an exit code (I3) — and `check_gate_liveness.py` caught none of them, because GL04 scopes to `check_*`/`validate_*` scripts and GL05 excludes `hooks/`. The rule is ahead of its enforcement. |
| 2026-08-06 | fleet-quality-remediation — ten defects drawn from the tech-debt ledger and the 2026-08-06 sentinel report, filtered on a single question: what does a person running Praxion on their own repository actually experience? Seven of the ten shared one root cause — self-hosting masks them | 10 findings, ~50 files across commands, agents, skills, scripts, CI-adjacent config and four test suites; 12 commits; 1 ADR fragment; 8 debt rows closed and 6 filed; 7 subagents across 6 lanes | Full | Full (scoped execution — no researcher/architect/planner stages) | user-explicit: the brief set the tier AND directed straight-to-execution on the grounds that the findings were themselves the specification | correct — Full was right on file count and blast radius, and skipping the front of the pipeline was right too: a research-then-architect pass would have re-derived a specification that already existed in reproducible form. The lane split by file-set disjointness held with zero collisions across seven concurrent agents. **Verification-first was the highest-value instruction and it paid off at a finer grain than expected.** All ten findings reproduced, so none was refuted outright — but two claims *inside* findings were: widening the forbidden-literal pattern to bare REQ-/AC- was measured at four findings, all false positives, so the pattern was right and the prose was wrong; and the golden-bad-case defect had two independent causes, not the one stated. Both would have been 'fixed' wrongly on a reasonable reading. Reproducing a finding is not the same as accepting its diagnosis. **The near-miss worth remembering is td-108.** Its symptom was 65 violations; its subject was the scope-fidelity defect underneath, where the commit-time seam scans staged paths only. Clearing the backlog and marking it resolved would have been precisely the failure the brief names — making the measurement stop complaining while the thing it measures is unchanged. Read the row's own notes before closing it on the evidence the task collected; the two can be about different things. **Two process defects, both caught by the same habit.** An aborted commit left files staged from a `git add` that had already succeeded, and the next commit swept them into an unrelated change — caught only by diffing what the commit CONTAINED rather than trusting its exit code, then split with reset --soft. Separately, a literal pipe inside a ledger notes cell silently split the row into 16 fields; the schema documents ` // ` as the separator for exactly this reason and nothing enforces it at write time, which is the same shape as the 41 non-conforming dedup_keys already tracked. A write-time validator on the ledger pair would close both. **Prefer deleting a drift surface to updating it earned its place as a standing instruction:** four stale prose counts deleted rather than corrected, a web-access summary deleted as fully derivable from its own table, an enumerated `.ai-state/` file list replaced by a pointer, and agent name-lists removed from four summaries — those restatements were the mechanism by which five agents went missing from the security baseline and three went stale unnoticed. |
| 2026-08-06 | fleet-quality follow-up — eight items chosen from the remaining ledger after the ten-defect pass: the finalize back-link crashes, the unwired-gates bundle, the coverage measurement chain, and the consumer-layout harness the earlier brief had named as the structural alternative to fixing instances one at a time | 8 items across 6 agent lanes in 2 waves; ~15 files plus one new 764-line harness and one new canary; 10 commits; 1 ADR fragment; 7 ledger rows closed and 1 filed; active ledger 37 to 29 | Full | Full (scoped execution, wave-sequenced by file-set contention) | orchestrator-proposed bundles, user-selected all four | correct, and the wave split was the load-bearing call rather than the lane split. Five lanes ran concurrently on disjoint files; a sixth ran alone afterward owning the CI workflow, because four earlier lanes all wanted to append a step to it and because **the new harness must not ship unwired** — building a consumer-layout gate and committing it without a CI job would have created, in the same session, the exact defect the session closed six instances of. **Three of the four rows closed wider than they were filed, and the pattern is worth expecting.** A ledger row records the visible half. The back-link row named two crashes; the third shape found while guarding them was worse and silent, writing YAML into an ADR's prose and returning zero — the two crashes were the *lucky* failure modes, because a traceback is a signal. The CI row named a missing Write grant; nothing consumed the output in any form, so a clean run reporting FAIL produced a green check. The install row named a sweep that pruned nothing; the real defect was one manifest reader with two callers whose fail-safe defaults ran in opposite directions, so one created contamination and the other declined to remove it. **Compounding fail-safes are a distinct class**: each default individually defensible, composing into a ratchet that only ever adds. **One row was disproved and that is a result, not a miss.** The calibration-advisory row inferred an absent-reader defect from the job's own comment saying it 'mirrors the release-staleness advisory pattern'. It has two named readers and always did. A comment describing itself by analogy to a sibling generated a defect report about a defect it did not have — worth remembering when a finding's only evidence is a self-description. **Coupling asserted from structure was later confirmed by measurement**, which is the better order but only if you go back and check: rendering per-module coverage alone would have handed the sentinel two guaranteed false positives, and the hooks-coverage fix removes exactly those two. Landing them apart would have manufactured debt rows against well-tested code. **The floor concession is tracked, not absorbed.** Bringing hooks into the measurement moved the aggregate below 80 and the floor was re-derived to 77. Nothing regressed — the prior slice is unchanged at 85.52% over the same 9739 statements, recomputed independently rather than taken from the agent's report — but it does loosen the bar for already-covered code, so it carries a re-raise trigger and the eight newly-visible under-tested hooks as one row. A threshold lowered without a row is how a measurement stops complaining. |
