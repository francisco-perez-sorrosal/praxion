# EVAL_LOG — context-layer quality baselines

One row per recorded baseline or post-slice measurement of the context layer (roadmap `docs/independent-analysis/process-economy-roadmap.md` §6 P0.7). Mechanical runs are free and run after every slice; the LLM-judged run repeats only after M-confidence slices. Report files live in `.ai-state/praxion_eval_reports/`.

| date | commit | purpose | mechanical (PASS/WARN/FAIL) | judged (PASS/WARN/FAIL) | always-loaded tokens | listing tokens | family5 verdict | notes |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 2026-09-07 | d483b2ec (main, pre-Phase-0) → worktree-process-economy-phase0 @ 84b5a100 | Phase 0 baseline | 758/389/1 (`PRAXION_EVAL_REPORT_2026-09-07T20-46-38Z.md`, target main HEAD) | 1503/39/2 (`PRAXION_EVAL_REPORT_2026-09-07T21-32-00Z.md`, target worktree, messages-api, one Haiku call per ADR, ~50 min sequential) | 24,542 / 25,000 (tokenizer, 9 files) | 11,336 (tokenizer; frozen ceiling) | WARN — inconclusive on the estimate basis inside the harness subprocess; the byte trend is flat | The 2 judged FAILs are the in-flight manifest scan: `SYSTEMS_PLAN.md` absent by decision (architect skipped) and `VERIFICATION_REPORT.md` absent because the verifier had not run; 36 of 39 WARNs are pre-existing `affected_reqs_resolvability`; 1 `adr_option_depth` WARN; 3 `test_harness_family1_corpus_integration.py` failures pre-exist on main. Cost column in the harness log was hardcoded at the time of this run (fixed in Step 20). |
| 2026-09-08 | rework/inheritance-guard-soundness @ 2afc51b1 | First live differential run of `eval/scripts/inheritance_probe.py` (scenario 6, `inheritance-probe`; td-183 / rw-d2e9a3a3) | N/A — not an `/eval-praxion` pass | verdict: **PASS** (standalone script, not the judge pipeline) — see notes | N/A | N/A | N/A | Two headless `claude -p --model sonnet --permission-mode bypassPermissions` sessions, one top-level and one instructed to spawn `praxion:researcher` via the Agent tool. Both reported the same 7 always-loaded `claude_md_paths`; the top-level run reported `## Agent Model Routing` present, the subagent run reported it absent — the differential the guard exists to prove. Cost: $0.210 (top-level, 3.5s API time) + $0.393 (subagent, 12.3s API time) = **$0.60**, both `claude-sonnet-5`. Raw envelopes recorded at `eval/tests/fixtures/inheritance_probe/live-2026-09-08/{top-level,subagent}.json`. |
| 2026-09-24 | 3242a76b (main) | Item B0 — continuity row at HEAD before the live-capture work (B1) | 798/522/0 (`PRAXION_EVAL_REPORT_2026-09-24T15-05-34Z.md`, target main HEAD) | not run — B0 is mechanical-only; the judged baseline follows B1 | 16,606 | 9,243 | PASS (the baseline's 1 FAIL was this check) | Seeded scenarios are fixture-invariant: each grades its frozen `recorded_*` against `expected_*` in the same YAML and reads no rule, agent or skill file, so their 5 PASS say nothing about HEAD (B1 replaces the producer with live `claude -p` capture). All 51 family-2 WARNs come from gitignored `.ai-work/_harvest/` verification reports (machine-local; the baseline read 0 `.ai-work` sources) — a git-ref target still reads working-tree `.ai-work/`, so this row is not a pure function of 3242a76b; p2-residual and p2-wrap carry byte-identical reports and are counted twice. `affected_reqs_resolvability` 389 → 471 tracks ADR-corpus growth. Phase 1 gate reports 2–6b exist in `praxion_eval_reports/` but were never logged here. |
| 2026-09-24 | 837adbee1cb9 | context-layer-live | 27 sessions | no | — | — | — | live context-layer run, k=3; spawn-selection=93%; ui-step-conformance=0%; adr-authoring=100%; commit-staging=100%; lightweight-fix=100%; lowered=True (head=93%, canary=67%). Baseline JSON `context_layer_baseline.json` (this dir); `praxion-evals-live 0.37.0`, scenarios on `claude-opus-5-5` at effort medium, isolation preflight `proven` on both variants (sonnet by design), 0 errors, $11.69 actual. Canary drops per case, none rises: spawn-selection#2 2/3 → 0/3 (Lightweight read as Direct), #4 3/3 → 1/3 (Full read as Standard). **ui-step-conformance 0/3 is an instrument defect, not a behaviour floor:** one repeat correctly returned `[BLOCKED]` because the fixture seeds `WIP.md` + `IMPLEMENTATION_PLAN.md` but no `LEARNINGS.md`; the other two fail a literal-citation check (`web-ui-design`, `skeleton`, `five UI states`) applied to the implementer's terse return summary, and the judge only runs after that check passes — so the scenario cannot move under any cut. Fixed before the judged run; compare that row's ui-step figure as an instrument change, not a delta. |
