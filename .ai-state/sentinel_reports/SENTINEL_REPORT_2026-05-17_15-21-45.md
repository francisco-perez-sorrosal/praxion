# Sentinel Report

**Run:** 2026-05-17 15:21:45
**Prior baseline:** 2026-05-17 13:05:50 (Health B / Coherence A, 0/1/5 findings)
**Branch at scan:** `feat/new-project-hackathon-flag`
**Scope:** Full exhaustive sweep — all dimensions, all artifact classes; confirming pass after findings-remediation wave

---

## Ecosystem Health: A

### Summary

This confirming pass was run after a targeted remediation wave addressing five of the six prior findings (I1 + S1/S2/S4/S5; S3 deliberately carried forward). All five addressed findings are verified resolved. The ecosystem advances from Health B to Health A.

**What improved:** I1 (`dispatch-reworks` missing from `commands/README.md`) is resolved — the catalog row now exists and FS/README counts match (38 commands each). S1 (dead `drafts/` pointer in DESIGN.md) is resolved — the constraint row now cites the finalized ADR cluster `dec-056`–`dec-061` directly. S2 (SH01 over-flagging `python-resources.md` path) is resolved — both `agents/sentinel.md` and `skills/spec-driven-development/references/sentinel-spec-checks.md` now scope SH01 to the traceability matrix `Implementation` column and Requirements section only, excluding change-narration prose. S4 (F07 cold-start WARNs for 40 sections) is fully resolved — `rules/swe/staleness-policy.md` now defines the Section Location semantics (search SKILL.md then references/*.md then contexts/*.md), `web-ui-design` frontmatter and inline markers were repaired, and all 50 staleness-sensitive sections across all 15 skills now have valid `<!-- last-verified: -->` markers (F07 WARN count: 40 → 0). S5 (README_DEV.md missing `architect-validator.md` and `interface-designer.md`) is resolved — both agents are now listed in the README_DEV.md agents tree.

**What carried forward:** S3 (`sentinel.md` 567L and `verifier.md` 577L exceeding the 500-line FAIL threshold) was deliberately not addressed in this wave. It remains the only open finding from the prior run. Token budget headroom continued to decline: 4.1% (was 6.3% in the 13:05 run) due to hackathon-mode additions to the coordination protocol rule (+1,850 chars / +529 tokens for the `*Hackathon mode:` footnote). Still within the 25,000-token ceiling but the downward trend is worth monitoring.

**New finding (SH01):** `SPEC_diagrams_2026-04-30.md` has two broken paths in its `Implementing file(s)` column: `.ai-state/ARCHITECTURE.md` (renamed to `DESIGN.md` after the spec was archived) and `docs/diagrams/architecture.c4` (the file is now at `docs/diagrams/architecture/src/architecture.c4`). This is a genuine SH01 finding under the rescoped rule — the Implementation column is in scope. Severity: Suggested (archived spec, no functional impact, but violates the traceability contract). Also: `dec-190` frontmatter `summary:` field contains unquoted colon characters that cause `yaml.safe_load` to fail; all required fields are present via manual extraction, but the YAML authoring quality is substandard (DL02 WARN).

**Comparison to last run:** Health B → **A** (all Important findings resolved; S3 carried forward, one new Suggested). Coherence A stable. Finding counts: 0/1/5 → 0/0/3.

---

## Ecosystem Coherence: A

### System-level EC checks

| Check | Result | Evidence |
|-------|--------|----------|
| EC01 | PASS | All 15 agents in coordination protocol table match `agents/*.md` 1:1 |
| EC04 | PASS | Pipeline has complete handoff coverage: promethean → researcher → systems-architect → implementation-planner → implementer/test-engineer → verifier; all outputs have producing and consuming agents |
| EC05 | PASS | No structural gaps for stated purpose |
| EC06 | PASS | Condensed deliverables block in `~/.claude/CLAUDE.md` lines 48–51 aligns with `skills/software-planning/references/coordination-details.md § Delegation Checklists` — all four agents, all conditional clauses |
| EC07 | PASS | `python3 scripts/check_aac_golden_rule.py --mode=audit --json` returns `[]` — zero important-severity findings |

### Cross-dimension anomalies

- Token budget headroom: 4.1% (837 tokens remaining before the 25,000-token ceiling). This is the lowest headroom recorded in the project. The per-run decline (10.3% → 6.3% → 4.1%) tracks the hackathon-mode feature additions. No ceiling breach yet, but the next unconditional addition will be critical.
- `dec-190` has unquoted colons in the YAML `summary:` field, causing `yaml.safe_load` to fail. All other ADR frontmatter is well-formed. This is an isolated authoring quality issue.

### Artifact coherence distribution

Scorecard coherence column: 49 A, 1 B (agents with T03 FAIL), 1 B (SPEC_diagrams stale paths). All skill, rule, command, and ADR coherence grades are A.

### Synthesis

The ecosystem functions as a coherent whole. The pipeline is fully connected, all 50 staleness markers are present and valid, the commands catalog is exactly aligned with the filesystem, behavioral contract is consistently applied, and the LikeC4 fence validator passes. The token budget is within bounds but the declining trend is the primary structural risk.

---

## Prior-Findings Resolution

| Finding | Status | Evidence |
|---------|--------|----------|
| **I1 (X02/EC02)** — `dispatch-reworks` missing from `commands/README.md` | **RESOLVED** | `grep "dispatch-reworks" commands/README.md` returns 1 match: `| /dispatch-reworks | Fan out...`. FS: 38 commands, README: 38 entries — exact match. |
| **S1 (F03)** — DESIGN.md dead `drafts/` pointer | **RESOLVED** | `.ai-state/DESIGN.md:244` constraint row now reads "see the concurrency & collaboration ADR cluster (`dec-056`–`dec-061`)" — no `drafts/` pointer remains in that row. The phrase `.ai-state/decisions/drafts/` still appears in §8 (process description paragraph, not a stale pointer) and in Section 3 component descriptions (architectural process notes) — both are appropriate informational references, not stale navigational pointers. |
| **S2 (SH01)** — over-application to change-narration prose | **RESOLVED** | `agents/sentinel.md` SH01 pass condition now reads "Live file references (matrix + Requirements) resolve; change-narration prose excluded". `skills/spec-driven-development/references/sentinel-spec-checks.md` §SH01 now defines: "SH01 checks the traceability matrix `Implementation` column and the Requirements section. Do NOT flag file paths that appear only in narrative prose." The SPEC_multi-language-support spec's `python-resources.md` path appears only in change-narration prose (L13, L56, L83 — all narrative sections, not the Implementation column). SH01 no longer flags it. |
| **S3 (T03)** — `sentinel.md` (567L) and `verifier.md` (577L) oversize | **CARRIED FORWARD (deliberate)** | Not addressed this wave. Both remain at 567L and 577L respectively, exceeding the 500-line FAIL threshold. `implementation-planner.md` (461L), `systems-architect.md` (401L), `skill-genesis.md` (336L), `context-engineer.md` (358L), `promethean.md` (322L), `researcher.md` (301L) remain at WARN. No regression — no new agents crossed thresholds. |
| **S4 (F07)** — cold-start F07 cluster (40 WARNs) | **RESOLVED** | `rules/swe/staleness-policy.md` now has `### Section Location` semantics (search SKILL.md first, then references/*.md, then contexts/*.md). `web-ui-design/SKILL.md` has well-formed frontmatter with `WCAG 2.2 AA — The Numbers` and `RAIL Model` sections and valid `<!-- last-verified: 2026-05-12 -->` markers on both. `agentic-interface-design`, `api-design-craft`, `tui-design` resolve via reference/context file markers. Full scan: **50 sections WITH markers, 0 sections WITHOUT markers**. F07 WARN count: 40 → **0**. |
| **S5 (X06)** — README_DEV.md agents tree missing 2 agents | **RESOLVED** | `README_DEV.md` lines 77–87 now lists all 15 agents including `interface-designer.md` (L78) and `architect-validator.md` (L84). |

**Delta:** Health B → **A**. Coherence A (stable). Findings: 0/1/5 → **0/0/3** (S3 carried forward, 2 new Suggested).

---

## Ecosystem Metrics

| Metric | Value | Trend |
|--------|-------|-------|
| Skills | 49 | Stable |
| Agents | 15 | Stable |
| Rules (unconditional) | 7 | Stable |
| Rules (path-scoped) | 13 | Stable |
| Commands | 38 (FS) | Stable |
| ADRs (finalized) | 190 | Stable |
| ADRs (drafts) | 0 | Stable |
| Always-loaded tokens (÷3.5) | 23,963 | Declining (was 23,434 at 13:05; headroom 4.1%, was 6.3%) |
| F07 WARN count | 0 | Resolved (was 40 in prior run) |
| F08/F09 issues | 0 | Stable |
| DL03 alignment | 190/190 | PASS |
| Calibration log rows | 4 | Stable (CA02 requires 5+; skip) |

---

## Scorecard

| Artifact | Type | Complete (C) | Consistent (N) | Fresh (F) | Spec (S) | Cross-Ref (X) | Tokens (T) | Coherence (EC) | Overall |
|----------|------|--------------|-----------------|-----------|----------|----------------|------------|----------------|---------|
| skills/* (49) | Skills | A | A | A (F07=0, F08/F09 PASS) | A | A | A | A | **A** |
| agents/* (15) | Agents | A | A | A | B (sentinel 567L, verifier 577L > 500L FAIL) | A | A | A | **B** |
| rules/* (20) | Rules | A | A | A | A | A | A | A | **A** |
| commands/* (38) | Commands | A | A | A | A | A | A | A | **A** |
| CLAUDE.md | Config | A | A | A | A | A | A | A | **A** |
| plugin.json | Config | A | A | A | A | A | A | A | **A** |
| ADRs (190) | Decisions | A | A | A | B (dec-190 YAML authoring) | A | A | A | **B** |
| Specs (5) | Specs | B | A | B | A | B | A | B | **B** |
| DESIGN.md | Architecture | A | A | A | A | A | A | A | **A** |
| docs/architecture.md | Architecture | A | A | A | A | A | A | A | **A** |
| TECH_DEBT_LEDGER.md | Debt | A | A | A | A | A | A | A | **A** |
| README_DEV.md | Doc | A | A | A | A | A | A | A | **A** |

---

## Findings

### Critical (blocks correct behavior)

None.

### Important (degrades quality or efficiency)

None.

### Suggested (improves but not urgent)

| # | Check | Dimension | Location | Finding | Recommended Action | Owner |
|---|-------|-----------|----------|---------|-------------------|-------|
| S1 | T03/S05 | Token / Spec | `agents/sentinel.md` (567L), `agents/verifier.md` (577L) | Both exceed the 500-line FAIL threshold for agent prompts. Carry-forward from prior run — deliberately not addressed in the remediation wave. `implementation-planner.md` (461L), `systems-architect.md` (401L), `skill-genesis.md` (336L), `context-engineer.md` (358L), `promethean.md` (322L), `researcher.md` (301L) remain at WARN. | Evaluate extracting check catalogs or supporting tables to reference files. | `context-engineer` |
| S2 | SH01 | Spec Health | `.ai-state/specs/SPEC_diagrams_2026-04-30.md` | Traceability matrix `Implementing file(s)` column contains two broken paths: `.ai-state/ARCHITECTURE.md` (now `.ai-state/DESIGN.md`) and `docs/diagrams/architecture.c4` (now at `docs/diagrams/architecture/src/architecture.c4`). These are genuine SH01 findings under the rescoped rule — the Implementation column is in scope. The spec is archived (post-feature) so the paths are historical, but they do misrepresent the current filesystem state. New finding this run. | Update the two broken paths in REQ-DIAGRAM-05's Implementing file(s) column to their current locations. | `context-engineer` |
| S3 | DL02 | Decision Log | `.ai-state/decisions/190-hackathon-mode-plan-decomposition.md` | `dec-190`'s `summary:` field contains unquoted colon characters that cause `yaml.safe_load` to fail with `mapping values are not allowed`. All required fields are present (confirmed via regex extraction), so the ADR is functionally complete, but it fails strict YAML validation. The `(1):`, `(2):`, `(3):` enumerations in the summary text are the offending characters. | Wrap the `summary:` value in double quotes: `summary: "Three implementation-planner decisions..."`. | `user` |

---

## Freshness Summary (F)

| Check | Result | Evidence |
|-------|--------|----------|
| F01 | PASS | All spec implementation-column paths checked; SPEC_diagrams stale paths flagged in SH01 (not F01). |
| F02 | PASS | All skill reference/context file links resolve (anchor-only links are in-file, files exist). |
| F03 | PASS (LLM) | No replaced APIs or stale patterns detected. DESIGN.md S1 pointer is resolved. |
| F04 | PASS (LLM) | Agent prompts reference current pipeline stages and correct agent names. |
| F05 | PASS | `.ai-state/SYSTEM_DEPLOYMENT.md` referenced paths exist. |
| F06 | PASS (LLM) | Deployment doc service list consistent with project config. |
| F07 | PASS | **0** sections missing markers (was 40 in prior run). Section Location semantics in staleness-policy.md enable cross-file marker resolution. |
| F08 | PASS | All 50 markers dated 2026-04-30 or 2026-05-12 — both within the 120-day threshold (no section is stale). |
| F09 | PASS | All markers use valid `YYYY-MM-DD` format; no future-dated markers. |
| F10 | WARN | `scripts/git-pre-commit-hook.sh` and `.git/hooks/pre-commit` differ. Installed copy lacks Block B canonical-block sync enforcement (lines 161–192: `exit 0` inserted early, plus the Block A/B logic body entirely absent from the installed copy). `git-finalize-hook.sh` → `post-merge`/`post-commit`/`post-checkout` are IDENTICAL. Run `bash install_claude.sh` to reinstall. Carry-forward from prior run. |

---

## Completeness and Consistency Summary (C/N)

| Check | Result | Evidence |
|-------|--------|----------|
| C01 | PASS | 49 skill dirs, all have `SKILL.md` |
| C02 | PASS | All 49 SKILL.md files have `description:` in frontmatter |
| C03 | PASS | All 15 agent .md files have `name:`, `description:`, `tools:` (CLAUDE.md excluded — not an agent) |
| C04 | PASS | Sampled commands have description fields |
| C05 | PASS | plugin.json: 15 agents = FS: 15 agents |
| C06 | PASS (LLM) | Skill descriptions are specific and activation-enabling |
| C07 | PASS (LLM) | Agent descriptions differentiate cleanly with no overlap confusion |
| C08 | PASS | No unfilled `[CUSTOMIZE]` sections found |
| C09 | PASS | No `compose.yaml`/`Dockerfile` deployment configs present |
| N01 | PASS | All 49 skill dirs use lowercase kebab-case |
| N02 | PASS | All 15 agent files use lowercase kebab-case `.md` (CLAUDE.md is a context file, not an agent) |
| N03 | PASS | Agent frontmatter keys are valid per agent-crafting spec |
| N04 | PASS (LLM) | Terminology consistent: "context artifact", "task slug", "always-loaded" |
| N05 | PASS (LLM) | No contradictions between rules and agent prompts |
| N06 | PASS (LLM) | Description style consistent across agents, skills, and commands |

---

## Token / Spec Summary (T/S)

| Check | Result | Evidence |
|-------|--------|----------|
| T01 | PASS | No SKILL.md exceeds 500 lines. No WARNs (>400L). |
| T02 | WARN | 83,869 chars ÷ 3.5 = 23,963 tokens. Headroom: **4.1%** (837 tokens). Declined from 6.3% in the prior same-day run due to hackathon-mode `*Hackathon mode:` footnote added to `rules/swe/swe-agent-coordination-protocol.md`. Ceiling: 25,000 tokens. Still within bounds but trend is critical — the next unconditional addition risks exceeding the budget. |
| T03 | FAIL | `sentinel.md` (567L), `verifier.md` (577L) exceed 500-line FAIL threshold. WARN: `implementation-planner.md` (461L), `systems-architect.md` (401L), `skill-genesis.md` (336L), `context-engineer.md` (358L), `promethean.md` (322L), `researcher.md` (301L). Carry-forward. |
| T04 | WARN | Six reference files between 500–800L: `cicd/references/patterns-and-examples.md` (732L), `python-prj-mgmt/references/pixi.md` (616L), `python-prj-mgmt/references/uv.md` (565L), `mcp-crafting/contexts/python.md` (543L), `mcp-crafting/contexts/typescript.md` (511L), `cicd/references/github-actions.md` (506L). None exceed the 800-line FAIL threshold. |
| T05 | PASS (LLM) | Progressive disclosure used appropriately; large agents have load-bearing catalogs |
| T06 | PASS (LLM) | No significant redundancy across artifacts |
| S01 | PASS | All SKILL.md files have `description:` |
| S02 | PASS | All 15 agent files have required frontmatter fields |
| S03 | PASS | All rule files start with `## ` heading after YAML frontmatter block |
| S04 | PASS | Commands using argument substitution have `$ARGUMENTS` |
| S05 | PASS (LLM) | Skills follow progressive disclosure pattern |
| S06 | PASS (LLM) | Agent prompts use numbered phases with clear completion criteria |
| S07 | PASS (LLM) | Rules contain domain knowledge and constraints, not procedures |

---

## Cross-Reference Integrity (X)

| Check | Result | Evidence |
|-------|--------|----------|
| X01 | PASS | All 15 plugin.json agent paths resolve |
| X02 | PASS | skills/ has 49 skill dirs; commands/ has 38 command files (excl. CLAUDE.md) |
| X03 | PASS | All 15 CLAUDE.md Repository layout rows exist on disk |
| X04 | PASS (LLM) | No idea ledger implemented ideas reference non-existent artifacts |
| X05 | PASS | Coordination protocol: 15 agent rows = 15 FS agent files |
| X06 | PASS | `agents/README.md` references 15 agent files; FS has 15 agent files |
| X07 | PASS (LLM) | README catalog descriptions consistent with frontmatter descriptions |
| X08 | PASS (LLM) | Agent collaboration sections reference correct counterparts |
| X09 | PASS | No dec-NNN references in deployment doc point to non-existent ADRs |

---

## Spec Health (SH)

| Check | Result | Evidence |
|-------|--------|----------|
| SH01 | WARN | `SPEC_diagrams_2026-04-30.md` has 2 broken paths in `Implementing file(s)` column: `.ai-state/ARCHITECTURE.md` (renamed to `DESIGN.md`) and `docs/diagrams/architecture.c4` (now under `src/`). All other 4 specs PASS. `SPEC_multi-language-support` now PASS — rescoped SH01 correctly excludes the `python-resources.md` reference from change-narration prose. |
| SH02 | PASS | All 5 specs have `## Traceability` or REQ-based matrix. |
| SH03 | PASS (LLM) | Spec behavioral claims consistent with current skill/script content. |
| SH04 | PASS (LLM) | No UNTESTED requirement IDs in traceability matrices. |
| SH05 | PASS (LLM) | Key Decisions sections are substantive (what/why/alternatives). |
| SH06 | N/A | No spec has a predecessor archived spec. |

---

## Decision Log (DL)

| Check | Result | Evidence |
|-------|--------|----------|
| DL01 | PASS | 190 finalized ADRs; 0 draft fragments. |
| DL02 | WARN | `dec-190` `summary:` field contains unquoted colons causing `yaml.safe_load` failure. All 8 required fields are present via manual extraction. Latest 4 other ADRs (dec-186/187/188/189) parse cleanly. |
| DL03 | PASS | `DECISIONS_INDEX.md`: 190 `dec-` rows = 190 finalized files. |
| DL04 | PASS | `dec-190` `re_affirms: dec-189` resolves to existing finalized file. |
| DL05 | PASS | Hackathon mode feature has dec-189 + dec-190. ADR frequency healthy. |

---

## Architecture Completeness (AC)

| Check | Result | Evidence |
|-------|--------|----------|
| AC01 | PASS | `.ai-state/DESIGN.md` exists; project has 15+ interacting components |
| AC02 | PASS (LLM) | Component names in DESIGN.md §3 internally consistent |
| AC03 | PASS | <50% of file paths in DESIGN.md are unresolvable |
| AC04 | PASS | All `dec-NNN` references in DESIGN.md and docs/architecture.md resolve |
| AC05 | PASS | `docs/architecture.md` exists |
| AC06 | PASS (LLM) | Component names in docs/architecture.md §3 match actual module directories |
| AC07 | PASS | File paths in docs/architecture.md component table resolve |
| AC08 | PASS (LLM) | `docs/architecture.md` has no Status column, no Planned items |
| AC09 | PASS (LLM) | Every component in docs/architecture.md appears in DESIGN.md |
| AC10 | PASS | `python3 scripts/aac_fence_validator.py docs/architecture.md` → `Verdict: PASS` |
| AC11 | PASS (LLM) | LikeC4 model substrate present; markdown substrate present; component names consistent |
| AC12 | SKIP | No `architectural_elements:` frontmatter in specs and no `metadata.req_ids` in LikeC4 elements — bidirectional convention not yet populated |

---

## Behavioral Contract (BC)

| Check | Result | Evidence |
|-------|--------|----------|
| BC01 | PASS | `rules/swe/agent-behavioral-contract.md` exists; no `paths:` frontmatter |
| BC02 | PASS | Both `~/.claude/CLAUDE.md` and project `CLAUDE.md` list all four behaviors in canonical order: Surface Assumptions → Register Objection → Stay Surgical → Simplicity First |
| BC03 | PASS | All 11 code-emitting agents reference `rules/swe/agent-behavioral-contract.md` |
| BC04 | PASS | `skills/code-review/references/report-template.md` contains `### Behavioral Contract Findings` with all 6 canonical tags |

---

## Hackathon Mode (HK)

HK01: `PRAXION_HACKATHON_MODE=0` in `.claude/settings.json`. All HK checks skipped — hackathon mode is inactive for the Praxion meta-project itself.

---

## Test Topology (TT)

`.ai-state/TEST_TOPOLOGY.md` not present; TT checks skipped (opt-in protocol). No WARN or FAIL on absence.

---

## Calibration Accuracy (CA)

| Check | Result | Evidence |
|-------|--------|----------|
| CA01 | PASS | `.ai-state/calibration_log.md` exists with header and data rows |
| CA02 | SKIP | 4 data rows (requires 5+); pending next pipeline completion |

---

## Self-Verification (V)

| Check | Result | Evidence |
|-------|--------|----------|
| V01 | PASS | `./agents/sentinel.md` in plugin.json agents array |
| V02 | PASS | `sentinel` row present in coordination protocol (6 mentions) |
| V03 | PASS | `sentinel` row present in `agents/README.md` (8 mentions) |
| V04 | PASS | `## Check Catalog` section present in `agents/sentinel.md` |

---

## Pipeline Discipline

Skipped: Task Chronograph MCP tools unavailable. No active pipelines detected on `feat/new-project-hackathon-flag` branch.

---

## Tech-Debt Findings

No new TD rows filed this run. Metrics report `METRICS_REPORT_2026-05-15_22-58-55.md` is 2 days old (within the 14-day staleness threshold). TD01–TD04 findings are unchanged from the prior same-day run at 13:05 (no new hotspot crossings, no new cyclic deps, no new coverage gaps). TD05 discipline: `td-033` (filed 2026-05-12) and `td-035` (filed 2026-05-14) both have `owner-role: unassigned` and are now 5 and 3 days old respectively — still within the 7-day TD05(c) threshold at this run. `td-033` will trigger a TD05(c) WARN at the next sentinel run if not assigned or resolved.

**Recommendation carry-forward:** Migrate `td-033` and `td-035` to `TECH_DEBT_RESOLVED.md` as `wontfix` — both are permanent upstream constraints (Claude Code path-scoped rules read-only inject; subagent Write sandbox permission propagation) with documented workarounds and explicitly-declined upstream fixes. See prior run's tech-debt findings section for the full rationale.

---

## Code Health (CH)

| Check | Result | Evidence |
|-------|--------|----------|
| CH01 | PASS (LLM) | No significant structural duplication across sampled implementation files. Rank-1 hotspot `metrics-dashboard.tsx` and rank-3 `scripts/project_metrics/report.py` are purpose-built with no cross-module duplication pattern. |

---

## Recommended Actions (prioritized)

1. **[F10] Reinstall git hooks** (`user`) — Run `bash install_claude.sh` to sync the installed `pre-commit` hook. The installed copy is missing Block B (canonical-block sync enforcement). This means the `sync_canonical_blocks.py --check` gate is not running on commits to this branch. Risk: shipped-block drift can land undetected.

2. **[T02 critical] Watch always-loaded token budget** (`user`) — 4.1% headroom (837 tokens). The per-run trend (10.3% → 6.3% → 4.1%) shows consistent burn from feature additions. The next unconditional rule addition or CLAUDE.md expansion requires `wc -c` measurement before committing. Consider whether any of the 7 unconditional rules could gain `paths:` scoping without operational cost.

3. **[S1 → S2] Fix SPEC_diagrams stale paths** (`context-engineer`) — Update `SPEC_diagrams_2026-04-30.md` Implementing file(s) column: `.ai-state/ARCHITECTURE.md` → `.ai-state/DESIGN.md` and `docs/diagrams/architecture.c4` → `docs/diagrams/architecture/src/architecture.c4`.

4. **[S3] Fix dec-190 YAML summary field** (`user`) — Add double-quotes around the `summary:` value in `190-hackathon-mode-plan-decomposition.md` to escape the colon characters. One-line fix.

5. **[S3 / T03 carry-forward] Reduce sentinel.md and verifier.md below 500 lines** (`context-engineer`) — Both remain at 567L and 577L. Extracting check catalog supporting tables to reference files is the recommended path.

6. **[TD housekeeping] Retire td-033 and td-035 as `wontfix`** (`user`) — Both are permanent upstream constraints with documented workarounds. Migration to `TECH_DEBT_RESOLVED.md` reduces active-ledger noise for implementation consumers.
