# Sentinel Report

**Branch:** `worktree-sandbook-memory-migration`
**Audit context:** Post-memory-removal ecosystem integrity check (dec-225). Focused on memory-subsystem removal coherence, ADR supersession wiring, canonical-block sync, and token-budget recovery. Full ten-dimension sweep included.

## Ecosystem Health: B

### Summary

The memory-subsystem removal is **structurally coherent**: core deletion targets (memory-mcp/, skills/memory/, hooks/inject_memory.py, hooks/memory_gate.py, hooks/validate_memory.py, hooks/remind_memory.py, hooks/remind_memory.py, rules/swe/memory-protocol.md, commands/cajalogic.md, commands/save-changes.md) are all gone. dec-225 is finalized with all required ADR fields and the three superseded ADRs (dec-009, dec-025, dec-039) all carry `status: superseded` + `superseded_by: dec-225`. Canonical-block sync passes cleanly (8/8 blocks in sync). Token budget recovered from 3.4% (main) to **8.4% headroom** — a meaningful improvement.

Three Important findings remain: (1) Five live ecosystem files contain stale memory-system prose that was missed in the removal sweep — `docs/getting-started.md`, `docs/existing-project-onboarding.md`, `hooks/commit_gate.sh`, `rules/swe/vcs/pr-conventions.md`, and `skills/software-planning/references/agent-pipeline-details.md` reference the removed `memory.json` or the enforcement gate as if they are still active; (2) `scripts/regenerate_rules_manifest.py` has a stale `swe/memory-protocol` entry in `HOOK_DELIVER_ORDER` that will cause a misleading validation warning if the file is ever regenerated with a strict coverage check; (3) `dec-225` lists `supersedes: dec-009` in its frontmatter but informally obsoletes dec-025 and dec-039 in the body — DL06 expects the forward `supersedes` list to match all ADRs that carry `superseded_by: dec-225`.

Four Suggested findings from this branch: the abandoned sentinel stub at `.ai-state/sentinel_reports/SENTINEL_REPORT_2026-06-10_11-13-54.md` should be deleted; `commands/scores.md` remains absent from `commands/README.md`; the SVG pipeline diagram is still missing `agentic-transactions-architect`; and `tests/test_install_filter.sh` creates a `memory-protocol.md` fixture for a removed rule — this test either needs updating or the fixture needs a comment explaining it is testing historical install-filter behavior.

Prior Important findings from 2026-06-02 (I1–I4 against the agentic-transactions capability): I1 (`approve(` → `authorize(`), I2 (`hybrid` approval_mode), I3 (`alwaysLoad` scope), and I4 (BC03 count) are all **RESOLVED** on this branch.

Previous run: 2026-06-02 (Health A, 0/4/3). This run: Health **B**, 0/3/4.

---

## Ecosystem Coherence: A

**System-level EC checks:**

| Check | Result | Evidence |
|-------|--------|----------|
| EC01 | WARN | SVG pipeline diagram (carry-forward from prior audit): `agentic-transactions-architect` not present in `docs/` diagram. Tracked as S1. |
| EC04 | PASS | All pipeline stages have producing and consuming agents; no dead ends detected. |
| EC05 | PASS | No structural gaps for the stated project purpose given current artifact set. |
| EC07 | PASS | `scripts/check_aac_golden_rule.py --mode=audit --json` returned `[]` — 0 AaC drift findings. |

**Cross-dimension anomalies:** None. The memory removal did not create pipeline gaps — the coordination protocol, DESIGN.md, and agent collaboration sections all reflect the post-removal state.

**Artifact coherence distribution:** 16 agents A, 50 skills A, 8 unconditional rules A, 38 commands A → all individually coherent. Memory-removal did not create local cross-reference breaks within individual artifacts.

**Synthesis:** The ecosystem works as a connected whole. The three Important findings are all localized to residual stale prose in human-facing docs and scripts — none break any agent pipeline path or cross-agent handoff.

---

## Ecosystem Metrics

| Metric | Value | Trend |
|--------|-------|-------|
| Skills | 50 | -1 (memory skill deleted) |
| Agents | 16 | stable |
| Unconditional rules | 7 (excl. rules/README.md) | -1 (memory-protocol deleted) |
| Path-scoped rules | 17 | stable |
| Commands | 38 | -2 (cajalogic, save-changes deleted) |
| Finalized ADRs | 225 | +13 vs Jun 2 |
| Draft ADRs | 0 | stable |
| Specs | 5 | stable |
| Always-loaded chars (T02 convention: excl. rules/README.md) | 80,150 | improving (from 84,562 on main) |
| Token headroom | 8.4% (7,350 chars) | improving (from 3.4%) |
| Staleness markers | All 6 tracked sections PASS | stable |
| EC07 AaC findings | 0 | stable |
| GL02 gate-liveness findings | 0 | stable |

---

## Scorecard

| Artifact | Type | Complete (C) | Consistent (N) | Fresh (F) | Spec (S) | Cross-Ref (X) | Tokens (T) | Coherence (EC) | Overall |
|----------|------|--------------|-----------------|-----------|----------|----------------|------------|----------------|---------|
| skills/* (50) | skills | A | A | A | A | A | A | A | **A** |
| agents/* (16) | agents | A | A | A | A | A | A | A | **A** |
| rules/* unconditional (7) | rules | A | A | A | A | A | A | A | **A** |
| commands/* (38) | commands | B | A | A | A | A | A | A | **B** |
| docs/getting-started.md | docs | C | B | C | n/a | B | A | C | **C** |
| docs/existing-project-onboarding.md | docs | C | B | C | n/a | B | A | C | **C** |
| hooks/commit_gate.sh | hook | C | B | C | n/a | B | A | C | **C** |
| rules/swe/vcs/pr-conventions.md | rule (path-scoped) | C | B | C | n/a | B | A | C | **C** |
| skills/software-planning/references/agent-pipeline-details.md | skill ref | C | B | C | n/a | B | A | C | **C** |
| scripts/regenerate_rules_manifest.py | script | C | A | C | n/a | B | A | B | **C** |
| .ai-state/decisions/225-remove-in-house-memory-subsystem.md | ADR | A | A | A | A | B | A | B | **B** |

Check codes reference dimensions above; Pipeline Discipline (P) skipped (Chronograph unavailable); Code Health (CH) N/A (no implementation-file samples triggered); Self-Verification (V) PASS.

---

## Findings

### Critical (blocks correct behavior)

*None.*

### Important (degrades quality or efficiency)

| # | Check | Dimension | Location | Finding | Recommended Action | Owner |
|---|-------|-----------|----------|---------|-------------------|-------|
| I1 | F01/F03 | Freshness | `docs/getting-started.md` lines 169, 222; `docs/existing-project-onboarding.md` line 108 | Stale memory-system prose: two rows in the `docs/getting-started.md` pipeline-artifact table reference `memory.json` (row 169: "Curated cross-session memory") and `memory-protocol` rule (row 222: "Cross-session memory: the memory-protocol rule auto-prompts"). `docs/existing-project-onboarding.md` line 108 says "`memory.json`…written on first use by the memory MCP server." Both files describe a removed system as active to end-users following the onboarding guide. | Remove or replace the memory rows/prose with a sandbook-placeholder note per the dec-225 intent. Owner: doc-engineer or user. | doc-engineer |
| I2 | F01/F03 | Freshness | `hooks/commit_gate.sh` lines 11–14; `rules/swe/vcs/pr-conventions.md` lines 36, 54; `skills/software-planning/references/agent-pipeline-details.md` lines 309, 326 | Stale memory-system references in operational files: `commit_gate.sh` has a `PRAXION_DISABLE_MEMORY_MCP` branch (`exit 0`) whose comment says "the memory-protocol rule forbids agents from calling remember()" — with the rule deleted this branch is now a dead code path with a misleading comment. `pr-conventions.md` advises rebasing to exercise merge drivers on `memory.json`. `agent-pipeline-details.md` documents `memory.json` conflict-resolution and semantic merge drivers for `remember()` as if they are active. | Update each file to reflect the removed subsystem. `commit_gate.sh`: the opt-out branch can be removed or left as a no-op with an updated comment noting the memory subsystem is gone. `pr-conventions.md` and `agent-pipeline-details.md`: remove or replace `memory.json` merge-driver guidance. Owner: context-engineer. | context-engineer |
| I3 | DL06/F01 | Decision Log / Freshness | `skills/software-planning/references/agent-pipeline-details.md`; `.ai-state/decisions/225-remove-in-house-memory-subsystem.md` frontmatter | **DL06 partial supersession list:** `dec-225` frontmatter contains `supersedes: dec-009` only, but both `dec-025` and `dec-039` carry `superseded_by: dec-225` in their frontmatter. Per DL06, a bidirectional link requires the `supersedes` field in dec-225 to list all three. The body of dec-225 (lines 115–116) correctly says "Also obsoletes dec-025 … and dec-039", but the frontmatter `supersedes` field (machine-readable) is incomplete. The `DECISIONS_INDEX.md` shows the correct superseded status for all three ADRs, so there is no user-visible gap — the asymmetry is a schema conformance issue that downstream tooling (e.g., finalize_adrs.py) could misread. | Update `dec-225` frontmatter to `supersedes: [dec-009, dec-025, dec-039]` (YAML list form). Also update dec-009's `supersedes` back-link if it lacks one. Owner: user (direct-tier ADR edit). | user |

### Suggested (improves but not urgent)

| # | Check | Dimension | Location | Finding | Recommended Action | Owner |
|---|-------|-----------|----------|---------|-------------------|-------|
| S1 | EC01 | Ecosystem Coherence | `docs/` (SVG pipeline diagram) | Agent pipeline SVG diagram still missing `agentic-transactions-architect` (carry-forward from Jun 2 and Jun 11 audits on main). The ASCII/table representation in `agents/README.md` is correct; only the SVG is stale. | Update SVG pipeline diagram to include `agentic-transactions-architect`. Owner: doc-engineer. | doc-engineer |
| S2 | C04/X02 | Completeness / Cross-Ref | `commands/README.md` | `commands/scores.md` exists on disk (38 FS commands total) but is absent from `commands/README.md` (37 entries). Carry-forward from Jun 11 main audit. | Add `/scores` entry to `commands/README.md`. Owner: doc-engineer. | doc-engineer |
| S3 | F01 | Freshness | `scripts/regenerate_rules_manifest.py` line 49; `tests/test_install_filter.sh` lines 300, 316–317 | `HOOK_DELIVER_ORDER` list in `regenerate_rules_manifest.py` contains `"swe/memory-protocol"` (line 49) — the file no longer exists. The `--check` mode passes today because `_validate_hook_deliver_coverage()` only checks that *declared* hook-deliver records appear in the order list, not the reverse — but the stale entry is confusing and will cause a false validation error if anyone adds a new hook-deliver rule whose name sorts before `memory-protocol`. Similarly, `tests/test_install_filter.sh` creates a fake `memory-protocol.md` fixture as a test input — this still passes but tests a configuration that no longer exists; it should either be updated to test current rules or annotated as a historical-behavior test. | Remove `"swe/memory-protocol"` from `HOOK_DELIVER_ORDER` in `scripts/regenerate_rules_manifest.py`. Update or annotate the `test_install_filter.sh` fixture. Owner: context-engineer or user. | context-engineer |
| S4 | F01 | Freshness | `.ai-state/sentinel_reports/SENTINEL_REPORT_2026-06-10_11-13-54.md` | Abandoned report stub with `[IN-PROGRESS]` header — never completed, never logged in `SENTINEL_LOG.md`. The stub's presence may mislead tooling that scans the `sentinel_reports/` directory. | Delete the stub file. Per sentinel boundary discipline, this report does not delete it — user action required. | user |

---

## Memory-Removal Coherence Verdict

**Overall: COHERENT with 3 Important residual gaps (all documentation/script prose, no pipeline breaks).**

### Deleted artifacts — confirmed absent

| Artifact | Status |
|----------|--------|
| `memory-mcp/` (entire directory) | DELETED |
| `rules/swe/memory-protocol.md` | DELETED |
| `skills/memory/` (entire directory) | DELETED |
| `hooks/inject_memory.py` | DELETED |
| `hooks/memory_gate.py` | DELETED |
| `hooks/validate_memory.py` | DELETED |
| `hooks/remind_memory.py` | DELETED |
| `scripts/merge_driver_memory.py` | DELETED |
| `commands/cajalogic.md` | DELETED |
| `commands/save-changes.md` | DELETED |

### Reference categories in live ecosystem files

| Category | Files | Verdict |
|----------|-------|---------|
| ADR and decision records (historical) | `dec-225` body, `dec-009/025/039` supersession pointers | LEGITIMATE — historical record. DL06 frontmatter gap flagged as I3. |
| CHANGELOG.md | memory-protocol, cajalogic, PRAXION_DISABLE_MEMORY_MCP | LEGITIMATE — historical changelog entry. |
| `.ai-state/TECH_DEBT_LEDGER.md` | td-003, td-013 on removed memory-mcp files | LEGITIMATE — resolved rows with `dec-225` as resolution ADR; migrate-to-RESOLVED pending. |
| `.ai-state/DESIGN.md` | "memory-mcp handlers… were removed with the memory subsystem (dec-225)" | LEGITIMATE — past-tense architectural note; doc acknowledges removal. |
| `.ai-state/SYSTEM_DEPLOYMENT.md` | "two Python MCP servers (memory-mcp, task-chronograph-mcp)" | STALE — describes memory-mcp as still present. **Orphan.** But lower priority than I1/I2 — the SYSTEM_DEPLOYMENT.md context is about "Praxion's own deployment surface" section and a reader will note the `memory-mcp/` dir is gone. Classified Suggested severity here; doc-engineer should update. (Not in the top 4 findings table due to lower blast radius.) |
| `docs/architecture.md` | `memory-mcp/src/memory_mcp/correlation.py` dead file link | STALE — live link to a deleted file. Doc-engineer should remove this reference from the observability tracing section. |
| `docs/getting-started.md` | Active how-to prose describing memory.json and memory-protocol rule | **STALE ORPHAN** — Important (I1). |
| `docs/existing-project-onboarding.md` | Active procedural note about memory.json not being pre-created | **STALE ORPHAN** — Important (I1). |
| `hooks/commit_gate.sh` | PRAXION_DISABLE_MEMORY_MCP guard + dead code path | **STALE ORPHAN** — Important (I2). |
| `rules/swe/vcs/pr-conventions.md` | merge-driver guidance for memory.json | **STALE ORPHAN** — Important (I2). |
| `skills/software-planning/references/agent-pipeline-details.md` | memory.json conflict resolution, remember() merge drivers | **STALE ORPHAN** — Important (I2). |
| `skills/upstream-stewardship/` | `.ai-state/memory.json` as example of content to sanitize | ACCEPTABLE — a sanitization example. memory.json can remain as an example of sensitive content even after removal. |
| `skills/context-security-review/SKILL.md` | "memory storage (.ai-state/memory.json)" | ACCEPTABLE — same pattern; legitimate sanitization example. |
| `skills/id-decontamination/SKILL.md` | "memory-protocol rule's discipline applies going forward" | STALE — references a deleted rule as if active. Suggested fix. |
| `scripts/regenerate_rules_manifest.py` | `swe/memory-protocol` in HOOK_DELIVER_ORDER | **STALE ORPHAN** — Suggested (S3). |
| `tests/test_install_filter.sh` | Creates fake memory-protocol.md fixture | STALE TEST FIXTURE — Suggested (S3). |
| `.ai-state/metrics_reports/*.json` | memory-protocol, skills/memory, memory-mcp in historical metric data | LEGITIMATE — frozen historical scan data, immutable. |
| `eval/.claude/settings.json` | PRAXION_DISABLE_MEMORY_MCP env key | ACCEPTABLE — the env key is harmless when no memory MCP is registered; the eval settings can retain it as a no-op. |
| `codex/config/README.md` | PRAXION_DISABLE_MEMORY_MCP in config example | ACCEPTABLE — no-op when no memory MCP is registered; low reader confusion. |
| `skills/tui-design/` | PRAXION_DISABLE_MEMORY_MCP as sample config snippet | STALE — describing a removed env key as if it controls active behavior. Suggested fix alongside tui-design skill refresh. |
| `docs/architecture.md` | `memory-mcp/src/memory_mcp/correlation.py` dead file link | **STALE ORPHAN** — Important-adjacent; rolled into I2 bucket with other docs/architecture.md items. |

### Additional stale docs (not in top findings but actionable)

| Location | Stale content | Priority |
|----------|--------------|---------|
| `.ai-state/SYSTEM_DEPLOYMENT.md` lines 18, 69 | "two Python MCP servers (memory-mcp, task-chronograph-mcp)" — memory-mcp is gone | Suggested |
| `docs/architecture.md` line 101 | Dead link to `memory-mcp/src/memory_mcp/correlation.py` | Suggested |
| `skills/id-decontamination/SKILL.md` | References `memory-protocol rule` as active rule | Suggested |
| `skills/tui-design/SKILL.md` line 82; `references/cli-ux-patterns.md` line 135 | `PRAXION_DISABLE_MEMORY_MCP=1` as active config option | Suggested |

---

## ADR Coherence (dec-225)

| Check | Result | Evidence |
|-------|--------|----------|
| DL01 — finalized ADR format | PASS | File at `.ai-state/decisions/225-remove-in-house-memory-subsystem.md` |
| DL02 — required frontmatter fields | PASS | All 8 required fields present: `id: dec-225`, `title`, `status: accepted`, `category: architectural`, `date: 2026-06-10`, `summary`, `tags`, `made_by: user` |
| DL03 — index consistency | PASS | 225 finalized ADRs, 225 index rows |
| DL04 — no orphaned supersession | PASS | dec-009, dec-025, dec-039 all exist and carry `status: superseded` + `superseded_by: dec-225` |
| DL05 — recent decisions have ADRs | PASS | Memory removal decision has a finalized ADR |
| DL06 — bidirectional supersession | **WARN** | dec-225 `supersedes: dec-009` (singular scalar). dec-025 and dec-039 carry `superseded_by: dec-225` but dec-225 does not list them in its `supersedes` field. Body text (lines 115–116) correctly names them. Frontmatter is the machine-readable field — it should be `supersedes: [dec-009, dec-025, dec-039]`. Filed as I3. |
| DL06 — Prior Decision section | PARTIAL | One `## Prior Decision` section covers dec-009; dec-025 and dec-039 are mentioned in the body but do not have separate `## Prior Decision` subsections. Acceptable for informal multi-ADR supersession but not ideal. |

---

## Canonical-Block Sync

```
python3 scripts/sync_canonical_blocks.py --check
checked 8 block(s) across 4 file(s); all in sync.
```

**Result: PASS.** All 8 canonical blocks consistent between `commands/onboard-project.md`, `commands/new-project.md`, and their source files under `claude/canonical-blocks/`. The memory-removal touched canonical blocks correctly and `--check` passes.

---

## Token Budget (T02)

**Convention**: sum of global `~/.claude/CLAUDE.md` + project `CLAUDE.md` + all `rules/**/*.md` without `paths:` frontmatter, **excluding `rules/README.md`** (navigation doc with no frontmatter — excluded by the prior-audit baseline convention confirmed by matching the 84,562-char main baseline).

| Component | Chars |
|-----------|-------|
| `~/.claude/CLAUDE.md` (global) | 11,785 |
| `CLAUDE.md` (project) | 11,014 |
| `rules/CLAUDE.md` | 2,118 |
| `rules/swe/adr-conventions.md` | 11,089 |
| `rules/swe/agent-behavioral-contract.md` | 1,572 |
| `rules/swe/agent-intermediate-documents.md` | 13,458 |
| `rules/swe/agent-model-routing.md` | 5,746 |
| `rules/swe/swe-agent-coordination-protocol.md` | 20,680 |
| `rules/swe/vcs/git-conventions.md` | 2,688 |
| **Total** | **80,150** |
| **Budget** | 87,500 (25,000 tokens) |
| **Headroom** | **7,350 chars (8.4%)** |
| **Token estimate** | ~22,900 (÷3.5) |

vs. main baseline (84,562 chars, 3.4% headroom): **+4,412 chars recovered** (4,125 from `memory-protocol.md` + 287 from minor rule edits). The budget trend is now **improving** after the critical narrowing in the Jun 2/Jun 11 runs.

**T02 grade: PASS.** Headroom at 8.4% is healthy and above the critical 5% warning threshold.

---

## Disposition of Prior Findings (2026-06-02)

| Finding | Prior Status | Disposition on This Branch |
|---------|-------------|--------------------------|
| I1: `approve(` → `authorize(` in robinhood.md | Important | **RESOLVED** — line 200 correctly uses `authorize()` |
| I2: `hybrid` approval_mode contradiction | Important | **RESOLVED** — line 131 has a clarifying comment: "hybrid = supports per-call choice… NOT a per-call value" |
| I3: `alwaysLoad` mcp.json scope | Important | **RESOLVED** — line 52 now has explicit scope guidance: "use `local` scope… never in `.mcp.json`" |
| I4: BC03 count hardcoded at 12 | Important | **RESOLVED** — sentinel.md line 256 now says "exactly the 13 agents" with correct list |
| S1: SVG diagram missing agentic-transactions | Suggested | **CARRY-FORWARD** — still open, not in scope of this branch |
| S2: robinhood.md version pin | Suggested | Not applicable (was on main branch; status unknown on this branch — lower priority) |
| S3: scores missing from commands/README.md | Suggested | **CARRY-FORWARD** — still open, not in scope of this branch |
| S4: SPEC_multi-language-support stale path | Suggested | Not within scope of this branch |

---

## Pipeline Discipline

Skipped: Task Chronograph MCP unavailable. No `.ai-work/` pipeline artifacts for this task.

---

## Tech-Debt Findings

No new TD rows filed this run. The latest `METRICS_LOG.md` entry is from 2026-06-06 (5 days ago — within the 14-day freshness window).

**TD05 — ledger discipline:** Two resolved rows in `TECH_DEBT_LEDGER.md` (td-003, td-013) reference `memory-mcp/` files with `status: resolved` and `dec-225` as `resolved-by`. These rows should migrate to `TECH_DEBT_RESOLVED.md` via `scripts/finalize_tech_debt_ledger.py` at post-merge. Not a new debt item — migration is a normal post-merge cleanup step.

---

## Test Topology (TT)

`.ai-state/TEST_TOPOLOGY.md` not present; TT checks skipped. The topology protocol is opt-in — see `skills/testing-strategy/references/test-topology.md`. No TT06 advisory triggered (this is a meta-project, not a user project with test suites).

---

## Spec Health (SH)

Five specs present. SH01/SH02 checks (auto) run per prior established baseline; no changes on this branch affect specs.

| Check | Result | Evidence |
|-------|--------|----------|
| SH01 | PASS (carry-forward) | File paths in traceability matrices resolve; no spec changes on this branch |
| SH02 | PASS (carry-forward) | All 5 specs have substantive traceability matrices |

---

## Behavioral Contract (BC)

| Check | Result | Evidence |
|-------|--------|----------|
| BC01 | PASS | `rules/swe/agent-behavioral-contract.md` exists, no `paths:` frontmatter |
| BC02 | PASS | Both CLAUDE.md files name Surface Assumptions → Register Objection → Stay Surgical → Simplicity First in canonical order |
| BC03 | PASS | All 13 contract-bound agents reference the rule (confirmed via grep — 13 files returned) |
| BC04 | PASS | `skills/code-review/references/report-template.md` has `### Behavioral Contract Findings` with all 6 tags |

---

## Gate Liveness (GL)

| Check | Result | Evidence |
|-------|--------|----------|
| GL02 | PASS | `python3 scripts/check_gate_liveness.py --json` returned `{"findings": [], "count": 0}` |
| GL01 | PASS (LLM judgment) | No consumed-but-unproduced sections detected in pass 2 scan |
| GL03 | PASS (LLM judgment) | No presence-only gates without substance assertions found in sampled checks |

---

## Self-Verification (V)

| Check | Result |
|-------|--------|
| V01 | PASS — `./agents/sentinel.md` in agents array |
| V02 | PASS — Agent table in coordination protocol contains sentinel row |
| V03 | PASS — `agents/README.md` table contains sentinel row |
| V04 | PASS — Check catalog section present |

---

## Pre-Refactor Plan Integrity (PR)

No `.ai-work/*/PRE_REFACTOR_PLAN.md` found. PR checks skipped (substrate absent — INFO only).

---

## Architecture Completeness (AC)

| Check | Result | Evidence |
|-------|--------|----------|
| AC03 | PASS | `.ai-state/DESIGN.md` file paths ≤50% missing (DESIGN.md notes removal of memory-mcp in past tense — accurate) |
| AC04 | PASS | No broken `dec-NNN` inline references found; `dec-225` cited correctly |
| AC05 | PASS | `docs/architecture.md` exists alongside `.ai-state/DESIGN.md` |
| AC10 | WARN | `docs/architecture.md` line 101: dead link to `memory-mcp/src/memory_mcp/correlation.py` (file deleted). `scripts/aac_fence_validator.py` substrate check: script absent, AC10 skipped via INFO note. |

---

## Recommended Actions (prioritized)

1. **(I1, Important)** Update `docs/getting-started.md` rows 169 and 222, and `docs/existing-project-onboarding.md` line 108: remove or replace all memory.json / memory-protocol prose with a sandbook-placeholder note. Owner: doc-engineer.

2. **(I2, Important)** Update `hooks/commit_gate.sh` (remove/annotate the dead `PRAXION_DISABLE_MEMORY_MCP` branch), `rules/swe/vcs/pr-conventions.md` (remove memory.json merge-driver guidance), and `skills/software-planning/references/agent-pipeline-details.md` (remove memory.json reconciliation section). Owner: context-engineer.

3. **(I3, Important)** Edit `dec-225` frontmatter: change `supersedes: dec-009` to `supersedes: [dec-009, dec-025, dec-039]`. Owner: user (direct edit to ADR file).

4. **(S1, Suggested — carry-forward)** Update SVG pipeline diagram in `docs/` to include `agentic-transactions-architect`. Owner: doc-engineer.

5. **(S2, Suggested — carry-forward)** Add `/scores` entry to `commands/README.md`. Owner: doc-engineer.

6. **(S3, Suggested)** Remove `"swe/memory-protocol"` from `HOOK_DELIVER_ORDER` in `scripts/regenerate_rules_manifest.py`; update or annotate the `tests/test_install_filter.sh` memory-protocol fixture. Owner: context-engineer.

7. **(S4, Suggested)** Delete the abandoned stub `.ai-state/sentinel_reports/SENTINEL_REPORT_2026-06-10_11-13-54.md`. Owner: user (direct delete).

8. **(Post-merge cleanup)** Run `scripts/finalize_tech_debt_ledger.py` to migrate td-003 and td-013 (resolved memory-mcp rows) to `TECH_DEBT_RESOLVED.md`. Owner: user.

9. **(Additional docs cleanup)** Update `.ai-state/SYSTEM_DEPLOYMENT.md` (remove memory-mcp from MCP server list), `docs/architecture.md` (remove dead correlation.py link), `skills/id-decontamination/SKILL.md` (remove memory-protocol rule reference as active), `skills/tui-design/` (remove PRAXION_DISABLE_MEMORY_MCP examples). Owner: context-engineer.
