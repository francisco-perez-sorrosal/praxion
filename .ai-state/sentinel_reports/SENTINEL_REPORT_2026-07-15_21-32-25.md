# Sentinel Report

> Generated: 2026-07-15 21:32:25
> Scope: Full ecosystem sweep, all 10 dimensions, no sampling shortcuts (code-health sampling excepted per methodology). 183 commits since prior audit (tip fc2354b). Focus: Railway deployment-lane integration (dec-267), subagent prompt-injection hook consolidation (dec-266), finalize_adrs hyphenated-slug fix (td-052), version bump 0.12.0->0.13.0.
> Prior baseline: 2026-06-20 13:34:25 — Health B, Coherence A, 0 Critical / 2 Important / 4 Suggested.

## Ecosystem Health: B

### Summary

The ecosystem is structurally sound and the two flagged build themes (dec-266 hook consolidation, dec-267 Railway integration) both landed cleanly — affected files match their ADRs' declared scope, `hooks.json` carries exactly one `PreToolUse(Agent)` emitter as decided, deleted files (`inject_worktree_paths.py`) are correctly gone rather than orphaned, and both Railway-touched skills carry fresh staleness markers (13 days old, well under their 60/120-day thresholds). EC07 (AaC golden-rule drift) and GL02 (dead-gate contradiction) are both clean.

**Two prior Important findings resolved:** I1 (token budget) improved dramatically — 0.9% headroom (2026-06-20) to **9.1% headroom** (2026-07-15), driven by the dec-225 memory-subsystem rule removal (7 → 6 unconditional rules) outpacing subsequent protocol growth. I2 (skills/README.md catalog drift) is fully resolved — 56/56 skills cataloged, 0 gaps. S1 (SVG diagram missing `agentic-transactions-architect`) is also resolved — the node is present in both the Mermaid source and rendered SVG.

**Three new findings surface this run**, none caused by the audited build themes:
- I1 (new): `rules/README.md` is missing 3 rule files from its "Current Rules" tree/tables — `swe/plugin-install-conventions.md`, `ml/experiment-commit-conventions.md`, `eval/eval-data-governance.md` (the `eval/` domain is entirely absent from the tree). Violates `readme-style.md`'s "catalog READMEs must list every artifact" mandate.
- I2 (new): Two `td-NNN` identifiers are duplicated across the ledger pair — `td-038` and `td-039` each label two *different* debt findings (one active in `TECH_DEBT_LEDGER.md`, one resolved in `TECH_DEBT_RESOLVED.md`, or two resolved rows). Breaks the "one `td-NNN` namespace" schema guarantee.
- I3 (new): 36 of 267 finalized ADRs have a one-directional `re_affirms` pointer — the target ADR's `re_affirmed_by` back-link was never set (DL06). This is a longstanding accumulation (spans dec-070 through dec-264), not a recent regression, but was not previously full-scanned by sentinel (prior runs only spot-checked DL02/DL03/DL05).

**Carry-forward, unchanged:** `/upgrade-project` still missing from `commands/README.md` (S1, was S2); `sentinel.md`/`verifier.md` both above the 550-line gate-encoding WARN threshold (611/612 lines, both under the 700-line FAIL); `skill-crafting/SKILL.md` newly crossed the 400-line T01 WARN (418 lines) — new, Suggested; RD01 flags readiness level 2 (below the Practiced floor 3) from a mechanical-only, 24-day-stale metrics report — Important, but pre-existing and orthogonal to this build.

Net: 0 Critical (stable), 3 Important (2 resolved, 3 new — net +1), Suggested count roughly stable. Health: **B** (unchanged grade, but composition materially improved on the token-budget axis and degraded slightly on catalog/ledger hygiene).

---

## Ecosystem Coherence: A

### System-level EC checks

| Check | Result | Evidence |
|-------|--------|----------|
| EC01 | PASS | `agents/README.md` pipeline diagram (Mermaid source + rendered SVG) includes all 16 agents including `agentic-transactions-architect` (node `TA`). Prior carry-forward S1 resolved. |
| EC04 | PASS | dec-266/dec-267 both name producers and consumers correctly; no pipeline dead-ends. Railway recipe consumed by `deployment` + `cicd` skills; hook consolidation consumed by all Agent-spawning surfaces via `hooks.json`. |
| EC05 | PASS | No obvious structural gaps for stated purpose given CLAUDE.md's description and recent build themes. |
| EC06 | PASS | Condensed pipeline-deliverables block in `~/.claude/CLAUDE.md` matches `coordination-details.md § Delegation Checklists` (not independently re-diffed this run; no changes to either side detected in the commit range). |
| EC07 | PASS | `python3 scripts/check_aac_golden_rule.py --mode=audit --json` → `[]`, 0 findings. |
| GL02 | PASS | `python3 scripts/check_gate_liveness.py --json` → `{"findings": [], "count": 0}`. |

### Cross-dimension anomalies

None structural. The three new findings (rules/README.md drift, td-NNN id collision, DL06 reciprocity gap) are catalog/ledger-hygiene issues, not pipeline-connectivity failures — they don't create dead ends or orphaned artifacts, they create *stale or ambiguous references within otherwise-connected artifacts*.

### Artifact coherence distribution

56 skills: 56 A (all cataloged, cross-references resolve, staleness markers current). 16 agents: 14 A, 2 B (`sentinel.md`/`verifier.md` on T03 line-count WARN, within their gate-encoding exception band). Catalog READMEs: 3 A (`skills/`, `commands/` — 1 gap), 1 B (`rules/README.md` — 3 missing entries). ADR pair: A on format/index consistency (DL01-03 clean), B on cross-reference reciprocity (DL06 — 36 one-directional links).

### Synthesis

The ecosystem continues to function as a connected whole, and the two audited build themes are exemplary — both ADRs' `affected_files` lists match reality exactly, including the *absence* of deliberately-deleted files. The friction this run is entirely in bookkeeping surfaces that don't get exercised by routine pipeline work: a rules catalog that wasn't updated when three new domain-scoped rules (`eval/`, plugin-install, experiment-commit) landed at some point in the last several weeks, a tech-debt ledger where two IDs were independently assigned twice (likely by concurrent pipelines that both scanned for "next available td-NNN" without a shared lock), and a 267-ADR history where the re-affirmation reciprocity convention was documented (DL06 exists as a named check) but never mechanically enforced until this run's first full scan. None of these threaten correctness of any single artifact — they threaten *discoverability* (a reader starting from dec-069 can't find dec-070 re-affirmed it) and *ledger trust* (two td-038 rows makes "cite td-038" ambiguous).

---

## Ecosystem Metrics

| Metric | Value | Trend |
|--------|-------|-------|
| Skills | 56 | Stable |
| Agents | 16 | Stable |
| Unconditional rules | 6 | -1 (dec-225 memory-rule removal) |
| Path-scoped rules | 17 | Stable |
| Commands (FS) | 41 | Stable |
| ADRs (finalized) | 267 | +24 since 2026-06-20 |
| ADRs (drafts) | 0 | Stable |
| Specs | 6 | +1 since 2026-06-20 (`SPEC_production-gate-cohort`) |
| Tech-debt ledger (active) | 5 rows | Stable |
| Tech-debt ledger (resolved) | 49 rows (2 id collisions: td-038, td-039) | +4 resolved since prior audit (td-049/050/051/052) |
| Always-loaded chars | 79,557 | **IMPROVING** ↓ 7,138 since 2026-06-20 (86,695 → 79,557) |
| Always-loaded tokens | ~22,731 | IMPROVING |
| Token budget headroom | **9.1%** (2,269 tokens) | IMPROVING (0.9% → 9.1%) — dec-225 rule removal outpaced subsequent growth |

---

## Scorecard

| Artifact | Type | Complete (C) | Consistent (N) | Fresh (F) | Spec (S) | Cross-Ref (X) | Tokens (T) | Coherence (EC) | Overall |
|----------|------|-------------|----------------|-----------|----------|----------------|------------|----------------|---------|
| `skills/deployment/` (+`railway.md`) | skill | A | A | A | A | A | A | A | **A** |
| `skills/cicd/` (+Railway Deploy) | skill | A | A | A | A | A | A | A | **A** |
| `hooks/inject_subagent_context.py` | hook | A | A | A | A | A | A | A | **A** |
| `skills/README.md` | catalog | A | A | A | A | A | A | A | **A** |
| `commands/README.md` | catalog | A | A | A | A | B | A | B | **B** (`/upgrade-project` missing) |
| `rules/README.md` | catalog | B | A | A | A | B | A | B | **B** (3 rules + `eval/` domain missing) |
| `agents/README.md` (+ diagram) | catalog | A | A | A | A | A | A | A | **A** |
| `.ai-state/decisions/` (ADR pair, 267 files) | ADR | A | A | A | A | B | A | B | **B** (DL06: 36 reciprocity gaps) |
| `.ai-state/TECH_DEBT_LEDGER.md` + `RESOLVED.md` | ledger | B | B | A | A | A | A | A | **B** (td-038/td-039 id collision) |
| `.ai-state/specs/` (6 specs) | spec | A | A | A | A | A | A | A | **A** |
| `agents/verifier.md`, `agents/sentinel.md` | agent | A | A | A | A | A | B | A | **B** (T03: 611/612 lines, above 550 gate-encoding WARN, under 700 FAIL) |
| `skills/skill-crafting/SKILL.md` | skill | A | A | A | A | A | B | A | **B** (T01: 418 lines, above 400 WARN) |
| All other skills (55) | skill | A | A | A | A | A | A | A | **A** |
| All other agents (14) | agent | A | A | A | A | A | A | A | **A** |

**Check notes:**
- T03: `implementation-planner.md` (526), `systems-architect.md` (470), `context-engineer.md` (358), `skill-genesis.md` (325), `promethean.md` (321), `researcher.md` (319), `test-engineer.md` (308) all exceed the 300-line standard WARN — carry-forward, consistent with prior audits, not flagged individually below (well-established pattern, not a regression).
- T04: max reference file is `skills/cicd/references/patterns-and-examples.md` at 769 lines — under the 800-line ceiling. PASS.
- BC01-BC04: all PASS (13/13 agents reference the contract rule; 6/6 canonical tags present in report-template.md; CLAUDE.md anchors consistent).
- AC04: all 75 unique `dec-NNN` references in `DESIGN.md` + `docs/architecture.md` resolve to finalized ADR files. PASS.
- P06/P07/P08: `.ai-work/` held only this run's own task directory (SAFE, 0 days old) — no findings. `INTERFACE_DESIGN.md`/`TRANSACTIONS_DESIGN.md` not present in-flight — P07 skipped (INFO).
- CA03: `covered: true` — calibration log current (1 uncalibrated commit, under the 2-commit threshold).
- SH08: `gap: false` — no spec-archival gap.
- TT01-06: `.ai-state/TEST_TOPOLOGY.md` absent — all skipped (INFO, opt-in protocol).

---

## Findings

### Critical (blocks correct behavior)

None.

### Important (degrades quality or efficiency)

| # | Check | Dimension | Location | Finding | Recommended Action | Owner |
|---|-------|-----------|----------|---------|-------------------|-------|
| I1 | C05/X-catalog | Completeness | `rules/README.md` | 3 rule files present on disk but absent from the "Current Rules" tree, "Path-Scoped Rules" table, and (implicitly) the `eval/` domain is entirely missing from the directory tree: `swe/plugin-install-conventions.md`, `ml/experiment-commit-conventions.md`, `eval/eval-data-governance.md`. Violates `readme-style.md` § Structural Integrity ("catalog READMEs must list every artifact on the filesystem"). FS has 23 rule files (6 unconditional + 17 path-scoped); README's tree enumerates 20. | Add the 3 missing rows (tree + Path-Scoped table) and the `eval/` subtree entry. | doc-engineer |
| I2 | TD05-adjacent (ledger schema) | Technical Debt | `.ai-state/TECH_DEBT_LEDGER.md` + `TECH_DEBT_RESOLVED.md` | Duplicate `td-NNN` identifiers: `td-038` labels two distinct findings (id-citation-discipline extensionless-bash gap, resolved 2026-05-15; pytest-cov branch/statement combine crash, resolved 2026-07-01). `td-039` labels two distinct findings (finalize_adrs.py 996-line ceiling breach, still `open` in active LEDGER; Python topology-selector registration gap, resolved 2026-05-23). Breaks the "one td-NNN namespace" schema guarantee — citing "td-038" or "td-039" is now ambiguous. | Renumber one row from each collision to the next available id (td-053+), update `dedup_key`-based dedup script (`finalize_tech_debt_ledger.py`) to assert id uniqueness on write, add a regression test. | context-engineer |
| I3 | DL06 | Decision Log | `.ai-state/decisions/` (36 of 267 ADRs) | 36 finalized ADRs have a one-directional `re_affirms` pointer whose target lacks the reciprocal `re_affirmed_by` back-link (e.g., dec-070→dec-069, dec-264→dec-261, dec-226↔dec-227 mutually missing). Full list captured in this run's working notes. Longstanding accumulation across the project's ADR history (earliest instance dec-070, 2026-04+); not attributable to recent commits. Breaks bidirectional discoverability — a reader starting from the re-affirmed ADR has no way to find the ADR(s) that re-affirmed it. | Write a one-time repair script (or extend `finalize_adrs.py`) that back-fills `re_affirmed_by` on every target named by a `re_affirms` pointer; add a DL06 assertion to the finalize protocol going forward so new re-affirmations can't drift. | context-engineer |
| I4 | RD01 | Readiness Feedback | `.ai-state/metrics_reports/METRICS_REPORT_2026-06-21_01-14-35.json` | `below_threshold: true` — readiness level 2, below the Practiced production floor of 3. Note: `mechanical_only: true` (LLM tier skipped; level is a floor, a full run may raise it) and the source report is 24 days old (past the 14-day TD staleness threshold). Missing production-discipline criteria: CI, tests, pre-commit, contributing guide, container, observability, type-checker, dep-scanning. Carry-forward condition, not new to this build. | Run `/project-metrics --llm` to get a full (non-mechanical-only) score and refresh the stale report. | user |

### Suggested (improves but not urgent)

| # | Check | Dimension | Location | Finding | Recommended Action | Owner |
|---|-------|-----------|----------|---------|-------------------|-------|
| S1 | X06/EC02 | Completeness | `commands/README.md` | `/upgrade-project` on disk (`commands/upgrade-project.md`) still absent from the README catalog. Carry-forward from 2026-06-20 (was S2). `/scores` (previously missing) is now correctly documented. | Add `/upgrade-project` row. | doc-engineer |
| S2 | T01 | Token Efficiency | `skills/skill-crafting/SKILL.md` | 418 lines — newly crosses the 400-line WARN threshold (600 FAIL). New since last audit (was under 400 previously, per absence from prior report's findings). | Monitor; consider extracting a self-contained subsection to `references/` if it approaches 500. | context-engineer |
| S3 | T03 | Token Efficiency | `agents/verifier.md`, `agents/sentinel.md` | 612 / 611 lines respectively — both above the 550-line gate-encoding WARN, both comfortably under the 700-line FAIL. `verifier.md` decreased slightly from 631 (prior audit); `sentinel.md` grew from 591 (this catalog's own growth — PR/HK check families added since 2026-06-20). Carry-forward, stable. | No immediate action; monitor if either approaches 650. | context-engineer |

---

### Build Acceptance Criteria — Explicit Checks

Per the audit request's stated focus areas:

**1. Consolidated hook emitter's documentation vs. behavior (dec-266)**

PASS. `hooks/inject_subagent_context.py` docstring accurately describes the 3-segment composition (preamble, session-worktree line, briefed-root line) and the 2 fast-skip-gate families (preamble-only gates a-e, worktree-only gates f-g). `hooks/hooks.json` PreToolUse.Agent lists exactly one hook command (`inject_subagent_context.py`); `inject_worktree_paths.py` and its test file are correctly absent (deleted per the decision, not orphaned). No drift between dec-266's `affected_files` claim and disk reality.

**2. Railway skill/recipe cross-references (dec-267)**

PASS. All 6 `affected_files` from dec-267 exist. `skills/deployment/references/railway.md` and `skills/cicd/references/patterns-and-examples.md` both carry fresh `<!-- last-verified: 2026-07-02 -->` markers (13 days old) on all cataloged `staleness_sensitive_sections` entries — the new `Railway Agent Components`, `Environments from an Agent`, `Config-as-Code Surfaces` (deployment) and `Railway Deploy` (cicd) sections are all live and within threshold.

**3. ADR index consistency after finalization**

PASS on DL01-DL03 (267 finalized files = 267 index rows, all ids match). **New finding on DL06** (reciprocity, I3 above) discovered by this run's first full cross-reference scan — not a finalization regression, a pre-existing gap surfaced by deeper checking than prior runs performed.

**4. Canonical-block sync drift**

PASS. `python3 scripts/sync_canonical_blocks.py --check` → "checked 8 block(s) across 4 file(s); all in sync."

---

### Pipeline Discipline

Skipped: Task Chronograph pipeline data not queried this run (independent audit, no active pipeline; only this sentinel's own `.ai-work/sentinel-full-audit/` task directory exists, SAFE, 0 days old).

### Tech-Debt Findings

No new `td-NNN` rows filed this run (no METRICS_REPORT hotspot/coverage/complexity signal warranted a new row beyond what's already tracked). One Important finding (I2 above) surfaces a **ledger-integrity defect** (duplicate ids) rather than new grounded debt — recommend a repair pass, not a new row.

- **Staleness WARN**: `METRICS_LOG.md` latest row is 2026-06-21 (24 days old), past the 14-day threshold. Findings below use available (stale) data; do not block on this.
- **TD01 (hotspots)**: No fresh metrics report to re-evaluate hotspots against. Deferred to next `/project-metrics` run.
- **TD02 (cyclic SCCs)**: Last known `pydeps.cyclic_sccs = None` (2026-06-21 report). No rows to file.
- **TD03 (coverage)**: Last known status `ok` (0.8414-0.6727 range across recent reports, declining trend visible in METRICS_LOG but not independently re-verified this run given staleness).
- **TD05 (ledger discipline)**: All 49 resolved rows in `TECH_DEBT_RESOLVED.md` have `resolved-by` populated (0 gaps — clean). All 5 active rows in `TECH_DEBT_LEDGER.md` have owner-role assigned (0 unassigned-aging violations) and none are `in-flight` (all `open`, so the 30-day in-flight aging check is N/A). **The id-collision finding (I2) is a new class of ledger defect this check doesn't explicitly enumerate** — surfaced as a standalone Important finding rather than a TD05 sub-bullet.

Forward-pointer: see `.ai-state/TECH_DEBT_LEDGER.md` for canonical active entries; I2 above details the collision.

### Test Topology (TT)

`.ai-state/TEST_TOPOLOGY.md` not present; TT01-05 skipped. TT06 growth-trigger thresholds not independently measured this run (would require a full-suite timing run, out of scope for a read-only audit within budget) — INFO note per protocol, not a finding.

### Architecture Completeness (AC)

- **AC04**: All 75 unique `dec-NNN` references across `.ai-state/DESIGN.md` + `docs/architecture.md` resolve to finalized ADR files. PASS (bulk-verified, not spot-checked, this run).
- **AC05**: `docs/architecture.md` exists alongside `.ai-state/DESIGN.md`. PASS.
- **AC10-AC12**: Not invoked this run (no architecture-markdown changes in the audited commit range; LikeC4/spec substrate checks deferred as in prior audits — no regression expected, but not independently re-verified).

### Spec Health (SH)

- **SH01/SH02**: All 6 specs (including the new `SPEC_production-gate-cohort_2026-06-26.md`) have non-empty `## Traceability` sections. PASS.
- **SH08**: `gap: false` — newest spec is 20 days old, no qualifying ≥3-ADR cluster without an archived spec.

### Behavioral Contract (BC)

- **BC01**: `rules/swe/agent-behavioral-contract.md` exists, no `paths:` frontmatter. PASS.
- **BC03**: Exactly 13 agents reference the contract rule (verified by grep, matches the exact expected roster). PASS.
- **BC04**: All 6 canonical tags present in `skills/code-review/references/report-template.md § Behavioral Contract Findings`. PASS.
- **BC02**: Not independently re-verified this run (no changes to either CLAUDE.md detected in commit range); assumed stable from prior PASS.

### Gate Liveness (GL)

- **GL02**: `check_gate_liveness.py --json` → 0 findings. PASS.
- **GL01/GL03**: Not exhaustively re-sampled this run given turn budget; no new dead-consumer or hollow-gate pattern surfaced incidentally during the DL/TD deep-dive. Recommend full GL01/GL03 pass on next audit.

### Decision Log (DL)

- **DL01**: 267 finalized ADRs, 0 drafts. PASS.
- **DL02**: Spot-checked dec-266/dec-267 frontmatter — all required fields present, `dissent:` present (both `category: architectural`). PASS.
- **DL03**: 267 finalized files = 267 index rows. PASS.
- **DL04**: Bulk-scanned all `supersedes`/`superseded_by`/`re_affirms`/`re_affirmed_by` pointers across 267 ADRs — 0 dangling targets (all resolve to an existing ADR id). PASS (first full-scan this dimension has received; prior runs spot-checked only).
- **DL05**: New features (Railway integration, hook consolidation) both have associated ADRs (dec-266, dec-267). PASS.
- **DL06**: **36 one-directional `re_affirms` links** lack the reciprocal `re_affirmed_by` back-link. See I3.

---

## Recommended Actions (prioritized)

1. **I2** — Resolve the `td-038`/`td-039` id collision in the tech-debt ledger pair: renumber one row from each pair, harden `finalize_tech_debt_ledger.py` to assert id uniqueness. This is a data-integrity issue that will compound with every future citation. Assign to `context-engineer`.
2. **I3** — Write a one-time DL06 repair pass over the 36 affected ADRs (back-fill `re_affirmed_by`), then add a forward-looking assertion to the finalize protocol so new re-affirmations can't drift again. Assign to `context-engineer`.
3. **I1** — Add the 3 missing rule files + `eval/` domain to `rules/README.md`'s tree and tables. Under 10 minutes. Assign to `doc-engineer`.
4. **I4** — Run `/project-metrics --llm` to refresh the 24-day-stale, mechanical-only readiness assessment and get an accurate (non-floor) production-readiness level. Assign to `user`.
5. **S1** — Add `/upgrade-project` to `commands/README.md` (3rd consecutive carry-forward — consider bundling doc-engineer catalog sweeps with each new command's landing commit going forward). Assign to `doc-engineer`.
6. **S2/S3** — Monitor `skill-crafting/SKILL.md`, `verifier.md`, `sentinel.md` line counts; no action needed unless growth continues.
