# Sentinel Report

## Ecosystem Health: B

### Summary

Audited the canonical checkout at `/Users/fperez/dev/praxion` (branch `main`, HEAD `be408c1`) — 6 new commits since the last report (2026-07-15), landing the prompting-UX touchpoints batch: a new path-scoped `rules/writing/html-output-conventions.md` extension (Authorship Boundary + ephemeral-HTML exception), its enforcing pre-commit gate `scripts/check_html_authorship.py` (7/7 tests pass, 87.5% coverage), the dashboard "Copy as prompt" feature (26/26 vitest tests pass), and finalized `dec-295`. This merge is coherent and well-integrated: the new rule cross-references its enforcing script correctly, `scripts/CLAUDE.md` and `rules/README.md` were updated in lockstep, `dec-295`'s frontmatter and body sections are well-formed (all 5 required sections for `category: architectural` present), and `DECISIONS_INDEX.md` is in sync (295 finalized files = 295 index rows).

Zero Critical findings. Two Important findings, both process-discipline gaps rather than artifact defects: calibration-log coverage has lapsed (7 pipeline commits since the last `calibration_log.md` row) and three `.ai-work/` slugs are missing `TASK_BRIEF.md` despite Standard/Full-tier evidence. Five Suggested findings are largely carry-forwards from prior audits (SVG diagram gap, a stale spec path, a version-pin gap) plus one freshly corrected staleness-marker signal (a prior sentinel run's F07/F08 script had a frontmatter-matching bug that produced false negatives — corrected this run, revealing 25 genuinely aging marker sections across 5 skills, all WARN not FAIL).

Token budget headroom is at 6.6% (23,339 / 25,000 tokens) — improved from the 3.4% low-water mark recorded 2026-06-11, a healthy trend reversal. Two new Tech-Debt Ledger rows were filed this run (td-058, td-059) after LLM-judgment gating against `METRICS_REPORT_2026-07-29_22-11-31`; TD05 ledger-discipline audit is clean (no stale in-flight rows, no unassigned rows past their grace period).

### Ecosystem Coherence: A

**System-level EC checks:**
| Check | Result | Evidence |
|-------|--------|----------|
| EC01 | PASS | All 16 agent files (excl. README/CLAUDE) appear in `agents/README.md`'s pipeline tables |
| EC04 | PASS | No orphaned pipeline-output docs identified; this merge's new artifacts (rule, script, dashboard components) all have both a producer commit and a consumer (pre-commit hook, `scripts/CLAUDE.md` catalog entry) |
| EC05 | PASS | No obvious structural gaps against CLAUDE.md's stated purpose/Future Paths |
| EC07 | PASS | `check_aac_golden_rule.py --mode=audit --json` returned `[]` (0 findings) over the audit horizon |

**Cross-dimension anomalies:** None spanning multiple artifacts. The two Important findings (CA03, P06) are both instances of a single underlying pattern — orchestrator-side append obligations (calibration row, TASK_BRIEF.md) that have no enforcing hook and can silently lapse during a busy multi-pipeline stretch (8 concurrent `.ai-work/` slugs currently tracked). This is a known, documented risk class (both checks exist precisely because the append has no mechanical enforcement) rather than a new structural gap.

**Artifact coherence distribution:** Of ~481 tracked artifacts, an estimated 8-10 carry a per-artifact Suggested-level finding (5 skills with aging staleness markers, `commands/README.md`, the pipeline SVG diagram, `robinhood.md`, one spec). The remainder score cleanly. Distribution: approximately 470 A, 8 B, 3 C, 0 D/F.

**Synthesis:** The ecosystem continues to work coherently as a system — new artifacts land with correct cross-references, tests, and catalog updates in the same commit batch (a strong signal of disciplined authoring, not just structural luck). The friction points are process-adherence gaps at the orchestrator seam (calibration logging, TASK_BRIEF capture) rather than artifact-quality gaps — these are exactly the kind of drift a periodic sentinel pass exists to surface, since nothing else catches them mechanically.

### Ecosystem Metrics

| Metric | Value | Trend |
|--------|-------|-------|
| Skills | 56 | up from 51 (2026-06-11) |
| Agents | 16 | stable |
| Rules (unconditional) | 7 | stable |
| Rules (path-scoped) | 17 | stable |
| Commands (FS) | 43 | up from 41 (2026-06-11) |
| ADRs (finalized) | 295 | up from 224 (2026-06-11) |
| ADRs (draft) | 0 | stable (finalize backstop working) |
| Specs | 6 | stable |
| Token budget headroom | 6.6% (23,339/25,000 tokens) | IMPROVING — was 3.4% at 2026-06-11 low-water mark |
| Active TD ledger rows | 7 (5 pre-existing + 2 filed this run) | td-058, td-059 new |
| Calibration log rows | 42 | CA03 flags 7 uncalibrated commits since last row |

### Scorecard

| Artifact | Type | Complete (C) | Consistent (N) | Fresh (F) | Spec (S) | Cross-Ref (X) | Tokens (T) | Coherence (EC) | Overall |
|----------|------|--------------|-----------------|-----------|----------|----------------|------------|----------------|---------|
| `rules/writing/html-output-conventions.md` | Rule (path-scoped) | A | A | A | A | A | A | A | A |
| `scripts/check_html_authorship.py` + test | Script | A | A | A | A | A | A | A | A |
| `.ai-state/decisions/295-*.md` | ADR (finalized) | A | A | A | A | A | A | A | A |
| `dashboard_app/` (Copy-as-prompt feature) | Code + tests | A | A | A | A | A | A | A | A |
| `skills/hook-crafting/` | Skill | A | A | B (F08: 4 sections 65d/60d thresh) | A | A | A | A | B |
| `skills/likec4-querying/` | Skill | A | A | B (F08: 2 sections 90d/60d thresh) | A | A | A | A | B |
| `skills/cicd/` | Skill | A | A | B (F08: 3 sections 65d/60d thresh) | A | A | A | A | B |
| `skills/mcp-crafting/` | Skill | A | A | B (F08: 7 sections 65d/60d thresh) | A | A | A | A | B |
| `skills/communicating-agents/` | Skill | A | A | B (F08: 9 sections 89d/60d thresh) | A | A | A | A | B |
| `skills/agentic-transactions/references/robinhood.md` | Reference | A | A | A | B (S2: no version pin) | A | A | A | B |
| `commands/README.md` | Catalog doc | B (X07: `refresh-claude-blocks` missing) | A | A | A | B | A | A | B |
| `docs/diagrams/architecture/rendered/agent_pipeline.svg` | Diagram | B (S1: missing agentic-transactions-architect node) | A | A | A | B | A | A | B |
| `.ai-state/specs/SPEC_multi-language-support_2026-05-11.md` | Spec | A | A | B (SH01: stale path) | A | A | A | A | B |
| `.ai-state/calibration_log.md` | State doc | C (CA03: 7 uncalibrated commits) | A | A | A | A | A | A | C |
| `.ai-work/{p1-installable-core,p2-cross-model-gate,p4-healing-sidecar}/` | Pipeline state | C (P06: TASK_BRIEF.md absent) | A | A | N/A | A | A | A | C |
| All other skills (51), agents (16), commands (42), rules (24), finalized ADRs (294) | Various | A | A | A | A | A | A | A | A |

### Findings

#### Critical (blocks correct behavior)
None found this pass.

#### Important (degrades quality or efficiency)
| # | Check | Dimension | Location | Finding | Recommended Action | Owner |
|---|-------|-----------|----------|---------|---------------------|-------|
| 1 | CA03 | Calibration Accuracy | `.ai-state/calibration_log.md` | `check_calibration_coverage.py --json` reports `covered: false` — newest calibration row is dated 2026-07-28, but 7 pipeline commits have landed since without a matching calibration-log append (the orchestrator's append-on-completion obligation lapsed; no enforcing hook exists). | Append the missing calibration rows for the 7 uncalibrated commits (retroactive is acceptable per the calibration-log spec), or confirm they were Direct-tier micro-fixes exempt from the multi-field row and use the single-line format. | user / orchestrator |
| 2 | P06 | Pipeline Discipline | `.ai-work/p1-installable-core/`, `.ai-work/p2-cross-model-gate/`, `.ai-work/p4-healing-sidecar/` | `check_p06_task_brief.py --json` returns 3 WARN rows — each slug has `SYSTEMS_PLAN.md` present (implying Standard/Full tier) but no `TASK_BRIEF.md`. These are older in-flight slugs (5-6 days idle per `clean_work_safety.py`) predating routine TASK_BRIEF capture, or the Intake Clarity Gate was skipped. | Backfill `TASK_BRIEF.md` for any slug still active, or clean up via `/clean-work` if abandoned (2 of the 3 are already `WARN`/`BLOCK` classified by `clean_work_safety.py` for unmerged learnings / unarchived traceability). | user / context-engineer |

#### Suggested (improves but not urgent)
| # | Check | Dimension | Location | Finding | Recommended Action | Owner |
|---|-------|-----------|----------|---------|---------------------|-------|
| 1 | F08 | Freshness | `skills/{hook-crafting,likec4-querying,cicd,mcp-crafting,communicating-agents}/` | 25 `staleness_sensitive_sections` markers across 5 skills are 65-90 days old against their declared 60-day `staleness_threshold_days` (WARN band; none exceed the 120-day FAIL threshold). A prior sentinel pass's marker-matching script had a bug (matched the frontmatter *declaration* line instead of the actual heading, producing false "NO_MARKER_FOUND" noise) — corrected this run. | Refresh the 5 skills' volatile sections (MCP SDK versions, GH Actions marketplace names, hook event reference) and update `<!-- last-verified: -->` dates, or relax `staleness_threshold_days` if 60 days is too aggressive for the content's actual volatility. | context-engineer |
| 2 | X07/C | Cross-Reference | `commands/README.md` | 43 filesystem commands vs 42 cataloged — `refresh-claude-blocks` is missing from the catalog table. | Add the missing row (recurring drift pattern; consider a pre-commit catalog-sync check per the pattern already used for canonical blocks). | doc-engineer |
| 3 | S1 | Ecosystem Coherence | `docs/diagrams/architecture/rendered/agent_pipeline.svg` | The pipeline SVG diagram still has no `agentic-transactions-architect` node (carry-forward since the agent was added 2026-06-02). | Regenerate the diagram source to include the 16th agent. | doc-engineer |
| 4 | S2 | Freshness | `skills/agentic-transactions/references/robinhood.md` | `## Volatile Specifics` section (verified 2026-06-02) has no explicit MCP/SDK version pin, despite the section's purpose being to track volatile provider facts. | Add an explicit version-pin line when the Robinhood MCP server version is next confirmed. | context-engineer |
| 5 | SH01 | Spec Health | `.ai-state/specs/SPEC_multi-language-support_2026-05-11.md:81` | References `rules/swe/coding-style-typescript.md`, which was consolidated into `skills/typescript-development/contexts/typescript.md` in commit `57013be` (2026-05-13). Archived-spec narration, low impact. | No action required (archived specs are historical snapshots); optionally annotate with a superseding pointer if the spec is ever re-read for onboarding. | user (informational) |

### Pipeline Discipline

Task Chronograph MCP tools were not invoked this pass (not required for a standalone audit). P06 and P08 (both scripted, no Chronograph dependency) ran successfully — see Important finding #2 for P06; P08 (`clean_work_safety.py`) reports `stale_safe: 0` (below the advisory threshold of 3), so no `/clean-work` advisory this run despite 2 BLOCK + 2 WARN classified task directories (those are freshness/safety concerns, not staleness — `clean_work_safety.py`'s BLOCK/WARN reasons were `unmerged-learnings` and `unarchived-traceability`, worth a look independent of P08's advisory gate). P07 skipped — no `INTERFACE_DESIGN.md`/`TRANSACTIONS_DESIGN.md` present in any active `.ai-work/` slug.

### Tech-Debt Findings

Two new rows filed this run after LLM-judgment gating against `.ai-state/metrics_reports/METRICS_REPORT_2026-07-29_22-11-31.md` (fresh; generated same session, well within the 14-day staleness window):

- **td-058** (important, `complexity`) — `codex/config/export-codex-rules-bridge.py`: rank-3 hotspot (score 39,842; complexity 22, churn 1,811/90d) and, more decisively, **1,435 lines** — nearly double the 800-line ceiling in `rules/swe/coding-style.md`. Filed because it crosses a hard, objective threshold (not merely "a high p95 file") and no existing ledger row covers it. Same class precedent as td-012/td-054.
- **td-059** (important, `coverage-gap`) — `scripts/aac_fence_validator.py` (31%) + `scripts/spec_drift.py` (16%): both well under the 70% project coverage floor. Elevated to `important` rather than mechanically filing at `suggested` because both scripts back live sentinel gates (AC10, SH07 respectively) — per `rules/swe/gate-liveness.md`, a thinly-tested detector risks silently false-PASSing the defects it exists to catch. Four other scripts sit modestly under floor (56-66%) but were deliberately **not** filed — routine utility scripts, not gate-critical, to avoid ledger noise per the TD dimension's explicit anti-flood guidance.

Not filed: TD02 (`pydeps.cyclic_sccs` is `None` — no cycles); a broader TD04 sweep (many functions cross the repo's `ccn_p95=7.0`, but the top offender, `export-codex-rules-bridge.py`, is already captured by td-058's `complexity` class — filing a second row for the same file would be redundant, not additive).

TD05 ledger-discipline audit: clean. 5 pre-existing active rows (td-012, td-037, td-054, td-056, td-057) all carry assigned `owner-role` values and none are `in-flight` past 30 days (all are `open`, not `in-flight`). No `resolved` rows in `TECH_DEBT_RESOLVED.md` are missing `resolved-by`.

Full row detail: `.ai-state/TECH_DEBT_LEDGER.md`.

### Recommended Actions (prioritized)

1. **(Important)** Backfill or reconcile the 7 uncalibrated pipeline commits in `.ai-state/calibration_log.md` — CA03. Owner: user/orchestrator.
2. **(Important)** Resolve the 3 `.ai-work/` slugs missing `TASK_BRIEF.md` (backfill or `/clean-work`) — P06. Owner: user/context-engineer.
3. **(Tech Debt)** Plan a module-split for `codex/config/export-codex-rules-bridge.py` (1,435 lines) — td-058. Owner: implementation-planner.
4. **(Tech Debt)** Raise test coverage on `scripts/aac_fence_validator.py` and `scripts/spec_drift.py` — both back live sentinel gates — td-059. Owner: test-engineer.
5. **(Suggested)** Refresh the 5 skills with aging staleness markers (F08) — context-engineer.
6. **(Suggested)** Add `refresh-claude-blocks` to `commands/README.md`; add `agentic-transactions-architect` to the pipeline SVG — doc-engineer.

### Check Notes (spot-checks not otherwise itemized above)

- **T03**: `agents/sentinel.md` (611) and `agents/verifier.md` (612) sit in the gate-encoding exception WARN band (550/700) — expected, not flagged. `implementation-planner.md` (526), `systems-architect.md` (470), `context-engineer.md` (358) exceed the standard-agent 300-line WARN (`implementation-planner.md` is over the 500-line FAIL threshold too) — carry-forward, unchanged since 2026-07-15, consistent with every prior audit back to at least 2026-06-17; not re-flagged as a fresh finding per established precedent (a considered, stable pattern, not a regression).
- **T01**: `skills/skill-crafting/SKILL.md` (418 lines) remains just over the 400-line WARN. Stable carry-forward.
- **T04**: no reference file sampled exceeds the 800-line ceiling.
