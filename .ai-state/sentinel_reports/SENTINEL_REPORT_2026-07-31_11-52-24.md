# Sentinel Report

**Vantage**: worktree `/Users/fperez/dev/praxion/.claude/worktrees/td-081-sealed-prior`, branch `worktree-td-081-sealed-prior`, HEAD `f3ba868`, base `1573c44` (= `v0.19.0` = `main`). This report's `.ai-state/sentinel_reports/` is worktree-scoped and distinct from the canonical checkout's.

**Scope**: Full ten-dimension ecosystem sweep, weighted toward the td-081 "sealed-prior" multidisciplinary-consult initiative's touched artifacts: `agents/discipline-consultant.md`, `agents/CLAUDE.md`, `commands/consult.md`, `skills/multi-perspective-analysis/**`, `skills/applied-statistics/**`, `skills/evidence-appraisal/**`, `skills/software-planning/references/coordination-details.md`, `rules/swe/gate-liveness.md`, `rules/swe/agent-intermediate-documents.md`, `rules/swe/swe-agent-coordination-protocol.md`, `fitness/**`, `.ai-state/CONSULT_*.md`. Gate Liveness (GL) given full weight per request — GL02 detector run, GL01/GL03 exercised as LLM judgment across the listed artifacts. P07 run against the live consult fragment `.ai-work/td-081-sealed-prior/CONSULT_statistician.md`. Dimensions outside this weighting (agents/skills/rules not touched by this wave) were spot-checked at baseline depth only (token budget, plugin registration, DL/SH/CA/RD gating scripts) rather than re-audited exhaustively — this is a scoped, not a from-scratch, sweep.

**Lens independence**: this audit did not read any `.ai-work/verify-multidisciplinary/VERIFICATION_*.md` file, per instruction. It did read `.ai-work/verify-multidisciplinary/GATE_INVENTORY.md` (permitted) and independently re-derived every claim taken from it (CI path-filter contents, `pyproject.toml` testpaths, fitness test count) rather than trusting it at face value.

## Ecosystem Health: B

### Summary

The td-081 sealed-prior mechanism is **substantively sound and live**: `scripts/check_gate_liveness.py` (GL02) returns zero findings; the full `fitness/tests/test_discipline_registry_invariants.py` + sibling meta-gates suite (100 tests: 17 check functions, canaries, controls, real-file tests) passes clean against the actual repo state; the live consult fragment (`CONSULT_statistician.md`, 7 challenges) is fully dispositioned with a verified seal-witness chain (`Round-0 HEAD` in the fragment matches the `seal-witness` column in `CONSULT_PRIORS.md § Challenge Classification`, byte-for-byte). P07's golden bad-case fixtures for the `CONSULT_*.md` variant exist on disk and match the catalog's documented shape.

Two **Important** findings survive independent verification, both narrow and mechanical rather than conceptual — the reasoning throughout this initiative is unusually rigorous (see Synthesis); the defects are in a CI trigger list and a single cross-reference pointer, not in the design:

1. A **scope-fidelity gap** (the exact clause this wave added to `gate-liveness.md`) between `fitness/tests/test_discipline_registry_invariants.py`'s documented policing scope and `architecture.yml`'s actual trigger `paths:` — five of the files this initiative's checks exist to police can be edited without ever running the fitness suite in CI.
2. A **rationale-ref miscitation** in `.ai-state/CONSULT_LEDGER.md`'s newest row (`td-081-sealed-prior`/CH-05) — pointing at an ADR that never discusses that challenge, when the correct durable home (`td-084`) already exists and is correctly written.

No Critical findings. Two Suggested findings (cross-reference asymmetry between sibling discipline skills; a documentation-locality gap on `CONSULT_COSTS.md`'s named-consumer story). Token budget: 8.8% headroom (22,790 / 25,000 est. tokens) — healthy, and markedly improved from this repo's historical low (3.4%, 2026-06-11 baseline in the canonical checkout).

**Comparison to last run**: this worktree's own `SENTINEL_LOG.md` shows the prior entry at 2026-07-30 11:03:40 (Health A, 618 artifacts, 0/1/3). This run is scoped and weighted rather than a full sweep, so the artifact count and finding mix are not directly comparable — the drop from A to B reflects two new Important findings surfaced by this run's GL-focused depth on a specific initiative, not a regression across the ecosystem at large.

### Ecosystem Coherence: A

**System-level EC checks:**
| Check | Result | Evidence |
|-------|--------|----------|
| EC01 | PASS | `agents/diagrams/agent-pipeline-flowchart/rendered/agent-pipeline-flowchart.svg` text content includes `discipline-consultant`, `agentic-transactions-architect`, and `interface-designer` — all three sub-architect peers present; `agents/README.md` prose also names all three |
| EC04 | PASS | `CONSULT_<discipline>.md` → convener disposition → `CONSULT_LEDGER.md` (11-col) + `CONSULT_COSTS.md` (8-col) + `CONSULT_PRIORS.md § Challenge Classification` (9-col) — every stage of the handoff chain has a confirmed producer and reader; verified live on the `td-081-sealed-prior` triple (seal-witness sha `cd12803...` agrees across the fragment and the classification row) |
| EC05 | PASS | No structural gaps found in the scoped initiative — registry, agent, command, two skills, coordination protocol doc, and ledger trio are all present and mutually consistent |
| EC07 | PASS | `python3 scripts/check_aac_golden_rule.py --mode=audit --json` → `[]` (0 findings) |

**Cross-dimension anomalies:** None spanning multiple artifacts. Both Important findings are narrow and artifact-local (one CI config, one ledger row) rather than a pattern recurring across many artifacts — see Synthesis.

**Artifact coherence distribution:** Of 14 initiative artifacts assessed at read-depth: 10 A (`discipline-consultant.md`, `agents/CLAUDE.md`, `commands/consult.md`, `discipline-registry.md`, `multi-perspective-analysis/SKILL.md`, `evidence-appraisal/SKILL.md`, `coordination-details.md` §Discipline-Consultant Dialogue Protocol, `gate-liveness.md`, `CONSULT_PRIORS.md`, `fitness/tests/test_discipline_registry_invariants.py` + sibling meta-gates), 2 B (`applied-statistics/SKILL.md`, `CONSULT_COSTS.md` — one Suggested finding each), 2 C (`CONSULT_LEDGER.md`, `.github/workflows/architecture.yml` — one Important finding each). No D or F.

**Synthesis:** This initiative's self-referential discipline is genuinely unusual — the ledger schema documents its own past failure mode ("This ledger's first six rows made exactly that mistake and were superseded by replacement rows") and the exact same failure class recurs once more in the very newest row (finding #2), which is itself evidence the mechanism catches real instances rather than a hypothetical risk. Likewise, `gate-liveness.md`'s own anti-pattern table already names this initiative's multi-site convention risk verbatim ("the discipline-consultant's rules span `agents/discipline-consultant.md`, `agents/CLAUDE.md`, and `skills/software-planning/references/coordination-details.md`") — and finding #1 is the CI-trigger-scope instance of exactly that risk, one layer down (a fourth site — the CI config — that the anti-pattern table doesn't yet enumerate). The system works as designed everywhere I traced a mechanism end-to-end (seal → challenge → classify → disposition → cost, and the fitness suite that enforces the shape of each). The friction is at the boundary between "the mechanism is correct" and "the mechanism is reliably *invoked*" — both findings are instances of a sound gate whose actual reach (CI trigger scope; a specific pointer) is narrower than its claimed reach, which is precisely gate-liveness clause 5's target class.

### Ecosystem Metrics

| Metric | Value | Trend |
|--------|-------|-------|
| Fitness suite (discipline invariants + meta-gates) | 100/100 passed | new this wave (17 check functions, up from 10 pre-td-081) |
| GL02 detector findings | 0 | clean |
| P07 live fragment (`CONSULT_statistician.md`) | 7/7 challenges dispositioned | clean |
| Token budget (project+global CLAUDE.md + unconditional rules) | 22,790 est. tokens / 25,000 (8.8% headroom) | improving vs. 3.4% low-water mark (2026-06-11, canonical checkout) |
| Finalized ADRs | 309 | DL03 index-row count matches file count exactly |
| Draft ADRs | 1 (`dec-draft-2c51b2f6`) | in-flight, this task |
| P06 / P08 / EC07 / CA03 / SH08 / RD01 gating scripts | all clean | see Findings-adjacent auto-check table below |

### Scorecard

| Artifact | Type | Complete (C) | Consistent (N) | Fresh (F) | Spec (S) | Cross-Ref (X) | Tokens (T) | Coherence (EC) | Overall |
|----------|------|--------------|-----------------|-----------|----------|----------------|------------|----------------|---------|
| `agents/discipline-consultant.md` | Agent | A | A | A | A | A | A (183 ln) | A | **A** |
| `agents/CLAUDE.md` | Config | A | A | A | A | A | A | A | **A** |
| `commands/consult.md` | Command | A | A | A | A | A | A | A | **A** |
| `skills/multi-perspective-analysis/SKILL.md` | Skill | A | A | A | A | A | A (82 ln) | A | **A** |
| `skills/multi-perspective-analysis/references/discipline-registry.md` | Reference | A | A | A | A | A | A (34 ln) | A | **A** |
| `skills/applied-statistics/SKILL.md` | Skill | A | A | A | A | B (Suggested #3) | A (106 ln) | B | **B** |
| `skills/evidence-appraisal/SKILL.md` | Skill | A | A | A | A | A | A (81 ln) | A | **A** |
| `skills/software-planning/references/coordination-details.md` (§ Discipline-Consultant Dialogue Protocol) | Reference | A | A | A | A | A | A | A | **A** |
| `rules/swe/gate-liveness.md` | Rule | A | A | A | A | A | A | A | **A** |
| `.ai-state/CONSULT_LEDGER.md` | Persistent data | A | B (Important #2) | A | A | C | — | B | **C** |
| `.ai-state/CONSULT_PRIORS.md` | Persistent data | A | A | A | A | A | — | A | **A** |
| `.ai-state/CONSULT_COSTS.md` | Persistent data | A | A | A | B (Suggested #4) | A | — | B | **B** |
| `fitness/tests/test_discipline_registry_invariants.py` + siblings | Fitness (CODE gates) | A | A | A | A | A | — | A | **A** |
| `.github/workflows/architecture.yml` | CI config | A | A | B | A | C (Important #1) | — | B | **C** |

### Findings

#### Critical (blocks correct behavior)
None found.

#### Important (degrades quality or efficiency)

| # | Check | Dimension | Location | Finding | Recommended Action | Owner |
|---|-------|-----------|----------|---------|---------------------|-------|
| 1 | GL (clause 5, scope fidelity) | Gate Liveness | `.github/workflows/architecture.yml` lines ~26-34 (`paths:` filter) | The only CI job that runs `fitness/tests/` (`architecture.yml` → `fitness-functions`) is gated on a `paths:` filter (`docs/diagrams/**`, `**/*.c4`, `**/DESIGN.md`, `docs/architecture.md`, `.ai-state/decisions/**`, `.ai-state/CONSULT_*.md`, `fitness/**`, `scripts/aac_fence_validator.py`, `scripts/check_aac_golden_rule.py`) that omits `skills/multi-perspective-analysis/references/discipline-registry.md`, `agents/discipline-consultant.md`, `agents/CLAUDE.md`, `commands/consult.md`, and `skills/software-planning/references/coordination-details.md`. Independently confirmed: `pyproject.toml:79 testpaths = ["tests","scripts"]` excludes `fitness/`, and `test.yml`'s two pytest invocations (`--cov-fail-under=80` default testpaths; explicit `hooks/`) never touch it either — `architecture.yml` really is the sole path. Checks 1 (`check_no_discipline_name_in_text`), 2 (`check_registry_row_shape`), 3 (`check_ledger_exists_if_registry_has_rows`), 5 (`check_fires_when_is_restrictive`), 6 (`check_documents_fail_loud_resolution`) police exactly the omitted files. Concretely: a PR that only adds a discipline-registry row (the documented common case — "one row here, plus at most one new skill file", `discipline-registry.md` § Adding a Discipline) or only edits `commands/consult.md`/`agents/CLAUDE.md`/`agents/discipline-consultant.md` triggers **zero** fitness jobs and can land a malformed row, a `fires-when` predicate that always fires, or a documented-vs-actual `[BLOCKED]` drift undetected until some unrelated later PR happens to also touch `fitness/**`. | Widen `architecture.yml`'s `paths:` to include the five omitted files (or adopt a general rule: any file inside a `check_*` function's asserted/documented scope belongs in the trigger list). Consider a companion fitness/CI meta-check that diffs `test_discipline_registry_invariants.py`'s `affected_files`-style claims against the workflow's `paths:` list, closing the loop mechanically rather than relying on the next sentinel pass. | cicd-engineer |
| 2 | P07-adjacent (ledger data integrity / durable-home rule) | Pipeline Discipline | `.ai-state/CONSULT_LEDGER.md` row `2026-07-31T06:10:00Z \| td-081-sealed-prior \| statistician \| architecture \| CH-05` | CH-05's disposition is `defer-with-rationale` with `rationale-ref: dec-draft-2c51b2f6`. That draft ADR (`.ai-state/decisions/drafts/20260731-0433-fperezsorrosal-worktree-td-081-sealed-prior-sealed-consult-prior.md`) mentions `CH-06` four times and mentions neither `CH-05` nor `td-084` anywhere. The actual durable home is `.ai-state/TECH_DEBT_LEDGER.md` row `td-084`, opened the same day, which explicitly states "[statistician consult CH-05 on td-081, dispositioned defer-with-rationale, dec-draft-2c51b2f6]" and fully documents the residual risk (between-run/spawn variance is unrecordable under the current schema) and its trigger condition — exactly the "tech-debt row when residual risk remains" durable home `CONSULT_LEDGER.md`'s own Column Definitions section prescribes for `defer-with-rationale`. This is the identical failure class the file's own header already documents as a past mistake ("This ledger's first six rows made exactly that mistake and were superseded by replacement rows") recurring in the newest row. A reader following CH-05's `rationale-ref` today does not find the reasoning. | Append a correcting row for the CH-05 triple with `rationale-ref: td-084` (ledger is append-only — supersede, don't edit in place, per the file's own convention for revisited dispositions). Consider a canary that, for `defer-with-rationale` rows whose `rationale-ref` is a `dec-*` id, greps the target document for the row's `challenge-id` as a weak semantic-correctness check. | systems-architect (data owner of this pipeline's ledger rows) / context-engineer (mechanical fix + canary proposal) |

#### Suggested (improves but not urgent)

| # | Check | Dimension | Location | Finding | Recommended Action | Owner |
|---|-------|-----------|----------|---------|---------------------|-------|
| 3 | EC03 (bidirectional collaboration refs) | Cross-Reference / Ecosystem Coherence | `skills/applied-statistics/SKILL.md` § Related Skills | `evidence-appraisal/SKILL.md` explicitly positions itself against `applied-statistics` in its own description ("Distinct from applied-statistics, which audits inference on our own numbers") and Content-boundary prose, and lists `applied-statistics` in its Related Skills. `applied-statistics/SKILL.md` does not reciprocate — its Related Skills section (agent-evals, performance-architecture, llm-training-eval, experiment-tracking, testing-strategy, observability) omits `evidence-appraisal` entirely, despite the two being sibling disciplines in the same registry with adjacent, easily-confused scope (own-data inference vs. imported-claim appraisal). | Add an `evidence-appraisal` entry to `applied-statistics/SKILL.md`'s Related Skills list, mirroring the existing one-line boundary statement from the other direction. | context-engineer |
| 4 | GL (clause 6, documentation locality) | Gate Liveness | `.ai-state/CONSULT_COSTS.md` | `CONSULT_PRIORS.md` carries an explicit `## Named consumer` section binding its computed value (the novelty rate) to `dec-306` and to any future ADR re-opening that criterion. Its sibling `CONSULT_COSTS.md` computes an equally load-bearing raw observation (tokens/model/difficulty, feeding the `AC15` cost-envelope criterion) but has no equivalent section. This is **not a liveness failure** — the consumer chain is real and independently traceable (`SPEC_multidisciplinary_identities_2026-07-30.md` § AC15, `td-071`/`td-076`, `dec-308`, and this very verification wave's `.ai-state/VERIFICATION_PLAN_multidisciplinary.md:126` which explicitly instructs re-deriving `AC15`) — but a reader who opens only `CONSULT_COSTS.md` has no pointer to any of that. | Add a short `## Named consumer` section to `CONSULT_COSTS.md` mirroring `CONSULT_PRIORS.md`'s, pointing at `dec-308` and the `AC15` criterion. | context-engineer |

**Auto-checks run clean (no findings, listed for audit-trail completeness):** GL02 (`scripts/check_gate_liveness.py --json` → 0 findings) · P06 (`scripts/check_p06_task_brief.py --json` → `[]`) · P08 (`scripts/clean_work_safety.py --json` → `stale_safe: 0`) · EC07 (`scripts/check_aac_golden_rule.py --mode=audit --json` → `[]`) · CA03 (`covered: true`, 0 uncalibrated commits) · SH08 (`gap: false`) · RD01 (`below_threshold: false`, readiness level 4 vs. floor 3) · DL03 (309 finalized ADR files = 309 `DECISIONS_INDEX.md` rows) · DL04/DL06 (`dec-306`'s currently-empty `re_affirmed_by` against the in-flight draft's `re_affirms: dec-306` is the **expected pre-finalize state**, not a violation — `scripts/finalize_adrs.py::backfill_re_affirmed_by` self-heals this reciprocal link at finalize time, confirmed by reading the function) · F02 (all 13 reference files across the three scoped skills exist; largest is 230 lines, well under the 800-line T04 ceiling) · `.ai-state/decisions/drafts/.finalize.lock` (14-hour-old mtime is **not** a stale-lock hazard — `acquire_lock()` uses `fcntl.flock`, which is process-scoped and auto-releases on exit; the file persisting on disk after use is by design, confirmed by reading `scripts/finalize_adrs.py:934-947`).

### Pipeline Discipline

- **P06** (TASK_BRIEF mandatory at Standard/Full): clean, `[]`.
- **P07** (undisposed challenges): the live `.ai-work/td-081-sealed-prior/CONSULT_statistician.md` fragment carries 7 `### CH-NN` entries, all 7 with a non-empty, non-placeholder `**Disposition:**` before the next heading (6× `switch-now`, 1× `defer-with-rationale`) — clean. The golden bad-case fixtures for this check's `CONSULT_*.md` variant (`tests/fixtures/sentinel/consult_no_disposition/CONSULT_statistician.md` with an undispositioned `### CH-01`, and the no-false-positive control `CONSULT_statistician_no_challenge.md`) both exist on disk and match the catalog's documented shape.
- **P08** (stale `.ai-work/` slugs): `stale_safe: 0`, below the advisory threshold of 3.
- No Task Chronograph data queried this pass (out of the requested GL/P07-weighted scope); P01-P05 skipped with this note.

### Tech-Debt Findings

No new `td-NNN` rows filed this run. The latest `METRICS_REPORT_2026-07-29_22-11-31.md` (1 day old, well under the 14-day staleness threshold) shows no hotspots implicating the scoped initiative's files. TD05 (ledger status-update discipline): the initiative's own tech-debt rows (`td-071`, `td-076`, `td-081`, `td-083`, `td-084`) are all 0-2 days old — none trip the 30-day in-flight or 7-day unassigned-owner thresholds. `td-081`'s `status: resolved` row sitting in the active `TECH_DEBT_LEDGER.md` (rather than `TECH_DEBT_RESOLVED.md`) is the **expected pre-merge state** — migration happens at `scripts/finalize_tech_debt_ledger.py`, invoked post-merge, and this worktree has not merged. Findings #1 and #2 above are reported as sentinel findings rather than filed as new `td-NNN` rows because both are immediately actionable, narrowly-scoped, mechanical corrections (a CI `paths:` edit; a ledger row `rationale-ref` correction) rather than accruing debt — see `.ai-state/TECH_DEBT_LEDGER.md` for the canonical entries if the owning agent prefers to track either as a row instead of a direct fix.

### Recommended Actions (prioritized)

1. **[Important #1]** Widen `.github/workflows/architecture.yml`'s `paths:` filter to include `skills/multi-perspective-analysis/references/discipline-registry.md`, `agents/discipline-consultant.md`, `agents/CLAUDE.md`, `commands/consult.md`, `skills/software-planning/references/coordination-details.md`. Owner: cicd-engineer.
2. **[Important #2]** Append a superseding `CONSULT_LEDGER.md` row for `td-081-sealed-prior`/CH-05 with `rationale-ref: td-084`. Owner: systems-architect / context-engineer.
3. **[Suggested #3]** Add `evidence-appraisal` to `applied-statistics/SKILL.md`'s Related Skills. Owner: context-engineer.
4. **[Suggested #4]** Add a `## Named consumer` section to `CONSULT_COSTS.md`. Owner: context-engineer.
5. Consider (not filed as a finding — a process suggestion): a fitness/CI meta-check that diffs each `check_*` function's asserted file scope in `test_discipline_registry_invariants.py` against `architecture.yml`'s `paths:` list, so a future scope drift of this initiative (finding #1's class) fails loudly instead of waiting for the next sentinel pass.
