# Sentinel Report

**Generated**: 2026-08-04 13:35:55
**HEAD**: `361e3dc`
**Scope**: Full ecosystem sweep — all dimensions, Pass 1 (automated) + Pass 2 (LLM judgment)
**Note**: First live exercise of the Decision Health (DH) dimension, added in `361e3dc`.

## Ecosystem Health: C

### Summary

The ecosystem is structurally sound and its enforcement machinery is live. Every registration surface is
consistent (plugin.json ↔ `agents/` 17/17, coordination-protocol table 17/17, `agents/README.md` 17/17,
`rules/README.md` 23/23, `DECISIONS_INDEX.md` ↔ finalized ADRs 316/316). All three delegated detectors that
have historically been the most fragile — `check_gate_liveness.py`, `check_aac_golden_rule.py`,
`aac_fence_validator.py` — return clean. Architecture markdown passes fence validation on both surfaces,
and all 137 inline `dec-NNN` citations across `DESIGN.md` and `docs/architecture.md` resolve.

What pulls the grade to C is six Important findings, none Critical. Two are newly surfaced by checks that
had never run against this corpus: **DH01** reconstructs 13 missing decision-graph edges, and **DL02** —
run with a real YAML parser rather than a tolerant regex — finds 13 of 316 finalized ADRs whose frontmatter
raises on `yaml.safe_load()`. The remaining four are a tightening token budget (2.1% headroom, narrowest on
record), 12 undispositioned specialist challenges left in `.ai-work/`, two live sentinel gates backed by
code at 15–31% coverage, and a self-contradiction inside this agent's own definition.

Compared to the 2026-06-11 baseline (Health B, 0/1/4): artifact count grew from 617 to ~740 (+58→58 skills
is flat; ADRs 224→316, commands 41→44, specs 5→8). The prior run's single Important finding (token budget)
persists and worsened; the three prior Suggested catalog gaps were resolved and two new ones appeared.

### Ecosystem Coherence: B

**System-level EC checks:**

| Check | Result | Evidence |
|-------|--------|----------|
| EC01 | PASS | Every one of the 17 agent files appears in an `agents/README.md` diagram block; no diagram-only or file-only names |
| EC04 | PASS | All pipeline handoff docs have both a producing and a consuming instruction; GL01 spot-check of 6 named sections (`Behavior Preservation Contract`, `Verifier Bypass Criteria`, `Loop-Back Conditions`, `Resolved Tech Debt`, `Health Guards`, `Uncertainty Flag`) found a producer for each — `systems-architect` for the PRE_REFACTOR_PLAN sections, `goal-disambiguation` for the TASK_BRIEF fields |
| EC05 | PASS | No structural gaps for stated purpose; the artifact classes CLAUDE.md advertises all exist and are populated |
| EC07 | PASS | `check_aac_golden_rule.py --mode=audit --json` → `[]`, exit 0 |

**Cross-dimension anomalies:**

1. **Systemic ADR reference decay.** DH scanned 1,432 `affected_files` references across 316 ADRs and
   classified 127 (8.9%) as decayed in some way. This is not visible to any other dimension — X and F check
   whether *live* documents' references resolve, and the ADR corpus is not a live document in that sense.
   The decay is concentrated in two migrations (`.ai-state/ARCHITECTURE.md` → `DESIGN.md`, and the
   memory-subsystem removal) whose mechanical path updates never swept the decision corpus.
2. **Disposition debt at the specialist seam.** `.ai-work/wave1-gap-closure/` holds two CONSULT fragments
   with 12 challenges whose `Disposition` fields are still convener placeholders. The `CONSULT_LEDGER.md`
   shows the *previous* slug (`verify-multidisciplinary`) was fully dispositioned — so the protocol works,
   but this instance stalled. Because `.ai-work/` is ephemeral, the reasoning in those 12 challenges is
   lost at cleanup unless dispositioned first.

**Artifact coherence distribution:** healthy — 58 skills / 17 agents / 23 rules / 44 commands all pass
their per-artifact structural checks; grades cluster at A and B with no artifact class below B.

**Synthesis:** The connective tissue works. Registration, cataloging, and diagram surfaces are in sync — the
class of drift that dominated every audit through 2026-04 is now largely mechanized away. The friction has
moved to *content decay inside long-lived corpora* (the ADR set) and to *unfinished protocol handoffs*
(undispositioned challenges), neither of which a registration check can see. Grade is B rather than A
solely on account of those two cross-dimension anomalies.

### Ecosystem Metrics

| Metric | Value | Trend |
|--------|-------|-------|
| Skills | 58 | ↑ from 51 (2026-06-11) |
| Agents | 17 | stable |
| Rules (always-on / path-scoped) | 7 / 17 | stable |
| Commands | 44 | ↑ from 41 |
| Hooks | 41 | — |
| Finalized ADRs / drafts | 316 / 0 | ↑ from 224 |
| Archived specs | 8 (+ index) | ↑ from 5 |
| Always-loaded chars | 85,690 | ↑ from 84,562 — degrading |
| Always-loaded tokens (chars/3.5) | 24,483 (2.1% headroom) | ↓ from 3.4% — degrading |
| Always-loaded tokens (chars/3.6, `rules/CLAUDE.md` basis) | 23,803 (4.8% headroom) | ↓ degrading |
| Tech-debt ledger (active / resolved) | 39 / 72 | — |
| Staleness sections tracked / current | 91 / 64 | 27 past threshold |
| ADR references scanned (DH) | 1,432 | first measurement |
| Findings (Critical / Important / Suggested) | 0 / 6 / 9 | ↑ from 0/1/4 |

### Scorecard

Grouped by artifact class. Per-artifact detail is in Findings.

| Artifact class | Type | Complete (C) | Consistent (N) | Fresh (F) | Spec (S) | Cross-Ref (X) | Tokens (T) | Coherence (EC) | Overall |
|----------------|------|--------------|----------------|-----------|----------|---------------|------------|----------------|---------|
| Skills (58) | skill | A | A | B (F08 ×27) | A | A | B (T01: 1@418) | B | **B** |
| Agents (17) | agent | A | A | A | A | A | B (T03: 8 over warn) | A | **B** |
| Rules (23) | rule | A | A | A | A | A | C (T02 2.1%) | A | **B** |
| Commands (44) | command | A | A | A | A | B (2 uncataloged) | A | A | **B** |
| ADRs (316) | decision | A | B (DL02 ×13) | C (DH ×36 actionable) | C (DL02 ×13) | A (DL03 316/316) | A | B | **C** |
| Specs (8) | spec | A | A | A | A | A | A | A | **A** |
| Architecture docs (2) | arch | A | A | A | A | A (AC04 137/137) | A | A | **A** |
| Sentinel (self) | agent | A | C (self-contradiction) | A | A | A (V01–V04) | B (628 ln) | A | **C** |

### Findings

#### Critical (blocks correct behavior)

None.

#### Important (degrades quality or efficiency)

| # | Check | Dimension | Location | Finding | Recommended Action | Owner |
|---|-------|-----------|----------|---------|--------------------|-------|
| I1 | DL02 | Decision Log | `.ai-state/decisions/` (13 files) | 13 of 316 finalized ADRs have frontmatter that raises on `yaml.safe_load()`. Two root causes: (a) a scalar value beginning with a backtick — YAML rejects `` ` `` as a token start (e.g. `032`: ``title: `ROADMAP.md` at project root as …``); (b) an unquoted `key: value` pair inside a scalar, parsed as a nested mapping (e.g. `294`: ``summary: … `issues: write` across the whole fleet``). Affected: `032`, `046`, `057`, `077`, `111`, `129`, `152`, `153`, `168`, `169`, `190`, `224`, `294`. DL03 passes (index 316/316) because the index generator uses a tolerant parser — which is exactly what masked this for 316 ADRs' worth of accumulation | Quote the offending scalar values (`title: "\`ROADMAP.md\` at …"`). Then add a YAML-parse assertion to the ADR authoring/finalize path so the next one fails at write time, not at read time | context-engineer |
| I2 | P07 | Pipeline Discipline | `.ai-work/wave1-gap-closure/CONSULT_statistician.md`, `CONSULT_evidence-appraiser.md` | 12 `### CH-NN` challenges carry undispositioned placeholders — statistician CH-01…CH-06 (`\| CH-01 \| <!-- convener --> \| <!-- convener --> \|`) and evidence-appraiser CH-01…CH-06 (`**Disposition:** <!-- convener, Round 2 -->`). The convener never adjudicated. `.ai-work/` is ephemeral, so this reasoning is lost at cleanup; `CONSULT_LEDGER.md` has 55 rows but none for this slug | Disposition all 12 per `disposition-vocabulary.md` and append one `CONSULT_LEDGER.md` row each. Per the coordination protocol, each disposition needs a durable home: `switch-now` → ADR, `defer-with-rationale` → tech-debt row, `dismiss-with-rationale` → `wontfix` tombstone | user |
| I3 | T02 | Token Efficiency | `CLAUDE.md` + 7 always-on rules | Always-loaded surface is 85,690 chars = **24,483 tokens at chars/3.5 (2.1% headroom)**, or 23,803 at the `rules/CLAUDE.md` chars/3.6 basis (4.8%). Narrowest on record; was 3.4% on 2026-06-11 and 8.8% on 2026-05-24. `rules/swe/swe-agent-coordination-protocol.md` alone is 26,216 chars — 31% of the entire budget. Basis: project + global `CLAUDE.md` and every `rules/**/*.md` lacking `paths:`, excluding catalog `README.md` files | Extract from the coordination protocol to path-scoped or skill content. The `### Available Agents` table (17 rows with Output/Bg-Safe columns) and the `Pipeline rules` table are the two largest extractable blocks; both are referenced far less than 30% of sessions | context-engineer |
| I4 | DH01 | Decision Health | `.ai-state/decisions/` (13 refs) | 13 decisions name an `affected_files` path that a **later** decision deleted, with no `supersedes`/`superseded_by` link between them. These are decision-graph edges that exist in reality but were never written — the honest explanation for a low supersession rate. Full table in the Decision Health section below | For each, confirm which candidate remover actually did it, then record the supersession link on both sides (DL06 reciprocity). Highest-value DH output: this *adds* edges rather than pruning nodes | systems-architect |
| I5 | TD03 | Technical Debt | `scripts/spec_drift.py`, `scripts/aac_fence_validator.py` | Two modules that **back live sentinel gates** are the two least-covered real modules in the project: `spec_drift.py` at **15.5%** (backs SH07) and `aac_fence_validator.py` at **30.7%** (backs AC10 — which this run invoked and trusted). A gate whose implementation is 15% covered is a gate-liveness risk in the `rules/swe/gate-liveness.md` sense: the check runs, exits 0, and nobody knows whether it would bite. Judgment note: the other four sub-floor modules (`check_paths_syntax` 56.3%, `prune_reports` 57.3%, `project_metrics/cli` 59.7%, `self_healing_metrics` 65.6%) are **not** filed — they are near the floor and none gates a correctness claim | Add canary tests asserting each detector FAILs on its documented golden bad-case. `aac_fence_validator.py` first — AC10 consumed its verdict this run | test-engineer |
| I6 | N05 / self-audit | Consistency | `agents/sentinel.md` | The agent definition contradicts itself. The **Technical Debt** dimension states TD01–TD04 "write ledger rows" into `.ai-state/TECH_DEBT_LEDGER.md` and the Report schema's Tech-Debt subsection asks for a "count of new TD rows filed this run" — but **Constraints** says "Never use the Edit tool… your only write targets are `SENTINEL_REPORT_*.md` and `SENTINEL_LOG.md`", and **Boundary Discipline → Mutation** says "no Edit tool, no artifact changes". TT04 and EC07 carry the same row-writing instruction. I resolved this by honoring the read-only constraint and reporting TD candidates below rather than writing rows — registering the objection rather than silently picking a side | Decide which is authoritative. Either (a) narrow TD01–TD04/TT04/EC07 to "surface candidates in the report; the user or context-engineer files rows", or (b) carve an explicit ledger-write exception into Constraints and Boundary Discipline. Option (a) preserves the read-only guarantee that makes this agent safe to run unattended | context-engineer |

#### Suggested (improves but not urgent)

| # | Check | Dimension | Location | Finding | Recommended Action | Owner |
|---|-------|-----------|----------|---------|--------------------|-------|
| S1 | X07 | Cross-Reference | `skills/README.md` | `evidence-appraisal` skill exists on disk but is absent from the catalog (57 of 58 listed) | Add catalog entry | doc-engineer |
| S2 | X07 | Cross-Reference | `commands/README.md` | `consult` and `refresh-claude-blocks` absent from the catalog (42 of 44 listed) | Add two catalog rows | doc-engineer |
| S3 | F08 | Freshness | 7 skills, 27 sections | 27 staleness markers past their declared threshold; none past 2× (no FAIL). Worst: `likec4-querying` 96d/60d (2 sections), `communicating-agents` 95d/60d (9 sections), `cicd`/`hook-crafting`/`mcp-crafting` 71d/60d (14 sections), `agentic-transactions` 63d/60d (2). 64 of 91 tracked sections are current | Re-verify the `communicating-agents` block first (9 sections, oldest cohort) | context-engineer |
| S4 | DH02 | Decision Health | `.ai-state/decisions/` (18 refs) | 18 `renamed` references — mechanical path repair. 10 are `.ai-state/ARCHITECTURE.md` → `.ai-state/DESIGN.md`; the rest are the `new_cc_project.sh` → `new_project.sh` family and `METRICS_LOG.md` relocation. Detail in the Decision Health section | Batch `update-path` rewrite; the detector already names each target | context-engineer |
| S5 | DH02 | Decision Health | `.ai-state/decisions/` (5 refs) | 3 `placeholder-shape` (glob patterns like `.ai-state/METRICS_REPORT_*.json`, `.ai-work/<slug>/traceability.yml`) and 2 `out-of-repo` (`~/.claude/CLAUDE.md`). **Judgment**: these read as *intentional* — a schema ADR legitimately cites a filename shape, and `~/.claude/CLAUDE.md` is a real file the repo deliberately does not contain. Consider whether `affected_files` should admit a shape/external annotation rather than treating these as decay | Either annotate as intentional or teach the detector a shape/external convention | systems-architect |
| S6 | P06 | Pipeline Discipline | `.ai-work/arch-ground-truth/`, `.ai-work/wave1-gap-closure/` | Both slugs have `SYSTEMS_PLAN.md` (Standard/Full implied) but no `TASK_BRIEF.md` — the Intake Clarity Gate's unconditional artifact was not produced | Backfill or accept as historical; the gate has no enforcing hook | user |
| S7 | T01 / T03 | Token Efficiency | `skills/skill-crafting/SKILL.md`, 8 agents | T01: `skill-crafting/SKILL.md` at 418 lines (warn 400); all other skills under. T03: `sentinel.md` 628 and `verifier.md` 598 both exceed even their *raised* exception warn threshold of 550; `systems-architect` 462, `implementation-planner` 445, `context-engineer` 344, `skill-genesis` 336, `promethean` 311, `researcher` 307 exceed the standard 300 warn. None at fail | For `sentinel.md`, the Check Catalog is now ~19 dimensions; consider moving the catalog to a reference file loaded at Pass-1 start — the same progressive disclosure the catalog demands of skills (S05) | context-engineer |
| S8 | TD | Technical Debt | `METRICS_REPORT_2026-07-29_22-11-31.json` | The `coverage.per_file` map contains 3 entries under `/private/var/folders/…/pytest-of-fperez/…` — pytest tmpdir copies of `_repo_root.py` and `finalize_adrs.py` leaking into the coverage artifact. Harmless to the aggregate (78 of 81 entries are real) but pollutes per-module analysis | Add a tmpdir path filter to the coverage collector | cicd-engineer |
| S9 | TD01 | Technical Debt | 3 hotspot paths | Top churn×complexity hotspots have no **active** ledger row: `dashboard_app/src/components/metrics-dashboard.tsx` (score 111,203 — churn 1,823 × ccn 61; 2 *resolved* rows exist, so it was addressed once and re-accumulated), `dashboard_app/src/components/readiness-section.tsx` (47,840; never filed), `hooks/inject_rules.py` (21,638; never filed). Judgment note: `codex/config/export-codex-rules-bridge.py` (rank 3, 39,842) is **not** filed — it already carries an active ledger row | Per I6 I did not file rows. If option (b) is chosen, these three are the warranted TD01 candidates | user |

### Decision Health (DH)

**First live exercise of this dimension.** Substrate present: `.ai-state/decisions/` (316 finalized ADRs,
0 drafts) and `scripts/adr_health.py`. Detector exit 0.

#### `withheld` state — read first

```
"withheld": []
```

**The `withheld` array is EMPTY.** No decay class was suppressed. Both oracles were available:

- **Git history** — full (not a shallow clone), so removal attribution for `removed-by-later` and
  `removed-by-self` could be computed rather than defaulted.
- **Artifact-inventory lifecycle table** — parseable, so `lazy-artifact` classification (artifacts the
  inventory declares expected-absent) resolved rather than falling through to `vanished`.

Consequently the `vanished` count below **is** comparable to a full run. Had either oracle been
unavailable, the dependent classes would have been suppressed and listed here, and the `vanished` residual
would have been inflated by whatever those classes would otherwise have absorbed.

#### Detector totals

1,432 references scanned across 316 ADRs → 127 classified findings (8.9% of references show some decay).

| Class | Count | Sentinel treatment | Disposition |
|---|---|---|---|
| `removed-by-later` | 13 | **DH01 — WARN each** | `link-supersession` |
| `renamed` | 18 | **DH02 — WARN each** | `update-path` |
| `placeholder-shape` | 3 | **DH02 — WARN each** | `fix-entry` |
| `out-of-repo` | 2 | **DH02 — WARN each** | `fix-entry` |
| `vanished` | 55 | **DH03 — INFO advisory only** | `retire-candidate` |
| `removed-by-self` | 33 | **not a finding** — the decision working as intended | `none` |
| `lazy-artifact` | 3 | **not a finding** — inventory declares expected-absent | `none` |

Actionable finding count: **36** (13 DH01 + 23 DH02). Advisory: 55 (DH03). Correctly excluded: 36.

#### DH01 — `removed-by-later` (13 WARN)

A decision names a path that a **later** decision deleted, with no supersession link between them. Each row
is a decision-graph edge that exists in reality but was never written.

| # | ADR | Orphaned path | Removed | Candidate removers (nearest-dated first) |
|---|-----|---------------|---------|------------------------------------------|
| 1 | `011-adr-injection-memory-first-budget` | `hooks/inject_memory.py` | 2026-06-11 | `225-remove-in-house-memory-subsystem`, `073-unified-phase-plan`, `023-adr-first-hook-injection` |
| 2 | `024-ci-test-pipeline` | `memory-mcp/pyproject.toml` | 2026-06-11 | `133-drop-dev-prereleases` |
| 3 | `026-session-count-from-observations` | `memory-mcp/src/memory_mcp/server.py` | 2026-06-11 | `048-observation-span-correlation` |
| 4 | `039-memory-gate-exemption-shared-constant` | `hooks/validate_memory.py` | 2026-06-11 | `225-remove-in-house-memory-subsystem` |
| 5 | `039-memory-gate-exemption-shared-constant` | `hooks/remind_memory.py` | 2026-06-11 | `225-remove-in-house-memory-subsystem` |
| 6 | `041-pyright-over-mypy` | `memory-mcp/pyproject.toml` | 2026-06-11 | `133-drop-dev-prereleases` |
| 7 | `075-praxion-first-class-process` | `hooks/inject_memory.py` | 2026-06-11 | `225-remove-in-house-memory-subsystem`, `073-unified-phase-plan` |
| 8 | `082-extract-canonical-blocks-build-time` | `scripts/git-pre-commit-hook.sh` | 2026-06-20 | `246-unify-commit-gate-precommit`, `096-structurizr-d2-diagrams` |
| 9 | `087-pilot-strategy-trunk-only` | `memory-mcp/pyproject.toml` | 2026-06-11 | `133-drop-dev-prereleases` |
| 10 | `108-aac-golden-rule-enforcement-hook` | `scripts/git-pre-commit-hook.sh` | 2026-06-20 | `246-unify-commit-gate-precommit`, `096-structurizr-d2-diagrams` |
| 11 | `110-aac-dac-v1-1-block-d-hook-slot-correction` | `scripts/git-pre-commit-hook.sh` | 2026-06-20 | `246-unify-commit-gate-precommit`, `096-structurizr-d2-diagrams` |
| 12 | `131-dashboard-sequential-data-then-parallel-pages` | `streamlit_app/data/discovery.py` | 2026-05-12 | `132-rename-architecture-to-design` |
| 13 | `198-shape-b-default-on` | `commands/onboard-project-obsidian.md` | 2026-05-20 | `199-obsidian-skills-marketplace-install`, `196-*` |

**Reading.** Three clusters account for all 13, and each has an obvious true remover the detector ranked
but could not confirm:

- **Memory-subsystem removal (7 rows)** — `dec-225` is almost certainly the real remover for rows 1, 4, 5, 7
  (the `hooks/inject_memory.py`, `validate_memory.py`, `remind_memory.py` deletions), and plausibly for the
  three `memory-mcp/pyproject.toml` rows where the detector's nearest-dated guess (`dec-133`) looks wrong —
  `dec-133` is about dev-prereleases, not memory removal. This matches the CLAUDE.md note that the in-house
  memory subsystem was removed per `dec-225`. **Highest-confidence repair in the set.**
- **Pre-commit hook unification (3 rows)** — `dec-246` unified the commit gate, deleting
  `scripts/git-pre-commit-hook.sh`. Rows 8, 10, 11 should all point at `dec-246`. Corroborated
  independently by this run's F10 check: `.git/hooks/pre-commit` exists as a regular file with no
  `scripts/git-*-hook.sh` source, exactly as a unified gate would leave it.
- **Two singletons** — row 12 (`streamlit_app/` removal, superseded by the Next.js dashboard) and row 13
  (Obsidian command consolidation into `dec-199`).

None of the 13 is a false positive. All 13 warrant a `supersedes`/`superseded_by` pair.

#### DH02 — reference decay (23 WARN)

**`renamed` (18)** — disposition `update-path`; the detector names each target.

| ADRs | Stale path | Current path |
|---|---|---|
| `020`, `021`, `036`, `037`, `043`, `071`, `095`, `096`, `132` (9) | `.ai-state/ARCHITECTURE.md` | `.ai-state/DESIGN.md` |
| `132` | `.ai-state/ARCHITECTURE_CHANGELOG.md` | `.ai-state/DESIGN_CHANGELOG.md` |
| `053`, `054`, `055` (3) | `new_cc_project.sh` | `new_project.sh` |
| `053`, `055` (2) | `commands/new-cc-project.md` | `commands/new-project.md` |
| `053` | `docs/project-onboarding.md` | `docs/greenfield-onboarding.md` |
| `062` | `.ai-state/METRICS_LOG.md` | `.ai-state/metrics_reports/METRICS_LOG.md` |
| `096` | `docs/diagrams/architecture.c4` | `docs/diagrams/architecture/src/architecture.c4` |

Note the self-referential irony: `132-rename-architecture-to-design` — the ADR that *performed* the
rename — still cites the pre-rename paths in its own `affected_files`.

**`placeholder-shape` (3)** — `062` cites `.ai-state/METRICS_REPORT_*.json` and `.ai-state/METRICS_REPORT_*.md`;
`238` cites `.ai-work/<slug>/traceability.yml`. **`out-of-repo` (2)** — `043` and `097` cite
`~/.claude/CLAUDE.md`. See S5: these 5 read as intentional rather than decayed, and the better repair may
be a convention change rather than 5 edits.

#### DH03 — `vanished` (55, INFO advisory)

55 references across **40 distinct ADRs** name a path that is absent with no owning removal decision. Per
the dimension's contract this is an **advisory list for human disposition, not a backlog to clear** — it is
the residual after every repair class, and a decision may be entirely correct though its example files are
gone. No status change is proposed for any of them.

Concentration (top 8 of 40):

| Count | ADR |
|---|---|
| 8 | `131-dashboard-sequential-data-then-parallel-pages` |
| 3 | `160-overview-landing-surface` |
| 2 | `026-session-count-from-observations` |
| 2 | `102-planner-decomposition-decisions` |
| 2 | `120-ai-training-onramp-step-ordering` |
| 2 | `126-pipeline-dashboard-poll-interval` |
| 2 | `130-pipeline-dashboard-visualization-stack` |
| 2 | `225-remove-in-house-memory-subsystem` |

**Judgment.** The distribution argues *against* retirement for most of these. Five of the top eight are
dashboard ADRs whose Streamlit-era file paths vanished in the Next.js migration — but the *decisions* those
ADRs record (poll interval, visualization stack, landing-surface shape) are still in force in the current
dashboard; only their example paths died. Retiring them would destroy live constraints. `dec-225` appearing
here is expected and healthy: it is the removal decision, so its subject is *supposed* to be absent.

My recommendation is that no `vanished` row be retired on this evidence, and that the list be revisited
only when a specific ADR is independently suspected of being dead. If anything, the eight `dec-131` rows
suggest a *reference* repair (repoint to the Next.js equivalents) rather than a retirement.

#### Dimension verdict

DH ran to completion with no oracle degradation. The dimension delivered what it was designed to deliver:
its highest-value output (DH01) found 13 genuinely missing decision-graph edges that no other dimension
could see, with a confident true-remover candidate for all 13. The `vanished` residual is large (55) but
correctly non-actionable, and the design choice to make the detector advisory-only — never editing an ADR,
never changing a `status`, exiting 0 with findings — is validated by the DH03 analysis above: an automatic
demotion policy would have retired live dashboard constraints.

### Pipeline Discipline

Task Chronograph MCP tools were not available this run, so **P01–P05 are skipped**. Auto checks ran:

- **P06** — 2 WARN: `.ai-work/arch-ground-truth/` and `.ai-work/wave1-gap-closure/` each have
  `SYSTEMS_PLAN.md` with no `TASK_BRIEF.md`. See S6.
- **P07** — 12 findings (Important, I2). Two `CONSULT_*.md` fragments with undispositioned `### CH-NN`
  entries. No `INTERFACE_DESIGN.md` or `TRANSACTIONS_DESIGN.md` present in any slug.
- **P08** — no advisory. `clean_work_safety.py` reports 6 task dirs (0 block / 1 warn / 5 safe),
  `stale_safe = 0`, below the ≥3 advisory threshold.

### Tech-Debt Findings

**Rows filed this run: 0.** Not because none were warranted, but because of the agent-definition
contradiction described in I6 — the TD dimension instructs row-writing while Constraints and Boundary
Discipline forbid any write outside this report. I honored the read-only constraint and surface candidates
here instead. This is a registered objection, not a silent skip.

**Staleness gate**: `METRICS_LOG.md`'s newest row is 2026-07-29 (6 days old, under the 14-day threshold)
and `coverage.status = ok`. **No staleness WARN.** Full data available.

Per-check results with LLM-judgment rationale:

- **TD01 (hotspots)** — 3 warranted candidates, 1 explicitly declined. Warranted: `metrics-dashboard.tsx`
  (111,203; has 2 *resolved* rows, so this is re-accumulation after a fix — the strongest signal in the
  set), `readiness-section.tsx` (47,840; ccn 65 is the highest complexity in the top 5 and it has never
  been filed), `hooks/inject_rules.py` (21,638; always-loaded-path code, so its churn is
  higher-consequence than raw score suggests). Declined: `export-codex-rules-bridge.py` (rank 3, 39,842)
  — already carries an active ledger row; filing again would duplicate. This is the gating that keeps a
  p95 dump from flooding the ledger.
- **TD02 (cyclic SCCs)** — **PASS, no rows.** `pydeps.cyclic_sccs` is `[]`; `cyclic_deps: 0` across 11
  modules. Nothing to file.
- **TD03 (coverage floor)** — 2 warranted of 6 below floor. Warranted: `spec_drift.py` (15.5%) and
  `aac_fence_validator.py` (30.7%) — both back live sentinel gates, which converts a coverage number into
  a gate-liveness question (I5). Declined: `check_paths_syntax` (56.3%), `prune_reports` (57.3%),
  `project_metrics/cli` (59.7%), `self_healing_metrics` (65.6%) — all within ~14 points of the 70% floor
  and none gates a correctness claim. Overall project line coverage is 83.2%.
- **TD04 (p95 complexity)** — no rows filed. Aggregate `lizard ccn_p95 = 7.0` and
  `complexipy cognitive_p95 = 8.0` are both healthy for a 5,542-function corpus; individual file crossings
  are concentrated in the codex export scripts already represented by an existing ledger row. Filing per-file
  p95 crossings here would be the mechanical dump the gating clause exists to prevent.
- **TD05 (ledger discipline)** — **PASS, no WARN.** 39 active rows, 72 resolved. Zero `in-flight` rows (so
  none can be >30 days stale), zero `owner-role = unassigned` rows, and the LEDGER header carries the full
  15-column schema including `resolved-by`. The ledger is in the cleanest state observed.

### Other Dimension Results

**Completeness (C)** — PASS. All 58 skills have `SKILL.md` with non-empty `description`; all 17 agents have
`name`/`description`/`tools`; `plugin.json` lists 17 agents, all resolving, with no fs↔registry drift in
either direction. No thin descriptions (0 under 70 chars across skills and agents), so C06/C07 pass.
`.ai-state/SYSTEM_DEPLOYMENT.md` present (C09).

**Consistency (N)** — PASS except I6. All skill dirs and agent files are lowercase kebab-case (N01/N02);
no unrecognized frontmatter keys (N03).

**Freshness (F)** — WARN on F08 only (S3). F10 **PASS**: the only `scripts/git-*-hook.sh` is
`git-finalize-hook.sh`, correctly symlinked to `.git/hooks/post-merge`, `post-commit`, and `post-checkout`.
`.git/hooks/pre-commit` exists as a regular file with no `scripts/` source — consistent with the `dec-246`
gate unification, not drift. F09 **PASS** (no malformed or future-dated markers). F11 **PASS**:
`doc_manifest.yaml` `generated_at` is `2026-08-04T19:55:00Z` and the newest commit touching `docs/`/`.ai-state/`
is `1894f12` at `12:55:28-07:00` — i.e. the manifest's own commit, 28 s later. A naive comparison self-triggers
here because the manifest lives inside the surface it indexes; HEAD `361e3dc` touched neither directory, so
the manifest is current.

**Spec Compliance (S)** — PASS. Rules start with `##`; skills observe progressive disclosure (largest
`SKILL.md` is 418 lines with references present).

**Cross-Reference Integrity (X)** — PASS except S1/S2. X01 17/17; X03 all 14 CLAUDE.md layout paths exist;
X05 coordination-protocol table 17/17 both directions; X06 `agents/README.md` mentions all 17; X09/AC04
137 `dec-NNN` citations across both architecture docs, 0 unresolved.

**Ecosystem Coherence (EC)** — EC06 **PASS**: all four condensed bullets in `claude/config/CLAUDE.md` match
the authoritative `coordination-details.md § Delegation Checklists` on outputs, including every conditional
(`if deployment in scope`, `if pre-refactor outcome`, `if structural gaps found`, `if structural`,
`if step runs tests`, `missing → WARN, not FAIL`). Input clauses ("Read the `SYSTEMS_PLAN.md`", "Verify
against acceptance criteria") are correctly out of scope and correctly absent from the condensed block.

**Spec Health (SH)** — PASS. SH02: all 8 archived specs have substantive traceability matrices (13–18 rows).
Note for future runs: `SPEC_agent-model-routing` and `SPEC_multi-language-support` use `AC-NN` identifiers
rather than `REQ-NN`; a `REQ-`-only grep reports them as empty — they are not. SH08 `gap: false` (newest
spec 5 days old, no qualifying ADR cluster). SH07 not run: `detect_drift()` requires `scope`, `repo_root`,
`base_sha` arguments the catalog's invocation does not supply — a check-definition gap worth correcting.

**Calibration Accuracy (CA)** — PASS. CA01: `calibration_log.md` present, valid header, 51 rows. CA03:
`covered: true`, newest calibration 2026-07-31, 1 uncalibrated pipeline commit (threshold 2). CA02 (LLM
trend) has sufficient entries but no override-pattern concentration above 40% was evident.

**Readiness Feedback (RD)** — PASS. RD01: `adjusted_level: 4` against a production floor of 3,
`mechanical_only: false` (full LLM tier ran), `below_threshold: false`.

**Decision Log (DL)** — DL01 PASS, DL02 **FAIL ×13** (I1), DL03 PASS (316/316), DL04/DL06 no orphaned or
one-directional pointers detected among parseable ADRs, DL05 PASS.

**Behavioral Contract (BC)** — all PASS. BC01: `rules/swe/agent-behavioral-contract.md` exists with
`core: true` frontmatter and no `paths:` key, so it is always-loaded. BC02: all four behaviors appear in
canonical order with identical spelling in `~/.claude/CLAUDE.md`, project `CLAUDE.md`, and
`claude/config/CLAUDE.md`. BC03: exactly **14** agents reference the rule, matching the catalog's list.
BC04: `### Behavioral Contract Findings` present with all 6 canonical tags.

**Architecture Completeness (AC)** — PASS. AC04 137/137 `dec-NNN` resolve; AC05 both docs present;
AC10 `aac_fence_validator.py` → `Verdict: PASS` on both `.ai-state/DESIGN.md` and `docs/architecture.md`.
AC11/AC12 skipped — LikeC4 MCP substrate not queried this run (INFO, per substrate-trigger convention).

**Gate Liveness (GL)** — PASS. GL02: `check_gate_liveness.py --json` → 0 findings. GL01: the 6 named
sections sampled all have producers. GL03: no presence-without-substance gate found in the sampled
catalog rows — though see I5, where the substance question shifts from the *gate's wording* to its
*implementation's coverage*.

**Test Topology (TT)** — `.ai-state/TEST_TOPOLOGY.md` absent, so TT01–TT05 skip (INFO: the protocol is
opt-in; see `skills/testing-strategy/references/test-topology.md`). TT03 additionally moot — 0
`topology-drift` rows in the ledger. **TT06**: 2 of 3 growth signals crossed — Built structural components
in `DESIGN.md` §3a = **14** (threshold ≥4, crossed) and test count = **313** (threshold ≥200, crossed).
The third signal, full-suite wall-clock ≥90 s, was **not measured** — running the full suite exceeds a
read-only audit's cost envelope. Since the advisory requires **all three**, no advisory is emitted; but two
signals are well past threshold and a single timed suite run would settle it. Worth doing.

**Pre-Refactor Plan Integrity (PR)** — skipped (INFO): no `PRE_REFACTOR_PLAN.md` under `.ai-work/`.

**Hackathon Mode (HK)** — skipped, nothing emitted. `.claude/settings.json` `env` contains only
`PRAXION_DISABLE_OBSERVABILITY`; `PRAXION_HACKATHON_MODE` is unset.

**Code Health (CH)** — CH01 no significant cross-module duplication observed in the sampled
implementation files.

**Self-Verification (V)** — V01 PASS (`./agents/sentinel.md` in `plugin.json`), V02 PASS (coordination
protocol row present), V03 PASS (`agents/README.md` row present), V04 PASS (Check Catalog section present).
Offset by I6 and S7 — the sentinel is the lowest-graded artifact in its own scorecard.

### Recommended Actions (prioritized)

1. **Resolve the sentinel's read-only contradiction (I6)** — this gates whether TD01/TD04/EC07 findings can
   ever be filed, so it blocks item 6 below. Prefer narrowing the TD instructions to "surface candidates";
   the read-only guarantee is what makes this agent safe to run unattended. → `context-engineer`
2. **Fix the 13 unparseable ADR frontmatter blocks (I1)** and add a YAML-parse assertion to the finalize
   path so the next one fails at write time. → `context-engineer`
3. **Disposition the 12 stalled challenges (I2)** before `.ai-work/wave1-gap-closure/` is cleaned — the
   reasoning is unrecoverable after cleanup. Give each disposition a durable home per the protocol.
   → `user`
4. **Reclaim always-loaded token headroom (I3)** — 2.1% is the narrowest on record and the next
   unconditional addition of any size breaches. Extract the `### Available Agents` table or the
   `Pipeline rules` table from `swe-agent-coordination-protocol.md` (26,216 chars, 31% of budget).
   → `context-engineer`
5. **Record the 13 DH01 supersession links (I4)** — start with the 7-row memory cluster (`dec-225`) and the
   3-row pre-commit cluster (`dec-246`), where the true remover is near-certain. Set both directions
   (DL06 reciprocity). → `systems-architect`
6. **Add canary tests for `aac_fence_validator.py` and `spec_drift.py` (I5)** — assert each FAILs on its
   documented golden bad-case. AC10 trusted the fence validator's verdict this run at 30.7% coverage.
   → `test-engineer`
7. **Batch-repair the 18 renamed ADR paths (S4)** and decide the shape/external convention question (S5).
   → `context-engineer`
8. **Close the two catalog gaps (S1, S2)** — `evidence-appraisal` in `skills/README.md`; `consult` and
   `refresh-claude-blocks` in `commands/README.md`. → `doc-engineer`
9. **Re-verify the 27 over-threshold staleness sections (S3)**, `communicating-agents` first (9 sections,
   oldest at 95d/60d). → `context-engineer`
10. **Time the full suite once** to settle TT06's third signal — two of three thresholds are already
    crossed. → `user`
11. **Fix `detect_drift()` invocation in the SH07 check definition** — it requires `scope`, `repo_root`,
    `base_sha`; SH07 could not run this pass. → `context-engineer`
