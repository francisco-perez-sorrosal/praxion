# Sentinel Report

**Generated**: 2026-08-05 00:14:32
**Scope**: Full ecosystem sweep, all dimensions. Working tree audited as it stands (uncommitted changes across `scripts/`, `agents/sentinel.md`, `commands/decisions.md`, `scripts/CLAUDE.md`, `.ai-state/calibration_log.md`, one finalized ADR, two new draft ADRs).
**Emphasis this run**: Architecture Completeness (AC) and Ecosystem Coherence (EC), weighted heavily per request.
**Baseline**: `SENTINEL_REPORT_2026-08-04_13-35-55.md` (Health C, 0/6/9, Coherence B).

---

## Agent-definition staleness disclosure

The session's cached `agents/sentinel.md` was stale. This report was executed against the
working-tree copy read from disk. Divergences found between the cached copy and disk — recorded
here because the request asked for them explicitly:

| # | Divergence | Cached copy | Working tree (authoritative) |
|---|---|---|---|
| 1 | GL dimension | GL01, GL02, GL03 only | adds **GL04** (`uninvoked-gate`) and **GL05** (`ambient-import`), both from the same `check_gate_liveness.py --json` run |
| 2 | AC dimension header | "**Three** additional conditional-activation checks (AC10, AC11, AC12)" | "**Four** conditional-activation checks (AC10, AC11, AC12, **AC13**)" plus a substrate trigger and a disambiguating paragraph for AC11-vs-AC13 |
| 3 | AC13 | absent | present — design-doc projection via `scripts/check_architecture_projection.py`; six finding kinds |
| 4 | DH dimension | DH01, DH02, DH03 | adds **DH04** (`reopen_candidates`) and **DH05** (`category_mix` / `architectural_share_recent`), plus a `skipped_terminal` preamble paragraph |
| 5 | SH07 | "Imports `detect_drift` from `scripts/spec_drift`" | "Run `python3 scripts/check_spec_drift.py --json`" — the wrapper is now the invoked path (this is the td-113 fix) |
| 6 | DL04 | pointer set omitted `retired_by` | `retired_by: [dec-MMM]` added to the existence-checked pointer set |
| 7 | DL06 | no `retired_by` clause | explicit **exemption**: `retired_by` is one-directional by design and must never be flagged |
| 8 | Phase-3 dispatch paragraph | dispatched neither SH07, AC13, GL04/GL05 nor DH04/DH05 | dispatches all of them |

Divergences 5–8 matter beyond bookkeeping: an agent running the cached copy would have executed
SH07 by a code path that no longer exists, skipped four live checks, and produced a false
`retired_by` WARN. All checks below were run from the disk copy.

---

## Ecosystem Health: C

*(0 Critical / 9 Important / 10 Suggested. Prior run: C, 0/6/9.)*

### Summary

**The wiring is sound; the self-description layer has drifted.** Every check that asks "does this
reference resolve, does this table match the filesystem, does this gate fire" passed: X01, X03,
X05, X06, EC01, EC02, EC07, AC04, AC10, AC13, DL03, DL04, BC01–BC04, V01–V04, GL01, GL02, GL04,
GL05, SH07, SH08, CA03, RD01, F10, N01–N03, S03, S04, C01–C05. The three newest detectors
(`check_gate_liveness.py`, `check_architecture_projection.py`, `adr_health.py`) all run clean on
the live repo, and the full suite is green — **2166 passed, 1 skipped in 78.0s**.

What degraded is the layer where checks *describe themselves*. Eleven of the nineteen findings
are one species: **a convention or check whose documented form has drifted from its operative
form.** The token-budget divisor disagrees with itself across two files and the disagreement now
straddles the ceiling (I1/I2). The sentinel's own Constraints forbid a write that six of its own
checks mandate (I3). An always-loaded rule describes EC06 as a byte-equivalence check it is not
(I4). The AaC doc still enumerates three AC checks where four ship (I5). F11 and AC11 both fire
on the healthy state (I6, INFO). That is precisely the anti-pattern `rules/swe/gate-liveness.md`
names — *a convention living at two textual sites* — appearing eleven times inside the machinery
built to detect it.

**Improved since 2026-08-04**: GL04's first-run finding (td-113, the uninvoked `check_spec_drift.py`
wrapper) is resolved and GL04 now reports zero. AC13 landed and reconciles 16 rows ↔ 16 elements
with no drift. DL04's `retired_by` gap is closed. Two draft ADRs are schema-clean.

**Degraded**: the always-loaded budget grew ~4,000 chars to its narrowest margin on record;
27 staleness markers crossed their thresholds; three catalogs lost entries.

---

### Ecosystem Coherence: C

**System-level EC checks:**

| Check | Result | Evidence |
|-------|--------|----------|
| EC01 | PASS | 17 agents on disk; all 17 appear in `agents/README.md` and its pipeline diagram; 0 orphans either direction |
| EC04 | PASS | Every pipeline output doc in `rules/swe/agent-intermediate-documents.md` has a named producer and consumer; the two newest artifact families (`PRE_REFACTOR_PLAN.md`, `CONSULT_<discipline>.md`) both have producer + consumer + a sentinel check (PR01, P07) |
| EC05 | WARN | No missing *artifact type*. But three catalog surfaces have fallen behind the filesystem (I9), and `docs/aac-dac.md` is missing a whole check (I5) — the gap is in the explanatory layer, not the structural one |
| EC06 | PASS | All four agents' produced deliverables in `coordination-details.md § Delegation Checklists` appear in the condensed block, conditionals included. See I4 — a *third* site misdescribes the check |
| EC07 | PASS | `check_aac_golden_rule.py --mode=audit --json` → `[]` |

**Cross-dimension anomalies:**

One dominant pattern, spanning EC, GL, AC, T, F, DL and V:
**checks and conventions whose prose has drifted from their behavior.** Findings I2, I3, I4, I5,
I6, S1, S2, S3, S4, S5, S6 and S11 are all instances. They cluster in the newest surfaces —
the ones edited in the last 48 hours — which is the expected place for this drift and the
cheapest place to correct it.

A secondary pattern: **two checks now fire permanently on the healthy state.** F11 (I6) WARNs
whenever the manifest-regeneration commit is the newest `.ai-state/` commit — the steady state
after every finalize-hook run. AC11 emits `validator-unable-to-query-likec4-mcp` on every run
because the LikeC4 MCP tools are not in the sentinel's `tools:` frontmatter and never can be.
A signal that is always on is not a signal.

**Artifact coherence distribution:** 44 A, 11 B, 3 C (per-artifact grades below).

**Synthesis:** The ecosystem works as a system. Agents, skills, commands, rules, ADRs, hooks and
gates are correctly wired to one another; the deterministic detectors all pass; the pipeline has
no dead ends. The friction is confined to the layer that *documents* those connections, and it is
concentrated in surfaces touched this week. This is localized friction (grade C) rather than
structural degradation — the connective tissue is intact, but the map of it has three errors and
one omission, and two gates have stopped carrying information.

---

### Ecosystem Metrics

| Metric | Value | Trend |
|--------|-------|-------|
| Total artifacts | 514 | ↑ from 507 |
| Skills | 58 | ↑ +1 (`evidence-appraisal`) |
| Agents | 17 | stable |
| Rules (unconditional / path-scoped) | 6 / 17 | stable |
| Commands | 45 | ↑ +2 (`consult`, `refresh-claude-blocks`) |
| Hooks | 41 | stable |
| ADRs (finalized / draft) | 319 / 2 | ↑ +2 drafts |
| Specs | 9 | stable |
| Always-loaded chars | 88,591 | ↑ +4,029 — **narrowest margin on record** |
| Always-loaded tokens (/3.6, authoritative) | 24,608 / 25,000 (98.4%) | ↓ headroom 3.4% → **1.6%** |
| Always-loaded tokens (/3.5, sentinel T02) | 25,312 / 25,000 | **over budget** |
| Full-suite wall clock | 78.0s (2166 tests) | below the 90s TT06 threshold |
| Test count | 2032 collected / 2166 executed | ↑ |
| Tech-debt ledger (active / resolved) | 38 open / 75 terminal | 0 in-flight, 0 unassigned |
| Staleness sections (total / stale) | 91 / 27 | ↓ was 0 stale |
| Coverage (line) | 83.2% | ↑ from 71.7% |
| Readiness level | 4 (floor 3) | stable |
| Architectural share (recent-50 / corpus) | 0.84 / 0.718 | at adoption baseline |

---

### Scorecard

| Artifact | Type | Complete (C) | Consistent (N) | Fresh (F) | Spec (S) | Cross-Ref (X) | Tokens (T) | Coherence (EC) | Overall |
|----------|------|--------------|-----------------|-----------|----------|----------------|------------|----------------|---------|
| `agents/sentinel.md` | agent | A | **C** | A | A | A | **B** | **B** | **C** |
| `agents/verifier.md` | agent | A | A | A | A | A | **B** | A | B |
| `agents/systems-architect.md` | agent | A | A | A | A | A | **B** | A | B |
| `agents/implementation-planner.md` | agent | A | A | A | A | A | **B** | A | B |
| `agents/*` (13 others) | agent | A | A | A | A | A | A/B | A | A |
| `rules/swe/swe-agent-coordination-protocol.md` | rule | A | **C** | A | A | A | **B** | **B** | **C** |
| `rules/CLAUDE.md` | rule | A | **B** | A | A | A | A | **B** | B |
| `rules/swe/agent-behavioral-contract.md` | rule | A | A | A | A | A | A | A | A |
| `rules/swe/adr-conventions.md` | rule | A | **B** | A | A | A | **B** | A | B |
| `rules/*` (19 others) | rule | A | A | A | A | A | A | A | A |
| `docs/aac-dac.md` | doc | **C** | A | **B** | A | **B** | A | **C** | **C** |
| `docs/architecture.md` | doc | A | A | A | **B** | A | A | A | B |
| `.ai-state/DESIGN.md` | doc | A | A | A | A | A | A | A | A |
| `skills/README.md` | catalog | **B** | A | A | A | **B** | A | A | B |
| `commands/README.md` | catalog | **B** | A | A | A | **B** | A | A | B |
| `agents/README.md` | catalog | A | A | A | A | A | A | A | A |
| `rules/README.md` | catalog | A | A | A | A | A | A | A | A |
| `skills/skill-crafting` | skill | A | A | A | A | A | **B** | A | B |
| `skills/communicating-agents` | skill | A | A | **B** | A | A | A | A | B |
| `skills/mcp-crafting` | skill | A | A | **B** | A | A | A | A | B |
| `skills/hook-crafting` | skill | A | A | **B** | A | A | A | A | B |
| `skills/cicd` | skill | A | A | **B** | A | A | A | A | B |
| `skills/likec4-querying` | skill | A | A | **B** | A | A | A | A | B |
| `skills/agentic-transactions` | skill | A | A | **B** | A | A | A | A | B |
| `skills/*` (51 others) | skill | A | A | A | A | A | A | A | A |
| `commands/*` (45) | command | A | A | A | A | A/B | A | A | A |
| `.ai-state/decisions/` (321) | ADR corpus | A | **B** | A | **B** | A | — | A | B |
| `scripts/check_gate_liveness.py` | gate | A | A | A | A | A | A | A | A |
| `scripts/check_architecture_projection.py` | gate | A | A | A | A | A | A | **B** | B |
| `scripts/adr_health.py` | gate | A | A | A | A | A | A | **B** | B |
| `.claude-plugin/plugin.json` | config | A | A | A | A | A | A | A | A |

---

## Findings

### Critical (blocks correct behavior)

*None.*

### Important (degrades quality or efficiency)

| # | Check | Dimension | Location | Finding | Recommended Action | Owner |
|---|-------|-----------|----------|---------|--------------------|-------|
| I1 | T02 | Tokens | always-loaded surface | **88,591 chars — narrowest margin on record.** Under `rules/CLAUDE.md`'s authoritative /3.6 basis: 24,608 tokens = **98.4% of the 25,000 budget, 1.6% headroom (392 tokens)**. Prior narrowest was 3.4% (84,562 chars, 2026-06-11); the surface grew 4,029 chars since. Largest contributors: `swe-agent-coordination-protocol.md` 26,355; `adr-conventions.md` 14,608; global `CLAUDE.md` 13,355; project `CLAUDE.md` 11,339. The next unconditional addition of any size breaches. | Extract a path-scoped or skill-resident section from `swe-agent-coordination-protocol.md` (the § Cross-Agent Skill Conventions block and the pipeline-rules table are both candidates) or from `adr-conventions.md` (the full Frontmatter field table could live in `adr-authoring-protocols.md`, which is already the procedural anchor) | context-engineer |
| I2 | T02 | Tokens / Consistency | `agents/sentinel.md:127` vs `rules/CLAUDE.md § Token Budget` | **The token-budget divisor lives at two sites, they disagree, and at today's size they straddle the ceiling.** T02 says "heuristic: chars / 3.5" → 25,312 tokens = **FAIL**. `rules/CLAUDE.md` says "divide bytes by 3.6 for a conservative token estimate (4.0 is more realistic for markdown)" and declares itself the authoritative basis → 24,608 tokens = **PASS**. The same file warns that "three mutually inconsistent bases have coexisted in this repo before, and every historical PASS was basis-dependent" — a fourth is live now, inside the check that enforces the budget. | Make T02 cite `rules/CLAUDE.md § Token Budget` as its basis rather than restating a divisor, per the gate-liveness single-source-of-truth remedy | context-engineer |
| I3 | N05 / V | Consistency / Self-Verification | `agents/sentinel.md` lines 168–171, 185, 201 vs 603, 637 | **`agents/sentinel.md` contradicts itself on whether the sentinel may write the tech-debt ledger.** Constraints (637): "Your only write targets are `SENTINEL_REPORT_*.md` and `SENTINEL_LOG.md`." Boundary Discipline (603): "Writes `SENTINEL_REPORT_*.md` and `SENTINEL_LOG.md` … **only**". But TD01–TD04 (168–171), TT04 (185) and EC07 (201) each instruct "write one row / file one row per § Producer overlays → **sentinel**" into `.ai-state/TECH_DEBT_LEDGER.md`. The always-loaded `rules/swe/agent-intermediate-documents.md` independently names the sentinel as one of only **four** ledger writers — so the ledger-write is the intended design and the Constraints/Boundary text is the stale half. An agent obeying Constraints literally drops six checks' outputs on the floor. | Amend Constraints and the Boundary Discipline row to name `.ai-state/TECH_DEBT_LEDGER.md` as a sanctioned append-only write target for TD01–TD04 / TT04 / EC07 | context-engineer |
| I4 | EC06 / N05 | Coherence / Consistency | `rules/swe/swe-agent-coordination-protocol.md § Delegation Checklists` | **An always-loaded rule misdescribes EC06 as a byte-equivalence check.** It states: "Sentinel `EC06` validates that the condensed block in `claude/config/CLAUDE.md` stays **byte-equivalent** with that section." EC06's actual rule is a semantic, outputs-only comparison ("verbatim **or as a recognizable shorthand**"; "ignore Read X / Verify against X input clauses"). `coordination-details.md` says "stays **in sync**". The condensed block is by construction *not* byte-equivalent — it is condensed. This is one convention at **three** textual sites, the strongest claim sitting in the always-loaded one, and an agent acting on it would destroy the condensation EC06 exists to preserve. | Reword the protocol rule to "stays in sync with" and cross-reference `coordination-details.md` as authoritative | context-engineer |
| I5 | X07 / EC05 | Cross-Ref / Coherence | `docs/aac-dac.md:163-165, 190, 217, 221` | **The AaC tier's explanatory doc enumerates "AC10–AC12" in four places; AC13 is absent entirely.** Lines 163–165 list the checks one per bullet (AC10 fence integrity, AC11 model↔markdown, AC12 traceability orphans) with no AC13 bullet; 190, 217 and 221 all say "AC10–AC12". AC13 (`check_architecture_projection.py`) shipped as a fourth AC check **and** is wired as the `architecture-projection` pre-commit gate (`.pre-commit-config.yaml:135-137`). A reader of `docs/aac-dac.md` concludes the AaC audit surface is three checks and misses the one covering the published half (canonical blocks installed into every managed project's `CLAUDE.md`). | Add an AC13 bullet at line 166 and widen the three range references to "AC10–AC13" | doc-engineer |
| I6 | F11 | Freshness | `.ai-state/doc_manifest.yaml` + `agents/sentinel.md:92` | **F11 has an inverted terminal condition — it fires on the healthy state.** `generated_at: 2026-08-05T05:12:58Z` vs `git log -1 --format=%cI -- docs/ .ai-state/` = `2026-08-05T05:13:27Z`. The manifest predates the commit by **29 seconds because that commit is the one that regenerated it** (`e6fa0e6 chore(state): doc manifest picks up the calibration row`). The builder stamps `generated_at` before the commit is written, so F11 WARNs whenever the newest `.ai-state/`-touching commit is the regeneration commit — precisely the steady state after every finalize-hook run. A gate that fires on correct behavior carries no signal. | Compare `generated_at` against the newest commit **excluding** the one that touched `doc_manifest.yaml` itself, or allow a tolerance window | context-engineer |
| I7 | F08 | Freshness | 6 skills, 27 sections | **27 of 91 cataloged staleness sections (30%) are past their skill-declared threshold; 0 FAIL.** `communicating-agents` 9 sections @ 96d, `mcp-crafting` 7 @ 72d, `hook-crafting` 4 @ 72d, `cicd` 3 @ 72d, `likec4-querying` 2 @ 97d, `agentic-transactions` 2 @ 64d. These skills declare 60-day thresholds *because* their subjects move fast (A2A spec, MCP SDK + `.mcpb` packaging, GitHub Actions marketplace, LikeC4 MCP tool surface, provider transports). Prior runs recorded all markers PASS, so this is a fresh regression, not a cold start. | Spawn `researcher` per skill to re-verify against current upstream, then refresh markers. `communicating-agents` and `likec4-querying` first (oldest + fastest-moving) | context-engineer / researcher |
| I8 | DL02 | Decision Log | 13 finalized ADRs | **13 finalized ADRs have frontmatter `yaml.safe_load` cannot parse** — unquoted scalars beginning with a backtick, or containing `: ` inside prose: `032`, `046`, `057`, `077`, `111`, `129`, `152`, `153`, `168`, `169`, `190`, `224`, `294`. DL02's pass condition is "valid YAML frontmatter". Currently **latent** — `adr_health.py` and `finalize_adrs.py` use tolerant line-based parsing, and they parsed all 319 — but the documented Discovery Protocol invites a real YAML parser, and any consumer using one silently drops those 13 records. No gate detects it: `check_adr_frontmatter_promotion.py` checks promotion, not parseability. | Quote the offending `title:` / `summary:` values; add a parseability assertion to `check_adr_frontmatter_promotion.py` so the class cannot recur | context-engineer |
| I9 | X07 | Cross-Ref | `skills/README.md`, `commands/README.md` | **Catalog drift on two surfaces.** `skills/README.md` lists 57 of 58 skills — **`evidence-appraisal` has 0 mentions**. `commands/README.md` lists 43 of 45 — **`consult` and `refresh-claude-blocks` both absent** (0 mentions each). `agents/README.md` and `rules/README.md` are complete. | Add the three missing catalog entries | doc-engineer |

### Suggested (improves but not urgent)

| # | Check | Dimension | Location | Finding | Recommended Action | Owner |
|---|-------|-----------|----------|---------|--------------------|-------|
| S1 | DH05 | Decision Health | `adr_health.py` `category_mix` | **The recent architectural share is at the adoption baseline, but the window cannot yet measure the test.** `architectural_share_recent = 0.84`; corpus `229/319 = 0.718`. Per `adr-authoring-protocols.md`, "a run at or above 84% is the test being ignored" — so this flags. But only **2** decisions post-date adoption (corpus 317→319), so 48 of the 50-decision window are pre-adoption records the rule explicitly exempts from retroactive migration. DH05 will read "test being ignored" for roughly the next 48 decisions regardless of author behavior. | Add a post-adoption-count qualifier to DH05's reading, or have `adr_health.py` report the share over post-adoption decisions only | context-engineer |
| S2 | GL03 | Gate Liveness | `gate-canaries.md § Naming` vs codebase | **The canary-naming convention lives at two sites and the codebase follows the unenforced one.** `gate-canaries.md` prescribes a closed keyword vocabulary "so the coverage meta-test can find it": `reject\|flag\|fail\|block\|deny\|detect\|nonzero\|violation\|invalid\|missing\|empty\|bad`. The codebase's dominant convention is a `test_canary_*` prefix — **42 such tests, only 16 of which contain a prescribed keyword**. The other 26 are invisible to `fitness/tests/test_gate_canary_coverage.py`. No gate is currently mis-reported, but the meta-test's coverage claim is weaker than it reads. | Add `canary` to `_CANARY_KEYWORDS` (one-word change, makes the dominant convention enforceable) | test-engineer |
| S3 | GL03 | Gate Liveness | `fitness/tests/test_gate_canary_coverage.py` | **`scripts/adr_health.py` is outside the canary meta-test's computed gate set.** The set is `scripts/check_*.py` + `scripts/validate_*.py` + `hooks/*_gate.py\|*_guard.py\|*_gate.sh` + `fitness/tests/test_*.py`. `adr_health.py` matches none — yet the sentinel delegates four checks to it (DH01/DH02/DH04/DH05) and `commands/decisions.md` delegates its whole Gather phase. It *is* well-canaried in fact (23 of 29 tests are `test_canary_*`), but nothing enforces that. The meta-test's own scope-fidelity clause, turned on itself: documented scope ("the full gate set") is wider than computed scope (a filename glob). | Add `scripts/adr_health.py` to the gate set explicitly, or widen the glob to catch detectors the sentinel delegates to | test-engineer |
| S4 | GL03 | Gate Liveness | `scripts/check_architecture_projection.py` | **The meta-test's check-level unit is inert for the newest gate in the repo.** That unit fires only "when a gate file defines module-level `check_*` / `validate_*` functions". `check_architecture_projection.py` defines none — its entrypoint is `reconcile()`, and its six finding kinds live inside it. The unit exists precisely to catch "a check added to a file that already carries canaries," and it is blind here. All six kinds *are* canaried (9 canaries in the sibling test), but only the file-level ≥1-canary rule is enforced. Contrast `check_gate_liveness.py`, whose three `check_*` functions are all covered at check level. | Either rename `reconcile()` to `check_projection()`, or key the check-level unit on emitted finding kinds rather than function-name prefix | test-engineer |
| S5 | AC08 | Architecture | `docs/architecture.md:165, 170` | **AC08's pass condition is document-wide but its intent is the component table.** §9.1 "Where to find what" has a `Status` column and one `Planned` row: `.ai-state/TEST_TOPOLOGY.md (per-project) \| Planned (no Praxion population by design)`. That is a navigation index for an opt-in **per-project** artifact, not a Praxion component — and the component table's own comment (line 231) already says "Only Built components appear here." Literal AC08 fires; substantive AC08 passes. | Scope AC08 to the component table, or reword the navigation row to avoid the reserved word | context-engineer |
| S6 | DL04 / DL06 | Decision Log | `.ai-state/decisions/319-*.md` | **DL04 and DL06 impose opposite requirements on a draft-stage back-link, and the ADR rule mandates the side DL04 warns about.** `319-a-gate-that-exists-is-not-a-gate-that-ru.md` (finalized, `accepted`) now carries `re_affirmed_by: ['dec-draft-404f464a']`, matching the new draft's `re_affirms: dec-319`. DL06 *requires* this back-link; DL04 classes a finalized ADR pointing at a `dec-draft-<hash>` as a mixed-pointer WARN; and `adr-conventions.md § Re-affirmation Protocol` *instructs* "append `<new-id>` to the old ADR's `re_affirmed_by`". DL06 defers the case to DL04, so the catalog routes consistently — but following the protocol correctly still produces a WARN. **The working tree's state here is correct**; the check is what needs the carve-out. | Add a draft-stage carve-out to DL04: a finalized ADR whose `re_affirmed_by` names a *live* draft is the sanctioned transient state, not drift | context-engineer |
| S7 | DL06 | Decision Log | `dec-040` / `dec-204` | **One genuine reciprocity break across 308 strictly-parsed ADRs.** `dec-040` lists `dec-204` in `re_affirmed_by`, but `dec-204` has no `re_affirms: dec-040`. (`dec-258`, the other entry in dec-040's list, reciprocates correctly.) | Set `re_affirms: dec-040` on `dec-204`, or remove it from `dec-040`'s list | context-engineer |
| S8 | P06 | Pipeline | `.ai-work/arch-ground-truth/`, `.ai-work/wave1-gap-closure/` | **Two slugs carry `SYSTEMS_PLAN.md` without `TASK_BRIEF.md`** — Standard/Full tier implied, so the Intake Clarity Gate's brief was never written. Both are recent, active slugs. | Backfill the briefs, or accept as a known gap in two in-flight pipelines | user |
| S9 | TD05(a) | Tech Debt | `.ai-state/TECH_DEBT_RESOLVED.md` | **`td-022` is `status = resolved` with an empty `resolved-by`** — 1 of 72 resolved rows. TD05(b) and (c) are clean: 0 in-flight >30d, 0 unassigned >7d, all 38 active rows `open` with an assigned `owner-role`. | Populate `resolved-by` on `td-022` | orchestrator |
| S10 | T01 / T03 | Tokens | agents + skills | **Size warnings, no failures.** `skills/skill-crafting/SKILL.md` 418 lines (warn 400, fail 500). `agents/sentinel.md` **644** and `agents/verifier.md` **598** exceed their own raised 550-line warn band (fail 700). Six agents exceed the standard 300 warn: systems-architect 462, implementation-planner 445, context-engineer 344, skill-genesis 336, promethean 311, researcher 307. T04 clean — largest reference is `skills/cicd/references/patterns-and-examples.md` at 769 (< 800). | Monitor. The sentinel's growth is intrinsic (the catalog gained AC13, GL04, GL05, DH04, DH05 this cycle) but is now 94 lines past its warn band | context-engineer |
| S11 | V / Phase 7 | Self-Verification | `agents/sentinel.md:587-589` vs `SENTINEL_LOG.md` header | **The prescribed log column order does not match the on-disk header.** Phase 7 prescribes `\| Timestamp \| Report File \| Health Grade \| Artifacts \| Findings \| Ecosystem Coherence \|`; `SENTINEL_LOG.md`'s actual header is `\| Timestamp \| Health Grade \| Artifacts \| Findings (C/I/S) \| Ecosystem Coherence \| Report File \|`. An agent following the schema literally would corrupt the table. This run followed the on-disk header. | Correct the Phase 7 template to match the live file | context-engineer |
| S12 | V | Self-Verification | `agents/sentinel.md:505-510` | **The ecosystem health-grade scale is non-exhaustive.** B requires "fewer than 3 Important"; C says "1-2 Important, no Critical"; D requires "any Critical". A run with 0 Critical and 3+ Important — this one, with 9 — falls in no bucket. Graded **C** here as the nearest defensible reading (worse than B, no Critical so not D). | Reword C as "3+ Important, no Critical" or "1+ Important, no Critical" | context-engineer |

---

## Dimensions examined, skipped, and why

Stated explicitly so the examined surface is visible rather than implied.

**Fully examined (all checks run):** C (C01–C09), N (N01–N06), F (F01–F11), S (S01–S07),
X (X01–X09), T (T01–T06), EC (EC01–EC07), DL (DL01–DL06), BC (BC01–BC04), GL (GL01–GL05),
DH (DH01–DH05), CA (CA01–CA03), SH (SH01–SH08), RD (RD01), CH (CH01).

| Dimension | Status | Reason |
|---|---|---|
| **AC01–AC10, AC13** | **Examined** | AC10 fence validator PASS on both `.ai-state/DESIGN.md` and `docs/architecture.md`. **AC13 PASS — 16 §3a rows ↔ 16 model elements, 0 findings, `withheld: []`**, covering both the structural half (model ↔ §3a) and the published half (canonical-block registry ↔ §4). AC04 PASS: 114 `dec-NNN` refs in DESIGN.md + 23 in architecture.md, **0 unresolved, 0 dangling `dec-draft-` refs**. AC08 → S5. |
| **AC11** | **Fallback path, permanent WARN** | LikeC4 MCP tools (`read-project-summary`) are **not in the sentinel's `tools:` frontmatter** (`Read, Glob, Grep, Bash, Write`), so the rule's documented fallback was used, emitting the prescribed `validator-unable-to-query-likec4-mcp` WARN. Because the toolset can never contain those MCP tools, **AC11 emits this WARN on every run** — same permanently-on shape as I6. Substantively covered: AC13's deterministic reconciliation over the same two surfaces returned 0 findings. Consider retiring AC11 in favour of AC13, or granting the sentinel the MCP tool. |
| **AC12** | **Skipped** | Bidirectional convention not populated: **0** archived SPECs carry `architectural_elements:` frontmatter and **0** LikeC4 elements carry `metadata.req_ids`. Substrate absent → INFO, not WARN. |
| **TT01–TT05** | **Skipped** | `.ai-state/TEST_TOPOLOGY.md` absent. The topology protocol is opt-in — see `skills/testing-strategy/references/test-topology.md`. |
| **TT06** | **PASS, no advisory** | Two of three growth signals crossed: 2032 tests ≥ 200 ✓, 17 Built §3a components ≥ 4 ✓. **Third signal below threshold: full suite 78.0s < 90s.** Advisory correctly withheld — but the margin is 13%, so expect TT06 to fire within a few growth cycles. |
| **P01–P05** | **Skipped** | Task Chronograph MCP tools (`get_pipeline_status`, `get_agent_events`) not in the sentinel's toolset. |
| **P07** | **PASS** | No `INTERFACE_DESIGN.md` / `TRANSACTIONS_DESIGN.md` / `CONSULT_*.md` with an undispositioned challenge across the 6 `.ai-work/` slugs. |
| **P08** | **No advisory** | `clean_work_safety.py --json` → `stale_safe = 0` (threshold 3). 6 slugs total: 4 safe, 2 warn, 0 blocking. |
| **PR01** | **Skipped** | No `PRE_REFACTOR_PLAN.md` anywhere under `.ai-work/`. Absence is the common case — the artifact is emitted only for the `emit-PRE_REFACTOR_PLAN` Phase 2.5 outcome. |
| **HK01** | **Skipped (inert)** | `PRAXION_HACKATHON_MODE` unset in `.claude/settings.json`. |
| **SH03–SH06** | **Examined** | 9 archived specs; traceability matrices present and non-empty on all. SH06 evaluated only where a predecessor exists. |
| **CA02** | **Examined** | Calibration log has ≥5 entries; no single override pattern exceeds 40%. |
| **TD01–TD04** | **Signals read, no rows written** | Read-only run per request; see also I3. TD02 clean (`cyclic_sccs: []`, 11 modules, 0 cyclic deps). TD03 clean (line coverage 83.2% > 70% floor). TD01: top hotspot `dashboard_app/src/components/metrics-dashboard.tsx` (churn 1823 × complexity 61 = 111,203), unchanged across the last three metrics reports and already represented in the ledger — no new row warranted. TD04: aggregate `ccn_p95 = 7.0`, `cognitive_p95 = 8.0`; no file crossing warrants a row. Metrics freshness OK (newest `METRICS_LOG` row 2026-07-29, 7 days < 14). |

---

## Pipeline Discipline

P01–P05 skipped — Task Chronograph MCP unavailable to this agent's toolset.
P06: 2 WARNs (S8). P07: PASS. P08: no advisory (`stale_safe = 0`).

---

## Tech-Debt Findings

**New rows filed this run: 0.** This was an explicitly read-only audit; per the request, findings
are reported rather than written. Note the standing contradiction in I3 — TD01–TD04, TT04 and
EC07 all instruct ledger writes that the sentinel's own Constraints forbid.

**TD05 discipline issues: 1** — `td-022` resolved without `resolved-by` (S9). (b) and (c) clean.

**Ledger health:** 38 active rows, all `open`, all with an assigned `owner-role`; 0 in-flight,
0 unassigned. 75 terminal rows (72 resolved, 3 wontfix). Class mix: drift 12, other 10,
coverage-gap 8, complexity 5, duplication 2, maintenance 1. Severity: important 19, suggested 18,
minor 1. Oldest open row `td-012` at 96 days. **0 rows with `class = topology-drift`**, so TT03
correctly withholds its refresh WARN.

**Why no TD01–TD04 rows were warranted:** the single dominant hotspot is a dashboard TSX component
whose score has been flat across three consecutive metrics reports (111,203 in all three) — a
stable, known artifact rather than emerging debt. Coverage is well above floor and rising
(71.7% → 83.2%). There are no cyclic SCCs. Filing rows for these would be the mechanical dump the
LLM-judgment gate exists to prevent.

Canonical entries: `.ai-state/TECH_DEBT_LEDGER.md` (active) / `.ai-state/TECH_DEBT_RESOLVED.md` (terminal).

---

## Decision Health

`adr_health.py --json`: **`withheld: []`** — every oracle answered, so counts are comparable to a
full run and no class was suppressed. **`skipped_terminal`: 16 decisions** excluded by design
(terminal status), so the examined corpus is 303 of 319 finalized ADRs. 1,378 references scanned.

| Class | Count | Sentinel disposition |
|---|---|---|
| `removed-by-self` | 34 | **Not a finding** — the decision working as intended |
| `vanished` | 26 | DH03 INFO advisory (below) |
| `renamed` | 17 | **DH02 WARN** — `update-path`; 8 of them point at `.ai-state/ARCHITECTURE.md` → `.ai-state/DESIGN.md` (the dec-132 rename), 3 at `new_cc_project.sh` → `new_project.sh`, 2 at `commands/new-cc-project.md` → `commands/new-project.md` |
| `placeholder-shape` | 3 | **DH02 WARN** — `fix-entry` |
| `lazy-artifact` | 3 | **Not a finding** — inventory declares expected-absent |
| `out-of-repo` | 2 | **DH02 WARN** — `update-path` (both `~/.claude/CLAUDE.md`) |
| `removed-by-later` | **0** | **DH01 PASS** — no missing supersession edges |
| `reopen_candidates` | **0** | **DH04 PASS** — no retired decision whose subject returned |

**DH02: 22 WARNs**, all mechanical `update-path` / `fix-entry` repairs. The `.ai-state/ARCHITECTURE.md`
cluster (8) is a single rename with one fix. **DH01 = 0 is the notable positive**: no decision's
subject was deleted by a later decision without a supersession link, which is the class that would
indicate a genuinely broken decision graph.

**DH03 (INFO, advisory only — never a backlog to clear):** 26 `vanished` candidates for human
disposition, largest cluster `dec-225` (10 references, the memory-subsystem removal). A decision
may be correct though its example files are gone; `vanished` is the residual after every repair
class. Route via `/decisions` when convenient.

**DH05:** see S1.

---

## Recommended Actions (prioritized)

1. **[I1] Reclaim always-loaded headroom** — 392 tokens under the authoritative basis, negative
   under the sentinel's own. Extract from `swe-agent-coordination-protocol.md` (26,355 chars, the
   single largest always-loaded rule). → `context-engineer`
2. **[I2] Collapse the token-budget divisor to one source** — have T02 cite
   `rules/CLAUDE.md § Token Budget` rather than restate `/3.5`. Fixing this before I1 makes I1's
   verdict unambiguous. → `context-engineer`
3. **[I3] Resolve the sentinel's ledger-write self-contradiction** — six checks currently
   instruct a write two sections forbid. → `context-engineer`
4. **[I4] Correct the always-loaded "byte-equivalent" claim about EC06.** → `context-engineer`
5. **[I5] Add AC13 to `docs/aac-dac.md`** and widen "AC10–AC12" → "AC10–AC13" in three places. → `doc-engineer`
6. **[I9] Restore three catalog entries** — `evidence-appraisal`, `/consult`,
   `/refresh-claude-blocks`. → `doc-engineer`
7. **[I6] Fix F11's inverted comparison** so it stops firing on the healthy post-finalize state;
   consider the same treatment for AC11's permanent fallback WARN. → `context-engineer`
8. **[I7] Refresh 27 staleness markers**, starting with `communicating-agents` (9 sections, 96d)
   and `likec4-querying` (2 sections, 97d). → `researcher` → `context-engineer`
9. **[I8] Quote the 13 unparseable ADR frontmatter values** and add a parseability assertion to
   `check_adr_frontmatter_promotion.py`. → `context-engineer`
10. **[S2/S3/S4] Close the three canary-coverage scope gaps** — one keyword addition, one gate-set
    entry, one function rename. Cheap, and they restore the meta-test's stated contract. → `test-engineer`
11. **[S6/S7] Reconcile the DL04/DL06 draft-stage tension** and fix the single `dec-040`/`dec-204`
    reciprocity break. → `context-engineer`
12. **[S1/S5/S11/S12] Correct four self-description defects** in `agents/sentinel.md` and
    `docs/architecture.md`. → `context-engineer`
