# Sentinel Report

**Run**: 2026-08-06_22-09-15 · **HEAD**: `b35da2c` · **Mode**: orchestrator-managed 5-lens fan-out
**Inventory**: 58 skills · 17 agents · 23 rules · 45 commands · 41 hooks · 327 finalized ADRs (0 drafts) · 8 archived specs · 1 MCP server · 24 active + 109 resolved tech-debt rows

## Ecosystem Health: C

*(0 Critical · 11 Important · 26 Suggested — grade set by the Important band per the Phase 5 table.)*

**Grade scope caveat, stated up front per the Depth Disclosure contract:** this grade did **not** improve because coverage narrowed. Coverage *widened*. All ~126 check IDs across all 22 dimension groups were reached; zero checks are `[not reached]`. The prior run's single Critical was resolved on the merits, moving D → C; the Important count rose 4 → 11 because five scoped lenses verified more deeply than one budget-constrained sweep could, not because the ecosystem degraded.

### Summary

**What is healthy.** The mechanical enforcement layer is clean in a way it has not been before. `check_gate_liveness.py` returns `count: 0` across all four of its classes — `forbidden-pattern`, `uninvoked-gate`, `ambient-import`, and the newly-added `discarded-verdict`. `check_architecture_projection.py` returns `findings: []` with `withheld: []` (16 §3a rows ↔ 16 model elements, published half readable). `check_aac_golden_rule.py` returns `[]`. All six DL checks PASS, all four BC checks PASS, all nine X checks PASS. The token budget measures 23,494 / 25,000 with basis `tokenizer` — a measurement, not an estimate. Agent readiness sits at level 4, above the level-3 production floor, with `mechanical_only: false`.

**Four findings from the prior run are closed on the merits**, each verified rather than assumed:
- **C1** (the sentinel's own audit instrument silently losing completed work) — `maxTurns` is now 300, Phase 1 captures the timestamp exactly once with explicit reuse mandated in Phase 6, and the `[pending]`-skeleton pattern is replaced by a per-dimension incremental-append durability contract.
- **I2** (`supersedes` list-form typing) — the catalog now states `string | list` with a named golden bad-case; Lens 3 iterated all three of `dec-225`'s elements and confirmed each back-link. The PASS is now earned by specification, not by executor judgment.
- **I3** (`check_id_citation_discipline.py` exit 1 on a surface where only exit 2 blocks) — now registered as `commit_gate.sh --blocking …`, which translates the findings exit into the blocking exit. GL06 was added to catch this class and returns 0 because the defect is gone, not because it is blind.
- **I4 / `td-125`** (bash test tier unwired, 2 of 4 suites red) — resolved, and resolved well: the CI job **globs** rather than enumerates, and explicitly closes the wildcard's own vacuous-pass failure mode by asserting a non-zero suite count. Latest `Test` run on `main`: success.

**What needs attention.** Two structural patterns dominate this run, and neither is visible from inside a single dimension — both were only assembled at synthesis:

1. **Catalogued checks whose only specified reader is a tool no agent holds** (see Anomaly A). AC12 and P01–P05 both skip citing *substrate absence*, which reads to a future auditor as "nothing to check." The honest reason is *tool-grant absence*: they are unrunnable in principle by the agent that owns them.
2. **Structure used as a proxy for substance in the catalog's own pass conditions** (see Anomaly B). AC05 and C09 were hardened with explicit "and has content" clauses after prior misses; AC01, AC08, P05 and DL02 were left behind and now sit inconsistently beside their hardened siblings.

Beyond those: `docs/architecture.md` carries two dead paths in the doc whose own stated contract is "code-verified"; the metrics substrate is 76 commits behind HEAD while reading `age_days: 1`; and two commands ship YAML frontmatter that does not parse, with nothing in the repo positioned to catch it.

### Ecosystem Coherence: B

**System-level EC checks:**

| Check | Result | Evidence |
|-------|--------|----------|
| EC01 | PASS | Pipeline diagram in `agents/README.md` resolves 17/17 agents to files 1:1; no diagram-only or file-only names |
| EC04 | PASS | 17/17 pipeline docs have a producer and a consumer; the one apparent gap (`TASK_BRIEF.md`) is orchestrator-produced by design, pre-first-spawn |
| EC05 | WARN | One structural gap — AC12's reader (MCP `query-by-metadata`) is outside every agent's tool grant |
| EC07 | PASS | `check_aac_golden_rule.py --mode=audit` → `[]` over the 10-commit horizon; zero Important findings, hence zero ledger rows |

**Cross-dimension anomalies:**

**A. The unreachable-reader class — two dimensions, one defect, neither lens could see the pair.**
Lens 4 found AC12's only specified reader is MCP `query-by-metadata`, and verified across all 17 `tools:` lines that **no agent in the fleet holds a LikeC4 MCP grant**. Independently, the orchestrator (which *does* hold Task Chronograph MCP) executed P01–P05's substrate and found `get_pipeline_status` returns **only the current session** and `get_agent_events` returns `[]` for every past agent type. So the class has two members and they fail differently: AC12 is unreachable by its owner; P01–P05 is unreachable by *anyone*, because the surface itself carries no history. Both currently present as substrate-absence skips. Per `gate-liveness.md § Existence is not operation`, a gate whose interpreter cannot reach it is indistinguishable from no gate — and a skip reason that names the wrong cause actively conceals that.

**B. Structure-as-proxy-for-substance recurs in the catalog's own wording — 4 instances (GL03).**
AC01 (bare existence, and AC02 then passes *vacuously* on the empty table it admits), AC08 (forbids a Status *column* where the contract is no *unbuilt items* — `docs/architecture.md:165` carries a Status column whose every value is `Built`, satisfying the intent while tripping the letter), P05, DL02. This is the sentinel auditing its own bite and finding it inconsistent.

**C. The enforced surface and the navigational surface have decoupled.**
Every mechanical AaC gate is green (AC10 fence, AC13 projection, EC07 golden rule) because each binds on **element ids and row identity** — none reads the `Key Files` column. Both architecture docs cite `commands/eval.md`, deleted in `b42cc59` (a breaking refactor collapsing `/eval` into `/eval-praxion`) which neither doc followed; `docs/architecture.md` additionally cites a non-existent `eval/src/praxion_evals/behavioral/`. Structure is gated; content is not.

**D. Two hard ceilings are simultaneously ~95% consumed, and the standard remedy is unavailable for one of them.**
Always-loaded context: 23,494 / 25,000 (6.0% headroom). `agents/sentinel.md`: 675 / 700 lines (3.6% headroom). These interact: **Anomaly B's remediation adds substance clauses to a file 25 lines from its own FAIL ceiling**, and T03's exception exists precisely because that file's size is *intrinsic* — so the usual fix (progressive disclosure into `references/`) is the one option that risks a dead gate. This tension is invisible to both contributing lenses and is the run's sharpest structural pressure.

**E. The `_git` fork splits cleanly along a population boundary.**
12 production `_git`/`_run_git` definitions; AST-verified 6 with `timeout=`, 6 without. All four `hooks/` helpers have timeouts; all six timeout-less definitions are in `scripts/`. `finalize_adrs.py` is the crossover — it lives in `scripts/` (no timeout) but executes on the blocking post-merge git-hook path. Filed as `td-134`.

**Artifact coherence distribution:** 17 agents, 58 skills, 45 commands, 23 rules assessed. Below-A artifacts: 2 commands (unparseable frontmatter, N03 FAIL), 1 agent (`architect-validator`, unhedged MCP instruction), 1 agent (`sentinel`, T03 ceiling proximity), 8 agents over T03 warn lines, 2 docs (`docs/architecture.md` AC07 FAIL, `.ai-state/DESIGN.md` AC03 stale cells). Everything else clean.

**Synthesis:** The system's *mechanical* layer is in its best measured state — every deterministic detector returns zero, and four prior findings closed on verified merits rather than assertion. The friction has migrated to three places a script cannot reach: checks whose readers do not exist (A), pass conditions that assert containers rather than contents (B), and prose columns no gate binds on (C). All three share one shape — **the enforcement has outrun the specification it enforces**. That is a healthier failure mode than the reverse, and it is why the coherence grade is B rather than C despite eleven Important findings: no system-level EC check FAILs, and every anomaly has a named, bounded remediation.

### Ecosystem Metrics

| Metric | Value | Trend |
|--------|-------|-------|
| Ecosystem health grade | C | ▲ improving (D → C; prior Critical resolved) |
| Critical findings | 0 | ▲ improving (1 → 0) |
| Important findings | 11 | ▼ up from 4 — deeper coverage, see caveat |
| Suggested findings | 26 | ▲ improving (17 → 26 raw, but 6 prior Suggested closed) |
| Ecosystem coherence | B | ▲ improving (C → B) |
| Always-loaded tokens | 23,494 / 25,000 (`basis: tokenizer`) | ◆ stable — 6.0% headroom |
| `check_gate_liveness.py` findings | 0 (all 4 classes) | ▲ improving |
| `check_architecture_projection.py` | 0 findings, 0 withheld | ◆ stable |
| Agent readiness (`adjusted_level`) | 4 / floor 3 | ◆ stable, above floor |
| Finalized ADRs | 327 (0 drafts) | ▲ growing |
| `architectural` share, recent-50 | 74% vs 84% baseline | ▲ test is discriminating |
| Active tech-debt rows | 24 → 25 (`td-134` filed) | ◆ stable |
| Checks reached | ~126 / ~126 | ▲ full coverage |

### Scorecard

| Artifact | Type | Complete (C) | Consistent (N) | Fresh (F) | Spec (S) | Cross-Ref (X) | Tokens (T) | Coherence (EC) | Overall |
|----------|------|--------------|-----------------|-----------|----------|----------------|------------|----------------|---------|
| `commands/check-experiment.md` | command | A | **C** (N03 FAIL) | A | A | A | B (N06) | A | **C** |
| `commands/run-experiment.md` | command | A | **C** (N03 FAIL) | A | A | A | B (N06) | A | **C** |
| `docs/architecture.md` | doc | A | A | **C** (AC07 FAIL) | A | A | A | B | **C** |
| `agents/architect-validator.md` | agent | A | **B** (N05) | A | A | A | A | A | **B** |
| `agents/sentinel.md` | agent | A | A | A | B (S06 wording) | A | **B** (T03 675/700) | A | **B** |
| `.ai-state/DESIGN.md` | state | A | A | **B** (AC03 cells) | A | A | A | A | **B** |
| `agents/verifier.md` | agent | A | A | A | A | A | **B** (599/700) | A | **B** |
| 6 agents over T03 warn | agent | A | A | A | A | A | **B** | A | **B** |
| `skills/skill-crafting/SKILL.md` | skill | A | A | A | A | A | **B** (418/400) | A | **B** |
| `skills/command-crafting/SKILL.md` | skill | A | A | A | **B** (S05) | A | B (T05) | A | **B** |
| `skills/upstream-stewardship/*` | skill | A | A | **B** (F03) | A | A | A | A | **B** |
| `rules/CLAUDE.md` | rule | A | **B** (N04) | A | A | A | A | A | **B** |
| `rules/ml/eval-driven-verification.md` | rule | A | A | A | **B** (S07) | A | A | A | **B** |
| `.ai-state/decisions/238-*.md` | ADR | A | A | **B** (DH02) | A | A | A | A | **B** |
| All other artifacts | — | A | A | A | A | A | A | A | **A** |

*Check codes reference the dimensions above; Pipeline Discipline (P), Code Health (CH), Technical Debt (TD), Gate Liveness (GL), Decision Log/Health (DL/DH), Spec Health (SH), Calibration (CA), Readiness (RD) and Self-Verification (V) appear in findings only.*

### Depth Disclosure

**Execution mode.** Orchestrator-managed 5-lens fan-out, sanctioned by this agent's own Methodology § Turn Budget clause 4. Dimensions were sharded — not artifacts — so each script runs exactly once across the whole run. One timestamp and one pre-computed inventory were injected into every lens so all five agree on the denominators.

| Lens | Dimensions | Checks | Turns used / 300 | Terminal state |
|---|---|---|---|---|
| L1 | C, N, S, V | 26 | 26 | `[COMPLETE]` |
| L2 | F, X, T | 26 | 25 | `[COMPLETE]` |
| L3 | DL, DH, SH, CA | 22 | 22 | `[COMPLETE]` |
| L4 | GL, AC, BC, EC | 29 | 29 | `[COMPLETE]` |
| L5 | P, TD, TT, CH, RD, PR, HK | 24 | — | API stall; **fragment complete on disk** |
| orchestrator | P01–P03 (MCP-dependent) | 3 | — | filled directly |

**Turn budget: no lens exceeded 10% of its allowance.** The prior 2026-08-05 solo sweep died at turn 100 with only Phase 1 written; this run's heaviest lens used 29 of 300.

**L5's termination was adjudicated against ground truth, not re-run.** Its agent stalled mid-stream on an API error during a cosmetic `%%` escaping fix. Per the completion-handshake rule, a missing terminal marker is a *suspected truncation* to be re-derived from the durable artifact rather than re-executed. The fragment on disk carries verdict rows for all 24 in-scope check IDs, complete Findings sections, the proposed ledger row, and a full Depth Disclosure. No `%%` artifact remains in any fragment. Nothing was lost; nothing was re-run.

**Read in full:** all 5 lens fragments; `agents/sentinel.md`; the prior report's finding tables (orchestrator only, for trend); `.ai-state/TECH_DEBT_LEDGER.md` header and recent rows; `.github/workflows/test.yml` shell-tier job.

**Independently verified by the orchestrator rather than accepted from a lens** — four claims, chosen because each was load-bearing and mechanically falsifiable:
- L1's unparseable-frontmatter claim → confirmed by parsing all 124 frontmatter-bearing artifacts with PyYAML: exactly 2 fail, both named. Root cause refined: the error is the unindented `description: >` block scalar on line 2, not the `argument-hint` line.
- L4's `discarded-verdict: 0` → confirmed *fixed*, not blind: `hooks/hooks.json:211` routes the script through `commit_gate.sh --blocking`.
- L5's `_git` fork → confirmed and **corrected**: 12 definitions, not 11; AST-verified 6 with `timeout=` / 6 without (a text-range scan gave the wrong answer). The population split (hooks all timeout-bearing, scripts mostly not) was found only at this step.
- Prior-run `td-125` → confirmed resolved, with the fix's own vacuous-pass mode closed.

**Sampled, not exhaustive:** CH01 (3-lens scan over 6 files from recent changes, then widened by name across `scripts/` + `hooks/`); T05/T06 judgment across skills; S05/S07 across skills and rules.

**Skipped — and by which cause, since the distinction is itself a finding:**
- **TOOL GRANT (not substrate):** P04, P05 — need Chronograph event streams no agent can reach. AC12 — needs MCP `query-by-metadata`; no agent holds it. *These must not be read as PASS.*
- **TOOL GRANT, then partially recovered:** P01, P02, P03 — the orchestrator holds Chronograph MCP and executed them. P01 (depth ≤ 2) and P02 (delegation↔result pairing) PASS for the live session. P03 was re-derived against `.ai-state/observations.jsonl`, the durable WAL `dec-248` designates, and **FAILS** — see finding I11.
- **SUBSTRATE ABSENT:** TT01–TT05 (`.ai-state/TEST_TOPOLOGY.md` absent — opt-in, absence is not a defect); PR01 (no `PRE_REFACTOR_PLAN.md` under any of 10 slugs); SH06 (no predecessor spec pairs).
- **INERT BY CONFIGURATION:** HK01 (`PRAXION_HACKATHON_MODE` unset).

**Checks marked `[not reached]`: none.** Every ID in the catalog was executed or explicitly skipped with a stated cause.

**Known scope limits that bound a reported zero:**
- **TD02's `cyclic_sccs: 0` describes 11 modules, not the repo.** The `pydeps` collector analysed `__main__` plus `tests.*` against 324 Python files. The zero is true of a narrow slice. L5 deliberately did not widen it — re-deriving the import graph would invent a second basis, the exact failure the token-budget divisor incident encodes.
- **TD06 is `stale`: 76 commits behind HEAD at `age_days: 1`.** `withheld: []`, so commit distance is recoverable and `hotspots_touched` is authoritative. Both barred paths were individually re-verified against current source rather than waved through.
- **SH01's raw signal is 96% false-positive** (84 raw hits, 3 genuine) from bare filenames in matrix cells; AC03/AC07 path resolution yields ~640 false unresolved paths before accounting for row-declared roots and backticked identifiers (verified residual: 4). Both lenses filtered manually and said so.

**Method corrections disclosed by lenses against themselves:** L1 revised three first-pass verdicts after reading source (S06 WARN→PASS, S07 PASS→WARN, N05 PASS→WARN). L2's first T05 pass mis-reported 12 skills as lacking progressive-disclosure targets — a zsh no-match suppressed the whole `ls`; the true count is 1.

### Findings

#### Critical (blocks correct behavior)

| # | Check | Dimension | Location | Finding | Recommended Action | Owner |
|---|---|---|---|---|---|---|
| — | — | — | — | **None.** | — | — |

#### Important (degrades quality or efficiency)

| # | Check | Dimension | Location | Finding | Recommended Action | Owner |
|---|---|---|---|---|---|---|
| I1 | N03 | N | `commands/check-experiment.md:2`, `commands/run-experiment.md:2` | **YAML frontmatter does not parse.** Independently confirmed: of 124 frontmatter-bearing artifacts, exactly these 2 fail. Root cause is the unindented `description: >` block scalar on line 2 — the parser is still inside the folded scalar when it meets the next key. A command whose frontmatter fails to load cannot register its `description`, the sole model-invocation mechanism, and these two carry the longest descriptions in the corpus. **The failure is silent: nothing in the repo parses command frontmatter.** `C04`/`S01`/`S02` grep for field *presence*, which succeeds on a file YAML cannot load — the `gate-liveness.md` "asserts a container exists" anti-pattern exactly | Indent the block-scalar continuation lines (or use a single-line `description:`) and quote `argument-hint`. Then add a real frontmatter *parse* gate covering commands, agents and skills — a canary that feeds an unparseable file and asserts failure | context-engineer |
| I2 | AC12 / EC05 | AC, EC | `agents/sentinel.md:20`, AC12 row, `docs/aac-dac.md:181` | AC12's only specified reader is MCP `query-by-metadata`, and **no agent in the fleet holds a LikeC4 MCP grant** (verified across all 17 `tools:` lines). Unrunnable *in principle*, not dormant for want of data. `docs/aac-dac.md:181` documents the substrate precondition but is silent on reachability, so the day someone populates `req_ids` the check fails on an input it cannot fetch. Invisible to `check_gate_liveness.py` because AC12 is an LLM-tier check with an MCP dependency rather than a script invocation | Give AC12 a Bash-reachable reader (parse the `.c4` source directly, as AC13 already does) **or** record in the AC12 row that it is blocked on a tool grant. Add `req_ids` scaffolding to `claude/aac-templates/` so the convention has a population path | context-engineer |
| I3 | P03 | P | `.ai-state/observations.jsonl` | **The recovery WAL cannot name what it recorded.** 7 `agent_stop` events carry no matching `agent_start`, all landing 1h15m–6h18m after the WAL's first record — so truncation does not explain them. 6 of those 7 carry an **empty `agent_type`**, and 9 of 39 agent-lifecycle events (23%) cannot identify their agent at all. `dec-248` designates this file as the reconciliation WAL whose job is *localizing* a truncated step; a WAL that cannot say which agent stopped cannot localize. Found only because the orchestrator re-derived P03 against the durable substrate after the catalogued one proved historyless | Fix the emitter so `agent_stop` always carries `agent_id` + `agent_type`, and add a canary asserting start/stop pairing. Separately, correct the P dimension's stated substrate: `get_pipeline_status` returns only the live session, so P01–P05 as written can never observe a historical violation | cicd-engineer |
| I4 | AC07 | AC | `docs/architecture.md:73` (Eval framework row, §3a Key Files) | **FAIL** — 2 of 84 §3a paths do not resolve. `commands/eval.md` was deleted in `b42cc59`; only `commands/eval-praxion.md` exists. `eval/src/praxion_evals/behavioral/` does not exist — the real subpackages are `harness/` and `stubs/`. This is the doc whose stated contract is "code-verified" and whose §3a header asserts every row "exists on disk" | `commands/eval.md` → `commands/eval-praxion.md`; replace `behavioral/` with `harness/` and `stubs/` | doc-engineer |
| I5 | N05 | N | `agents/architect-validator.md:73-79` | **An agent is instructed to use tools its frontmatter does not grant.** A 5-item ordered `likec4` MCP procedure ending in `query-by-metadata`, against `tools: Read, Glob, Grep, Bash, Write` with no `mcpServers` key. Unhedged — "Preferred" implies a fallback the text never supplies. Same class as I2; `agents/sentinel.md:284` documents this limitation for AC12 while this agent instructs past it | Add the `likec4` MCP server to the frontmatter, or rewrite 73–79 to lead with the Bash-reachable path and mark the MCP route unavailable-under-current-grant | context-engineer |
| I6 | T03 | T | `agents/sentinel.md` | 675 lines against its own gate-encoding fail ceiling of 700 — **25 lines (3.6%) of headroom** on a catalog that grows monotonically. **Interacts with I8**: that finding's remediation *adds* substance clauses to this file. The T03 exception exists because the catalog is intrinsic, so extraction risks a dead gate — the standard remedy is the unsafe one here | Re-derive the exception ceiling from the catalog's actual row count rather than a fixed 700, **or** extract the catalog to a reference the agent loads at Phase 3 with a canary proving the loaded copy is the executed one |  context-engineer |
| I7 | T06 | T | 9 agents | The task-slug scoping paragraph exists in **two textually distinct variants**. Forked boilerplate is a live drift vector: an edit to one variant silently widens the gap and neither copy is authoritative. Same shape as `gate-liveness.md`'s "a convention lives at two+ textual sites" anti-pattern | Extract the 5 replicated boilerplate blocks to a single referenced source, or at minimum reconcile the two task-slug variants to one string this pass | context-engineer |
| I8 | GL03 | GL | `agents/sentinel.md` AC01, AC08, P05, DL02 rows | **Structure-as-proxy-for-substance, 4 instances** — the sentinel auditing its own bite. AC01 asserts bare existence, and AC02 then passes *vacuously* on the empty component table AC01 admits, so nothing in the dimension catches a heading-only DESIGN.md. AC08 forbids a Status *column* where the contract is no *unbuilt items* — `docs/architecture.md:165` carries a Status column reading `Built`/`Built (M2)`/`Opt-in per project`, satisfying the intent while tripping the letter. P05 and DL02 assert presence where siblings (AC05, C09, S02) were hardened to "present **and non-empty**" | AC01 → "…exists **and carries ≥1 populated §3a component row**". AC08 → "no Status column **in §3a/§3b component tables**, and no row anywhere whose status is Planned/Designed/Proposed/Not built". P05 → append "…and each required section is non-empty". DL02 → align to S02's wording. **Budget against I6 before editing** | context-engineer |
| I9 | TD06 | TD | `.ai-state/metrics_reports/METRICS_REPORT_2026-08-05_22-03-40.*` | The metrics substrate is **76 commits** behind HEAD while reading `age_days: 1` — silent under the 14-day rule. Concretely consequential this run: the report's **worst** coverage entry (`hooks/_hook_utils.py`, 42.86%) is stale by construction — `td-123` brought `hooks/` into `testpaths` on 2026-08-06 and the module measures **100.00%** at HEAD. A sentinel trusting the report would have re-filed `td-122`, already refuted and tombstoned `wontfix` | Run `/project-metrics` before the next TD pass. Days are the wrong staleness unit; commit distance is the load-bearing one | user |
| I10 | TD03 | TD | 16 modules; worst `hooks/worktree_guard.py` (21.28%), `inject_subagent_context.py` (24.55%), `send_event.py` (33.04%) | Re-measured at HEAD: 16 of 106 modules below the 70% floor, **8 of them hooks** — enforcement surfaces, not leaf utilities. `worktree_guard.py` is the `PreToolUse` hook that blocks `Write`/`Edit` resolving outside the session worktree; at 21% its own failure modes are untested, and per the `td-059` precedent a thinly-tested gate risks a silent false-PASS on exactly the defects it exists to catch. **Already tracked by `td-132`** — a progress observation, not a new filing | Address under `td-132`; open no new rows. `td-132` closes only when hook tests are written **and** the aggregate floor is restored from 77 to 80 | orchestrator |
| I11 | CH01 | CH | `scripts/finalize_adrs.py:136` + 5 siblings (see `td-134`) | **12 production `_git`/`_run_git` helpers** independently defined across `scripts/` and `hooks/` with divergent contracts, in a tree that already has a sanctioned shared-primitive home (`scripts/_repo_root.py`, `scripts/_script_cli.py`). AST-verified: 6 pass `timeout=`, 6 do not — and the split is **populational**, all four `hooks/` helpers timeout-bearing, all six timeout-less ones in `scripts/`. `finalize_adrs.py::_git` is the crossover: it lives in `scripts/` but executes ≥6 times on the **blocking post-merge git-hook path**, so a hung or credential-prompting `git` blocks ADR finalize with no bound. Body-hash duplication detection is structurally blind to this — the helpers are not copies, they are a behavioural fork | Extract one `_git` primitive beside `_repo_root.py` with a mandatory timeout and one error contract; migrate the call sites, `finalize_adrs.py` first. **Filed as `td-134`** | implementer |

#### Suggested (improves but not urgent)

| # | Check | Dimension | Location | Finding | Recommended Action | Owner |
|---|---|---|---|---|---|---|
| S1 | AC03 | AC | `.ai-state/DESIGN.md:98`, `:102` | AC03 PASSes on ratio (2/65 = 3.1% vs 50% threshold) but both unresolved paths are real staleness: `commands/eval.md` (deleted in `b42cc59`, same root as I4) and `.cz.toml` — which git history shows **was never created at all**; commitizen config lives at `pyproject.toml:19` `[tool.commitizen]` | Fix both cells | doc-engineer |
| S2 | AC09 | AC | `docs/architecture.md` §3a (14 rows) vs `.ai-state/DESIGN.md` §3a (16 rows) | AC09 PASSes (subset direction is all it requires), but 2 **Built** structural components are absent from the developer guide: `ADR fragments` (`persistence.adrs`) and `Versioning` (`tooling.versioning`). A developer navigating `docs/architecture.md` cannot find the ADR-finalize or release surfaces at all | Add both rows, or record why they are architect-doc-only | doc-engineer |
| S3 | TT06 | TT | `.ai-state/TEST_TOPOLOGY.md` (absent) | All three growth thresholds crossed simultaneously — runtime **155.59 s** (≥90), Built §3a components **16** (≥4), tests **2614** (≥200). The topology protocol would let the implementer run scoped invocations instead of a 2.5-minute full suite at every step | Surface `/refresh-topology --init`. Advisory — the protocol is opt-in and absence is not a defect | user |
| S4 | DH02 | DH | `.ai-state/decisions/238-spec-drift-detect-not-regenerate.md` | `affected_files` lists `.ai-work/<slug>/traceability.yml` — a path **shape**, not a path. `<slug>` can never resolve, so the entry is permanently invisible to every path-resolving consumer (DH, AC03, F01) while occupying a slot that reads as a real reference | Replace with the concrete durable paths the decision governs, or drop it — a gitignored, per-slug, deleted-at-cleanup artifact is not a durable `affected_files` target | context-engineer |
| S5 | DH03 | DH | 22 ADRs (dec-040, 059, 061, 066, 073, 102, 104, 105, 120, 144, 148, 156, 160, 161, 198, 205, 211, 223, 241, 262, 289, 305) | 26 `vanished` references, **concentrated by shape**: 9 point at ephemeral `.ai-work/<slug>/` docs (gitignored and deleted by contract — an authoring-convention issue, not decay); 4 point at the single deleted `dashboard_app/src/components/viz/diagram-viewer.tsx`, one deletion stranding four decisions; ~13 singletons | Disposition in three passes: (a) treat the 9 `.ai-work/` entries as an authoring fix; (b) evaluate the 4 diagram-viewer decisions as one cluster with a shared `retired_by`; (c) judge singletons individually. Never bulk-retire | user |
| S6 | SH01 | SH | `SPEC_diagrams_2026-04-30.md`, `SPEC_project-metrics_2026-04-23.md` | 3 stale live references in traceability/behavioral sections: `.mcp.json` (LikeC4 MCP consolidated into `plugin.json` at `c531fed`), `scripts/git-pre-commit-hook.sh` (removed 2026-06-20), `.ai-state/METRICS_LOG.md` (moved under `metrics_reports/`) | Update the METRICS_LOG path; for the other two, annotate as historical-at-archival — archived specs are records, so dating may be more honest than rewriting | doc-engineer |
| S7 | TD03 | TD | 8 sub-floor scripts incl. `check_metrics_freshness.py` (65.49%) | The deferred sub-floor cluster grew **4 → 8** with no member improving. `td-059`'s own re-evaluation trigger is *"if they drift further **or gain gate-consuming callers**"* — arguably satisfied for `check_metrics_freshness.py`, which backs TD06, whose verdict **barred two paths from TD01 filing this very run**. A false-PASS there silently un-bars stale filings | Decide explicitly: re-affirm the deferral with a note, or file `check_metrics_freshness.py` alone on the gate-consuming-caller clause. Not another silent roll-forward | orchestrator |
| S8 | TD01 | TD | `scripts/project_metrics/report.py` | `td-012`'s subject worsened: re-severitied at **929 lines** against the 800-line ceiling, now **1087** (+17%). No new row (consumers update status in place), but `last-seen` is stalled at 2026-05-17 | Refresh `td-012`'s `last-seen` and note the new size | orchestrator |
| S9 | TD03 | TD | `METRICS_REPORT_2026-08-05_22-03-40.md` | TD03's designated input — a **Lowest-covered files** list in the `### coverage` deep-dive — is absent; only `Line coverage: 0.85` renders, forcing the sanctioned JSON fallback. Aggregate prose also renders the fraction as a percent (*"Line coverage is 0.85%"*). **Already `td-127`, resolved 2026-08-06** — the fix landed after this report was generated | Verify at the next `/project-metrics` run that both render. No new row | orchestrator |
| S10 | P06 | P | `.ai-work/arch-ground-truth/`, `.ai-work/wave1-gap-closure/` | Both carry `SYSTEMS_PLAN.md` (Standard/Full implied) with no `TASK_BRIEF.md`. Historical slugs, so retrospective. Worth noting the append has **no enforcing hook**, so the obligation lapses silently — the same failure shape CA03 exists to catch for the calibration log | Backfill or clean via `/clean-work` | user |
| S11 | F03 | F | `skills/upstream-stewardship/SKILL.md:79` + 2 references, `skills/context-security-review/SKILL.md:130` | 4 sites cite `.ai-state/memory.json` / Memory-MCP `remember()` as live; `dec-225` removed the subsystem. Low blast radius (all four are *defensive* redaction rows guarding an artifact that can never appear) but they imply a subsystem that is gone. 8 sibling sites were correctly reconciled; these 4 were missed | Remove or reword to the historical form. Also review `hooks/test_send_event.py:94` | context-engineer |
| S12 | N04 | N | `rules/CLAUDE.md:15` vs `:21` | The always-loaded predicate is stated two ways six lines apart: `:15` "rules **without frontmatter**"; `:21` "rules without **`paths:`** frontmatter". Only `:21` matches disk — 23/23 rules carry frontmatter. Read literally, `:15` says zero rules load unconditionally | Reword `:15` to key on `paths:` absence, matching `:21` and BC01's predicate | context-engineer |
| S13 | N03 | N | `rules/*.md` (23 files) | Rules carry three top-level keys — `core:`, `load:`, `install:` — documented in **no field table anywhere**. Usage is 23/23 consistent, so this is undocumented convention rather than drift, but a new author has no spec to copy and the nested-`codex.load` / top-level-`load` collision is a live trap | Add a frontmatter field table to `skills/rule-crafting/SKILL.md`, as `agent-crafting` and `skill-crafting` already do | context-engineer |
| S14 | S06 | S | `agents/sentinel.md` S06 row | Pass condition says "numbered **phases**", but the repo uses three equally rigorous vocabularies: `Phase N` (15 agents), `Step N` (`implementer`), `Round N` (`discipline-consultant`). A literal grep false-flags two agents that fully satisfy the intent | Reword to "numbered stages (Phase / Step / Round)" so the check tests substance rather than one label | context-engineer |
| S15 | S07 | S | `rules/ml/eval-driven-verification.md` | Two executable 4–5-step procedures, the second openly labelled "identical to `val_bpb` path" and then restated verbatim. The rule already points at `skills/llm-training-eval/SKILL.md` for the methodology. Path-scoped (`core: false`), so no always-loaded cost — hence Suggested | Move the procedures into the skill; keep threshold syntax, classification semantics and priority rules in the rule | context-engineer |
| S16 | N06 | N | `commands/full-security-scan.md:3`, `project-coverage.md:3`, `resume-rework.md:4` | Three commands declare an **empty** `argument-hint` (one YAML-null, two `""`) — occupying the field without telling a user anything, which is its only purpose | Remove or populate | context-engineer |
| S17 | N06 | N | `commands/check-experiment.md`, `run-experiment.md`, `refresh-topology.md` | Description style is bimodal: 42/45 single-line, these three use 10–14-line `>` block scalars. Not wrong — but two of the three are I1's parse failures, and the length is *why*: it pushed the malformed region below the eye-line | Cap command descriptions at ~3 lines; move overflow to the body, which the model reads after invocation anyway | context-engineer |
| S18 | T03 | T | 7 agents: verifier 599, systems-architect 462, implementation-planner 445, context-engineer 344, skill-genesis 336, promethean 311, researcher 307 | Over warn lines, none over fail. verifier (599/700) is second-closest to a ceiling after sentinel | Monitor; prefer progressive disclosure over inline growth on the next edit | context-engineer |
| S19 | T01 | T | `skills/skill-crafting/SKILL.md` | 418 lines, 18 over the warn line. Already carries 9 `references/` files — the pattern is applied, the body has simply outgrown it | Move a section to `references/` on the next substantive edit | context-engineer |
| S20 | T05 | T | `skills/command-crafting/SKILL.md` | 273 lines with zero `references/` and zero `contexts/` — the only skill over 250 lines with no progressive-disclosure target; its sibling crafting skills all split | Split deep-detail sections into `references/` when next edited | context-engineer |
| S21 | T02 | T | always-loaded corpus | PASSES with 1,506 tokens (6.0%) headroom. Not a failure — recorded because the margin is thin enough that one always-loaded rule addition could consume it | Re-measure with `scripts/measure_token_budget.py` before adding any unconditional rule | user |
| S22 | DL04/DL06 | DL | `.ai-state/decisions/225-remove-in-house-memory-subsystem.md` | **Gate-liveness confirmation, not a defect.** `supersedes: [dec-009, dec-025, dec-039]` is the corpus's only multi-element supersession and the exact golden bad-case both checks are written against. All three elements iterated; all three resolve; all three targets set `superseded_by: dec-225`. A head-only reader would have returned PASS *for the wrong reason* | No action. Recorded so a future run that silently regresses to head-only iteration is detectable by this note's absence | — |
| S23 | DL02 | DL | `.ai-state/decisions/` (155 architectural ADRs before 2026-07-01) | `dissent:` is schema-required for `category: architectural`; 155 of 230 lack it. **Verified fully historical**: adherence is **51/51 (100%) since 2026-07-01** and 5/5 since 2026-08-01 | No backfill (consistent with the no-retroactive-migration stance). Recorded so the 75/230 corpus ratio is not misread as live drift | — |
| S24 | DH05 | DH | `.ai-state/decisions/` corpus | The `architectural` category is **discriminating better than at adoption**: recent-50 share fell from the 84% baseline to **74%**. The post-adoption window is stronger still (0.222) but rests on **n = 9**, well under the 50-window, so it is reported rather than relied on | No action; re-measure next run. Treat 74% as the live signal | — |
| S25 | TD02 | TD | `pydeps` collector scope | `cyclic_sccs: 0` covers **11 modules** (`__main__` + `tests.*`) against 324 Python files. A true statement about a narrow slice, not a whole-repo guarantee | Widen the collector's scope, or annotate the metric so the zero is not over-read | orchestrator |
| S26 | CH01 | CH | `scripts/finalize_adrs.py` ↔ `finalize_adrs_backlinks.py` | The back-reference is `TYPE_CHECKING`-guarded — the correct pattern. **Not** a cycle, not filed. Recorded so a future scan does not re-raise it | No action | — |

### Pipeline Discipline

Partially recovered by the orchestrator; the sentinel's own tool grant cannot reach this dimension.

| Check | Result | Evidence |
|---|---|---|
| P01 | PASS (live session only) | Delegation-depth distribution `{1: 5}` — all 5 lenses at depth 1 from `main_agent`. Zero chains deeper than 2 |
| P02 | PASS (live session only) | 5 `delegation` interactions, 5 matching agent records. No unmatched delegation |
| P03 | **FAIL** | Re-derived against `.ai-state/observations.jsonl`. 7 orphaned `agent_stop` events inside the WAL window; 6 carry an empty `agent_type`; 23% of lifecycle events cannot identify their agent. See I3 |
| P04 | SKIP — tool grant | Requires Chronograph event streams |
| P05 | SKIP — tool grant | Requires Chronograph pipeline event data |
| P06 | WARN ×2 | `arch-ground-truth`, `wave1-gap-closure` — `SYSTEMS_PLAN.md` present, `TASK_BRIEF.md` absent. See S10 |
| P07 | PASS | No `INTERFACE_DESIGN.md`/`TRANSACTIONS_DESIGN.md` under any of 10 slugs. Two CONSULT fragments in `wave1-gap-closure`; **all 12 `### CH-NN` entries carry a populated per-entry `**Disposition:**`** (8× `switch-now`, 4× `defer-with-rationale`). No placeholder survives, no field omitted, no summary-table substitution |
| P08 | PASS | `clean_work_safety.py` `stale_safe = 0` (threshold ≥3). 8 SAFE dirs, oldest idle 8 days |

**A note on the dimension's specification.** `get_pipeline_status` returns only the current session and `get_agent_events` returns `[]` for every past agent type. P01–P05 as catalogued can therefore never observe a historical violation — they always measure an in-flight session, which is trivially clean. The substrate that *does* carry history is `observations.jsonl`. This is a specification defect, not a data gap; recorded under I3.

### Tech-Debt Findings

**New rows filed this run: 1** (`td-134`, class `duplication`). **Rows proposed and declined: 0.** **TD05 discipline issues: 0.**

- **`td-134`** — the `_git` fork. **Why filed:** this is not a threshold breach dressed as debt. AST body-hashing found *nothing*, which is precisely why it warrants a row: the helpers are not copies but a **behavioural fork**, structurally invisible to duplication detection, and the fork has already produced a concrete asymmetry on a live enforcement path. The consequence is named and bounded — an unbounded `git` call inside the blocking post-merge hook — rather than a generic duplication claim. The repo already ships the remedy's shape (`_repo_root.py`, `_script_cli.py`), so the fix is a migration, not a design.
- **TD06 clearance:** not applicable, deliberately. No named module appears in `hotspots_touched`; the finding derives from reading current source at HEAD, not from the metrics report, so it is immune to the 76-commit staleness gating TD01.
- **Dedup adjudication:** closest active row is **`td-056`** (`suggested`/`duplication`) — same class and fix-direction, **different subject**: one byte-identical function pair inside canonical-block tooling whose risk is *future* divergence. `td-134` is twelve modules whose divergence has **already occurred** and reaches the post-merge hook. Filed as new rather than widening `td-056`, because folding a concrete unbounded-subprocess risk into a `suggested` cosmetic row would bury it. Four resolved rows (`td-004`, `td-020`, `td-041`, `td-054`) mention `_git` in note prose only; none carries it as `location`.
- **TD01–TD04 filed 0 rows.** All 10 hotspots and all p95 crossings were dispositioned as already-tracked (`td-115`, `td-058`, `td-012`, `td-132`), TD06-barred-and-re-verified, or explicitly rejected by name in `td-115`'s own filing note. Both TD06-barred paths were individually re-verified: `fitness/tests/test_discipline_registry_invariants.py` is already carried by `td-105`/`td-084`/`td-098`; `scripts/adr_health.py` is 601 lines (under the 800 ceiling) at 84.7% coverage — judged not debt-worthy on re-verification, not merely deferred.
- **Staleness WARN (TD06):** the metrics substrate is 76 commits behind HEAD. Findings were produced from available data and re-measured at HEAD where it mattered (TD03). See I9.

Forward-pointer: `.ai-state/TECH_DEBT_LEDGER.md` holds the canonical entry.

### Recommended Actions (prioritized)

1. **Fix the two unparseable command frontmatters, then add a frontmatter *parse* gate** (I1). Highest leverage: it repairs live artifacts *and* closes the gap that let them ship. A presence-grep cannot see a file YAML rejects. — `context-engineer`
2. **Repair `docs/architecture.md`'s two dead §3a paths, and `.ai-state/DESIGN.md`'s two stale cells** (I4, S1, S2). Same root cause (`b42cc59`); fix together. — `doc-engineer`
3. **Extract the `_git` primitive with a mandatory timeout, `finalize_adrs.py` first** (I11 / `td-134`). The post-merge hook path is the concrete exposure. — `implementer`
4. **Resolve the unreachable-reader class** (I2, I5): give AC12 a Bash-reachable reader or declare it grant-blocked; hedge or grant `architect-validator`'s MCP procedure. — `context-engineer`
5. **Fix the observations WAL emitter and correct the P dimension's stated substrate** (I3). `dec-248`'s recovery design depends on it. — `cicd-engineer`
6. **Budget I6 and I8 together, in that order.** Decide the sentinel's size strategy *before* adding four substance clauses to a file 25 lines from its FAIL ceiling. Re-deriving the ceiling from the catalog's row count is the smaller change. — `context-engineer`
7. **Run `/project-metrics`** to refresh a substrate 76 commits stale before the next TD pass (I9). — `user`
8. **Decide `td-059`'s deferral explicitly** rather than rolling it forward a third time (S7) — its own gate-consuming-caller trigger is arguably met. — `orchestrator`
9. **Reconcile the forked task-slug boilerplate** across 9 agents (I7). — `context-engineer`
10. **Consider `/refresh-topology --init`** — all three growth thresholds are crossed and the full suite now costs 155 s per step (S3). — `user`
11. **Disposition the 26 `vanished` ADR references in three shaped passes**, never in bulk (S5). — `user`
12. Remaining Suggested items (S6, S8–S26) as capacity allows.
