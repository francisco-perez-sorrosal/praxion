# Sentinel Report

**Run**: 2026-08-07_14-00-50
**Method**: 5-lane orchestrated fan-out (see Depth Disclosure)
**Coverage**: 125 of 125 catalog checks executed — **0 `[not reached]`**

## Ecosystem Health: C

### Summary

The ecosystem is structurally sound and **connectively** healthy: every system-level coherence
check passes, no artifact is orphaned, the pipeline diagram matches the agent inventory 1:1,
`plugin.json` registers exactly the 17 agents on disk, and no check in any dimension produced a
**Critical** finding. All 58 skills carry progressive disclosure; the always-loaded token budget
is within ceiling.

What needs attention is not the ecosystem's *structure* but its **verification layer**. The
dominant pattern this run — spanning four dimensions and surfaced independently by three of five
lanes — is **gates that cannot bite**: checks whose pass condition is satisfied by the absence of
the thing they exist to measure. Four instances are named below, including the sentinel's own
meta-check. Each is a one-line fix; collectively they mean several dimensions have been reporting
PASS without evidence, in one case since 2026-04-17.

Two secondary patterns: **derived content drifting from its source** (six instances, including
`agents/sentinel.md` violating its own anti-count mandate six lines away from stating it), and
**unenforced obligations lapsing silently** (TASK_BRIEF.md absent in 2 of 2 eligible slugs — a
100% lapse rate on an unconditional Standard/Full requirement).

**Comparison to last run** (2026-08-06 22:09:15 — C, 0/11/26, coherence B): health grade
**stable at C**. Important findings rose 11 → 15 and Suggested fell 26 → 21. The rise is partly
newly measurable substrate rather than new decay: `hooks/` joined `testpaths` on 2026-08-06,
making 41 hook modules coverage-visible for the first time and accounting for one Important
finding outright. Coherence is downgraded B → C on cross-dimension pattern, detailed below.

### Ecosystem Coherence: C

**System-level EC checks:**

| Check | Result | Evidence |
|-------|--------|----------|
| EC01 | PASS | Pipeline-diagram agent names in `agents/README.md` match `agents/*.md` 1:1 (17/17) |
| EC04 | PASS | Every pipeline output document has both a producing and a consuming agent; no dead ends |
| EC05 | PASS | No structural gaps against the stated purpose in `CLAUDE.md` and Future Paths |
| EC07 | PASS | `check_aac_golden_rule.py --mode=audit --json` returned `[]` over the 10-commit horizon |

**Cross-dimension anomalies:**

1. **Vacuous or unfireable gates — 4 instances across 4 dimensions.** This is the significant
   pattern that sets the grade.
   - `SH04` (Spec Health) — `SPEC_design-dialectic_2026-04-17.md` has **no test column**, so a
     grep for the token `UNTESTED` cannot match. 15/15 REQs carry zero test reference and the
     check has passed vacuously since 2026-04-17.
   - `V04` (Self-Verification) — the sentinel's own meta-check is presence-only. A
     `## Check Catalog` heading with zero rows satisfies it.
   - `TT04` (Test Topology) — 13 of 13 topology groups declare a measured
     `expected_runtime_envelope`; 0 of 10 metrics reports emit per-group timings. A declared
     threshold with no oracle, reporting PASS-by-absence.
   - `AC12` (Architecture) — blocked twice: substrate unpopulated (0 `req_ids` in the `.c4`, 0 of
     8 SPECs with `architectural_elements:`) **and** its reader is an MCP tool outside the
     sentinel's grant. Populating the convention alone moves it to "unreachable reader", not
     "passing".

   Two further instances raised by the lanes — `DL02` lacking a non-empty clause and `AC08`
   firing on correct content — were **withdrawn at reconciliation**: both were already fixed in
   the working tree by commit `85033b4`, and the lanes saw the pre-fix text because their
   embedded catalog is one commit stale (see S-01a and Depth Disclosure).

2. **Derived content drifting from its source — 6 instances.** `agents/sentinel.md:49` restating
   a check count its own line 55 forbids (and already off by one); two always-loaded files
   disagreeing on whether a memory backend exists; pipeline deliverables enumerated twice in
   always-loaded content and diverged; two `skills/README.md` rows contradicting their skills'
   frontmatter (one contradicting the same file 107 lines later); six `dec-draft-<hash>` ids in
   the idea ledger unresolvable because the finalize walk scope omits `.ai-state/idea_ledgers/`.

3. **Unenforced obligations lapsing silently — 3 instances.** `TASK_BRIEF.md` absent in 2 of 2
   eligible slugs (100%); 144 `agent_stop` WAL records minting an `agent_id` matching no start;
   calibration-log tier cells carrying execution-mode annotations contra the taxonomy. All three
   share one shape: an obligation with no enforcing hook.

**Artifact coherence distribution:** 4 artifacts (`skills/adapt-claude-to-agents/`,
`skills/likec4-querying/`, `skills/stakeholder-communications/`, `commands/star-repo.md`) are
anchored only in their own catalog file. **Not orphans** — skills activate by description match
and `/star-repo` is user-invoked — but they are the least-reachable surfaces and the first place
a silent decay would appear. All other artifacts have ≥1 behavioral anchor. EC02: PASS.

**Synthesis:** The ecosystem works as a connected whole — every EC check passes, and Lane B's
provisional grade on that evidence alone was **A**. The downgrade to **C** comes entirely from
folding in cross-lane anomalies, which is the reconciliation step no single lane can perform.
The friction is not in the connections between artifacts; it is that the layer meant to *verify*
those connections has holes in four of its dimensions. An ecosystem whose gates pass without
evidence is indistinguishable, from the inside, from one that is genuinely clean — and that
indistinguishability is precisely the cost.

### Ecosystem Metrics

| Metric | Value | Trend |
|--------|-------|-------|
| Catalog checks executed | 125 / 125 | ■ stable |
| Checks unreached | 0 | ■ stable |
| Artifacts audited | 513 | ▲ (basis stated in Depth Disclosure) |
| Critical findings | 0 | ■ stable (0 prior) |
| Important findings | 15 | ▲ (11 prior — partly new substrate; see Summary) |
| Suggested findings | 21 | ▼ (26 prior) |
| Findings withdrawn at reconciliation | 2 | — (stale-catalog artifacts; see S-01a) |
| Skills with progressive disclosure | 58 / 58 | ■ stable |
| Agents registered vs on disk | 17 / 17 | ■ stable |
| Finalized ADRs | 330 | ▲ |
| Unfinalized ADR drafts | 0 | ■ stable |
| WAL records examined | 22,171 | ▲ |
| Unpaired `agent_start` (P03) | 113 (74 one workflow class) | ▲ worst rate this month |
| Unmatched `agent_stop` (P03) | 144 | ▲ 39% of stops in 2026-08 |
| Modules below 70% coverage floor | 14 (aggregate 84.47%) | ▲ 5 newly measurable |
| Tech-debt rows filed this run | 2 | — |

### Scorecard

Rows are emitted only for artifacts carrying a non-PASS result in some dimension; the remaining
~500 artifacts scored A across every dimension their lane examined and are not enumerated.

| Artifact | Type | Complete (C) | Consistent (N) | Fresh (F) | Spec (S) | Cross-Ref (X) | Tokens (T) | Coherence (EC) | Overall |
|----------|------|--------------|----------------|-----------|----------|---------------|------------|----------------|---------|
| `agents/sentinel.md` | agent | A | **C** (N05) | A | A | A | B (T03 warn) | A | **C** |
| `skills/README.md` | catalog | A | A | A | A | **C** (X07 ×2) | A | A | **C** |
| `.ai-state/idea_ledgers/IDEA_LEDGER.md` | state | A | A | A | A | **C** (X04) | A | A | **C** |
| `.ai-state/specs/SPEC_design-dialectic_2026-04-17.md` | spec | B (SH04) | A | A | A | A | A | A | **B** |
| `docs/architecture.md` | doc | A | A | A | A | B (AC09) | A | A | **B** |
| `~/.claude/CLAUDE.md` | global cfg | A | **C** (N05) | A | A | A | B (T06) | A | **C** |
| `rules/swe/swe-agent-coordination-protocol.md` | rule | A | B | A | A | A | B (T06) | A | **B** |
| `skills/skill-crafting/SKILL.md` | skill | A | A | A | A | A | B (T01, 418 ln) | A | **B** |
| `agents/implementer.md` | agent | A | A | A | B (S06) | A | B (T03) | A | **B** |
| `commands/check-experiment.md` | command | A | B (N06) | A | A | A | A | A | **B** |
| `skills/agent-crafting/SKILL.md` | skill | A | B (N04) | A | A | A | A | A | **B** |
| `commands/` (5 files, S04) | command | A | A | A | B (S04) | A | A | A | **B** |
| `agents/*.md` (8 files, T03 warn band) | agent | A | A | A | A | A | B | A | **B** |
| `.ai-state/calibration_log.md` | state | A | B (CA02) | A | A | A | A | A | **B** |
| `.ai-state/TEST_TOPOLOGY.md` | state | A | A | A | A | A | A | B (TT04) | **B** |
| `hooks/` (5 modules, TD03) | code | — | — | — | — | — | — | — | **C** (coverage) |
| `tests/test_ci_autofix_hub_invariants.py` | code | — | — | — | — | — | — | — | **C** (1,712 ln) |

Check codes reference the catalog dimensions; Pipeline Discipline (P), Code Health (CH),
Technical Debt (TD), Gate Liveness (GL), Decision Health (DH) and Self-Verification (V) appear in
Findings only, per the report schema.

### Depth Disclosure

**A grade without its scope is a claim without a warrant.** This run's scope, stated plainly.

**Execution method — orchestrated 5-lane fan-out.** `agents/sentinel.md:49` states a full sweep
does not reliably fit one turn budget and instructs scoped instances to honour their lens
strictly. The orchestrator partitioned the 125 checks into 5 disjoint lanes by substrate
affinity, ran them concurrently as `i-am:sentinel` instances, and reconciled their fragments.
Every lane confirmed strict scope adherence; no check was executed twice, and no check was
executed by no one.

| Lane | Dimensions | Checks | Result profile | Fragment |
|---|---|---|---|---|
| A | C, N, S, T | 28 | 22 PASS, 4 WARN, 1 FAIL, 0 SKIP | `SENTINEL_FRAGMENT_A.md` |
| B | F, X, EC | 27 | 24 PASS, 2 WARN, 0 FAIL, 1 SKIP | `SENTINEL_FRAGMENT_B.md` |
| C | DL, DH, SH, CA, RD | 23 | 20 PASS, 1 WARN, 0 FAIL, 1 SKIP, 1 advisory | `SENTINEL_FRAGMENT_C.md` |
| D | P, TD, TT, CH, PR, HK | 21 | verdict on all 21 | `SENTINEL_FRAGMENT_D.md` |
| E | AC, GL, BC, V | 26 | 23 PASS, 2 WARN, 1 SKIP | `SENTINEL_FRAGMENT_E.md` |

Fragments are ephemeral (`.ai-work/sentinel-full-sweep/`, gitignored); this report is the durable
record.

**Checks `[not reached]`: none.** All 125 catalog check IDs reached a verdict.

**The audit ran against a catalog one commit newer than the one its agents carry — disclosed
because it demonstrably affected two findings.** The installed plugin is `i-am` **v0.22.0**; the
working tree is one commit ahead of it (`85033b4 feat(gates): close the detection gaps the
sentinel audit surfaced`, 2026-08-07 00:21, landing 27 minutes after the v0.22.0 release). The
delta is narrow and fully enumerated: **every one of the working tree's 125 checks is present in
the installed copy**; the installed copy carries **two extra rows, `P01` and `P02`**, retired in
that commit; and **11 rule texts** are pre-hardening — `AC01`, `AC08`, `AC09`, `AC12`, `DL02`,
`P03`, `P04`, `P05`, `S06`, `T03`, `TD02`.

**Consequence, stated rather than assumed.** Findings about the *repository* are unaffected — the
lanes read repository state directly. Findings about *the catalog's own rule text* are not: Lane
E filed `DL02` (missing non-empty clause) and `AC08` (over-broad Status-column prohibition)
against text `85033b4` had already replaced. Both are **withdrawn** above. Conversely, Lane A's
`S06` and Lane E's `AC09` reached conclusions matching the newer on-disk rule despite carrying
the older text, because both verified against source. The general lesson: in a self-hosting
checkout, a sentinel's verdicts on *its own definition* inherit the installed copy's staleness,
while its verdicts on everything else do not.

**This paragraph replaces an incorrect earlier version of itself.** The first reconciliation
identified the installed copy as v0.20.0 with 118 checks, claiming 7 missing checks and 29
diverged rules. That was wrong: the version was selected by a `find | head -1` across 24 cached
version directories, which returned a stale sibling rather than the active install. The
corrected comparison above is against v0.22.0. The original claim is retained here, struck, so a
reader of this report is not left reconciling it against the record. Downgraded from Important
to Suggested as **S-01a**.

**Read in full:** all 58 `SKILL.md` frontmatter blocks and bodies for disclosure structure; all
17 agent definitions; all 23 rule files; `plugin.json`; both `CLAUDE.md` layers; `.ai-state/DESIGN.md`
§3a (16 rows) and §3b; `docs/architecture.md`; `.ai-state/TEST_TOPOLOGY.md` (869 lines, 13/13 YAML
blocks parsed); both tech-debt ledgers (26 + 111 rows, field-parsed); `METRICS_REPORT_2026-08-07_07-41-41`
and its JSON sibling including all 110 `coverage.per_file` entries; all 8 spec traceability
matrices (112 tokens).

**Enumerated exhaustively, not sampled:** all **22,171** WAL records (0 unparseable) for P03/P04;
all **14** `.ai-work/` slugs; all **13** topology groups; all **10** hotspots; all **10**
`METRICS_REPORT_*.json`; all **330** ADRs for the DL dimension; **313** ADRs for DH
(17 `skipped_terminal` by design; `withheld: []`, so the published verdict is complete, not a
suppressed remainder); all **62** calibration rows.

**Sampled, and how — these are bounded claims:**
- **SH03 (spec↔code fidelity) judged 3 of 8 specs**, selected by recency. The five specs dated
  2026-04-17 → 2026-05-11 were **not** judged for spec-to-code drift. This is the largest
  coverage gap in the run.
- **CH01 sampled 23 implementation files** — every non-test `.py` under `scripts/` or `hooks/`
  touched in the last 15 commits. Duplication between two files both stable for months lies
  outside what CH01 examined.
- **DL04/DL05/DH03 LLM judgment** read 4 ADR bodies in full against mechanical scans of all 330.

**Skipped, each naming its reason:**
- `SH06` — no spec has a predecessor; delta comparison is undefined. *Substrate absent.*
- `F06` — no project `compose.yaml`/`Dockerfile` exists. *Substrate absent.*
- `AC12` — **two** blocks, both stated: the bidirectional convention is unpopulated (*substrate
  absent*) **and** its `query-by-metadata` reader is an MCP tool outside this agent's
  `Read, Glob, Grep, Bash, Write` grant (*reader unreachable*). Naming only the first would
  conceal the second.
- `TT04` — no per-group runtime data in any of 10 reports. *Substrate carries no history.*
- `TT06` — correctly inactive; `.ai-state/TEST_TOPOLOGY.md` exists.
- `AC11`, `P01`, `P02` — retired; not run by design.

**Bounds on what a passing check can mean here:**
- **P04 claims surface, never semantics.** WAL records name tools and file paths, not intent.
  An implementer making a design decision inside a file it was legitimately assigned emits
  records indistinguishable from correct work. P04 cannot see that, and its PASS must be read
  as "no out-of-grant tool use and no cross-owner writes", not "agents behaved correctly".
- **Task Chronograph is *reader-unreachable*, not merely unused** — no agent in the fleet holds
  an MCP grant for it. P03/P04 ran against `observations.jsonl`, the only durable agent-event
  history reachable from this tool grant.
- **TD02's "0 cyclic SCCs" covers 80.6% of the repo's Python** — 139 of 206 modules across the
  pydeps package roots; `hooks/` is not a package root, so 67 files (19.4%) sit outside the
  import graph entirely. The zero is a bounded claim, not a repo-wide all-clear.
- **TD03 was read from `coverage.per_file` in the JSON, not the markdown list.** That list shows
  10 of 110 and states its bound as "all at or above 65.66%" — *below* the 70% floor, therefore
  not exhaustive for this floor. The JSON yields 14 sub-floor modules, 4 more than the list
  could show.
- **T02 basis**: `measure_token_budget.py --json` — report the `basis` field alongside the
  number; a run without `ANTHROPIC_API_KEY` is a labelled estimate erring ~5% high, not a
  measurement.

**Artifact count basis (513).** 58 skills + 17 agents + 23 rules + 45 commands + 40 hooks +
330 finalized ADRs. Each catalog directory's own `CLAUDE.md` and `README.md` are excluded, per
the Phase 2 inventory convention — including them inflates every family by exactly 2 and is how
this run's own pre-flight inventory initially over-counted skills, agents, rules, commands and
drafts. Prior runs' figures (507–519) may use a different composition; this basis is stated so
the trend is interpretable rather than assumed comparable.

**No coverage was narrowed for turn budget.** No grade in this report improved by looking at
less.

### Findings

#### Critical (blocks correct behavior)

| # | Check | Dimension | Location | Finding | Recommended Action | Owner |
|---|-------|-----------|----------|---------|--------------------|-------|
| — | — | — | — | *No Critical findings across all 125 checks.* | — | — |

#### Important (degrades quality or efficiency)

| # | Check | Dimension | Location | Finding | Recommended Action | Owner |
|---|-------|-----------|----------|---------|--------------------|-------|
| I-02 | SH04 | Spec Health | `.ai-state/specs/SPEC_design-dialectic_2026-04-17.md:39-58` | **Traceability matrix has no test column**, so the check's grep for `UNTESTED` cannot match. Columns are `REQ-DDL \| Satisfying artifact(s) \| Kind`, where `Kind` holds `Runtime`/`Implementation-time` — *when* a requirement applies, not *what verifies it*. 15/15 REQs carry zero test reference; the gap has been invisible since 2026-04-17. The other 7 specs carry a verification column with 0 empty cells across 104 rows, so the omission is anomalous, not conventional. | Add a test/verification column; for any REQ legitimately untestable, write an explicit `no test — <rationale>` cell rather than omitting the column. Separately amend SH04 to assert the column *exists* before asserting its cells are populated. | doc-engineer (matrix); context-engineer (SH04 clause) |
| I-03 | P03 | Pipeline | `hooks/capture_session.py`, `hooks/send_event.py`, `hooks/_hook_utils.py` | **144 `agent_stop` records carry an `agent_id` matching no `agent_start` anywhere in the WAL** (0 of 144 matched), all with `agent_type: ""` and `summary: "Agent completed: unknown"`; 104 are `project: praxion`. Correlation is exact — all 144 empty-`agent_type` stops are unmatched. Front-truncation is **falsified**: 0 unmatched stops predate the earliest start. Rate at its historical worst in 2026-08 (39% of stops). Every agent-lifecycle metric derived from this WAL silently under-counts completions. | Fix the stop-side hook to resolve the spawning agent's `agent_id`/`agent_type` rather than minting a fresh one. Until then treat WAL-derived duration and completion stats as lower bounds. | implementer |
| I-04 | P03 | Pipeline | GitHub Actions self-healing workflow surface | **`workflow-subagent`: 109 starts, 35 stops** — 74 of the repo's 113 unmatched starts (65%). The harness ends the job without firing the Stop hook, so two thirds of the unmatched-start population is one workflow class. Residual rate excluding it: 7.9%. | Emit `agent_stop` from an `always()`-guarded final workflow step, or exclude `workflow-subagent` from P03 pairing with a documented rationale so the residual signal stays readable. | cicd-engineer |
| I-05 | P06 | Pipeline | `.ai-work/arch-ground-truth/`, `.ai-work/wave1-gap-closure/` | **`TASK_BRIEF.md` absent in 2 of 2 slugs carrying a `SYSTEMS_PLAN.md`** — a 100% lapse rate over the eligible population (14 slugs total, 12 correctly not evaluated). The Intake Clarity Gate's *unconditional* Standard/Full obligation has no enforcing hook, so it lapses silently — the same shape as CA03's calibration-append lapse. | Either add a pre-spawn check refusing the first agent spawn without a `TASK_BRIEF.md`, or downgrade the obligation to conditional and say so in the coordination protocol. Silent 100% non-compliance is the worst of both. | context-engineer |
| I-06 | P05 | Pipeline | `.ai-work/arch-ground-truth/SYSTEMS_PLAN.md` | **0 of the 8 canonical `##` sections** (`agents/systems-architect.md:323-379`) appear; the document uses a bespoke numbered set, and `VERIFICATION_REPORT.md` in the same slug does the same. A consumer grepping `## Acceptance Criteria` or `## Codebase Readiness` finds nothing, against an architect Phase 10 rule to write the skeleton at Phase 1. | Confirm whether `baseline-audit` mode licenses a different schema. If yes, document the alternate schema in the agent definition; if no, this is a contract breach warranting an explicit instruction. | context-engineer |
| I-07 | TD03 | Tech Debt | `hooks/auto_complete_install.py` + 4 siblings | **5 hook modules below the 70% floor**, worst **38.07%** (75/197 — the lowest-covered file in the repository). Invisible until 2026-08-06, when `hooks` joined `testpaths` (`pyproject.toml:86-101`); this is their first measurement. The 84.47% aggregate clears the floor and conceals every one of them — TD03's golden bad-case, reproduced exactly. | Filed as **`td-139`**. Write tests for uncovered branches, worst-first. | test-engineer |
| I-08 | TD01 | Tech Debt | `tests/test_ci_autofix_hub_invariants.py` | Rank-7 hotspot (churn 2,022 × complexity 9 = 18,198). **1,712 lines — 2.1× the 800-line ceiling** in `rules/swe/coding-style.md`. Untracked: 0 hits in both ledger files. Size verified against current source (last touched by a repo-wide ruff pass, so not mid-refactor). Direct precedent `td-105` filed a test module on identical grounds. | Filed as **`td-138`**. Split by invariant family (autofix routing, PM detection, green-decline, bot-actor guard). | implementation-planner |
| I-09 | N05 | Consistency | `agents/sentinel.md:49` vs `:55` | **The catalog violates its own anti-count mandate six lines from stating it, and the count has already drifted.** Line 55: "do not restate its size anywhere… or in any other prose". Line 49 is prose in the same file: "~126 checks across 22 dimension groups". Measured from disk: **125** checks, 22 groups — off by one, exactly the failure line 55 predicts. A rule its own file breaks teaches readers the rule is decorative. | Delete the numeral from line 49; the sentence's point survives intact ("The catalog exceeds what one budget can execute"). The frontmatter `description` is already clean — this is the last site. | context-engineer |
| I-10 | N05 | Consistency | `~/.claude/CLAUDE.md:79-83,94-95` vs `CLAUDE.md:64` | **Two always-loaded surfaces disagree about whether a memory backend exists.** The global file instructs agents to use one in five places (incl. "persistent memory (Memory MCP)" and an ecosystem-table row); Praxion's `CLAUDE.md:64` states the opposite per dec-225 — "no memory tools exist". Both load every session; no memory tool is registered in `plugin.json`. Cost: a wasted failing tool call plus a mis-set expectation in every agent's Learning Loop. | Reword the global Learning Loop to name the *durable substrate* rather than a backend ("persistent memory where a backend is configured; `LEARNINGS.md` and ADRs otherwise") so it degrades correctly. **Owner is `user`** — that file is outside this repo and no Praxion agent may edit it. | user |
| I-11 | T06 | Token Efficiency | `~/.claude/CLAUDE.md:55-59` vs `rules/swe/swe-agent-coordination-protocol.md:58-63` | **The four Standard/Full agents' deliverables are enumerated twice in always-loaded content and have diverged.** (a) the table's `systems-architect` row omits `SYSTEM_DEPLOYMENT.md` and `PRE_REFACTOR_PLAN.md`; (b) its `verifier` row omits architecture-doc validation and the `TEST_RESULTS.md` read; (c) conditionals are a footnote in one, inline in the other. **The pair is unguarded**: EC06 validates the condensed block against `coordination-details.md` and nothing validates it against this sibling. A gate-liveness *scope-fidelity* gap. | Make the `Output` column a pointer ("see Delegation Checklists") rather than a second enumeration — this removes the token cost *and* the drift surface, preferable to extending EC06 to police a duplication that need not exist. | context-engineer |
| I-12 | X07 | Cross-Ref | `skills/README.md:87` | The `typescript-development` row advertises "ESLint, Prettier, tsc"; the skill's frontmatter names **Biome v2 (greenfield)** / ESLint v9 (framework), **Vitest**, React 19/Next 15/Vite, Vue 3/Nuxt 3. Body counts: Biome ×8, Prettier ×3. The catalog omits the greenfield-default formatter, the test runner and the framework layering while foregrounding a superseded formatter — a reader choosing a toolchain from the catalog gets the wrong answer without ever opening the skill. | Rewrite the row to lead with the Biome v2 / ESLint v9 split and name Vitest and the React/Vue layering, matching frontmatter. | doc-engineer |
| I-13 | X07 | Cross-Ref | `skills/README.md:15` (contradicted by `:122`) | The `mcp-crafting` row says "in Python with FastMCP"; the frontmatter says "official SDKs" and the skill ships both `contexts/python.md` and `contexts/typescript.md`. **The same file contradicts itself 107 lines later** — line 122 lists `mcp-crafting` among skills with TypeScript support. A TypeScript reader is told the skill does not cover them. | Broaden the row to "official MCP SDKs; Python and TypeScript language contexts" so lines 15 and 122 agree. | doc-engineer |
| I-14 | X04 | Cross-Ref | `.ai-state/idea_ledgers/IDEA_LEDGER.md:26-31` | **All six `dec-draft-<hash>` ids grounding the newest cluster are unresolvable** — none of the six hashes appears anywhere under `.ai-state/decisions/`, and `drafts/` holds only `CLAUDE.md` (0 drafts). The cause is **structural**: `adr-conventions.md § Finalize Protocol` defines a bounded rewrite scope covering `DESIGN.md`, the tech-debt ledgers, `CONSULT_LEDGER.md`, root `ROADMAP.md`, `docs/**`, in-flight `.ai-work/` planning docs and slug-matching specs — but **not `.ai-state/idea_ledgers/`**. Every future finalize strands ledger citations the same way. | (a) Resolve the six ids to their finalized `dec-NNN` (or record that they were dropped) and rewrite the ledger. (b) Decide whether `.ai-state/idea_ledgers/` joins the finalize walk scope in `adr-conventions.md` + `scripts/finalize_adrs.py`, or whether the ledger must cite finalized ids only. **(b) is the durable fix.** | context-engineer (a); systems-architect (b) |
| I-15 | AC09 | Architecture | `docs/architecture.md:92` ↔ `.ai-state/DESIGN.md` §3b | The developer guide carries an `Existing-project onboarding` capability row with **no counterpart** in the architect doc's §3b — which records `Greenfield project onboarding` (`:115`) and `Agentic-Transactions onboarding` (`:129`), with the existing-project half appearing only as prose *inside* the greenfield row. AC09's contract is that the guide is a **subset** of the architect doc; here the guide is the superset. `CLAUDE.md § Onboarding contract` names **two** converging paths, so an architect reading §3b sees only one. §3b is where architecture docs currently drift, precisely because AC13 binds only §3a. | Add an `Existing-project onboarding` row to `.ai-state/DESIGN.md` §3b mirroring the guide, or — if both paths are one capability by design — rename the guide's rows to a single shared label so the subset relation holds by construction. | systems-architect |
| I-16 | GL03 | Gate Liveness | `agents/sentinel.md` — V04 row | **The sentinel's own meta-check is presence-only.** V04's pass condition reads "Check catalog section exists in this agent definition" — a `## Check Catalog` heading with **zero rows** satisfies it. V04 is the one gate guaranteeing the sentinel still has checks at all, so it is the gate least able to afford this shape. Not currently damaging (125 rows present), but the gate would not notice if that changed. | Amend V04 to assert substance: "heading present **and** ≥1 check-ID table row beneath it, with ≥1 row per dimension named in the catalog's own headings". One line; no new tooling. | context-engineer |

#### Suggested (improves but not urgent)

| # | Check | Dimension | Location | Finding | Recommended Action | Owner |
|---|-------|-----------|----------|---------|--------------------|-------|
| S-01 | SH01 | Spec Health | All 8 `SPEC_*.md` matrices | Path columns mix repo-relative paths with non-path tokens, making SH01 **mechanically undecidable**. 34 of 112 backticked tokens fail bare resolution; every one sampled is benign, but a genuinely broken path produces the identical signature. Six shorthand species in circulation (bare basenames, skill-relative paths, shell commands, `::` pointer syntax, globs, branch names). Worst: `SPEC_production-gate-cohort` 14/24, `SPEC_p5-issue-autofix` 6/6. | One convention for the path column — repo-root-relative paths only — with commands, pointers and globs in an adjacent prose column. Until then SH01 can only return "sampled, all benign", weaker than the check is written to claim. | context-engineer |
| S-02 | DH03 | Decision Health | `.ai-state/decisions/` (15 ADRs) | 18 `vanished` references across 15 ADRs await human disposition (dec-040, 059, 061, 144, 148, 156, 160 ×3, 161, 198, 205, 211, 223, 241, 262, 289, 305). `withheld: []` — the complete residual, not a suppressed remainder. Two clusters dominate: the dashboard rewrite (144/148/156/160/161, one cause) and never-adopted lazy artifacts (241, 305). **Advisory — not a backlog.** A decision can be correct though its example files are gone. | Disposition per-ADR at human discretion; the dashboard cluster is the highest-yield batch. Do not auto-retire. Note dec-262's `dec-133-drop-pep440-dev-prereleases.md` is a **filename-shape error** (real file `133-drop-dev-prereleases.md`) — a mechanical repair, not a retirement question. | user (disposition); context-engineer (dec-262 path) |
| S-03 | CA02 | Calibration | `.ai-state/calibration_log.md`, 22 of 62 rows | Tier cells carry **execution-mode annotations inline** (`Standard (orchestrator-driven batch)`, `Full (scoped execution, wave-sequenced…)`), contra the tier-vs-execution-mode taxonomy, which places such notes in `Source` prose. Measurable cost: raw column equality reads **64.5%** where base-tier normalization reads **95.2%** — a 31-point swing that would put a future CA02 within 5 points of the 60% floor on a corpus with only 3 genuine overrides. | Keep tier cells to the bare 5-tier enum; move mode notes to `Source`. Consider whether CA02's pass condition should specify base-tier normalization, since the two readings differ by more than the threshold's own margin. | orchestrator (hygiene); context-engineer (CA02 clause) |
| S-04 | CA02 | Calibration | `.ai-state/calibration_log.md`, 2 of 62 rows | 2 rows carry `[pending]` in `Retrospective` and 3 carry a post-hoc marker rather than the enum. Enum mix over 62 rows: 27 `correct`, 8 `on target` (pre-convention synonym), 2 `[pending]`, 3 post-hoc, remainder prose. Historical rows are exempt by the log's own preamble; the `[pending]` ones are not — a retrospective never filled is a calibration signal permanently lost. | Fill the 2 `[pending]` retrospectives or mark them explicitly abandoned. The 8 `on target` rows are pre-convention and need no change. | orchestrator |
| S-05 | TT04 | Test Topology | `.ai-state/TEST_TOPOLOGY.md` (all 13 blocks) + `scripts/project_metrics/` | **13 of 13 groups declare a measured `expected_runtime_envelope` that no instrument reads.** 0 of 10 metrics reports emit per-group timings. TT04 cannot fire however far a group drifts — a declared threshold with no oracle, reported as PASS-by-absence. | Either add a per-group timing collector so TT04 has an oracle, or mark the envelopes advisory in the topology so a reader knows they are undefended. | test-engineer |
| S-06 | TD02 | Tech Debt | `scripts/project_metrics/collectors/pydeps_collector.py` | `hooks/` is **not a pydeps package root** — 0 of 139 analyzed modules are hooks, and 67 Python files (19.4%) sit outside the import graph. "0 cyclic SCCs" therefore covers 80.6% of the repo's Python. Asymmetric with coverage, which *added* `hooks/` on 2026-08-06 for exactly this reason. | Add `hooks` to the pydeps package roots so the two measurements share a denominator. | implementer |
| S-07 | P04 | Pipeline | `.claude/worktrees/pipeline-dashboard/.ai-work/pipeline-dashboard/{WIP.md,TEST_RESULTS.md}` | The verifier wrote `WIP.md` and `TEST_RESULTS.md` — documents owned by implementer / implementation-planner / test-engineer per `rules/swe/agent-intermediate-documents.md`. 2 of 40 verifier writes (5%); the other 38 are `VERIFICATION_REPORT.md` / `TECH_DEBT_LEDGER.md`. | Confirm whether a verifier may update `WIP.md` on rework; if not, state the prohibition in the verifier's boundary table. | context-engineer |
| S-08 | P05 | Pipeline | `.ai-work/multidisciplinary-identities-wave2/WAVE_2_HANDOFF.md` | A handoff document whose filename appears **nowhere** in the `.ai-work/` inventory of `rules/swe/agent-intermediate-documents.md`. It is the slug's only file, so no consumer is instructed to read it. | Add the filename to the inventory, or fold its content into a canonical document (`LEARNINGS.md`). | context-engineer |
| S-09 | CH01 | Code Health | `scripts/check_aac_golden_rule.py` | Imports `aac_fence_validator._CLOSER_PATTERN` and `._GENERATED_PATTERN` — `_`-prefixed constants of a *public* module, carrying no stability contract. Every other private import in the 23-file sample targets a `_`-prefixed module's public API. | Promote both patterns to public names in `aac_fence_validator`, or expose a public predicate encapsulating them. | implementer |
| S-10 | TD03 | Tech Debt | `scripts/check_agent_shared_blocks.py` | 65.69% (67/102), below the floor and untracked, but matching `td-135`'s DEFERRAL REGISTER profile exactly (a `scripts/` gate, modest gap, no gate-consuming caller). An 8th near-identical row would be noise. | **Append to `td-135`'s existing register** rather than open a new row. The sentinel appends rows and does not edit note cells, so this is routed rather than actioned. | orchestrator |
| S-01a | GL / meta | Gate Liveness | installed `i-am` v0.22.0 vs working tree | **The installed catalog is one commit stale, and it produced two false findings this run.** All 125 working-tree checks are present, but the installed copy carries two extra rows — the **retired `P01` and `P02`** — and 11 pre-hardening rule texts (`AC01`, `AC08`, `AC09`, `AC12`, `DL02`, `P03`, `P04`, `P05`, `S06`, `T03`, `TD02`). `P02` names a `delegation`↔`result` event pair no emitter has ever produced, so an unorchestrated run executes a check that cannot fire and reads its silence as PASS. The 11 stale texts caused Lane E to file `DL02` and `AC08` findings against rules `85033b4` had already fixed — both withdrawn. This is `gate-liveness.md`'s "existence is not operation" clause at small scale: the copy being loaded is not the copy that was edited. | Self-heals on the next release; no action needed beyond the routine refresh (push → `/release` → marketplace update → plugin update → restart). The durable improvement is a startup assertion comparing the installed catalog's check-ID set against the repo copy when self-hosting, so the divergence is announced rather than discovered. | user |
| S-13 | AC12 | Architecture | `agents/sentinel.md` AC12 row + tool-grant note | AC12 is blocked **twice**, and only the first block is visible today. (1) Substrate: 0 `req_ids` in `architecture.c4`, 0 of 8 SPECs with `architectural_elements:`. (2) Reader: `query-by-metadata` is an MCP tool outside the sentinel's grant. The catalog documents (2) in prose but nothing tracks it — whoever populates the convention will satisfy (1) and discover (2) only when AC12 reports against an input it cannot fetch. | File a tech-debt row (or an explicit AC12 precondition) requiring a Bash-reachable reader — a small `scripts/check_traceability_orphans.py` over the `.c4` source and SPEC frontmatter — to land **before** the convention is populated. | systems-architect |
| S-14 | AC09 | Architecture | `docs/architecture.md:84,101` ↔ `.ai-state/DESIGN.md:124,142` | Two §3b capability pairs describe the same substance under different labels (`Polyglot Skills (TypeScript / Node)` vs `Polyglot skill plane`; `Discipline-Consultant framework` vs `Multidisciplinary Consulting Identities (Wave 1 — statistician)`). No structural drift — both bodies match — but a name-keyed reader cannot join them, which is what made I-15 hard to distinguish from a rename. | Align labels in one direction, or add the design-doc label parenthetically in the guide row so the join is mechanical. | doc-engineer |
| S-15 | T03 | Token Efficiency | `agents/*.md` (8 files) | 8 of 17 agents sit in the warn band; **none** approaches a fail threshold (largest standard agent 462 of 500). Read alongside T05: `skills/agent-crafting/SKILL.md:115` establishes agents **cannot** use reference files across projects, so the only remedy is deletion, not extraction. A warn line of 300 against a population whose median phase-bearing agent runs ~330 lines is flagging the design, not a defect. | Treat as threshold calibration, not per-agent debt: raise the standard agent warn line to ~400 (fail stays 500), or record why 300 remains right. Do **not** open 8 remediation items. | context-engineer |
| S-16 | T01 | Token Efficiency | `skills/skill-crafting/SKILL.md` | 418 lines, 18 over the 400 warn line (fail 600). The skill already carries 20 disclosure files, so S05 passes — this is size, not structure. | Low priority. If touched for other reasons, move one section to `references/`. No standalone action warranted. | context-engineer |
| S-17 | S04 | Spec Compliance | `commands/clean-work.md`, `eval-praxion.md`, `refresh-claude-blocks.md`, `release.md`, `resume-rework.md` | 5 commands declare `argument-hint` but plumb no `$ARGUMENTS`/`$N` token, relying on raw invocation text being ambient; 33 of 45 commands use the token. Works today (4 of 5 are `disable-model-invocation: true`) but is an inconsistent convention within one artifact family. | Add `$ARGUMENTS` to each, or document the prose-parsing pattern in `skills/command-crafting` as a sanctioned alternative. Consistency here is cheap. | context-engineer |
| S-18 | N01 | Consistency | `skills/spec-driven-development/` | The naming convention reserves `-development` for language skills. `spec-driven-development` shares the suffix without being one. **Not a violation** — the rule is a one-way implication — but the suffix no longer discriminates, so a reader cannot infer skill family from the name. | Document the exception in the naming convention, or rename to `spec-driven-dev`. Renaming touches every reference; documenting is the cheaper correct move. | context-engineer |
| S-19 | N06 | Consistency | `commands/check-experiment.md:2` | Description is 815 chars — 8× the 103-char command median, 2× the next longest — and carries embedded *procedure* rather than the what+when every other command states. Body content living in a frontmatter field. | Cut to a what+when sentence (~120 chars); the procedure already belongs in the command body. | context-engineer |
| S-20 | S06 | Spec Compliance | `agents/implementer.md:50-98` | `## Execution Workflow` numbering runs 1–7, then **7.5–7.9**, then 8–10. Five sub-steps exhaust the decimal space before the next integer and carry the heaviest content, while step 7 ("Self-review") is one line — the numbering tracks neither sequence weight nor insertion room. A growth artifact from repeated insertion without renumbering. | Renumber 1–15, or promote 7.5–7.9 to a named `### Post-implementation updates` sub-section. Purely structural. | context-engineer |
| S-21 | N04 | Consistency | `skills/agent-crafting/SKILL.md:115,151` | 2 genuine `sub-agent` spellings against 177 `subagent` (1.1%). The other 4 hits are correct usage (the `sub-agents-mcp` package name; an Anthropic article paraphrase). | Normalize the 2 instances on next touch. Below the threshold for a dedicated change. | context-engineer |
| S-22 | EC02 | Ecosystem | `skills/adapt-claude-to-agents/`, `skills/likec4-querying/`, `skills/stakeholder-communications/`, `commands/star-repo.md` | 4 artifacts are anchored **only** in their own catalog file — no agent, rule, command, CLAUDE.md, hook or doc names them. Not orphans, but the least-reachable surfaces and the first place silent decay would appear. `likec4-querying` is the most surprising: the AaC tier ships it, `plugin.json` registers a `likec4` MCP server, and `docs/aac-dac.md` describes the dimension — yet no behavioral surface points at the skill. | No action required. If AaC guidance is consolidated, cite `likec4-querying` from `docs/aac-dac.md` or the architect prompt. Re-check next sweep. | user |

### Pipeline Discipline

**P03 — FAIL. 22,171 WAL records examined** (0 unparseable), paired on `agent_id` per the check's
own trap-avoidance clause (pairing on `agent_type` lets a sibling instance's stop satisfy another's
start). Two distinct defects, both filed above:

- **113 unmatched `agent_start`** — 74 of them (65%) from a single class, `workflow-subagent`
  (109 starts / 35 stops), whose GitHub Actions harness ends the job without firing the Stop
  hook. Residual rate excluding that class: **7.9%**. → I-04.
- **144 unmatched `agent_stop`** — these mint an `agent_id` matching no start anywhere, all with
  empty `agent_type`. The check's INFO-not-WARN allowance for front-truncation was tested and
  **falsified**: 0 of the 144 predate the file's earliest start, so these are not a rotation
  artifact. → I-03.

The denominator matters: 0 findings over 3 records is not a clean fleet, and only the count of
records examined distinguishes the two. 22,171 records is a substantial history, so these rates
are meaningful rather than noise.

**P04 — PASS, with its bound stated.** No agent's `tool_use` records show a tool outside its
frontmatter grant. One cross-owner write surfaced (S-07). **This check claims surface, never
semantics**: the records name tools and paths, not intent, so an agent making a design decision
inside a file it was legitimately assigned emits records indistinguishable from correct work.
Reporting the dimension "clean" without this bound would be the failure the P dimension was
rewritten to prevent.

**Substrate state, named precisely.** P03/P04 ran against `.ai-state/observations.jsonl`. **Task
Chronograph is `reader unreachable`** — the data would exist but no agent in the fleet holds an
MCP grant for it, and this agent's grant is `Read, Glob, Grep, Bash, Write`. That is a
*specification* boundary, not a data gap. `P01`/`P02` remain retired and were not run.

**P05 — WARN** (I-06): 1 of 4 handoff documents carries none of its canonical sections; 1
additional file is outside the documented inventory (S-08).
**P06 — WARN** (I-05): 2 of 2 eligible slugs lack `TASK_BRIEF.md`; 12 of 14 slugs correctly not
evaluated.
**P07 — PASS**: every `## Architecture Challenges` and `### CH-NN` entry across the in-flight
specialist fragments carries a disposition.
**P08 — no advisory**: `clean_work_safety.py --json` returned `stale_safe` below the threshold
of 3.

### Tech-Debt Findings

**2 rows filed this run**, both by TD01/TD03, both `important`:

| id | class | location | why filed |
|---|---|---|---|
| `td-138` | complexity | `tests/test_ci_autofix_hub_invariants.py` | 10 hotspots examined, **9 declined**. This one survived because it is untracked (verified in *both* ledger files, not assumed), its 1,712-line size was verified against current source rather than read from the report, and `td-105` is an exact precedent for filing a test module past the ceiling. A threshold breach alone would not have justified it — four other hotspots breached and were declined. |
| `td-139` | coverage-gap | `hooks/auto_complete_install.py` + 4 siblings | 14 modules breached the 70% floor; 8 are already tracked under `td-135`'s register (its two-clause re-evaluation trigger was checked and has **not** fired), 1 is routed to that register (S-10), 5 remain. Warranted precisely because they are *not* a breach that went unfiled — `hooks` joined `testpaths` on 2026-08-06, so this is their **first possible disposition**. Filed as one carrier row with the cause named, per `td-135`'s cohort precedent, rather than five near-identical rows. |

**TD06 gate result**: `check_metrics_freshness.py --json` — the metrics report still describes
current HEAD, so no TD01 candidate was barred from filing.
**TT04 rows: none** — SKIP, no per-group runtime data exists in any of the 10 reports, so no
sustained-drift determination is possible (S-05).
**EC07 rows: none** — the AaC golden-rule audit returned `[]`.
**TD05 discipline issues: 0** — no `resolved` row missing `resolved-by`, no `in-flight` row older
than 30 days, no `unassigned` row older than 7 days.

Canonical entries: `.ai-state/TECH_DEBT_LEDGER.md`.

### Recommended Actions (prioritized)

1. **Fix the agent-stop hook's `agent_id` resolution** (I-03) — 144 records and 39% of August's
   stops; every WAL-derived lifecycle metric is currently a lower bound. **Owner: implementer.**
2. **Close the two remaining vacuous gates with one-line substance clauses** — V04 (I-16) and
   SH04's column-existence assertion (I-02). Both are edits to `agents/sentinel.md`; the
   equivalent fixes for DL02 and AC08 already landed in `85033b4`.
   **Owner: context-engineer.**
3. **Repair `SPEC_design-dialectic`'s traceability matrix** (I-02) — 15 REQs have carried no test
   reference since 2026-04-17. **Owner: doc-engineer.**
4. **Decide the `TASK_BRIEF.md` obligation** (I-05) — enforce it with a pre-spawn check or
   downgrade it to conditional. A 100% silent lapse rate is the worst of both.
   **Owner: context-engineer.**
5. **Extend the finalize walk scope to `.ai-state/idea_ledgers/`** (I-14) — without it every
   future finalize strands ledger citations identically. **Owner: systems-architect.**
6. **Emit `agent_stop` from an `always()`-guarded workflow step** (I-04) — recovers 65% of the
   unmatched-start population. **Owner: cicd-engineer.**
7. **Work `td-139` worst-first** (I-07) — `auto_complete_install.py` at 38.07% is the
   lowest-covered file in the repository. **Owner: test-engineer.**
8. **De-duplicate the pipeline-deliverables enumeration** (I-11) — make the `Output` column a
   pointer; removes token cost and drift surface together. **Owner: context-engineer.**
9. **Split `test_ci_autofix_hub_invariants.py`** by invariant family (I-08, `td-138`).
    **Owner: implementation-planner.**
10. **Correct the two `skills/README.md` rows** (I-12, I-13) — one contradicts its skill, the
    other contradicts the same file 107 lines later. **Owner: doc-engineer.**
11. **Add the `Existing-project onboarding` row to `DESIGN.md` §3b** (I-15) — §3b is where
    architecture docs drift, because AC13 binds only §3a. **Owner: systems-architect.**
12. **Delete the check count from `agents/sentinel.md:49`** (I-09) — the file breaking its own
    rule teaches readers the rule is decorative. **Owner: context-engineer.**
13. **Raise `~/.claude/CLAUDE.md`'s memory-backend contradiction** (I-10) — outside this repo; no
    Praxion agent may edit it. **Owner: user.**
15. Remaining Suggested items (S-01 … S-22) are batchable at the next context-engineer pass; none
    is time-sensitive.

---

## Remediation — 2026-08-07, task slug `important-findings-remediation`

All 15 Important findings were dispatched to 10 agents over two waves with provably disjoint file
sets. This section records outcomes and, where remediation **falsified a finding's premise**,
corrects the record. Findings above are left as written — they are the audit as it stood; this
section is what survived contact with the evidence.

### Three findings had wrong premises

Each was refuted by an agent reading ground truth rather than complying with the report. In every
case the prescribed fix would have been wrong, and in one case actively harmful.

| # | The report claimed | What the evidence showed |
|---|---|---|
| I-04 | A **GitHub Actions** harness ends jobs without firing the Stop hook; emit `agent_stop` from an `always()` step | **No CI run has ever written to this WAL** — `grep -c '/home/runner'` returns **0** across 4,626 file paths in 22,438 records, and no workflow installs the observability hooks. All 250 `workflow-subagent` records sit in **one local session** (2026-06-21, 05:12–05:17) bracketed by a `/deep-research` skill activation; `workflow-subagent` is that shipped harness's fan-out label. 25 claims × 3 verification votes ≈ the 74 unmatched, and **66 of the 74 ran zero tools** — a rate-limit abstention cascade, not a missed hook. Complying would have **fabricated** stop records for agents no workflow started. |
| I-03 | The stop-side hook **mints a fresh `agent_id`** instead of resolving the start's | The stop side mints nothing — across 22,400 rows **zero** stop ids are fallbacks; all carry the harness's `a`+16-hex shape. The **start** row is what goes missing (614 `agent_start` vs 651 `agent_stop`). Four unmatched stops carry 2/6/11/24 `tool_use` rows under the same id, proving the subagent ran while its `SubagentStart` capture produced nothing: **upstream delivery loss**. |
| I-15 | An `AC09` violation — the guide is a superset of the architect doc | The divergence is real, but **`AC09` binds §3a only** — both its subset and coverage clauses say §3a, and the catalog states explicitly that §3b capabilities "do not demand they appear." No check binds §3b in either direction, which is *why* §3b is where these documents drift. Filed as `td-144`. |

**One ownership correction.** I-10 assigns `~/.claude/CLAUDE.md` to `user` on the grounds that it
is "outside this repo." It is a **symlink to `claude/config/CLAUDE.md`** inside this repository —
Praxion's own shipped global config. It is further a *generated* file: gitignored at
`.gitignore:9`, untracked, and rendered from the tracked `claude/config/CLAUDE.md.tmpl`. An edit
to the rendered file alone would have been silently discarded at the next render.

### Outcomes

| # | Check | Outcome |
|---|---|---|
| I-02a | SH04 | Verification column added to all 15 REQs: 5 real references, 10 explicit `no test — <rationale>`. All 12 cited paths verified to resolve. A gate whose `_detect_archived` is a stub was deliberately **not** cited rather than fabricating coverage. |
| I-02b | SH04 | Rule now asserts the column **exists** before asserting cells are populated; absent column is a finding. |
| I-03 | P03 | Root cause corrected (above). Emitter now records `start_correlation` ∈ `paired`/`unobserved-start`/`not-applicable` inside a tail scan the stop path already ran — zero extra I/O. Proven by mutation: reverting the key fails exactly 1 of 71 tests; four other mutations fail 10, 2, 5, 11. |
| I-04 | P03 | Premise refuted; **no workflow changed**. Real remediation: P03 now reports unpaired starts **per `session_id`** with concentration beside any total, and WARN attaches only to a start whose `agent_id` appears in a `tool_use` record. Its PASS is explicitly bounded to "no agent that did work went unstopped." |
| I-05 | P06 | Non-blocking `PreToolUse` advisory (`hooks/remind_task_brief.py`), stdout-empty by construction so it cannot contend with the consolidated `updatedInput` emitter. Canary asserts on the exact input that lapsed 2/2 times, with six paired suppression cases. ADR fragment written. |
| I-06 | P05 | Cause was neither of the candidate readings: **no architect was ever spawned** — the orchestrator authored the documents directly. Invariant added: **the schema binds the path, not the author.** The `### Section Guidelines` bullet that licensed omitting any `##` heading was rescoped to `###` sub-sections. |
| I-07 | TD03 | Five modules lifted from 37–59% to **96–100%**; suite 631 → **832 passed**. Root cause is a **measurement defect**, filed as `td-140`. `td-139` resolved. |
| I-08 | TD01 | 1,712 lines → five modules (531/396/275/248/552), all under the ceiling. Behaviour-preserving proven three ways: 87→87 tests, `--collect-only` node-id sets diff-clean, AST compare showing 83 test functions and 20 helpers with byte-identical bodies. `td-138` resolved. |
| I-09 | N05 | Numeral removed; the scope-fidelity point survives without it. Check count re-verified at 125. |
| I-10 | N05 | Reworded to name a generic **durable store**, degrading correctly across all three configurations. Mirrored into `CLAUDE.md.tmpl` and proven to render byte-for-byte. |
| I-11 | T06 | Pointer form adopted; the four condensed-block rows read `→ Delegation Checklists`, the `^1` footnote deleted. **−52 tokens** (`basis: tokenizer`, `measured: true`). |
| I-12/13 | X07 | Both rows corrected, plus **5 further drifted rows** found by sweeping all 58 against their frontmatter. Line 122's "Nine skills" was independently wrong — the filesystem has **14**. |
| I-14 | X04 | All **6** `dec-draft` ids resolved (→ `dec-216`…`dec-221`), none dropped. Durable fix: `.ai-state/idea_ledgers/` joins the finalize walk scope. The decisive evidence was that finalize's own gap detector **already scans that subtree** and its warning says to add it to `_cross_reference_targets()` — detector and rewriter disagreed. Canary proven in both directions: deleting the branch fails it, and an unbounded `.ai-state` rglob also fails it. |
| I-15 | AC09 | `Existing-project onboarding` row added to `.ai-state/DESIGN.md` §3b after establishing these are **two** capabilities (`/onboard-project` redirects to `/new-project` on greenfield shape; `/new-project` chains into it). Attribution corrected above. |
| I-16 | GL03 | V04 now asserts ≥1 row per dimension heading the catalog declares — **derived**, so it cannot drift as the catalog grows. |

### Newly surfaced during remediation — filed, not fixed

| id | Finding |
|---|---|
| `td-140` | **Coverage is structurally unmeasurable for subprocess-driven tests.** Under pytest-cov 7.1.0 no `COV_CORE_*`/`COVERAGE_PROCESS_START` var is exported during an active `--cov` run, so `a1_coverage.pth` never activates in children and every subprocess-driven test measures **zero**. `inject_worktree_banner.py` had 15 real behavioural tests and measured 37%. Contradicts the comment at `pyproject.toml:76-84`. **TD03 findings against subprocess-tested modules are false positives until resolved.** |
| `td-141` | `main()` is not total against well-formed non-object JSON in three hooks — `[]`, `null`, `"str"`, `123` parse then reach `payload.get(...)`. Nothing raises into the harness today because the `__main__` wrapper catches it, but the fail-open property lives in the wrapper, not the function. |
| `td-142` | **SH07 is blind to 48% of the REQ corpus.** `REQ_PATTERN = r"\bREQ-\d+\b"` matches only bare `REQ-NN`: 130 matched vs 121 unmatched (`REQ-PM-`, `REQ-DDL-`, `REQ-DIAGRAM-`). `_detect_archived` is an acknowledged stub. |
| `td-143` | No gate validates that `TEST_TOPOLOGY` selector args resolve. Hit live: the `td-138` split left the `ci-workflows` group naming a deleted path, so it would exit 4 while staying green by not running. Instance repaired by hand (group verified at 335 passed); the missing gate is what this row tracks. |
| `td-144` | §3b is unguarded in both directions. A naive name-equality extension would misfire on 8 pairs whose differing labels are each correct for their own document — reproducing the AC08 fire-on-correct-content failure this batch just fixed. |

Also observed, not filed: a third drifted copy of the finalize allowlist in `scripts/CLAUDE.md`;
six further dangling `dec-draft` ids in `.ai-state/TECH_DEBT_RESOLVED.md`; two tests whose names
promise job-scoping but which grep the whole raw workflow file.

### Verification

- Full suite `tests/ scripts/ hooks/`: **3,235 passed, 1 skipped, 1 failed**. The single failure,
  `hooks/test_cleanup_gate.py::test_non_matching_command_under_latency_budget`, is **pre-existing
  and unrelated**: `cleanup_gate.py` is byte-identical to HEAD, the measured path
  (`cleanup_gate.sh` + `promote_learnings.py`) was not modified and reads neither `hooks.json` nor
  the new hook, and the test asserts a 25 ms budget on a subprocess spawn that fails
  intermittently on a quiet machine.
- Always-loaded token budget: **23,490 / 25,000**, headroom 1,510, `over_by: 0` —
  `basis: tokenizer`, `measured: true` (a real measurement, not the ~5%-high estimate).
- `sync_canonical_blocks.py --check`: 8 blocks across 4 files, all in sync.
- `check_state_ledgers.py --check`: no blocking findings (the 5 advisories are pre-existing
  must-not-repair dedup collisions).
- `rules/_manifest.yaml` regenerated (24 rules); drift traced to exactly the two edited rule bodies.
- Two ADR fragments pending finalize at commit.
