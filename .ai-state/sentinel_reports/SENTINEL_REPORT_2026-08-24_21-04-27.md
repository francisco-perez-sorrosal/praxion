# Sentinel Report

**Run**: 2026-08-24_21-04-27 | **Scope**: full ecosystem sweep | **Baseline**: SENTINEL_REPORT_2026-08-07_14-00-50.md (C, 0/15/21, coherence C)

## Inventory

_(populated Phase 2)_

## Dimension Findings

_(appended per dimension as each completes)_

### Inventory (Phase 2)

| Family | Count |
|---|---|
| Skills (`skills/*/SKILL.md`) | 58 |
| Skill reference files | 215 |
| Skill context files | 19 |
| Agents (`agents/*.md`, excl. README/CLAUDE) | 17 |
| Rules (excl. CLAUDE.md/README.md) | 23 |
| Commands | 45 |
| Hooks | 44 |
| Scripts (`scripts/*.py|*.sh`) | 120 |
| ADRs finalized | 333 |
| ADR drafts | 0 |
| Archived specs | 8 |
| **Audited basis total** | **541** |

Substrate present: `.ai-state/DESIGN.md`, `docs/architecture.md`, `calibration_log.md`,
`observations.jsonl`, `TEST_TOPOLOGY.md`, `TECH_DEBT_LEDGER.md`, `doc_manifest.yaml`,
`SYSTEM_DEPLOYMENT.md`, `metrics_reports/`, `specs/`, `decisions/`. **Absent**: `compose.yaml`
(so C09/F06 have no deployment-config trigger), `.ai-state/decisions/drafts/*` (empty).

### Script-delegated gates (Pass 1)

| Check | Result | Evidence |
|---|---|---|
| T02 token budget | **PASS** | 23,558 / 25,000 tokens, `basis: tokenizer` (measured, not estimated), headroom 1,442, utilisation **94.2%** — inside ceiling but the thinnest margin on record |
| GL02 forbidden-pattern | PASS | `check_gate_liveness.py --json` → 0 findings |
| GL04 uninvoked-gate | PASS | same run, 0 `uninvoked-gate` |
| GL05 ambient-import | PASS | same run, 0 `ambient-import` |
| GL06 discarded-verdict | PASS | same run, 0 `discarded-verdict` |
| EC07 AaC golden rule | PASS | `check_aac_golden_rule.py --mode=audit` → `[]` |
| AC13 design-doc projection | **PASS** | 16 rows / 16 elements, 0 findings, `withheld: []` — both halves (model↔§3a, registry↔§4) read |
| AC10 fence integrity | see AC section | |
| SH07 spec drift | PASS | `check_spec_drift.py` → 0 findings |
| SH08 spec-archival gap | PASS | `gap: false`; newest spec 26d old, no ≥3 ADR cluster |
| CA03 calibration coverage | PASS | `covered: true`, newest row 2026-08-24, 0 uncalibrated pipeline commits |
| RD01 readiness | PASS | `adjusted_level: 4` ≥ floor 3; `mechanical_only: false` |
| P06 TASK_BRIEF | PASS | `check_p06_task_brief.py` → `[]` (**regression closed** — was a 2/2 lapse on 2026-08-07) |
| P08 stale slugs | skip | `stale_safe: 1` < 3 threshold (2 total slugs; nine stale dirs removed today) |
| **TD06 metrics freshness** | **WARN** | `status: stale` — report `METRICS_REPORT_2026-08-07_07-41-41` is 17 days old and **39 commits** behind HEAD; 3 hotspots flagged `hotspot-moved-since-report` and barred from TD01 filing |

### Decision Health (DH)

`adr_health.py`: 1,428 references scanned across 333 ADRs. `withheld: []` (complete run — the
`vanished` count is comparable to prior runs). `skipped_terminal`: 27 decisions at terminal status
excluded by design.

| Check | Result | Evidence |
|---|---|---|
| DH01 removed-by-later | **WARN (3)** | dec-285, dec-286, dec-290 each name `tests/test_ci_autofix_hub_invariants.py`, removed 2026-08-07; nearest candidate remover is **dec-292** (the PM-aware supersession). No `supersedes`/`superseded_by` edge links dec-292 to 285/286/290. This is a real decision-graph edge that was never written. |
| DH02 reference decay | **PASS** | 0 `renamed`, 0 `placeholder-shape`, 0 `out-of-repo`. **Regression closed**: the dec-262 filename-shape error reported 2026-08-07 no longer appears. |
| DH04 reopen candidates | PASS | `reopen_candidates: []` |
| DH05 category discrimination | **measurement, not a finding** | corpus architectural share 232/333 = 69.7%; `recent` window (n=50) 70.0%; **`post_adoption.n = 15`**, architectural share **26.7%**. The recent window is still dominated by pre-adoption records — do not read 70% as authoring behaviour. The 15 post-adoption decisions show the test *is* discriminating (26.7% vs 69.7% corpus). Below the window size, so no flag; re-measure when `n` ≥ 50. |
| DH03 retirement candidates | **INFO advisory (17)** | 17 `vanished` (was 18) awaiting human disposition. Clusters: dashboard rewrite (144/148/156/160×3/161/223 — one cause), never-adopted lazy artifacts (241, 305, 198), removed commands (040, 289). Not a backlog; a decision may be correct though its examples are gone. |

### Completeness (C)

| Check | Result | Evidence |
|---|---|---|
| C01 | PASS | 58 skill dirs, 58 `SKILL.md` — 1:1 |
| C02 / S01 | PASS | all 58 carry a non-empty `description:` |
| C03 / S02 | PASS | all 17 agents carry non-empty `name`, `description`, `tools` |
| C04 | PASS | all 45 commands carry a non-empty `description` |
| C05 / X01 / V01 | PASS | `plugin.json` registers exactly 17 agents; 0 missing paths, 0 unregistered files, 0 ghost registrations; `./agents/sentinel.md` present |
| C08 | PASS | 15 `[CUSTOMIZE]` hits, **all** in documentation *about* the marker (`rules/README.md` ×6, `skills/rule-crafting/REFERENCE.md` ×4, `commands/add-rules.md` ×2, `agents/sentinel.md` ×1, one prior report). Zero unfilled sections in shipped artifacts. |
| C09 | skip | no `compose.yaml` / `Dockerfile` at repo root — the deployment-config trigger is absent, so the substance requirement does not activate. `.ai-state/SYSTEM_DEPLOYMENT.md` exists regardless. |

### Consistency (N) & Spec Compliance (S) — structural

| Check | Result | Evidence |
|---|---|---|
| N01 | PASS | all 58 skill dirs lowercase kebab-case |
| N02 | PASS | all 17 agent files lowercase kebab-case `.md` |
| S03 | PASS | every rule file's first non-frontmatter line is a `## ` heading (23 rules) |
| S04 | **PASS — regression closed** | 0 commands declare `argument-hint` without plumbing `$ARGUMENTS`/`$N`. The 2026-08-07 finding **S-17** (5 such commands) is resolved. |

### Freshness (F)

| Check | Result | Evidence |
|---|---|---|
| F01 / F02 | PASS | sampled; no unresolved reference paths surfaced in the X/AC path sweeps |
| F05 | PASS | `SYSTEM_DEPLOYMENT.md` referenced paths resolve |
| F10 | **PASS** | `.git/hooks/{post-merge,post-commit,post-checkout}` are byte-identical copies of `scripts/git-finalize-hook.sh` (the multiplexed entrypoint); `pre-commit` is pre-commit-framework-generated. **Note a check-spec defect** — F10's stated derivation (`scripts/git-<name>-hook.sh` → `.git/hooks/<name>`) mis-derives a nonexistent `.git/hooks/finalize` for a multiplexed entrypoint. See finding S-04. |
| F11 | PASS | `generated_at: 2026-08-25T03:40:53Z` post-dates the newest `--diff-filter=ADR` commit under `docs/`/`.ai-state/` (2026-08-07T23:17) |
| F07 | **WARN ×2** | (a) `skills/deployment` catalogs `GPU Compute Budgeting` with no `last-verified` marker in `SKILL.md`, `references/` or `contexts/`. (b) `skills/likec4-querying/SKILL.md` declares `staleness_sensitive_sections:` with **no list items** — a catalog that can never fire, see finding I-02. |
| F08 | **WARN ×2** | `skills/llm-prompt-engineering/SKILL.md` — `Model Family Matrix` and `Platform Comparison`, both `last-verified: 2026-04-16` = **130 days** against the default 120-day threshold (this skill sets no `staleness_threshold_days`). Not past 2× (240d), so WARN not FAIL. Two sibling markers in `contexts/python.md` and `contexts/typescript.md` share the date but are not cataloged sections. |
| F09 | PASS | 3 `[YYYY-MM-DD]`/`YYYY-MM-DD` textual hits: one is the sanctioned template placeholder in `skills/agentic-transactions/references/_provider-template.md` (`_`-prefixed, exempt); two are prose *describing* the marker syntax (`test-topology.md:15`, `skill-crafting/references/schema.md:199`), not markers. Zero invalid or future-dated markers. |

### Token Efficiency (T)

| Check | Result | Evidence |
|---|---|---|
| T01 | **WARN ×1** | `skills/skill-crafting/SKILL.md` at **418** lines (warn 400, fail 600). Next: `rule-crafting` 399, `context-security-review` 396. 55 of 58 skills comfortably under. |
| T02 | PASS | 23,558 / 25,000 tokens, `basis: tokenizer` (measured), utilisation **94.2%**, headroom 1,442 — see finding I-01: inside ceiling, thinnest margin recorded |
| T03 | **WARN ×4** | `sentinel.md` 684 lines against its **derived** ceiling (catalog span `S`=350 → warn 650 / fail 750) — 34 over warn, 66 under fail. `verifier.md` 600 vs its fixed warn 550 / fail 700. `systems-architect.md` 466 and `implementation-planner.md` 445 vs standard warn 400 / fail 500. No FAILs. |
| T04 | PASS | largest reference `skills/cicd/references/patterns-and-examples.md` at 769 lines (<800, thin margin) |

### Cross-Reference Integrity (X)

| Check | Result | Evidence |
|---|---|---|
| X01 | PASS | all 17 plugin.json agent paths resolve |
| X02 | PASS | every registered skill dir holds a `SKILL.md`; every command file non-empty |
| X03 | PASS | every dir in CLAUDE.md `## Repository layout` exists |
| X05 / V02 | PASS | coordination-protocol Available-Agents table ↔ `agents/*.md` — exact 1:1, no coord-only, no file-only; sentinel row present |
| X06 / V03 / EC01 | PASS | `agents/README.md` names all 17; sentinel row present |
| X09 | PASS | every `dec-NNN` in `SYSTEM_DEPLOYMENT.md` resolves |

### Decision Log (DL)

| Check | Result | Evidence |
|---|---|---|
| DL01 | PASS | 333 finalized ADRs matching `^\d{3}-.+\.md$`; 0 drafts (clean post-finalize state) |
| DL02 | **PASS** | all 333 carry all eight required keys, **all non-empty** (incl. `summary`, `tags`); every `id` matches `dec-\d{3}` |
| DL03 | PASS | 333 files ↔ 333 index rows |
| DL04 / DL06 | PASS | see DH — `adr_health` found 0 dangling pointers; 0 `placeholder-shape` (the 2026-08-07 `dec-262` filename-shape error is **resolved**) |
| DL05 | PASS | 8 archived specs, 333 ADRs — ample decision frequency |

### Behavioral Contract (BC)

| Check | Result | Evidence |
|---|---|---|
| BC01 | PASS | `rules/swe/agent-behavioral-contract.md` exists, frontmatter is `core: true / load: always_on` with **no `paths:`** (unconditionally loaded), and names all four behaviors |
| BC02 | PASS | `~/.claude/CLAUDE.md` and `claude/config/CLAUDE.md` both name the four in canonical order with identical spelling |
| BC03 | **PASS** | exactly 14 agents reference the rule; the 3 that do not — `promethean`, `roadmap-cartographer`, `skill-genesis` — are precisely the three outside the contract-bound set |
| BC04 | PASS | `### Behavioral Contract Findings` present with all 6 canonical tags |

### Architecture Completeness (AC)

| Check | Result | Evidence |
|---|---|---|
| AC01 | PASS | `.ai-state/DESIGN.md` present with 16 populated §3a component rows |
| AC03 | PASS | 270 backticked paths in `DESIGN.md`, 102 unresolved (**37%**, under the 50% WARN line). Unresolved set is dominated by *illustrative bare filenames* (`WIP.md`, `LEARNINGS.md`, `PROGRESS.md`) and lifecycle-scoped `.ai-work/` paths — correct usage, not drift. |
| AC04 | PASS | every `dec-NNN` in `DESIGN.md` and `docs/architecture.md` resolves to a finalized file |
| AC05 | PASS | `docs/architecture.md` exists and is substantive |
| AC07 | PASS | 242 backticked paths, 34 unresolved — every one is a component-relative fragment (`contexts/`, `harness/`, `src/app/api/diagram/route.ts` under `dashboard_app/`) or an API route, not a repo-root path claim |
| AC08 | PASS | zero rows anywhere in `docs/architecture.md` carrying `Planned`/`Designed`/`Proposed`/`Not built` |
| AC10 | PASS | `aac_fence_validator.py` → `Verdict: PASS` on both `.ai-state/DESIGN.md` and `docs/architecture.md` |
| AC13 | PASS | `check_architecture_projection.py` → 16 rows / 16 elements, 0 findings, `withheld: []` — both the structural (model↔§3a) and published (registry↔§4) halves read cleanly |
| AC12 | **skip — reader-reachable, convention unpopulated** | The `.c4` model carries **no** `metadata.req_ids`, and no archived SPEC carries `architectural_elements:` frontmatter. The bidirectional convention is **not yet populated** — this is the precondition that failed, *not* reader unreachability (the Bash `grep` reader is available to this agent). Re-activates the day the first `req_ids` lands. |

### Test Topology (TT)

| Check | Result | Evidence |
|---|---|---|
| TT01 | PASS | 13 groups; every `subsystems` value resolves to a `Status: Built` §3a row in `DESIGN.md` (incl. `Agents (authored definitions)`, `DESIGN.md:88`) |
| TT02 | PASS | all 13 groups declare `strategy: pytest-globs`, a Registry-1 identifier |
| TT03 | PASS | 0 open `class = topology-drift` rows in the ledger (threshold 3) |
| TT04 | skip | only 1 `METRICS_REPORT_*.json` carries per-group data; the check requires ≥7 |
| TT05 | skip | no group declares a language-leaf **marker-selector** entry (all 13 use `pytest-globs`), so neither the registration nor the reserved-name clause activates |
| TT06 | skip | `.ai-state/TEST_TOPOLOGY.md` exists — growth-trigger advisory does not apply |

### Pipeline Discipline (P)

**Substrate**: `.ai-state/observations.jsonl` present, **999 records examined**. Not Task Chronograph (no MCP grant; carries no cross-session history).

| Check | Result | Evidence |
|---|---|---|
| P03 | **WARN ×1** | Paired on `agent_id` (never `agent_type`). 13 `agent_start`, 21 `agent_stop`. **One unpaired start that did work**: `agent_id a045b98b6e9067762`, `agent_type i-am:implementer`, session `84edfc44…` — it emitted `tool_use` and never stopped. Concentration: 1 unpaired start in 1 session — a **rate of one**, not an incident burst. Zero unpaired starts that emitted no `tool_use`. **10 unmatched stops → INFO** (front-truncation from WAL rotation / `.ai-state/` merges, not a fleet defect). `start_correlation` distribution: 11 **absent** (records predating the field — reported as *unattested*, never read as agreement), 8 `unobserved-start` (upstream start-delivery loss), 2 `paired`; **zero producer-vs-WAL disagreements**. **Bound**: P03 clean would mean *no agent that did work went unstopped* — here exactly one did, so the bound is breached by one. |
| P04 | PASS (bounded) | Grouped 999 records by `agent_id`. **Zero** out-of-grant tool uses and zero read-only-agent write violations across every `agent_type` resolving to a file in `agents/`. Non-fleet emitters (`main` = the orchestrator, `claude-code-guide` = a foreign plugin agent) carry no `agents/*.md` grant to judge against and are excluded. **Bound**: these records name tools and paths, never intent — an agent making an out-of-scope *decision* inside a file it was legitimately assigned emits records indistinguishable from correct work. This check cannot see that; the PASS covers surface only. |
| P05 | PASS | the 2 live `.ai-work/` slugs carry no incomplete handoff docs with empty required sections |
| P06 | **PASS — regression closed** | `check_p06_task_brief.py` → `[]`. The 2026-08-07 finding (TASK_BRIEF.md absent in 2 of 2 eligible slugs, a 100% lapse) no longer reproduces. |
| P07 | skip | no `INTERFACE_DESIGN.md`, `TRANSACTIONS_DESIGN.md` or `CONSULT_*.md` under `.ai-work/` |
| P08 | skip | `stale_safe: 1` < 3 (nine stale dirs removed 2026-08-24) |

### Pre-Refactor Plan Integrity (PR)

PR01 — skip. No `PRE_REFACTOR_PLAN.md` under `.ai-work/`; the artifact is emitted only for the `emit-PRE_REFACTOR_PLAN` Phase 2.5 outcome, so absence is the expected case.

### Hackathon Mode (HK)

HK01 — inert. `PRAXION_HACKATHON_MODE` unset in `.claude/settings.json`. Nothing emitted.

### Spec Health (SH)

| Check | Result | Evidence |
|---|---|---|
| SH01 | PASS | path-column cells in all 8 archived specs resolve; no shell commands / globs / branch names occupying a path column |
| SH02 | PASS | all 8 specs carry a non-empty `## Traceability` section |
| SH03 | PASS (sampled) | behavioral claims in the 3 most recent specs match shipped artifacts |
| SH04 | **PASS — regression closed** | **Every one of the 8 specs now carries a verification column.** `SPEC_design-dialectic_2026-04-17.md` — the 2026-08-07 **I-02** finding, whose matrix was `REQ-DDL \| Satisfying artifact(s) \| Kind` with nothing naming what verified each REQ — carries an explicit note: *"Verification column added retroactively 2026-08-07."* Zero empty verification cells and zero `UNTESTED` markers across all specs (the single `UNTESTED` grep hit is that explanatory note itself). The column-exists-then-cells-filled order was applied. |
| SH05 | PASS (sampled) | Key Decisions sections carry what / why / alternatives |
| SH06 | not deeply executed | see Depth Disclosure |
| SH07 / SH08 | PASS | script-delegated, above |

### Calibration Accuracy (CA)

| Check | Result | Evidence |
|---|---|---|
| CA01 | PASS | header `Timestamp \| Task \| Signals \| Recommended Tier \| Actual Tier \| Source \| Retrospective`; **66** data rows |
| CA02 | PASS | After stripping parenthetical execution-mode annotations: **61/66 = 92.4%** raw match, far above the 60% floor. Largest single override pattern is 1 row (1.5%), well under the 40% ceiling. **Genuine** mismatches are only 2 (`Full→Standard`, `Standard/Full→Full`) = **97.0%** — the other 3 "mismatches" are the annotation defect in S-11 below, not calibration error. |
| CA03 | PASS | script-delegated, above |

### Ecosystem Coherence (EC)

| Check | Result | Evidence |
|---|---|---|
| EC01 | PASS | `agents/README.md` pipeline diagram ↔ `agents/*.md` — 17:17 |
| EC02 | PASS (with note) | Only **2** artifacts are anchored solely in their own catalog file: `skills/stakeholder-communications/`, `commands/star-repo.md`. **Improved from 4** on 2026-08-07 — `skills/adapt-claude-to-agents/` and `skills/likec4-querying/` have both since acquired behavioral anchors. Not orphans; the least-reachable surfaces. |
| EC03 | PASS | every agent named in a collaboration section exists; all 17 fleet names resolve; zero `agents/<name>.md` references pointing at a nonexistent file. **Reciprocity deliberately not asserted** — Praxion's topology is orchestrator-mediated, so asymmetric references are the design. |
| EC04 | PASS | every pipeline handoff document has a producing and a consuming instruction. `TASK_BRIEF.md` is produced by the **orchestrator** at the Intake Clarity Gate (coordination protocol § Conversation Checkpoints) and `IMPLEMENTATION_PLAN.md` by implementation-planner via `coordination-details.md § Delegation Checklists` — both live outside `agents/*.md`, so a producer scan restricted to agent files under-reports them. No dead ends. |
| EC05 | PASS | no structural gaps against the stated purpose in `CLAUDE.md` |
| EC06 | **PASS** | All four condensed bullets in `claude/config/CLAUDE.md:55-59` reconcile against `coordination-details.md § Delegation Checklists`, outputs-only. Every produced deliverable is present, and every conditional clause — `if deployment in scope`, `if pre-refactor outcome`, `if structural gaps found`, `if structural`, `if step runs tests`, `missing → WARN, not FAIL` — appears in **both** files. No drift in either direction. |
| EC07 | PASS | script-delegated, above |

### Code Health (CH)

CH01 — **1 Suggested finding**. AST-normalised exact-body comparison across all non-test
`hooks/*.py` + `scripts/*.py` modules (functions of ≥5 statements) found exactly **one**
cross-module duplicate group: `_find_ruff` is byte-identical in `hooks/check_code_quality.py`
and `hooks/format_python.py`. Everything else in the sampled surface is structurally distinct.
See S-09.

### Technical Debt (TD)

**Staleness WARN (dimension-level).** `METRICS_LOG.md`'s newest row is `2026-08-07T07:40:53Z` —
**17 days** old, past the 14-day threshold — and TD06 independently returns `status: stale`
(39 commits behind HEAD). Findings below are produced from available data; the dimension does
not block on staleness.

| Check | Result | Rows filed | Rationale |
|---|---|---|---|
| TD01 hotspots | **no rows filed** | 0 | TD06 bars 3 of the top-7 hotspots (`dashboard_app/src/components/readiness-section.tsx` #2, `codex/config/export-codex-rules-bridge.py` #3, `tests/test_ci_autofix_hub_invariants.py` #7) as `hotspot-moved-since-report`. The #1 hotspot, `fitness/tests/test_discipline_registry_invariants.py` (churn 3,705 × complexity 20 = 74,100), is a **test file** whose churn is the signature of a recently-landed wave, not of debt — LLM-judgment gating declines it. Filing against a 39-commit-stale ranking would put unverifiable rows in a consumer-facing ledger. Recommend `/project-metrics` before the next TD01 pass. |
| TD02 cyclic SCCs | **no rows filed — reported with its denominator** | 0 | `cyclic_deps: 0` **across 278 of 345 tracked Python files (80.6% import-graph coverage) over 6 package roots**. This is a bounded claim, not a repo-wide all-clear: pydeps is a package analyser, so directories of loose modules with no `__init__.py` lie outside its reach by construction. The 19.4% shortfall is expected and is stated rather than apologised for. Flat vs the prior report (0 → 0). |
| TD03 coverage | **1 row filed — `td-147`** | 1 | Read the **per-file** list, never the 84.47% aggregate. 14 modules sit below the 70% floor. 7 are already claimed by `td-146`'s active `scripts/` deferral register; 2 more (`check_metrics_freshness.py` 65.49%, `check_state_ledgers.py` 67.05%) belong to that register's class and are recorded as pointers. The remaining **5 are `hooks/` modules** — every one registered in `hooks/hooks.json` and firing on a live lifecycle event, so a defect changes pipeline behaviour silently rather than failing an operator-invoked command. That is a materially different risk profile from td-146's stated "no gate-consuming caller" rationale, which is why they are filed separately rather than folded in. Worst: `auto_complete_install.py` at **38.07%** of 197 lines. |
| TD04 complexity p95 | **no rows filed** | 0 | Repo-wide `ccn_p95 = 7.00`, `cognitive_p95 = 8.00` — both flat vs the prior report and both modest. The single outlier, `dashboard_app/src/components/readiness-section.tsx` at p95 CCN 40.2, is **TD06-barred** (moved since the report). |
| TD05 ledger discipline | **1 WARN** | writes no rows | (a) 0 `resolved` rows in `TECH_DEBT_RESOLVED.md` missing `resolved-by` across **125** terminal rows — clean. (b) 0 `in-flight` rows over 30 days (all 21 active rows are `open`). (c) **`td-130` has `owner-role: unassigned` and is 18 days old** (threshold 7). See S-10. |
| TD06 freshness | **WARN** | gates TD01, files nothing | `status: stale`, 17 days / 39 commits, `withheld: []` |

Ledger state: **21 active** (`open` ×21 — 7 important, 13 suggested, 1 nice-to-have),
**125 resolved/terminal**. Class mix: coverage-gap 8, drift 5, other 4, complexity 2,
duplication 1, maintenance 1. Canonical entries: `.ai-state/TECH_DEBT_LEDGER.md`.

### Gate Liveness (GL)

| Check | Result | Evidence |
|---|---|---|
| GL01 | PASS (sampled) | every named section agents are told to read/harvest/digest has a producing instruction somewhere in `agents/` or `rules/` — `## Sources Read`, `## Research Stage Review`, `## Uncertainty Flag`, `## Pre-Refactor Assessment`, `## Acceptance Criteria` all resolve to producers |
| GL02 / GL04 / GL05 / GL06 | PASS | `check_gate_liveness.py --json` → 0 findings of every kind |
| GL03 | PASS (with 1 Suggested) | Sampling this catalog's own Pass conditions, the presence-only anti-pattern has been **systematically remediated**: C04, X02, V04, AC01, AC05, DL02, P05 and SH04 all now carry explicit substance clauses. One residual — **SH02** asserts `## Traceability` is "present and non-empty" without asserting the section contains a *matrix*; a spec whose Traceability section is a paragraph of prose and nothing else would pass. See S-12. |

### Self-Verification (V)

| Check | Result | Evidence |
|---|---|---|
| V01 | PASS | `./agents/sentinel.md` in `plugin.json` agents array |
| V02 | PASS | sentinel row present in the coordination-protocol Available Agents table |
| V03 | PASS | sentinel row present in `agents/README.md` |
| V04 | **PASS** | `## Check Catalog` present, **125** check-ID rows beneath it, and **every one** of the 22 `###` dimension headings the catalog declares carries ≥1 row (min 1 — CH, RD, PR, HK; max 12 — AC). The derived per-dimension clause is satisfied with zero hollow dimensions. |

---

## Ecosystem Health: B

> **Read this grade with its scope.** The previous run (2026-08-07, grade C) was a **5-lane
> orchestrated fan-out** executing 125/125 catalog checks with zero `[not reached]`. This run is a
> **single agent under one turn budget**. The improvement from C to B is **partly real and partly
> narrower coverage** — see Depth Disclosure, which names every check executed at reduced depth.
> The regression closures below are individually verified and are real; the *absence* of new
> Important findings in the deep-LLM dimensions is weaker evidence than last run's absence was.

### Summary

The ecosystem is in materially better shape than at the last sweep, and the improvement is
concentrated exactly where the 2026-08-07 report said the problem was: **the verification layer**.
That report's dominant pattern was "gates that cannot bite — checks whose pass condition is
satisfied by the absence of the thing they exist to measure." Every named instance is now closed:

- **SH04** — `SPEC_design-dialectic` had carried no verification column since 2026-04-17, making
  the `UNTESTED` grep structurally incapable of matching. All 8 specs now carry one; 0 empty cells.
- **P06** — TASK_BRIEF.md was absent in 2 of 2 eligible slugs (a 100% lapse on an unconditional
  Standard/Full obligation). Now `[]`.
- **S04** — 5 commands declared `argument-hint` while plumbing no `$ARGUMENTS`. Now 0.
- **DL04 / `dec-262`** — the `dec-133-drop-pep440-dev-prereleases.md` filename-shape error is gone;
  `adr_health` reports 0 `placeholder-shape`, 0 `renamed`, 0 `out-of-repo` across 1,428 references.
- **GL03 broadly** — C04, X02, V04, AC01, AC05, DL02 and P05 have all acquired explicit substance
  clauses. The catalog now asserts content where it previously asserted containers.

Structural health is unchanged and strong: `plugin.json` ↔ `agents/` ↔ coordination protocol ↔
`agents/README.md` are 17:17:17:17; 333 ADRs ↔ 333 index rows with all eight required frontmatter
keys non-empty on every one; AC13's projection is clean on both halves; `AC09` now passes in
**both** directions (16 Built §3a components, none missing from the developer guide).

What remains is three Important items, none structural: the always-loaded token budget is at
**94.2% utilisation** with 1,442 tokens of headroom — the thinnest on record; the metrics substrate
that the entire TD dimension reads is **17 days and 39 commits stale**, which barred 3 of the top-7
hotspots from filing and left TD01/TD04 unable to produce evidence-backed rows; and one agent spawn
**did work and never emitted a Stop record**.

**Comparison to 2026-08-07** (C, 0/15/21, coherence C): grade **C → B**; Important **15 → 3**;
Suggested **21 → 13**; coherence **C → A**. Artifacts audited 513 → 541 (basis restated in
Inventory — this run counts skill reference and context files explicitly).

### Ecosystem Coherence: A

**System-level EC checks:**

| Check | Result | Evidence |
|-------|--------|----------|
| EC01 | PASS | pipeline diagram in `agents/README.md` ↔ `agents/*.md`, 17:17 |
| EC04 | PASS | every handoff doc has a producer and a consumer; no dead ends |
| EC05 | PASS | no structural gaps against the purpose stated in `CLAUDE.md` |
| EC07 | PASS | `check_aac_golden_rule.py --mode=audit` → `[]` over the commit horizon |

**Cross-dimension anomalies:** none of consequence. The 2026-08-07 pattern that drove coherence
down to C — verification-layer gates passing without evidence across four dimensions — does not
reproduce in any dimension this run. The one weak cross-dimension signal is **substrate freshness**
(TD06 stale, and the same 2026-08-07 metrics report underpinning TD01–TD04 and RD01), but that is a
single upstream cause with a single one-command remedy, not a structural pattern.

**Artifact coherence distribution:** 541 artifacts audited. **533 score A** across every dimension
their lane examined. 8 carry a non-PASS in some dimension: 4 agents on T03 size, 1 skill on T01
size, 1 skill on F07 marker placement, 1 skill on F08 marker age, 2 hooks on CH01 duplication
(counted once). No artifact scores below B.

**Synthesis:** the connections work. What the last sweep diagnosed as a verification-layer failure
has been repaired rather than papered over — each closure was confirmed against the artifact, not
inferred from a passing check. The remaining friction is *upstream data age*, not ecosystem
structure: the TD dimension is reading a snapshot the repository has moved 39 commits past.

### Ecosystem Metrics

| Metric | Value | Trend |
|--------|-------|-------|
| Artifacts audited | 541 | ▲ (basis restated; see Inventory) |
| Health grade | B | ▲ from C |
| Important findings | 3 | ▼▼ from 15 |
| Suggested findings | 13 | ▼ from 21 |
| Critical findings | 0 | = |
| Ecosystem coherence | A | ▲▲ from C |
| Always-loaded tokens | 23,558 / 25,000 (94.2%) | ▲ utilisation — thinnest recorded |
| Finalized ADRs | 333 | ▲ |
| Active tech-debt rows | 21 open (7 important) | ▼ from 32 (11 closed 2026-08-13) |
| Resolved tech-debt rows | 125 | ▲ |
| Calibration match rate | 92.4% raw / 97.0% genuine (n=66) | ▲ |
| ADR reference decay (`vanished`) | 17 | ▼ from 18 |
| Least-reachable artifacts (EC02) | 2 | ▼ from 4 |
| Line coverage | 84.47% | ▼ 0.65% (stale basis) |
| Catalog checks with populated dimensions | 22/22, 125 rows | = |

### Scorecard

Rows are emitted only for artifacts carrying a non-PASS in some dimension. The remaining ~533
artifacts scored A across every dimension their lane examined and are not enumerated.

| Artifact | Type | Complete (C) | Consistent (N) | Fresh (F) | Spec (S) | Cross-Ref (X) | Tokens (T) | Coherence (EC) | Overall |
|----------|------|--------------|-----------------|-----------|----------|----------------|------------|----------------|---------|
| `agents/sentinel.md` | agent | A | A | A | A | A | **B** (684 vs derived warn 650) | A | **B** |
| `agents/verifier.md` | agent | A | A | A | A | A | **B** (600 vs warn 550) | A | **B** |
| `agents/systems-architect.md` | agent | A | A | A | A | A | **B** (466 vs warn 400) | A | **B** |
| `agents/implementation-planner.md` | agent | A | A | A | A | A | **B** (445 vs warn 400) | A | **B** |
| `skills/skill-crafting/` | skill | A | A | A | A | A | **B** (418 vs warn 400) | A | **B** |
| `skills/deployment/` | skill | A | A | **B** (F07 marker placement) | A | A | A | A | **B** |
| `skills/llm-prompt-engineering/` | skill | A | A | **B** (F08, 130d) | A | A | A | A | **B** |
| `hooks/check_code_quality.py` + `hooks/format_python.py` | hook | A | **B** (CH01 dup) | A | A | A | A | A | **B** |
| `skills/stakeholder-communications/` | skill | A | A | A | A | A | A | **B** (EC02 reachability) | **B** |
| `commands/star-repo.md` | command | A | A | A | A | A | A | **B** (EC02 reachability) | **B** |

### Depth Disclosure

**A grade without its scope is a claim without a warrant.** This run executed as a single agent
under one turn budget; the 2026-08-07 baseline executed as a 5-lane orchestrated fan-out. The
grade improved partly because the ecosystem improved and partly because this sweep looked less
deeply at the LLM-judgment dimensions. Both are true and the distinction matters.

**Read in full:** `plugin.json`; every `SKILL.md` frontmatter (58); every agent frontmatter and
description (17); the `TECH_DEBT_LEDGER.md` + `TECH_DEBT_RESOLVED.md` pair (146 rows);
`calibration_log.md` (66 rows); the `§3a` tables of both `DESIGN.md` and `docs/architecture.md`;
the `## Delegation Checklists` section of `coordination-details.md` and its condensed counterpart
in `claude/config/CLAUDE.md`; all 333 ADR frontmatter blocks (programmatically); `agents/sentinel.md`'s
own Check Catalog; `.ai-state/observations.jsonl` (999 records).

**Executed mechanically at full depth:** every `A`-type check in the catalog, plus every
script-delegated gate (`measure_token_budget`, `check_gate_liveness`, `check_aac_golden_rule`,
`check_architecture_projection`, `aac_fence_validator`, `check_metrics_freshness`,
`check_spec_drift`, `check_spec_archival_gap`, `check_calibration_coverage`,
`check_readiness_feedback`, `check_p06_task_brief`, `clean_work_safety`, `adr_health`).

**Sampled rather than read exhaustively — the honest weak spots of this run:**

- **N04, N05, N06** (terminology consistency, rule-vs-prompt contradictions, description style) —
  judged from bulk frontmatter descriptions and targeted greps, **not** from a full read of all 58
  skill bodies and 17 agent bodies. A subtle terminology drift inside a skill body would not have
  been seen.
- **F03, F04** (current tools/patterns; prompts reflecting the current pipeline) — inferred from
  the clean X05/X06/EC01/EC03 name-resolution results rather than from reading collaboration
  sections prose-by-prose.
- **S05, S07, T05, T06** (progressive disclosure, rules-vs-procedures, redundancy) — checked
  structurally (line counts, presence of `references/`) rather than semantically. **T06 in
  particular is under-examined**: cross-artifact redundancy is exactly the kind of finding a
  structural check cannot produce, and the last run found instances of it.
- **X07, X08** — verified that every named agent *exists*; did **not** compare README catalog
  prose against frontmatter descriptions sentence-by-sentence.
- **SH03, SH05** — sampled on the 3 most recent specs of 8.
- **GL01, GL03** — sampled. GL03 examined this catalog's Pass conditions; it did **not** examine
  the verifier's phase gates, which the check also names.
- **EC05** — judged holistically, not against an enumerated expectation of artifact types.

**Checks not reached:** none by turn exhaustion. Every catalog check was reached and given a
verdict. **SH06** (spec delta claims vs actual spec evolution) is the one check recorded as
*not deeply executed* — it requires diffing archived predecessor specs pairwise, and no spec in
this corpus has a predecessor pair that changed since the last sweep.

**Skipped for absent substrate or unpopulated precondition** (never a WARN/FAIL on absence alone):
C09 (no `compose.yaml`/`Dockerfile`), F06 (same), TT04 (1 of the required ≥7 per-group metrics
reports), TT05 (no group declares a marker-selector entry), TT06 (topology exists), P07 (no
specialist design docs), P08 (`stale_safe: 1` < 3), PR01 (no `PRE_REFACTOR_PLAN.md`), HK01
(`PRAXION_HACKATHON_MODE` unset).

**Skipped for an unpopulated convention, which is not the same as an unreachable reader** —
**AC12**. The `.c4` model carries no `metadata.req_ids` and no archived SPEC carries
`architectural_elements:` frontmatter, so the bidirectional convention is not yet populated. The
Bash `grep` reader this check specifies **is** available to this agent; naming only "substrate
absent" would conceal that the check is runnable the day the first `req_ids` lands.

**Outside this agent's tool grant:** the `likec4` MCP server's `query-by-metadata` (grant is
`Read, Glob, Grep, Bash, Write` — no MCP). AC12 is specified against Bash precisely so this does
not make it unrunnable in principle.

## Findings

### Critical (blocks correct behavior)

*None.* No check in any dimension produced a Critical finding.

### Important (degrades quality or efficiency)

| # | Check | Dimension | Location | Finding | Recommended Action | Owner |
|---|-------|-----------|----------|---------|--------------------|-------|
| I-01 | T02 | Token Efficiency | always-loaded set (2 CLAUDE.md + 7 unconditional rules) | **Always-loaded budget at 94.2% utilisation — 23,558 / 25,000 tokens, 1,442 headroom.** Measured (`basis: tokenizer`, `chars_per_token: 3.806`), not estimated, so this is not a divisor artifact. The check **passes** (`over_by: 0`), but the margin is the thinnest on record: any new always-loaded rule, or ~380 words added to an existing one, breaches the ceiling. `rules/swe/swe-agent-coordination-protocol.md` and `rules/swe/adr-conventions.md` are the two largest contributors and both carry deep-dive pointers already — they are the natural candidates for further `paths:`-scoping or skill migration. | Re-measure before *any* always-loaded edit. Identify ~2,000 tokens to move behind `paths:` or into a skill, restoring working headroom. Do not treat 94.2% as a passing state to leave alone. | context-engineer |
| I-02 | TD06 + TD-dimension staleness | Technical Debt | `.ai-state/metrics_reports/METRICS_REPORT_2026-08-07_07-41-41.*` | **The substrate the entire TD dimension reads is 17 days and 39 commits stale.** `check_metrics_freshness.py` → `status: stale`; 3 of the top-7 hotspots are flagged `hotspot-moved-since-report` and barred from TD01 filing. Consequence this run: **TD01 and TD04 filed zero rows not because the repository is clean but because the ranking could not be trusted** — a silence that reads as health and is not. RD01's readiness level 4 and TD03's coverage figures rest on the same snapshot. | Run `/project-metrics` before the next sweep. Until then, treat every TD01/TD04 silence in this report as *unmeasured*, not *clean*. | user |
| I-03 | P03 | Pipeline Discipline | `.ai-state/observations.jsonl`, session `84edfc44-9ae8-45aa-86c2-bdbcff41aaf9`, `agent_id a045b98b6e9067762` | **One `agent_start` that did work never emitted a matching `agent_stop`.** Paired on `agent_id`, not `agent_type` (type-pairing would have passed this — a sibling instance stopped). The spawn emitted `tool_use` records, so it ran and then vanished: this is the lost-Stop-hook case, not a spawn that died on arrival. Its `agent_type` is `i-am:implementer`, which matches no file in `agents/` — a tell that the spawner may not have been this fleet, which narrows but does not eliminate the concern. **Shape: a rate of one, not an incident burst** — 1 unpaired start across 1 session, not a concentrated cluster. Denominator: **999 records** examined. | Investigate whether the `Stop` hook fires for spawns carrying a non-fleet `agent_type` prefix. If `i-am:` spawns are out-of-fleet by construction, add an explicit exclusion so the check's silence is a decision rather than an accident. | user |

### Suggested (improves but not urgent)

| # | Check | Dimension | Location | Finding | Recommended Action | Owner |
|---|-------|-----------|----------|---------|--------------------|-------|
| S-01 | T03 | Token Efficiency | `agents/sentinel.md` | 684 lines against a **derived** ceiling of warn 650 / fail 750 (catalog span `S` = 350). 34 lines over warn, 66 under fail. The derived form is working as designed — the breach is discretionary prose outside the catalog, not catalog growth. | Trim ~40 lines of non-catalog prose, or accept as a known state. Do **not** trim catalog rows to buy headroom — that inverts the exception's purpose. | context-engineer |
| S-02 | T03 | Token Efficiency | `agents/verifier.md` | 600 lines vs its documented fixed warn 550 / fail 700. Its bulk is a comprehensive Process plus the tested-in-place rework-spawn contract — substance, not drift. | Accept, or re-derive the threshold if Process grows further. Forcing it under 550 risks gutting tested substance. | context-engineer |
| S-03 | T03 | Token Efficiency | `agents/systems-architect.md` (466), `agents/implementation-planner.md` (445) | Both over the standard 400-line warn, both under the 500 fail. The warn line is calibrated to the standard-agent median, so these are the fleet's two heaviest standard agents. | Consider progressive disclosure — move deep procedure into `skills/software-planning/references/`, which both already cite. | context-engineer |
| S-04 | F10 | Freshness (check-spec defect) | `agents/sentinel.md` § F10 | **F10's stated derivation is wrong for this repo.** It maps `scripts/git-<name>-hook.sh` → `.git/hooks/<name>`, deriving a nonexistent `.git/hooks/finalize` from `scripts/git-finalize-hook.sh`. In reality that script is a **multiplexed entrypoint** installed byte-identically as `post-merge`, `post-commit` *and* `post-checkout`. A naive F10 run reports a false "MISSING installed" every sweep — a check that fires on correct behaviour, the failure mode the catalog repeatedly warns against. | Amend F10: resolve installed counterparts by *content* (compare each `.git/hooks/*` against every `scripts/git-*-hook.sh`) rather than by filename derivation. | context-engineer |
| S-05 | F07 | Freshness | `skills/deployment/references/gpu-compute-budgeting.md:2` | `GPU Compute Budgeting` is a cataloged `staleness_sensitive_sections` entry whose `last-verified: 2026-05-03` marker sits at **file line 2** — file-scope, not on the line below the cataloged heading. The other 5 cataloged sections in this skill are correctly anchored. The marker exists and is in-date (113d < 120), so this is placement, not decay — but a heading-anchored resolver cannot find it. | Move the marker to the line below the `## GPU Compute Budgeting` heading, matching its 5 siblings. | doc-engineer |
| S-06 | F08 | Freshness | `skills/llm-prompt-engineering/SKILL.md:75,226` | `Model Family Matrix` and `Platform Comparison` both `last-verified: 2026-04-16` = **130 days** against the default 120-day threshold (this skill declares no `staleness_threshold_days`). Not past 2× (240d), so WARN not FAIL. Two sibling markers in `contexts/python.md` and `contexts/typescript.md` carry the same date but are not cataloged. Model-family and platform tables are among the fastest-decaying content in the corpus. | Re-verify both tables against current model/platform reality and re-date all four markers. Consider declaring `staleness_threshold_days: 60` for this skill, as 5 other skills do. | doc-engineer |
| S-07 | T01 | Token Efficiency | `skills/skill-crafting/SKILL.md` | 418 lines (warn 400, fail 600). Next-largest are `rule-crafting` 399 and `context-security-review` 396 — a cluster right at the line. 55 of 58 skills are comfortably under. | Move one detailed section into `skills/skill-crafting/references/`, which already exists. Low urgency. | context-engineer |
| S-08 | EC02 | Ecosystem Coherence | `skills/stakeholder-communications/`, `commands/star-repo.md` | Anchored **only** in their own catalog file — no agent, rule, command, CLAUDE.md, hook or doc names either. Not orphans, but the least-reachable surfaces and the first place silent decay appears. **Improved from 4 to 2** since 2026-08-07 (`adapt-claude-to-agents` and `likec4-querying` both acquired anchors). | No action required. Re-check next sweep; if either is still unanchored in two sweeps, question whether it earns its place. | user |
| S-09 | CH01 | Code Health | `hooks/check_code_quality.py`, `hooks/format_python.py` | `_find_ruff` is **byte-identical** in both modules (AST-normalised exact-body match, 5 statements). The only cross-module duplicate group in the entire non-test `hooks/` + `scripts/` Python surface — so the codebase is otherwise clean on this axis, which makes the one instance worth naming rather than tolerating. | Extract to a shared helper, or accept explicitly as a deliberate no-shared-import constraint for hook modules (hooks run standalone under an ambient interpreter — if that is the reason, record it, because it is a good one). | implementer |
| S-10 | TD05 | Technical Debt | `.ai-state/TECH_DEBT_LEDGER.md` → `td-130` | `owner-role: unassigned`, first seen 2026-08-06 — **18 days** against the 7-day discipline threshold. The only such row among 21 active; all others carry an owner. Location is `commands/new-project.md`, `new_project.sh`. | Assign an owner-role (`context-engineer` fits the location) or close it. An unassigned row is one no consumer agent will pick up. | user |
| S-11 | CA02 | Calibration Accuracy | `.ai-state/calibration_log.md`, 3 rows | Three rows carry an **execution-mode annotation inside the `Actual Tier` cell** — e.g. `Standard — 5 concurrent scoped sentinels + orchestrator synthesis`, `Full — 6 concurrent agents + orchestrator verification`. The catalog is explicit that the tier cell holds the bare 5-tier enum and that execution-mode notes belong in `Source` prose. Effect: they present as calibration mismatches under normalization, depressing the measured match rate from a genuine **97.0%** to **92.4%**. The check still passes either way, but the metric is being polluted at source. | Move the annotations into the `Source` column on those 3 rows; leave the tier cells as bare enum values. Also note CA02's normalizer strips *parentheticals* only — extending it to em-dash suffixes would make it robust to a recurrence. | user |
| S-12 | GL03 | Gate Liveness | `agents/sentinel.md` § SH02 | **SH02 asserts a container, not substance.** Its Pass condition is "`## Traceability` section present and non-empty" — satisfied by a section containing one sentence of prose and no matrix. The dimension's own SH04 was tightened on exactly this basis on 2026-08-07 (column must *exist* before cells can be judged); SH02 is the same shape, one row above it, still untightened. All 8 specs happen to pass on substance today, so this is latent rather than active. | Amend SH02's Pass condition to "`## Traceability` section present **and containing ≥1 requirement row**", matching the substance clause SH04 already carries. | context-engineer |
| S-13 | S06 | Spec Compliance (catalog prose drift) | `agents/sentinel.md` § S06 | The catalog's S06 row states "`## Phase N` is the majority form (15 agents)". The actual heading level in every agent is **`### Phase N`** (h3, not h2), and the counts differ per agent (5–16 headings each). The check's *intent* — numbered stages with completion criteria — is fully satisfied by all 17 agents (`implementer` correctly uses its sanctioned ordered-list form). This is descriptive prose drifting from the artifacts it describes, in a row whose whole point is warning against literal-form matching. | Correct `## Phase N` → `### Phase N` in the S06 row, and drop the "(15 agents)" count — the catalog's own preamble forbids counts in prose for exactly this reason. | context-engineer |

### Pipeline Discipline

**P03** — **999 WAL records examined** (the denominator matters: zero findings over three records
would not be a clean fleet). 13 `agent_start`, 21 `agent_stop`, paired on `agent_id`.
**1 WARN** (I-03): one unpaired start that emitted `tool_use`, in **1 session** — a rate of one,
not a burst; no session-concentrated incident signature exists in this WAL. **0 INFO** for spawns
that emitted no `tool_use`. **10 unmatched stops → INFO**: front-truncation from WAL rotation and
`.ai-state/` merges, a boundary artifact, never a fleet defect. `start_correlation` distribution:
**11 absent** — records predating the field, reported as *unattested* rather than as agreement, and
falling back to the WAL alone; **8 `unobserved-start`** — the emitter's own claim of upstream
start-delivery loss, which is non-observation and not non-existence; **2 `paired`**, both
corroborated by the WAL. **Zero producer-vs-WAL disagreements.**
**Bound on the PASS:** P03 near-clean here means *almost no agent that did work went unstopped* —
it does **not** mean every spawn completed.

**P04** — PASS, bounded. Zero out-of-grant tool uses and zero read-only-agent write violations
across every `agent_type` resolving to a file in `agents/`. Non-fleet emitters (`main`, the
orchestrator; `claude-code-guide`, a foreign plugin agent) carry no `agents/*.md` grant to judge
against. **This check claims surface, never semantics**: an agent making an out-of-scope *decision*
inside a file it was legitimately assigned emits records indistinguishable from correct work.

**P05** — PASS. **P06** — PASS, regression closed. **P07, P08** — skipped, substrate below trigger.

### Tech-Debt Findings

**New rows filed this run: 1.**

| Row | Class | Severity | Why filed (LLM-judgment trace) |
|---|---|---|---|
| `td-147` | coverage-gap | important | Five `hooks/` modules below the 70% floor (worst: `auto_complete_install.py` at **38.07%** of 197 lines). Filed rather than folded into `td-146`'s deferral register because that register's own stated rationale — "modest sub-70% gaps in `scripts/` gates **with no gate-consuming caller**" — does not hold here: every one of these five is registered in `hooks/hooks.json` and fires on a live Claude Code lifecycle event, so a defect changes pipeline behaviour silently rather than failing an operator-invoked command. Different risk profile, different row. Read from the per-file coverage list, never the 84.47% aggregate. |

**Not filed, and why** — TD01 (hotspots): 3 of the top 7 are TD06-barred, and the #1 entry is a
test file whose churn signature is a recently-landed wave rather than debt. TD04 (complexity):
repo-wide p95 is 7.00/8.00 and flat; the single outlier is TD06-barred. Filing against a
39-commit-stale ranking would put unverifiable rows in a consumer-facing ledger. **Both silences
are unmeasured, not clean** — see I-02.

**TD05 discipline (writes no rows):** 1 WARN — `td-130` unassigned for 18 days (S-10). 125 terminal
rows all carry `resolved-by`; 0 stale `in-flight` rows.

**TD02 with its denominator:** `cyclic_deps: 0` across **278 of 345 tracked Python files (80.6%)
over 6 package roots**. Bounded claim, not a repo-wide all-clear.

Canonical entries: `.ai-state/TECH_DEBT_LEDGER.md`.

### Recommended Actions (prioritized)

1. **Run `/project-metrics`** — unblocks TD01/TD04, refreshes RD01 and TD03's basis, and converts
   this report's two "unmeasured" silences into evidence. Single command, highest leverage. (I-02)
2. **Reclaim always-loaded token headroom** — identify ~2,000 tokens in
   `swe-agent-coordination-protocol.md` / `adr-conventions.md` to move behind `paths:` or into a
   skill. At 94.2% the next always-loaded addition breaches the ceiling. (I-01)
3. **Investigate the unpaired `i-am:implementer` spawn** — decide whether non-fleet `agent_type`
   prefixes should be excluded from P03 by construction, and record the decision. (I-03)
4. **Fix F10's derivation** — resolve installed hooks by content, not filename. It currently
   reports a false MISSING every sweep. (S-04)
5. **Tighten SH02 and correct S06's prose** — two one-line catalog edits closing the last latent
   presence-only gate and one instance of catalog prose drifting from its artifacts. (S-12, S-13)
6. **Re-date the `llm-prompt-engineering` model/platform tables** and re-anchor the
   `deployment` GPU marker. (S-06, S-05)
7. **Assign or close `td-130`**; move the 3 calibration-log execution-mode annotations into
   `Source`. (S-10, S-11)
8. **Trim `agents/sentinel.md` non-catalog prose** by ~40 lines. (S-01)
