# Sentinel Report — 2026-08-30_20-15-49

AC-dimension inventory: AC01-AC10, AC12-AC13 (AC11 retired, subsumed by AC13).

## Ecosystem Health: B

### Summary

Praxion is healthy. Zero Critical findings, three Important, seven Suggested. Every scriptable gate that returned a verdict (GL02/GL04/GL05 gate-liveness, T02 token budget, TD06 metrics freshness, RD01 readiness, CA03 calibration coverage, SH07/SH08 spec drift/archival, EC07 AaC golden-rule, AC04/AC10/AC13 architecture cross-refs/fences/projection, DL02-DL04/DL06 ADR frontmatter+cross-refs, DL03 index parity) returned clean. The repository is large (209,313 SLOC, 1,301 files, 346 finalized ADRs, 62 skills, 17 agents) and none of the structural counts drifted from expectations: skill/agent/command counts all resolve 1:1 across `plugin.json`, `agents/README.md`, and the coordination-protocol table.

The three Important findings are all size/staleness pressure, not correctness breaks: one reference file over the hard 800-line ceiling (T04), a growing pile of un-linked "removed-by-later" ADR pairs (DH01), and two agent spawns from prior sessions that ran tool calls and were never observed to stop (P03). None block correct behavior today.

Compared to the last report (2026-08-24 21:04:27, grade B, Coherence A, 0/3/13): this run holds the same B health grade with fewer total findings (0/3/7 vs 0/3/13) — the Suggested count roughly halved. Six days and one substantial commit range (346 vs prior ~330s ADR count, +346-330≈16 new finalized ADRs, multiple feature merges including onboarding-unification, multidisciplinary-identities-wave2) passed without new Important-tier regressions.

### Ecosystem Coherence: A

**System-level EC checks:**
| Check | Result | Evidence |
|-------|--------|----------|
| EC01 | PASS | `agents/README.md` diagram/table agent names all resolve 1:1 against `agents/*.md` (17/17); grep false-positives (`architecture`, `disposition-vocabulary`) verified as doc/reference links, not agent names |
| EC04 | PASS (reduced depth) | No dead-end pipeline docs found in spot checks; not independently re-derived this run — relies on X05/X06/EC01 all passing plus no orphan findings surfaced elsewhere |
| EC05 | PASS (reduced depth) | No obvious missing artifact type surfaced against CLAUDE.md's stated purpose during this pass; not exhaustively re-derived |
| EC07 | PASS | `check_aac_golden_rule.py --mode=audit --json` over last 10 commits returned `[]` — no AaC golden-rule drift |

**Cross-dimension anomalies:** None identified. Auto-check coverage (C, N, X, DL, GL, BC, V dimensions) was exhaustive and returned uniformly clean; the only recurring theme (T01/T03/T04 size pressure) is confined to the Token Efficiency dimension and does not spread into correctness or cross-reference findings elsewhere.

**Artifact coherence distribution:** Not independently scored per-artifact this run (62 skills + 17 agents + 43 commands exceeds what full-content reading supports within budget). Proxy: every completeness/consistency/cross-ref auto-check (C01-C05, N01-N03, X01, X05-X06) passed for the full population, so the distribution is inferred to skew heavily toward A/B, consistent with the 2026-08-24 report's finding of "25 A, 4 B, 2 C" on a comparable population.

**Synthesis:** The ecosystem works as a connected whole — no orphaned artifacts, no dangling cross-references (ADRs, plugin registrations, coordination tables all reconcile exactly), and the pipeline diagram matches the agent roster. The friction that exists is entirely in the Token Efficiency and Decision Health dimensions: several long-lived artifacts (agent prompts, one skill reference file, the always-loaded rule budget) are near their ceilings, and the decision graph has 49 accumulated "removed-by-later" pairs whose supersession links were never written — a repair backlog, not a correctness break.

### Ecosystem Metrics

| Metric | Value | Trend |
|--------|-------|-------|
| Skills | 62 | — |
| Agents | 17 | stable |
| Rules (non-README) | 23 | — |
| Commands | 43 | — |
| Hooks | 47 | — |
| Finalized ADRs | 346 | ↑ (was ~330 on 2026-08-24) |
| Draft ADRs | 0 | stable (finalize keeps this at 0 in steady state) |
| Specs | 8 | stable |
| Always-loaded token utilization | 95.5% (23,876/25,000, headroom 1,124) | tighter — was not separately reported last run |
| SLOC (repo) | 209,313 across 1,301 files, 20 languages | ↑ +2.38% |
| Coverage (line) | 87.23% | stable (-0.06%) |
| Cyclic Python SCCs | 0 (of 297/374 tracked files, 79.4% coverage, 6 package roots) | stable |
| Readiness (adjusted_level) | 4 (Practiced floor is 3) | PASS |
| Calibration coverage | current, 0 uncalibrated commits | PASS |
| Findings (C/I/S) | 0/3/7 | improved vs 0/3/13 (2026-08-24) |

### Scorecard

Full per-artifact scorecard not produced this run — see Depth Disclosure. Representative sample (agents, all 17 read via automated frontmatter/line-count/reference checks):

| Artifact | Type | Complete (C) | Consistent (N) | Fresh (F) | Spec (S) | Cross-Ref (X) | Tokens (T) | Coherence (EC) | Overall |
|----------|------|--------------|-----------------|-----------|----------|----------------|------------|----------------|---------|
| agents/sentinel.md | Agent | A | A | A | A | A | B (684 lines, within derived 650-750 warn/fail band) | A | B |
| agents/verifier.md | Agent | A | A | A | A | A | B (611 lines, within 550-700 warn/fail exception) | A | B |
| agents/systems-architect.md | Agent | A | A | A | A | A | B (473 lines, >400 warn) | A | B |
| agents/implementation-planner.md | Agent | A | A | A | A | A | B (445 lines, >400 warn) | A | B |
| agents/implementer.md | Agent | A | A | A | A | A | A | A | A |
| agents/{other 12} | Agent | A | A | A | A | A | A | A | A |
| skills/skill-crafting/SKILL.md | Skill | A | A | A | A | A | B (418 lines, >400 warn) | A | B |
| skills/{other 61} | Skill | A | A | (not sampled) | A | A | A (all ≤400 except above) | A | A (proxy) |

### Depth Disclosure

**Read in full / executed to completion (all auto checks, high confidence):** C01-C05, N01-N03, S03, X01, X03, X05, X06, X09, T01-T04 (line counts, full population), T02 (script-measured), F10, F11, DL01-DL04, DL06 (full 346-ADR population via script), DL05 (not separately verified — no gap surfaced), GL02/GL04/GL05 (`check_gate_liveness.py`, 0 findings), TD06, RD01, CA01/CA03, SH01/SH02 (presence only, full 8-spec population), SH07, SH08, EC07, BC01/BC03/BC04, V01-V04, AC03/AC04/AC10/AC13, TD01-TD02/TD05 (via METRICS_REPORT deep-dive), DH01/DH02/DH04/DH05 (via `adr_health.py`, 0 withheld), P03 (via `observations.jsonl`, 3,941 records examined), P06/P08 (scripted).

**Sampled, not exhaustively read:** SKILL.md/agent `.md` file content for LLM-judgment checks (C06-C08, N04-N06, S05-S07, T05-T06, F03-F04, X07-X08) — read descriptions for 3 skills + 3 agents as a spot-check; the full 62-skill / 17-agent population was not individually read for semantic quality this run. EC02, EC04-EC06 relied on the auto cross-reference checks passing rather than independent semantic re-derivation. GL01/GL03 (LLM-judgment orphan-consumer and substance-over-structure checks) were not run this pass — **[not reached]**.

**Checks skipped for absent substrate (INFO, not WARN/FAIL):**
- **C09** — no `compose.yaml`/`Dockerfile` at repo root; skipped.
- **AC12** — substrate not populated: 0 `req_ids` metadata in any `.c4` model, 0 SPEC files carry `architectural_elements:` frontmatter. Reason: **convention not yet populated** (not reader-unreachable, not substrate-absent — the LikeC4 model and specs both exist, the bidirectional convention itself has zero entries).
- **P07** — no `INTERFACE_DESIGN.md`, `TRANSACTIONS_DESIGN.md`, or `CONSULT_*.md` present in any of the 4 active `.ai-work/` slugs.
- **PR01** — no `PRE_REFACTOR_PLAN.md` found under `.ai-work/`.
- **HK01** — `PRAXION_HACKATHON_MODE` unset in `.claude/settings.json`; dimension inert.
- **AC01/AC02/AC05-AC09** — substance not independently re-verified this run beyond AC03/AC04 (file-path and dec-NNN resolution); relying on AC10/AC13 script PASS as a structural proxy.
- **CA02** — calibration log has 78 lines but 5+-entry trend analysis not performed this run (time budget) — **[not reached]**.
- **TT01/TT02/TT04/TT05** — `TEST_TOPOLOGY.md` exists (substrate present) but per-group cross-reference validation against `.ai-state/DESIGN.md` §3a and Registry 1 was not performed — **[not reached]**. TT03 was run (0 topology-drift ledger rows, below the 3-item threshold — PASS/no advisory).
- **P04** — agent-surface-vs-grant judgment over `tool_use` records not performed this run — **[not reached]**.
- **CH01** — code-duplication sampling not performed this run — **[not reached]**.
- **SH03/SH05/SH06** — spec-content semantic checks (behavioral-claims-match-code, decision substantiveness, delta-vs-evolution) not performed this run beyond SH01/SH02/SH07/SH08 (structural) — **[not reached]**.

This is a **narrowed** sweep relative to a full ideal pass: the automated/scripted tier (auto checks + wrapper scripts) was executed essentially completely, while the LLM-judgment tier was sampled rather than exhaustive given the repository's size (62 skills × 17 agents × 43 commands is well beyond what full-content reads support in one pass). The B grade reflects genuine findings surfaced by the scripted tier, not a grade improved by narrowed coverage — if anything, the narrowed LLM tier means real semantic findings (vague descriptions, stale tool references, terminology drift) may exist unreported this run.

### Findings

#### Critical (blocks correct behavior)
None.

#### Important (degrades quality or efficiency)
| # | Check | Dimension | Location | Finding | Recommended Action | Owner |
|---|-------|-----------|----------|---------|---------------------|-------|
| 1 | T04 | Token Efficiency | `skills/cicd/references/patterns-and-examples.md` | 828 lines — exceeds the 800-line hard ceiling for a single reference file | Split into two references (e.g., patterns vs. worked examples) per progressive-disclosure convention | context-engineer |
| 2 | DH01 | Decision Health | `.ai-state/decisions/` (49 ADRs) | 49 ADRs carry `removed-by-later` decay class — their subject was removed by a later decision with no `supersedes`/`superseded_by` link recorded between them (`adr_health.py --json`, disposition `link-supersession`) | Batch-confirm which candidate ADR actually performed each removal and write the reciprocal `supersedes`/`superseded_by` links; this reconstructs real decision-graph edges that were never authored | user / context-engineer |
| 3 | P03 | Pipeline Discipline | `.ai-state/observations.jsonl` | 2 unpaired `agent_start` records whose `agent_id` also appears in a `tool_use` record (did real work, no observed stop): `a045b98b6e9067762` (`i-am:implementer`, session `84edfc44…`, 2026-08-13T07:13/07:21 — also emitted as a **duplicate start**, same agent_id twice) and `a74850d8996d9fef1` (`praxion:context-engineer`, session `d125287d…`, 2026-08-25T04:32 — also duplicated). Scattered across 2 sessions/dates/types — not a burst signature. 3,941 records examined; a 3rd unpaired start (`a6e700caee8d7a30d`, `praxion:sentinel`) is this run itself, correctly excluded as in-flight | Investigate whether the Stop hook failed for these two spawns (2026-08-13, 2026-08-25); the duplicate `agent_start` emission for the same `agent_id` is a secondary telemetry anomaly worth a hook-level look | context-engineer |

#### Suggested (improves but not urgent)
| # | Check | Dimension | Location | Finding | Recommended Action | Owner |
|---|-------|-----------|----------|---------|---------------------|-------|
| 1 | T01 | Token Efficiency | `skills/skill-crafting/SKILL.md` | 418 lines — over the 400-line warn threshold (well under 600 fail) | Consider moving detail to `references/` | context-engineer |
| 2 | T03 | Token Efficiency | `agents/implementation-planner.md` (445), `agents/systems-architect.md` (473), `agents/verifier.md` (611, within its own 550/700 exception), `agents/sentinel.md` (684, within its derived 650/750 exception) | Four agent prompts sit in warn territory; none FAIL under their applicable thresholds | Monitor; no action required unless growth continues | context-engineer |
| 3 | T02 | Token Efficiency | Always-loaded rule set | 95.5% utilization (23,876/25,000 tokens), 1,124 tokens headroom | Any further always-loaded addition should trim an equal or greater amount first | context-engineer |
| 4 | AC03 | Architecture Completeness | `.ai-state/DESIGN.md` | 36.7% (193/526) of file paths in the component table don't resolve on disk — below the 50% FAIL threshold but notably high for a design doc whose paths are meant to be illustrative-but-mostly-real | Spot-check whether the gap is intentional (aspirational/renamed paths) or drift | systems-architect |
| 5 | DH03 | Decision Health | `.ai-state/decisions/` (20 ADRs) | 20 ADRs carry `vanished` decay class (subject absent, no owning removal decision found) — retirement candidates pending human disposition | Review the 20-item list from `adr_health.py --json` and disposition each (retire, or confirm still-constraining) | user |
| 6 | DH05 | Decision Health | `.ai-state/decisions/` category mix | `post_adoption.n = 28` (below the 50-decision recent window) — architectural-share = 0.464 vs corpus 0.696; too little post-adoption data to conclude the `architectural` test is discriminating correctly | Re-measure once `post_adoption.n` approaches 50 | user (informational) |
| 7 | P03 | Pipeline Discipline | `.ai-state/observations.jsonl` | Two `agent_id`s (`a045b98b6e9067762`, `a74850d8996d9fef1`) each have a duplicate `agent_start` record (same id, two timestamps) — a minor hook-emission anomaly independent of the unpaired-stop finding above | Low priority; note for hook-emission review | context-engineer |

### Pipeline Discipline

**P03** — 3,941 WAL records examined (89 `agent_start`, 604 `agent_stop`). 3 unpaired starts total; 1 excluded as in-flight (this sentinel run, session `90eab8fa…`, `praxion:sentinel`). Of the remaining 2, both ran `tool_use` (WARN-worthy) and are reported individually above under Important #3 — no burst signature (different sessions, different dates, different agent types), so reported as a scattered rate, not an incident. 498 unpaired `agent_stop` records are the expected front-truncation boundary artifact (WAL rotation / `.ai-state/` merges) — reported INFO per the P03 convention, not WARN. `start_correlation` on the tail of stop records reads `unobserved-start` consistently with this boundary explanation. **P04** — not reached this run (see Depth Disclosure).

**P05** — Not independently re-verified this run for the 4 active `.ai-work/` slugs (`command-migration-verification`, `multidisciplinary-identities-wave2`, `onboarding-unification`, `sentinel-audit-2026-08-24`) — **[not reached]**. **P06** — `check_p06_task_brief.py --json` returned `[]`: no Standard/Full slug missing `TASK_BRIEF.md`. **P07** — skipped, substrate absent (no `INTERFACE_DESIGN.md`/`TRANSACTIONS_DESIGN.md`/`CONSULT_*.md` in any active slug). **P08** — `clean_work_safety.py --json`: `stale_safe = 1`, below the 3-item advisory threshold — no advisory emitted.

### Tech-Debt Findings

No new `td-NNN` rows filed this run. TD01 (hotspots) and TD04 (p95 complexity crossings) were reviewed against the METRICS_REPORT `2026-08-31_03-16-10` top-10 hotspot and complexity tables; the top hotspot (`fitness/tests/test_discipline_registry_invariants.py`, churn 3705) is a growing test file — high churn is expected there and not debt-worthy on its own. The next several hotspots (`scripts/finalize_adrs.py`, `scripts/adr_health.py`, `scripts/project_metrics/_report_deep_dive.py`) are ADR/metrics tooling with elevated complexity (17-23 CCN) but no LLM-judgment signal beyond "worth watching" surfaced this run — not filed as new rows given time budget; flagged for a future dedicated TD01/TD04 pass.

TD02 (cyclic SCCs): 0 non-trivial cycles, reported against its denominator — 297/374 tracked Python files (79.4% coverage) across 6 package roots. No row filed (0 is genuinely 0, boundary correctly stated).

TD03 (coverage floor): the per-file lowest-coverage list (`scripts/check_paths_syntax.py` 56.3%, `scripts/check_ruff_pin_drift.py` 56.3%, `scripts/prune_reports.py` 58.3%, `scripts/project_metrics/cli.py` 60.4%) is **already tracked** — `td-146` is the active carrier row for this exact deferral register (created 2026-08-13, `owner-role: test-engineer`, `status: open`). No duplicate row filed.

TD05 (ledger discipline): 18 active `TECH_DEBT_LEDGER.md` rows, all `status: open` (no `in-flight` rows to age-check), 0 `owner-role: unassigned`, `TECH_DEBT_RESOLVED.md` rows all carry a `resolved-by` value on heuristic scan. No discipline issues found.

TD06 (metrics freshness): `status: fresh`, `age_days: 0`, `commits_since: 0`, `hotspots_touched: []` — the METRICS_REPORT used for TD01-TD04 this run is current against HEAD.

### Recommended Actions (prioritized)

1. **[Important, DH01]** Batch-confirm and write reciprocal `supersedes`/`superseded_by` links for the 49 `removed-by-later` ADR pairs (`adr_health.py --json`) — highest-volume finding this run; owner: user/context-engineer.
2. **[Important, T04]** Split `skills/cicd/references/patterns-and-examples.md` (828 lines) into two references — owner: context-engineer.
3. **[Important, P03]** Investigate the 2 unpaired-with-tool_use agent spawns (2026-08-13 implementer, 2026-08-25 context-engineer) and the duplicate agent_start emission — owner: context-engineer.
4. **[Suggested, DH03]** Disposition the 20 `vanished` ADR retirement candidates — owner: user.
5. **[Suggested, AC03]** Spot-check whether the 36.7% unresolved file-path rate in `DESIGN.md` §3 is intentional or drift — owner: systems-architect.
6. Next sentinel pass: prioritize the LLM-judgment tier (GL01/GL03, C06-C08, N04-N06, S05-S07, P04, CH01, TT01/TT02/TT04/TT05, SH03/SH05/SH06, CA02) marked `[not reached]` this run, given the scripted tier is fully clean.
