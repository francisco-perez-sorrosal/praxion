# Sentinel Report

**Run**: 2026-09-04_22-41-32 | **HEAD**: bc67256c | **Branch**: main | **Scope**: full ecosystem sweep
**Prior run**: SENTINEL_REPORT_2026-08-30_20-15-49.md (grade B, 546 artifacts, 0/3/7)

## Inventory

| Family | Count |
|---|---|
| Skills (`SKILL.md`) | 62 |
| Skill reference leaves | 239 |
| Agents | 17 |
| Rules (excl. catalog READMEs) | 23 |
| Commands | 43 |
| Hooks | 54 |
| Scripts | 160 |
| ADRs finalized | 369 |
| ADR drafts | 0 |
| Archived specs | 9 |
| MCP servers | 1 |
| **Total audited artifacts** | **569** |

Prior run: 546. Delta +23 (chiefly the sidecar module family, its scripts and tests, and dec-355..dec-369).

## Per-Dimension Results

### Completeness (C)

| Check | Result | Evidence |
|---|---|---|
| C01 | PASS | 62/62 skill dirs carry `SKILL.md` |
| C02 | PASS | All 62 `SKILL.md` frontmatter blocks carry a non-empty `description` |
| C03 | PASS | All 17 agents carry non-empty `name`, `description`, `tools` |
| C04 | PASS | All 43 commands carry a non-empty, non-placeholder `description` |
| C05 | PASS | `plugin.json` lists 17 agents; 17 files on disk; no missing, no unregistered |
| C06 | PASS | Skill descriptions carry activation triggers (see Pass 2) |
| C07 | PASS | Agent descriptions discriminate (see Pass 2) |
| C08 | PASS | No unfilled `[CUSTOMIZE]` blocks outside template assets |
| C09 | n/a → PASS | No project-root `compose.yaml`/`Dockerfile` (only `skills/deployment/assets/` fixtures), so the trigger does not fire; `.ai-state/SYSTEM_DEPLOYMENT.md` nonetheless exists at 213 lines with all 10 sections populated |

### Consistency (N)

| Check | Result | Evidence |
|---|---|---|
| N01 | PASS | All 62 skill dirs lowercase kebab-case; suffix conventions held |
| N02 | PASS | All 17 agent filenames lowercase kebab-case `.md` |
| N03 | PASS | No unrecognized frontmatter keys in skills/agents/rules |
| N04 | PASS | Terminology stable across the sidecar delta (see Pass 2) |
| N05 | PASS | No rule↔agent boundary contradictions found |
| N06 | PASS | Description tone/structure consistent across catalogs |

### Spec Compliance (S)

| Check | Result | Evidence |
|---|---|---|
| S01 | PASS | All `SKILL.md` carry non-empty `description` |
| S02 | PASS | All agents carry `name`/`description`/`tools` |
| S03 | PASS | All 23 rule files open with a level-2 heading (post-frontmatter) |
| S04 | PASS | Argument-substituting commands carry `$ARGUMENTS` |
| S05 | PASS | Progressive disclosure held: max `SKILL.md` 418 lines, 239 reference leaves |
| S06 | PASS | Numbered, terminated stages present in all agents (`### Phase N`, `### Round N`, ordered list) |
| S07 | PASS | Rules remain declarative; no procedural drift into `rules/` |

### Cross-Reference Integrity (X)

| Check | Result | Evidence |
|---|---|---|
| X01 | PASS | All 17 `plugin.json` agent paths resolve |
| X02 | PASS | `skills/` — 62 dirs each with `SKILL.md`; `commands/` — 43 non-empty files |
| X03 | PASS | Every dir in `CLAUDE.md` § Repository layout exists |
| X04 | PASS | Idea-ledger implemented entries resolve to real artifacts |
| X05 | PASS | Coordination-protocol Available Agents table ↔ `agents/` 1:1 (17) |
| X06 | PASS | `agents/README.md` table ↔ `agents/` 1:1 (17; one non-agent `--agents` flag row is a CLI mention, not a roster row) |
| X07 | PASS | README catalog descriptions consistent with frontmatter |
| X08 | PASS | Collaboration sections name only existing agents |
| X09 | PASS | Every `dec-NNN` in `SYSTEM_DEPLOYMENT.md` § 9 resolves |

### Token Efficiency (T)

| Check | Result | Evidence |
|---|---|---|
| T01 | WARN | `skills/skill-crafting/SKILL.md` = 418 lines (warn 400). All others ≤ 399. No fails. |
| T02 | PASS (tight) | `measure_token_budget.py`: **24,606 / 25,000 tokens, `over_by: 0`, headroom 394 (98.4% utilisation)**. `basis: tokenizer`, `measured: true` — a real measurement, not an estimate. |
| T03 | WARN ×2 | `agents/sentinel.md` 689 lines vs derived ceiling warn `S+300` = **653** (catalog span S=353), fail 753 → **WARN**. `agents/verifier.md` 612 vs warn 550 / fail 700 → **WARN**. All 15 other agents ≤ 476 (standard warn 400 → `systems-architect` 476 and `implementation-planner` 447 also exceed warn). |
| T04 | WARN | `skills/cicd/references/patterns-and-examples.md` = 828 lines (> 800). Sole offender across 239 leaves. |
| T05 | PASS | No monolith that would split cleanly without losing coherence |
| T06 | PASS | No significant cross-artifact redundancy introduced by the delta |

### Freshness (F)

| Check | Result | Evidence |
|---|---|---|
| F01 | PASS | Referenced file paths resolve across audited artifacts |
| F02 | PASS | 9 apparent dangling `references/*.md` are all cross-skill shorthand (`<sibling-skill> → references/<leaf>`); every named leaf exists in its owning skill (`beautiful-code`, `cicd`, `deployment`, `llm-prompt-engineering`, `data-structure-design`, `test-coverage`, `testing-strategy`). `test-coverage`'s `references/go.md` is explicitly labelled a future leaf. |
| F03 | PASS | No references to replaced tools; the Streamlit→Next.js migration is narrated, not stale |
| F04 | PASS | Agent collaboration sections match the current pipeline roster |
| F05 | PASS | 14 path-shaped refs in `SYSTEM_DEPLOYMENT.md`; the 4 non-resolving (`streamlit_app/`, `app/`, `store/`, `references/secrets-management.md`) are narrated-as-removed or skill-relative, not broken links |
| F06 | skip | No project compose files (only `skills/deployment/assets/` fixtures) |
| F07/F08/F09 | PASS | No skill declares `staleness_sensitive_sections`; no invalid or future-dated markers |
| F10 | PASS | `scripts/git-finalize-hook.sh` resolves **by content** to three installed hooks (`post-checkout`, `post-commit`, `post-merge`) — byte-identical. `.git/hooks/pre-commit` matches no `scripts/git-*-hook.sh` source, which is correct: `scripts/git-pre-commit-hook.sh` was removed by dec-246 (unified into `commit_gate.sh`). No findings. |
| F11 | PASS | `doc_manifest.yaml` `generated_at` 2026-09-05T05:31:31Z post-dates the newest set-changing (`--diff-filter=ADR`) commit under `docs/`/`.ai-state/` (`7a579435`, 2026-09-04T19:55:45-07:00) |

### Decision Log (DL)

| Check | Result | Evidence |
|---|---|---|
| DL01 | PASS | 369 finalized ADRs, 0 drafts (the sidecar-remediation drafts were finalized at `bc67256c`) |
| DL02 | PASS | All 369 ADRs parse; all 8 required frontmatter keys present **and non-empty** on every record; every `id` matches `dec-\d{3}`. `dec-365`'s previously unparseable `summary:` was fixed at `53c72b5e` — **regression closed** |
| DL03 | PASS | `DECISIONS_INDEX.md` holds 369 rows; id sets identical in both directions |
| DL04 | PASS | 0 dangling pointers across `supersedes` (string \| list), `superseded_by`, `re_affirms`, `re_affirmed_by`, `retired_by`, `supersedes_in_part`, `superseded_in_part_by` — every list element resolved individually |
| DL05 | PASS | 9 archived specs; each feature cohort carries ADRs (sidecar → dec-355..369) |
| DL06 | PASS | 0 one-directional links. Every `supersedes`/`superseded_by`, `re_affirms`/`re_affirmed_by`, and `supersedes_in_part`/`superseded_in_part_by` pair is reciprocal, iterating every list element. `retired_by` correctly exempt |

### Decision Health (DH)

`scripts/adr_health.py --json` — 1,630 references scanned across 369 ADRs; `withheld: []` (every oracle answered, so `vanished` is a complete residual); `skipped_terminal: 20` (terminal-status decisions excluded by design — the dimension examined 349 of 369).

| Check | Result | Evidence |
|---|---|---|
| DH01 | PASS | 0 `removed-by-later` findings — no decision graph edges missing |
| DH02 | PASS | 0 `renamed` / `placeholder-shape` / `out-of-repo`. The 57 `removed-by-self`, 3 `renamed-by-self` and 3 `lazy-artifact` are explicitly **not findings** |
| DH03 | INFO | 5 `vanished` retirement candidates: `dec-160` (3 dashboard overview paths) and **`dec-357` + `dec-358`, both citing `.ai-work/sidecar-placement/INTERFACE_DESIGN.md`** — see Important finding I-1 |
| DH04 | WARN | 1 `reopen_candidate`: **`dec-360`** (containment-guard state root) is `retired`, yet all four paths it names resolve again — `dashboard_app/src/server/artifacts/project-root.ts`, `hooks/worktree_guard.py`, `scripts/_state_repo.py`, `scripts/praxion-dashboard`. New this run, directly attributable to the sidecar delta |
| DH05 | measurement | `architectural_share_recent` **0.48** (n=50 window) vs corpus **0.686**; `post_adoption` n=51, share **0.49** against an adoption baseline of **72%**. `post_adoption.n` (51) now exceeds the 50-row window, so this reads as authoring behaviour, not history: the share has fallen 72% → 49%, i.e. **the `architectural` test is discriminating**. No flag. |
| DH06 | PASS | `status_edge_conflicts: 0` — no shape (a)–(e) partial-supersession conflicts |

### Test Topology (TT)

| Check | Result | Evidence |
|---|---|---|
| TT01 | PASS | 13 groups; all 13 distinct `subsystems` values resolve to §3a Built components in `.ai-state/DESIGN.md` (16 rows, 15 Built) |
| TT02 | PASS | All 13 `selectors` use `pytest-globs`, a registered Registry-1 identifier |
| TT03 | PASS | 0 open `topology-drift` ledger rows (threshold 3) |
| TT04 | skip | 0 of 10 `METRICS_REPORT_*.json` carry per-group runtime data (threshold 7) |
| TT05 | PASS (n/a) | No group declares a marker selector — the topology documents this deliberately (line 175-177): no `[tool.pytest.ini_options] markers` entries are required, so there is no reserved-name collision surface |
| TT06 | skip | `.ai-state/TEST_TOPOLOGY.md` exists |

### Spec Health (SH)

| Check | Result | Evidence |
|---|---|---|
| SH01 | PASS | Path-column cells conform; live file references resolve |
| SH02 | PASS | All 9 specs carry `## Traceability` **with ≥1 requirement row** (10/15/13/9/25/18/13/16/**41**) |
| SH03 | PASS | Behavioral claims match implementation (see Pass 2) |
| SH04 | PASS | **All 9 matrices carry a verification column** (`Verification` / `Test reference` / `Tests` / `Verifying AC` / `Test(s)`), and **0 requirement rows have an empty verification cell**. The 7 apparently-empty cells in `SPEC_multidisciplinary_identities` belong to a second coverage-bucket summary table, not to requirement rows. 0 `UNTESTED` markers |
| SH05 | PASS | Key Decisions sections carry what/why/alternatives |
| SH06 | PASS | `SPEC_sidecar-placement_2026-09-03.md` (41 REQs, 41/41 COVERED, 0 UNTESTED/TEST-ONLY/MISSING) is a new spec with no predecessor — no delta claims to contradict |
| SH07 | PASS | `check_spec_drift.py --json`: 0 findings |
| SH08 | PASS | `gap: false` — newest spec 2 days old; no qualifying ADR cluster |

### Calibration Accuracy (CA)

| Check | Result | Evidence |
|---|---|---|
| CA01 | PASS | 84 rows; header `Timestamp \| Task \| Signals \| Recommended Tier \| Actual Tier \| Source \| Retrospective` |
| CA02 | PASS | 83 comparable rows after stripping parenthetical execution-mode annotations; **95.2% match rate** (79/83, floor 60%). Largest single override pattern 1.2% (floor 40%). The 4 mismatches are 3 genuine one-tier adjustments plus 1 prose-valued Recommended cell |
| CA03 | PASS | `covered: true` — newest calibration 2026-09-04 (the sidecar-remediation row); 0 uncalibrated pipeline commits |

### Readiness Feedback (RD)

| Check | Result | Evidence |
|---|---|---|
| RD01 | PASS | `adjusted_level: 4` ≥ floor 3; `mechanical_only: false` (full LLM tier ran) |

### Behavioral Contract (BC)

| Check | Result | Evidence |
|---|---|---|
| BC01 | PASS | `rules/swe/agent-behavioral-contract.md` exists, `load: always_on`, no `paths:`, names all four behaviors |
| BC02 | PASS | `~/.claude/CLAUDE.md` and project `CLAUDE.md` both name Surface Assumptions → Register Objection → Stay Surgical → Simplicity First in canonical order, identical spelling |
| BC03 | PASS | Exactly **14** agents reference the rule, and they are exactly the 14 the catalog names |
| BC04 | PASS | `### Behavioral Contract Findings` present with all 6 canonical tags |

### Architecture Completeness (AC)

| Check | Result | Evidence |
|---|---|---|
| AC01 | PASS | `.ai-state/DESIGN.md` present with 16 populated §3a rows |
| AC02 | PASS | Every Data Flow component appears in the §3a Components table |
| AC03 | PASS | 321 slash-bearing paths in §3a–§4; 45 non-resolving = **14.0%**, far under the 50% WARN line. Misses are relative sub-paths (`contexts/react.md`), globs, and lazy artifacts |
| AC04 | PASS | Every inline `dec-NNN` in `.ai-state/DESIGN.md` and `docs/architecture.md` resolves to a finalized record |
| AC05 | PASS | `docs/architecture.md` exists, 348 lines |
| AC06 | PASS | Component names resolve to actual modules |
| AC07 | WARN | 241 paths in the guide's §3a; 5 root-anchored non-resolving: `.ai-state/eval_ledger/`, `.ai-state/eval_ledger/EVAL_LOG.md`, `.ai-state/training_runs/` (all declared `optional-lazy`), plus scratch `tmp/issue.md`, `tmp/prism.md`. Suggested — see S-2 |
| AC08 | PASS | No Status column in §3a/§3b; **no** `Planned`/`Designed`/`Proposed`/`Not built` value in any table |
| AC09 | **PASS** | **Both directions clean.** (a) 16/16 guide §3a components appear in `.ai-state/DESIGN.md` §3a. (b) All **15 `Status: Built`** design-doc components appear in `docs/architecture.md` §3a — 0 coverage gaps. The two-Built-components gap flagged in earlier runs is closed |
| AC10 | PASS | `aac_fence_validator.py`: Verdict PASS on both `.ai-state/DESIGN.md` and `docs/architecture.md` |
| AC12 | **skip — convention not yet populated** | Substrate (a) specs and (b) a LikeC4 model (`docs/diagrams/architecture/src/architecture.c4`) are both present, but precondition (c) fails: `grep -rn 'req_ids' --include='*.c4'` returns **0 hits** and **0** archived specs carry `architectural_elements:` frontmatter. This is *not* "substrate absent" and *not* "reader unreachable" — the Bash reader works; the bidirectional convention has simply never been populated, so there is nothing to orphan in either direction |
| AC13 | PASS | `check_architecture_projection.py --json`: **0 findings**, `withheld: []`, 16 rows ↔ 16 elements. Both halves clean — no `element-without-row`, `row-without-element`, `unknown-element`, `not-structural`, `block-without-row`, or `row-without-block` |
| AC14 | advisory | `checkpoint_state: present`, mark `dec-354` asserted 2026-08-31, corpus tip `dec-369`, `unparseable: 0`. **8 un-folded architecture-bearing ADRs**: dec-355, 356, 359, 361, 362, **364, 365, 366**. Expected steady state between architect visits — not a gate. Note the last three are the sidecar family and carry `cited_in_guide: false` |

### Ecosystem Coherence (EC)

| Check | Result | Evidence |
|---|---|---|
| EC01 | PASS | Every agent named in the `agents/README.md` pipeline diagram has a file |
| EC02 | PASS | **0 orphaned artifacts** across 62 skills, 43 commands, 23 rules — each referenced by ≥1 agent, rule, command, skill, or CLAUDE.md |
| EC03 | PASS | Every named collaborator exists and is pipeline-reachable. Reciprocity deliberately not asserted |
| EC04 | PASS | Every pipeline output doc has a producing and a consuming agent |
| EC05 | PASS | No structural gap against CLAUDE.md's stated purpose |
| EC06 | PASS | Condensed block in `claude/config/CLAUDE.md` ↔ `coordination-details.md § Delegation Checklists`: all four agents' **produced** deliverables match, and all five conditional clauses (`if deployment in scope`, `if pre-refactor outcome`, `if structural gaps found`, `if structural`, `if step runs tests`) appear in both |
| EC07 | PASS | `check_aac_golden_rule.py --mode=audit --json`: `[]` over the last 10 commits — the sidecar delta introduced no AaC golden-rule drift |

### Gate Liveness (GL)

| Check | Result | Evidence |
|---|---|---|
| GL01 | PASS | Every named section an agent is told to read/harvest has a producing instruction |
| GL02 | PASS | `check_gate_liveness.py --json`: 0 `forbidden-pattern` findings |
| GL03 | PASS | Sampled this catalog and the verifier's phase gates; no presence-only gate found where substance is the contract. C09, SH02, SH04, V04 and AC01 all carry explicit substance clauses |
| GL04 | PASS | 0 `uninvoked-gate` findings across 160 scripts |
| GL05 | PASS | 0 `ambient-import` findings. Confirmed independently: ambient `python3` is 3.11.5 with `yaml` available; all 14 gate scripts executed cleanly |
| GL06 | PASS | 0 `discarded-verdict` findings — no exit-1 gate stranded on a block-only event |

### Pipeline Discipline (P)

Substrate: `.ai-state/observations.jsonl` — **15,599 records, 0 unparseable**. (Not Task Chronograph: no history, no grant.)

| Check | Result | Evidence |
|---|---|---|
| P03 | WARN | **6 unpaired `agent_start` records** out of 211 starts outside the newest session (**2.8%**), each of which emitted `tool_use` (i.e. ran work then vanished). **This is a rate, not an incident**: the 6 sit in **6 distinct `session_id`s** spanning 2026-08-13 → 2026-09-05 — largest single session's share is **1 of 6**. Types: `implementer` ×4, `context-engineer` ×1, `systems-architect` ×1; all in-roster after `<namespace>:` stripping. 0 unpaired starts emitted no `tool_use`. **Bound**: a clean P03 would mean *no agent that did work went unstopped* — this run is not clean on that reading. |
| P03 (INFO) | INFO | 2,759 `agent_stop` records have no matching start. 2,756 self-attest `start_correlation: unobserved-start` (upstream start-delivery non-observation); 3 carry **no** field at all and are **unattested**, not agreeing. The corpus-wide ratio — 3,013 stops against 257 starts — exceeds what front-truncation alone explains and is worth a producer-side look; per catalog these remain INFO, never WARN |
| P04 | WARN | **0 out-of-grant tool uses** across 230 agents with `tool_use` records against 17 known grants. One **surface-ownership** hit: `implementer` emitted **11 `Edit` calls under `.ai-state/decisions/`** (11 finalized ADR files, `adr-living-view` task) — the exact golden bad-case, though plausibly a sanctioned plan step since the task was ADR-corpus work. `discipline-consultant`'s 12 `Edit`s all target its own `CONSULT_*.md`; `researcher`'s 1 `Edit` is within grant. **Bound: this check claims the surface, never the semantics** — an agent making a design decision inside a file it was legitimately assigned emits records indistinguishable from correct work |
| P05 | PASS | 4 handoff docs across `.ai-work/`; **0 empty required sections** (8/7/13/28 sections, all populated) |
| P06 | PASS | `check_p06_task_brief.py --json`: `[]` — no Standard/Full slug missing `TASK_BRIEF.md` |
| P07 | PASS | 1 `INTERFACE_DESIGN.md` with an empty `## Architecture Challenges` (no flag); 0 `TRANSACTIONS_DESIGN.md`; 0 `CONSULT_*.md` in the canonical checkout |
| P08 | skip | `clean_work_safety.py`: `stale_safe: 1` (< 3 advisory threshold) of 9 slugs (0 block / 4 warn / 5 safe) |

*(P01, P02 retired — specified against events no producer ever wrote.)*

### Self-Verification (V)

| Check | Result | Evidence |
|---|---|---|
| V01 | PASS | `./agents/sentinel.md` in `plugin.json` agents array |
| V02 | PASS | Coordination-protocol Available Agents table carries a sentinel row |
| V03 | PASS | `agents/README.md` carries a sentinel row |
| V04 | PASS | `## Check Catalog` present with ≥1 check-ID row; **every `###` dimension heading the catalog declares carries ≥1 row** (derived per-dimension clause — 0 hollow dimensions) |

### Technical Debt (TD)

Source: `.ai-state/metrics_reports/METRICS_REPORT_2026-08-31_03-16-10.{md,json}` (schema 1.2.1, commit `2ac930a5`). Age 4 days — **inside** the 14-day age rule, so no age-based staleness WARN. `coverage.status: ok`.

**But days are not the unit that matters** — TD06 reports `status: stale` on commit distance, and that is the gate for filing.

| Check | Result | Evidence |
|---|---|---|
| TD06 | WARN | `check_metrics_freshness.py --json`: `status: stale`, `withheld: []`. Three ranked hotspots moved since the report commit and are **barred from TD01 filing**: `scripts/finalize_adrs.py` (#2, 3 commits), `scripts/adr_health.py` (#3, 1 commit), `scripts/test_finalize_adrs.py` (#6, 7 commits). Recommend re-running `/project-metrics`. |
| TD01 | 1 row filed | 8 ranked hotspots. #2/#3/#6 barred by TD06. #1 `fitness/tests/test_discipline_registry_invariants.py` (score 74100) already carried by open row **td-105** — dedup hit, not re-filed. #5/#7/#8 (`reconcile_pipeline_state.py`, `clean_work_safety.py`, `resolve_test_scope.py`) sit at or below the 3× p95 complexity line — high churn on actively-evolving operational scripts is feature growth, not decay. **#4 `scripts/project_metrics/_report_deep_dive.py`** is the only unbarred candidate where high churn (560) and high complexity (ccn 23, above the 3×-p95 line of 21) coincide — the churn×complexity signal TD01 exists to surface. → **td-166** |
| TD02 | PASS | `cyclic_sccs: []` — **0 non-trivial cycles**. Stated with its denominator, as the check requires: *no cycles in **297 of 374** repo Python files (**79.4%** file coverage) across **6** package roots* (`eval`, `fitness`, `scripts`, `tests`, `eval/src/praxion_evals`, `task-chronograph-mcp/src/task_chronograph_mcp`), 148 modules analysed. The ~20% shortfall is pydeps' package-analyser boundary — directories of loose modules with no `__init__.py` yield an empty graph and lie outside the collector's reach by construction. This is **not** a repo-wide all-clear. No rows filed. |
| TD03 | PASS (no rows) | The rendered **Lowest-covered files** list is bounded at **65.25%**, which is *below* the 70% floor, so per the check I read `coverage.per_file` in the JSON sibling for the remainder. Filtering the 15 pytest-tmpdir artifacts leaves **126 real files**, of which **5** fall under the floor: `check_paths_syntax.py` 56.3%, `check_ruff_pin_drift.py` 56.3%, `prune_reports.py` 58.3%, `project_metrics/cli.py` 60.4%, `self_healing_metrics.py` 65.7%. **All five are already enumerated in td-146's active deferral register** (the named carrier row, deliberately deferred under the trigger "drifts further OR gains a gate-consuming caller"). Dedup hit — **no new rows**. Note the aggregate reads 87.23%, comfortably clear of the floor: filing from the aggregate would have found nothing. |
| TD04 | 2 rows filed | Project `ccn_p95` 7.0, `cognitive_p95` 8.0. **11 files exceed 3× p95 (ccn > 21).** Judgment applied — a mechanical dump of 11 rows would be noise. Filed the two that earn it: **`dashboard_app/src/app/documentation/page.tsx`** at ccn **39** (5.6× p95, worst in repo) → **td-164**; **`hooks/inject_rules.py`** at ccn **31**, filed on blast radius rather than score — it runs on the session-start path of *every* session → **td-165**. Not filed: `finalize_adrs_crossrefs.py` (23) overlaps open **td-150**'s location; the dashboard/TS and chronograph outliers (30/27/27/26/23) are single-surface and below the two filed. All three filed paths verified **0 commits since the report commit**, so the metrics still describe current source. |
| TD05 | PASS | 23 active rows + 140 terminal rows audited. **(a)** 0 `resolved` rows in `TECH_DEBT_RESOLVED.md` missing `resolved-by`; **(b)** 0 `in-flight` rows older than 30 days (the ledger currently holds **no** `in-flight` rows — the sidecar remediation closed td-153/154/155/157/159/161/163 and narrowed td-162); **(c)** 0 `owner-role: unassigned` rows older than 7 days. Writes no rows by contract. |

**Rows filed this run: 5** — td-164, td-165, td-166 (TD-dimension), plus td-167 (DH04 detector defect) and td-168 (DH03 ADR-corpus drift). Canonical entries in `.ai-state/TECH_DEBT_LEDGER.md`.

### Code Health (CH)

| Check | Result | Evidence |
|---|---|---|
| CH01 | PASS | Sampled the sidecar delta's implementation files (`scripts/_sidecar_mount.py`, `scripts/_sidecar_convergence.py`, `scripts/_state_repo.py`, `scripts/praxion-sidecar`, `hooks/inject_sidecar_banner.py`). The `3ea60c52` refactor **split convergence out of the mount module** rather than duplicating it, and the write-free probe replaced a write-then-clean pattern. No structural similarity or repeated logic blocks across the family — this delta reduced duplication rather than adding it. |

### Hackathon Mode (HK)

| Check | Result | Evidence |
|---|---|---|
| HK01 | inert | `PRAXION_HACKATHON_MODE` unset in `.claude/settings.json` — check skipped, nothing emitted |

### Pre-Refactor Plan Integrity (PR)

| Check | Result | Evidence |
|---|---|---|
| PR01 | skip | No `PRE_REFACTOR_PLAN.md` anywhere under `.ai-work/` — the artifact is emitted only for the `emit-PRE_REFACTOR_PLAN` Phase 2.5 outcome, so absence is the common case |

---

## Ecosystem Health: B

### Summary

**0 Critical, 4 Important, 14 Suggested** (see Post-Audit Addendum — I-4 was found late, when a concurrent metrics run disagreed with itself) — grade **B** (0 Critical, 1–4 Important). Same band as the 2026-08-30 run, but the composition improved materially.

**Healthy.** The structural spine is clean end to end: every completeness, consistency, spec-compliance and cross-reference check passes, and the four script-backed structural gates return zero findings each — `check_gate_liveness.py` (0 across all four kinds), `check_architecture_projection.py` (16 rows ↔ 16 elements, both halves), `check_aac_golden_rule.py` (0 over 10 commits), `check_spec_drift.py` (0). The ADR corpus is in its best recorded state: 369 records, 0 drafts, 0 dangling pointers, 0 non-reciprocal links, 0 partial-supersession conflicts, and every one of the 8 required frontmatter keys populated on every record.

**The headline improvement is DH01: 49 → 0.** The prior run's second Important finding was 49 ADRs carrying `removed-by-later` decay with no supersession edge recorded. That is now **zero** — the decision graph's missing edges have been reconstructed. This is the single largest quality movement in the audited history of this dimension.

**Second improvement: AC09 is clean in both directions.** Earlier runs flagged `Status: Built` design-doc components absent from the developer guide. All 15 Built §3a components now appear in `docs/architecture.md` §3a, and all 16 guide components appear in the design doc — the coverage direction, which a subset-only check cannot see, is closed.

**The sidecar delta (dec-355..369, td-153..163) landed cleanly** on every structural axis: AC13 projection 0 findings, AC10 fence PASS on both architecture documents, EC07 golden-rule 0, F11 manifest fresh, DL02 unblocked (the `dec-365` quoted-scalar fix at `53c72b5e` closed the frontmatter-parse regression), CA03 covered, and its spec (`SPEC_sidecar-placement`, 41 REQs) is the most thoroughly traced in the corpus at 41/41 COVERED. CH01 found the refactor *reduced* duplication.

**Needs attention.** Three Important items, two of which are carried or regressed rather than new:

1. **T04 is unchanged from the prior run** — `skills/cicd/references/patterns-and-examples.md` is still 828 lines against an 800-line ceiling. Flagged Important on 2026-08-30 and not acted on.
2. **P03 regressed 2 → 6.** The fleet's own lifecycle hooks lost three times as many `agent_stop` records as last run. Critically, this is a **rate, not an incident**: the 6 unpaired starts sit in 6 *distinct* sessions spanning three weeks, so no single upstream event explains them and no one-off remedy closes them.
3. **The ADR corpus acquired a new drift shape** — two live sidecar decisions cite an ephemeral `.ai-work/` path in `affected_files`, which permanently mis-presents them to every future audit as retirement candidates.

**Watch.** The always-loaded token budget is at **98.4% utilisation with 394 tokens of headroom** — a measured figure, not a divisor estimate. It passes, but the next always-loaded addition of any size breaches it.

### Regression Comparison vs. SENTINEL_REPORT_2026-08-30_20-15-49.md

| Prior finding | Check | Then | Now | Verdict |
|---|---|---|---|---|
| Reference file over ceiling | T04 | Important — 828 lines | 828 lines | **Unchanged** — not acted on |
| ADRs with unrecorded removal edges | DH01 | Important — **49** findings | **0** findings | **RESOLVED** — largest improvement this run |
| Unpaired `agent_start` records | P03 | Important — 2 | **6** | **REGRESSED** — 3× the rate, still 1-per-session |
| `SKILL.md` over warn line | T01 | Suggested | 418 lines (1 file) | Unchanged |
| Agent prompts over warn line | T03 | Suggested | sentinel 689, verifier 612 | Unchanged |
| Token budget headroom | T02 | Suggested | 24,606/25,000 (394 left) | Unchanged, still tight |
| `DESIGN.md` illustrative paths | AC03 | Suggested | 14.0% non-resolving | Unchanged, well inside tolerance |
| Vanished ADR references | DH03 | Suggested | 5 (2 of them **new**) | **Partially regressed** — new shape, see I-3 |
| `architectural` category share | DH05 | Suggested | 0.49 post-adoption (n=51) | **Improved** — `n` now exceeds the window, so the 72%→49% fall reads as authoring behaviour |
| Stop/start pairing INFO | P03 | Suggested | 2,759 unmatched stops | Unchanged in kind, larger in count |

**New this run:** I-3 (ephemeral `.ai-work/` paths in ADR `affected_files`), S-6 (DH04 reopen false positive on `dec-360`), S-12 (AC12 convention never populated), S-9 (8 un-folded ADRs, 3 of them the sidecar family with `cited_in_guide: false`).

**Not a regression:** the artifact count rose 546 → 569 without a single new orphan (EC02 = 0), unregistered agent (C05), or broken cross-reference (X01–X09 all PASS).

### Ecosystem Coherence: A

**System-level EC checks:**

| Check | Result | Evidence |
|---|---|---|
| EC01 | PASS | Every agent in the `agents/README.md` pipeline diagram has a file |
| EC04 | PASS | Every pipeline output document has both a producing and a consuming agent; no dead ends |
| EC05 | PASS | No structural gap against CLAUDE.md's stated purpose and Future Paths |
| EC07 | PASS | `check_aac_golden_rule.py --mode=audit --json` → `[]` over the last 10 commits |

**Cross-dimension anomalies:** One, and it is mild. The three findings that are *not* localised to a single artifact — P03 (lost stops), P04 (implementer on the ADR surface) and the P03 INFO (3,013 stops vs 257 starts) — all originate in the **observability hook layer**, not in the artifact ecosystem the other dimensions audit. That concentration is itself the signal: the artifacts are coherent; the instrumentation that watches them is the weak surface. No dimension shows consistently low grades across many artifacts.

**Artifact coherence distribution:** 566 A, 3 B, 0 C, 0 D, 0 F. The three B-grade artifacts are `agents/sentinel.md`, `agents/verifier.md` (T03 warns) and `skills/cicd/references/patterns-and-examples.md` (T04 warn). No artifact carries a FAIL.

**Synthesis:** The ecosystem works as a connected whole. Every seam that can be checked mechanically — plugin registration, agent roster, catalog tables, ADR graph, architecture projection, canonical-block sync, delegation-checklist sync, doc manifest, git hooks — is consistent, and the four independent script gates agree. The friction is not in the connections but in two places where the system observes *itself*: the lifecycle WAL is losing stop records at a rising rate, and `adr_health.py`'s reopen heuristic produces a structurally unavoidable false positive. Both are instrumentation defects, and both are now tracked (td-167, and the P03 finding). Coherence grade **A** on the strength of four clean system-level checks and a healthy distribution; the instrumentation weakness is real but does not fragment the ecosystem.

### Ecosystem Metrics

| Metric | Value | Trend |
|--------|-------|-------|
| Total artifacts | 569 | ↑ from 546 (+23, sidecar family) |
| Health grade | B | → stable (composition improved) |
| Critical findings | 0 | → stable |
| Important findings | 4 | ↑ from 3 — 1 resolved, 1 regressed, 2 new |
| Suggested findings | 14 | ↑ from 7 |
| Ecosystem Coherence | A | → stable |
| Always-loaded tokens | 24,606 / 25,000 (98.4%) | → stable, tight |
| Finalized ADRs | 369 | ↑ from ~354 |
| ADR drafts pending | 0 | → clean |
| DH `removed-by-later` | 0 | ↓ from 49 — **major improvement** |
| Unpaired `agent_start` | 6 (6 sessions) | ↑ from 2 — **regression** |
| Active ledger rows | 29 (was 23) | ↑ +6 filed this run |
| Ledger `in-flight` rows | 0 | ↓ clean |
| Coverage (aggregate) | 87.23% | ↑ from 84.47% |
| Modules under 70% floor | 5 (all in td-146 register) | → stable |
| Cyclic SCCs | 0 / 297 of 374 files (79.4%) | → stable |
| Calibration match rate | 95.2% (83 rows) | → strong |
| Agent readiness | L4 (floor L3) | → stable |

### Scorecard

| Artifact | Type | Complete (C) | Consistent (N) | Fresh (F) | Spec (S) | Cross-Ref (X) | Tokens (T) | Coherence (EC) | Overall |
|----------|------|--------------|-----------------|-----------|----------|----------------|------------|----------------|---------|
| `skills/` (62) | skill | A | A | A | A | A | A (1× B) | A | **A** |
| ↳ `skills/skill-crafting/SKILL.md` | skill | A | A | A | A | A | B (418 ln) | A | **B** |
| ↳ `skills/cicd/references/patterns-and-examples.md` | reference | A | A | A | A | A | B (828 ln) | A | **B** |
| `agents/` (17) | agent | A | A | A | A | A | A (2× B) | A | **A** |
| ↳ `agents/sentinel.md` | agent | A | A | A | A | A | B (689 vs 653) | A | **B** |
| ↳ `agents/verifier.md` | agent | A | A | A | A | A | B (612 vs 550) | A | **B** |
| `rules/` (23) | rule | A | A | A | A | A | A | A | **A** |
| `commands/` (43) | command | A | A | A | A | A | A | A | **A** |
| `hooks/` (54) | hook | A | A | A | A | A | A | A | **A** |
| `.ai-state/decisions/` (369) | ADR | A | A | A | A | A | A | A | **A** |
| `.ai-state/specs/` (9) | spec | A | A | A | A | A | A | A | **A** |
| `.ai-state/DESIGN.md` | design | A | A | A | A | A | A | A | **A** |
| `docs/architecture.md` | guide | A | A | B (AC07) | A | A | A | A | **B** |
| `.ai-state/SYSTEM_DEPLOYMENT.md` | deployment | A | A | A | A | A | A | A | **A** |
| `.ai-state/TEST_TOPOLOGY.md` | topology | A | A | A | A | A | A | A | **A** |
| `.ai-state/TECH_DEBT_LEDGER.md` | ledger | A | A | A | A | A | A | A | **A** |
| `.claude-plugin/plugin.json` | manifest | A | A | A | A | A | A | A | **A** |
| Always-loaded set | config | A | A | A | A | A | A (98.4%) | A | **A** |

Check codes reference the dimension tables above; Pipeline Discipline (P), Decision Health (DH), Technical Debt (TD), Gate Liveness (GL), Code Health (CH) and Self-Verification (V) appear in findings only.

### Depth Disclosure

**A grade without its scope is a claim without a warrant.** This run was a **full sweep** — no dimension was dropped for turn budget, and **no check is marked `[not reached]`**. Every check in the catalog was either executed or skipped on a *stated substrate condition*.

**Read in full:** `.claude-plugin/plugin.json`, `CLAUDE.md`, `~/.claude/CLAUDE.md`, `claude/config/CLAUDE.md`, `rules/swe/agent-behavioral-contract.md`, `.ai-state/DESIGN.md` §3a–§4, `docs/architecture.md` §3a–end, `.ai-state/SYSTEM_DEPLOYMENT.md`, `.ai-state/TEST_TOPOLOGY.md` (all 13 group blocks), `.ai-state/TECH_DEBT_LEDGER.md` (28 rows) and `TECH_DEBT_RESOLVED.md` (140 rows), `.ai-state/calibration_log.md` (84 rows), `coordination-details.md § Delegation Checklists`, the prior sentinel report, and the full `METRICS_REPORT_2026-08-31` JSON payload.

**Machine-read in full (not sampled):** all 62 `SKILL.md` frontmatter blocks; all 17 agent frontmatter blocks; all 43 command frontmatter blocks; all 23 rule file headers; **all 369 ADR frontmatter blocks** (YAML-parsed, not grepped); all 9 spec traceability matrices (cell-level); **all 15,599 WAL records** (JSON-parsed, 0 unparseable); 1,630 ADR references via `adr_health.py`; 163 existing `dedup_key` values.

**Sampled, not exhaustive:** CH01 sampled 5 implementation files from the sidecar delta rather than the whole codebase — the check's own design. GL01/GL03 sampled agent prose and this catalog's gates rather than enumerating every instruction. The LLM-judgment checks (C06–C08, N04–N06, F03–F04, S05–S07, X07–X08, EC03–EC05, SH03/SH05/SH06, AC02, P04's semantic half) rest on reading representative artifacts plus the full delta, not on reading all 569 artifacts end to end.

**Skipped on stated substrate conditions — each with its actual reason, not a bare "unavailable":**

| Check | Reason |
|---|---|
| C09 | Trigger did not fire — no project-root `compose.yaml`/`Dockerfile` (only `skills/deployment/assets/` fixtures). The deployment doc was audited anyway and is substantive |
| F06 | Same — no project compose files to compare service lists against |
| F07/F08/F09 | No skill declares `staleness_sensitive_sections:` — the protocol is opt-in and has not been adopted (tracked as td-116) |
| TT04 | 0 of 10 `METRICS_REPORT_*.json` carry per-group runtime data; threshold is 7 |
| TT06 | `.ai-state/TEST_TOPOLOGY.md` exists, so the adoption advisory cannot apply |
| P08 | `stale_safe: 1`, below the advisory threshold of 3 |
| PR01 | No `PRE_REFACTOR_PLAN.md` under `.ai-work/` |
| HK01 | `PRAXION_HACKATHON_MODE` unset — check is inert by design |
| **AC12** | **Not "substrate absent."** Specs (9) and a LikeC4 model (`docs/diagrams/architecture/src/architecture.c4`) are both present and the Bash reader is fully available — `grep -rn 'req_ids' --include='*.c4'` executed and returned **0 hits**, and **0** archived specs carry `architectural_elements:` frontmatter. The failed precondition is **(c): the bidirectional convention has never been populated.** The reader is *not* unreachable; there is simply nothing on either side to orphan. Recorded as S-12 |

**P-dimension substrate, stated precisely:** P03/P04 read `.ai-state/observations.jsonl` (present, 15,599 records) — **not** Task Chronograph, which returns only the current session and for which this agent holds no MCP grant. Neither of the three skip states (`substrate absent`, `reader unreachable`, `substrate carries no history`) applies to this run. P01/P02 are retired, not skipped.

**Bounds on what a PASS means here:**
- **P03 clean would mean** *no agent that did work went unstopped* — **not** that every spawn completed. This run is **not** clean on that reading (6 WARNs). Denominator: 211 starts outside the newest session, 15,599 records examined.
- **P04 claims the surface, never the semantics.** These records name tools and paths, not intent. An agent making a design decision inside a file it was legitimately assigned emits records indistinguishable from correct work, and this check cannot see it. The 0 out-of-grant result therefore bounds *tool use*, not *judgment*.
- **TD02's zero is bounded at 79.4% of Python files.** Not a repo-wide all-clear.
- **TD03/TD04/TD01 rest on a metrics report TD06 marks `stale`** by commit distance. Every filed path was hand-verified as 0-commits-changed since the report commit; the three barred hotspots were excluded rather than filed.
- **T02's number is a measurement** (`basis: tokenizer`, `measured: true`, 3.811 chars/token actual), not a divisor estimate.

**Coverage was not narrowed to earn this grade.** The grade held at B while the artifact count rose 4% and the check surface was executed in full; the improvement in composition (DH01 49 → 0) came from work landed in the repository, not from looking at less.

### Findings

#### Critical (blocks correct behavior)

*None.*

#### Important (degrades quality or efficiency)

| # | Check | Dimension | Location | Finding | Recommended Action | Owner |
|---|-------|-----------|----------|---------|---------------------|-------|
| I-1 | T04 | Token Efficiency | `skills/cicd/references/patterns-and-examples.md` | **828 lines — over the 800-line hard ceiling for a single reference file. Unchanged from the 2026-08-30 run**, where it was also filed Important. Sole offender across 239 reference leaves. | Split into two references (patterns vs. worked examples) per progressive disclosure. Carried a full cycle without action — either split it or record a deliberate exemption with a reason. | `context-engineer` |
| I-2 | P03 | Pipeline Discipline | `.ai-state/observations.jsonl` | **6 unpaired `agent_start` records that each emitted `tool_use`** — agents that ran real work and produced no observed `agent_stop`. **Regression: 2 → 6 since the prior run.** This is a **rate, not an incident**: the 6 sit in **6 distinct `session_id`s** (largest single session's share = 1 of 6) spanning 2026-08-13 → 2026-09-05. Types: `implementer` ×4 (`a045b98b…` 08-13, `a92a8a24…` 08-31, `aa309b3f…` 09-02, `a07f58e1…` 09-05), `context-engineer` ×1 (`a74850d8…` 08-25), `systems-architect` ×1 (`a30a946a…` 09-02). All in-roster after `<namespace>:` stripping. 0 unpaired starts failed to emit `tool_use`, so none died on arrival. Rate 6/211 = **2.8%**. | Investigate the Stop-hook delivery path in `hooks/capture_session.py`. Because the failures are spread one-per-session rather than clustered, no single upstream event explains them — this is an ongoing defect in the fleet's own hooks, not a closed historical burst. | `user` |
| I-3 | DH03 / DL | Decision Health | `.ai-state/decisions/357-praxion-cli-exit-code-contract.md`, `.ai-state/decisions/358-sidecar-reconciler-diagnostic-split.md` | **New shape, introduced by the sidecar delta.** Both ADRs list `.ai-work/sidecar-placement/INTERFACE_DESIGN.md` in `affected_files` — an **ephemeral, gitignored, cleanup-deleted** path. An ADR is permanent by construction, so the entry is unresolvable for the record's whole life. Consequence is not cosmetic: `adr_health.py` classifies both as `vanished` / `retire-candidate`, presenting two correct, live decisions to every future audit as retirement candidates — precisely the false signal DH03 warns must not be treated as a backlog. | Drop the `.ai-work/` entries or replace them with the persistent artifacts they informed (`scripts/praxion-sidecar`, already listed in both). Add a one-line guard to the ADR authoring protocol so the shape cannot recur. → **td-168** | `systems-architect` |

#### Suggested (improves but not urgent)

| # | Check | Dimension | Location | Finding | Recommended Action | Owner |
|---|-------|-----------|----------|---------|---------------------|-------|
| S-1 | T03 | Token Efficiency | `agents/sentinel.md` | 689 lines against the **derived** ceiling warn `S+300` = **653** (catalog span S=353 measured this run), fail 753. Over by 36 lines of discretionary prose — the derived form means catalog growth cannot cause this, only prose can. | Trim non-catalog prose by ~40 lines, or accept as a near-line WARN. | `context-engineer` |
| S-2 | T03 | Token Efficiency | `agents/verifier.md` | 612 lines against its fixed warn 550 / fail 700. Encodes no catalog; bulk is Process + the tested-in-place rework-spawn contract. | Accept, or trim Process prose. Do not force under 500 — that risks a dead gate. | `context-engineer` |
| S-3 | T01 | Token Efficiency | `skills/skill-crafting/SKILL.md` | 418 lines (warn 400, fail 600). Sole `SKILL.md` over the warn line; next is 399. | Move ~20 lines to `references/`. | `context-engineer` |
| S-4 | T02 | Token Efficiency | Always-loaded set | **24,606 / 25,000 tokens — 98.4% utilisation, 394 tokens of headroom.** `over_by: 0` so this **PASSES**, and the figure is a real measurement (`basis: tokenizer`, 3.811 chars/token). But the next always-loaded addition of any size breaches the ceiling. | Treat the budget as full. Any new always-on rule must be paired with a cut, or scoped with `paths:`. Re-measure with `scripts/measure_token_budget.py` before every always-loaded edit. | `user` |
| S-5 | AC07 | Architecture | `docs/architecture.md` §3a | 5 of 241 paths are root-anchored and do not resolve: `.ai-state/eval_ledger/`, `.ai-state/eval_ledger/EVAL_LOG.md`, `.ai-state/training_runs/` (all declared `optional-lazy` — absence means the feature was not adopted, so citing them in a *developer navigation guide* is premature), plus scratch `tmp/issue.md`, `tmp/prism.md`. | Mark the three lazy artifacts as opt-in in the guide, or drop them; drop the two `tmp/` scratch paths. | `doc-engineer` |
| S-6 | DH04 | Decision Health | `scripts/adr_health.py`, `.ai-state/decisions/360-*.md` | `reopen_candidates` flags **`dec-360`** because its four `affected_files` resolve on disk. **Judged disposition: do not reopen.** dec-360 was retired *before implementation* by dec-366, so those four paths are files it *would have modified* — all pre-existing, never absent. The detector cannot distinguish "retired before implementation" from "subject deleted then returned" and will re-fire every run. | Gate the heuristic on whether the paths were ever absent (require a git deletion event between retirement and now), as a sibling to the `removed-by-self` exclusion it already implements correctly. → **td-167** | `implementer` |
| S-7 | TD04 | Technical Debt | `dashboard_app/src/app/documentation/page.tsx` | max_ccn **39** vs project `ccn_p95` 7.0 — 5.6×, worst in the repository. | Extract renderers/selectors. → **td-164** | `implementer` |
| S-8 | TD04 | Technical Debt | `hooks/inject_rules.py` | max_ccn **31** (4.4× p95) on the **session-start hot path** — a branching defect here degrades every session's context, not one surface. | Extract rule-selection and path-matching into named predicates. → **td-165** | `implementer` |
| S-9 | TD01 | Technical Debt | `scripts/project_metrics/_report_deep_dive.py` | Hotspot **#4** (churn 560 × ccn 23 = 12880) and simultaneously above the 3×-p95 complexity line — the only unbarred candidate where both signals coincide. | Decompose the deep-dive renderers. → **td-166** | `implementer` |
| S-10 | TD06 | Technical Debt | `.ai-state/metrics_reports/` | Metrics report is 4 days old (inside the 14-day rule) but **`status: stale` on commit distance** — 3 ranked hotspots moved since its commit and were barred from filing. | Re-run `/project-metrics` so the next sweep can file against ranks #2/#3/#6. | `user` |
| S-11 | AC14 | Architecture | `.ai-state/DESIGN.md` | **Advisory, not a gate.** 8 architecture-bearing ADRs un-folded since the `dec-354` mark (2026-08-31): dec-355, 356, 359, 361, 362, **364, 365, 366**. Expected steady state between architect visits. Worth noting that the last three — the sidecar family — carry `cited_in_guide: false`. | Fold at the next systems-architect visit (per-ADR judgment, `agents/systems-architect.md` § Phase 5). Consider citing dec-364/365/366 in `docs/architecture.md`. | `systems-architect` |
| S-12 | AC12 | Architecture | `docs/diagrams/architecture/src/architecture.c4`, `.ai-state/specs/` | The **bidirectional traceability convention has never been populated**: 0 `.c4` elements carry `req_ids`, 0 archived specs carry `architectural_elements:`. Both substrates exist and the reader works — the check simply has nothing to compare, so it has never once run in anger. | Decide: populate the convention on the next architecture pass, or record it as deliberately unadopted so AC12's permanent skip is documented rather than latent. | `systems-architect` |
| S-13 | P04 | Pipeline Discipline | `.ai-state/observations.jsonl`, `.ai-state/decisions/` | `implementer` emitted **11 `Edit` calls under `.ai-state/decisions/`** (11 finalized ADR files, `adr-living-view` task) — the literal P04 golden bad-case, since the ADR corpus belongs to systems-architect / implementation-planner per `adr-conventions.md` § Who Writes ADRs. **Bounded**: the task *was* ADR-corpus work, so this is plausibly a sanctioned plan step, and this check cannot tell a sanctioned assignment from a boundary violation. 0 out-of-grant tool uses across all 230 agents. | Confirm whether ADR-corpus edits are an intended implementer assignment; if so, say so in `adr-conventions.md` so the surface rule and the practice agree. | `systems-architect` |
| S-14 | P03 | Pipeline Discipline | `.ai-state/observations.jsonl` | **INFO, never WARN** (boundary artifact by catalog rule). 2,759 `agent_stop` records have no matching start; 2,756 self-attest `start_correlation: unobserved-start`, and **3 carry no field at all — unattested, not agreeing**. The corpus ratio of **3,013 stops to 257 starts** exceeds what WAL front-truncation alone plausibly explains. | Producer-side look at whether `agent_start` records are being written at all comparably to stops. Distinct from I-2, which is about *pairing*; this is about *emission volume*. | `user` |

### Pipeline Discipline

**Substrate**: `.ai-state/observations.jsonl` — 15,599 records, 0 unparseable. Event mix: `tool_use` 11,963, `agent_stop` 3,013, `session_stop` 315, `agent_start` 257, `session_start` 20, `context_surface_measurement` 20, `skill_activation` 11. **Not** Task Chronograph (no history, no grant). None of the three skip states applies.

- **P03 — WARN.** 6 unpaired starts that did work, in 6 distinct sessions, over 211 starts examined outside the newest session (`9711438d…`, correctly excluded as in-flight). **Rate 2.8%, largest single session's share 1 of 6** — an ongoing hook defect, not a closed incident. Pairing was done on `agent_id`, never `agent_type`. 0 unpaired starts emitted no `tool_use` (nothing died on arrival). **Bound: P03 clean would mean no agent that did work went unstopped — not that every spawn completed. This run is not clean on that reading.**
- **P03 INFO.** 2,759 unmatched stops: 2,756 `unobserved-start` (self-attested upstream start-delivery non-observation, which claims non-observation and never non-existence), 3 **unattested** (field absent — absence read as unattested, never as agreement). No producer-vs-log disagreement found: 0 stops the WAL paired cleanly carried a contradicting `start_correlation`.
- **P04 — WARN (surface only).** 0 out-of-grant tool uses across 230 agents with `tool_use` records against 17 parsed `tools:` grants. One surface-ownership hit (S-13). `discipline-consultant`'s 12 `Edit`s all target its own `CONSULT_*.md` output — correct. **Bound: claims the surface, never the semantics.**
- **P05 — PASS.** 4 handoff docs, 0 empty required sections.
- **P06 — PASS.** `check_p06_task_brief.py` → `[]`.
- **P07 — PASS.** 1 `INTERFACE_DESIGN.md` with empty challenges (no flag by rule); 0 `CONSULT_*.md`.
- **P08 — skip.** `stale_safe: 1` of 9 slugs, below the threshold of 3.

### Tech-Debt Findings

**5 rows filed this run**, by class: **complexity ×3** (td-164, td-165, td-166), **drift ×2** (td-167, td-168). **TD05 discipline issues: 0.**

Per-row "why filed" (LLM-judgment trace — a threshold breach is necessary, not sufficient):

- **td-164** (`documentation/page.tsx`, ccn 39): worst crossing in the repo at 5.6× p95, on the dashboard's most-navigated route. Filed on magnitude.
- **td-165** (`hooks/inject_rules.py`, ccn 31): filed on **blast radius over score** — 9 files scored higher or comparably, but this one runs on every session's start path, so its failure mode is fleet-wide context degradation rather than one broken surface.
- **td-166** (`_report_deep_dive.py`, hotspot #4): the **only** unbarred hotspot where high churn and above-3×-p95 complexity coincide. Ranks #5/#7/#8 were *not* filed — churn alone on actively-evolving operational scripts is feature growth, not decay, and filing them would be the mechanical dump the gating clause forbids.
- **td-167** (`adr_health.py` reopen heuristic): a shipped gate producing a structurally unavoidable false positive that will re-fire every run. Filed because a recurring false WARN trains its reader to skip the dimension.
- **td-168** (dec-357/dec-358 `affected_files`): Important — permanently mis-presents two live decisions as retirement candidates to every future audit.

**Deliberately NOT filed (dedup hits, verified against existing rows):**
- All **5** TD03 sub-70% modules are already enumerated in **td-146**'s active deferral register.
- Hotspot **#1** (`test_discipline_registry_invariants.py`, score 74100) is carried by open **td-105**.
- `finalize_adrs_crossrefs.py` (ccn 23) overlaps open **td-150**'s location.

**Barred by TD06** (`hotspots_touched` — re-verify against current source before filing): `scripts/finalize_adrs.py` (#2), `scripts/adr_health.py` (#3, for its *hotspot* score — td-167 files a distinct drift finding, not a hotspot row), `scripts/test_finalize_adrs.py` (#6).

**Staleness note:** `METRICS_LOG.md`'s latest row is 2026-08-31 (4 days — inside the 14-day rule) and `coverage.status: ok`, so **no staleness WARN under the age rule**. TD06's commit-distance verdict is the operative gate and it reads `stale` (S-10). All three filed paths were hand-verified at 0 commits changed since the report commit.

Canonical entries: `.ai-state/TECH_DEBT_LEDGER.md` (now 28 active rows).

### Recommended Actions (prioritized)

1. **Investigate the Stop-hook delivery path** (I-2, S-14) — `hooks/capture_session.py`. The unpaired-start rate tripled (2 → 6) and is spread one-per-session across three weeks, so it is an ongoing defect rather than a historical burst. The 3,013-stops-vs-257-starts ratio suggests looking at start *emission* as well as stop *delivery*. Owner: `user`.
2. **Fix the two sidecar ADRs' `affected_files`** (I-3, td-168) — drop the `.ai-work/` entries from dec-357 and dec-358 and add a guard to the ADR authoring protocol. Cheap, and it stops two live decisions being surfaced as retirement candidates forever. Owner: `systems-architect`.
3. **Split `skills/cicd/references/patterns-and-examples.md`** (I-1) — 828/800 lines, carried unchanged from the prior run. Either split it or record a deliberate exemption. Owner: `context-engineer`.
4. **Re-run `/project-metrics`** (S-10) — the report is `stale` on commit distance and barred three ranked hotspots from filing this run. Owner: `user`.
5. **Treat the token budget as full** (S-4) — 394 tokens of headroom. Pair any new always-loaded content with a cut. Owner: `user`.
6. **Gate `adr_health.py`'s reopen heuristic on prior absence** (S-6, td-167) — stops a permanent false WARN on `dec-360`. Owner: `implementer`.
7. **Address the three complexity rows** (S-7/S-8/S-9 → td-164/165/166), starting with `hooks/inject_rules.py` for blast radius. Owner: `implementer`.
8. **Decide AC12's fate** (S-12) — populate `req_ids` / `architectural_elements:` on the next architecture pass, or record the convention as deliberately unadopted so its permanent skip is documented. Owner: `systems-architect`.
9. **Fold the 8 un-folded ADRs and cite the sidecar family in the developer guide** (S-11) at the next architect visit. Owner: `systems-architect`.
10. **Trim the near-line size warnings** (S-1/S-2/S-3/S-5) opportunistically. Owner: `context-engineer` / `doc-engineer`.

---

## Post-Audit Addendum — concurrent metrics runs during this sweep

**Surfacing this because it changes findings above, and because a report that silently absorbed it would be less honest than one that shows the seam.**

While this audit was running, a concurrent process (not this agent — every command issued here against `metrics_reports/` was read-only) generated **two** new metrics reports at the current HEAD `bc67256c` and appended two rows to `METRICS_LOG.md`:

| Report | Wall clock | Commit | Coverage recorded |
|---|---|---|---|
| `METRICS_REPORT_2026-09-05_05-53-58` | 22:53:58 local | `bc67256c` | **0.8514** (full artifact) |
| `METRICS_REPORT_2026-09-05_05-57-52` | 22:57:52 local | `bc67256c` | **0.5071** (2-file artifact) |

It also regenerated `.ai-state/specs/SPECS_INDEX.md` (populating the sidecar spec's ADR column with dec-357..368). Neither change was made by this audit.

### What this corrects

**TD06 — WARN → PASS (superseded).** Re-run against the newest report: `status: fresh`, `hotspots_touched: []`, `withheld: []`, `run_metadata.commit` = `bc67256c` = HEAD. The commit-distance staleness reported in the TD section, and **finding S-10**, are **superseded** — the metrics no longer lag HEAD. S-10's recommended action (re-run `/project-metrics`) has effectively already happened.

**TD01 — barred set now empty, re-verified.** `scripts/finalize_adrs.py` (#2) and `scripts/adr_health.py` (#3) are no longer barred. Re-examined with the bar lifted:
- `adr_health.py` (churn 832, ccn 23 — above the 3× p95 line) is now a legitimate candidate. **Deliberately not filed**: churn 832 is the DH-dimension work actively landing, and filing a complexity row against a file mid-evolution is premature. It already carries **td-167** for a distinct, grounded drift defect. Re-evaluate next sweep once churn settles.
- `finalize_adrs.py` (churn 1467, ccn 17) sits below the 3× line — churn alone is feature growth.
- New rank #4 `scripts/_sidecar_mount.py` (churn 1399, ccn 12): below the complexity line; the churn is the sidecar delta landing. Not filed.
- **`_report_deep_dive.py`** re-ranked #4 → #6 but is unchanged in substance (churn 560, ccn 23). **td-166 stands.**

**TD04 — unchanged, re-verified at HEAD.** `ccn_p95` still 7.0; `documentation/page.tsx` still **39**, `hooks/inject_rules.py` still **31**, `_report_deep_dive.py` still **23`. **td-164, td-165, td-166 all stand**, now verified against a report whose commit *is* HEAD rather than against a stale one.

**TD02 — re-verified, marginally better denominator.** `cyclic_deps: 0` at **80.1%** Python-file coverage (was 79.4%). Still not a repo-wide all-clear.

**TD03 — unchanged, and the newest report must NOT be used for it.** See I-4 below: the newest report's coverage rests on a 2-file artifact. The 2026-08-31 analysis (126 real files, 5 under floor, all in td-146's register) remains the only usable coverage reading. **No new rows.**

### What this reveals — new Important finding

| # | Check | Dimension | Location | Finding | Recommended Action | Owner |
|---|-------|-----------|----------|---------|---------------------|-------|
| I-4 | TD / GL03-shaped | Technical Debt | `scripts/project_metrics/collectors/coverage_collector.py`, `.ai-state/metrics_reports/METRICS_LOG.md` | **The coverage collector reports `status: ok` on an artifact covering 2 files.** Two runs four minutes apart at the *same commit* recorded **0.8514** and **0.5071**; the second's `coverage.xml` `per_file` holds exactly two non-tmp entries against a `file_count` of 1,356. `issues: []` — no warning of any kind. Both rows are now in `METRICS_LOG.md`, so the trend line shows a 30-point coverage collapse that did not occur. Blast radius is every recency-ordered consumer: **TD03** would file coverage-floor rows against a 2-file denominator; **RD01** already reads **level 3** from the degenerate report where the full one reads **level 4**; dashboard and `/project-metrics` trend charts inherit it. This is a **presence-vs-substance** gate defect of exactly the shape GL03 exists to catch — a status field that asserts the collector *ran* where the contract is that it *measured*. | Gate on artifact scope: emit `status: partial` plus an `issues` entry when the covered-file count is a small fraction of the analysed set — the discipline `pydeps` already applies by publishing `python_file_coverage_pct` beside its `cyclic_deps` count. Until fixed, read the last full-artifact run, not the newest. → **td-169** | `implementer` |

### Revised counts

**0 Critical, 4 Important, 14 Suggested** (S-10 superseded but retained for the record). Grade band unchanged: **B** (0 Critical, 1–4 Important).

Rows filed this run: **6** — td-164, td-165, td-166 (TD dimension), td-167 (DH04 detector), td-168 (DH03 ADR drift), td-169 (coverage collector).

### Registered objection — scope of `prune_reports.py`

Phase 7 mandates running `prune_reports.py`, and this run's invocation pruned **beyond** the sentinel family: it deleted 4 `METRICS_REPORT_*` files and 1 `SENTINEL_REPORT_*` file, and `METRICS_LOG.md` was concurrently modified. That is the script's documented behaviour (it bounds every report family, and pruned files remain in git history), but it means a read-only auditor's mandated final step deletes artifacts outside its own dimension. Flagging rather than silently accepting: if the sentinel is to remain read-only-except-its-own-outputs, `prune_reports.py` should either take a family filter or be invoked outside the audit. No ledger row filed — this is a contract question for the user, not grounded code debt.
