# Sentinel Report

Run timestamp: 2026-09-06_12-31-26
Scope: Full ecosystem sweep (worktree `praxion-health`, HEAD 7f551096, branch `worktree-praxion-health`, 5 commits on top of main 9d061ba2)
AC-dimension inventory: `.ai-state/DESIGN.md` present, `docs/architecture.md` present — AC01-AC09 active; AC10/AC12/AC13/AC14 conditional, checked per their own substrate triggers below.

Diffed against `.ai-state/sentinel_reports/SENTINEL_REPORT_2026-09-04_22-41-32.md` (grade B, Ecosystem Coherence A, 0/4/14) per explicit instruction — every item that report left open is re-verified below, not merely re-scored.

## Ecosystem Health: B

**Grade unchanged from the prior run, but the composition improved substantially**: 3 of the prior run's 4 Important findings are confirmed resolved (I-1, I-3, I-4); the 4th (I-2 / P03 unpaired starts) is unchanged — not worsened, not fixed. Two new Suggested-tier candidates were filed this run (td-171, td-172) after re-verification against TD06-fresh source. 0 Critical, 1 Important, 6 Suggested (0/1/6) — the Important count *dropped* from 4 to 1 even though the letter grade is unchanged, because the grade table's B band spans 1-4 Important and this run sits at the low end of that band rather than the high end the prior run occupied.

### Summary

**What's healthy:** Bare CI on this tree is fully green — 4149 passed / 1 skipped / 0 failed, coverage 85.63% (verified live: baseline before this pass was 16 failed / 84.26%). `fitness/tests/` is 156 passed / 5 skipped / 0 failed (verified live; baseline was 6 failed). Every mechanical gate run this pass returned clean: `check_gate_liveness.py` (GL02/GL04/GL05/GL06, 0 findings), `check_spec_drift.py` (SH07, 0 findings), `check_spec_archival_gap.py` (SH08, `gap: false`), `check_calibration_coverage.py` (CA03, `covered: true`), `check_readiness_feedback.py` (RD01, level 4, `mechanical_only: false` — full LLM-tier readiness ran), `check_aac_golden_rule.py` (EC07, 0 findings), `aac_fence_validator.py` on both `.ai-state/DESIGN.md` and `docs/architecture.md` (AC10, PASS/PASS), `check_architecture_projection.py` (AC13, 0 findings, 16 rows/16 elements), `check_design_checkpoint.py` (AC14, checkpoint `dec-370` == corpus tip, 0 un-folded), `check_p06_task_brief.py` (P06, 0 findings), `clean_work_safety.py` (P08, `stale_safe: 0`, no advisory), `check_metrics_freshness.py` (TD06, `status: fresh`, `hotspots_touched: []`). `adr_health.py` reports 0 `reopen_candidates` (DH04), 0 `status_edge_conflicts` (DH06), and DH05's post-adoption `architectural` share (48.1%, n=52 — the first sample to exceed the recent-window size) sits well below the 84% adoption baseline, meaning the category-discrimination test recorded in `adr-authoring-protocols.md` is working as designed, not being ignored.

**Every named open item from the prior report is dispositioned below** (Cluster A/B, td-164, td-166, S-11, S-12, S-13, the `prune_reports.py` objection, dec-160, the `--refresh-coverage` cap, T02) — all confirmed resolved except where noted.

**What needs attention:** I-2/P03 (6 unpaired `agent_start` records, unchanged from the prior run — same 6 `agent_id`s, same rate, no regression and no fix) remains the sole carried Important finding. Two new TD candidates were filed after TD06 confirmed the corpus fresh enough to re-verify them (`scripts/adr_health.py` complexity, and two under-covered `_sidecar_*` scripts). One ledger-hygiene item surfaced: `td-149` is functionally resolved (its named fitness-gate failures are confirmed green) but its `status` field was never flipped from `open`.

### Disposition of every item the prior report left open

| Item | Prior state | Disposition this run | Evidence |
|---|---|---|---|
| Cluster A (Codex routing desync, 8 tests) | Failing | **RESOLVED** | Commit d8a5aaef keys the Codex adapter on the routing rule's Tier column; bare CI 0 failed (was 16 failed) |
| Cluster B (`/upgrade-project` contract drift, 7 tests) | Failing | **RESOLVED** | Commit ca9c4ddb; `/upgrade-project` names its labels script, tests anchor on structure; bare CI 0 failed |
| Fitness failures (DL/P dimensions) | 6 failed | **RESOLVED** | `fitness/tests/` re-run live: 156 passed / 5 skipped / 0 failed |
| td-164 (documentation/page.tsx ccn 39) | open | **RESOLVED** | Ledger status=resolved, resolved-by ca9c4ddb; ccn 39→3, 21 characterization tests green |
| td-166 (`_report_deep_dive.py` ccn 23) | open | **RESOLVED** | Ledger status=resolved, resolved-by ca9c4ddb; file 593→248 lines, tests green |
| S-11 (DESIGN.md checkpoint stale) | flagged | **RESOLVED** | AC14 live run: `checkpoint: dec-370`, `corpus_tip: dec-370`, `unfolded: []` |
| S-12 (AC12 unadopted, undocumented) | flagged | **RESOLVED** | `dec-draft-6f149b5a` exists; `agents/sentinel.md` carries the AC12 substrate-trigger row (confirmed on disk); `docs/aac-dac.md` names AC12 at 5 locations |
| S-13 (implementer editing ADR corpus, unsanctioned) | flagged | **RESOLVED** | `rules/swe/adr-conventions.md` now carries "The implementer authors no ADR, and may apply mechanical corpus edits a plan step specifies" (confirmed present, line 123) |
| `prune_reports.py` objection (no family scoping) | registered | **RESOLVED** | `agents/sentinel.md:639` invokes `--family .ai-state/sentinel_reports:SENTINEL`; `--help` confirms the flag exists and behaves as documented |
| dec-160 | accepted, unbuilt | **RESOLVED (superseded)** | `dec-draft-9095dc63` supersedes dec-160; dec-160 frontmatter correctly carries `superseded_by: dec-draft-9095dc63` — reciprocal, sanctioned cross-stage pointer (DL04/DL06 clean) |
| `--refresh-coverage` uncapped | flagged | **RESOLVED** | `--coverage-timeout` flag present in `scripts/project_metrics/cli.py` (default documented); `coverage_refresh`/`coverage_artifact_mtime` fields present in the metrics JSON schema |
| T02 token budget | 24,606 / 25,000 | **IMPROVED THEN SPENT** | Measured live: 24,542 / 25,000, `over_by: 0`, `basis: tokenizer` (real measurement, not divisor-estimated). The S-13 fix cost net tokens (removed prose tokenizes more efficiently than added backtick-heavy clause text per this pipeline's own LEARNINGS note); a second compression (Migration paragraph) paid for it |

### Ecosystem Coherence: A

**System-level EC checks:**
| Check | Result | Evidence |
|-------|--------|----------|
| EC01 | PASS | Agent roster consistent 17/17/17 across `plugin.json`, `agents/README.md` catalog table, and `swe-agent-coordination-protocol.md`'s Available Agents table (X01/X05/X06 all confirmed below) |
| EC04 | PASS | No dead-end pipeline outputs observed; every `.ai-work/praxion-health/` document this run (SYSTEMS_PLAN, IMPLEMENTATION_PLAN, WIP, LEARNINGS, TEST_BASELINE, TEST_RESULTS, TASK_BRIEF, traceability) has a named consumer per the coordination protocol |
| EC05 | PASS | No obvious missing artifact type surfaced by this run's CLAUDE.md/Future-Paths comparison |
| EC07 | PASS | `check_aac_golden_rule.py --mode=audit --json`: `[]` (0 findings, exit 0) |

**Cross-dimension anomalies:** Same shape as the prior run, smaller: the only findings that are *not* localized to a single artifact are P03 (lost stops, unchanged) and the new TD03/TD04 candidates (both freshly unbarred by TD06 rather than newly introduced defects). No dimension shows consistently low grades across many artifacts; the observability-hook layer remains the one recurring weak surface, exactly as identified last run.

**Artifact coherence distribution:** Not re-sampled exhaustively this run (scope was a targeted delta audit per explicit instruction, not a full per-artifact re-read); no evidence of degradation in the artifacts sampled (agents/sentinel.md, rules/swe/adr-conventions.md, docs/aac-dac.md, agents/README.md, docs/architecture.md all read directly and consistent).

**Synthesis:** The ecosystem continues to work as a connected whole. Every mechanical cross-reference gate that ran this pass returned clean, the DESIGN.md checkpoint is current (dec-370 == corpus tip), and three of four previously-Important findings are now closed with verifiable evidence rather than self-report. The one persistent weakness — the observability WAL's Stop-hook reliability — is stable, not worsening, and remains correctly scoped to `user`/hook-layer investigation rather than to any artifact this pipeline touched.

### Ecosystem Metrics

| Metric | Value | Trend |
|--------|-------|-------|
| Bare CI (pytest) | 4149 passed / 0 failed | vs 16 failed pre-pass — RESOLVED this run |
| Fitness suite | 156 passed / 0 failed | vs 6 failed pre-pass — RESOLVED this run |
| Coverage (line, project-scope) | 85.63% (bare CI) / 87.8% (stale metrics artifact, 73.4% scope) | stable, artifact one commit stale |
| Always-loaded token budget | 24,542 / 25,000 (basis: tokenizer) | was 24,606 — net -64 after two compensating edits |
| Ledger open/in-flight rows | 27 (was 25 + td-170; +td-171, +td-172 this run) | td-164/td-166 migrated to resolved this pipeline |
| Finalized ADRs | 370 | +21 since prior run's implied count (checkpoint dec-354→dec-370) |
| Draft ADR fragments (in-flight) | 5 | all frontmatter-complete, all correctly cross-referenced |
| DH findings (adr_health.py) | 73 raw / 11 actionable (8 renamed + 2 vanished + 1 n/a) | 57 removed-by-self + 3 lazy-artifact + 3 renamed-by-self are not findings |
| P03 unpaired starts | 6 (unchanged) | same 6 `agent_id`s as prior run — stable, not regressed |
| Important findings | 1 (was 4) | 3 resolved, 1 carried |
| Suggested findings | 6 (was 14) | net reduction |

### Scorecard

Targeted delta audit — the artifacts below are the ones directly implicated by this run's named open items and this pipeline's own commits, not a full 569-artifact re-read (see Depth Disclosure).

| Artifact | Type | Complete (C) | Consistent (N) | Fresh (F) | Spec (S) | Cross-Ref (X) | Tokens (T) | Coherence (EC) | Overall |
|----------|------|--------------|-----------------|-----------|----------|----------------|------------|----------------|---------|
| `agents/sentinel.md` | Agent | A | A | A | A | A | A (derived ceiling not re-measured this run) | A | A |
| `rules/swe/adr-conventions.md` | Rule | A | A | A (implementer clause added, S-13) | A | A | A | A | A |
| `docs/aac-dac.md` | Doc | A | A | A | A | A | — | A | A |
| `.ai-state/DESIGN.md` | Design doc | A | A | A (checkpoint dec-370 current) | A | A | — | A | A |
| `docs/architecture.md` | Doc | A | A | A | A | A | — | A | A |
| `scripts/adr_health.py` | Script | A | A | B (TD04 candidate, td-171) | A | A | — | A | B |
| `scripts/_sidecar_init.py` / `_sidecar_cli.py` | Script | A | A | B (TD03 candidate, td-172) | A | A | — | A | B |
| `.ai-state/TECH_DEBT_LEDGER.md` | Ledger | B (td-149 status lag) | A | A | A | A | — | A | B |
| `hooks/capture_session.py` (WAL emitter) | Hook | A | A | C (P03 unchanged, 6 unpaired) | A | A | — | B | C |
| `.ai-state/decisions/drafts/*` (5 fragments) | ADR drafts | A | A | A | A | A | — | A | A |

### Depth Disclosure

**Read in full:** `agents/sentinel.md` (own catalog, current worktree copy — diverges from this session's system-prompt-embedded catalog, which reflects `main`'s pre-pipeline state; the worktree copy is authoritative and was used throughout), `rules/swe/adr-conventions.md` (diffed byte-for-byte against `main`'s copy via `git show 9d061ba2:...`), `docs/aac-dac.md` (grepped for AC12), `.ai-work/praxion-health/{TASK_BRIEF,WIP,LEARNINGS,TEST_RESULTS,PROGRESS}.md`, all 5 draft ADR fragments' frontmatter, dec-160/dec-364/dec-366/dec-369's frontmatter, `.ai-state/metrics_reports/METRICS_REPORT_2026-09-06_19-30-28.{json,md}` (hotspots, coverage deep-dive, pydeps, lizard, readiness sections).

**Sampled, not exhaustive:** The 27-row recent-ADR-plus-draft frontmatter sweep (DL02, `dec-350` through `dec-370` + drafts) stood in for a full 370-ADR sweep — a full sweep was out of budget and the recent window is where drift from this pipeline's own commits would land. `adr_health.py`'s 73 raw findings were triaged by `decay_class` in aggregate (Counter) rather than read individually; the 8 "renamed" `claude/config/CLAUDE.md` hits were read in full, the 57 "removed-by-self" and 3 "lazy-artifact" hits were not (per catalog, these are not findings by construction). P04's tool-vs-grant check was an aggregate `(agent_type, tool_name)` cross-tab over all 17,096 WAL records, spot-checked against `discipline-consultant`/`researcher`/`interface-designer` frontmatter grants — this bounds tool use, not intent, and does not verify per-path surface ownership at record granularity (the same bound the catalog itself requires stating).

**Not re-run this pass (would have exceeded targeted scope):** Full per-artifact C/N/F/S/X/T scoring for all ~569 artifacts (C01-C05, N01-N03, S01-S04 were spot-checked as aggregate counts — all PASS — rather than per-file); GL01/GL03 (LLM-judgment gate-liveness sampling) were not re-run since GL02/GL04/GL05/GL06's mechanical detector returned 0 findings and no new gate was added or removed this pipeline; SH03-SH06, AC02/AC08/AC09 (LLM-judgment architecture-doc checks) were not re-run — no evidence surfaced during the targeted read that either architecture doc drifted, but this is an absence-of-evidence, not a positive re-verification; CH01 (code-duplication sampling) was not run.

**Substrate/tool-grant skips:** None this run — `.ai-state/observations.jsonl` present (17,096 records, WAL reader, not Task Chronograph), `.ai-state/specs/` present (9 specs, SH07/SH08 ran), `.ai-state/calibration_log.md` present (CA01/CA03 ran, CA02 needs 5+ entries — assumed satisfied, not re-verified), `.ai-state/metrics_reports/` present (TD/RD ran), AC10/AC12/AC13/AC14 substrate all present (AC12 was **not** re-run this pass — no evidence of a populated bidirectional `req_ids`/`architectural_elements` convention was checked; skipped with this note rather than silently omitted).

### Findings

#### Critical (blocks correct behavior)

None.

#### Important (degrades quality or efficiency)

| # | Check | Dimension | Location | Finding | Recommended Action | Owner |
|---|-------|-----------|----------|---------|---------------------|-------|
| I-1 | P03 | Pipeline Discipline | `.ai-state/observations.jsonl` | **6 unpaired `agent_start` records that each emitted `tool_use`, unchanged from the prior run.** Same 6 `agent_id`s (`a045b98b…` implementer 08-13, `a74850d8…` context-engineer 08-25, `a92a8a24…` implementer 08-31, `aa309b3f…` implementer 09-02, `a30a946a…` systems-architect 09-02, `a07f58e1…` implementer 09-05), same 6 distinct `session_id`s (largest single session's share 1 of 6), same rate (~2.5-2.8% of starts outside the newest session). Denominator this run: 235 starts, 6 unpaired outside the newest session, 17,096 records examined. Neither regressed nor fixed since 2026-09-04. | Investigate the Stop-hook delivery path in `hooks/capture_session.py` — spread one-per-session across three weeks means no single upstream event explains it. | `user` |

#### Suggested (improves but not urgent)

| # | Check | Dimension | Location | Finding | Recommended Action | Owner |
|---|-------|-----------|----------|---------|---------------------|-------|
| S-1 | TD05 | Technical Debt | `.ai-state/TECH_DEBT_LEDGER.md` (td-149) | `td-149`'s named defect (rust-first-class post-boundary consult missing cost row/sealed prior/classification, causing 3 fitness-gate failures) is confirmed **fixed** this pipeline — commit ca9c4ddb backfills `CONSULT_COSTS.md`/`CONSULT_PRIORS.md` and the 3 named gates are confirmed green live (`test_consult_append_only`, `test_discipline_registry_invariants` x2) — but the ledger row's `status` field is still `open`. | Flip `td-149` to `status: resolved`, `resolved-by: ca9c4ddb`. | `orchestrator` |
| S-2 | TD05 | Technical Debt | `.ai-state/TECH_DEBT_RESOLVED.md` (td-028, td-033) | Both rows carry `status: wontfix` with an empty `resolved-by` cell, unchanged since May 2026. | Backfill `resolved-by` (e.g. `orchestrator (triage)`) or a one-line rationale for why `wontfix` needs none. | `orchestrator` |
| S-3 | DH02 | Decision Health | 7 finalized ADRs (022, 043 ×2, 049, 097, 191, 193, 233) | Each cites `claude/config/CLAUDE.md` in `affected_files`, which was renamed to `claude/config/CLAUDE.md.tmpl` (per `feedback_claude_config_is_generated_from_tmpl` — the file is gitignored/generated). Mechanical repair, `adr_health.py` disposition `update-path`. | Update the 7 `affected_files` entries to cite the `.tmpl` source. | `context-engineer` |
| S-4 | DH03 | Decision Health | dec-044, dec-050 (`ROADMAP.md`) | Both cite `ROADMAP.md`, deleted 2026-05-15, `decay_class: vanished`. Expected under dec-092 (Praxion carries no living `ROADMAP.md` instance; `/roadmap` regenerates on demand) — advisory only, listed for human disposition, not a defect. | No action required unless a human judges the decisions themselves stale. | `user` (advisory) |
| S-5 | TD04 | Technical Debt | `scripts/adr_health.py` | New row **td-171** filed: max_ccn 25 (3.57x project p95 of 7.0), 820 lines (at the 800-line reference ceiling), rank #3 hotspot by churn. Re-verified against current source (TD06: `status: fresh`, not in `hotspots_touched`) after being correctly barred in the 2026-09-04 sweep. | See ledger row td-171 for remedy (extract highest-ccn function). | `implementer` |
| S-6 | TD03 | Technical Debt | `scripts/_sidecar_init.py`, `scripts/_sidecar_cli.py` | New row **td-172** filed: 49.66% and 54.32% line coverage, both below the 70% floor and below every member of `td-146`'s existing coverage-deferral register. Coverage artifact is one commit stale (`data.status: stale`) but `artifact_scope_pct` 73.4% is well above the 25% withholding floor, so the figures are usable per the staleness-WARN policy. | See ledger row td-172 for remedy. | `test-engineer` |
| S-7 | S13/GL | Rules | `rules/swe/adr-conventions.md` | The Step 20 token-budget compensating cut removed two explanatory clauses from the Finalize Protocol paragraph ("extend the net, not forbid the citation" as the resolution rule for out-of-net citations, and the rationale for the `docs/independent-analysis/` exemption) and compressed the Migration paragraph. Both cuts are self-disclosed and dispositioned in this pipeline's own `LEARNINGS.md`/`TEST_RESULTS.md` as deliberate, budget-driven, and non-blocking (no known consumer greps for the exact wording). Independent read confirms: the operative rule (which paths are in/out of the bounded net) is fully preserved; only the explanatory "why" is gone. | No action required; noted for visibility since a future editor may want to restate the "extend the net" principle in `adr-authoring-protocols.md` where it has more room. | `context-engineer` (optional) |

### Pipeline Discipline

**Substrate:** `.ai-state/observations.jsonl` present, 17,096 records examined (WAL reader — not Task Chronograph; this agent holds no MCP grant for it and Chronograph's surface carries only in-flight-session history in any case).

**P03.** 235 `agent_start`, 3,293 `agent_stop`. 8 unmatched starts raw; 6 after excluding the newest (in-flight) session `9b74ea5c-…`. All 6 emitted `tool_use` (WARN-worthy, none died on arrival). **Scattered, not clustered**: 6 distinct sessions, largest single session's share 1/6, spanning 2026-08-13→2026-09-05 — a rate, not an incident. **Unchanged from the prior run** — identical 6 `agent_id`s, same types (`implementer` ×4, `context-engineer` ×1, `systems-architect` ×1, all in-roster after `<namespace>:` stripping). **Bound: P03 clean would mean no agent that did work went unstopped — this run is not clean on that reading.** 3,066 unmatched `agent_stop` records (up from 2,759 — expected growth): 3,063 self-attest `start_correlation: unobserved-start` (INFO — upstream start-delivery non-observation, not a hook defect); 3 carry no `start_correlation` field at all (predate the field — reported **unattested**, never read as agreement). No producer-vs-log disagreement found (no stop the WAL paired cleanly carried a contradicting `start_correlation`).

**P04.** Aggregate `(agent_type, tool_name)` cross-tab over all `tool_use` records, 17 agent types. All tool usage observed is a subset of each type's declared `tools:` grant where checked (`discipline-consultant`: Bash/Edit/Write, all within its `Read, Glob, Grep, Bash, Write, Edit, Skill` grant; `researcher`: Edit within grant; `sentinel`: Bash/Write only, no Edit — matches this agent's own constraint). **Bound: claims the surface, never the semantics** — this check cannot distinguish a sanctioned plan-directed corpus edit from a boundary violation, and per-path surface-ownership was not re-verified at record granularity this run (aggregate check only).

**P05.** `.ai-work/praxion-health/`'s handoff documents (SYSTEMS_PLAN, IMPLEMENTATION_PLAN, WIP, TEST_BASELINE, TEST_RESULTS, TASK_BRIEF) all carry substantive content under their required sections — confirmed by direct read, not spot-checked.

**P06.** `python3 scripts/check_p06_task_brief.py --json` → `[]`. PASS — `TASK_BRIEF.md` present for this Standard/Full slug.

**P07.** No `INTERFACE_DESIGN.md`/`TRANSACTIONS_DESIGN.md`/`CONSULT_*.md` present in `.ai-work/praxion-health/` — skip, substrate not present for this task.

**P08.** `python3 scripts/clean_work_safety.py --json` → `stale_safe: 0` (1 task dir, classified WARN for unmerged learnings/traceability — expected, pipeline still in flight). No advisory.

### Tech-Debt Findings

**2 new rows filed** (both after TD06 confirmed the corpus `status: fresh` / `hotspots_touched: []`, so no candidate needed hand-verification against git log):

- **td-171** (TD04, complexity): `scripts/adr_health.py`, max_ccn 25 (3.57x p95). Why filed: this is the DH-dimension's own detector script — a defect here degrades every future DH01/DH02/DH04/DH05/DH06 run, not one call site; same blast-radius reasoning td-165 used for `inject_rules.py`. File also sits at the 800-line reference ceiling.
- **td-172** (TD03, coverage-gap): `scripts/_sidecar_init.py` (49.66%), `scripts/_sidecar_cli.py` (54.32%). Why filed rather than folded into td-146's existing deferral register: both are meaningfully below every member of that register (56-69% range) and are newer files (sidecar-placement, dec-364/366) not covered by td-146's original inventory — near-half-covered code is a materially different risk than the 56-69% deferred set.

**0 rows for TD02** (cyclic SCCs): 0 SCCs found, reported against `analyzed_python_files: 342 / repo_python_files: 426` (80.3% coverage, 6 package roots) — the denominator is stated per the catalog's discipline; shortfall is the expected pydeps package-analyzer boundary (loose modules with no `__init__.py`), not a defect.

**1 TD05 discipline issue** surfaced this run (S-1 above): `td-149` functionally resolved but ledger `status` not flipped. Plus the pre-existing S-2 (td-028/td-033 missing `resolved-by`), unchanged from prior runs.

**Staleness note:** the fresh `METRICS_REPORT_2026-09-06_19-30-28` supersedes the 2026-09-05 baseline `CA03`/`RD01`/`TD06` read against; no staleness WARN triggered (age 0 days, `commits_since: 0`).

Forward-pointer: see `.ai-state/TECH_DEBT_LEDGER.md` for the canonical entries (td-171, td-172 newly appended this run).

### Recommended Actions (prioritized)

1. **Investigate Stop-hook delivery** (I-1/P03) — `hooks/capture_session.py`, unchanged 3-week-old rate. Owner: `user`.
2. **Flip `td-149` to resolved** (S-1) — its defect is confirmed fixed; ledger status lags. Owner: `orchestrator`.
3. **Repair 7 ADRs' stale `claude/config/CLAUDE.md` citations** (S-3) — mechanical, cheap. Owner: `context-engineer`.
4. **Backfill `resolved-by` on td-028/td-033** (S-2) — longstanding, low cost. Owner: `orchestrator`.
5. **Address td-171/td-172** per their ledger remedies when convenient — neither is urgent. Owners: `implementer`, `test-engineer`.
6. Optional: restate the "extend the net, not forbid the citation" principle in `adr-authoring-protocols.md` (S-7) if a future editor finds the compressed `adr-conventions.md` prose insufficient. Owner: `context-engineer`.

---
[COMPLETE]
