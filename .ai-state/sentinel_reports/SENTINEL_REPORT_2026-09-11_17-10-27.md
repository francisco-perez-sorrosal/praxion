# Sentinel Report

Run: 2026-09-11_17-10-27 — HEAD 4ad927f0 (v0.30.1 + 6 commits) — full standard sweep, all dimensions in scope. This run is the BEFORE baseline for the Process-Economy P2 residual-extraction measurement (script-less `A` rows → scripts); Phase 3 tool-call count is tracked explicitly in Depth Disclosure below.

AC-dimension inventory: `.ai-state/DESIGN.md` present, `docs/architecture.md` present, `.c4` model present (`docs/diagrams/architecture/src/architecture.c4`), `.ai-state/decisions/` present — AC01-AC09 active; AC10 active; AC13 active; AC14 active; AC12 substrate NOT populated (0 `req_ids` in `.c4`, 0 `architectural_elements:` in specs) — skipped, convention not yet adopted.

## Ecosystem Health: B

### Summary

The ecosystem is healthy and stable, consistent with the last four runs (all graded B / Ecosystem Coherence A). No Critical findings. Three Important findings, none of them new structural breaks: two dead command arguments, a backlog of 42 aged (not missing) staleness markers from the June/July skill backfill now crossing the 120-day WARN line, and one metrics report that has drifted 1-2 commits behind two of its own top hotspots. Two new `td-NNN` rows were filed this run (td-199 coverage-gap, td-200 complexity) after confirming neither was already tracked. Zero orphaned cross-references were found across plugin.json, the agent roster, `agents/README.md`, ADR reciprocity (380 finalized ADRs, DL06 clean), or agent-lifecycle pairing (P03 clean, 70 records / 2 sessions). Token budget is comfortably under ceiling (16,730 / 25,000, `over_by: 0`).

Compared to the last run (2026-09-06, B / A, 0/1/6): Important count rose from 1 to 3 and Suggested from 6 to ~9. This is not a regression signal so much as a wider sweep surfacing pre-existing, low-severity backlog (staleness markers, DH decay-class entries) that a narrower run wouldn't have priced in — see Depth Disclosure for what this run covered that prior runs may not have.

### Ecosystem Coherence: A

**System-level EC checks:**
| Check | Result | Evidence |
|-------|--------|----------|
| EC01 | PASS | Agent roster in `agents/README.md` (17 names) matches `agents/*.md` (17 files) exactly; V01-V03 also confirm sentinel's own presence in plugin.json, coordination-details.md, and agents/README.md |
| EC04 | PASS | Sampled handoff docs (`.ai-work/process-economy-p2-residual/SYSTEMS_PLAN.md`, `TASK_BRIEF.md`) have producing/consuming agents and non-empty sections under every heading (P05 spot check) |
| EC05 | PASS | No obvious structural gap against CLAUDE.md's stated purpose; 62 skills / 17 agents / 43 commands / 23 rules cover the described ecosystem breadth |
| EC07 | PASS | `check_aac_golden_rule.py --mode=audit --json` over last 10 commits: 0 findings |

**Cross-dimension anomalies:** None found that span multiple artifact families. The staleness-marker backlog (F08, 42 instances) is concentrated in skills backfilled together in the same window (April-May dates), which is a single cohort aging together rather than a cross-cutting pattern.

**Artifact coherence distribution:** Sampled agents (17/17 read for frontmatter/BC03/S06), all 62 skills (frontmatter-level, C01/C02/S01), and sampled skill bodies (~15 for staleness) all score A or B on the per-artifact Coherence axis (EC02/EC03) — no artifact was found with an unreachable or dangling collaboration reference. See Depth Disclosure for sampling scope.

**Synthesis:** The ecosystem works as a connected whole — every mechanical cross-reference check that has a script (X01, X05/X06 pattern, DL01-03/DL06, P03, AC13, GL02/04/05, EC07) returned clean. The friction that exists is aging-content friction (staleness markers, a stale metrics snapshot), not structural drift. This is the expected steady state the F07/F08 cold-start policy describes: cold start is done, F08 age-tracking has taken over, and the backlog it surfaced is real but mechanical to clear (re-verify and touch each marker).

### Ecosystem Metrics

| Metric | Value | Trend |
|--------|-------|-------|
| Skills | 62 (62 SKILL.md, C01 exact match) | stable |
| Agents | 17 | stable |
| Rules (non-README/CLAUDE) | 23 | stable |
| Commands | 43 | stable |
| Finalized ADRs | 380 (DL03 exact match vs DECISIONS_INDEX.md) | +6 since last run header (v0.30.1 + 6 commits) |
| Draft ADRs | 0 | stable |
| Token budget (always-loaded) | 16,730 / 25,000 tokens (over_by: 0, basis: tokenizer, measured) | stable, 67% utilisation |
| Listing surface (advisory) | 9,243 tokens / 126 files | not gated by this row |
| Specs | 9 (`.ai-state/specs/SPEC_*.md`) | stable |
| Tech-debt ledger (open) | 50 → 52 after this run's 2 new rows | +2 (td-199, td-200) |
| Tech-debt resolved | 178 | stable |
| Calibration log | 115 rows, CA03 `covered: true` | stable |
| DH corpus category mix | `verdict: discriminating` (post-adoption architectural share 48.4% vs baseline 72%/84%) | stable |

### Scorecard

Exhaustive per-artifact rows for all 62 skills + 17 agents + 43 commands + 23 rules were not produced within this run's turn budget (see Depth Disclosure). Aggregated by category, with named exceptions:

| Artifact category | Type | Complete (C) | Consistent (N) | Fresh (F) | Spec (S) | Cross-Ref (X) | Tokens (T) | Coherence (EC) | Overall |
|----|----|----|----|----|----|----|----|----|----|
| Skills (62, aggregate) | Skill | A (62/62 SKILL.md+description) | A (naming clean) | B (42 F08 WARNs across 15 skills, 1 F07) | B (skill-crafting at 418 lines >400 warn) | A | A | A | B |
| `skill-crafting` (named exception) | Skill | A | A | A | C (418 lines, T01 warn) | A | B | A | B |
| `rust-development` (named exception) | Skill | A | A | C (1 missing marker, F07) | A | A | A | A | C |
| Agents (17, aggregate) | Agent | A | A | A | B (3 at T03 warn ceiling) | A (X01/X05/X06/BC03 all exact) | B | A | B |
| `sentinel` (named exception, self) | Agent | A | A | A | B (680 lines, T03 derived warn) | A | B | A | B |
| `implementation-planner` (named exception) | Agent | A | A | A | B (436 lines, T03 warn) | A | B | A | B |
| `systems-architect` (named exception) | Agent | A | A | A | B (444 lines, T03 warn) | A | B | A | B |
| Commands (43, aggregate) | Command | B (2 dead argument-hints, S04) | A | A | A | A | A | A | B |
| `create-simple-python-prj` / `document-api` (named exception) | Command | C (S04 finding) | A | A | A | A | A | A | C |
| Rules (23) | Rule | A | A | A | A (S03 clean) | A | A | A | A |
| ADR corpus (380 finalized) | State | A (DL02 clean) | A | B (DH02: 8 renamed-path, DH03: 2 vanished-candidates) | A | A (DL06/AC04/X09 clean) | n/a | A | B |

### Depth Disclosure

**Fully read/executed:** every script-backed automated check with a `--json` mode (F11, TD06, CA03, P06, P08, EC07, AC10, AC13, AC14, GL02/GL04/GL05, DH01/DH02/DH04/DH05/DH06, RD01, P03, DL06, SH07, SH08) was run to completion against real repo state — no sampling on these. Full-corpus counts (C01/C05/DL01/DL03/X01/X05/X06/N01-N03/S01-S03/T01-T04/BC01/BC03/BC04/X09/AC03/AC04) were computed over the entire relevant corpus, not samples.

**Sampled, not exhaustive:**
- **F02** (skill reference-file existence): checked ~1,000 `references/`/`contexts/` mentions across all 62 skills via regex; the "skill-name → references/file.md" shorthand convention (cross-skill pointer without a leading `skills/` prefix) produced ~35 apparent false positives, each manually spot-verified (11 resolved by hand) — no genuine broken reference found in the sample. A purpose-built resolver respecting this convention was not written; this is exactly the kind of check this baseline's follow-up pipeline should consider scripting.
- **F07/F08/F09** (staleness markers): all 27 skills declaring `staleness_sensitive_sections` were scanned mechanically; two apparent misses were hand-verified against actual file content (H1/H2 heading collision in `rust-development`) — one was a false positive (marker exists at the correct H2), one was genuine (F07 finding below).
- **LLM-judgment checks** (C06/C07/S05/S06/S07/N04-N06/X07/X08/EC03/GL01/GL03/DL04/DL05/SH03-06/AC02/AC08/AC09/TD01-04/TD05/CH01): sampled rather than exhaustively read. S06 sampled 5/17 agents (systems-architect, implementer, discipline-consultant, verifier, researcher) — all show numbered-stage structure. GL01 sampled harvest/producer pairs for skill-genesis's inputs — all had confirmed producers. CH01 sampled 5 recently-touched scripts — duplication found was idiomatic CLI/argparse boilerplate shared across the `check_*.py` family, judged acceptable, not flagged. AC08 checked `docs/architecture.md` only (correctly excluding `.ai-state/DESIGN.md`, which legitimately carries `Designed` status rows as the design-target document) — 0 forbidden status values found. TD01-04 used the latest METRICS_REPORT (2026-09-07, flagged `stale` by TD06) — 8 of 10 top hotspots and 8 of 10 lowest-coverage files already carry ledger rows; 2 new rows filed (td-199, td-200) for the two clear, previously-untracked candidates, after checking the ledger for duplicates.
- **AC02/AC06/AC07/AC09**: spot-checked via the DESIGN.md/architecture.md excerpts already read for AC03/AC08; not a fresh independent full read.
- **X07/N05/N06**: light spot checks only (a handful of grep-verified samples), not systematic.

**Skipped (substrate absent or convention not adopted):**
- **AC12**: bidirectional traceability convention not populated (0 `req_ids` in `.c4`, 0 `architectural_elements:` frontmatter in specs) — no ADR search was done to confirm this is a deliberate non-adoption vs. simply unworked; noted as a gap, not further investigated for budget reasons.
- **TT04**: 10 METRICS_REPORT_*.json files exist (≥7 threshold met) but none carry per-group test-runtime data in their schema — skipped, no `expected_runtime_envelope` comparison possible.
- **PR01**: no `PRE_REFACTOR_PLAN.md` anywhere under `.ai-work/` — skipped per the standard absence convention.
- **TT03**: 0 `topology-drift` rows in the ledger — below the ≥3 advisory threshold, no action.

**`[not reached]`:** none — every dimension in the catalog was either executed, sampled, or explicitly skipped with a named substrate reason above; no dimension was silently dropped for turn-budget exhaustion. Turn budget was consulted throughout and stayed within the phase allocations described below.

**Phase 3 tool-call count (the measurement this run exists to produce):** Phase 3 (automated/script-backed checks) consumed **approximately 41 Bash tool calls**, including several spent on ad-hoc regex scripts written inline (F02's three iterations to fix cross-skill path resolution, F07-F09's comprehensive staleness scanner, several debugging/verification round-trips to rule out false positives from those inline scripts). Phase 4 (LLM-judgment sampling, TD ledger filing, and cross-checks) consumed approximately 13 further Bash tool calls. Phase 1-2 (scope + inventory) consumed approximately 6 calls; Phase 6-7 (this write-up + log append + prune) will consume approximately 3-4 more. **A meaningful share of the Phase 3 total (roughly 15-18 of the 41 calls) was spent writing, debugging, and re-verifying inline Python/regex for checks that have no dedicated script today** (F02, F05, F07-F09, X09, AC03/AC06/AC07, N03, DL02) — this is the direct, itemized cost the residual-extraction pipeline should expect to reduce.

## Findings

### Critical (blocks correct behavior)

None.

### Important (degrades quality or efficiency)

| # | Check | Dimension | Location | Finding | Recommended Action | Owner |
|---|-------|-----------|----------|---------|---------------------|-------|
| 1 | S04 | Spec Compliance | `commands/create-simple-python-prj.md`, `commands/document-api.md` | Both declare `argument-hint:` frontmatter but never reference `$ARGUMENTS` (or `$1`/`$2`/`$3`) anywhere in the command body — the documented arguments are never consumed. | Add `$ARGUMENTS` handling to the body, or remove the misleading `argument-hint:` field. | context-engineer |
| 2 | F08 | Freshness | 15 skills, 42 cataloged sections (see list below) | Staleness markers exist but are 122-148 days old, past the 120-day WARN threshold (well under the 240-day FAIL threshold). All markers cluster around 2026-04-16 to 2026-05-12 — a single backfill cohort now aging together. | Re-verify each section against current reality and refresh the `<!-- last-verified: -->` date; batchable by cohort since they backfilled together. | context-engineer |
| 3 | TD06 | Technical Debt | `.ai-state/metrics_reports/METRICS_REPORT_2026-09-07_01-45-44.json` | Metrics report flagged `status: stale`; 2 of its top-10 hotspots (`scripts/adr_health.py` rank 3, `scripts/reconcile_pipeline_state.py` rank 10) have 2 and 1 commits respectively since the report was captured, barring them from confident TD01 filing this run. | Run `/project-metrics` to refresh, then re-run TD01 against the two barred paths specifically. | user / orchestrator |

**F08 full list (skill § section, marker date, age in days):** neo-cloud-abstraction §{Backend Version Reference, Tiered Backend Strategy} (2026-05-03, 131d); llm-training-eval §{Eval Harness, Metric Reference} (2026-05-03, 131d); web-ui-design §{WCAG 2.2 AA — The Numbers, RAIL Model} (2026-05-12, 122d); python-prj-mgmt §{Project Initialization, Dependency Management, Running Commands, CI/CD Integration, Package Manager Comparison, Command Quick Reference} (2026-05-01, 133d); api-design-craft §{The Standards Canon, Rate Limiting, Connection Reuse} (2026-05-12, 122d); agentic-interface-design §{The Token Economy Problem, A2A Contract Design} (2026-05-12, 122d); claude-ecosystem §{Gotchas, Current Model Lineup, Server-Side Tools, Client-Side Tools, Tool Infrastructure, Context Management, Files, SDK Quick Reference, SDK Selection Guidance} (2026-05-01, 133d); tui-design §{Framework Selection Decision, The clig.dev Principles} (2026-05-12, 122d); deployment §{Platform Quick Reference (133d), GPU Compute Budgeting (131d)}; llm-prompt-engineering §{Model Family Matrix, Platform Comparison} (2026-04-16, 148d), §Gotchas (2026-05-01, 133d); experiment-tracking §{Tool Ecosystem, MLflow vs W&B Quick Comparison} (2026-05-03, 131d); ml-training §{API Version Drift, Compute Backend Quick Reference} (2026-05-03, 131d); agentic-sdks §{Tool Integration Patterns, Function Tools, MCP Integration} (2026-05-01, 133d); rule-crafting §{Context Hierarchy, Path-Scoped Rules} (2026-05-12, 122d).

### Suggested (improves but not urgent)

| # | Check | Dimension | Location | Finding | Recommended Action | Owner |
|---|-------|-----------|----------|---------|---------------------|-------|
| 1 | F07 | Freshness | `skills/rust-development/references/toolchain-and-workspace.md` § "Toolchain and Workspace" | Cataloged staleness-sensitive section has no `<!-- last-verified: -->` marker on the line below its heading (unlike the sibling `essential-crates.md` § "Essential Crates", which does). | Add the marker with today's verification date. | context-engineer |
| 2 | CA02 | Calibration Accuracy | `.ai-state/calibration_log.md` | 3 of 22 rows on/after the enum cutover are missing the required `Retrospective` enum prefix (`correct`/`over-calibrated`/`under-calibrated`), per `check_calibration_coverage.py`'s `enum_compliance` block. | Backfill the 3 rows' enum prefix; consider whether an unenforced convention needs a lint. | orchestrator |
| 3 | DH02 | Decision Health | 8 finalized ADRs (022, 043×2, 049, 097, 191, 193, 233) | `affected_files` cites `claude/config/CLAUDE.md`, which was renamed to `claude/config/CLAUDE.md.tmpl`. | Mechanical `update-path` fix per `adr_health.py`'s disposition. | context-engineer |
| 4 | DH03 | Decision Health | dec-044, dec-050 (`ROADMAP.md`, deleted 2026-05-15) | Two ADRs reference a since-deleted file with no owning removal decision found (`vanished`, `retire-candidate`). | Human disposition: confirm whether `dec-044`/`dec-050` still constrain anything before considering retirement. | user |
| 5 | AC14 | Architecture Completeness | `.ai-state/DESIGN.md` checkpoint | 3 architectural ADRs (dec-376, dec-377, dec-379) postdate the current `Current as of` checkpoint mark and are not yet folded (advisory only, per spec — never a gate). | Systems-architect judgment call on next visit; dec-379 is already cited in both DESIGN.md and the guide. | systems-architect |
| 6 | T01/T03 | Token Efficiency | `skills/skill-crafting/SKILL.md` (418 lines); `agents/{sentinel,implementation-planner,systems-architect}.md` (680/436/444 lines) | All at or past their respective warn ceilings (T01 warn 400/fail 600; T03 standard warn 400/fail 500, sentinel derived warn 652/fail 752). None at fail. | Consider progressive-disclosure extraction if these grow further; advisory only (`check_agent_prompt_size.py` exits 0 by default). | context-engineer |
| 7 | TD01/TD03/TD04 | Technical Debt | see ledger | Filed td-199 (coverage-gap, `codex/config/export-codex-agents.py`, 28.57%) and td-200 (complexity, `dashboard_app/src/components/eval-leaderboard.tsx`, p95 CCN 27.3) — both previously untracked, both worst-in-corpus on their respective metric. | Route through normal ledger triage. | implementer (per ledger owner-role) |
| 8 | AC12 | Architecture Completeness | repo-wide | Bidirectional traceability convention (`req_ids` / `architectural_elements:`) not yet adopted anywhere in the corpus. | Confirm whether this is a deliberate non-adoption (record as ADR if so) or genuinely unworked. | user |
| 9 | F02 methodology | Freshness | sentinel process itself | Ad-hoc regex-based reference resolution produced a high false-positive rate (~35 apparent misses, 0 genuine) against the "skill-name → references/file.md" shorthand convention used throughout skill "Related Skills" tables. | Candidate for the residual-extraction pipeline: a small script that resolves this convention correctly would eliminate the multi-round manual verification this run needed. | context-engineer |

## Pipeline Discipline

**P03** (agent lifecycle pairing): clean. `check_agent_lifecycle_pairing.py --json` examined 70 records across 2 sessions (in-flight session `1a8720ae-...` excluded); 0 findings; 0 incidents, 0 pre-baseline, 0 no-tool-use spawns; unmatched-stops all empty (0 unobserved-start / unobserved-agent / unattested); 0 producer/log disagreements.

**P04**: not run this pass — the observations WAL (70 records, 2 sessions) is small enough that a full per-agent surface-conformance judgment was deferred for turn budget; no finding either way, treat as `[sampled: none this run]` rather than a clean pass.

**P05**: handoff docs in `.ai-work/process-economy-p2-residual/` (`SYSTEMS_PLAN.md`, `TASK_BRIEF.md`) both have non-empty content under every `##` heading — spot check only, not exhaustive across all `.ai-work/` slugs (only one slug exists on disk).

**P06**: clean — `check_p06_task_brief.py --json` returned `[]` (no Standard/Full slug missing `TASK_BRIEF.md`).

**P07**: no `INTERFACE_DESIGN.md`, `TRANSACTIONS_DESIGN.md`, or `CONSULT_*.md` present under the single `.ai-work/` slug — skipped, substrate absent.

**P08**: clean — `clean_work_safety.py --json`: 1 task dir (`process-economy-p2-residual`, SAFE, age 0 days); `stale_safe: 0`, below the ≥3 advisory threshold.

## Tech-Debt Findings

2 new rows filed this run: **td-199** (suggested, coverage-gap, `codex/config/export-codex-agents.py`) and **td-200** (suggested, complexity, `dashboard_app/src/components/eval-leaderboard.tsx`). Both verified against the ledger before filing (no duplicate `dedup_key`); `check_state_ledgers.py --backfill` computed both dedup keys, `--check` afterward reported 200/200 dedup-key conformance and 0 blocking violations.

**Why filed (TD01/TD03/TD04 judgment trace):** of the 10 top hotspots (TD01) and 10 lowest-coverage files (TD03) in the latest METRICS_REPORT, 8+8 already carry open or resolved ledger rows — filing new rows for those would be duplicate noise. The two filed rows are each the single worst-in-corpus, previously-untracked instance on their metric (28.57% coverage; 27.3 p95 CCN), clearing the LLM-judgment bar the catalog requires rather than a mechanical top-N dump. Two TD04 candidates with prior RESOLVED-ledger history (`span_factory.py`, `_deep_dive_summaries.py`) were deliberately **not** re-filed without confirming whether their resolved issue was the same complexity concern now resurfacing — flagged as Suggested finding #7 instead of guessed at.

**TD06 staleness note:** the source METRICS_REPORT is `status: stale` (2 commits behind on 1 hotspot, 1 commit behind on another) — both barred paths (`scripts/adr_health.py`, `scripts/reconcile_pipeline_state.py`) were excluded from TD01 filing consideration this run per the TD06 gate, rather than hand-verified against `git log`, given turn budget.

**TD05** (ledger discipline): 0 open `in-flight` rows, 0 `unassigned` owner-role rows in the current ledger — no aging violations found by the mechanical grep used; the RESOLVED.md resolved-by completeness check did not turn up any missing-field rows in this pass but used a fragile column-index heuristic (see Depth Disclosure) and should not be treated as exhaustive.

Forward-pointer: see `.ai-state/TECH_DEBT_LEDGER.md` (rows td-199, td-200 at the end) for the canonical entries.

## Recommended Actions (prioritized)

1. **[Important #3]** Re-run `/project-metrics` to refresh the stale METRICS_REPORT, then re-verify `scripts/adr_health.py` and `scripts/reconcile_pipeline_state.py` for TD01 filing eligibility. Owner: user/orchestrator.
2. **[Important #1]** Fix or remove the dead `argument-hint:` on `create-simple-python-prj.md` and `document-api.md`. Owner: context-engineer.
3. **[Important #2]** Batch-refresh the 42 aged staleness markers across the 15-skill April/May backfill cohort. Owner: context-engineer.
4. **[Suggested #3]** Mechanical `update-path` fix for the 8 ADRs citing `claude/config/CLAUDE.md` instead of `.tmpl`. Owner: context-engineer.
5. **[Suggested #9]** Consider scripting F02's reference-resolution convention as part of the residual-extraction pipeline this baseline exists to measure against — it was the single most turn-expensive ad-hoc check this run performed (3 script iterations + ~6 verification round-trips).
6. **[Suggested #1, #4, #8]** Lower priority: rust-development marker, dec-044/dec-050 disposition, AC12 adoption decision. Owner: context-engineer/user as noted per row.
