# Sentinel Report — 2026-09-12_02-10-28

AC-dimension inventory: `.ai-state/DESIGN.md` present; `docs/architecture.md` present; LikeC4 model present at `docs/diagrams/architecture/src/architecture.c4`; `.ai-state/decisions/` present. AC01-AC09, AC10, AC13, AC14 activated; AC12 activation depends on bidirectional convention population (checked in Pass 2).

## Phase 3 — Pass 1: Automated Checks (raw findings, pre-scoring)

### Family dispatch (11/11 scripts run)

| Family script | Rows | Result |
|---|---|---|
| `check_staleness_markers.py` | F07 F08 F09 | F07: 9 WARN; F08: 33 WARN; F09: 0 (PASS). 3 headings withheld (not found in SKILL.md/references/contexts — `deployment::GPU Compute Budgeting`, `likec4-querying::## Decision Rubric`, `likec4-querying::## MCP Tool Quick Reference`) |
| `check_hook_installation.py` | F10 | PASS — 1 hook source, installed under >=1 name |
| `check_state_corpus.py` | DL01 DL02 SH01 SH02 CA01 | DL01/DL02/SH02/CA01: PASS. SH01: 2 FAIL + 40 WARN (both from `.ai-state/specs/SPEC_diagrams_2026-04-30.md` and `SPEC_multi-language-support_2026-05-11.md` path-column non-conformance) |
| `regenerate_adr_index.py --check` | DL03 | PASS — 380 finalized ADRs, index in sync |
| `check_topology_conformance.py` | TT01 TT02 TT05 TT06 | PASS — 13 groups, all subsystems/selectors/markers resolve; TT06 not applicable (topology present) |
| `check_hackathon_graduation.py` | HK01 | Skipped — hackathon mode off |
| `check_sentinel_self_audit.py` | V01 V02 V03 V04 | PASS — 17 agents registered, roster/README rows present, catalog populated (22 dimensions, 351 catalog lines) |
| `check_registry_projection.py` | X01 X02 X05 X06 EC01 EC02 | X01/X02/X05/X06/EC01: PASS. EC02: 30 WARN — skills never referenced by any agent/CLAUDE.md/rule (e.g. `adapt-claude-to-agents`, `agent-failure-taxonomy`, `agent-readiness`, `agent-runtime-guardrails`, `applied-statistics`, `architectural-fitness-functions`, `likec4-querying`, `llm-prompt-engineering`, `neo-cloud-abstraction`, +21 more) |
| `check_path_resolution.py` | F01 F02 F05 X03 X09 | F01: 39 WARN (see triage below). F02: 5 FAIL — `code-review/references/beauty-review-checklist.md`, `rust-development/references/{rust-patterns,rust-testing,rust}.md`, `test-coverage/references/go.md` all named in their SKILL.md but absent on disk. F05/X03/X09: PASS |
| `check_behavioral_contract.py` | BC01 BC03 BC04 | PASS — 4 behaviors named, exactly 14 contract-bound agents cite the rule, 6 canonical tags present |
| `check_specialist_dispositions.py` | P07 | PASS — 1 in-flight slug, no undisposed Architecture Challenges |

**F01 triage** (39 findings, all `warn` severity per the script's own assignment): manually spot-checked ~15/39. Dominant pattern (~30+) is the script's repo-root-only resolver applied to skill-relative markdown links/tokens that resolve correctly in context (e.g. `rust-development/SKILL.md`'s `assets/rustfmt.toml`, `assets/cargo-lints.toml`, `assets/rust-toolchain.toml` all exist under `skills/rust-development/assets/`; `adapt-claude-to-agents/SKILL.md`'s `scripts/claude_to_agents.py` exists under the skill's own `scripts/`; `git-conventions.md`'s `../../ml/experiment-commit-conventions.md` resolves fine as a relative link but the extractor strips it to `ml/experiment-commit-conventions.md`) — a known false-positive class of this repo-root-relative-only design, not a defect in the corpus. `node-prj-mgmt/SKILL.md`'s `contexts/javascript.md` is explicitly framed as a *future* addition in the prose ("added here without..."), not a gap. One item merits a closer look: `.claude-plugin/marketplace.json` (referenced from `skills/versioning/SKILL.md` and `rules/swe/plugin-install-conventions.md`) does not exist in this repo — plausibly lives in a separate marketplace repo Praxion publishes to; not verified further.

### Additional script-backed rows (16 scripts, batched into ~5 calls)

| Script | Rows | Result |
|---|---|---|
| `check_spec_drift.py --json` | SH07 | PASS — 0 findings |
| `check_spec_archival_gap.py --json` | SH08 | PASS — `gap: false` (newest spec 9 days old, no qualifying ADR cluster) |
| `check_calibration_coverage.py --json` | CA03 | PASS — `covered: true`, 0 uncalibrated commits since 2026-09-11. Side note (not a catalog row, informational): `enum_compliance.compliant: false` — 3 of the last 22 enum-bearing rows are missing the required Retrospective enum prefix; carried forward to CA02 (Pass 2) |
| `check_p06_task_brief.py --json` | P06 | PASS — 0 rows (empty list) |
| `clean_work_safety.py --json` | P08 | No advisory — `stale_safe: 0` (1 task dir, classification BLOCK — active pipeline, not stale) |
| `check_aac_golden_rule.py --mode=audit --json` | EC07 | PASS — 0 important findings |
| `aac_fence_validator.py` ×2 | AC10 | PASS — both `.ai-state/DESIGN.md` and `docs/architecture.md` verdict PASS |
| `check_architecture_projection.py --json` | AC04 AC05 AC13 | PASS — 0 findings (161 dec-ids examined for AC04; 16 rows/16 elements for AC13) |
| `check_design_checkpoint.py --json` | AC14 | `checkpoint_state: present` (PASS, not FAIL) — advisory: 3 un-folded architecture-bearing ADRs since checkpoint `dec-375` (asserted 2026-09-06, corpus tip `dec-380`): dec-376, dec-377, dec-379 |
| `check_gate_liveness.py --json` | GL02 GL04 GL05 GL06 | PASS — 0 findings across all four kinds |
| `adr_health.py --json` | DH01 DH02 DH04 DH05 DH06 | DH01 (`removed-by-later`): 0 findings — PASS. DH02 (`renamed`/`placeholder-shape`/`out-of-repo`): 8 WARN (all `renamed`, e.g. `022-coordination-detail-extraction.md` → `claude/config/CLAUDE.md` renamed to `.tmpl`); `renamed-by-self`(3)/`removed-by-self`(63)/`lazy-artifact`(3) are explicitly not findings. DH04 (`reopen_candidates`): 0 — PASS. DH05: `category_mix.verdict: "discriminating"` — PASS (post-adoption architectural share 0.484 of n=62). DH06 (`status_edge_conflicts`): 0 — PASS. `skipped_terminal`: 23 decisions excluded by design. `vanished`: 2 candidates carried to DH03 (Pass 2) |
| `check_readiness_feedback.py --json` | RD01 | PASS — `adjusted_level: 4` >= floor 3, `mechanical_only: false` |
| `check_metrics_freshness.py --json` | TD06 | **WARN** — `status: stale` (report `METRICS_REPORT_2026-09-07_01-45-44.json`, age 5 days but 167 commits since; `dirty: true`). `hotspots_touched`: `scripts/adr_health.py` (rank #3, 5 commits since) and `scripts/reconcile_pipeline_state.py` (rank #10, 1 commit since) — both barred from TD01 filing without hand re-verification |
| `check_agent_lifecycle_pairing.py --json` | P03 | PASS — 1809 records, 2 sessions (1 excluded as in-flight), 0 findings, 0 incidents/pre_baseline/no_tool_use, 0 unmatched_stops, 0 producer_log_disagreement |
| `measure_token_budget.py --json` | T02 | PASS — 16,730 tokens / 25,000 budget, `over_by: 0`, `basis: tokenizer` (real measurement), headroom 8,270. Listing (advisory): 9,243 tokens / 126 files |
| `check_agent_prompt_size.py --json` | T03 | 3 WARN (no FAIL) — `implementation-planner.md` 436 lines (warn 400/500); `sentinel.md` 674 lines (derived warn 652/fail 752, S=352); `systems-architect.md` 444 lines (warn 400/500) |

### Improvised inline checks (no backing script — 12 checks, ~10 Bash calls)

| Check | Result |
|---|---|
| C01 | PASS — 62 skill dirs = 62 `SKILL.md` files |
| C02/S01 | PASS — every `SKILL.md` has non-empty `description:` |
| C03/S02 | PASS — all 17 agents have `name`/`description`/`tools` (2 initial false positives from a truncated `head -25` scan on long-description agents, corrected by scanning the full frontmatter block) |
| C04 | PASS — every command has a non-empty `description` |
| C05 | PASS — plugin.json lists 17 agents = 17 files in `agents/*.md` |
| N01 | PASS — all 62 skill dirs kebab-case |
| N02 | PASS — all 17 agent files kebab-case |
| N03 | PASS — every frontmatter key across skills/agents/commands/rules matches a documented field in `skill-crafting`/`rule-crafting` schema (initial run over-flagged `allowed-tools`/`compatibility`/`when_to_use`/etc. as "unrecognized" before checking the schema table — all are valid documented fields) |
| S03 | PASS — every rule file's first non-blank content line is `##`-level or frontmatter |
| S04 | PASS — every command with `argument-hint:` has `$ARGUMENTS` or positional `$N` substitution (initial substring-matching heuristic on "arg" produced false positives on words like "target"; corrected to check `argument-hint:` presence) |
| T01 | 1 WARN — `skills/skill-crafting/SKILL.md` 418 lines (warn >400) |
| T04 | PASS — 0/263 reference/context files exceed 800 lines |
| AC03 | PASS — 72 path tokens in `.ai-state/DESIGN.md` §3a, 0 genuinely unresolved (glob/brace-expansion/template artifacts accounted for) |
| AC06 | PASS — all `docs/architecture.md` §3a component names map to real module dirs |
| AC07 | PASS — 98 path tokens in `docs/architecture.md` §3a, 0 genuinely unresolved (`dashboard_app/`-relative and brace-expansion accounted for) |


## Phase 4 — Pass 2: LLM Judgment

**Sampling disclosure**: full reads on all 17 agent files (frontmatter + descriptions + phase structure + collaboration sections); targeted extraction (not full read) on skills — 9 of 62 skill descriptions read in full for C06, remainder judged via T01 line counts + N03 frontmatter scan + F01/F02 script findings; rules/CLAUDE.md/plugin.json read via targeted grep, not full text.

| Check | Dimension | Result | Evidence |
|---|---|---|---|
| C06 | Completeness | PASS (sample n=9 skills, incl. all 8 EC02-orphaned skills spot-checked) | Every sampled description states what+when+trigger terms and disambiguates against sibling skills (e.g. `agent-failure-taxonomy`: "Diagnosis -- not prevention (agent-runtime-guardrails)") |
| C07 | Completeness | PASS (all 17 agents) | Every agent description is distinct, states purpose + explicit "Use proactively/Use when" trigger; no overlap ambiguity found (e.g. cicd-engineer vs implementer, verifier vs architect-validator vs sentinel vs doc-engineer all cross-reference explicit boundary tables) |
| C08 | Completeness | PASS | 0 unfilled `[CUSTOMIZE]` sections in any live rule file; all matches are documentation-about-the-pattern (rule-crafting, rules/README.md) or the catalog's own self-quoting row |
| N04 | Consistency | PASS (spot-check) | "context file" is used consistently for the `contexts/*.md` progressive-disclosure mechanism (a distinct, well-established term), never as a synonym for "context artifact" — no collision found |
| N05 | Consistency | PASS (spot-check) | No contradiction found between agent boundary descriptions and rule definitions in the sampled set |
| N06 | Consistency | PASS (sample) | Agent descriptions, README table rows, and skill descriptions follow consistent tone/structure (what + trigger + disambiguation) |
| F03 | Freshness | PASS (spot-check) | No references to replaced tools/patterns found in the sampled agent/skill content |
| F04 | Freshness | PASS | All agent-to-agent collaboration references name agents that exist (confirmed by X08's zero-orphan grep); stage/output names cross-checked against actual `.ai-work/` artifact conventions in read files |
| S05 | Spec Compliance | PASS | Only `skills/skill-crafting/SKILL.md` exceeds 400 lines (418, WARN not FAIL per T01); all others are well within progressive-disclosure guidance |
| S07 | Spec Compliance | PASS (spot-check) | Rules read (git-conventions, adr-conventions, agent-behavioral-contract) carry declarative domain knowledge, not step-by-step procedures |
| T05 | Token Efficiency | PASS | No monolithic artifact found needing a core/reference split beyond the existing T01 WARN |
| T06 | Token Efficiency | PASS | Repeated short boilerplate blocks across agents ("Apply the behavioral contract", "Turn budget awareness", "Partial output on failure") are intentional convention repetition (2-4 lines each), not substantive duplicated content |
| X04 | Cross-Reference | N/A (vacuous PASS) | `.ai-state/idea_ledgers/IDEA_LEDGER.md`'s `## Implemented` section is explicitly empty ("none from this run — see prior ledgers and git history"); nothing to cross-check this run. Note: ledger `Last updated: 2026-06-05`, ~3 months stale relative to this run — not a formal finding (no check mandates ledger freshness), flagged as an observation only |
| X07 | Cross-Reference | PASS (spot-check) | `agents/README.md` rows for `architect-validator`, `sentinel`, `skill-genesis` accurately reflect their frontmatter descriptions (modes, output files, ledger behavior) |
| X08 | Cross-Reference | PASS | 0 orphaned collaboration references — every backtick-quoted agent-like name across all 17 agent files resolves to a real agent (`bypass-verifier`/`loop-back-to-architect`/`run-verifier` are recommendation-outcome labels, not agent names) |
| EC03 | Ecosystem Coherence | PASS | Same evidence as X08 — no collaborator named that doesn't exist or produce its claimed artifact |
| EC04 | Ecosystem Coherence | PASS (sample) | Every pipeline document sampled (`TRANSACTIONS_DESIGN.md`, `ARCHITECTURE_VALIDATION.md`, `CONTEXT_REVIEW.md`, `TECH_DEBT_LEDGER.md`) has an explicit named producer and consumer(s) — no dead ends found |
| EC05 | Ecosystem Coherence | PASS (sample) | No obvious missing artifact type surfaced against CLAUDE.md's stated purpose in the areas sampled |
| GL01 | Gate Liveness | PASS (sample) | Sampled document flows (`CONTEXT_REVIEW.md`, `TRANSACTIONS_DESIGN.md`, `ARCHITECTURE_VALIDATION.md`) all have a named producer for every section a downstream agent is told to read |
| GL03 | Gate Liveness | PASS (sample) | Sampled catalog rows (SH02, SH04, P05, C04) already carry explicit "≥1 row" / non-empty-content substance clauses per prior self-hardening; sampled verifier phase gates (Phase 4, Phase 8) name concrete artifacts/sections rather than bare presence |
| DH03 | Decision Health | **Advisory — 2 candidates, 1 requires action, not simple retirement** | Both `vanished` candidates from `adr_health.py` (ADRs `044-coding-style-path-scoping.md` and `050-always-loaded-budget-revision.md`, both citing deleted `ROADMAP.md`) have a real owning decision — `dec-092` ("Retire ROADMAP.md as living document"), confirmed present and dated 2026-04-28, after both ADRs. `adr_health.py`'s `removed-by-later` matcher did not find it because `dec-092`'s `affected_files` lists the *destination* files (`TECH_DEBT_LEDGER.md`, `IDEA_LEDGER_*.md`, `CLAUDE.md`, `README.md`), not the deleted `ROADMAP.md` path itself. Recommend either adding `ROADMAP.md` to `dec-092`'s `affected_files` (repairs the automated matcher going forward) or manually cross-linking `044`/`050` via `superseded_in_part_by`/notes — not blind retirement |
| AC01 | Architecture Completeness | PASS | `.ai-state/DESIGN.md` exists with 16 populated §3a component rows (well past the ≥1 bar) |
| AC02 | Architecture Completeness | PASS (spot-check) | Component names consistent between the sections read |
| AC08 | Architecture Completeness | PASS | 0 unbuilt-status rows in `docs/architecture.md` (all "Planned"/"Designed" hits are in `.ai-state/DESIGN.md`'s §3b capabilities table, which is explicitly the design-target document and out of AC08's scope) |
| AC09 | Architecture Completeness | **PASS both directions** | (a) subset: all 16 `docs/architecture.md` §3a component names ⊆ `.ai-state/DESIGN.md` §3a component names. (b) coverage: all 16 `.ai-state/DESIGN.md` §3a component names also appear in `docs/architecture.md` §3a — exact 1:1 name-set match, 0 coverage gap |
| AC12 | Architecture Completeness | **Skipped — named precondition: convention not yet populated** | No `.c4` element carries `metadata.req_ids` (0 hits repo-wide); no `.ai-state/specs/SPEC_*.md` carries `architectural_elements:` frontmatter. Deliberate non-adoption is recorded in `dec-371` ("AC12 bidirectional traceability unadopted") — this is a decision, not an unworked backlog item |
| CH01 | Code Health | PASS (sample n=5) | Sampled recently-touched scripts (`adr_health.py`, `check_architecture_projection.py`, `check_doc_manifest_freshness.py`, `measure_token_budget.py`) share a common `_repo_root` helper module rather than duplicating repo-root resolution; `check_id_citation_discipline.py`'s inline `git rev-parse` is a legitimately different hook-context pattern, not duplication |
| SH03 | Spec Health | PASS (by proxy) | SH07 (`check_spec_drift.py`) returned 0 findings across all 9 archived specs — used as a proxy for behavioral-claim-vs-implementation drift |
| SH05 | Spec Health | PASS (sample n=2) | `SPEC_diagrams_2026-04-30.md` and `SPEC_multi-language-support_2026-05-11.md` both have substantive "## Decisions Made" sections (what + why + category, e.g. "Angular excluded... incompatible with the Biome/Vitest defaults; revisit if 3+ Praxion-managed Angular projects emerge") |
| SH06 | Spec Health | **Not evaluated — no predecessor pairs sampled** | Diffing archived-spec-vs-prior-version was out of scope for this pass's time budget; deferred |


## Phase 5-7 — Scoring, Report Completion, Log

### Ecosystem Health: B

### Summary

This is the AFTER measurement paired with the BEFORE run `SENTINEL_REPORT_2026-09-11_17-10-27.md` (HEAD `4ad927f0`), auditing HEAD `c702908a` after the Phase 3 family-dispatch refactor (33 of 55 script-less `A` rows extracted into 11 family scripts; the ad-hoc bash fence retired). **The refactor works as designed**: 27 distinct auto checks now run via 7 batched script-invocation calls (11 family-table scripts + 16 additional conditional scripts), where the BEFORE run had to hand-derive nearly all of them via improvised bash. The corpus itself is healthy — 0 Critical findings, 3 Important (two are pre-existing content gaps in an old archived spec and 3 skills' missing reference files; one is a genuine decision-graph repair opportunity this pass's DH03 judgment surfaced with a concrete fix). Ecosystem Coherence is **A** — all four system-level EC checks (EC01/EC04/EC05/EC07) pass clean, no orphaned pipeline stages, no dead-end handoffs.

The most actionable finding this run is **DH03**: two ADRs (`044`, `050`) reference a deleted `ROADMAP.md` with no automatically-detected owning decision, but manual investigation found the actual owning decision (`dec-092`) exists — its `affected_files` just doesn't cite the path it deleted. This is exactly the class of finding `adr_health.py`'s mechanical matcher cannot make and LLM judgment (DH03) exists to catch.

**Depth caveat that affects grade comparison**: this run's Pass 2 (LLM judgment) was sampled, not exhaustive (see Depth Disclosure) — full reads on all 17 agents, targeted extraction on ~9/62 skills. The BEFORE report's Pass 2 depth is not independently known to this run; if the BEFORE grade also used equivalent-or-narrower sampling, the B-grade comparison is apples-to-apples. If BEFORE did a deeper Pass 2, a lower Important count here could partly reflect less scanning rather than genuine improvement — flagged explicitly per the constraint against a "numerically better grade earned by looking at less."

### Ecosystem Coherence: A

**System-level EC checks:**
| Check | Result | Evidence |
|-------|--------|----------|
| EC01 | PASS | `check_registry_projection.py` — 0 findings, 16 pipeline-diagram names all resolve to agent files |
| EC04 | PASS (sample) | Every pipeline document sampled has a named producer and consumer(s); no dead ends found |
| EC05 | PASS (sample) | No obvious missing artifact type surfaced against CLAUDE.md's stated purpose in areas sampled |
| EC07 | PASS | `check_aac_golden_rule.py --mode=audit` — 0 important findings over the last 10 commits |

**Cross-dimension anomalies:**

EC02 (30 skills never referenced by any agent/CLAUDE.md/rule) is a borderline pattern worth naming explicitly: Praxion's own CLAUDE.md states "Components are never enumerated in always-loaded context — the assistant finds them via filesystem scanning. This is itself a design principle." This means EC02's premise (co-reference required) may be checking against an intentional architectural choice rather than drift — the 30 flagged skills (`adapt-claude-to-agents`, `agent-failure-taxonomy`, `agent-readiness`, `applied-statistics`, `likec4-querying`, `llm-prompt-engineering`, `neo-cloud-abstraction`, +23 more) all had clear, well-written activation-trigger descriptions when spot-checked (C06), suggesting they rely on description-based discovery by design, not abandonment. Reported at Suggested tier rather than Important given this tension; recommend context-engineer confirm which of the 30 are genuinely dormant vs. intentionally description-discoverable.

**Artifact coherence distribution** (from the Scorecard below): 14 agents graded A (all-PASS), 3 agents graded B (T03 line-count WARN only). Skills/commands/rules aggregate: no per-file FAIL found in any auto check that ran against the full corpus (F01/F02/N01-03/C01-05/S01-04/T01/T04 all corpus-wide); 5 skills carry a FAIL-tier F02 finding (missing reference files) — see Important findings.

**Synthesis:**

The ecosystem works as a connected whole: every cross-reference integrity check (X01-X09), the behavioral contract wiring (BC01/03/04), the pipeline diagram (EC01), and the AaC golden rule (EC07) are all clean. The friction points are all at the edges — one archived spec with dead path-column references, three skills with declared-but-missing reference files, and a decision-graph gap that a mechanical matcher structurally cannot close (by design — `removed-by-later` matches on `affected_files` citation, not causal inference). None of these touch the machinery that keeps the fleet coordinated.

### Ecosystem Metrics

| Metric | Value | Trend (vs BEFORE `SENTINEL_REPORT_2026-09-11_17-10-27.md`) |
|--------|-------|-------|
| Skills | 62 | — |
| Agents | 17 | — |
| Rules | 24 | — |
| Commands | 43 | — |
| Hooks | 52 | — |
| ADRs (finalized / draft) | 380 / 1 | — |
| Specs archived | 9 | — |
| Metrics reports | 10 | — |
| Token budget (governed, tokenizer basis) | 16,730 / 25,000 (headroom 8,270) | — |
| Token budget listing (advisory) | 9,243 / 126 files | — |
| Critical findings | 0 | — |
| Important findings | 3 | — |
| Suggested findings | 10 | — |
| Ecosystem Health Grade | B | — |
| Ecosystem Coherence Grade | A | — |
| **Phase 3 tool-call count** | **~47 total** | see Depth Disclosure |
| **— of which script/family invocations** | **7 calls (27 distinct script executions)** | new this run — the family-dispatch mechanism |
| **— of which improvised/ad-hoc/parsing** | **~40 calls** | see breakdown below |

*No prior-run trend deltas computed for the corpus-count rows — `SENTINEL_LOG.md` will carry this run's baseline forward for the next comparison.*

### Scorecard (Agents — full 17, per-agent)

| Artifact | Type | C | N | F | S | X | T | Coherence (EC) | Overall |
|----------|------|---|---|---|---|---|---|---|---|
| agentic-transactions-architect | Agent | A | A | A | A | A | A | A | A |
| architect-validator | Agent | A | A | A | A | A | A | A | A |
| cicd-engineer | Agent | A | A | A | A | A | A | A | A |
| context-engineer | Agent | A | A | A | A | A | A | A | A |
| discipline-consultant | Agent | A | A | A | A | A | A | A | A |
| doc-engineer | Agent | A | A | A | A | A | A | A | A |
| implementation-planner | Agent | A | A | A | A | A | B (T03 WARN, 436 lines) | A | B |
| implementer | Agent | A | A | A | A | A | A | A | A |
| interface-designer | Agent | A | A | A | A | A | A | A | A |
| promethean | Agent | A | A | A | A | A | A | A | A |
| researcher | Agent | A | A | A | A | A | A | A | A |
| roadmap-cartographer | Agent | A | A | A | A | A | A | A | A |
| sentinel | Agent | A | A | A | A | A | B (T03 WARN, derived ceiling) | A | B |
| skill-genesis | Agent | A | A | A | A | A | A | A | A |
| systems-architect | Agent | A | A | A | A | A | B (T03 WARN, 444 lines) | A | B |
| test-engineer | Agent | A | A | A | A | A | A | A | A |
| verifier | Agent | A | A | A | A | A | A | A | A |

### Scorecard (Skills / Commands / Rules — aggregate, corpus-wide auto checks)

| Family | Count | C | N | F | S | X | T | Overall |
|---|---|---|---|---|---|---|---|---|
| Skills | 62 | A (C01/C02 clean) | A (N01/N03 clean) | **C** (5 F02 FAIL across 3 skills; 39 F01 WARN mostly false-positive; 42 F07/F08 WARN) | A (S01/S04 n/a) | — | B (1 T01 WARN: skill-crafting) | C |
| Commands | 43 | A (C04 clean) | A (N/A — no naming check) | A | A (S04 clean) | — | — | A |
| Rules | 24 | — | A (N03 clean) | A | A (S03 clean) | — | — | A |

### Depth Disclosure

**Full reads**: all 17 `agents/*.md` files (frontmatter, descriptions, phase structure, collaboration/boundary sections). All script JSON outputs (27 distinct script executions) read in full.

**Sampled, not exhaustive**: 9/62 skill descriptions read in full (C06); remainder judged via corpus-wide auto checks (T01 line counts, N03 frontmatter, F01/F02 script findings) plus inference from the 9-skill sample. Rules/CLAUDE.md/plugin.json read via targeted grep, never full text. SH03/SH05 spot-checked on 2/9 archived specs; SH06 not evaluated at all this run (no predecessor-spec diff performed — explicitly deferred). EC04/EC05/GL01/GL03/N04-06/F03/F04/T05/T06 judged via representative samples (agents fully read; skills/rules spot-checked), not per-artifact verification across the full 146-artifact corpus.

**Checks not reached**: none — every check in the catalog that had its substrate present was either run via script, ad-hoc bash, or sampled LLM judgment. AC12 was substrate-absent (named precondition: bidirectional convention not populated; `dec-371` records the deliberate non-adoption). SH06 was substrate-present but explicitly not evaluated due to time budget — this is a genuine gap, named here rather than silently passed.

**Tool grant boundaries**: P03/P04 read `.ai-state/observations.jsonl` directly (not Task Chronograph, per the P-dimension preamble — this agent holds no MCP grant for it).

**Phase 3 tool-call accounting (the metric this run was specifically asked to record)**:

- **Total Phase 3 Bash calls: ~47.**
- **Script/family invocations: 7 calls, running 27 distinct script executions** — 3 calls for the 11 family-dispatch-table scripts (`check_staleness_markers`, `check_hook_installation`, `check_state_corpus`, `regenerate_adr_index --check`, `check_topology_conformance`, `check_hackathon_graduation`, `check_sentinel_self_audit`, `check_registry_projection`, `check_path_resolution`, `check_behavioral_contract`, `check_specialist_dispositions`), plus 4 calls for the 16 additional conditional script-backed rows (`check_spec_drift`, `check_spec_archival_gap`, `check_calibration_coverage`, `check_p06_task_brief`, `clean_work_safety`, `check_aac_golden_rule`, `aac_fence_validator` ×2, `check_architecture_projection`, `check_design_checkpoint`, `check_gate_liveness`, `adr_health`, `check_readiness_feedback`, `check_metrics_freshness`, `check_agent_lifecycle_pairing`, `measure_token_budget`, `check_agent_prompt_size`).
- **Improvised/ad-hoc/parsing: ~40 calls**, decomposing into three distinct kinds worth separating rather than treating as one undifferentiated total:
  - **True ad-hoc catalog checks (no backing script exists yet): ~15 calls** — covering C01, C02/S01, C03/S02, C04, C05, N01, N02, N03, S03, S04, T01, T04, AC03, AC06, AC07. These are the genuine residual surface the family-dispatch refactor has not yet reached (12 script-less A-rows spanning C/N/S/T + 3 AC rows).
  - **JSON-output parsing/interpretation of already-produced script results: ~12 calls** — reading, counting, and cross-tabulating the 27 scripts' JSON output (this is overhead the family-dispatch table's own instruction anticipates: "route each finding... read skipped first... reproduce bound verbatim").
  - **Optional-depth investigation/triage beyond the catalog's own requirement: ~13 calls** — false-positive verification on F01 (skill-relative link resolution), N03 (frontmatter key legitimacy against the skill-crafting/rule-crafting schema tables), S04 (argument-hint heuristic correction), and C03/S02 (truncated-frontmatter false positive). This depth was a choice for report quality, not a catalog mandate — a leaner run could have reported these as raw script/grep output at ~15 fewer calls.

**Comparison to BEFORE** (`SENTINEL_REPORT_2026-09-11_17-10-27.md`, ~41 Bash calls in Phase 3, entirely improvised): the family-dispatch refactor's leverage is real but partial. It collapsed **27 checks into 7 calls** that previously required manual derivation — a genuine win. But total Phase 3 call count did not drop proportionally this run, for two reasons: (1) ~15 calls remain genuinely improvised because 12 script-less A-rows were not part of the 33-row extraction, and (2) this run chose ~13 calls of optional false-positive triage the catalog does not require. A future run skipping the optional triage would land Phase 3 at roughly **7 script calls + 15 ad-hoc calls + 12 parsing calls ≈ 34 calls** — a real but modest reduction from 41, concentrated entirely in *what* those calls buy (27 checks via 7 calls vs. needing to hand-derive most of them) rather than in raw call count.


### Findings

#### Critical (blocks correct behavior)

None found.

#### Important (degrades quality or efficiency)

| # | Check | Dimension | Location | Finding | Recommended Action | Owner |
|---|---|---|---|---|---|---|
| 1 | SH01 | Spec Health | `.ai-state/specs/SPEC_diagrams_2026-04-30.md`, `SPEC_multi-language-support_2026-05-11.md` | 2 FAIL + 40 WARN path-column non-conformance (2 dead-link references: `scripts/git-pre-commit-hook.sh`, `commands/onboard-project.md`; remainder are non-path tokens in the path column, e.g. bare `dec-NNN` ids and prose cells with no backtick-quoted path) | Repair the 2 dead links; move non-path tokens (ADR ids, step ids, prose) out of the path column into an adjacent notes column per SH01's own stated convention | doc-engineer |
| 2 | F02 | Freshness | `skills/code-review/references/beauty-review-checklist.md`, `skills/rust-development/references/{rust-patterns,rust-testing,rust}.md`, `skills/test-coverage/references/go.md` | 5 skill-declared reference files named in their `SKILL.md` do not exist on disk | Create the missing reference files or remove the dangling references | context-engineer |
| 3 | DH03 | Decision Health | `.ai-state/decisions/044-coding-style-path-scoping.md`, `050-always-loaded-budget-revision.md` | Both reference deleted `ROADMAP.md` with no auto-detected owning decision; manual investigation found the actual owning decision (`dec-092`, "Retire ROADMAP.md as living document", dated 2026-04-28 — after both) exists but its `affected_files` cites only the destination files, not `ROADMAP.md` itself, so `adr_health.py`'s `removed-by-later` matcher cannot find it | Add `ROADMAP.md` to `dec-092`'s `affected_files` (repairs the matcher for future runs) and/or cross-link `044`/`050` via notes to `dec-092` | user (ADR edit) |

#### Suggested (improves but not urgent)

| # | Check | Dimension | Location | Finding | Recommended Action | Owner |
|---|---|---|---|---|---|---|
| 4 | F08 | Freshness | 33 sections across ~15 skills (e.g. `agentic-interface-design`, `agentic-sdks`, `api-design-craft`, `claude-ecosystem`, `llm-prompt-engineering`) | Staleness markers exceed the 120-day threshold | Run `/refresh-skill` (per `commands/refresh-skill.md`) on the flagged skills, prioritizing model/tooling-reference sections most likely to have drifted | context-engineer |
| 5 | F07 | Freshness | 9 sections across `experiment-tracking`, `llm-prompt-engineering`, `llm-training-eval`, `neo-cloud-abstraction`, `rust-development` | Cataloged staleness-sensitive sections have no `<!-- last-verified: -->` marker yet | Expected cold-start state per `staleness-policy.md` — add markers on next content touch, not urgent | context-engineer |
| 6 | EC02 | Ecosystem Coherence | 30 skills (e.g. `adapt-claude-to-agents`, `agent-failure-taxonomy`, `agent-readiness`, `applied-statistics`, `likec4-querying`, `llm-prompt-engineering`, `neo-cloud-abstraction`) | Never referenced by any agent/CLAUDE.md/rule — but see Ecosystem Coherence "Cross-dimension anomalies" for the auto-discovery-by-design caveat | Confirm which are genuinely dormant vs. intentionally description-discoverable; only the former need action | context-engineer |
| 7 | F01 | Freshness | 39 path references across ~20 skills/agents/commands/rules | Repo-root-only path resolver flags skill-relative markdown links as broken (known false-positive class); 1 item (`.claude-plugin/marketplace.json`, referenced from `skills/versioning/SKILL.md` and `rules/swe/plugin-install-conventions.md`) not resolved by triage — plausibly an external marketplace-repo path | Confirm whether `marketplace.json` lives outside this repo; no action needed on the skill-relative false positives | context-engineer |
| 8 | DH02 | Decision Health | 8 finalized ADRs (e.g. `022-coordination-detail-extraction.md`, `043-behavioral-contract-layer.md`) | `affected_files` cite `claude/config/CLAUDE.md`, renamed to `claude/config/CLAUDE.md.tmpl` | Mechanical path update per `adr_health.py`'s `update-path` disposition | user (ADR edit) |
| 9 | T01 | Token Efficiency | `skills/skill-crafting/SKILL.md` | 418 lines, past the 400-line warn threshold | Consider extracting detail to `references/` | context-engineer |
| 10 | T03 | Token Efficiency | `agents/implementation-planner.md` (436 lines), `agents/sentinel.md` (674 lines, derived ceiling), `agents/systems-architect.md` (444 lines) | Past warn line-count ceiling, no FAIL | No action required unless growth continues | context-engineer |
| 11 | TD06 | Technical Debt | `.ai-state/metrics_reports/METRICS_REPORT_2026-09-07_01-45-44.json` | Report `stale` — 5 days old but 167 commits since; 2 hotspot paths (`scripts/adr_health.py`, `scripts/reconcile_pipeline_state.py`) barred from TD01 filing without hand re-verification | Run `/project-metrics` to refresh | user |
| 12 | AC14 | Architecture Completeness | `.ai-state/DESIGN.md` checkpoint `dec-375` | 3 architecture-bearing ADRs (`dec-376`, `dec-377`, `dec-379`) un-folded since checkpoint — advisory only, expected steady state between architect visits | No action required unless the systems-architect has not visited in a long interval | systems-architect |
| 13 | (CA02-adjacent) | Calibration Accuracy | `.ai-state/calibration_log.md` | `enum_compliance`: 3 of the last 22 enum-bearing rows missing the required Retrospective enum prefix (`correct`/`over-calibrated`/`under-calibrated`) | Backfill the enum prefix on the 3 flagged rows | orchestrator |


### Pipeline Discipline

**P03**: PASS — `check_agent_lifecycle_pairing.py` examined 1,809 WAL records across 2 sessions (the in-flight session `1a8720ae-72a5-4222-8cb6-cc28d88a7993` correctly excluded); 0 unpaired `agent_start`/`agent_stop` records, 0 incidents, 0 pre-baseline, 0 no-tool-use spawns, 0 unmatched stops (`unobserved-start`/`unobserved-agent`/`unattested` all zero), 0 producer/log disagreement.

**P04** (LLM judgment, sampled from the same WAL): grouped 1,340 `tool_use` records by `agent_type` across 6 named subagent types seen in the log (`praxion:researcher`, `praxion:systems-architect`, `praxion:sentinel`, `praxion:implementation-planner`, `praxion:implementer`, `praxion:verifier`) plus `main` (orchestrator, not subject to a `tools:` grant). Cross-checked each subagent's exercised `tool_name` set against its frontmatter `tools:`/`disallowedTools:` grant. **0 violations** — every agent's tool usage stayed within its declared surface. Boundary stated per the check's own discipline: these records name tools, not intent — a subagent making a design decision inside a file it was legitimately assigned would be indistinguishable from correct work in this data.

**P05-P08** (from `.ai-work/`): 1 active slug (`process-economy-p2-residual`, this pipeline). P06: 0 rows (no Standard/Full slug missing `TASK_BRIEF.md`). P07: 0 undisposed Architecture Challenges. P08: `stale_safe: 0` (the 1 slug is classified `BLOCK` — active pipeline with unchecked `WIP.md` steps — not stale).

### Tech-Debt Findings

**No new `td-NNN` rows filed this run.** Rationale: `TD06` (`check_metrics_freshness.py`) reported the latest metrics report `stale` (5 days old, 167 commits since) and named 2 `hotspots_touched` paths (`scripts/adr_health.py`, `scripts/reconcile_pipeline_state.py`) explicitly barred from TD01 filing without hand re-verification. Per the LLM-judgment gating requirement ("a numeric threshold breach is necessary but not sufficient"), filing TD01/TD02/TD03/TD04 rows from a stale, non-fresh coverage/complexity dataset without independent re-verification against current source would risk exactly the golden bad-case TD06 exists to prevent. No fresh `METRICS_REPORT_*.md` beyond the 2026-09-07 one was available this run. **TD05** (ledger status-update discipline): ran clean — 0 `resolved` rows in `TECH_DEBT_RESOLVED.md` missing `resolved-by` (159 resolved + 5 wontfix, all checked), 0 `in-flight` rows in `TECH_DEBT_LEDGER.md` older than 30 days, 0 `unassigned` owner-role rows older than 7 days (36 open rows checked).

Forward-pointer: see `.ai-state/TECH_DEBT_LEDGER.md` for the canonical entries (unchanged by this run — the sentinel wrote 0 rows).

### Recommended Actions (prioritized)

1. **(Important #3, DH03)** — Add `ROADMAP.md` to `dec-092`'s `affected_files` and/or cross-link ADRs `044`/`050` in their notes. *Owner: user.*
2. **(Important #1, SH01)** — Fix the 2 dead path-column links in `SPEC_diagrams_2026-04-30.md`; relocate non-path tokens out of the path column across both flagged specs. *Owner: doc-engineer.*
3. **(Important #2, F02)** — Create or de-reference the 5 missing skill reference files (`code-review`, `rust-development` ×3, `test-coverage`). *Owner: context-engineer.*
4. **(Suggested #11, TD06)** — Run `/project-metrics` to refresh the stale metrics report before the next sentinel TD-dimension pass. *Owner: user.*
5. **(Suggested #6, EC02)** — Confirm intent for the 30 unreferenced skills (dormant vs. description-discoverable-by-design). *Owner: context-engineer.*
6. **(Suggested #4/#7/#8/#13)** — Batch the remaining low-priority hygiene items (staleness-marker refresh, `.claude-plugin/marketplace.json` path confirmation, 8 renamed-path ADR fixes, 3 calibration-log enum backfills) at the next convenient context-engineer pass.
7. **Not this run** — SH06 (spec-delta-vs-evolution) was not evaluated; consider covering it in the next sentinel pass or a dedicated spec-health audit.
