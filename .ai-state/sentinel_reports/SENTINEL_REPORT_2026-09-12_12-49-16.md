# Sentinel Report

**Run timestamp:** 2026-09-12_12-49-16
**Scope:** Full ecosystem sweep — all dimensions in the Check Catalog, all artifact families.
**Worktree:** `.claude/worktrees/process-economy-p2-wrap` (HEAD e9b45645 at Phase-3 start; HEAD advanced to `f247664b` during this run by a concurrent light-review/orchestrator commit — see Depth Disclosure) — third measurement in the Phase-3 tool-call-shape series (BEFORE `2026-09-11_17-10-27`, AFTER-extraction `2026-09-12_02-10-28`, THIS RUN uses `scripts/run_check_families.py --table` per the current catalog).

## Ecosystem Health: B

### Summary

The ecosystem is healthy and continues to improve mechanically: the aggregator (`scripts/run_check_families.py --table`) now covers 55 of the catalog's auto checks across 14 family scripts in a single call, and every standalone auto check (T02, SH07/SH08, CA03, P06/P08, EC07, AC10/AC13/AC14, GL02/04/05, DH01/02/04/05/06, RD01, TD06, P03, DL06, F11) ran clean or produced small, well-scoped findings. Zero Critical findings. Two Important findings stand: **CA02** (under-calibrated share in the last 20 enum-bearing calibration rows is 30%, above the 25% ceiling) and **TD06** (the metrics report is 5 days old but 197 commits behind HEAD — "stale" by commit-distance, not just calendar age — barring 4 hotspot candidates from TD01 filing this run). A third finding, **DL02** (malformed YAML frontmatter in a draft ADR breaking `adr_health.py`/`check_adr_reciprocity.py`/`check_design_checkpoint.py`), was FAIL when Phase 3 captured it but was resolved by a concurrent commit (`f247664b`, 1-line fix) before this report was finalized — it is reported as **resolved-during-audit**, not counted in the Important tally. Everything else is Suggested-tier mechanical drift (stale `DECISIONS_INDEX.md`, stale `doc_manifest.yaml`, two dead paths in an April-archived spec, illustrative brace-expansion paths in `DESIGN.md`'s Key Files column, small helper duplication across three newly-extracted family scripts). No new tech-debt rows were filed — every hotspot/coverage/complexity candidate this run either failed TD06's re-verification bar or already carries an existing ledger/resolved row (spot-checked; see Tech-Debt Findings).

Compared to the last run (`2026-09-12_02-10-28`, grade B, 0/3/10): this run holds B with 0/2/10 (net Important count down by 1, mechanically explained by DL02's mid-audit fix rather than by a deeper sweep — flagged per the constraint against a "numerically better grade earned by looking at less"; this run's Pass 2 depth is comparable to or greater than the prior run's, see Depth Disclosure).

### Ecosystem Coherence: A

**System-level EC checks:**
| Check | Result | Evidence |
|-------|--------|----------|
| EC01 | PASS | `check_registry_projection.py` (`X01 X02 X05 X06 EC01 EC02` family) — `diagram_names=16`, 0 findings |
| EC02 | PASS | same family — `skills=62, commands=43, rules=23`, 0 findings (orphan-artifact check clean) |
| EC04 | PASS (sampled) | Handoff docs sampled in `.ai-work/process-economy-p2-wrap/` (HANDOFF_PHASE2_PHASE3.md, LEARNINGS.md, VERIFICATION_REPORT.md, LIGHT_REVIEW_wrap.md) all have a stated producer and consumer; no dead ends found in the sample |
| EC05 | PASS (sampled) | No obvious missing artifact type against CLAUDE.md's stated purpose; idea ledger's "Implemented" section is empty (no open cross-refs to check under X04) |
| EC07 | PASS | `check_aac_golden_rule.py --mode=audit --json` — 0 findings over the last-10-commit horizon |

**Cross-dimension anomalies:**

None rising to system-level concern. The one recurring pattern worth naming: `AC07`'s 30 WARNs and `F01`'s 39 WARNs both trace substantially to the same root cause — `.ai-state/DESIGN.md`'s "Key Files (illustrative)" columns use shell brace-expansion notation (e.g. `agents/{verifier,sentinel,...}.md`, `src/app/{globals,tokens,...}.css`) that a literal path-resolver correctly reports as non-existent, even though the column is explicitly labelled illustrative and each individual expansion does resolve. This is a check-vs-convention friction point, not drift — recommend either expanding brace lists to real per-file entries or teaching the two path-resolution scripts to skip brace-expansion tokens, so future runs don't have to re-triage the same ~30-40 WARNs as false positives.

**Artifact coherence distribution** (from the Scorecard below): 14 agents graded A (all-PASS), 3 agents graded B (T03 line-count WARN only: `implementation-planner` 436 lines, `sentinel` 677 lines vs its derived 652/752 ceiling, `systems-architect` 444 lines). Skills/commands/rules assessed corpus-wide via the auto-check families (C01-05, N01-03, S01-04, F01/F02, T01/T04 all ran against the full 62/43/23 corpus) plus a ~9-skill LLM-judgment sample; no per-artifact FAIL surfaced in any auto check.

**Synthesis:**

The machinery that keeps the fleet coordinated is fully clean: every cross-reference integrity check (X01-X09), the behavioral-contract wiring (BC01/03/04), the pipeline diagram (EC01), the AaC golden rule (EC07), gate liveness (GL02/04/05, sampled GL01/GL03), and the ADR reciprocity/health checks (DL06, DH01/04/05/06) all passed. The friction is entirely at the edges: two mechanically-stale generated artifacts (`DECISIONS_INDEX.md`, `doc_manifest.yaml`) that a one-command regen clears, one now-fixed malformed-YAML draft, an illustrative-path convention that trips two literal path resolvers, and a calibration-log signal (CA02) worth a human look at whether recent under-calibration reflects genuine over-caution or a labeling drift.

### Ecosystem Metrics

| Metric | Value | Trend (vs `2026-09-12_02-10-28`) |
|--------|-------|-------|
| Skills | 62 | — |
| Agents | 17 | — |
| Rules | 23 (+ README) | — |
| Commands | 43 | — |
| Hooks | 52 | — |
| ADRs (finalized / draft) | 381 / 1 | +1 finalized |
| Specs archived | 9 | — |
| Metrics reports | 10 | — |
| Token budget (governed, tokenizer basis) | 16,730 / 25,000 (headroom 8,270) | — |
| Token budget listing (advisory) | 9,243 / 126 files | — |
| Critical findings | 0 | — |
| Important findings | 2 (CA02, TD06) | -1 |
| Suggested findings | 10 | — |
| Ecosystem Health Grade | B | stable |
| Ecosystem Coherence Grade | A | stable |
| **Phase 3 tool-call count (this run)** | **20** | **-27 vs AFTER's ~47** |
| **— aggregator calls** | **1** (`run_check_families.py --table`, 55 checks/14 families in one call) | same mechanism as AFTER |
| **— drill-down into one family's `--json`** | **1** (`check_state_corpus.py --json`, to split SH01's 2 FAIL from its 40 WARN) | new-lowest |
| **— other Phase-3 calls** (standalone auto scripts not in the family table: T02, SH07, SH08, CA03, P06, P08, EC07, AC14, GL, DH, RD01, TD06, P03, DL06, F11, AC10×2 — plus existence/setup verification and output inspection) | **18** | see table below |

**Three-run Phase-3 comparison:**

| Run | Total tool uses | Phase 3 calls | Phase 3 shape |
|---|---|---|---|
| BEFORE (`2026-09-11_17-10-27`) | 67 | ≈41 | Every `A` row improvised or run one script at a time; no aggregator existed |
| AFTER-extraction (`2026-09-12_02-10-28`) | 105 | ≈47 | 7 script calls (27 distinct executions) + ~12 JSON-parsing calls + ~13 triage calls; aggregator not yet mandated as the single evidence source |
| THIS RUN (`2026-09-12_12-49-16`) | ~78 | 20 | 1 aggregator call (55 checks) + 1 family-`--json` drill-down + 18 other calls (standalone scripts outside the family table, each run once, output parsed inline where feasible) |

Phase 3's shape kept improving: the aggregator call itself replaced what would otherwise be 14 separate script invocations (its own footer: "calls saved: 14 → 1"), and the family-dispatch preamble's "use the digest as evidence" instruction meant only one genuine drill-down was needed all run (to split SH01's FAIL/WARN mix, which the digest's 5-sample rule doesn't disambiguate by severity). The 18 "other" calls are almost entirely the ~16 auto checks the family-dispatch table does not yet cover (T02, SH-suite, CA03, P06/P08, EC07, AC10/AC14, GL, DH, RD01, TD06, P03, DL06, F11) — each still requires its own invocation because no second aggregator wraps them. That is the next mechanical lever: a second family-dispatch tier for these 16 checks would likely take Phase 3 from 20 calls to under 10.

### Scorecard (Agents — full 17)

| Artifact | Type | C | N | F | S | X | T | Coherence (EC) | Overall |
|----------|------|---|---|---|---|---|---|---|---|
| agentic-transactions-architect | Agent | A | A | A | A | A | A | A | A |
| architect-validator | Agent | A | A | A | A | A | A | A | A |
| cicd-engineer | Agent | A | A | A | A | A | A | A | A |
| context-engineer | Agent | A | A | A | A | A | A | A | A |
| discipline-consultant | Agent | A | A | A | A | A | A | A | A |
| doc-engineer | Agent | A | A | A | A | A | A | A | A |
| implementation-planner | Agent | A | A | A | A | A | B (T03, 436 lines) | A | B |
| implementer | Agent | A | A | A | A | A | A | A | A |
| interface-designer | Agent | A | A | A | A | A | A | A | A |
| promethean | Agent | A | A | A | A | A | A | A | A |
| researcher | Agent | A | A | A | A | A | A | A | A |
| roadmap-cartographer | Agent | A | A | A | A | A | A | A | A |
| sentinel | Agent | A | A | A | A | A | B (T03, 677/752 derived ceiling) | A | B |
| skill-genesis | Agent | A | A | A | A | A | A | A | A |
| systems-architect | Agent | A | A | A | A | A | B (T03, 444 lines) | A | B |
| test-engineer | Agent | A | A | A | A | A | A | A | A |
| verifier | Agent | A | A | A | A | A | A | A | A |

**Skills / Commands / Rules (corpus-wide, not per-artifact):** C01-05/N01-03/S01-04/T01/T04 ran against all 62 skills, 43 commands, 23 rules with 0 FAIL. Notable WARNs: `skill-crafting/SKILL.md` (T01, 418 lines, at warn ceiling), `agentic-sdks` skill dir (N01, reads as a crafting-style skill but does not end `-crafting` — likely a false-positive heuristic, not a real naming violation), `skills/test-coverage` (F02, references `references/go.md` which does not exist under that skill).

### Depth Disclosure

**Pass 1 (auto):** Read in full via script — every `.md` skill/agent/rule/command frontmatter (62+17+23+43 files) via `check_artifact_conformance.py`; every cross-reference surface via `check_registry_projection.py` and `check_path_resolution.py`; every ADR (381 finalized + 1 draft) via `check_state_corpus.py`, `regenerate_adr_index.py --check`, `adr_health.py`, and `check_adr_reciprocity.py`; both architecture documents (`.ai-state/DESIGN.md`, `docs/architecture.md`) via `check_architecture_projection.py` and `aac_fence_validator.py`; the full `.ai-state/observations.jsonl` WAL (432 records, 2 sessions) via `check_agent_lifecycle_pairing.py`; the full `.ai-state/calibration_log.md` (108 rows) via `check_calibration_coverage.py`. All 22 dimensions in the catalog were exercised except where substrate-gated (see skips below). No check was marked `[not reached]` — the full catalog ran within budget, though at higher tool-call cost than the target shape (see Ecosystem Metrics).

**Pass 2 (LLM judgment) — sampled, not exhaustive:** Full frontmatter + stage-heading + collaboration-section read on all 17 agents (Batch 2, complete). Skills sampled: line-count/references-dir scan across all 62 dirs (structural, cheap), full description read on 5 skills flagged by F07/F08 plus 2 more for pattern-check (Batch 1, ~7/62 skills read in depth). Rules: S03 heading check ran mechanically across all 23; BC02 order-check read both CLAUDE.md anchors and the rule file directly (all pass). Specs: SH01/SH02/SH07/SH08 ran mechanically across all 9; SH03/SH05/SH06 LLM-judged on 5/9 specs (newest `SPEC_sidecar-placement`, `SPEC_diagrams`, `SPEC_multi-language-support`, `SPEC_p5-issue-autofix`, `SPEC_production-gate-cohort` — no red flags in the sample, "Decisions Made" sections substantive in every spec checked). P04 computed by grouping all `tool_use` WAL records by `agent_id` (6 agents, exhaustive over available records) against each agent's declared `tools:` grant; P05 read all 6 `.ai-work/process-economy-p2-wrap/` documents in full. AC02/AC08/AC09 read `.ai-state/DESIGN.md` §3/§5 and `docs/architecture.md` §3 in full (both are single documents, not sampled) — AC09's coverage direction was checked by extracting both §3a Built-component name lists programmatically and diffing (16/16 match both ways). GL01/GL03 sampled (grep-based, ~10 candidate consumer/producer pairs checked, no orphans found in the sample — not exhaustive over every "harvest"/"digest" verb in the corpus). CH01 sampled 4 of the newly-extracted `check_*.py` family scripts for structural duplication (confirmed byte-identical `_read_text()` in 3 of them). TD01-04 read the full metrics-report JSON (`hotspots.top_n`, `pydeps.data.aggregate`, `coverage.data.per_file`, `lizard.data.files`) exhaustively but applied judgment via ledger-grep sampling (6-12 candidate paths each checked against `TECH_DEBT_LEDGER.md`/`TECH_DEBT_RESOLVED.md` for existing coverage, not a full corpus dedup pass).

**Skipped with substrate/reader reason named:**
- **AC12** (traceability orphans) — skipped: `.c4` model present (2 files) but no `req_ids` metadata anywhere in it and no `architectural_elements:` frontmatter in any of the 9 archived specs — the bidirectional convention is not yet populated (not "reader unreachable," not "substrate absent" — the specific failed precondition is convention-not-populated).
- **PR01** (pre-refactor plan integrity) — skipped: no `PRE_REFACTOR_PLAN.md` under `.ai-work/` anywhere (substrate absent; this is the common case per the check's own preamble).
- **TT04** (per-group runtime vs envelope) — skipped: 10 metrics reports exist (≥7 threshold met) but none carries per-group test-topology runtime data — `expected_runtime_envelope` is declared in `.ai-state/TEST_TOPOLOGY.md` but no `METRICS_REPORT_*.json` yet populates the matching per-group field (substrate carries no history for this specific field, distinct from the report being absent).
- **P03/P04** substrate present and used (`.ai-state/observations.jsonl`, 432 records / 2 sessions, excluding the in-flight session per the script's own exclusion) — not a skip, named here only to distinguish from AC12/PR01/TT04 above per the "name which of three states" requirement.

**Consistency note on DL02 (methodological, not a finding):** the malformed-YAML draft ADR that Phase 3's `check_state_corpus.py` flagged as FAIL was captured against HEAD `e9b45645`. A concurrent commit (`f247664b`, landed mid-audit, 1-line diff) fixed exactly that field before this report was written. Re-verified directly: the frontmatter now parses cleanly, and the one dependent pointer it had made look orphaned (`dec-380`'s `superseded_in_part_by: dec-draft-239ddd67`) now resolves for real, not just in principle. This is reported as **resolved-during-audit** rather than as an open Important finding — the alternative (reporting a defect already fixed by the time a human reads this report) would be a false alarm, and silently dropping it without explanation would look like the finding was missed rather than overtaken by events. This is the cost of auditing a live worktree with concurrent writers; a fully static snapshot would not have this ambiguity.

**No coverage was narrowed for turn budget** — Phase 3 ran to completion within budget, and Pass 2's sampling depth is comparable to (not narrower than) the prior run's stated ~9/62 skills and full 17-agent read.

### Findings

#### Critical (blocks correct behavior)
None.

#### Important (degrades quality or efficiency)
| # | Check | Dimension | Location | Finding | Recommended Action | Owner |
|---|-------|-----------|----------|---------|---------------------|-------|
| 1 | CA02 | Calibration Accuracy | `.ai-state/calibration_log.md` (last 20 enum-bearing rows) | Under-calibrated share = 6/20 (30%), exceeding the 25% ceiling; over-calibrated = 1/20 (5%, fine); 2 rows carry a non-canonical `well-calibrated:`/`well-calibrated.` prefix instead of the sanctioned `correct`/`over-calibrated`/`under-calibrated` enum | Human review: is recent tier selection genuinely under-scoped, or is the Retrospective enum being applied inconsistently (the 2 non-canonical prefixes suggest the latter, at least partially)? Normalize the 2 non-canonical rows to the sanctioned enum going forward | user |
| 2 | TD06 | Technical Debt | `.ai-state/metrics_reports/METRICS_REPORT_2026-09-07_01-45-44.json` | `status: stale` — report is only 5 days old by calendar but 197 commits behind current HEAD; 4 hotspot candidates (`scripts/finalize_adrs.py`, `scripts/adr_health.py`, `scripts/test_finalize_adrs.py`, `scripts/reconcile_pipeline_state.py`) barred from TD01 filing this run pending re-verification | Run `/project-metrics` to refresh before the next TD01-04 pass; the 197-commit drift is well past the point where hotspot ranks are trustworthy | user |

#### Suggested (improves but not urgent)
| # | Check | Dimension | Location | Finding | Recommended Action | Owner |
|---|-------|-----------|----------|---------|---------------------|-------|
| 1 | DL02 (resolved during audit) | Decision Log | `.ai-state/decisions/drafts/20260912-1915-...-family-row-form.md` | FAIL at Phase-3 capture time (unquoted `summary:` scalar containing a literal `: ` broke YAML parsing, cascading into `adr_health.py`/`check_adr_reciprocity.py`/`check_design_checkpoint.py` withheld/unparseable counts); fixed by commit `f247664b` before report completion | None — already fixed; noted for the record | — |
| 2 | SH01 | Spec Health | `.ai-state/specs/SPEC_diagrams_2026-04-30.md` | 2 dead path-column references (`scripts/git-pre-commit-hook.sh`, `commands/onboard-project.md`) — both removed by later decisions (`dec-096`, `dec-132` per `DH02`'s `removed-by-self` findings); archived-spec record, no current-behavior impact | No action required — archival record; optionally annotate the spec with a post-archive note if the pattern recurs | context-engineer |
| 3 | DL03 | Decision Log | `.ai-state/decisions/DECISIONS_INDEX.md` | Stale vs 381-entry source of truth | Run `scripts/regenerate_adr_index.py` (normally automatic at finalize) | user |
| 4 | F11 | Freshness | `.ai-state/doc_manifest.yaml` | `generated_at` predates commit `47169b394` which touched a `docs/`/`.ai-state/` surface | Run `python3 scripts/build_doc_manifest.py` | context-engineer |
| 5 | AC07 / F01 | Freshness / Arch. Completeness | `.ai-state/DESIGN.md` §3a "Key Files (illustrative)" column, various skill `SKILL.md` files | 30 + 39 non-resolving path tokens; a majority in DESIGN.md trace to shell brace-expansion notation (`agents/{a,b,c}.md`) the resolvers correctly can't expand; the skill-doc F01 hits are mostly small (missing example paths, one real gap: `skills/test-coverage/references/go.md` referenced but absent) | Either expand brace-lists to literal per-file entries or teach `check_architecture_projection.py`/`check_path_resolution.py` to skip brace-expansion tokens; create the missing `go.md` reference or remove the dangling citation | context-engineer |
| 6 | N01 | Consistency | `skills/agentic-sdks` | Naming heuristic flags this as reading like a crafting skill (does not end `-crafting`) | Likely a heuristic false-positive (the skill is about building agents, not "crafting" a Praxion artifact type) — confirm intent, no rename expected | context-engineer |
| 7 | T01 / T03 | Token Efficiency | `skills/skill-crafting/SKILL.md` (418 ln), `agents/implementation-planner.md` (436 ln), `agents/sentinel.md` (677 ln), `agents/systems-architect.md` (444 ln) | 4 files at/past their warn ceiling (none past fail) | Routine size management at next touch; not urgent | context-engineer |
| 8 | DH02 | Decision Health | 8 ADRs (e.g. `022`, `043`, `049`, `097`) citing `claude/config/CLAUDE.md`, since renamed to `claude/config/CLAUDE.md.tmpl` | `affected_files` path decay, disposition `update-path` | Mechanical batch fix at next ADR-corpus touch | context-engineer |
| 9 | CH01 | Code Health | `scripts/check_registry_projection.py`, `scripts/check_path_resolution.py`, `scripts/check_artifact_conformance.py` | Byte-identical `_read_text(path)` helper duplicated verbatim across 3 newly-extracted family-dispatch scripts (and a near-duplicate `_heading_span` in 2 of them) | Lift to a shared `scripts/_sentinel_common.py` before a 4th family script duplicates it again | context-engineer |
| 10 | F07/F08 | Freshness | 9 sections with no `<!-- last-verified: -->` marker (cold-start, expected — `staleness_sensitive_sections` recently backfilled), 33 sections with markers >120d old (worst: `agentic-sdks` at 134d) | Per staleness-policy cold-start semantics, WARN-only and expected to shrink over subsequent runs; the 33 aged markers are routine refresh candidates | context-engineer |

### Pipeline Discipline

**P03** (agent lifecycle pairing): 432 records examined across 2 sessions (the in-flight session excluded per the script's own convention). 0 findings — no agent that did work went unstopped. `incidents=0`, `pre_baseline=0`, `no_tool_use=0`; `unmatched_stops` all empty (`unobserved-start=0`, `unobserved-agent=0`, `unattested=0`); `producer_log_disagreement=0`. Per-session breakdown not separately itemized since the WAL spans exactly 2 sessions with 0 anomalies in either.

**P04** (surface discipline, sampled from the same WAL): 6 distinct `agent_id`s with `tool_use` records — 3 `praxion:implementer` spawns (Bash/Read/Edit/Write only, within grant), 1 `praxion:verifier` spawn (Bash only, within grant — `disallowedTools: Edit` never exercised), 1 `praxion:sentinel` spawn from the prior run (Bash only), 1 `main` orchestrator (Bash/Agent). No tool outside any agent's declared grant. **Bound stated explicitly**: these records name tools and file paths, not intent — a spot-check for `Write`/`Edit` records touching `.ai-state/decisions/` or `.ai-state/TECH_DEBT_LEDGER.md` (surfaces the implementer should not own) found none, but an implementer legitimately assigned to edit inside one of those files via a plan step would look identical to a violation in these records; this check cannot distinguish the two.

**P05** (handoff-doc substance): all 6 `.ai-work/process-economy-p2-wrap/` documents (`HANDOFF_PHASE2_PHASE3.md`, `LEARNINGS.md`, `VERIFICATION_REPORT.md`, `LIGHT_REVIEW_wrap.md`, `COST_LEDGER.md`, `COST_LEDGER_WRAP.md`) have substantive content under every heading — no hollow-heading pattern found.

**P06/P07/P08**: P06 — 0 rows (no Standard/Full slug missing `TASK_BRIEF.md`; this slug has no `SYSTEMS_PLAN.md` so the check doesn't apply to it). P07 — 0 findings (`slugs=1`, no `INTERFACE_DESIGN.md`/`TRANSACTIONS_DESIGN.md`/`CONSULT_*.md` present in this slug). P08 — `stale_safe=0` (below the 3-directory advisory threshold; the one task dir present, this slug itself, is `age_days=0` and classified `WARN` for unmerged `LEARNINGS.md`/`VERIFICATION_REPORT.md`, which is expected mid-pipeline state, not staleness).

### Tech-Debt Findings

**0 new `td-NNN` rows filed this run.** Reasoning, per check:

- **TD01** (hotspots): top-10 hotspot list has 4 candidates barred by TD06's `hotspots_touched` (finalize_adrs.py, adr_health.py, test_finalize_adrs.py, reconcile_pipeline_state.py — each modified 1-5 times since the stale report). Of the remaining 6, all 6 (`fitness/tests/test_discipline_registry_invariants.py`, `scripts/_sidecar_mount.py`, `scripts/project_metrics/_report_deep_dive.py`, `scripts/install_git_hooks.py`, `fitness/tests/test_consult_append_only.py`, `scripts/test_praxion_sidecar.py`) were checked against the ledger by grep; 5 of 6 already carry ≥1 existing `TECH_DEBT_LEDGER.md`/`TECH_DEBT_RESOLVED.md` mention. The 6th (`scripts/test_praxion_sidecar.py`, rank 9, high churn/low complexity) was judged not debt-worthy — a test file under active development churning with the code it covers is expected, not a hotspot.
- **TD02** (cyclic SCCs): `pydeps.data.aggregate.cyclic_sccs = []` — 0 non-trivial cycles across 342 of 426 tracked Python files (80.3% coverage) spanning 6 package roots (`eval`, `fitness`, `scripts`, `tests`, `eval/src/praxion_evals`, `task-chronograph-mcp/src/task_chronograph_mcp`). No rows warranted.
- **TD03** (coverage floor): the rendered report's "Lowest-covered files" list bottoms out at 67.05%, below the 70% project floor, so per the check's own instruction the JSON `coverage.per_file` sibling was read for the remainder — 12 files total below 70% (worst: `codex/config/export-codex-agents.py` at 28.57%). All 12 already carry ≥1 existing ledger/resolved mention (spot-checked by grep). No new rows filed; coverage artifact itself is `status: stale` per the same 2026-09-07 report.
- **TD04** (complexity p95): project CCN p95 = 7.0. Top-10 by max function CCN (`dashboard_app/src/components/eval-leaderboard.tsx` at 30 down to `codex/config/manage-codex-mcp.py` at 20) all already carry ≥1 existing ledger/resolved mention (spot-checked). No new rows filed.
- **TD05** (ledger discipline): not audited in depth this run given the above — deferred to next pass.

Since no rows were filed, `scripts/check_state_ledgers.py` was not run (task instruction was conditional on filing).

### Recommended Actions (prioritized)

1. **[Important, user]** Review the 30% under-calibrated share in the last 20 calibration rows (CA02) — decide whether it reflects genuine over-conservative tiering or an enum-labeling drift (2 rows used non-canonical prefixes).
2. **[Important, user]** Run `/project-metrics` to refresh the 197-commit-stale metrics report (TD06) before the next TD01-04 pass.
3. **[Suggested, context-engineer]** Regenerate `DECISIONS_INDEX.md` (DL03) and `doc_manifest.yaml` (F11) — both one-command fixes.
4. **[Suggested, context-engineer]** Resolve the brace-expansion path-resolution friction in `DESIGN.md` §3a (AC07/F01) so future sentinel runs don't re-triage the same ~30-40 illustrative-path WARNs.
5. **[Suggested, context-engineer]** Lift the duplicated `_read_text`/`_heading_span` helpers (CH01) out of the 3 family-dispatch scripts before a 4th script repeats them.
6. **[Suggested, context-engineer]** Create `skills/test-coverage/references/go.md` or remove the dangling reference to it (F02).
7. **[Process lever, no owner assigned]** Consider a second family-dispatch tier wrapping the ~16 standalone auto checks (T02, SH-suite, CA03, P06/P08, EC07, AC10/AC14, GL, DH, RD01, TD06, P03, DL06, F11) — would likely cut Phase 3 from 20 calls toward single digits, continuing the trend BEFORE (41) → AFTER (47, but 7 of those were the new aggregator's family calls) → THIS RUN (20).
