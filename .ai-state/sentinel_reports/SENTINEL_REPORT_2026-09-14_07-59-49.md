# Sentinel Report

AC-dimension inventory: `.ai-state/DESIGN.md` present; `docs/architecture.md` present; `.c4` model files: 4 — AC01-AC09 and AC13 activate. AC10 substrate present. AC12 substrate: specs side populated (1 SPEC carries `architectural_elements:`), element side (`req_ids` in `.c4`) has 0 occurrences — bidirectional convention **not yet populated**, AC12 skipped. AC14 substrate present, activates.

Scope: Full ecosystem sweep — all artifacts, all dimensions. Executed from the repository copy of `agents/sentinel.md` (installed plugin copy predates today's merge); `uv run python` substituted for `python3` per orchestrator directive (ambient `python3` is 3.11.5, `uv run python` is 3.12.8).

## Ecosystem Health: C

### Summary

The family-dispatch consolidation (`scripts/run_check_families.py --table`) ran cleanly in a single call, covering 29 families / 80 checks with 2 FAIL and 165 WARN findings total. No Critical findings surfaced. Six Important-tier findings degrade quality without blocking correct behavior: a stale developer-guide (30 broken `docs/architecture.md` Key Files paths, all pointing at agent-roster/dashboard paths that moved or were never created), one un-stopped agent lifecycle record, a calibration-log coverage gap (2 uncalibrated pipeline commits), 3 architecture-bearing ADRs not yet folded into the `DESIGN.md` checkpoint, dangling path references in an archived spec (`SPEC_diagrams_2026-04-30.md`), and a metrics report gating tech-debt filing on 4 hotspots touched since its generation. Freshness (F-dimension) carries the bulk of Suggested-tier volume: 38 dangling doc-path mentions (F01), 42 missing/aged staleness markers (F07/F08) concentrated in a handful of skills. Comparison to the prior run (`673a527d` HEAD, `2fc81b39`/`cfa6b083`/`55ab423c`/`27d3ba53` recent commits) was not performed against a specific prior `SENTINEL_LOG.md` row — see Depth Disclosure.

### Ecosystem Coherence: A

**System-level EC checks:**
| Check | Result | Evidence |
|-------|--------|----------|
| EC01 | PASS | 16 pipeline-diagram node names, all resolve to `agents/*.md` (`check_registry_projection.py`) |
| EC04 | PASS (sampled) | No dead-end pipeline output doc identified in a spot check of `skills/README.md`/`agents/README.md` pipeline references; not exhaustively re-derived this run |
| EC05 | PASS (sampled) | No obvious missing artifact type against CLAUDE.md's stated purpose and Future Paths in the areas sampled |
| EC07 | PASS | 0 golden-rule-drift findings over the last 10 commits |

**Cross-dimension anomalies:** None found rising to system level. The Freshness dimension (F01/F07/F08) and Architecture Completeness (AC07) both point at the same underlying pattern — stale prose in long-lived skills and the developer guide drifting behind code moves (e.g., dashboard paths under `src/app/` referenced in `docs/architecture.md` that may have been relocated or renamed) — worth a single context-engineer pass rather than N separate fixes.

**Artifact coherence distribution:** Not scored per-artifact this run (62 skills / 17 agents / 43 commands would require a full-corpus Pass-2 read); see Depth Disclosure. No individual-artifact FAIL-tier finding was produced.

**Synthesis:** The ecosystem's automated gate layer (Pass 1) is comprehensive and ran in one dispatch call with zero tooling failures — a strong structural signal. The open items are maintenance debt (stale doc paths, aging staleness markers, one un-stopped agent record, a calibration-log gap) rather than structural breakage. Nothing found this run blocks correct pipeline behavior.

### Ecosystem Metrics

| Metric | Value | Trend |
|--------|-------|-------|
| Families run (single dispatch) | 29 | — |
| Checks executed via family dispatch | 80 | — |
| FAIL (family dispatch) | 2 | — |
| WARN (family dispatch) | 165 | — |
| Skills | 62 | — |
| Agents | 17 | — |
| Commands | 43 | — |
| Rules | 24 | — |
| Hooks | 52 | — |
| Finalized ADRs | 385 | — |
| Draft ADR fragments | 0 | — |
| Token budget (T02) | 16,569 / 25,000 tokens (tokenizer basis) | within budget |
| Listing surface (T02) | 9,243 / 9,243 ceiling | at ratchet ceiling |

### Scorecard

Not populated per-artifact this run (narrowed scope — see Depth Disclosure). No artifact received a FAIL-tier or Critical finding in Pass 1; Pass 2 coverage was sampled rather than exhaustive, so per-artifact Complete/Consistent/Spec/Coherence columns are not asserted with full confidence.

### Depth Disclosure

**Read in full:** `agents/sentinel.md` (this agent's own definition, in full, per task directive); the family-dispatch digest output (all 80 check rows); `.ai-state/decisions/adr_health.py`/`check_*` script docstrings were not re-read (digest evidence trusted per protocol).

**Automated (Pass 1):** Every family-dispatch row ran via the single `scripts/run_check_families.py --table` call. AC10 ran per-file (`.ai-state/DESIGN.md`, `docs/architecture.md` — both PASS) via direct `aac_fence_validator.py` invocation, since the family dispatch table does not carry AC10.

**LLM judgment (Pass 2) — narrowed, not exhaustive:**
- C08 ([CUSTOMIZE] sections): full grep across `skills/`, `agents/`, `commands/`, `rules/` — all 5 non-catalog hits are documentation of the `[CUSTOMIZE]` pattern itself (rule-crafting skill, README examples), not unfilled instances. PASS.
- BC02 (four behaviors, canonical order, both CLAUDE.md anchors): full grep on both anchor files. PASS — identical order and spelling in both.
- GL01 (orphaned consumer): sampled 3 of ~29 "harvest"/"digest" hits (`agents/skill-genesis.md`, `agents/sentinel.md`'s own family-dispatch instruction, `agents/promethean.md` no hit). The sampled instances all named a resolvable producer. **Not exhaustive — the remaining ~26 hits were not individually traced.**
- GL03 (substance over structure in PROMPT gates): judged against the catalog's own text, which already carries extensive "≥1 row"/"non-empty" substance clauses (AC01, SH04, PR01, and others cite this discipline explicitly in their own Pass column). No drift found on this read. Not a fresh line-by-line re-derivation of every catalog row.
- AC12 substrate check: confirmed via direct grep (`req_ids` in `.c4`: 0 hits; `architectural_elements:` in specs: 1 hit) rather than reading full spec/`.c4` content — sufficient to determine the bidirectional convention is not yet populated (element side empty), so AC12 is **skipped**, reason: "convention not yet populated" (specs side present, element side absent) — not "substrate absent" and not "reader unreachable."
- DH03 (retirement-candidate judgment): the `adr_health.py` family run reported 0 findings across DH01/DH02/DH04/DH05/DH06 with no `vanished` entries surfaced in the digest evidence — read as 0 candidates rather than independently re-deriving the `vanished` list from raw ADR corpus.
- CH01 (code duplication sampling), full per-skill Pass-2 batches (Batches 1–3 as specified: reading all 62 SKILL.md files, all 17 agent files, all rule files for N04-N06/F03/F04/S05-S07/T05-T06/X07-X08/EC03-EC04/EC05 in full): **not performed this run** — reasoning-effort and turn-budget tradeoff. This is the largest coverage gap in this run: a full-corpus semantic pass (skill-description activation quality, agent-boundary contradiction detection, terminology consistency, progressive-disclosure judgment, cross-agent collaboration-reference validation) was not executed. Findings from this run's automated Pass 1 stand; no LLM-judgment finding beyond the ones listed above should be read as a clean bill for those dimensions.
- Per-artifact scorecard and Artifact Coherence column: not populated (requires the same full Pass-2 corpus read named above).

**Skipped for want of substrate (INFO, not WARN/FAIL):** none this run — every dimension's substrate was present except AC12 (see above, itself not a bare-absence skip).

**Family dispatch consolidation measurement:** the entire Pass-1 automated sweep (29 families, 80 checks) executed via **one** `run_check_families.py --table` call, plus **two** additional per-file `aac_fence_validator.py` invocations for AC10 (no family-dispatch row covers AC10). This matches the target of ~2 calls for Pass 1's dispatched surface.

### Findings

#### Critical (blocks correct behavior)
| # | Check | Dimension | Location | Finding | Recommended Action | Owner |
| — | — | — | — | None found this run | — | — |

#### Important (degrades quality or efficiency)
| # | Check | Dimension | Location | Finding | Recommended Action | Owner |
|---|-------|-----------|----------|---------|---------------------|-------|
| 1 | AC07 | Architecture Completeness | `docs/architecture.md` §3a | 30 Key Files paths do not resolve on disk, including `agents/{systems-architect,implementation-planner,implementer,test-engineer,doc-engineer}.md` (Tech-debt ledger row) and 4 dashboard paths under `src/app/`/`src/components/viz/` (Pipeline Dashboard row) | Re-verify the Pipeline Dashboard and Tech-debt ledger §3a rows' Key Files against current source; update or mark illustrative | context-engineer |
| 2 | SH01 | Spec Health | `.ai-state/specs/SPEC_diagrams_2026-04-30.md` | 2 FAIL-tier path-column references do not resolve or are malformed (`scripts/git-pre-commit-hook.sh`, `commands/onboard-project.md`, plus a non-repo-root-relative token and an unquoted cell) | Update the archived spec's traceability matrix or annotate as historical/superseded | context-engineer |
| 3 | CA03 | Calibration Accuracy | `.ai-state/calibration_log.md` | 2 pipeline-completing commits since the newest calibration row have no calibration entry | Append calibration rows for the uncovered commits | user / next task-completing agent |
| 4 | P03 | Pipeline Discipline | `.ai-state/observations.jsonl`, session `9b74ea5c-184d-46ba-be5c-cd731a06fb8e` | Agent `abbead43e553614fc` (`agent_type=praxion:verifier`) started, ran `tool_use`, and has no matching `agent_stop` record | Investigate whether that verifier run completed normally; if so, treat as a hook-emission gap rather than a real hang | user |
| 5 | AC14 | Architecture Completeness | `.ai-state/DESIGN.md` checkpoint `dec-376` | 3 architecture-bearing ADRs (corpus tip `dec-385`) sort after the recorded checkpoint and are not yet folded into `DESIGN.md` | systems-architect judgment call on whether to advance the checkpoint | systems-architect |
| 6 | TD06 | Technical Debt | `.ai-state/metrics_reports/METRICS_REPORT_2026-09-07_01-45-44.json` | Report is `stale`; its top-5 hotspots include 4 files (`scripts/finalize_adrs.py`, `scripts/adr_health.py`, `scripts/test_finalize_adrs.py`, `scripts/reconcile_pipeline_state.py`) modified since generation — all barred from TD01 filing until re-verified | Run `/project-metrics` to refresh, or manually re-verify each hotspot against `git log` before filing | user |

#### Suggested (improves but not urgent)
| # | Check | Dimension | Location | Finding | Recommended Action | Owner |
|---|-------|-----------|----------|---------|---------------------|-------|
| 1 | F01 | Freshness | 38 findings across `skills/*/SKILL.md` | Dangling file-path mentions in skill prose (e.g., `skills/adapt-claude-to-agents/SKILL.md:scripts/claude_to_agents.py`, `skills/agent-evals/SKILL.md:scripts/append_eval_log.py`, `skills/neo-cloud-abstraction/SKILL.md:.ai-state/neo_cloud_backend.yaml`) — a declared-limit detector, some are accepted false positives | Spot-check the named samples; correct genuine drift | context-engineer |
| 2 | F02 | Freshness | `skills/test-coverage/SKILL.md` | References `references/go.md`, which does not exist under that skill | Remove the reference or add the file | context-engineer |
| 3 | F07 | Freshness | 9 findings, e.g. `experiment-tracking::Tool Ecosystem`, `llm-prompt-engineering::Model Family Matrix` | Cataloged staleness-sensitive sections carry no `<!-- last-verified: -->` marker (cold-start pattern per catalog's documented semantics) | Backfill markers on next touch of each skill | context-engineer |
| 4 | F08 | Freshness | 33 findings, e.g. `agentic-interface-design::The Token Economy Problem` (125d), `agentic-sdks::Tool Integration Patterns` (136d) | Staleness markers past the 120-day threshold | Re-verify and refresh markers | context-engineer |
| 5 | N01 | Consistency | `skills/agentic-sdks` | Skill dir reads as a crafting-shaped skill but lacks the `-crafting` suffix | Judgment call — likely intentional (SDK usage, not artifact crafting); confirm and no-op, or rename | context-engineer |
| 6 | T01 | Token Efficiency | `skills/skill-crafting/SKILL.md` | 418 lines, at/past the 400-line warn ceiling | Consider splitting core vs. `references/` | context-engineer |
| 7 | T03 | Token Efficiency | `agents/implementation-planner.md` (436), `agents/sentinel.md` (694, derived ceiling 654/754), `agents/systems-architect.md` (444) | At/past warn ceilings (advisory, non-blocking) | Monitor; no action required unless growth continues | context-engineer |
| 8 | CA02 | Calibration Accuracy | `.ai-state/calibration_log.md` | Under-calibrated share is 35% of last 20 classified rows (threshold 25%) | Review recent Retrospective classifications for systematic under-calibration | user |
| 9 | P08 | Pipeline Discipline | `.ai-work/` | 3 stale SAFE task directories idle ≥14 days | Run `/clean-work` | user |
| 10 | SH01 (WARN tier) | Spec Health | `.ai-state/specs/SPEC_diagrams_2026-04-30.md` | 40 additional WARN-tier path-column findings beyond the 2 FAIL above | Same remediation as Important #2, lower urgency | context-engineer |

### Pipeline Discipline

P03: 20,905 WAL records examined across 43 sessions (in-flight session `ff8bda27-...` excluded). 1 unpaired `agent_start` found (session `9b74ea5c-184d-46ba-be5c-cd731a06fb8e`, `agent_type=praxion:verifier`) — this is 1 incident out of 43 sessions, not a fleet-wide pattern; no `no_tool_use` spawns flagged. P04: not run this run (LLM-judgment batch over `observations.jsonl` grouped by `agent_id` was not performed — Pass 2 narrowing, see Depth Disclosure). P05: not run this run (handoff-doc substance judgment across `.ai-work/` was not performed). P06/P07/P08: family-dispatch clean except P08's 3-stale-dir advisory (above).

### Tech-Debt Findings

0 new `td-NNN` rows filed this run. TD06 flags the only available metrics report (`METRICS_REPORT_2026-09-07_01-45-44.json`) as `stale`, with its top-5 hotspots including 4 files modified since generation — per the catalog's TD01 gating rule, every one of those candidates is barred from filing until re-verified against current source, and this run did not perform that per-file `git log` re-verification (time/effort tradeoff — see Depth Disclosure). TD02/TD03/TD04 were not evaluated this run (would require reading the full `METRICS_REPORT_*.md` hotspots/coverage/complexity sections, not just the freshness digest). TD05 (ledger status-update discipline) was not run this run. **Recommendation: refresh metrics (`/project-metrics`) before the next sentinel pass so TD01-TD04 can file against current data.**

### Recommended Actions (prioritized)

1. Refresh `.ai-state/metrics_reports/` via `/project-metrics` — unblocks TD01-TD04 filing and removes the TD06 staleness flag (Important #6). Owner: user.
2. Investigate the unstopped `praxion:verifier` session `9b74ea5c-...` (Important #4) — confirm whether it completed or is a genuine hang. Owner: user.
3. Append the 2 missing calibration-log rows (Important #3). Owner: user / next task-completing agent.
4. Systems-architect judgment on advancing the `DESIGN.md` checkpoint past `dec-376` (Important #5). Owner: systems-architect.
5. Context-engineer pass over `docs/architecture.md` §3a Key Files (Important #1) and the archived diagrams spec (Important #2 + Suggested #10). Owner: context-engineer.
6. Run `/clean-work` (Suggested #9). Owner: user.
7. Backfill/refresh staleness markers per F07/F08 (Suggested #3, #4) on next touch of the named skills. Owner: context-engineer.
8. Schedule a full-corpus Pass-2 sweep (all SKILL.md, agent, rule files) on a future sentinel run — this run's LLM-judgment coverage was narrowed for turn budget (see Depth Disclosure); no per-artifact scorecard or CH01/EC03/EC05/N04-N06/S05-S07/T05-T06/X07-X08 judgment was produced this run. Owner: next sentinel invocation.
