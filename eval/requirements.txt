# Evaluation dependencies (install in the Phoenix venv or a separate venv)
# arize-phoenix>=20.13.0 unlocks Sessions pagination + list-detail session turns
# (v14.6) and the span/trace annotation CLI (v14.5).
arize-phoenix>=20.13.0
arize-phoenix-evals>=3.8.0
openai
