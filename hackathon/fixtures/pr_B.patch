--- a/cache.py
+++ b/cache.py
@@ -21,3 +21,12 @@ class Cache:
         if age > CACHE_TTL_SECONDS:
             return None
         return self._store[key]
+
+
+def cache_lookup(key, seen=set()):
+    """Look up key in cache, tracking seen keys.
+
+    WARNING: mutable default argument -- seen is shared across all calls.
+    """
+    seen.add(key)
+    return None
