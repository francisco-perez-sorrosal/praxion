| Timestamp | Task | Signals | Recommended Tier | Actual Tier | Source | Retrospective |
|-----------|------|---------|------------------|-------------|--------|----------------|
| 2026-08-06 | sentinel-fanout-audit — re-run the ecosystem audit as a six-agent fan-out after a solo run died at its turn cap, then remediate the Critical, all four Important, and seven Suggested findings | 6 agents (5 collectors + synthesis), 221 collection turns, 516 artifacts, ~126 checks over 22 dimension groups; remediation touched 15 files across agents, rules, commands, skills, scripts, hooks and CI; 8 commits | Full | Full | user-directed (audit requested, then "follow the recommended fixes in the order you prefer"); mode (actual tier cell): agent fan-out for collection, orchestrator-driven remediation | **The fan-out was worth more for independence than for survival.** It was designed to beat a `maxTurns: 100` cap — every lens finished near half its budget, so the cap was never the binding constraint once scope was right. The real yield was lens independence: two lenses disagreed on DL04/DL06 (one read the written contract, the other executed it) and the reconciliation — that the check passes by executor judgment, not by specification — was reachable from neither alone. A collector also caught a `supersedes` typing gap introduced *that same day by the orchestrator commissioning the audit*, which a solo run scoped by that orchestrator would likely have inherited. **Grade moved A→D and the audit got better**, because the sole Critical was in the instrument itself and a solo run structurally cannot report the defect that terminates it. Two things to carry forward: (1) `prune_reports.py` destroyed the same two committed files for the *second* time mid-session, so a known-but-unfixed data-loss bug outranks any Suggested finding — fix bleeding before polish; (2) three findings this session were the same meta-defect wearing different clothes — a gate whose input is wrong (td-123), a gate nothing invokes (td-125), a gate whose verdict is discarded by an exit code (I3) — and `check_gate_liveness.py` caught none of them, because GL04 scopes to `check_*`/`validate_*` scripts and GL05 excludes `hooks/`. The rule is ahead of its enforcement. |
| 2026-08-06 | sentinel-fanout-audit — full ecosystem sweep of the ~126-check catalog, run as an orchestrator-managed 5-lens fan-out after the prior solo sweep died at its turn ceiling | Read-only audit, no code change. ~126 checks / 22 dimension groups / 519 artifacts — a scope the agent's own Turn Budget clause 4 states does not fit one budget. User explicitly requested multi-agent decomposition with reconciliation | Standard | Standard | user request ("execute the sentinel with great care so we don't reach the max turn limits"); mode (recommended tier cell): fan-out sanctioned by the agent definition itself (execution mode: 5 concurrent scoped sentinels + orchestrator synthesis) | Sharding by DIMENSION rather than by artifact was the load-bearing choice: dimensions own their scripts 1:1, so each detector ran exactly once. Turn usage came in at 22-29 of 300 per lens (<10%), so the shard count could halve. Centralizing ledger writes at the orchestrator (lenses propose, orchestrator appends) prevented a concurrent-append race and let dedup run against context no lens held. Two orchestrator verifications changed reported facts: the _git fork is 12 sites not 11, and its timeout split is populational (hooks all safe, scripts mostly not) — a text-range scan gave the wrong answer, AST gave the right one. L5 stalled on an API error but its fragment was complete on disk; re-deriving from ground truth per the completion-handshake rule avoided a needless re-run. |
| 2026-09-08 | process-economy-phase1 / P1.1 — trim interface + agentic skill preloads from implementer, verifier, systems-architect to on-demand loads (Step 1, spawn 1) | 3 agent files, 7 insertions / 6 deletions; roadmap tier Direct; no always-loaded file | Direct | Direct-in-pipeline (1 solo implementer spawn, 15 tool calls, [COMPLETE] within cap; orchestrator gate) | roadmap § 6 P1.1 row; IMPLEMENTATION_PLAN Step 1 | correct — measurable: `skills:` line bytes 144→73 / 118→47 / 119→69; guard greps 1 / 2 / 1 (verifier 2 because both 'injected' sentences were rewritten — plan guard text imprecise); 246 agent-scoped tests green; governed budget 24,542 unchanged by design (agent bodies are outside the 9-file set); projected −36,278 tok/pipeline per roadmap, verified per-spawn only by the eval's token accounting at Step 14. |
| 2026-09-08 | process-economy-phase1 / P1.2 — ADR schema, fragment construction, four relation protocols and finalize prose relocated from rules/swe/adr-conventions.md to adr-authoring-protocols.md; systems-architect + implementation-planner cite it (Step 2, spawn 2) | 1 always-loaded rule, 1 reference, 2 agent lines; roadmap tier Lightweight | Lightweight | Lightweight-in-pipeline (1 implementer spawn shared with Step 3, turn-capped at 85 tool uses with Step 2 complete on disk; orchestrator verified + committed) | roadmap § 6 P1.2; IMPLEMENTATION_PLAN Step 2 | under-calibrated on spawn size (a design-heavy relocation is one step per spawn, not two) — measurable: rule 16,321→8,754 B; governed budget 24,542→22,527 (−2,015 tok, target ≥2,000); 76 finalize tests green; adr_health clean; validate_references --strict clean ×4; architectural test + 4-row relation table + field-name list kept resident (PM-2). |
| 2026-09-08 | process-economy-phase1 — PROGRAMME ROW: Phase 1 of the process-economy roadmap (12 slices P1.1–P1.12, 14 plan steps, 6 merge gates, 1 ADR dec-379) | Full programme executed as Direct/Lightweight slices; 60+ files; always-loaded rules, CLAUDE.md pair, 17 agents, 62 skills, hooks; TASK_BRIEF S1–S13 | Full (as sequenced slices) | Full: planner + 11 implementer spawns (5 turn-capped, work on disk, finished by the orchestrator) + 5 light-reviews (4 real defects found) + 1 verifier + 2 helpers = 20 spawns vs plan 17 | pipeline-explicit (worktree process-economy-phase1; TASK_BRIEF → planner → pre-mortem gate → implementer per slice → light-review on RISKY → gate from main → verifier) | under-calibrated — verifier-written verdict (dec-376): "5 of 11 implementer spawns (45%) turn-capped with work on disk, forcing the orchestrator to finish Steps 2/3, Step 10 bookkeeping and Step 12 batches 4–5 outside the spawn model; 20 actual spawns vs the plan's 17. The tier was right; the execution unit was not." Verifier verdict FAIL (2 convention FAILs: Codex bridge still rendered the deleted guard; ADR retirement protocol unapplied) → both fixed by the orchestrator in f4e29cd2 with td-190/191/192/194 resolved and td-195(a); Key Signals 6 PASS / 7 WARN / 0 FAIL; programme targets 3/3 missed (governed 17,289 vs ≤13,500 — ~49% of the gap is roadmap over-commitment, the slices' own cost lines summed to 15,340; listing 8,043 vs ≤2,000; pipeline −26.5% vs −33%); dec-378 PASS (13 slice rows each with a measured figure + executable guard); judged evals 1509/37/0 and 1512/37/0; full CI suite 4,181 green at 84.8% after the 4 relocated-prose tests were repointed. Open: td-189, td-193, td-195(b); 12 skill-retirement candidates proposed; next-session live checks in the worktree's LIVE_CHECKS.md. **Correction (2026-09-11, td-197):** the 8,043 listing figure above was credited by `measure_token_budget.py` honouring 12 name-only `skillOverrides` the harness ignores (PW-1, inert on two instruments); the uncredited reading on today's corpus is 9,243, so Phase 1's listing reduction is roughly 11,336→9,300, not →8,043 (≈1,300 tokens overstated). The instrument no longer credits overrides. |
| 2026-09-11 | process-economy-p2-residual preflight — Direct batch before the P2.1 residual: td-196 commit-msg AI-trailer gate (scripts/check_commit_msg_authorship.py + canary + pre-commit commit-msg stage), td-197 budget instrument stops crediting inert skillOverrides (baseline flag removed, Phase 1 calibration rows corrected), GL05 dead-gate import guard in check_id_citation_discipline.py | 3 independent single-behaviour fixes, 8 files, no architectural decision; each cites a measured cost and an executable guard (dec-378) | Direct | Direct | orchestrator direct-execution, no agents spawned (one read-only digest helper for the P2.1 LEARNINGS harvest) | correct — measurable: 0 spawns for the fixes, 6 commits 8178505f..HEAD, gate liveness 1→0 findings, the trailer gate rejected a live probe commit, listing reading unchanged at 9,243 (the honest floor; the 7,936 it replaced was credited). Gotcha captured: `git checkout -- <file>` to undo a --ratchet sample append also discarded an unstaged edit to the same file — the git-conventions warning, reproduced. |
| 2026-09-12 | process-economy-p2-residual — P2.1 residual: the remaining script-less sentinel A rows extracted into family scripts under the dec-380 contract (35 of 55 extracted/re-pointed into 11 new family scripts + 2 extensions; 20 recorded leave-as-is by the plan's byte gates); Triangle guard binding rows↔registry↔CHECK_IDS; Phase 3 family-dispatch table replacing per-family sentences; ad-hoc fence retired; byte-ceiling fitness guard | 55 rows, 12 plan steps executed as 10 implementer spawns + 6 light-reviews + verifier; ~25 files; one behavioral ADR (dec-381 re_affirms dec-380); agents/sentinel.md 107,397→107,347 B | Standard | Standard (batched, one script per spawn after Batch B) | pipeline-explicit (worktree process-economy-p2-residual; TASK_BRIEF → researcher ∥ architect → planner → pre-mortem gate (user: go) → implementer per batch → light-review on RISKY → verifier, 1 round + 1b re-check) | under-calibrated — verifier-written verdict (Round 1b, PASS after rework): the tier was right and the contract paid on process cost (19 pipeline spawns for 35 rows = 0.29 implementer spawns per row against a 0.4 target and a 1.5 falsifier; P2.1 was 3.2), but the execution unit was not: 5 of 10 implementer spawns hit the 80-turn cap inside their own verification pass, so the operating split became spawn-authors/orchestrator-verifies-and-commits and the spawn count undercounts work that migrated into the orchestrator's context; two RISKY halves (E2, F1) were not light-reviewed until the verifier flagged it, and both reviews returned real findings (a pre-commit scope blind to 3 of 4 substrates; a YAML-spelling miss); AC-10 was breached at two committed boundaries and a third over-ceiling commit landed during rework, stopping only once an executable guard (fitness/tests/test_sentinel_byte_ceiling.py) existed — dec-378's point, relearned. Measured product outcome, stated honestly: bytes ≈0 net (the byte thesis for ≤100 B rows is falsified; dispatch text eats the savings), sentinel per-run cost ROSE 67→105 tool uses / Phase 3 41→47 calls (n=1 each; the scripts surface ~124 WARN the sentinel then parses and triages), no governed-budget win claimed; what improved is determinism (35 verdicts scripted, canaried, Triangle-bound; full suite 4,463→4,647+ green). Open: the ADR category falsifier text vs precedent (verifier W-8), td-201..td-203. |
| 2026-09-20 | process-economy-p3-6-adopt — per-step mutation sensor, the P3.6 ADOPT follow-up: `scripts/mutation_sensor.py` (stdlib runner encoding the spike's eight-point mutmut recipe; sensor never gate — exit 0 on any survivor count, exit 2 with a closed reason code when a count cannot be trusted), one optional `Mutation:` line in the TEST_RESULTS.md schema with tolerance tests over both code readers, a planner-owned `mutation: on` tag beside `review:`, producer prose in test-engineer/implementer and a verifier Phase 10 disposition with a golden bad-case; td-220 (the second F1-class miss) closed by 21 real-adapter tests measured by the sensor itself (49 → 7 survivors of 107, every remainder proven equivalent or environmental); ADR dec-388 re-affirms dec-378 (no metrics column). The user amended AC-1 from `survivors ≤ 3` to "every survivor carries a proven equivalence-or-environment argument" at the pre-verification checkpoint after the step 10 reviewer showed a fixed count is filesystem-dependent | 14 files, 20 REQs, 4 behaviors, one new component (architectural ADR), 4 agent prompts + 4 skill references touched (context-engineer shadow), two RISKY steps | Standard | Standard (worktree; 1 architect + 1 context-engineer shadow + 1 planner + 6 implementer spawns + 5 test-engineer (RED-first, 1 turn-cap resume) + 1 doc-engineer + 5 light-reviews + 1 verifier with 1 rework round; orchestrator executed the two measurement steps) | recommended | correct — measurable: 20 commits each green on its own tree, final suite 4,799 passed / 2 pre-existing failures (baseline 4,745), coverage 83.52%, budget unchanged at 16,606, 20/20 REQs traced; both `review: force` steps returned revise on first pass (step 3: a class-method trampoline shape a function-only validation module never exercised, plus a vacuous-green path; step 10: an "unreachable" survivor argument refuted with a dangling symlink) — the tag paid for itself twice; the verifier's one FAIL (self-heal ran before the concurrency guard) was reproduced before rework and closed on discrimination against the pre-fix module. Misses: one implementer shipped 15 REQ citations into code despite instruction (stripped by the orchestrator before commit — the id gate caught it); the plan's fixed survivor threshold was a category error caught only at review; the runner ended at 803 lines, over the 800 ceiling (td-225). Process: the review loop was budgeted by finding class and stopped when only boundary-message WARNs remained; the WAL-drift stash hazard was avoided by never committing while a subagent held tracked edits |
| 2026-09-22 | process-economy-p3-2-adopt — Workflow-encoded lens fan-out as a user-invocable `lens-fanout` skill (`Workflow` script: N isolated Collect closures + one Reconcile aggregator, pointer return) + per-run cost reader `workflow_run_cost.py` + isolation guard `check_lens_isolation.py` over the shared resolver `_workflow_run.py`; three prose seams (453 B; always-loaded unchanged at 16,606) and catalog rows; ADR dec-389 re-affirms dec-378; live dogfood `wf_0a8b3a0a-cc6` user-invoked from a `--plugin-dir <worktree>` session (S1 delta 1,393 tokens for a 1,158 B return; S2 four rows all `agree`, helper rows windowed; S3 exit 0, `--include-main` reporting pointers and no summary payload; S4 handshake preserved) | 17 files, 13 REQs, 4 behaviors + 4 measurements, one new component (architectural ADR), 3 prose seams (context-engineer shadow), 2 RISKY steps, one user-only dogfood step | Standard | Standard (worktree; 1 architect + 1 context-engineer shadow + 1 planner + 3 implementer + 2 test-engineer (RED-first) + 1 doc-engineer + 1 light-review (revise → fix → accept via SendMessage) + 1 verifier with 1 rework round resolved in place + 1 re-review resume; 3 Direct orchestrator fixes after the checkpoint) | recommended | under-calibrated — the verifier's verdict (medium confidence), reproduced from the diff: 17 files (the selector routes 9+ to Full), 2,821 inserted lines, a parallel batch (Steps 1/5/6) plus doc-engineer and context-engineer shadowing — Full-shaped execution under a Standard label; consequence benign, no mid-task re-tier, the machinery was used without the name. Measurable: 19 branch commits each green on its own tree; final suite 4,843 passed / 2 pre-existing (baseline 4,803), coverage 83.63%; budget unchanged at 16,606; 13/13 REQs traced. Misses: both instruments were `run-empty` on the first live run (fixtures keyed the journal by `event`, the harness writes `type`) and the verifier's FAIL-2 sat exactly where the mutation sensor had pointed and been argued away — two fixture-mirrors-assumption defects in one pipeline, both closed with verbatim-shaped RED tests; the user's typed arguments never reached the skill body (no `$ARGUMENTS`, td-233, ecosystem-wide). Open: td-226, td-232..236. |
